---
phase: 7
plan: 7
title: Terminal MudBlazor strip + sandbox cleanup
requirements: MIG-01, MIG-02, MIG-03
wave: 7
gap_closure: false
---

# Plan 07-07: Terminal MudBlazor strip + sandbox cleanup

## Goal
Delete the `MudBlazor` and `MudBlazor.Services` package references. Remove all remaining MudBlazor wiring. Delete the `/design-sandbox` route. The `dotnet build` succeeds with zero MudBlazor in the dependency graph; a repo-wide `grep "Mud[A-Z]"` in `src/CookBot.Web/` returns zero hits.

This is the LAST plan in v1.2. After it ships, the milestone is shippable.

## Pre-flight checks
Before starting, run:
```bash
grep -rn "Mud[A-Z]" src/CookBot.Web/ --include="*.razor" --include="*.cs" --include="*.razor.cs"
```

If any hits remain (excluding comments / strings deliberately referring to MudBlazor as documentation), STOP and fix them in the appropriate prior plan. Plans 07-01..07-05 should have eliminated all `Mud*` references in their respective surfaces and dialogs.

## Tasks

### Task 1: MainLayout.razor cleanup (MIG-03 partial)
- Remove `<MudThemeProvider>`, `<MudPopoverProvider>`, `<MudDialogProvider>`, `<MudSnackbarProvider>` from the layout
- Remove `_theme` field and the entire `MudTheme` constructor (no longer needed; tokens live in `cookbot-design.css`)
- The `_isDarkMode` field stays (it drives `body.dark-mode` class via existing JS interop) — only the `<MudThemeProvider IsDarkMode="...">` binding goes
- Remove any remaining `using MudBlazor;` directives in `.razor.cs` code-behinds

### Task 2: App.razor cleanup
- Remove `<link rel="stylesheet" href="_content/MudBlazor/MudBlazor.min.css" />` and any related MudBlazor CSS link
- Remove `<script src="_content/MudBlazor/MudBlazor.min.js"></script>`

### Task 3: Program.cs cleanup (MIG-01)
- Remove `using MudBlazor.Services;`
- Remove `builder.Services.AddMudServices(...)` call

### Task 4: _Imports.razor cleanup (MIG-01)
- Remove `@using MudBlazor`
- Remove any `@using MudBlazor.*` namespaces

### Task 5: csproj cleanup (MIG-01)
- In `src/CookBot.Web/CookBot.Web.csproj`, delete the `<PackageReference Include="MudBlazor" ... />` line and any related package references

### Task 6: Sandbox cleanup (per Phase 5 D-27 + Phase 7 D-16)
- Delete `Components/Pages/DesignSandbox.razor`
- Verify no other code references the `/design-sandbox` route

### Task 7: Verify (MIG-02)
- Run `grep -rn "Mud[A-Z]" src/CookBot.Web/ --include="*.razor" --include="*.cs"` — must return ZERO hits
- `dotnet restore` → no MudBlazor download
- `dotnet build` → clean (0 errors, 0 warnings)
- `dotnet test --filter "Category!=RequiresApiKey"` → 196/196 baseline
- Manual smoke pass description: walk through every route — Home / Cookbooks list+detail / Pantry / Grocery / AI Chat / Prompt Builder / Recipe View / Recipe Editor / Cooking Mode / Profile — and verify each renders + functions correctly in light + dark mode

### Task 8: Commit + Phase 7 SUMMARY + milestone-ready state
- Single atomic commit for the strip
- Write 07-07-SUMMARY.md detailing the strip
- Write 07-PHASE-SUMMARY.md aggregating all 7 Phase 7 plans
- Write v1.2-MILESTONE-SUMMARY.md aggregating all 3 v1.2 phases (Foundation / Marquee / Remaining)
- Update STATE.md: Phase 7 complete, milestone v1.2 complete, ready for `/gsd-audit-milestone` + `/gsd-complete-milestone`
- Commit doc/state updates

## Files affected
- MODIFIED: `src/CookBot.Web/Components/Layout/MainLayout.razor`
- MODIFIED: `src/CookBot.Web/Components/App.razor`
- MODIFIED: `src/CookBot.Web/Program.cs`
- MODIFIED: `src/CookBot.Web/Components/_Imports.razor`
- MODIFIED: `src/CookBot.Web/CookBot.Web.csproj`
- DELETED: `src/CookBot.Web/Components/Pages/DesignSandbox.razor`

## Acceptance criteria
- MIG-01..03 satisfied per ROADMAP.md SC#5
- `grep -rn "Mud[A-Z]" src/CookBot.Web/ --include="*.razor" --include="*.cs"` returns 0 hits
- `dotnet build` clean
- `dotnet test --filter "Category!=RequiresApiKey"` 196/196 baseline preserved
- Existing dark-mode toggle, user-switcher with password prompt, admin "Manage users", session-scoped current user, AI-off per-user kill switch, browser notifications in cooking mode, JS interop in cooking-timers.js + recipe-chip-composer.js — ALL functioning as before

## Hard invariants
- This plan deletes the final MudBlazor coupling — there is no rollback within v1.2 (a future plan to re-add MudBlazor would be a fresh dependency add)
