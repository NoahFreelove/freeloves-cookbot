---
phase: 7
plan: 4
title: AI Chat + Prompt Builder
requirements: AIC-01, AIC-02, AIC-03, AIC-04, AIC-05, PB-01, PB-02, PB-03
wave: 4
gap_closure: false
---

# Plan 07-04: AI Chat + Prompt Builder

## Goal
Rewrite `AiChat.razor` and `PromptBuilder.razor` against Phase 5 atoms per `screens/ai-chat.jsx` + `screens/prompt-builder.jsx`. Preserve all v1.1 Phase 2 wiring — `IAiRecipeGenerator` orchestrator, structured-output, repair loop, prompt-injection guard, secret redaction.

## Tasks

### Task 1: AiChat.razor (AIC-01..05)
- Two-column layout: 380px left chat rail (paper-2 bg) + flex right canvas
- **Left rail** (AIC-02, AIC-03):
  - TopBar shows "AI Assistant" title + breadcrumb "generate" + sub "claude haiku 4.5 · streaming"
  - Message stream with eyebrow timestamps; user messages in white card with line border; assistant messages with accent "CookBot" label and animated streaming caret on the active turn
  - Chat input at bottom: bordered card with placeholder + suggestion chips ("make spicier" / "half it" / "vegan") + send button (accent circle 30×30)
- **Right canvas** (AIC-04, AIC-05):
  - Save bar at top: "drafting · 87%" `<CbChip>` with accent-soft bg + pulse animation; Copy JSON `CbButton ghost` + Save to cookbook `CbButton accent`
  - Streaming recipe card (`<CbCard>` 40px padding): eyebrow ("Weeknight · 28 min · serves 4") + 44px display title + lead paragraph + 2-col ingredients/method
  - Active step has accent-soft numbered circle + trailing animated blink caret
- Streaming animations: CSS keyframes `@keyframes cb-blink { 50% { opacity: 0; } }` and `@keyframes cb-pulse` already in `cookbot-design.css` from earlier plans (or add if not)
- Recipe canvas pulls from canonical `RecipeDocument` produced by `IAiRecipeGenerator` (v1.1 Phase 2). The legacy three-tier `AiChat.ExtractRecipeContent` extractor stays deleted (POLISH-01 invariant from v1.1 — preserved).

### Task 2: PromptBuilder.razor (PB-01..03)
- Two-column layout: 320px config rail + flex preview, 32px gap
- **Config rail** (`<CbCard>` per group):
  - Output format: `<CbRadio>` group (Canonical JSON / Markdown / Plain text)
  - Include: `<CbCheckbox>` group (Pantry context / Dietary preferences / Equipment list / Past favorites)
  - Voice: `<CbSelect>` (Neutral · concise / Warm · home cook / Technical · pro kitchen)
- **Preview**:
  - Header: eyebrow "Generated prompt" + char/token counter (tabular numeral, right-aligned)
  - Dark mono `<pre class="mono">` panel: bg `var(--ink)`, color `var(--cream)`, 28px padding, 14px radius, 12.5px font, 1.65 line-height, white-space pre-wrap
  - Substituted sections (PANTRY, DIETARY) highlighted in accent-soft inline span
  - Top-bar action: Copy prompt `CbButton accent`
- Sources from `RecipeSchemaDocumentationProvider` (v1.1 AI-05) — preserve

### Task 3: Build verify + commit
- `dotnet build` clean
- `dotnet test` baseline (the AI-related fixture tests at `Category=RequiresApiKey` continue to be excluded by default)
- Atomic commit

## Acceptance criteria
- AIC-01..05, PB-01..03 satisfied per ROADMAP.md SC#3
- Zero `Mud*` in AiChat.razor or PromptBuilder.razor
- Recipe canvas consumes `RecipeDocument` from `IAiRecipeGenerator` orchestrator (no three-tier extractor revival)
- `dotnet build` clean; `dotnet test` baseline
