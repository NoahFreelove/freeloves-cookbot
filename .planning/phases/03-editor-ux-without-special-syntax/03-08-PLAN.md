---
phase: 03-editor-ux-without-special-syntax
plan: 08
type: execute
wave: 2
depends_on: [03-05, 03-06]
files_modified:
  - .planning/phases/03-editor-ux-without-special-syntax/03-VERIFICATION.md
  - .planning/STATE.md
autonomous: false
gap_closure: true
requirements: [EDITOR-07]
tags: [docs, manual-verify, uat, accessibility-checklist, gap-closure, checkpoint]

must_haves:
  truths:
    - "The 03-VERIFICATION.md original auto-approval log is REDACTED (its verification claim is functionally retracted) — the 9 smoke items are explicitly RE-OPENED post-WR-01/WR-03 fixes and a future skim-reader of the file sees the redaction marker rather than a surviving verification claim"
    - "Three new gap-fix-specific verification items (10, 11, 12) appear at the bottom of 03-VERIFICATION.md with concrete browser steps for the WR-01 typing flow, the IN-03 Backspace semantic, and the WR-03 sidebar highlight"
    - ".planning/STATE.md surfaces the outstanding manual-smoke UAT under a new 'Outstanding UAT' section so /gsd-progress flags it on the next workflow check"
    - "The plan reaches the human-verify checkpoint and STOPS — it does NOT auto-approve. /gsd-execute-phase --gaps-only running this plan must pause for the developer to either walk the 12-item checklist or explicitly defer it again with a documented reason"
    - "The `approved` resume signal is gated on every checkbox in items 1-12 being marked `- [x]` — zero `- [ ]` unchecked-box markdown lines may remain in 03-VERIFICATION.md before the orchestrator may honor `approved`. A bare sign-off line with unchecked items is NOT a valid `approved` state and must be rejected."
  artifacts:
    - path: .planning/phases/03-editor-ux-without-special-syntax/03-VERIFICATION.md
      provides: "Re-opened smoke checklist with REDACTED original auto-approval block, three new gap-fix-specific verification items appended, updated post-gap-closure sign-off block"
      contains: "REDACTED"
    - path: .planning/STATE.md
      provides: "Outstanding UAT entry referencing 03-VERIFICATION.md"
      contains: "Outstanding UAT"
  key_links:
    - from: ".planning/STATE.md (Outstanding UAT section)"
      to: ".planning/phases/03-editor-ux-without-special-syntax/03-VERIFICATION.md"
      via: "Markdown reference in Outstanding UAT entry"
      pattern: "03-VERIFICATION.md"
---

<objective>
Fix Gap 2 — re-open the EDITOR-07 manual a11y smoke checklist that was auto-approved in Plan 04 without being walked. The 9 items in `03-VERIFICATION.md` cannot be programmatically verified (no axe-core / Playwright / Lighthouse infra in this codebase, and CONTEXT.md / RESEARCH.md explicitly defer that infra to a separate milestone). Per the gap-closure constraint guidance, recommendation (b) — flip the auto-approval back to require a real walk — is the chosen path.

Purpose: Restore honesty to the EDITOR-07 verification gate. The auto-approval log already states "the 9 smoke items above were NOT manually walked" — this plan operationalizes that admission by:
1. **Redacting the original auto-approval log block** so a future skim-reader does not see a surviving verification claim from the original auto-approve. The block's `Verified by:` line is replaced with a redaction stub pointing forward to the post-gap-closure sign-off; the block's existence as historical pointer is preserved, but its verification claim is functionally retracted (not merely annotated below).
2. Appending three new gap-fix-specific verification items derived from `03-GOAL-VERIFICATION.md` `human_verification:`.
3. Surfacing the outstanding UAT in `STATE.md` so `/gsd-progress` flags it on the next workflow check.
4. Reaching a `checkpoint:human-verify` and STOPPING — the plan does NOT auto-approve. The `approved` resume signal is gated on every checkbox being marked, not just on a sign-off line being added.

Output: An updated `03-VERIFICATION.md` whose original auto-approval block has been redacted (verification claim retracted) and which the user can walk after the gap plans land, plus a `STATE.md` Outstanding UAT entry that will surface in `/gsd-progress` until the walk completes. Plus a checkpoint that pauses execution for developer decision and gates `approved` on real checkbox marks.

Why depends on 03-05 and 03-06: The smoke walk verifies post-fix behavior. If 03-05 (chip-composer typing) and 03-06 (cooking-mode highlight) haven't landed, the walk would fail on items 2, 4, 5, 9, and the sidebar-highlight item — not because the smoke checklist is broken but because the underlying fixes aren't in place. Sequencing this in Wave 2 ensures the developer walks code that actually has the fixes.

Why NOT depend on 03-07: 03-07 deletes a dead service with no UI surface. The smoke checklist doesn't exercise it; sequencing is unnecessary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/phases/03-editor-ux-without-special-syntax/03-CONTEXT.md
@.planning/phases/03-editor-ux-without-special-syntax/03-VERIFICATION.md
@.planning/phases/03-editor-ux-without-special-syntax/03-GOAL-VERIFICATION.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Redact original auto-approval, re-open the smoke checklist, and append gap-fix verification items</name>
  <read_first>
    - .planning/phases/03-editor-ux-without-special-syntax/03-VERIFICATION.md (FULL file — 95 lines; the existing structure has 9 smoke items + a sign-off section + an auto-approval log at lines ~88-95. The original 9 smoke items and their checkboxes are preserved unchanged. The auto-approval log block at lines ~88-95 is REDACTED in place. New content is appended below.)
    - .planning/phases/03-editor-ux-without-special-syntax/03-GOAL-VERIFICATION.md (the `human_verification:` block — three numbered tests that should become the new gap-fix items at the bottom of the checklist)
    - .planning/phases/03-editor-ux-without-special-syntax/03-05-SUMMARY.md (if 03-05 has executed before this plan — confirm WR-01 / IN-03 fix landed cleanly so the smoke walk will exercise working code)
    - .planning/phases/03-editor-ux-without-special-syntax/03-06-SUMMARY.md (if 03-06 has executed — confirm WR-03 fix landed)
  </read_first>
  <files>.planning/phases/03-editor-ux-without-special-syntax/03-VERIFICATION.md</files>
  <action>
    Modify `.planning/phases/03-editor-ux-without-special-syntax/03-VERIFICATION.md`. One in-place redaction of the original auto-approval block, plus appending new content. The original 9 smoke items and their `- [ ]` checkboxes are preserved unchanged.

    **Step A — Redact the original auto-approval log block IN PLACE (do NOT just annotate below it):**

    Locate the existing block at lines ~88-95 of `03-VERIFICATION.md`:

    ```
    ---

    ### Auto-approval log (2026-04-26)

    Verified by: auto-approved via `/gsd-execute-phase 03 --auto` (user confirmed)
    Date: 2026-04-26
    Notes: Checkpoint auto-approved by the user under `workflow.auto_advance: true`. **The 9 smoke items above were NOT manually walked.** This file remains the canonical reference — re-running the checklist in a real browser is recommended before shipping. The phase is marked complete on the strength of the 185 automated tests and Plans 01–04 atomic commits; the manual a11y / browser-degradation / IME items above are outstanding UAT and will surface in `/gsd-progress` until manually walked.
    ```

    Replace the block (lines beginning at `### Auto-approval log (2026-04-26)` through the end of the `Notes:` paragraph) with this redaction stub — preserving the `---` separator above:

    ```markdown
    ---

    ### Auto-approval log (2026-04-26) — REDACTED

    [Original auto-approval block redacted by gap-closure plan 03-08 on 2026-04-26; the auto-approval's verification claim is retracted. The 9 smoke items above were NOT manually walked at the time of that auto-approval, and the surface has materially changed since (WR-01 / IN-03 / WR-03 fixes in gap-closure plans 03-05 and 03-06). The binding sign-off lives in the "Re-opened (post-gap-closure)" section below.]
    ```

    The redaction stub MUST contain the literal token `REDACTED` (so the acceptance gate can detect it) AND it MUST replace the original `Verified by: auto-approved via ...` line entirely (the original verification claim must not survive in the file). The acceptance gates below verify both conditions.

    Rationale: The checker's WARNING 1 noted that an "INVALIDATED below the block" annotation leaves a mixed signal — the file still nominally claimed verification at the original block. An in-place redaction of the `Verified by:` line is the only durable fix.

    **Step B — Append the re-opened section, three new items, and post-gap-closure sign-off block at the end of the file:**

    Append the following content at the end of the file (after the redacted auto-approval stub):

    ```markdown

    ## Re-opened (post-gap-closure)

    **Status:** RE-OPENED — the original auto-approval (now redacted above) is invalidated for two compounding reasons:

    1. **The walk never happened.** The original auto-approval log itself recorded this honestly: "the 9 smoke items above were NOT manually walked." That admission is preserved in this re-opened section's audit trail even though the verification claim above has been redacted.
    2. **The surface has materially changed.** Gap-closure plans 03-05 and 03-06 modified the chip composer's contenteditable typing path (WR-01), the Backspace-removes-prior-chip semantic (IN-03), and the cooking-mode sidebar highlight (WR-03). Items 2, 4, 5, 9, and "Cooking-mode chip click → scroll-and-highlight" all touch the changed surface. Even a hypothetical pre-fix walk would not reflect post-fix behavior.

    The 9 items above MUST be re-walked against the post-gap-closure code before EDITOR-07 can be claimed as verified. Three additional items are appended below to cover the specific gap-closure surfaces.

    ## Gap-Closure Verification Items (added by gap-plan 03-08)

    These three items derive from `03-GOAL-VERIFICATION.md` `human_verification:` and cover the specific surfaces touched by gap-closure plans 03-05 (WR-01 + IN-03) and 03-06 (WR-03). Walk all three before signing off EDITOR-07.

    ### Item 10 — End-to-end recipe authoring flow with WR-01 fix (gap-closure 03-05)

    - [ ] Open `./run.sh`. Navigate to `http://localhost:7000`. Sign in. Open a fresh recipe.
    - [ ] In a step's chip composer text segment, type `Bake the ` (note the trailing space). The text must appear character-by-character — no empty-string wipe per keystroke.
    - [ ] Click the `+` "Insert ingredient" button. Select `Salt` from the autocomplete (or any ingredient on the recipe).
    - [ ] After the chip lands, type ` for 5 minutes`. Save the recipe.
    - [ ] Reload the recipe. Open the editor again. Confirm the saved step text reads `Bake the [Salt](#1) for 5 minutes` (or the equivalent with whichever ingredient was chosen — confirm via the D-A4 "View as text" toggle).

    Why this gates EDITOR-01: The chip-composer free-text typing flow is the centerpiece of the phase goal. Pre-WR-01-fix, every keystroke wiped the segment to empty string, making the centerpiece flow non-functional in a real browser circuit even though the `@`-trigger and Insert-button paths produced correct strings via `Simulate*` helpers in CI.

    ### Item 11 — Backspace-removes-prior-chip keyboard semantic (gap-closure 03-05)

    - [ ] In the chip composer, place the caret immediately after a chip (the leftmost position of the segment immediately following the chip). Press `Backspace` once.
    - [ ] The chip is removed. The underlying `[name](#id)` markdown is gone (verify via the D-A4 "View as text" toggle).
    - [ ] Pressing `Backspace` again at the new caret position deletes a single character of the preceding segment (browser-default behavior).
    - [ ] Place the caret 5 characters into a segment that follows a chip. Press `Backspace`. A single character is deleted; the chip is NOT removed.

    Why this gates EDITOR-07: The Backspace-removes-prior-chip semantic was wired but no-op (`OnSegmentKeyDown` placeholder) before gap-closure 03-05. The user-advertised behavior must actually fire.

    ### Item 12 — Cooking-mode sidebar highlight on freshly-saved recipe (gap-closure 03-06)

    - [ ] Save a brand-new recipe with chips referencing 3 ingredients in step 1 (e.g. `Mix [Salt](#1), [Pepper](#2), and [Olive Oil](#3) thoroughly`).
    - [ ] Open cooking mode. Navigate to step 1.
    - [ ] In the ingredients sidebar (right column), the three referenced ingredients (#1, #2, #3) are visually highlighted with the `--mud-palette-primary-lighten` background; non-referenced ones are not.
    - [ ] Navigate to step 2 (which uses different chips). The highlight set updates correctly — the previous step's highlights clear, the new step's highlights light up.

    Why this gates EDITOR-06 SC#4: Pre-WR-03-fix, the sidebar read `CurrentStep.IngredientRefs` which Phase 1 D-13 retired writes to. Every freshly-saved recipe had an empty `IngredientRefs` and no highlights ever fired. The fix re-routes through `IngredientLinkPatterns.Pattern.Matches(CurrentStep.Text)` — exactly the path EDITOR-06 mandates.

    ## Updated Sign-off (post-gap-closure)

    Once **all 12 items above** (items 1-9 original + items 10-12 gap-fix) have every `- [ ]` checkbox marked as `- [x]`, append the post-gap-closure sign-off below. Marking only the sign-off line without checking the items is an invalid sign-off — see the Task 3 acceptance gate, which fails any sign-off attempt while unchecked-box lines remain.

    ```
    Verified by (post-gap-closure walk): <developer name>
    Date: <YYYY-MM-DD>
    Notes: <any deviations or accepted residual issues from items 1-12>
    ```

    Once signed AND all 12 items checked, mark the EDITOR-07 line in `.planning/REQUIREMENTS.md` as `[x]` and clear the Outstanding UAT entry in `.planning/STATE.md`.
    ```

    **Step C — Pin the checkbox count for downstream Task 3 gating (post_task_action):**

    After Steps A and B land, run the following to pin the exact number of `- [ ]` (unchecked-box) markdown lines that the file will contain BEFORE any walk happens:

    ```bash
    UNCHECKED_COUNT=$(grep -c '^- \[ \]' .planning/phases/03-editor-ux-without-special-syntax/03-VERIFICATION.md)
    echo "Pre-walk unchecked-box count: ${UNCHECKED_COUNT}"
    ```

    The expected count is the original 9-items checkbox total (~26 across items 1-9 plus the cooking-mode chip-click sub-block) PLUS items 10-12 (~13 across the three new items) ≈ ~39. Record the exact value in the eventual 03-08-SUMMARY.md so Task 3's reviewer can sanity-check that the developer actually walked everything (the post-walk file should have 0 unchecked boxes, and `(- [x] count) - (pre-walk - [x] count) >= UNCHECKED_COUNT`).

    **Step D — Verify file integrity:**

    `wc -l .planning/phases/03-editor-ux-without-special-syntax/03-VERIFICATION.md` should now report ≥ 130 lines (the original ~95 minus the redacted auto-approval block ~7 lines, plus ~50 new lines for items 10-12 + sign-off). The original 9 items and their `- [ ]` checkboxes are unchanged. The original auto-approval `Verified by:` line is gone (replaced by the redaction stub). The new redaction stub, three new items, and updated sign-off section are present.

    **DO NOT** modify the original 9 smoke items or their checkboxes. The redaction is targeted at the auto-approval block only; the 9-item checklist itself remains canonical and walk-ready.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/freeloves-cookbot &amp;&amp; grep -q "REDACTED" .planning/phases/03-editor-ux-without-special-syntax/03-VERIFICATION.md &amp;&amp; ! grep -B1 "Verified by:" .planning/phases/03-editor-ux-without-special-syntax/03-VERIFICATION.md | grep -q "auto-approved via" &amp;&amp; grep -q "Item 10" .planning/phases/03-editor-ux-without-special-syntax/03-VERIFICATION.md &amp;&amp; grep -q "Item 11" .planning/phases/03-editor-ux-without-special-syntax/03-VERIFICATION.md &amp;&amp; grep -q "Item 12" .planning/phases/03-editor-ux-without-special-syntax/03-VERIFICATION.md &amp;&amp; grep -q "gap-closure 03-05" .planning/phases/03-editor-ux-without-special-syntax/03-VERIFICATION.md &amp;&amp; grep -q "gap-closure 03-06" .planning/phases/03-editor-ux-without-special-syntax/03-VERIFICATION.md &amp;&amp; grep -q "Re-opened (post-gap-closure)" .planning/phases/03-editor-ux-without-special-syntax/03-VERIFICATION.md &amp;&amp; test $(wc -l &lt; .planning/phases/03-editor-ux-without-special-syntax/03-VERIFICATION.md) -ge 125</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "REDACTED" .planning/phases/03-editor-ux-without-special-syntax/03-VERIFICATION.md` exits 0 — the redaction marker is present.
    - `! grep -B1 "Verified by:" .planning/phases/03-editor-ux-without-special-syntax/03-VERIFICATION.md | grep -q "auto-approved via"` exits 0 — no surviving "Verified by" line points at the auto-approval. (Note: a NEW "Verified by (post-gap-closure walk):" line MAY appear later if the developer has walked the checklist; that is fine because its preceding context line is the post-gap-closure sign-off block, not the auto-approval block.)
    - `grep -q "Item 10"`, `grep -q "Item 11"`, `grep -q "Item 12"` all exit 0.
    - `grep -q "gap-closure 03-05"` and `grep -q "gap-closure 03-06"` both exit 0.
    - `grep -q "Re-opened (post-gap-closure)"` exits 0 — the new section is present.
    - Original 9 items still present: `grep -c "^### Item [1-9] —" .planning/phases/03-editor-ux-without-special-syntax/03-VERIFICATION.md` returns ≥ 9.
    - Original 9 items' checkboxes preserved as unchecked: every `- [ ]` line that existed in the original 9-item block still exists post-redaction (the redaction targets only the auto-approval block).
    - Line count ≥ 125 (original ~95 minus ~7 removed lines, plus ~50 added lines for items 10-12 and sign-off).
    - `grep -q "post-gap-closure walk" .planning/phases/03-editor-ux-without-special-syntax/03-VERIFICATION.md` exits 0 — the new sign-off block exists.
    - The pre-walk unchecked-box count from Step C is recorded in 03-08-SUMMARY.md (or in the executor's stdout for reference).
  </acceptance_criteria>
  <done>
    `03-VERIFICATION.md` is updated. The original auto-approval block's `Verified by: auto-approved via ...` line has been replaced with a `REDACTED` stub (functionally retracting the verification claim). A new "Re-opened (post-gap-closure)" section explains why the redaction was necessary and preserves the original honest admission as audit context. Three new gap-fix items (10, 11, 12) cover the WR-01, IN-03, and WR-03 surfaces with concrete browser steps. A new post-gap-closure sign-off block is appended. The pre-walk unchecked-box count is captured for Task 3's gate.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Surface outstanding UAT in STATE.md</name>
  <read_first>
    - .planning/STATE.md (FULL file — note the structure: frontmatter, "Project Reference", "Current Position", "Performance Metrics", "Accumulated Context" with Decisions/Pending Todos/Blockers, "Deferred Items", "Session Continuity")
  </read_first>
  <files>.planning/STATE.md</files>
  <action>
    Modify `.planning/STATE.md` to surface the outstanding manual-smoke UAT. Two changes:

    **Step A — Add a new `## Outstanding UAT` section IMMEDIATELY BEFORE the existing `## Deferred Items` section:**

    The current STATE.md has these top-level sections in order: `## Project Reference`, `## Current Position`, `## Performance Metrics`, `## Accumulated Context`, `## Deferred Items`, `## Session Continuity`. There is no existing "Outstanding UAT" section, so create one. Position it BETWEEN `## Accumulated Context` (which ends after `### Blockers/Concerns` and the EF Core 10 bullet) and `## Deferred Items`.

    Insert this section verbatim:

    ```markdown
    ## Outstanding UAT

    Manual verification items that must close before the corresponding requirement / phase can be marked complete.

    ### EDITOR-07 manual a11y smoke checklist (Phase 3)

    **Status:** Re-opened by gap-closure plan 03-08 (2026-04-26). Originally auto-approved 2026-04-26 without manual walk; the original auto-approval block has been redacted in `03-VERIFICATION.md` and the verification claim retracted following WR-01, IN-03, and WR-03 fixes in gap-closure plans 03-05 and 03-06.

    **Action required:** Walk the 12-item checklist in `.planning/phases/03-editor-ux-without-special-syntax/03-VERIFICATION.md` (items 1-9 original a11y + items 10-12 gap-fix verification) by marking every `- [ ]` checkbox as `- [x]` after performing the step. Append the post-gap-closure sign-off line at the bottom of that file when complete, then remove this entry. The orchestrator's `approved` resume signal for Plan 08 Task 3 is gated on **zero `- [ ]` lines remaining** in 03-VERIFICATION.md — a sign-off line alone is not sufficient.

    **Why human:** No axe-core / Playwright / Lighthouse infra in this codebase per RESEARCH.md / CONTEXT.md (deferred to a future infrastructure milestone). Browser-only behavior (focus order, screen reader announcements, IME composition, color contrast in dark mode, JS-interop blocking, contenteditable real-keystroke flow, sidebar highlight rendering) cannot be programmatically verified.

    **Surfaces in:** `/gsd-progress` flags this UAT entry until the sign-off line is added to `03-VERIFICATION.md` AND every checkbox in items 1-12 is marked, AND this entry is removed from STATE.md.

    **Gates:** EDITOR-07 cannot be marked `[x]` in `.planning/REQUIREMENTS.md` until this UAT closes. Phase 3 cannot be marked complete until this UAT closes.
    ```

    **Step B — Update the "Current Position" section to reflect the gap-closure round:**

    The current "Current Position" block reads (lines ~26-33):
    ```
    Phase: 03 (Editor UX Without Special Syntax) — EXECUTING
    Plan: 1 of 4
    Status: Executing Phase 03
    Last activity: 2026-04-26 -- Phase 03 execution started

    Progress: [██████████] 100%
    ```

    Append (do NOT overwrite the existing block) a single new line at the bottom of "Current Position":

    ```
    Note: Phase 3 gap-closure round in progress (plans 03-05 / 03-06 / 03-07 / 03-08). Phase 3 cannot be marked complete until the EDITOR-07 manual smoke UAT (see Outstanding UAT section below) closes.
    ```

    **Step C — DO NOT TOUCH:**

    - The frontmatter (top of file lines 1-15) — `last_updated`, `progress`, `stopped_at` are mechanically managed by other commands.
    - The `### Decisions` accumulated list — those are locked.
    - The `### Pending Todos` and `### Blockers/Concerns` lists — those are sibling concerns.
    - The `## Deferred Items` table — different concept (long-term roadmap deferrals, not active UAT).
    - The `## Session Continuity` block — last-session metadata.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/freeloves-cookbot &amp;&amp; grep -q "## Outstanding UAT" .planning/STATE.md &amp;&amp; grep -q "EDITOR-07 manual a11y smoke checklist" .planning/STATE.md &amp;&amp; grep -q "03-VERIFICATION.md" .planning/STATE.md &amp;&amp; grep -q "gap-closure" .planning/STATE.md &amp;&amp; grep -q "gap-closure round in progress" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "## Outstanding UAT" .planning/STATE.md` exits 0 — new section exists.
    - `grep -q "EDITOR-07 manual a11y smoke checklist" .planning/STATE.md` exits 0.
    - `grep -q "03-VERIFICATION.md" .planning/STATE.md` exits 0 — link to the checklist file.
    - `grep -q "gap-closure" .planning/STATE.md` exits 0.
    - The new section sits BEFORE `## Deferred Items` and AFTER `## Accumulated Context` — verified by inspecting line order.
    - The frontmatter is unchanged: `head -15 .planning/STATE.md` still shows the original gsd_state_version, milestone, etc.
    - The "Current Position" gap-closure note exists: `grep -q "gap-closure round in progress" .planning/STATE.md` exits 0.
  </acceptance_criteria>
  <done>
    `STATE.md` exposes a new "Outstanding UAT" section that flags the 12-item manual smoke checklist as a phase-blocking item. `/gsd-progress` will surface this entry on the next workflow check until the developer walks the checklist (every checkbox marked) and removes the entry. The "Current Position" block is annotated to reflect the gap-closure round.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify checkpoint — pause for developer to walk OR explicitly defer; `approved` is gated on every checkbox being marked</name>
  <files>(no files modified by this task — checkpoint pauses execution awaiting developer signal)</files>
  <what-built>
    Tasks 1 and 2 have redacted the original auto-approval block in `03-VERIFICATION.md` (its verification claim is now retracted, with `REDACTED` stub in place), added items 10-12, and surfaced an Outstanding UAT entry in `STATE.md`. The 12-item manual smoke checklist is now actionable for a real-browser walk.

    Gap-closure plans 03-05 (WR-01 + IN-03 fixes) and 03-06 (WR-03 fix) have already landed by Wave 1 — the chip-composer typing flow, Backspace-removes-prior-chip semantic, and cooking-mode sidebar highlight are all post-fix and ready to be smoke-walked.

    **CRITICAL — `approved` semantics:** The `approved` resume signal is NOT honored simply because the developer typed it. The orchestrator MUST verify that every `- [ ]` checkbox in items 1-12 of `03-VERIFICATION.md` has been marked as `- [x]` BEFORE accepting `approved`. A bare sign-off line with unchecked items reproduces the original Gap 2 failure mode (verification claim without verification) and must be rejected.
  </what-built>
  <how-to-verify>
    The developer (the user) has two valid choices at this checkpoint. Pick ONE.

    **Option A: Walk the checklist now.**

    1. Run `./run.sh` and navigate to `http://localhost:7000`.
    2. Sign in. Open or create a recipe with at least 3 ingredients and 2 steps.
    3. Walk all 12 items in `.planning/phases/03-editor-ux-without-special-syntax/03-VERIFICATION.md`:
       - Items 1-9: original a11y / browser-degradation smoke checklist (Tab nav, Backspace+Arrow chip semantics, screen reader, `@`-trigger, Step/Section radiogroup, inline timer suggestion, JS-interop-fail fallback via DevTools Request Blocking, color contrast, IME / cooking-mode chip click).
       - Items 10-12: gap-fix verification (WR-01 typing flow end-to-end; IN-03 Backspace-removes-prior-chip; WR-03 cooking-mode sidebar highlight on freshly-saved recipe).
    4. **Mark every `- [ ]` checkbox as `- [x]` as you complete each step.** This is not optional cosmetic bookkeeping — it is the gate the orchestrator uses to honor your `approved` signal. If you leave any box unchecked, `approved` will be rejected and you will be prompted to either complete the remaining items or use `deferred: <reason>` instead.
    5. After all 12 items pass and every checkbox is marked, append the post-gap-closure sign-off line at the bottom of `03-VERIFICATION.md`:
       ```
       Verified by (post-gap-closure walk): <your name>
       Date: <YYYY-MM-DD>
       Notes: <any deviations or accepted residual issues>
       ```
    6. Remove the "EDITOR-07 manual a11y smoke checklist" entry from `STATE.md`'s Outstanding UAT section.
    7. Mark `EDITOR-07` as `[x]` in `.planning/REQUIREMENTS.md`.
    8. Type `approved` to resume — the gap-closure round is complete and Phase 3 can be marked done.

    **Option B: Defer the walk to a later session, with documented reason.**

    1. Append a deferred-walk note to `03-VERIFICATION.md` below the new sign-off block — for example:
       ```
       Walk deferred: <YYYY-MM-DD> — reason: <e.g. "no real browser available in current environment", "blocked on time">. Outstanding UAT remains in STATE.md until walked.
       ```
    2. The Outstanding UAT entry STAYS in STATE.md — `/gsd-progress` will continue to flag it until walked.
    3. EDITOR-07 stays `[ ]` in REQUIREMENTS.md.
    4. Phase 3 stays in "gap-closure complete; UAT pending" status.
    5. Type `deferred` (or describe the deferral reason) to resume — the gap-closure round closes with the UAT explicitly outstanding.

    **Option C (NOT VALID): Auto-approve.**

    Auto-approving this checkpoint would replicate the exact failure mode that created Gap 2 in the first place. If `workflow.auto_advance=true` is set, the executor MUST honor the `gate="blocking"` attribute on this checkpoint and refuse to auto-skip. If somehow auto-approval fires, the next round of `/gsd-verify-work` will surface the same gap again.

    **Option D (NOT VALID): Type `approved` without checking the boxes.**

    The orchestrator's acceptance gate (see `<acceptance_criteria>` below) verifies that zero `- [ ]` unchecked-box lines remain in `03-VERIFICATION.md` before honoring `approved`. A developer who types `approved` and adds a sign-off line but leaves checkboxes unmarked will have the signal rejected. This is a deliberate gate added in revision 2 (after the checker's WARNING 2) to close the back-door reproduction of Gap 2.
  </how-to-verify>
  <action>
    Pause execution and present the user with the two-option decision in `<how-to-verify>` (walk the 12-item checklist now AND mark every checkbox, OR explicitly defer with documented reason). DO NOT auto-approve under any circumstance — this checkpoint exists specifically to prevent the silent auto-approval that created Gap 2.

    Wait for the user's resume signal (`approved` or `deferred: <reason>`).

    **On `approved`:** BEFORE honoring the signal, run the gate from `<acceptance_criteria>`:
    1. `[ "$(grep -c '^- \[ \]' .planning/phases/03-editor-ux-without-special-syntax/03-VERIFICATION.md)" = "0" ]` — zero unchecked-box markdown lines remain.
    2. `grep -q "Verified by (post-gap-closure walk)" .planning/phases/03-editor-ux-without-special-syntax/03-VERIFICATION.md` — the post-gap-closure sign-off line exists.
    3. REQUIREMENTS.md EDITOR-07 line shows `[x]`.
    4. STATE.md Outstanding UAT entry for EDITOR-07 is removed.

    If gate (1) fails, the orchestrator MUST reject `approved` and report which items still have `- [ ]` checkboxes (run `grep -n '^- \[ \]' .planning/phases/03-editor-ux-without-special-syntax/03-VERIFICATION.md` and surface the line numbers / text). The user must either complete the remaining items, or switch to `deferred: <reason>`.

    **On `deferred: <reason>`:** Confirm the deferral note was appended to 03-VERIFICATION.md and the Outstanding UAT entry remains in STATE.md. EDITOR-07 stays `[ ]`. The `<reason>` is recorded verbatim in 03-08-SUMMARY.md.

    The detailed walkthrough steps and option semantics live in `<how-to-verify>` above; this `<action>` block exists to satisfy the plan-structure validator's requirement that every task carry an `<action>` element AND to make the gate enforcement contract explicit.
  </action>
  <verify>
    <automated>echo "checkpoint task — verification is the user's resume signal AND the post-signal acceptance-gate evaluation, not a single automated command. Resume signal must be 'approved' (gated on zero unchecked-box lines) or 'deferred: &lt;reason&gt;' per the resume-signal contract."</automated>
  </verify>
  <acceptance_criteria>
    - The executor pauses at this task and presents the two-option decision to the user.
    - The executor does NOT auto-approve under `workflow.auto_advance=true`. The `gate="blocking"` attribute MUST be honored.
    - **`approved` gate (REVISION 2 — explicit checkbox enforcement):** Before honoring an `approved` signal, ALL of the following MUST hold:
      - **Zero unchecked-box lines:** `[ "$(grep -c '^- \[ \]' .planning/phases/03-editor-ux-without-special-syntax/03-VERIFICATION.md)" = "0" ]` exits 0. Equivalently: `! grep '^- \[ \]' .planning/phases/03-editor-ux-without-special-syntax/03-VERIFICATION.md` exits 0. If this gate fails, the orchestrator MUST reject `approved` and surface the unchecked items via `grep -n '^- \[ \]' .planning/phases/03-editor-ux-without-special-syntax/03-VERIFICATION.md`.
      - **Sign-off line present:** `grep -q "Verified by (post-gap-closure walk)" .planning/phases/03-editor-ux-without-special-syntax/03-VERIFICATION.md` exits 0.
      - **REQUIREMENTS.md updated:** the EDITOR-07 line shows `[x]` (i.e. `grep -E '^- \[x\] EDITOR-07' .planning/REQUIREMENTS.md` exits 0, or equivalent for whatever exact format that file uses).
      - **STATE.md Outstanding UAT cleared:** `! grep -q "EDITOR-07 manual a11y smoke checklist" .planning/STATE.md` exits 0.
    - **`deferred: <reason>` gate:** `grep -q "Walk deferred:" .planning/phases/03-editor-ux-without-special-syntax/03-VERIFICATION.md` exits 0; STATE.md Outstanding UAT entry STAYS; REQUIREMENTS.md EDITOR-07 stays `[ ]`.
    - In either case, the resume signal verbatim is recorded in 03-08-SUMMARY.md.
    - **Optional sanity check:** the post-walk `- [x]` count is at least the pre-walk `- [ ]` count pinned in Task 1 Step C (i.e. the developer actually flipped the boxes rather than deleting them). This is captured in 03-08-SUMMARY.md but is not strictly required for the gate (the zero-`- [ ]` gate is sufficient by itself — the simpler form per the checker's WARNING 2 guidance).
  </acceptance_criteria>
  <done>
    The checkpoint resolved with one of the two valid options (walked-and-checked-all-boxes, or deferred). The plan does NOT close silently. The `approved` signal is rejected if any checkbox is unmarked, closing the back-door reproduction of Gap 2 that the checker's WARNING 2 surfaced. Gap 2 is fully closed at the planning layer; Gap 2 closes at the verification layer when (and only when) the developer walks the checklist, marks every box, and signs off.
  </done>
  <resume-signal>
    Type `approved` (after walking, marking every `- [ ]` as `- [x]`, AND adding the sign-off line) OR `deferred: <reason>` (after deferring with documented reason) to resume.

    Note: `approved` is gated on every checkbox in items 1-12 being marked. A bare `approved` with unchecked items will be rejected by the orchestrator's acceptance gate; you will be prompted to either complete the remaining items or switch to `deferred: <reason>`.

    Do NOT type a generic "ok" or "skip" — the response must be one of the two explicit options above so the gap-closure history records what was decided.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

(No new trust boundaries — this plan modifies documentation files only. No code surfaces, no data flows.)

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03P08-01 | Repudiation | Verification gate honesty | mitigate | The original auto-approval log block is redacted IN PLACE — its `Verified by: auto-approved via ...` line is replaced with a `REDACTED` stub that points forward to the post-gap-closure sign-off. The block's existence as a historical pointer is preserved (so audit readers can see that an auto-approval happened and was retracted), but its verification claim is functionally retracted (so skim-readers do not see a surviving verification claim). The new "Re-opened (post-gap-closure)" section preserves the original honest admission ("the 9 smoke items above were NOT manually walked") as audit context. No history is rewritten silently; the redaction is explicit and dated. |
| T-03P08-02 | Tampering | STATE.md frontmatter / locked decisions | mitigate | The action explicitly forbids touching the frontmatter, the Decisions list, or the Deferred Items table. The new section is purely additive. |
| T-03P08-03 | Elevation of Privilege | Auto-approval bypass | mitigate | The checkpoint is `gate="blocking"`. Even under `workflow.auto_advance=true`, blocking checkpoints must not auto-fire. If they do, the next `/gsd-verify-work` round re-surfaces the gap. The plan-level honesty is enforced by the verifier, not just by orchestrator courtesy. |
| T-03P08-04 | Repudiation | `approved` signal without verification | mitigate | The `approved` resume signal is gated on `grep -c '^- \[ \]' 03-VERIFICATION.md` returning 0. A developer who types `approved` while leaving checkboxes unchecked has the signal rejected — closing the back-door reproduction of Gap 2 (signing off without walking). The simpler "no unchecked boxes" form is used per the checker's WARNING 2 guidance. |

</threat_model>

<verification>
After all three tasks land (Task 3 reaches a paused checkpoint, not "completes"):

1. **`03-VERIFICATION.md` updated:** All grep gates from Task 1 acceptance criteria pass — including the `REDACTED` marker present and no surviving `Verified by: auto-approved via ...` line.
2. **`STATE.md` updated:** All grep gates from Task 2 acceptance criteria pass.
3. **Checkpoint reached:** The executor pauses at Task 3 awaiting the user's `approved` or `deferred` signal. The plan does NOT auto-approve.
4. **`approved` is gated on real walk:** If the user types `approved`, the orchestrator MUST verify zero `- [ ]` lines remain in `03-VERIFICATION.md` before honoring the signal. A bare `approved` with unchecked boxes is rejected.
5. **No code modified:** `git diff src/ tests/` is empty for this plan — only `.planning/` files changed.
6. **`/gsd-progress` surfacing:** After this plan lands and the developer continues their workflow, the next `/gsd-progress` invocation will show "Outstanding UAT: EDITOR-07 manual a11y smoke checklist" as a phase-blocking item until the checklist closes (every box marked AND sign-off appended).
</verification>

<success_criteria>
- `03-VERIFICATION.md` is updated: the original auto-approval `Verified by: auto-approved via ...` line is REDACTED in place; items 10-12 are appended; the post-gap-closure sign-off block is added.
- `STATE.md` exposes a new "Outstanding UAT" section that lists the EDITOR-07 manual smoke checklist as a phase-blocking item.
- The plan reaches a `checkpoint:human-verify` and pauses — does NOT auto-approve.
- The `approved` resume signal is gated on zero unchecked-box lines remaining in `03-VERIFICATION.md` (closing the back-door Gap 2 reproduction).
- Gap 2 is closed at the planning layer (the smoke checklist is operationally re-opened and the original verification claim is retracted, not merely annotated); the actual walk closes Gap 2 at the verification layer (separate developer action with checkbox enforcement).
- No code or test files are modified.
</success_criteria>

<output>
After completion (post-checkpoint resolution), create `.planning/phases/03-editor-ux-without-special-syntax/03-08-SUMMARY.md` documenting:
- The two file modifications (03-VERIFICATION.md, STATE.md) — describe the in-place redaction of the original auto-approval block, the appended items 10-12, the new Outstanding UAT section in STATE.md.
- The pre-walk unchecked-box count from Task 1 Step C (so future readers can sanity-check the post-walk file).
- The checkpoint resolution: which option (A or B) the developer chose, with the resume-signal verbatim.
- If Option A (walk): list any deviations the developer noted and where they were documented; confirm the `approved` gate fired (zero `- [ ]` lines confirmed); confirm REQUIREMENTS.md EDITOR-07 was marked `[x]` and STATE.md Outstanding UAT entry was removed.
- If Option B (defer): document the deferral reason and confirm the Outstanding UAT entry remains; note that EDITOR-07 stays `[ ]` until the walk happens in a later session.
- Note the meta-lesson: this gap-closure round (revised after checker WARNING 1 + WARNING 2) added (a) an in-place redaction of the original auto-approval block (not just an "INVALIDATED" annotation below it) and (b) an explicit `- [ ]`-count gate on the `approved` signal — patterns any future EDITOR-07-style manual-only verification should follow to prevent both the silent auto-approval failure mode AND the back-door "approved without walking" failure mode.
</output>
</tasks>
</content>
</invoke>