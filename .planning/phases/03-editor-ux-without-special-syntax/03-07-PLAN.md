---
phase: 03-editor-ux-without-special-syntax
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/CookBot.Application/Services/IngredientRefDetectionService.cs
  - tests/CookBot.Tests/Services/IngredientRefDetectionServiceTests.cs
autonomous: true
gap_closure: true
requirements: [EDITOR-04, EDITOR-06]
tags: [refactor, regex-consolidation, dead-code-removal, gap-closure]

must_haves:
  truths:
    - "There is exactly ONE [name](#id) regex definition in the production codebase: IngredientLinkPatterns.Pattern in src/CookBot.Application/Recipes/IngredientLinkPatterns.cs"
    - "IngredientRefDetectionService.cs is deleted from src/CookBot.Application/Services/ — no production caller exists, the duplicate MarkdownLinkPattern (with `+` vs canonical `*`) is gone with it"
    - "tests/CookBot.Tests/Services/IngredientRefDetectionServiceTests.cs is deleted — the test class only exercised the deleted service"
    - "Solution builds green and full test suite passes (down by 3 tests — the deleted IngredientRefDetectionServiceTests file held 3 [Fact]s)"
  artifacts:
    - path: src/CookBot.Application/Services/IngredientRefDetectionService.cs
      provides: "DELETED — file no longer exists"
      contains: "DELETED"
    - path: tests/CookBot.Tests/Services/IngredientRefDetectionServiceTests.cs
      provides: "DELETED — file no longer exists"
      contains: "DELETED"
  key_links:
    - from: "Production code"
      to: "IngredientLinkPatterns.Pattern (single source of truth)"
      via: "Direct consumption via [InternalsVisibleTo] bridge — no re-definitions"
      pattern: "IngredientLinkPatterns\\.Pattern"
---

<objective>
Fix Gap 4 (IN-01) — delete the dead `IngredientRefDetectionService` which still defines a duplicate `[name](#id)` regex (with `+` quantifier vs canonical `*`), undermining the "single source of truth" doc-comment in `IngredientLinkPatterns.cs`. The service has no production callers (Phase 1 D-13 retired the call site in `RecipeService` when `IngredientRefs` writes were dropped); only the service's own test class still references it.

Purpose: Close the regression vector. Anyone reviving the dead service in the future would consume the wrong regex. Deletion is the cleanest mitigation — the canonical `IngredientLinkPatterns.Pattern` is the single source of truth.

Output: Two files deleted (the service + its tests), zero production callers updated (none exist), build and test suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/phases/03-editor-ux-without-special-syntax/03-CONTEXT.md
@.planning/phases/03-editor-ux-without-special-syntax/03-GOAL-VERIFICATION.md
@.planning/phases/03-editor-ux-without-special-syntax/03-REVIEW.md

<interfaces>
<!-- Pre-deletion grep evidence (already verified): -->
<!-- $ grep -rn "IngredientRefDetectionService" src tests -->
<!-- src/CookBot.Application/Services/IngredientRefDetectionService.cs:6:public static class IngredientRefDetectionService -->
<!-- tests/CookBot.Tests/Services/IngredientRefDetectionServiceTests.cs:6:public class IngredientRefDetectionServiceTests -->
<!-- tests/CookBot.Tests/Services/IngredientRefDetectionServiceTests.cs:16:        var refs = IngredientRefDetectionService.DetectRefs( -->
<!-- tests/CookBot.Tests/Services/IngredientRefDetectionServiceTests.cs:34:        var refs = IngredientRefDetectionService.DetectRefs( -->
<!-- tests/CookBot.Tests/Services/IngredientRefDetectionServiceTests.cs:46:        var refs = IngredientRefDetectionService.DetectRefs( -->
<!-- All 5 references are in the two files being deleted. ZERO production callers in src/ outside the service file itself. Safe to delete. -->

<!-- The canonical [name](#id) regex (preserved): -->
<!-- src/CookBot.Application/Recipes/IngredientLinkPatterns.cs -->
<!-- public static readonly Regex Pattern = new(@"\[([^\]]*)\]\(#(\d+)\)", RegexOptions.Compiled); -->

<!-- The dead service's divergent regex (to be deleted): -->
<!-- src/CookBot.Application/Services/IngredientRefDetectionService.cs:8-10 -->
<!-- private static readonly Regex MarkdownLinkPattern = new(@"\[([^\]]+)\]\(#(\d+)\)", RegexOptions.Compiled); -->
<!-- Note the `+` (1+ chars) vs canonical `*` (0+ chars) — the divergence. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Delete IngredientRefDetectionService and its test class; verify zero callers</name>
  <read_first>
    - src/CookBot.Application/Services/IngredientRefDetectionService.cs (29-line file — confirm it's the only `IngredientRefDetectionService` definition)
    - tests/CookBot.Tests/Services/IngredientRefDetectionServiceTests.cs (50-line file — confirm it only exercises the service being deleted)
    - src/CookBot.Infrastructure/Migrations/ (the EF migrations folder — confirm any mentions of `IngredientRefs` are EF column references on the `RecipeStep` entity, NOT references to the `IngredientRefDetectionService` class — those are separate concerns and remain untouched)
  </read_first>
  <files>src/CookBot.Application/Services/IngredientRefDetectionService.cs, tests/CookBot.Tests/Services/IngredientRefDetectionServiceTests.cs</files>
  <action>
    Delete two files and verify nothing else broke.

    **Step A — Pre-flight grep (do this BEFORE deleting):**

    Run from the repo root:
    ```bash
    grep -rn "IngredientRefDetectionService" src/ tests/
    ```

    Expected output: exactly 5 lines, all in the two files being deleted.
    - `src/CookBot.Application/Services/IngredientRefDetectionService.cs:6:public static class IngredientRefDetectionService`
    - `tests/CookBot.Tests/Services/IngredientRefDetectionServiceTests.cs:6:public class IngredientRefDetectionServiceTests`
    - `tests/CookBot.Tests/Services/IngredientRefDetectionServiceTests.cs:16:` (and 34, 46 — three `IngredientRefDetectionService.DetectRefs(...)` calls in the tests).

    If grep finds ANY OTHER reference (e.g. a production caller in `RecipeService`, a DI registration in `Program.cs`, a comment elsewhere) — STOP. Do not delete. Surface the additional reference in the summary; the gap-closure scope was specified on the assumption of zero production callers, so any unexpected reference invalidates that assumption and needs human review.

    **Step B — Delete the production file:**

    ```bash
    rm src/CookBot.Application/Services/IngredientRefDetectionService.cs
    ```

    **Step C — Delete the test file:**

    ```bash
    rm tests/CookBot.Tests/Services/IngredientRefDetectionServiceTests.cs
    ```

    **Step D — Build verification:**

    ```bash
    dotnet build FreelovesCookBot.sln
    ```

    Must produce 0 warnings, 0 errors. If ANY error references `IngredientRefDetectionService` (a build error pointing at a previously-undiscovered consumer), the deletion premise is wrong — restore the deleted files via `git checkout HEAD -- <path>` and surface the consumer in the summary for human review.

    **Step E — Test verification:**

    ```bash
    dotnet test tests/CookBot.Tests/CookBot.Tests.csproj --filter "Category!=RequiresApiKey" --no-build
    ```

    Expected: full test suite passes minus the 3 deleted `IngredientRefDetectionServiceTests` `[Fact]`s. So baseline-was-185 → expected ≥ 182 passing, 0 failing.

    Note: Plan 03-05 (parallel) and Plan 03-06 (parallel) may also be running in the same wave. If their tests have already landed when this plan runs, the test count may be different. The relevant assertion is: zero failing tests, and `IngredientRefDetectionServiceTests` no longer appears in the test output.

    **Step F — Confirm the canonical regex remains:**

    Final grep: confirm there is exactly ONE `[name](#id)`-shaped regex declaration left in the production codebase:
    ```bash
    grep -rn 'Regex.*\[\\\[\(\.\*\?\|\[\^\\\]\]\)' src/ 2>/dev/null
    ```

    Or simpler — confirm the canonical regex is intact:
    ```bash
    grep -q "Pattern = new" src/CookBot.Application/Recipes/IngredientLinkPatterns.cs
    ```

    Should exit 0. The canonical regex file is untouched.

    **Step G — DO NOT TOUCH:**

    - `src/CookBot.Application/Recipes/IngredientLinkPatterns.cs` — the canonical regex file. UNCHANGED.
    - `src/CookBot.Application/Services/RecipeService.cs` — the Phase 1 D-13 comment about `IngredientRefs` retirement is still relevant; leave it.
    - `src/CookBot.Domain/Entities/RecipeStep.cs` — the `IngredientRefs` column on the entity is a Phase 4 deletion target (POLISH-03); not in this gap-closure scope.
    - Any EF migration file mentioning `IngredientRefs` — these are EF schema artifacts and must not be edited.

    Removing the service does NOT require dropping the database column. The column is now (and was already, post-Phase-1-D-13) write-empty — it persists across migrations until POLISH-03 in Phase 4 issues a drop. Out of scope here.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/freeloves-cookbot &amp;&amp; ! test -f src/CookBot.Application/Services/IngredientRefDetectionService.cs &amp;&amp; ! test -f tests/CookBot.Tests/Services/IngredientRefDetectionServiceTests.cs &amp;&amp; ! grep -rn "IngredientRefDetectionService" src/ tests/ &amp;&amp; grep -q "IngredientLinkPatterns" src/CookBot.Application/Recipes/IngredientLinkPatterns.cs &amp;&amp; dotnet build FreelovesCookBot.sln /p:TreatWarningsAsErrors=false 2>&amp;1 | grep -E "Build succeeded|Error" | tail -3 &amp;&amp; dotnet test tests/CookBot.Tests/CookBot.Tests.csproj --filter "Category!=RequiresApiKey" --no-build 2>&amp;1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `! test -f src/CookBot.Application/Services/IngredientRefDetectionService.cs` exits 0 (file deleted).
    - `! test -f tests/CookBot.Tests/Services/IngredientRefDetectionServiceTests.cs` exits 0 (file deleted).
    - `! grep -rn 'IngredientRefDetectionService' src/ tests/` exits 0 (zero remaining references in source or tests — note: this is `! grep -rn` so it succeeds when grep returns no matches).
    - `grep -q "Pattern = new" src/CookBot.Application/Recipes/IngredientLinkPatterns.cs` exits 0 (canonical regex untouched).
    - `! grep -rn 'MarkdownLinkPattern' src/' exits 0 (the duplicate regex name is gone — the only place it lived was the deleted service).
    - `dotnet build FreelovesCookBot.sln` exits 0 with 0 warnings, 0 errors.
    - `dotnet test --filter "Category!=RequiresApiKey" --no-build` shows 0 failing tests. The total count drops by 3 from the baseline (or by 3 from whatever 03-05 / 03-06 contribute, depending on wave merge order).
    - `git status` shows two `D` (deleted) entries for the deleted files.
  </acceptance_criteria>
  <done>
    `IngredientRefDetectionService` and its tests are deleted. Zero production callers existed; zero remain. The duplicate `MarkdownLinkPattern` regex is gone with the service. The canonical `IngredientLinkPatterns.Pattern` is now the single, unambiguous `[name](#id)` regex source in the codebase. Build green, test suite green (count down by 3).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

(No new trust boundaries introduced — this plan deletes code; it does not add a new external surface or data flow.)

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03P07-01 | Tampering | Latent regex divergence | mitigate | Deleting the duplicate `MarkdownLinkPattern` (with `+` quantifier) eliminates a potential vector where a future caller revives the dead service and consumes a regex that disagrees with the canonical one. Single source of truth restored. |
| T-03P07-02 | Information Disclosure | Production code surface | accept | The deleted service has no production callers; deletion does not change runtime behavior. No information-disclosure risk introduced or removed. |

</threat_model>

<verification>
After this task lands:

1. **Files deleted:** Two `D` entries in `git status`.
2. **Zero references:** `grep -rn "IngredientRefDetectionService" src/ tests/` returns no matches.
3. **Build:** `dotnet build FreelovesCookBot.sln` exits 0 with 0 warnings, 0 errors.
4. **Tests:** `dotnet test --filter "Category!=RequiresApiKey" --no-build` shows 0 failing. Test count drops by exactly 3 (the three [Fact]s in the deleted test file).
5. **Canonical regex preserved:** `IngredientLinkPatterns.cs` is untouched.
6. **Single source of truth assertion:** `grep -rn 'MarkdownLinkPattern' src/` returns no matches; `grep -rn 'IngredientLinkPatterns.Pattern' src/' returns the consumers wired by Plan 01 (formatter, validator, chip composer, step editor) plus Plan 03-06 (cooking mode, if it lands first or in parallel).
</verification>

<success_criteria>
- `IngredientRefDetectionService.cs` and `IngredientRefDetectionServiceTests.cs` are deleted from the working tree.
- `grep -rn "IngredientRefDetectionService" src/ tests/` returns no matches.
- `grep -rn "MarkdownLinkPattern" src/` returns no matches (the divergent name is gone).
- Build and test suite green.
- Gap 4 (IN-01) closed: there is exactly one `[name](#id)` regex definition in the production codebase, in `IngredientLinkPatterns.cs`.
</success_criteria>

<output>
After completion, create `.planning/phases/03-editor-ux-without-special-syntax/03-07-SUMMARY.md` documenting:
- The grep evidence confirming zero production callers existed before deletion.
- The two files deleted (with line counts).
- Confirmation that `MarkdownLinkPattern` and `IngredientRefDetectionService` no longer appear anywhere in `src/` or `tests/`.
- Test count delta (down by 3).
- A note that the EF column `RecipeStep.IngredientRefs` was deliberately NOT touched — that's POLISH-03 / Phase 4 territory, and the column is already write-empty per Phase 1 D-13.
</output>
