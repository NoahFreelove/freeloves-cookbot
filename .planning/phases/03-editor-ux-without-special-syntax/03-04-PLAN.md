---
phase: 03-editor-ux-without-special-syntax
plan: 04
type: execute
wave: 3
depends_on: [03-02, 03-03]
files_modified:
  - src/CookBot.Application/Services/RecipeService.cs
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/03-editor-ux-without-special-syntax/03-RESEARCH.md
files_created:
  - .planning/phases/03-editor-ux-without-special-syntax/03-VERIFICATION.md
autonomous: false
requirements: [EDITOR-03, EDITOR-07]
tags: [auto-write-deletion, docs, requirements-amendment, accessibility, smoke-checklist, manual-verify]
must_haves:
  truths:
    - "RecipeService.CreateAsync and RecipeService.UpdateAsync no longer auto-write timers from the regex when a step has no explicit timers; explicit timer chips are the only persisted source (EDITOR-03 finalization)."
    - "REQUIREMENTS.md EDITOR-01 wording is amended per CONTEXT.md D-A5: chips render with the ingredient NAME (not 'user-facing index'), and the immutable id is what serializes."
    - "ROADMAP.md Phase 3 Success Criterion #1 wording is updated to align with the EDITOR-01 amendment (no 'user-facing index' clause)."
    - "An accessibility manual smoke checklist is documented in 03-VERIFICATION.md covering Tab/Shift+Tab nav, screen reader, JS-interop-fail fallback, color contrast, and IME composition."
    - "Tests asserting timer auto-detection-on-save behavior are updated or removed; the inverse assertion ('step text containing 25 minutes but no Timers results in zero persisted timers') is added."
    - "A human reviewer (the developer) walks through the smoke checklist in a checkpoint task before phase signoff."
  artifacts:
    - path: src/CookBot.Application/Services/RecipeService.cs
      provides: "CreateAsync and UpdateAsync no longer call TimerDetectionService.DetectTimers as a fallback; explicit Timers chips are the sole source."
      contains: "ps.Timers ?? new()"
    - path: .planning/REQUIREMENTS.md
      provides: "EDITOR-01 amended per D-A5 (no 'user-facing index'; chips render the name)."
      contains: "selecting one inserts a chip showing the ingredient name"
    - path: .planning/ROADMAP.md
      provides: "Phase 3 Success Criterion #1 aligned with EDITOR-01 amendment."
      contains: "chips render with the ingredient name"
    - path: .planning/phases/03-editor-ux-without-special-syntax/03-VERIFICATION.md
      provides: "8-item manual smoke checklist for accessibility + JS-interop fallback + color contrast verification (EDITOR-07 manual gate)."
      contains: "Tab/Shift+Tab; screen reader; prefers-reduced-motion; JS-interop"
  key_links:
    - from: src/CookBot.Application/Services/RecipeService.cs
      to: src/CookBot.Application/Services/TimerDetectionService.cs
      via: "TimerDetectionService is no longer called from CreateAsync/UpdateAsync as a save-path fallback"
      pattern: "(?!.*TimerDetectionService\\.DetectTimers\\(ps\\.Text\\))"
    - from: .planning/REQUIREMENTS.md
      to: .planning/phases/03-editor-ux-without-special-syntax/03-CONTEXT.md
      via: "D-A5 amendment locked into REQUIREMENTS.md"
      pattern: "selecting one inserts a chip showing the ingredient name"
---

<objective>
Close the phase by deleting the auto-write-on-save timer fallback in `RecipeService` (EDITOR-03 final clause: "explicit timer chips are the only persisted source"), amending `REQUIREMENTS.md` EDITOR-01 wording per CONTEXT.md D-A5 (chips display ingredient NAME — no "user-facing index"), aligning `ROADMAP.md` Phase 3 SC#1 with the same amendment, and authoring a manual accessibility smoke checklist in `03-VERIFICATION.md` covering EDITOR-07's "passes axe-core/screen-reader smoke pass and degrades gracefully if JS interop fails." The plan ends with a human-verify checkpoint where the developer walks through the smoke checklist in a real browser before phase signoff.

Purpose: D-A5 says the EDITOR-01 amendment is **P0** — must land in `REQUIREMENTS.md` as part of plan-phase, not after. ROADMAP.md SC#1 has the same conflicting "user-facing index" clause and needs the same fix. The auto-write deletion is the last code-side change for EDITOR-03; everything else for EDITOR-03 (the suggestion UI) shipped in Plan 03. The accessibility smoke checklist is the verification gate for EDITOR-07 (no Playwright/axe-core CI infra in this milestone).

Output: REQUIREMENTS and ROADMAP in sync with shipped behavior; auto-write fallback removed; smoke checklist authored; checkpoint task gates phase completion on developer verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-editor-ux-without-special-syntax/03-CONTEXT.md
@.planning/phases/03-editor-ux-without-special-syntax/03-RESEARCH.md
@.planning/phases/03-editor-ux-without-special-syntax/03-PATTERNS.md
@.planning/phases/03-editor-ux-without-special-syntax/03-02-SUMMARY.md
@.planning/phases/03-editor-ux-without-special-syntax/03-03-SUMMARY.md

<interfaces>
<!-- Code surfaces this plan modifies. -->

From src/CookBot.Application/Services/RecipeService.cs (current — full file 185 lines):
- Lines 65-79 (CreateAsync step loop): the ternary fallback `: TimerDetectionService.DetectTimers(ps.Text)` at the end of `Timers = ps.IsSection ? new() : (ps.Timers?.Any() == true ? ... : TimerDetectionService.DetectTimers(ps.Text))`.
- Lines 131-147 (UpdateAsync step loop): same pattern.
- Lines 35-49, 85-86, 102-106, 150-151: auth + canonical-doc patterns. UNTOUCHED.

From .planning/REQUIREMENTS.md line 50 (current):
```
- [ ] **EDITOR-01**: `RecipeEditor.razor`'s step textarea is replaced with a chip-aware composer built on `MudAutocomplete<Ingredient>` + `MudChipSet<T>`. Typing `@` or clicking an "Insert ingredient" affordance opens autocomplete over recipe ingredients; selecting one inserts a chip; the underlying string keeps `[name](#id)` markdown invisibly.
```

From .planning/ROADMAP.md line 65 (current — Phase 3 Success Criterion #1):
```
1. A user can author a 5-ingredient recipe with 2 timers and 3 ingredient references in `RecipeEditor.razor` without typing any `[name](#id)` markdown — every reference is inserted via `@`-trigger autocomplete or an "Insert ingredient" affordance, and chips render with the user-facing index while the underlying string keeps the immutable `id`.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 0: Mark RESEARCH.md `## Open Questions` as `(RESOLVED)` with per-question RESOLVED markers (revision Blocker 2 fix)</name>
  <files>
    .planning/phases/03-editor-ux-without-special-syntax/03-RESEARCH.md
  </files>
  <read_first>
    - .planning/phases/03-editor-ux-without-special-syntax/03-RESEARCH.md (lines ~1075-1097 — the four numbered open questions and their inline "Recommendation:" lines that need explicit `**RESOLVED:**` markers)
  </read_first>
  <action>
    Surgical docs-only edit. Read `.planning/phases/03-editor-ux-without-special-syntax/03-RESEARCH.md` to capture the current text of lines ~1075-1097. Apply two changes:

    Edit 1 — Update the section heading on line ~1075:

    BEFORE:
    ```
    ## Open Questions
    ```

    AFTER:
    ```
    ## Open Questions (RESOLVED)
    ```

    Edit 2 — For each of the four numbered questions, prepend a `**RESOLVED:**` line explaining how the plan honors the recommendation. The exact mapping (verified by reading the plans):

    Q1 (Range-timer persistence): Plan 03 Task 1 implements `ParseRangeToSeconds` that persists the lowest bound (e.g. "20-25 minutes" → 1200 seconds = 20 min). The acceptance criteria + tests in `TimerDetectionServiceRegexTests.cs` pin this. Add immediately after the question title:
    ```
       - **RESOLVED:** Lowest-bound persistence locked. Implemented in Plan 03 Task 1 (`TimerDetectionService.ParseRangeToSeconds`), tested in `TimerDetectionServiceRegexTests.Range_PersistsLowestBound`.
    ```

    Q2 (Drag handles for ingredient reorder): Deferred — no plan in Phase 3 adds drag-and-drop. Existing arrow buttons (preserved in `RecipeStepEditor.razor` reorder controls) satisfy EDITOR-04. Add:
    ```
       - **RESOLVED:** Deferred to Phase 4 polish / backlog. Phase 3 keeps the existing arrow-button reorder UX in `RecipeStepEditor.razor`; no DnD added. EDITOR-04 is satisfied by `id`-based chip rendering (Phase 1 D-06 immutable id flowing through Wave 1 chip composer), independent of reorder mechanism.
    ```

    Q3 (Lift `IngredientLinkPattern` to a shared `internal` location): Plan 01 Task 1 lifts the regex into a new `IngredientLinkPatterns` static class shared between `RecipeStepTextFormatter`, `RecipeValidator`, and the new chip composer. Add:
    ```
       - **RESOLVED:** Lifted to `CookBot.Application.Recipes.IngredientLinkPatterns` in Plan 01 Task 1; consumed by `RecipeStepTextFormatter`, `RecipeValidator`, `RecipeChipComposer`, and `RecipeStepEditor` (D-A4 / D-B2 reference-strip path). Marked `internal` with `[InternalsVisibleTo("CookBot.Web")]`.
    ```

    Q4 (CSS location for chip styling — `app.css` vs scoped `.razor.css`): Plan 01 Task 2 adds Phase 3 chip rules to `wwwroot/app.css` under a `/* === Recipe Chip Composer === */` banner, honoring CONVENTIONS.md's "single stylesheet" pattern. Add:
    ```
       - **RESOLVED:** Stays in `wwwroot/app.css` per CONVENTIONS.md. Plan 01 Task 2 appends a `/* === Recipe Chip Composer === */` banner section with the chip / chip-suggestion / chip-highlight-pulse rules. No `.razor.css` files introduced.
    ```

    The four `**RESOLVED:**` lines must be inserted directly after each question's title line (the `**Range-timer persistence...**`, `**Drag handles...**`, etc. line) and BEFORE the existing "What we know:" / "Recommendation:" sub-bullets, so each question reads:
    ```
    1. **<original title>**
       - **RESOLVED:** <answer>
       - What we know: ...
       - What's unclear: ...
       - Recommendation: ...
    ```

    Do NOT delete the existing "What we know / What's unclear / Recommendation" bullets — they remain as historical context. Only PREPEND the RESOLVED line.

    Verify after editing: `grep -c '**RESOLVED:**' .planning/phases/03-editor-ux-without-special-syntax/03-RESEARCH.md` should return at least 4.
  </action>
  <verify>
    <automated>grep -q '## Open Questions (RESOLVED)' .planning/phases/03-editor-ux-without-special-syntax/03-RESEARCH.md</automated>
    <automated>! grep -qE '^## Open Questions$' .planning/phases/03-editor-ux-without-special-syntax/03-RESEARCH.md</automated>
    <automated>grep -cE '\*\*RESOLVED:\*\*' .planning/phases/03-editor-ux-without-special-syntax/03-RESEARCH.md | awk '{ if ($1 < 4) exit 1 }'</automated>
    <automated>grep -q 'Lowest-bound persistence locked' .planning/phases/03-editor-ux-without-special-syntax/03-RESEARCH.md</automated>
    <automated>grep -q 'Deferred to Phase 4 polish' .planning/phases/03-editor-ux-without-special-syntax/03-RESEARCH.md</automated>
    <automated>grep -q 'IngredientLinkPatterns' .planning/phases/03-editor-ux-without-special-syntax/03-RESEARCH.md</automated>
    <automated>grep -q 'Stays in `wwwroot/app.css`' .planning/phases/03-editor-ux-without-special-syntax/03-RESEARCH.md</automated>
  </verify>
  <acceptance_criteria>
    - `03-RESEARCH.md` heading on line ~1075 reads `## Open Questions (RESOLVED)` (NOT `## Open Questions`).
    - Each of the four numbered questions has a `**RESOLVED:**` sub-bullet immediately after its title.
    - The existing "What we know / What's unclear / Recommendation" bullets are preserved (historical context).
    - At least 4 occurrences of the literal `**RESOLVED:**` exist in the file.
  </acceptance_criteria>
  <done>RESEARCH.md Open Questions section is fully resolved with explicit `**RESOLVED:**` markers tracing each question to its implementation in Plans 01/03 or its deferral.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 1: Delete timer auto-write fallback in RecipeService.CreateAsync and UpdateAsync</name>
  <files>
    src/CookBot.Application/Services/RecipeService.cs
  </files>
  <read_first>
    - src/CookBot.Application/Services/RecipeService.cs (FULL FILE — capture exact current text of the two ternary fallbacks at lines 65-79 and 131-147)
    - tests/CookBot.Tests/Services/ (grep for any tests asserting auto-detection-on-save behavior — likely candidates: OwnershipTests.cs, RecipeServiceTests.cs if it exists)
    - .planning/phases/03-editor-ux-without-special-syntax/03-RESEARCH.md §"Item 11 — Auto-write deletion (per EDITOR-03)" (lines 977-1015)
    - .planning/phases/03-editor-ux-without-special-syntax/03-PATTERNS.md §"RecipeService.cs (modified)"
  </read_first>
  <action>
    Step A — Modify `src/CookBot.Application/Services/RecipeService.cs` at the `CreateAsync` step loop (lines 65-79). Find the existing pattern:
    ```csharp
    Timers = ps.IsSection ? new() :
        (ps.Timers?.Any() == true
            ? ps.Timers.Select(t => new StepTimer { Duration = t.Duration, Unit = t.Unit, Label = t.Label }).ToList()
            : TimerDetectionService.DetectTimers(ps.Text)),
    ```
    Replace with the simplified explicit-only assignment:
    ```csharp
    Timers = ps.IsSection
        ? new()
        : (ps.Timers ?? new()).Select(t => new StepTimer { Duration = t.Duration, Unit = t.Unit, Label = t.Label }).ToList(),
    ```

    Step B — Apply the same change at `UpdateAsync` (lines 131-147). The pattern is identical; the replacement is identical.

    Step C — Verify there are no other call sites of `TimerDetectionService.DetectTimers` inside `RecipeService.cs` after the edit. The `using CookBot.Application.Services;` directive can stay — `TimerDetectionService` may still be referenced from other files in `CookBot.Application` (it's not, but the import is harmless).

    Step D — Search existing tests for assertions about auto-detection-on-save behavior:
    ```bash
    grep -rn "DetectTimers\|auto.*timer\|TimerDetection" tests/CookBot.Tests/
    ```
    Document findings in the SUMMARY. The pre-existing standalone `TimerDetectionServiceTests.cs` (in `tests/CookBot.Tests/Services/`) tests the regex itself in isolation — UNCHANGED behavior, no edits needed. Look for any other test in `OwnershipTests.cs` or similar that exercises the save-path fallback. If found:
    - Update the assertion to invert: feeding "Cook 25 minutes" with `Timers = null` results in `step.Timers.Count == 0` after save.
    - Do NOT add a `RecipeServiceAutoWriteRemovalTests.cs` source-reading test (revision Warning 5 fix). The deletion is fully enforced by the `<verify>` grep block of THIS task — see the `! grep -q 'TimerDetectionService.DetectTimers(ps.Text)'` line below. Source-reading tests with relative paths are flaky across `dotnet test` working directories and add no coverage beyond the grep check. End-to-end integration of the deletion is exercised in the smoke checklist (Plan 04 Task 4 — Item 6 of `03-VERIFICATION.md`).
  </action>
  <verify>
    <automated>! grep -q 'TimerDetectionService.DetectTimers(ps.Text)' src/CookBot.Application/Services/RecipeService.cs</automated>
    <automated>grep -q '(ps.Timers ?? new())' src/CookBot.Application/Services/RecipeService.cs</automated>
    <automated>dotnet build CookBot.sln</automated>
    <automated>dotnet test tests/CookBot.Tests/CookBot.Tests.csproj</automated>
  </verify>
  <acceptance_criteria>
    - `src/CookBot.Application/Services/RecipeService.cs` no longer contains the literal string `TimerDetectionService.DetectTimers(ps.Text)` at any call site.
    - The replacement uses `(ps.Timers ?? new()).Select(...)` (explicit-only timer projection).
    - `dotnet build CookBot.sln` exits 0.
    - Full test suite (`dotnet test`) exits 0; pre-existing simple-pattern timer detection tests in `TimerDetectionServiceTests.cs` continue to pass (regex behavior unchanged); auto-write-on-save coupling is verified absent via grep.
    - If the grep-search uncovered a pre-existing test asserting auto-detection-on-save, the test is updated to assert the inverse (zero auto-written timers).
  </acceptance_criteria>
  <done>EDITOR-03 final clause shipped: explicit chips are the only persisted source.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Amend REQUIREMENTS.md EDITOR-01 wording (D-A5 P0) and align ROADMAP.md SC#1</name>
  <files>
    .planning/REQUIREMENTS.md,
    .planning/ROADMAP.md
  </files>
  <read_first>
    - .planning/REQUIREMENTS.md (line 50 — current EDITOR-01 wording with the conflicting "user-facing index" clause)
    - .planning/ROADMAP.md (line 65 — Phase 3 Success Criterion #1 with the same conflicting clause)
    - .planning/phases/03-editor-ux-without-special-syntax/03-CONTEXT.md §"D-A5 (REQUIREMENTS edit needed)" (lines 43-45) — verbatim new wording proposed
    - .planning/phases/03-editor-ux-without-special-syntax/03-RESEARCH.md §"Item 10 — EDITOR-01 docs-amendment task" (lines 961-973) — exact diff
  </read_first>
  <action>
    Step A — Edit `.planning/REQUIREMENTS.md` line 50. Read the file to capture the exact current line, then apply this replacement:

    BEFORE (current line 50, verbatim):
    ```
    - [ ] **EDITOR-01**: `RecipeEditor.razor`'s step textarea is replaced with a chip-aware composer built on `MudAutocomplete<Ingredient>` + `MudChipSet<T>`. Typing `@` or clicking an "Insert ingredient" affordance opens autocomplete over recipe ingredients; selecting one inserts a chip; the underlying string keeps `[name](#id)` markdown invisibly.
    ```

    AFTER (D-A5 amended):
    ```
    - [ ] **EDITOR-01**: `RecipeEditor.razor`'s step textarea is replaced with a chip-aware composer built on `MudAutocomplete<Ingredient>` + `MudChipSet<T>`. Typing `@` or clicking an "Insert ingredient" affordance opens autocomplete over recipe ingredients; selecting one inserts a chip showing the ingredient name. The underlying string keeps `[name](#id)` markdown invisibly; the immutable `id` is what serializes.
    ```

    Step B — Edit `.planning/ROADMAP.md` line 65 (Phase 3 Success Criterion #1). Read the file to capture the current full SC#1 line, then replace:

    BEFORE (current SC#1, verbatim):
    ```
      1. A user can author a 5-ingredient recipe with 2 timers and 3 ingredient references in `RecipeEditor.razor` without typing any `[name](#id)` markdown — every reference is inserted via `@`-trigger autocomplete or an "Insert ingredient" affordance, and chips render with the user-facing index while the underlying string keeps the immutable `id`.
    ```

    AFTER (aligned with D-A3 / D-A5):
    ```
      1. A user can author a 5-ingredient recipe with 2 timers and 3 ingredient references in `RecipeEditor.razor` without typing any `[name](#id)` markdown — every reference is inserted via `@`-trigger autocomplete or an "Insert ingredient" affordance, and chips render with the ingredient name while the underlying string keeps the immutable `id`.
    ```

    Step C — Verify the diff: only one bullet in REQUIREMENTS.md and one bullet in ROADMAP.md change. Other content (line-level whitespace, surrounding bullets, phase summaries) is preserved.
  </action>
  <verify>
    <automated>grep -q 'selecting one inserts a chip showing the ingredient name' .planning/REQUIREMENTS.md</automated>
    <automated>grep -q 'the immutable .id. is what serializes' .planning/REQUIREMENTS.md</automated>
    <automated>! grep -qF 'chips render with the user-facing index while the underlying string keeps the immutable' .planning/REQUIREMENTS.md</automated>
    <automated>grep -q 'chips render with the ingredient name while the underlying string keeps the immutable' .planning/ROADMAP.md</automated>
    <automated>! grep -qF 'chips render with the user-facing index while the underlying string keeps the immutable' .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` contains the verbatim phrase `selecting one inserts a chip showing the ingredient name`.
    - `.planning/REQUIREMENTS.md` contains the verbatim phrase `the immutable \`id\` is what serializes`.
    - `.planning/REQUIREMENTS.md` does NOT contain the phrase `chips render with the user-facing index while the underlying string keeps the immutable`.
    - `.planning/ROADMAP.md` contains the verbatim phrase `chips render with the ingredient name while the underlying string keeps the immutable`.
    - `.planning/ROADMAP.md` does NOT contain the phrase `chips render with the user-facing index while the underlying string keeps the immutable`.
    - All other content in REQUIREMENTS.md and ROADMAP.md is preserved (no accidental edits to other phases or sections).
  </acceptance_criteria>
  <done>EDITOR-01 wording locked. REQUIREMENTS and ROADMAP agree with the shipped chip-name-only behavior.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Author manual accessibility smoke checklist in 03-VERIFICATION.md</name>
  <files>
    .planning/phases/03-editor-ux-without-special-syntax/03-VERIFICATION.md
  </files>
  <read_first>
    - .planning/phases/03-editor-ux-without-special-syntax/03-RESEARCH.md §"Item 8 — Accessibility test mechanism" (lines 907-925) — 8-item smoke checklist to seed from
    - .planning/phases/03-editor-ux-without-special-syntax/03-CONTEXT.md §"D-D4 (EDITOR-07 — JS-interop fallback)" — fallback contract under test
    - .planning/phases/02-ai-structured-output-conformance/02-VERIFICATION.md (Phase 2 verification artifact — copy structure / frontmatter shape)
  </read_first>
  <action>
    Create `.planning/phases/03-editor-ux-without-special-syntax/03-VERIFICATION.md` with this content:

    ```markdown
    # Phase 3: Editor UX Without Special Syntax — Verification

    **Generated:** {YYYY-MM-DD at execution}
    **Scope:** EDITOR-01 .. EDITOR-07 manual + automated verification.

    ## Automated Verification

    The following tests gate phase completion (run via `dotnet test tests/CookBot.Tests/CookBot.Tests.csproj`):

    | Test Class | Covers | Decision Refs |
    |---|---|---|
    | `RecipeChipComposerTests` (bUnit) | Chip rendering, D-A1 invariant (@-path == button-path), D-A6 unresolved chip, D-D4 fallback | EDITOR-01, EDITOR-04, EDITOR-07 |
    | `StepSectionToggleTests` (bUnit) | Step→Section conversion, D-B1 toggle, D-B2 heading reuse, D-B3 confirmation helper | EDITOR-02 |
    | `TimerSuggestionTests` (xUnit on formatter) | Timer-suggestion span wrapping, D-C3 skip-already-converted, no-double-wrap, range lowest | EDITOR-03 |
    | `TimerDetectionServiceRegexTests` (xUnit) | Fractional / Range / Multi-segment / Simple regex broadening | EDITOR-03 (regex broadening Claude's discretion) |
    | `PasteFlowTests` (bUnit + source-presence) | Pass-through flow; numbered-list fallback deletion verified | EDITOR-05 |
    | Existing Phase 1 + Phase 2 suites | Round-trip / parser / validator / AI / structured-output unchanged | Regression gates |

    ## Manual Smoke Checklist (EDITOR-07 gate)

    **No automated accessibility infra exists in this codebase** (no axe-core, Playwright, Lighthouse). The following checklist is the verification gate for EDITOR-07 — work through it on a real browser session before marking the phase complete.

    Pre-requisite: `./run.sh` running, navigate to `http://localhost:7000`. Sign in. Create a test cookbook and a fresh recipe; you'll edit it during the smoke pass.

    ### Item 1 — Tab / Shift+Tab navigation across step rows
    - [ ] Press Tab from the recipe Name field. Focus moves: Name → Servings → PrepTime → CookTime → Tags → Source → first Ingredient row controls → first step row's [Step | Section] toggle → first step's chip composer container → first step's "Insert ingredient" button → first step's Delete button → second step row → ... → "Add Step" button → "Save" button.
    - [ ] Press Shift+Tab. Focus moves backward through the same chain. No focus traps.
    - [ ] Verify there is no visible focus on a "hidden" element (e.g. a closed MudMenu).

    ### Item 2 — Backspace and Arrow-Left chip-removal semantics
    - [ ] Author a step containing one ingredient chip ("Add @Salt"). Place caret immediately after the chip. Press Backspace once. The chip is removed. The underlying `[Salt](#1)` markdown is also gone (verify by toggling to "View as text" or by reading the chip count).
    - [ ] Author another chip. Place caret immediately after. Press Arrow-Left. Caret moves to the end of the chip body (selectable position, no entry into chip text). Press Arrow-Left again — caret advances into the preceding text segment.

    ### Item 3 — Screen-reader announces chips correctly
    - [ ] Enable VoiceOver (macOS: Cmd-F5) or NVDA (Windows). Tab to a step's chip composer.
    - [ ] An ingredient chip should announce as: `"<ingredient name>, button"` or `"<ingredient name>, ingredient chip, button"`. Activating it (Enter / VO-Space) should open the replace-popover with focus moved into it.
    - [ ] Press Escape on the popover. Focus returns to the chip.

    ### Item 4 — `@`-trigger autocomplete is keyboard-only operable
    - [ ] In a step text segment, type `@par`. The autocomplete picker should open at the caret position.
    - [ ] Arrow Down / Up navigates the result list. Enter selects the highlighted option. The chip replaces `@par` literally.
    - [ ] Type `@xyz` (no matches). Picker shows empty / "no results." Press Escape. The literal text `@xyz` remains in the segment (no auto-removal); the picker closes.

    ### Item 5 — Step / Section toggle is announced as a radiogroup
    - [ ] Tab to the Step/Section toggle. Screen reader announces `"radiogroup"` or `"toggle group, 2 options"`.
    - [ ] Arrow keys cycle between Step and Section. Enter / Space activates the highlighted option.
    - [ ] When toggling Section → Step on a populated step (with timers / refs), the confirmation dialog opens and is keyboard-operable: Tab cycles between Cancel and Convert; Enter activates; Escape cancels.

    ### Item 6 — Inline timer suggestion popover (D-C1)
    - [ ] Author a step containing "Bake 25 minutes." After the 500ms debounce, a dotted-underline appears under "25 minutes" (warning color).
    - [ ] Click "25 minutes." A popover opens with "Detected: 25 minutes — Convert to a timer? [Yes] [No]." Tab to Yes; Enter. A timer chip appears below the step text.
    - [ ] Author a second timer in a new step. Click No. The dotted-underline remains (the user can re-click) but no chip is added.
    - [ ] Save the recipe. Reload. The accepted timer chip persists; the rejected one does not (no auto-write — Plan 04 Task 1 deletion verified).

    ### Item 7 — JS-interop-fail fallback (D-D4)
    - [ ] Open browser DevTools. Network tab: throttle to "Offline" or block `js/recipe-chip-composer.js` via Request Blocking. Reload the editor.
    - [ ] Each step's text area falls back to a `MudTextField Lines=3` displaying raw `[name](#id)` markdown. The Step/Section toggle still works (server-rendered via the existing toggle event). Save still works — the recipe round-trips through Phase 1's parser unchanged.
    - [ ] No "JS interop disconnected" red snackbar appears.
    - [ ] Re-enable the JS file and reload. Chip composer returns. No data loss.

    ### Item 8 — Color contrast on chip variants
    - [ ] In light mode: ingredient chip (Color.Info) text + background pass WCAG AA contrast (~4.5:1). Timer chip (Color.Warning) passes. Error chip (red border) passes.
    - [ ] Toggle dark mode. Re-verify all three chip variants. The `chip-highlight-pulse` CSS uses `--mud-palette-info-lighten` which adapts; visually verify the pulse is visible against both backgrounds.
    - [ ] Verify the dotted-underline timer-suggestion is distinguishable from regular text in both modes.

    ### Item 9 (recommended) — IME composition (Pitfall 7)
    - [ ] If you have a Japanese / Chinese / Korean IME available, enable it. Type a step in the IME (composition mode). Verify chips render correctly; the contenteditable segments do not break composition mid-character.
    - [ ] Pasting formatted text from a webpage (e.g. copy a Wikipedia paragraph with bold/italic) into a chip composer text segment: only plain text is inserted (the `contenteditable="plaintext-only"` attribute strips formatting). No images or links survive the paste.

    ### Cooking-mode chip click → scroll-and-highlight (D-D3)

    - [ ] Open a recipe with at least 5 ingredients in cooking mode. Click an ingredient chip in the current step's text. The ingredients sidebar scrolls smoothly to the matching ingredient and pulses for ~1.5s.
    - [ ] Click another ingredient chip rapidly. The first highlight is canceled before the second begins (no flicker).
    - [ ] With JS interop blocked (DevTools as in Item 7), click an ingredient chip. No scroll; no error snackbar; the page remains usable.

    ## Sign-off

    The smoke checklist above is gated by the developer's manual verification. After all items are checked, append a sign-off line to this file:

    ```
    Verified by: <developer name>
    Date: <YYYY-MM-DD>
    Notes: <any deviations or accepted residual issues>
    ```

    Once signed, mark Phase 3 complete in `.planning/STATE.md`.
    ```

    Note: the checklist explicitly captures the D-D4 fallback test (Item 7), the D-D3 cooking-mode interop (last section), and EDITOR-07's "passes axe-core/screen-reader smoke pass" via Items 1, 3, 5. The IME / paste-sanitization items address Pitfall 7 from RESEARCH.md.
  </action>
  <verify>
    <automated>test -f .planning/phases/03-editor-ux-without-special-syntax/03-VERIFICATION.md</automated>
    <automated>grep -q 'Tab.*Shift+Tab' .planning/phases/03-editor-ux-without-special-syntax/03-VERIFICATION.md</automated>
    <automated>grep -q 'JS-interop-fail fallback' .planning/phases/03-editor-ux-without-special-syntax/03-VERIFICATION.md</automated>
    <automated>grep -q 'D-D4' .planning/phases/03-editor-ux-without-special-syntax/03-VERIFICATION.md</automated>
    <automated>grep -q 'WCAG AA' .planning/phases/03-editor-ux-without-special-syntax/03-VERIFICATION.md</automated>
    <automated>grep -q 'IME composition' .planning/phases/03-editor-ux-without-special-syntax/03-VERIFICATION.md</automated>
    <automated>grep -q 'cooking-mode' .planning/phases/03-editor-ux-without-special-syntax/03-VERIFICATION.md</automated>
    <automated>grep -cE '^### Item [1-9]' .planning/phases/03-editor-ux-without-special-syntax/03-VERIFICATION.md | awk '{ if ($1 < 8) exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/03-editor-ux-without-special-syntax/03-VERIFICATION.md` exists.
    - File contains at least 8 numbered checklist items in the "Manual Smoke Checklist" section.
    - File mentions all five EDITOR-07 themes: Tab/Shift+Tab navigation, Backspace chip removal, screen reader announcement, JS-interop fallback (D-D4), color contrast (WCAG AA).
    - File contains a separate cooking-mode-chip-click section validating D-D3.
    - File contains an IME composition item per Pitfall 7.
    - File contains a Sign-off section with a placeholder for developer name + date.
  </acceptance_criteria>
  <done>Smoke checklist authored; verification gate ready for the developer checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Developer walks through 03-VERIFICATION.md smoke checklist</name>
  <files>.planning/phases/03-editor-ux-without-special-syntax/03-VERIFICATION.md</files>
  <action>This is a human-verify checkpoint — see &lt;how-to-verify&gt; below for the developer instructions. Claude executes nothing here; the developer walks through the smoke checklist manually and appends a sign-off block to 03-VERIFICATION.md.</action>
  <verify>The 03-VERIFICATION.md file gains a "Verified by: ..." sign-off block at the bottom; all 9 + 3 checklist items have `- [x]` markers (or documented `(DEVIATION:)` notes).</verify>
  <done>Developer signs off the smoke checklist; no blocking deviations remain.</done>
  <what-built>
    Phase 3 ships a chip-aware editor (Plan 02), cooking-mode chip rendering with scroll-highlight (Plan 03), broadened timer detection regex (Plan 03), paste-flow simplification (Plan 03), the EDITOR-01 docs amendment (Plan 04 Task 2), and the auto-write deletion (Plan 04 Task 1). All automated tests are green via `dotnet test`.

    The remaining EDITOR-07 verification gate is the manual smoke checklist authored in Plan 04 Task 3. This checkpoint asks the developer (Noah) to walk through that checklist in a real browser session.
  </what-built>
  <how-to-verify>
    1. Run `./run.sh` from the repo root. Confirm the server binds at `http://localhost:7000`.
    2. Open `http://localhost:7000` in a browser and sign in (any account works — single-user trusted-LAN).
    3. Open the file `.planning/phases/03-editor-ux-without-special-syntax/03-VERIFICATION.md` in your editor of choice.
    4. Work through the 9-item Manual Smoke Checklist. For each item:
       - Read the description.
       - Perform the user actions described.
       - Mark the checklist item with `- [x]` (checked) if it passes, OR mark `- [x] (DEVIATION: <text>)` and add a note explaining the deviation.
    5. Repeat for the cooking-mode chip-click section.
    6. After all items are checked (or deviations are documented), append the Sign-off block at the end of `03-VERIFICATION.md`:
       ```
       Verified by: Noah Freelove
       Date: <today's date YYYY-MM-DD>
       Notes: <any deviations / accepted residual issues / observations>
       ```
    7. Save the file.

    Expected outcomes:
    - All 9 main items + 3 cooking-mode items pass on a modern browser (Chrome / Edge / Firefox / Safari).
    - JS-interop fallback (Item 7) works silently — no error snackbars when the JS file is blocked.
    - Cooking-mode ingredient-chip click smooth-scrolls and pulses the sidebar entry.
    - Color contrast passes WCAG AA in both light and dark mode.

    Acceptable deviations to surface (NOT blockers):
    - IME composition (Item 9) — if you don't have a CJK IME installed, mark "skipped, dev environment limitation" and proceed.
    - Older Firefox `contenteditable="plaintext-only"` (Pitfall 7) — if testing on Firefox <115, the paste sanitization may differ; document and accept.
    - Fine-grained focus ring visibility on dark mode — minor visual polish; document and accept.

    BLOCKING deviations (do NOT sign off):
    - Save fails when JS interop is blocked — the canonical fallback contract is violated.
    - Chip click in cooking mode throws a snackbar error — Pitfall 4 mitigation is broken.
    - The Step/Section toggle's Cancel button does not revert the visual state — Pitfall 5 mitigation is broken.
    - REQUIREMENTS.md or ROADMAP.md still contains "user-facing index" — Plan 04 Task 2 amendment is broken.
  </how-to-verify>
  <resume-signal>Type "approved" with the date and any deviation notes, OR describe specific issues found and which checklist item failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan introduces no new code-level trust boundaries. The auto-write deletion REMOVES a path; the docs amendments are pure-text edits to planning artifacts.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03P04-01 | (No new threat — defensive change) | RecipeService.CreateAsync/UpdateAsync auto-write deletion. Removes the silent-rewrite footgun (CONCERNS §7) where step text "Cook 25 minutes" would silently produce a `step.Timers` entry the user never explicitly asked for. | mitigate (the deletion IS the mitigation) | The Phase 3 invariant "explicit timer chips are the only persisted source" is asserted in tests + documented in REQUIREMENTS.md EDITOR-03. No further mitigation needed; this is a security/UX-improving change. |
| T-03P04-02 | Tampering (docs drift) | REQUIREMENTS.md and ROADMAP.md amendments must stay in sync. If only one is updated, downstream phases (Phase 4) could pick up the stale wording and re-introduce the index-on-chip behavior. | mitigate | Both files are amended in the same Task 2 atomic step; acceptance criteria explicitly grep-check that the old phrase is gone from BOTH files and the new phrase is present in BOTH files. The bUnit test for Plan 03 (`UnresolvedChipRendersAsErrorChip_DA6`) further pins runtime behavior. |
</threat_model>

<verification>
- All Tasks 1-3 acceptance criteria pass.
- Task 4 checkpoint signed off by the developer.
- `dotnet build CookBot.sln` exits 0; `dotnet test tests/CookBot.Tests/CookBot.Tests.csproj` exits 0 (full Phase 1 + 2 + 3 suites green).
- `grep "TimerDetectionService.DetectTimers(ps.Text)" src/` returns no hits across the entire `src/` tree.
- `grep "user-facing index" .planning/REQUIREMENTS.md .planning/ROADMAP.md` returns no hits in either file.
- `.planning/phases/03-editor-ux-without-special-syntax/03-VERIFICATION.md` exists with a completed sign-off block.
</verification>

<success_criteria>
- Auto-write fallback in `RecipeService` deleted (EDITOR-03 final).
- REQUIREMENTS.md EDITOR-01 + ROADMAP.md SC#1 amended per D-A5 (no "user-facing index"; chips display name).
- Manual smoke checklist authored covering all five EDITOR-07 themes.
- Developer signs off the smoke checklist via the human-verify checkpoint.
- Phase 3 status in STATE.md is updated to "Complete" only after sign-off.
</success_criteria>

<output>
After completion, create `.planning/phases/03-editor-ux-without-special-syntax/03-04-SUMMARY.md` documenting:
- Final state of `src/CookBot.Application/Services/RecipeService.cs` (line count diff vs. before; confirmation of two ternary deletions).
- Sign-off result of the smoke checklist (pass / pass-with-deviations / fail-with-issues).
- Any deviations the developer noted during manual verification, including: any non-blocking accepted residual issues; any blocking issues that required a follow-up fix loop.
- Pointer to the completed `03-VERIFICATION.md` sign-off block.
</output>
