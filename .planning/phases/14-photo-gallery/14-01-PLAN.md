---
phase: 14-photo-gallery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/CookBot.Domain/Entities/RecipePhoto.cs
  - src/CookBot.Domain/Entities/Recipe.cs
  - src/CookBot.Infrastructure/Data/Configurations/RecipePhotoConfiguration.cs
  - src/CookBot.Infrastructure/Data/CookBotDbContext.cs
  - src/CookBot.Infrastructure/Migrations/{ts}_AddRecipePhotosTable.cs
  - src/CookBot.Application/DTOs/CookBotSettings.cs
  - tests/CookBot.Tests/Migration/RecipePhotoBackfillTests.cs
autonomous: true
requirements: [GALLERY-01]
must_haves:
  truths:
    - "Existing recipes with a non-empty Recipe.PhotoUrl get exactly one primary RecipePhoto row after migration (no data loss)"
    - "Recipes with NULL or empty PhotoUrl get zero RecipePhoto rows"
    - "Deleting a Recipe cascade-deletes its RecipePhoto rows (FK OnDelete Cascade)"
    - "MaxPhotosPerRecipe setting exists with default 10"
  artifacts:
    - path: "src/CookBot.Domain/Entities/RecipePhoto.cs"
      provides: "RecipePhoto POCO entity (Id, RecipeId, Url, Caption, SortOrder, IsPrimary, Recipe nav)"
      contains: "public bool IsPrimary"
    - path: "src/CookBot.Infrastructure/Data/Configurations/RecipePhotoConfiguration.cs"
      provides: "EF fluent config: FK cascade + composite index + max-lengths"
      contains: "OnDelete(DeleteBehavior.Cascade)"
    - path: "src/CookBot.Infrastructure/Migrations/{ts}_AddRecipePhotosTable.cs"
      provides: "RecipePhotos table + GALLERY-01 backfill SQL"
      contains: "INSERT INTO RecipePhotos"
    - path: "src/CookBot.Application/DTOs/CookBotSettings.cs"
      provides: "MaxPhotosPerRecipe clamped-int setting"
      contains: "MaxPhotosPerRecipe"
  key_links:
    - from: "src/CookBot.Domain/Entities/Recipe.cs"
      to: "RecipePhoto"
      via: "ICollection<RecipePhoto> Photos navigation"
      pattern: "ICollection<RecipePhoto>\\s+Photos"
    - from: "src/CookBot.Infrastructure/Data/CookBotDbContext.cs"
      to: "RecipePhoto"
      via: "DbSet<RecipePhoto> RecipePhotos"
      pattern: "DbSet<RecipePhoto>"
---

<objective>
Add the `RecipePhoto` relational child entity, its EF configuration, the table-creation migration with a `Recipe.PhotoUrl` backfill, and the `CookBotSettings.MaxPhotosPerRecipe` cap setting — the data foundation for the multi-photo gallery (GALLERY-01).

Purpose: Every subsequent plan (service, UI) reads/writes `RecipePhoto` rows and enforces the `MaxPhotosPerRecipe` cap. This wave produces the schema and contracts they build against. The backfill guarantees existing single-hero recipes are not disrupted (ROADMAP SC1).
Output: `RecipePhoto.cs`, `RecipePhotoConfiguration.cs`, the migration, the `Recipe.Photos` navigation, the `RecipePhotos` DbSet, the `MaxPhotosPerRecipe` setting, and a backfill regression test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/14-photo-gallery/14-CONTEXT.md
@.planning/phases/14-photo-gallery/14-RESEARCH.md
@.planning/phases/14-photo-gallery/14-PATTERNS.md

<interfaces>
<!-- Contracts this plan ESTABLISHES for downstream plans (02/03/04). -->
<!-- Domain entity (CookBot.Domain.Entities namespace): -->
public class RecipePhoto
{
    public int Id { get; set; }
    public int RecipeId { get; set; }
    public string Url { get; set; } = string.Empty;   // local "/uploads/{guid}.ext" OR external http(s)://
    public string? Caption { get; set; }
    public int SortOrder { get; set; }
    public bool IsPrimary { get; set; }
    public Recipe Recipe { get; set; } = null!;
}

<!-- Recipe.cs navigation (alongside existing RecipeIngredients / Tags collections): -->
public ICollection<RecipePhoto> Photos { get; set; } = new List<RecipePhoto>();

<!-- CookBotSettings field (Application.DTOs): -->
public int MaxPhotosPerRecipe { get; set; } = 10;   // clamped [1,20] by the CONSUMING service, not here
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RecipePhoto entity + Recipe navigation + DbSet + EF configuration</name>
  <files>src/CookBot.Domain/Entities/RecipePhoto.cs, src/CookBot.Domain/Entities/Recipe.cs, src/CookBot.Infrastructure/Data/CookBotDbContext.cs, src/CookBot.Infrastructure/Data/Configurations/RecipePhotoConfiguration.cs</files>
  <read_first>
    - src/CookBot.Domain/Entities/RecipeIngredient.cs (the POCO analog — copy its Id / RecipeId / `Recipe Recipe = null!` shape)
    - src/CookBot.Domain/Entities/Recipe.cs (nav collections at lines ~24-26 — add Photos after Tags)
    - src/CookBot.Infrastructure/Data/Configurations/RecipeIngredientConfiguration.cs (HasKey + HasIndex composite + property config)
    - src/CookBot.Infrastructure/Data/Configurations/RecipeConfiguration.cs (line 29 HasMany.WithOne.HasForeignKey.OnDelete(Cascade); line 19 PhotoUrl max-length 2048)
    - src/CookBot.Infrastructure/Data/CookBotDbContext.cs (lines 28-42: existing DbSet&lt;RecipeTag&gt; pattern + ApplyConfigurationsFromAssembly)
  </read_first>
  <behavior>
    - RecipePhoto exposes Id, RecipeId, Url, Caption (nullable), SortOrder, IsPrimary, and a `Recipe Recipe = null!` back-ref.
    - Recipe gains `ICollection<RecipePhoto> Photos = new List<RecipePhoto>()` after the Tags collection.
    - CookBotDbContext exposes `DbSet<RecipePhoto> RecipePhotos => Set<RecipePhoto>()` after RecipeTags.
    - RecipePhotoConfiguration configures: PK on Id; Url max 2048 + IsRequired; Caption max 512 (nullable, no IsRequired); SortOrder default 0; IsPrimary default false; composite index { RecipeId, SortOrder }; FK HasOne(p => p.Recipe).WithMany(r => r.Photos).HasForeignKey(p => p.RecipeId).OnDelete(DeleteBehavior.Cascade).
    - The FK + cascade are configured ON THE CHILD (RecipePhotoConfiguration), so RecipeConfiguration.cs is NOT modified.
  </behavior>
  <action>
    Create `RecipePhoto.cs` in `CookBot.Domain/Entities/` mirroring `RecipeIngredient.cs` with the shape in `<interfaces>` (D-14-03). Add `public ICollection<RecipePhoto> Photos { get; set; } = new List<RecipePhoto>();` to `Recipe.cs` after the Tags collection (D-14-02). Add `public DbSet<RecipePhoto> RecipePhotos => Set<RecipePhoto>();` to `CookBotDbContext.cs` after the RecipeTags DbSet — do NOT touch OnModelCreating (ApplyConfigurationsFromAssembly auto-discovers the config). Create `RecipePhotoConfiguration.cs` implementing IEntityTypeConfiguration&lt;RecipePhoto&gt; per the behavior block: Url HasMaxLength(2048).IsRequired() (matches Recipe.PhotoUrl), Caption HasMaxLength(512) (Claude's discretion ~512 per D-14-03), SortOrder HasDefaultValue(0), IsPrimary HasDefaultValue(false), HasIndex(p => new { p.RecipeId, p.SortOrder }), and the cascade FK configured on the child entity so RecipeConfiguration.cs stays untouched. No new NuGet packages.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/freeloves-cookbot && dotnet build src/CookBot.Infrastructure 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - dotnet build src/CookBot.Infrastructure exits 0.
    - RecipePhoto.cs contains "public bool IsPrimary" and "public Recipe Recipe { get; set; } = null!;".
    - Recipe.cs contains "ICollection<RecipePhoto> Photos".
    - CookBotDbContext.cs contains "DbSet<RecipePhoto> RecipePhotos".
    - RecipePhotoConfiguration.cs contains "OnDelete(DeleteBehavior.Cascade)" and "HasIndex(p => new { p.RecipeId, p.SortOrder })".
    - RecipeConfiguration.cs is unmodified (git diff shows no change to that file).
  </acceptance_criteria>
  <done>RecipePhoto entity, Recipe.Photos nav, RecipePhotos DbSet, and RecipePhotoConfiguration compile; FK cascade + composite index configured on the child entity.</done>
</task>

<task type="auto">
  <name>Task 2: RecipePhotos table migration with GALLERY-01 backfill + MaxPhotosPerRecipe setting</name>
  <files>src/CookBot.Infrastructure/Migrations/{ts}_AddRecipePhotosTable.cs, src/CookBot.Application/DTOs/CookBotSettings.cs</files>
  <read_first>
    - src/CookBot.Infrastructure/Migrations/20260516034336_AddRecipeTagTable.cs (the CreateTable + CreateIndex + migrationBuilder.Sql backfill + DropTable analog)
    - .planning/phases/14-photo-gallery/14-RESEARCH.md §"Pattern 3" (backfill SQL + why migration-SQL over DatabaseSeeder)
    - .planning/phases/14-photo-gallery/14-PATTERNS.md §"AddRecipePhotosTable" (column types, backfill SQL, SQLite keywords)
    - src/CookBot.Application/DTOs/CookBotSettings.cs (lines 23-26: DatabaseBackupRetention clamped-int precedent — add MaxPhotosPerRecipe immediately after)
    - src/CookBot.Infrastructure/Data/DatabaseSeeder.cs (confirm MigrateAsync runs the migration at startup; do NOT add backfill here — backfill lives in the migration Up())
  </read_first>
  <action>
    Generate the migration with `dotnet ef migrations add AddRecipePhotosTable --project src/CookBot.Infrastructure --startup-project src/CookBot.Web` (or hand-author following the AddRecipeTagTable shape if the EF CLI is unavailable). The Up() must: (1) CreateTable("RecipePhotos") with columns Id (INTEGER autoincrement), RecipeId (INTEGER, not null), Url (TEXT maxLength 2048, not null), Caption (TEXT maxLength 512, nullable), SortOrder (INTEGER, not null, defaultValue 0), IsPrimary (INTEGER, not null, defaultValue false), PK PK_RecipePhotos, and FK FK_RecipePhotos_Recipes_RecipeId → Recipes.Id ReferentialAction.Cascade; (2) CreateIndex("IX_RecipePhotos_RecipeId_SortOrder") on { RecipeId, SortOrder } (non-unique); (3) the GALLERY-01 backfill via migrationBuilder.Sql: INSERT INTO RecipePhotos (RecipeId, Url, SortOrder, IsPrimary) SELECT Id, PhotoUrl, 0, 1 FROM Recipes WHERE PhotoUrl IS NOT NULL AND PhotoUrl != ''. Down() drops the table. Backfill runs in Up() for atomicity with the schema change (D-14 Claude's discretion → migration-SQL, matching the v1.1 CanonicalDocumentJson backfill precedent). Then add `public int MaxPhotosPerRecipe { get; set; } = 10;` to CookBotSettings.cs immediately after DatabaseBackupRetention, with an XML-doc note that the value is clamped [1,20] at runtime by the consuming service (D-14-04-cap) — do NOT clamp in the getter.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/freeloves-cookbot && dotnet build 2>&1 | tail -3 && grep -rl "INSERT INTO RecipePhotos" src/CookBot.Infrastructure/Migrations/ && grep -c "MaxPhotosPerRecipe" src/CookBot.Application/DTOs/CookBotSettings.cs</automated>
  </verify>
  <acceptance_criteria>
    - dotnet build exits 0.
    - A migration file under src/CookBot.Infrastructure/Migrations/ contains "INSERT INTO RecipePhotos" with the WHERE "PhotoUrl IS NOT NULL AND PhotoUrl != ''" guard.
    - The migration creates index IX_RecipePhotos_RecipeId_SortOrder and FK FK_RecipePhotos_Recipes_RecipeId with ReferentialAction.Cascade.
    - CookBotSettings.cs contains "public int MaxPhotosPerRecipe { get; set; } = 10;".
    - No backfill logic added to DatabaseSeeder.cs (git diff shows DatabaseSeeder.cs unchanged).
  </acceptance_criteria>
  <done>Migration creates RecipePhotos table, composite index, cascade FK, and backfills one primary row per recipe with a non-empty PhotoUrl; MaxPhotosPerRecipe setting added with default 10.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Backfill + cascade regression test</name>
  <files>tests/CookBot.Tests/Migration/RecipePhotoBackfillTests.cs</files>
  <read_first>
    - tests/CookBot.Tests/Migration/CanonicalBackfillTests.cs (the migration/backfill test analog — how it builds a DbContext and asserts post-migrate state)
    - tests/CookBot.Tests/Services/RecipeServiceV4FieldsTests.cs (lines 1-55: in-memory SQLite DbContext setup via DbContextOptionsBuilder.UseSqlite("DataSource=:memory:") + OpenConnection + EnsureCreated, then seeding User/Cookbook)
    - src/CookBot.Domain/Entities/RecipePhoto.cs (the entity under test)
  </read_first>
  <behavior>
    - Test "Backfill creates one primary row per recipe with a non-empty PhotoUrl": seed 2 recipes with PhotoUrl set + 1 recipe with PhotoUrl null + 1 recipe with PhotoUrl "" → run the backfill SQL → assert exactly 2 RecipePhoto rows, each IsPrimary=true, SortOrder=0, Url matching the source PhotoUrl.
    - Test "Recipe delete cascades to RecipePhoto rows": seed a recipe + 3 RecipePhoto rows → delete the recipe → assert 0 RecipePhoto rows remain for that recipe.
    - Note: EnsureCreated does not run migrations, so the backfill INSERT SQL is executed directly in the test (the same INSERT statement the migration runs) to prove the SQL is correct and lossless; the cascade is proven via the EF model (DeleteBehavior.Cascade configured in RecipePhotoConfiguration).
  </behavior>
  <action>
    Create `RecipePhotoBackfillTests.cs` in `tests/CookBot.Tests/Migration/`, following the in-memory SQLite setup from RecipeServiceV4FieldsTests (DbContextOptionsBuilder.UseSqlite(":memory:") + OpenConnection + EnsureCreated). For the backfill test: seed recipes with mixed PhotoUrl states, execute the exact backfill statement `_db.Database.ExecuteSqlRaw("INSERT INTO RecipePhotos (RecipeId, Url, SortOrder, IsPrimary) SELECT Id, PhotoUrl, 0, 1 FROM Recipes WHERE PhotoUrl IS NOT NULL AND PhotoUrl != ''")`, then assert row count and per-row state via `_db.RecipePhotos`. For the cascade test: add a recipe + 3 RecipePhoto rows through the context, SaveChanges, remove the recipe, SaveChanges, assert `_db.RecipePhotos.Count(p => p.RecipeId == id) == 0`. Implement IDisposable to dispose the context.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/freeloves-cookbot && dotnet test --filter "FullyQualifiedName~RecipePhotoBackfillTests" 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - dotnet test --filter RecipePhotoBackfillTests passes (2 tests green).
    - Backfill test proves: recipes with null/empty PhotoUrl produce zero rows; recipes with a PhotoUrl produce exactly one IsPrimary=true row each.
    - Cascade test proves: deleting a recipe removes all its RecipePhoto rows.
  </acceptance_criteria>
  <done>Backfill SQL is proven lossless (no rows for null/empty PhotoUrl, one primary row per populated PhotoUrl) and cascade delete is proven to remove child rows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → DB | Backfill SQL writes the RecipePhotos table at startup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | Backfill SQL on RecipePhotos | mitigate | INSERT…SELECT from a trusted local column (Recipes.PhotoUrl); no user-supplied input in the SQL; WHERE guard excludes NULL/empty |
| T-14-02 | Denial of Service | Unbounded photo rows | mitigate | MaxPhotosPerRecipe setting (default 10) added here; enforced server-side in plan 14-03 |
| T-14-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs this plan — zero new NuGet packages (hard invariant). N/A for this wave. |
</threat_model>

<verification>
- `dotnet build` exits 0.
- `dotnet test --filter "FullyQualifiedName~RecipePhotoBackfillTests"` passes.
- Migration file contains the GALLERY-01 backfill INSERT with the NULL/empty guard.
- `Recipe.PhotoUrl` itself is NOT removed and NOT touched (D-14-01 — it remains the canonical primary mirror; this plan only adds the new table + backfill).
</verification>

<success_criteria>
- RecipePhoto entity, EF config, migration+backfill, DbSet, Recipe.Photos navigation, and MaxPhotosPerRecipe setting all exist and compile.
- Backfill is proven lossless by automated test; cascade delete proven.
- ROADMAP SC1 foundation in place: existing single-hero recipes have a backing primary RecipePhoto row.
</success_criteria>

<output>
Create `.planning/phases/14-photo-gallery/14-01-SUMMARY.md` when done.
</output>
