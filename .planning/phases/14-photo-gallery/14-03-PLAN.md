---
phase: 14-photo-gallery
plan: 03
type: execute
wave: 2
depends_on: ["14-01", "14-02"]
files_modified:
  - src/CookBot.Application/Services/RecipePhotoService.cs
  - src/CookBot.Application/Services/RecipeService.cs
  - src/CookBot.Web/Services/LocalRecipePhotoStorage.cs
  - src/CookBot.Application/DependencyInjection.cs
  - src/CookBot.Web/Program.cs
  - tests/CookBot.Tests/Services/RecipePhotoServiceTests.cs
autonomous: true
requirements: [GALLERY-02, GALLERY-03]
must_haves:
  truths:
    - "Adding photos persists RecipePhoto rows; the first photo is set primary and Recipe.PhotoUrl re-syncs to it"
    - "Setting a new primary clears the prior primary so exactly one IsPrimary row exists per recipe"
    - "Deleting the primary photo promotes the lowest-SortOrder remaining photo and re-syncs Recipe.PhotoUrl"
    - "Deleting a single photo whose Url is a local /uploads path removes the backing file; external URL rows leave no file"
    - "Deleting a recipe removes all its local photo files BEFORE the EF cascade drops the rows"
    - "AddPhotoAsync throws when the recipe is already at MaxPhotosPerRecipe (clamped [1,20])"
    - "A cross-user mutation (userId does not own the cookbook) is rejected"
  artifacts:
    - path: "src/CookBot.Application/Services/RecipePhotoService.cs"
      provides: "Gallery CRUD: Get/Add/SetPrimary/Reorder/Delete/UpdateCaption with one-primary invariant + cap + ownership"
      contains: "MaxPhotosPerRecipe"
    - path: "src/CookBot.Web/Services/LocalRecipePhotoStorage.cs"
      provides: "DeletePhysicalFile(url) guarded by AssertPathInsideUploadsDirectory"
      contains: "DeletePhysicalFile"
  key_links:
    - from: "src/CookBot.Application/Services/RecipePhotoService.cs"
      to: "RecipeService.SyncPrimaryPhotoUrlAsync"
      via: "called after every gallery mutation (D-14-01)"
      pattern: "SyncPrimaryPhotoUrlAsync"
    - from: "src/CookBot.Application/Services/RecipeService.cs"
      to: "LocalRecipePhotoStorage.DeletePhysicalFile"
      via: "DeleteAsync enumerates local photos before cascade"
      pattern: "DeletePhysicalFile"
---

<objective>
Build the service layer for the gallery: `RecipePhotoService` (add/reorder/set-primary/delete/caption with the one-primary invariant, server-side cap, and ownership checks), the `RecipeService.SyncPrimaryPhotoUrlAsync` re-sync that keeps `Recipe.PhotoUrl` + `CanonicalDocumentJson` pointing at the primary photo (D-14-01, P15), the `RecipeService.DeleteAsync` orphaned-file cleanup (D-14-11, P13), the `LocalRecipePhotoStorage.DeletePhysicalFile` helper, and the DI wiring.

Purpose: This is the heart of GALLERY-02 (authoring mutations) and GALLERY-03 (delete + cleanup). The UI plan (04) calls these methods; correctness of the one-primary invariant and file cleanup is proven here by service tests, not deferred to UAT.
Output: `RecipePhotoService.cs`, modified `RecipeService.cs` + `LocalRecipePhotoStorage.cs`, DI registrations, and a service test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/14-photo-gallery/14-CONTEXT.md
@.planning/phases/14-photo-gallery/14-RESEARCH.md
@.planning/phases/14-photo-gallery/14-PATTERNS.md
@.planning/phases/14-photo-gallery/14-01-SUMMARY.md
@.planning/phases/14-photo-gallery/14-02-SUMMARY.md

<interfaces>
<!-- Established by plan 01: RecipePhoto entity, Recipe.Photos nav, RecipePhotos DbSet, CookBotSettings.MaxPhotosPerRecipe. -->
<!-- Established by plan 02: PhotoUrlHeadValidator (UI calls it directly; this plan does not). -->

<!-- Contract this plan ESTABLISHES for the UI plan (04). All mutations IMMEDIATE-PERSIST
     (no batch-with-recipe-save staging) per RESEARCH §Open Questions #3: -->
namespace CookBot.Application.Services;
public class RecipePhotoService
{
    Task<IReadOnlyList<RecipePhoto>> GetPhotosAsync(int recipeId, int userId);                       // ordered by SortOrder
    Task<RecipePhoto> AddPhotoAsync(int recipeId, string url, int userId, string? caption = null);   // enforces cap; first photo → primary
    Task SetPrimaryAsync(int recipeId, int photoId, int userId);                                     // clears all, sets one
    Task ReorderAsync(int recipeId, int[] orderedPhotoIds, int userId);                              // re-assigns SortOrder
    Task DeleteAsync(int recipeId, int photoId, int userId);                                         // file-cleanup + promote-primary
    Task UpdateCaptionAsync(int recipeId, int photoId, string? caption, int userId);
}

<!-- New on RecipeService (only RecipeService writes CanonicalDocumentJson — P15): -->
internal Task SyncPrimaryPhotoUrlAsync(int recipeId);

<!-- New on LocalRecipePhotoStorage (Web layer — has IWebHostEnvironment / _uploadsDir): -->
public void DeletePhysicalFile(string url);   // guarded by AssertPathInsideUploadsDirectory; missing file = non-fatal
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RecipeService.SyncPrimaryPhotoUrlAsync + DeleteAsync file cleanup + LocalRecipePhotoStorage.DeletePhysicalFile</name>
  <files>src/CookBot.Application/Services/RecipeService.cs, src/CookBot.Web/Services/LocalRecipePhotoStorage.cs</files>
  <read_first>
    - src/CookBot.Application/Services/RecipeService.cs (constructor lines 17-31; CreateAsync PhotoUrl write lines 48-56; UpdateAsync PhotoUrl write lines 172-183; DeleteAsync lines 268-280 — the current gap with no file cleanup)
    - src/CookBot.Web/Services/LocalRecipePhotoStorage.cs (AssertPathInsideUploadsDirectory lines 118-134; the _uploadsDir + _logger private fields)
    - .planning/phases/14-photo-gallery/14-RESEARCH.md §"Pattern 5" (orphaned-file cleanup sequencing + DeletePhysicalFile body + the IRepository-no-Include note)
    - .planning/phases/14-photo-gallery/14-PATTERNS.md §"RecipeService.cs (MODIFIED)" + §"LocalRecipePhotoStorage.cs (MODIFIED)"
    - src/CookBot.Domain/Interfaces/IRepository.cs (confirms GetByIdAsync has NO Include overload — must load photos via DbContext directly)
  </read_first>
  <behavior>
    - SyncPrimaryPhotoUrlAsync(recipeId): load recipe; find the IsPrimary RecipePhoto (or, if none flagged, the lowest-SortOrder row as a defensive fallback); set recipe.PhotoUrl = primary?.Url (null when no photos); deserialize CanonicalDocumentJson, re-serialize with `doc with { PhotoUrl = recipe.PhotoUrl }`; persist via _recipeRepo.UpdateAsync. This is the ONLY place that writes Recipe.PhotoUrl/CanonicalDocumentJson for gallery sync (P15).
    - DeleteAsync(recipeId, userId): keep the existing ownership check; BEFORE calling _recipeRepo.DeleteAsync, query RecipePhotos for this recipe (AsNoTracking) and for each row whose Url starts with "/uploads/" call _photoStorage.DeletePhysicalFile(url) inside a try/catch that logs and continues (missing/failed file delete is non-fatal). External http(s) URLs are skipped (no local file). Then _recipeRepo.DeleteAsync(recipe) triggers the cascade.
    - DeletePhysicalFile(url): extract filename via Path.GetFileName(url); combine with _uploadsDir; AssertPathInsideUploadsDirectory(fullPath); if File.Exists → File.Delete + log; missing file → no-op (non-fatal).
  </behavior>
  <action>
    In `LocalRecipePhotoStorage.cs`, add the public `DeletePhysicalFile(string url)` method per `<behavior>`, calling the existing `AssertPathInsideUploadsDirectory` guard (PITFALL H2) and using the existing `_uploadsDir` / `_logger` fields. In `RecipeService.cs`, add `CookBotDbContext` and `LocalRecipePhotoStorage` as new constructor dependencies (both registered Scoped already). Add the `internal async Task SyncPrimaryPhotoUrlAsync(int recipeId)` helper per `<behavior>`, reusing the exact PhotoUrl/CanonicalDocumentJson write pattern from CreateAsync/UpdateAsync (D-14-01). Modify `DeleteAsync` to enumerate local-path photos via `_db.RecipePhotos.Where(p => p.RecipeId == recipeId).AsNoTracking().ToListAsync()` and delete their files (non-fatal try/catch, log-and-continue) BEFORE `_recipeRepo.DeleteAsync(recipe)` so the cascade does not orphan files (P13). Do NOT change CreateAsync/UpdateAsync PhotoUrl behavior — they remain the editor-text save path; SyncPrimaryPhotoUrlAsync handles gallery-driven re-sync. No new NuGet packages.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/freeloves-cookbot && dotnet build 2>&1 | tail -3 && grep -c "DeletePhysicalFile" src/CookBot.Web/Services/LocalRecipePhotoStorage.cs && grep -c "SyncPrimaryPhotoUrlAsync" src/CookBot.Application/Services/RecipeService.cs</automated>
  </verify>
  <acceptance_criteria>
    - dotnet build exits 0.
    - LocalRecipePhotoStorage.cs DeletePhysicalFile calls AssertPathInsideUploadsDirectory before File.Delete.
    - RecipeService.SyncPrimaryPhotoUrlAsync writes both recipe.PhotoUrl and recipe.CanonicalDocumentJson (the only gallery-sync canonical write — P15).
    - RecipeService.DeleteAsync enumerates RecipePhotos and deletes local files before _recipeRepo.DeleteAsync (the file-delete loop appears textually before the DeleteAsync call).
    - External http(s) URLs are skipped in the delete loop (a StartsWith "/uploads/" guard is present).
  </acceptance_criteria>
  <done>RecipeService can re-sync the primary mirror and clean up orphaned files before cascade; LocalRecipePhotoStorage can safely delete a local file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RecipePhotoService (gallery CRUD + one-primary invariant + cap + ownership) + DI wiring</name>
  <files>src/CookBot.Application/Services/RecipePhotoService.cs, src/CookBot.Application/DependencyInjection.cs, src/CookBot.Web/Program.cs</files>
  <read_first>
    - src/CookBot.Application/Services/RecipeService.cs (constructor injection pattern; ownership check lines 35-39 / 154-158 — copy verbatim; the SyncPrimaryPhotoUrlAsync added in Task 1)
    - .planning/phases/14-photo-gallery/14-RESEARCH.md §"Pattern 4" (method signatures, one-primary ExecuteUpdateAsync, cap enforcement, promote-on-delete)
    - .planning/phases/14-photo-gallery/14-PATTERNS.md §"RecipePhotoService.cs" (DbContext-direct queries; ownership; ExecuteUpdateAsync clear-all-then-set; cap with IOptions<CookBotSettings>)
    - src/CookBot.Application/DependencyInjection.cs (AddScoped<RecipeService>() — add AddScoped<RecipePhotoService>() next to it)
    - src/CookBot.Web/Program.cs (confirm LocalRecipePhotoStorage already AddScoped; no change unless wiring is missing)
  </read_first>
  <behavior>
    - Ownership: every mutation first loads the recipe + its cookbook and throws UnauthorizedAccessException if cookbook.UserId != userId (verbatim from RecipeService).
    - GetPhotosAsync: returns RecipePhotos for recipeId ordered by SortOrder.
    - AddPhotoAsync: enforce cap — clamp MaxPhotosPerRecipe to [1,20], throw InvalidOperationException("Maximum {n} photos per recipe.") when count >= cap; persist row with SortOrder = current max+1; if it is the first photo for the recipe, set IsPrimary=true; call RecipeService.SyncPrimaryPhotoUrlAsync.
    - SetPrimaryAsync: clear all IsPrimary for the recipe in one bulk update (ExecuteUpdateAsync), set the target IsPrimary=true, SaveChanges, then SyncPrimaryPhotoUrlAsync (prevents two-primary drift — RESEARCH Pitfall 3).
    - ReorderAsync: re-assign SortOrder from the ordered ID array (index = new SortOrder); validate every id belongs to recipeId; SaveChanges; SyncPrimaryPhotoUrlAsync (primary's Url unchanged but defensive).
    - DeleteAsync(recipeId, photoId, userId): load row; if Url is local /uploads call _photoStorage.DeletePhysicalFile; remove row; if it was primary, promote the lowest-SortOrder remaining row to IsPrimary; SaveChanges; SyncPrimaryPhotoUrlAsync.
    - UpdateCaptionAsync: set Caption (trim, allow null/empty) on the row; SaveChanges. (No primary re-sync needed; caption is not mirrored.)
  </behavior>
  <action>
    Create `RecipePhotoService.cs` in `CookBot.Application/Services/`. Inject `CookBotDbContext` (for Include/OrderBy/Where/ExecuteUpdateAsync — the generic IRepository has no such surface), `IRepository<Cookbook>` (ownership), `RecipeService` (for SyncPrimaryPhotoUrlAsync per D-14-01), `IOptions<CookBotSettings>` (the cap), `LocalRecipePhotoStorage` (single-photo file delete), and `ILogger<RecipePhotoService>`. Implement all six methods per `<behavior>`, copying the ownership-check block verbatim from RecipeService into a private helper used by every mutation. Enforce the cap server-side with the clamp `Math.Clamp(_settings.MaxPhotosPerRecipe, 1, 20)` (D-14-04-cap). Use the clear-all-then-set ExecuteUpdateAsync pattern for SetPrimaryAsync to guarantee the one-primary invariant. End every mutation with the SyncPrimaryPhotoUrlAsync call (except UpdateCaptionAsync). Register `services.AddScoped<RecipePhotoService>();` in `DependencyInjection.cs` next to `AddScoped<RecipeService>()`. Confirm `Program.cs` already registers `LocalRecipePhotoStorage` Scoped; only add a registration if one is missing. No new NuGet packages.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/freeloves-cookbot && dotnet build 2>&1 | tail -3 && grep -c "AddScoped<RecipePhotoService>" src/CookBot.Application/DependencyInjection.cs && grep -c "Math.Clamp" src/CookBot.Application/Services/RecipePhotoService.cs</automated>
  </verify>
  <acceptance_criteria>
    - dotnet build exits 0.
    - RecipePhotoService.cs contains all six methods (GetPhotosAsync, AddPhotoAsync, SetPrimaryAsync, ReorderAsync, DeleteAsync, UpdateCaptionAsync).
    - SetPrimaryAsync clears all IsPrimary before setting the target (ExecuteUpdateAsync clear-all present).
    - AddPhotoAsync uses Math.Clamp(_settings.MaxPhotosPerRecipe, 1, 20) and throws when at cap.
    - Every mutation except UpdateCaptionAsync calls SyncPrimaryPhotoUrlAsync.
    - The ownership check (cookbook.UserId != userId → UnauthorizedAccessException) runs before any DB write.
    - DependencyInjection.cs contains "AddScoped<RecipePhotoService>".
  </acceptance_criteria>
  <done>RecipePhotoService persists the gallery, enforces exactly one primary and the cap, checks ownership, deletes local files on single-photo delete, and re-syncs Recipe.PhotoUrl after every mutation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RecipePhotoService behavior tests (one-primary, cap, promote-on-delete, file cleanup, ownership)</name>
  <files>tests/CookBot.Tests/Services/RecipePhotoServiceTests.cs</files>
  <read_first>
    - tests/CookBot.Tests/Services/RecipeServiceV4FieldsTests.cs (lines 1-60: in-memory SQLite DbContext + Repository<T> + RecipeService construction — copy this fixture setup and extend it to construct RecipePhotoService)
    - tests/CookBot.Tests/Services/OwnershipTests.cs (the cross-user rejection assertion style — Assert.ThrowsAsync<UnauthorizedAccessException>)
    - src/CookBot.Application/Services/RecipePhotoService.cs (the unit under test — created in Task 2)
    - src/CookBot.Web/Services/LocalRecipePhotoStorage.cs (DeletePhysicalFile — the test uses a temp uploads dir + real file to prove deletion, or a fake/stub storage)
  </read_first>
  <behavior>
    - "First photo becomes primary and re-syncs PhotoUrl": AddPhotoAsync on an empty recipe → that row IsPrimary=true and recipe.PhotoUrl equals the added Url.
    - "Exactly one primary after SetPrimary": add 3, SetPrimaryAsync the 2nd → exactly one IsPrimary row (the 2nd) and recipe.PhotoUrl equals the 2nd's Url.
    - "Promote on primary delete": add 3 (1st primary), DeleteAsync the primary → the lowest-SortOrder remaining row is primary and PhotoUrl re-syncs to it.
    - "Cap enforced": with MaxPhotosPerRecipe=2 (via IOptions), adding a 3rd throws InvalidOperationException.
    - "Local file deleted on single delete": seed a real temp file under a temp uploads dir, add a "/uploads/x.jpg" row, DeleteAsync → file no longer exists; an external https row delete leaves no file error.
    - "Cross-user rejected": AddPhotoAsync with a userId that does not own the cookbook throws UnauthorizedAccessException.
    - "Reorder reassigns SortOrder": add 3, ReorderAsync with reversed ids → GetPhotosAsync returns them in the new order.
  </behavior>
  <action>
    Create `RecipePhotoServiceTests.cs` in `tests/CookBot.Tests/Services/`, extending the RecipeServiceV4FieldsTests in-memory SQLite fixture to also build a `RecipePhotoService` (wire a real RecipeService for SyncPrimaryPhotoUrlAsync, an `IOptions<CookBotSettings>` stub for the cap, and a `LocalRecipePhotoStorage` pointed at a temp uploads directory created in the constructor and cleaned in Dispose). Write the seven tests in `<behavior>`. For the file-cleanup test, write a real byte file into the temp uploads dir and assert `File.Exists` flips to false after delete; for the external-URL case assert no exception. Implement IDisposable to dispose the context and remove the temp directory.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/freeloves-cookbot && dotnet test --filter "FullyQualifiedName~RecipePhotoServiceTests" 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - dotnet test --filter RecipePhotoServiceTests passes (7 tests green).
    - The one-primary test proves a single IsPrimary row after SetPrimary; the promote test proves promotion after deleting the primary.
    - The cap test proves a 3rd add throws when the cap is 2.
    - The file-cleanup test proves a local /uploads file is physically deleted and an external-URL delete is a no-op (no error).
    - The cross-user test proves UnauthorizedAccessException.
  </acceptance_criteria>
  <done>The one-primary invariant, cap, promote-on-delete, local-file cleanup, reorder, and ownership are all proven by automated service tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → RecipePhotoService | userId-scoped mutation requests cross into the service |
| service → filesystem | physical file deletes under wwwroot/uploads/ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-06 | Elevation of Privilege | Cross-user gallery mutation (user A edits user B's photos) | mitigate | Ownership check (cookbook.UserId == userId) at the top of every RecipePhotoService mutation; proven by RecipePhotoServiceTests |
| T-14-07 | Elevation of Privilege | Path traversal on file delete | mitigate | DeletePhysicalFile routes through AssertPathInsideUploadsDirectory (PITFALL H2); only /uploads-prefixed Urls are touched |
| T-14-08 | Denial of Service | Unbounded photo upload fills disk | mitigate | Server-side cap Math.Clamp(MaxPhotosPerRecipe,1,20) enforced in AddPhotoAsync; proven by the cap test |
| T-14-09 | Tampering | Orphaned files after recipe/photo delete (P13) | mitigate | DeleteAsync (recipe) enumerates + deletes local files BEFORE cascade; single-photo DeleteAsync deletes file then row |
| T-14-10 | Tampering | Primary-mirror desync (P15 / canonical integrity) | mitigate | Only RecipeService.SyncPrimaryPhotoUrlAsync writes Recipe.PhotoUrl/CanonicalDocumentJson; called after every mutation |
| T-14-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs this plan — zero new NuGet packages (hard invariant). N/A. |
</threat_model>

<verification>
- `dotnet build` exits 0; `dotnet test --filter "FullyQualifiedName~RecipePhotoServiceTests"` passes.
- Gallery rows live ONLY in RecipePhoto; nothing writes the gallery array into CanonicalDocumentJson (hard invariant). The only canonical write is the single PhotoUrl mirror in SyncPrimaryPhotoUrlAsync.
- File cleanup runs before the EF cascade on recipe delete (P13).
</verification>

<success_criteria>
- RecipePhotoService implements GALLERY-02 mutations (add/reorder/caption/set-primary) and GALLERY-03 single-photo delete + file cleanup, with the one-primary invariant and the server-side cap.
- RecipeService.DeleteAsync cleans up orphaned files before cascade; SyncPrimaryPhotoUrlAsync keeps the primary mirror correct (Phase 13 JSON-LD image needs no change).
- All behavior proven by automated tests.
</success_criteria>

<output>
Create `.planning/phases/14-photo-gallery/14-03-SUMMARY.md` when done.
</output>
