---
phase: 7
plan: 2
title: Pantry + pantry dialogs migration
requirements: PA-01, PA-02, PA-03, PA-04
wave: 2
gap_closure: false
---

# Plan 07-02: Pantry + pantry dialogs migration

## Goal
Rewrite `PantryView.razor` against Phase 5 atoms per `screens/pantry.jsx`. Migrate pantry-related dialogs.

## Tasks

### Task 1: PantryView.razor (PA-01..04)
- TopBar right-slot actions: AI standardize / AI populate (HIDDEN when AI off — verify via `UserProfile.AiEnabled`) + Add item `CbButton accent`
- Sub-line: "{N} items · last sync {ago}"
- 4-tile summary strip (`<CbCard>` each, 16px padding): In stock (green) / Running low (warn) / Expiring this week (accent) / Out (gray). Each tile has a colored vertical bar + value (tabular 26px) + label
- Search row with `<CbInput>` + filter buttons (All / Low only / Expiring) using `<CbButton>` ghost variants
- Categorized stock cards. Each category is a section: eyebrow ("Dry goods") + item count, then a `<CbCard>` (no padding) with rows:
  - Grid `1fr 120px 110px 80px` (item name / qty / status chip / actions)
  - Status `<CbBadge Status="...">` per row (in-stock / low / expiring / out)
  - Actions: cart icon button + more icon button
  - `expiring` rows have `background: rgba(194,65,12,0.04)` (accent-soft tint)

### Task 2: Migrate pantry dialogs
For `AddPantryItemDialog.razor`, `CreateSharedPantryDialog.razor`, `ManagePantryMembersDialog.razor`, `AiPopulatePantryDialog.razor`, `AiStandardizePantryDialog.razor`:
- Migrate to `<CbDialog>` slots per Plan 07-01 Task 3 pattern
- Update call sites to use `CbDialogService.ShowAsync<...>(...)`
- AI-related dialogs (`AiPopulate`, `AiStandardize`) — preserve existing AI integration logic verbatim; only markup changes

### Task 3: Build verify + commit
- `dotnet build` clean
- `dotnet test` baseline
- Atomic commit

## Acceptance criteria
- PA-01..04 satisfied per ROADMAP.md SC#1
- AI-off contract: AI populate/standardize buttons hidden when AiEnabled=false
- Zero `Mud*` in PantryView.razor and migrated pantry dialogs
- `dotnet build` clean; `dotnet test` baseline
