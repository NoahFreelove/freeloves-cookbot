---
phase: 11-v1.3-uat-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/CookBot.Application/Services/RecipeUnitDisplayService.cs
  - src/CookBot.Application/DependencyInjection.cs
  - tests/CookBot.Tests/Services/RecipeUnitDisplayServiceTests.cs
autonomous: true
requirements: [CLEANUP-04]
must_haves:
  truths:
    - "A pure Application-layer service converts a (amount, unit) pair from one unit to a target unit system's display unit, returning the original unchanged when the unit is non-convertible (e.g. 'to taste', '1 clove')."
    - "A °C↔°F↔gas-mark temperature table converts StepTemperature values; 200°C displays as ~400°F (cooking-rounded), gas mark 6 = 200°C = 400°F."
    - "The canonical RecipeDocument is never mutated — conversion returns formatted display strings/values only."
    - "Reference values are unit-tested and pass: 100 g ≈ 3.53 oz, 1 lb = 16 oz = 453.6 g, 250 ml ≈ 1 US cup, 200°C ≈ 400°F (cook-rounded), gas 6 = 200°C."
  artifacts:
    - path: "src/CookBot.Application/Services/RecipeUnitDisplayService.cs"
      provides: "Display-time unit + temperature conversion for ingredients and step temperatures, keyed off UserProfile.UnitSystem"
      min_lines: 60
    - path: "tests/CookBot.Tests/Services/RecipeUnitDisplayServiceTests.cs"
      provides: "Round-trip + reference-value + non-convertible-passthrough tests"
      min_lines: 60
  key_links:
    - from: "RecipeUnitDisplayService"
      to: "IUnitConverter.Convert"
      via: "delegation for weight/volume"
      pattern: "IUnitConverter|_converter\\.Convert"
    - from: "RecipeUnitDisplayService"
      to: "FractionFormatter.Format"
      via: "cooking-rounded amount formatting"
      pattern: "FractionFormatter"
    - from: "DependencyInjection.AddApplication"
      to: "RecipeUnitDisplayService"
      via: "singleton registration"
      pattern: "RecipeUnitDisplayService"
---

<objective>
Build the CLEANUP-04 conversion engine as a pure, stateless Application-layer service. This is the net-new logic for unit-system display conversion: it wraps the EXISTING weight/volume `IUnitConverter` (g↔oz/lb, ml↔fl oz/cup already built) and ADDS the missing °C↔°F↔gas-mark temperature table modeled on `StepTemperature`'s `TemperatureUnit { F, C, Gas }` enum.

Purpose: Wave 2 (Plan 11-04) wires this single service into RecipeView / CookingMode / AiChat. Building + testing the engine in isolation first means the wiring plan only does call-site threading, never conversion math.

Output: `RecipeUnitDisplayService.cs` (Application), its DI registration, and a tests file proving the reference values from CONTEXT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-v1.3-uat-cleanup/11-CONTEXT.md
@.planning/phases/11-v1.3-uat-cleanup/11-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Contracts the executor needs — extracted from the codebase. Do NOT re-explore. -->

EXISTING weight/volume converter to REUSE (do not rebuild) — src/CookBot.Domain/Interfaces/IUnitConverter.cs:
```csharp
public interface IUnitConverter
{
    bool CanConvert(string fromUnit, string toUnit);
    double? Convert(double amount, string fromUnit, string toUnit);  // null = non-convertible
    bool IsVolume(string unit);
    bool IsWeight(string unit);
}
```
Implemented by src/CookBot.Application/Services/UnitConversionService.cs (handles g↔oz/lb, ml↔fl oz/cup; returns null for unknown/non-convertible units).

Unit string normalisation + non-convertible passthrough — CookBot.Application.Services.UnitParser:
```csharp
public static MeasurementUnit? TryParse(string unit);     // null for "clove"/"to taste"/unknown
public static string ToDisplayString(string unit);         // canonical name, else input unchanged
```

Cooking-rounded amount formatting (REUSE so output is "14 oz", not "13.9876 oz") — CookBot.Application.Services.FractionFormatter:
```csharp
public static string Format(double value);
```

Temperature model to REUSE for the new temp table — src/CookBot.Domain/Recipes/StepTemperature.cs:
```csharp
public sealed record StepTemperature { public required decimal Value { get; init; } public required TemperatureUnit Unit { get; init; } }
public enum TemperatureUnit { F, C, Gas }
```

Target preference enum — src/CookBot.Domain/Enums/UnitSystem.cs:
```csharp
public enum UnitSystem { Imperial, Metric, Canadian }   // default Imperial; Canadian is mixed (cups/tbsp + grams)
```

How UnitSystem→target-unit mapping is done today (mirror this for picking the destination unit per system) — src/CookBot.Application/Services/PromptBuilderService.cs L125-145 switches on profile.UnitSystem.

DI registration analog — src/CookBot.Application/DependencyInjection.cs:
```csharp
services.AddSingleton<IUnitConverter, UnitConversionService>();
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RecipeUnitDisplayService — ingredient (weight/volume) conversion + non-convertible passthrough</name>
  <files>src/CookBot.Application/Services/RecipeUnitDisplayService.cs, src/CookBot.Application/DependencyInjection.cs</files>
  <read_first>
    - src/CookBot.Application/Services/UnitConversionService.cs (the IUnitConverter impl to delegate to)
    - src/CookBot.Application/Services/UnitParser.cs L1-140 (TryParse / ToDisplayString — non-convertible passthrough)
    - src/CookBot.Application/Services/RecipeScalingService.cs (static display-formatter shape to mirror; calls FractionFormatter.Format)
    - src/CookBot.Application/Services/PromptBuilderService.cs L125-145 (UnitSystem→unit-family mapping precedent)
    - src/CookBot.Application/DependencyInjection.cs (registration site)
  </read_first>
  <behavior>
    - FormatIngredientAmount(amount, unit, UnitSystem target): a weight in grams with target=Imperial returns ounces/pounds display; a volume in ml with target=Imperial returns fl oz/cup display; same-family same-unit is unchanged.
    - Non-convertible unit ("to taste", "1 clove", "a pinch", unit empty/whitespace, amount <= 0) returns the original amount+unit unchanged (delegates to UnitParser passthrough, never throws).
    - Reference values pass: 100 g → ≈3.53 oz, 1 lb → 16 oz, 453.6 g → 1 lb, 250 ml → ≈1 US cup. Output uses FractionFormatter for cooking rounding (no "13.9876 oz").
    - Canonical inputs are never mutated — method takes primitives / returns a formatted string; no RecipeDocument/IngredientEntry write path.
  </behavior>
  <action>
    Create `RecipeUnitDisplayService` in namespace `CookBot.Application.Services`, constructor-injected with `IUnitConverter`. Mirror the role of the existing `RecipeScalingService` static helper but as an injectable instance (it needs `IUnitConverter`). Add a per-`UnitSystem` target-unit selection helper that picks the destination weight unit and destination volume unit for `Imperial` / `Metric` / `Canadian` — reuse the family logic from `PromptBuilderService` (Canadian = grams for weight, cups/tbsp for volume). Expose `FormatIngredientAmount(double amount, string unit, UnitSystem target)` returning the converted display string: parse the source unit with `UnitParser.TryParse`; if null OR `IUnitConverter.Convert` returns null, fall back to the original `amount` + `UnitParser.ToDisplayString(unit)` (passthrough). On success, run the converted numeric through `FractionFormatter.Format` and append `UnitParser.ToDisplayString` of the chosen target unit. Do NOT scale amounts here (scaling stays in `RecipeScalingService`). Register in `DependencyInjection.AddApplication` as `services.AddSingleton<RecipeUnitDisplayService>();` adjacent to the existing `AddSingleton<IUnitConverter, UnitConversionService>()` line.
  </action>
  <verify>
    <automated>dotnet build src/CookBot.Application/CookBot.Application.csproj -c Debug 2>&1 | grep -q "Build succeeded" && echo BUILD_OK</automated>
  </verify>
  <acceptance_criteria>
    - File `src/CookBot.Application/Services/RecipeUnitDisplayService.cs` exists and declares `public class RecipeUnitDisplayService` with a constructor taking `IUnitConverter`.
    - `grep -c "RecipeUnitDisplayService" src/CookBot.Application/DependencyInjection.cs` returns >= 1 (singleton registration present).
    - `grep -n "FractionFormatter" src/CookBot.Application/Services/RecipeUnitDisplayService.cs` matches (cooking rounding is wired).
    - No write to any `RecipeDocument` / `IngredientEntry` / `CanonicalDocumentJson` member: `grep -nE "CanonicalDocumentJson\\s*=|IngredientEntry.*=.*with|\\.Amount\\s*=" src/CookBot.Application/Services/RecipeUnitDisplayService.cs` returns no mutation hits.
    - `dotnet build src/CookBot.Application/CookBot.Application.csproj` succeeds.
  </acceptance_criteria>
  <done>The service compiles, is DI-registered as a singleton, converts ingredient weight/volume to a target UnitSystem, and passes non-convertible units through unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Temperature conversion table (°C↔°F↔gas) + RecipeUnitDisplayServiceTests</name>
  <files>src/CookBot.Application/Services/RecipeUnitDisplayService.cs, tests/CookBot.Tests/Services/RecipeUnitDisplayServiceTests.cs</files>
  <read_first>
    - src/CookBot.Domain/Recipes/StepTemperature.cs (Value + TemperatureUnit { F, C, Gas } — reuse this enum, do not invent a parallel one)
    - tests/CookBot.Tests/Services (existing xUnit conventions — pick any *Tests.cs as a structure template)
    - src/CookBot.Application/Services/RecipeUnitDisplayService.cs (the file Task 1 created)
  </read_first>
  <behavior>
    - FormatTemperature(StepTemperature, UnitSystem target): a C value with target=Imperial returns °F display; an F value with target=Metric returns °C display; gas mark maps via the standard table (gas 6 = 200°C = 400°F).
    - Reference values pass: 200°C → 400°F (cook-rounded, accept 392–400 tolerance per CONTEXT), gas mark 6 → 200°C and → 400°F, 180°C → 350°F (cook-round).
    - Temperatures are NEVER scaled by servings (CLAUDE.md guardrail) — conversion is unit-only.
    - Gas-mark target only applies where it is the meaningful display (e.g. UK); for Imperial target a Gas input converts to °F, for Metric target a Gas input converts to °C.
  </behavior>
  <action>
    Add a temperature conversion path to `RecipeUnitDisplayService`. Implement a private static table mapping `TemperatureUnit.Gas` marks to °C reference points (gas 1=140, 2=150, 3=170, 4=180, 5=190, 6=200, 7=220, 8=230, 9=240) and the linear °C↔°F formula (`F = C * 9/5 + 32`). Expose `FormatTemperature(StepTemperature temp, UnitSystem target)` returning a display string with the converted value + unit symbol (`°F` / `°C` / `Gas N`), choosing the target scale from `UnitSystem` (Imperial→F, Metric→C; Canadian→C for oven temps per PromptBuilderService precedent). Apply cooking rounding (round °F to nearest 5 or 25 as appropriate; round °C to nearest 5) so 200°C displays as 400°F not 392°F. Reuse `TemperatureUnit` from `StepTemperature.cs` — do NOT declare a second temperature enum. Then create `tests/CookBot.Tests/Services/RecipeUnitDisplayServiceTests.cs` with xUnit `[Theory]`/`[Fact]` cases covering every reference value from CONTEXT §Specific Ideas (100 g≈3.53 oz, 1 lb=16 oz=453.6 g, 250 ml≈1 cup, 200°C≈400°F, gas 6=200°C=400°F) plus non-convertible passthrough cases ("to taste", "1 clove", "a pinch", null/empty unit). Use `UnitConversionService` as the real `IUnitConverter` in the test (not a mock) so the full path is exercised.
  </action>
  <verify>
    <automated>dotnet test tests/CookBot.Tests/CookBot.Tests.csproj --filter "FullyQualifiedName~RecipeUnitDisplayService" 2>&1 | grep -E "Passed!|Failed!" | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - `tests/CookBot.Tests/Services/RecipeUnitDisplayServiceTests.cs` exists with at least 8 assertions: the 5 named reference values + at least 3 non-convertible passthrough cases.
    - `grep -nE "TemperatureUnit\\.(F|C|Gas)" tests/CookBot.Tests/Services/RecipeUnitDisplayServiceTests.cs` matches (temperature path is tested with the reused enum).
    - `grep -cE "enum TemperatureUnit" src/CookBot.Application/Services/RecipeUnitDisplayService.cs` returns 0 (no duplicate temperature enum declared).
    - `dotnet test --filter "FullyQualifiedName~RecipeUnitDisplayService"` exits 0 (all reference-value and passthrough tests pass).
  </acceptance_criteria>
  <done>Temperature conversion (°C↔°F↔gas) is added to the service, reuses StepTemperature's enum, and every CONTEXT reference value plus non-convertible passthrough case passes under `dotnet test`.</done>
</task>

</tasks>

<verification>
- `dotnet build` of the solution succeeds.
- `dotnet test --filter "FullyQualifiedName~RecipeUnitDisplayService"` exits 0.
- No new package added (System.Text.Json only; reuses existing IUnitConverter, FractionFormatter, UnitParser).
- `RecipeUnitDisplayService` performs zero writes to canonical types.
</verification>

<success_criteria>
A DI-registered, unit-tested `RecipeUnitDisplayService` converts ingredient amounts (weight + volume) and step temperatures to a target `UnitSystem` for DISPLAY only, passes non-convertible units through unchanged, and never mutates the canonical RecipeDocument. The Wave 2 wiring plan can call it without writing any conversion math.
</success_criteria>

<output>
Create `.planning/phases/11-v1.3-uat-cleanup/11-01-SUMMARY.md` when done.
</output>
