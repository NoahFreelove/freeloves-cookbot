---
phase: 11-v1.3-uat-cleanup
plan: 03
type: execute
wave: 2
depends_on: ["11-02"]
files_modified:
  - src/CookBot.Web/wwwroot/css/cookbot-design.css
  - src/CookBot.Web/Components/Layout/MainLayout.razor
  - src/CookBot.Web/Components/Layout/Sidebar.razor
autonomous: false
# WAVE 2 reason: shares cookbot-design.css with 11-02 (Wave 1). Sequenced after
# 11-02 to avoid a same-wave write collision on that stylesheet. No dependency on
# 11-02's content — purely a file-ownership ordering.
requirements: [CLEANUP-03]
must_haves:
  truths:
    - "At default desktop zoom the sidebar Profile row at the bottom of the sidebar is fully visible — not clipped."
    - "The --cream body background extends to the full sidebar height; there is no exposed body background gap below the cream column."
    - "The fix corrects the height/grid inheritance chain; no overflow:hidden / clip hack is used to mask the symptom."
  artifacts:
    - path: "src/CookBot.Web/wwwroot/css/cookbot-design.css"
      provides: ".cb-shell height resolves to a real viewport height so the grid row, .side, and the --cream main column all reach the viewport bottom"
      contains: "cb-shell"
  key_links:
    - from: "MainLayout.razor .cb-shell wrapper (height:100vh)"
      to: "cookbot-design.css .cb-shell rule"
      via: "resolved viewport height (100vh / min-height chain)"
      pattern: "cb-shell"
    - from: ".cb-shell .side"
      to: "Sidebar Profile NavRow"
      via: "full-height flex column so the bottom row is not cut"
      pattern: "\\.side"
---

<objective>
Close CLEANUP-03: the sidebar Profile row is clipped and the `--cream` body background stops short of the sidebar bottom. Per PATTERNS, the root cause is the height-inheritance chain — the wrapper is `height:100vh` but `.cb-shell { height:100% }` only resolves against a fully-sized ancestor chain; when it collapses, the grid row (and therefore `.side` and the `--cream` main column) does not fill the viewport, exposing the body background and cutting the bottom Profile NavRow.

Fix the inheritance, not the symptom — do NOT mask with `overflow` hacks.
Output: a corrected `.cb-shell` height so the grid row, `.side`, and main column all reach the viewport bottom.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-v1.3-uat-cleanup/11-CONTEXT.md
@.planning/phases/11-v1.3-uat-cleanup/11-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Exact lines — extracted from PATTERNS. -->

Wrapper (height:100vh on the div) — MainLayout.razor ~L26:
  <div class="cb cb-shell @(_drawerCollapsed ? "is-collapsed" : "")" style="height:100vh;">

Grid container (height:100% — the weak link) — cookbot-design.css L218-223:
  .cb-shell { display: grid; grid-template-columns: 232px 1fr; height: 100%; background: var(--cream); }

Sidebar grid cell — cookbot-design.css L230-237:
  .cb-shell .side { background: var(--paper-2); border-right: 1px solid var(--line); padding: 18px 14px; display: flex; flex-direction: column; gap: 4px; }

Sidebar <aside> (own width:232px, NO height) — Sidebar.razor ~L15; clipped bottom Profile NavRow pushed by flex spacer — Sidebar.razor L33-35:
  <div style="flex:1;"></div>
  <NavRow IconName="@Icon.Names.User" Label="Profile" Href="/profile" MatchMode="NavLinkMatch.All" />
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the .cb-shell height-inheritance chain</name>
  <files>src/CookBot.Web/wwwroot/css/cookbot-design.css, src/CookBot.Web/Components/Layout/MainLayout.razor, src/CookBot.Web/Components/Layout/Sidebar.razor</files>
  <read_first>
    - .planning/phases/11-v1.3-uat-cleanup/11-PATTERNS.md (CLEANUP-03 section — root-cause chain + "do NOT mask with overflow hacks")
    - src/CookBot.Web/wwwroot/css/cookbot-design.css L215-240 (.cb-shell + .cb-shell .side)
    - src/CookBot.Web/Components/Layout/MainLayout.razor L20-30 (the height:100vh wrapper)
    - src/CookBot.Web/Components/Layout/Sidebar.razor L10-36 (the <aside>, flex spacer, and Profile NavRow)
  </read_first>
  <action>
    Make `.cb-shell` resolve to a real viewport height. Preferred fix: change `.cb-shell { height: 100% }` to `min-height: 100vh` (so the grid row always fills at least the viewport and grows with content), OR keep `height:100vh` on the rule and verify the wrapper/ancestor chain (`html, body, #app/app-host`) is `height:100%` end-to-end. Confirm `.cb-shell .side` stretches to the full grid-row height (a CSS grid cell stretches by default — once the row is full height the `.side` and the bottom Profile NavRow follow). Ensure the right-hand main column carries the `--cream` background to the same full height (it inherits from `.cb-shell { background: var(--cream) }`; verify nothing overrides it short). Remove the redundant inline `style="height:100vh;"` from MainLayout's `.cb-shell` wrapper ONLY if the CSS rule now owns the height (avoid two competing height sources); otherwise leave it. Do NOT add `overflow:hidden`, `overflow:clip`, or a fixed pixel height to mask the gap. If Sidebar's `<aside>` needs `height:100%` to fill its grid cell, add it there — but first confirm via the grid stretch whether it is needed.
  </action>
  <verify>
    <automated>grep -A6 "\.cb-shell {" src/CookBot.Web/wwwroot/css/cookbot-design.css | grep -vE '^\s*/?\*' | grep -qE "min-height: ?100vh|height: ?100vh" && ! grep -A8 "\.cb-shell {" src/CookBot.Web/wwwroot/css/cookbot-design.css | grep -qE "overflow: ?(hidden|clip)" && echo HEIGHT_OK</automated>
  </verify>
  <acceptance_criteria>
    - The `.cb-shell` rule in cookbot-design.css uses `min-height:100vh` or `height:100vh` (no longer a bare `height:100%` that collapses).
    - No `overflow:hidden` / `overflow:clip` was added to `.cb-shell`, `.side`, or the main column to mask the gap (grep confirms absence in the edited rules).
    - The `--cream` background is still applied to `.cb-shell` (or the main column) — `grep -n "var(--cream)" cookbot-design.css` still matches the shell/main rule.
    - `dotnet build src/CookBot.Web/CookBot.Web.csproj` succeeds.
  </acceptance_criteria>
  <done>The shell grid resolves to full viewport height; the sidebar (`.side`) and main `--cream` column both reach the viewport bottom, and the Profile NavRow is fully visible. No overflow hack was used.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify CLEANUP-03 — sidebar Profile row + cream background full height</name>
  <what-built>CLEANUP-03 height-inheritance fix: sidebar reaches full viewport height, Profile row fully visible, --cream background no longer ends short.</what-built>
  <action>Checkpoint task — no autonomous code. Pause for the human to follow the steps in &lt;how-to-verify&gt; and confirm. This is verification of work completed by the preceding auto task(s) in this plan; do not write code here.</action>
  <how-to-verify>
    1. `./run.sh`, open http://localhost:7000/ at default desktop zoom on a normal-height window (e.g. 1080p).
    2. Look at the BOTTOM of the left sidebar: the Profile row (User icon + "Profile" text) must be fully visible — the icon is NOT clipped, the text reads "Profile" not "rofile".
    3. Look at where the sidebar bottom meets the main content area: the cream (`--cream`) background of the main column must extend all the way to the bottom of the sidebar — there must be NO strip of a different (body) background showing below the cream.
    4. Collapse the sidebar (drawer toggle) and confirm the same: no clipped row, no background gap.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the remaining clip / gap.</resume-signal>
</task>

</tasks>

<verification>
- `dotnet build` succeeds.
- `.cb-shell` uses a resolved viewport height (`min-height:100vh` / `height:100vh`), not a collapsing `height:100%`.
- No `overflow` masking hack added.
- `--cream` still applied to the shell/main column.
</verification>

<success_criteria>
At default desktop zoom the sidebar Profile row is fully visible and the `--cream` body background reaches the full sidebar height, fixed by correcting the height/grid inheritance rather than masking it.
</success_criteria>

<output>
Create `.planning/phases/11-v1.3-uat-cleanup/11-03-SUMMARY.md` when done.
</output>
