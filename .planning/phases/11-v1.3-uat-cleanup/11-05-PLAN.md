---
phase: 11-v1.3-uat-cleanup
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - tests/uat-harness/package.json
  - tests/uat-harness/README.md
  - tests/uat-harness/run.mjs
  - tests/uat-harness/lib/session.mjs
  - tests/uat-harness/lib/app.mjs
  - tests/uat-harness/tests/test5-reparenting.mjs
  - tests/uat-harness/tests/test7-responsive.mjs
  - tests/uat-harness/tests/test4-validation-fail.mjs
autonomous: false
# WAVE 1: the harness CODE has no file overlap with any other plan (own tree under
# tests/uat-harness/ + a .gitignore append). It can be WRITTEN in Wave 1. NOTE for
# execute-phase: the harness's pass/fail RUN against CLEANUP-01/02 is only
# meaningful AFTER 11-02 lands — the final checkpoint run is sequenced last.
requirements: [UATAUTO-01]
user_setup:
  - service: chromium
    why: "Playwright drives a real chromium; reuse the system snap chromium to avoid a large download"
    env_vars: []
    dashboard_config:
      - task: "System chromium already present at /snap/bin/chromium (confirmed 2026-06-05). If snap confinement fights Playwright at run time, fall back to `npx playwright install chromium`."
        location: "local machine"
must_haves:
  truths:
    - "A Node/Playwright harness under tests/uat-harness/ launches/uses the app on :7000 and drives chromium through Phase 10 UAT Test 5 and Test 7 with real pass/fail assertions."
    - "The harness establishes a trusted-LAN session (default Home Chef user, no password) before driving the tests."
    - "The harness is isolated from the .NET solution build: it is NOT in FreelovesCookBot.sln, and CookBot.Tests gains NO Playwright/Selenium package."
    - "UAT Test 4 (validation-fail fallback) is EITHER fault-injected OR explicitly recorded as a manual/deferred check — never reported as a fake pass."
    - "The harness emits a per-test pass/fail summary and a non-zero exit code on any failure."
  artifacts:
    - path: "tests/uat-harness/run.mjs"
      provides: "Entry point: launches/awaits app, establishes session, runs Test 5 + Test 7 (+ Test 4 disposition), prints summary, exits non-zero on failure"
      min_lines: 40
    - path: "tests/uat-harness/lib/session.mjs"
      provides: "Trusted-LAN session establishment (Home Chef default / sessionStorage user id)"
    - path: "tests/uat-harness/tests/test5-reparenting.mjs"
      provides: "Cookbook reparenting flow + assertion"
    - path: "tests/uat-harness/tests/test7-responsive.mjs"
      provides: "719px responsive-collapse assertions (TopBar hidden, inline fallback visible, Edit present, layout stacked)"
    - path: "tests/uat-harness/README.md"
      provides: "How to run + the Test 4 honesty disposition"
    - path: ".gitignore"
      provides: "tests/uat-harness/node_modules ignored"
      contains: "uat-harness"
  key_links:
    - from: "run.mjs"
      to: "app on :7000 (/healthz)"
      via: "readiness poll before driving tests"
      pattern: "healthz|7000"
    - from: "session.mjs"
      to: "sessionStorage cookbot_current_user"
      via: "trusted-LAN user establishment"
      pattern: "cookbot_current_user|Home Chef"
    - from: "test7-responsive.mjs"
      to: ".recipe-actions-inline-fallback / .topbar-right-slot"
      via: "CSS-hook assertions at 719px"
      pattern: "recipe-actions-inline-fallback"
---

<objective>
Stand up UATAUTO-01: a reusable Playwright/Node browser-UAT harness, isolated from the shipped app and the .NET solution build, that drives chromium through the two still-open Phase 10 UAT reruns hands-free — Test 5 (cookbook reparenting, POLISH-01) and Test 7 (responsive collapse, POLISH-04 + CLEANUP-01/02). It must establish a trusted-LAN session (no login form — default "Home Chef" user) and emit a clear pass/fail summary with a non-zero exit code on failure.

Honesty constraint (NON-NEGOTIABLE): UAT Test 4 (validation-fail fallback) CANNOT be triggered while the AI happy-path succeeds. The harness MUST EITHER expose a fault-injection seam that forces a schema-mismatch AI response so `RawRecipeEditorDialog` opens, OR record Test 4 as a manual/deferred check in the harness output + README. Do NOT fake a Test 4 pass.

Output: a `tests/uat-harness/` tree (own package.json, gitignored node_modules), a README, and a runner that exits 0 only when the automated tests genuinely pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-v1.3-uat-cleanup/11-CONTEXT.md
@.planning/phases/11-v1.3-uat-cleanup/11-PATTERNS.md
@.planning/phases/10-qol-polish-consumer-surfaces/10-HUMAN-UAT.md
@CLAUDE.md

<interfaces>
<!-- App-side hooks the harness scripts against — extracted from PATTERNS + source. -->

App launch — run.sh → `dotnet run --project src/CookBot.Web`. Binds http://localhost:7000 (launchSettings applicationUrl). Readiness: GET http://localhost:7000/healthz returns 200 (Program.cs app.MapHealthChecks("/healthz")).

Session (trusted-LAN, NO login form):
  - CurrentUserService.CurrentUserId is circuit-scoped, restored from sessionStorage["cookbot_current_user"] in MainLayout.razor L82-88.
  - If none set, MainLayout auto-creates/defaults to a "Home Chef" admin.
  - User picker = TopBar CbDropdown (TopBar.razor L52-58, each item Label="@u.DisplayName"); switching writes sessionStorage["cookbot_current_user"] then cookbot.hardReloadTo("/").
  - VerifyPasswordAsync returns true when PasswordHash == null (default Home Chef has no password).
  - Harness recipe: open http://localhost:7000/, let "Home Chef" auto-establish (or set sessionStorage["cookbot_current_user"] to a known id and reload). Prefer visible text / data hooks over brittle CSS.

UAT Test 5 (reparenting, POLISH-01): /recipes/{id} → Edit → change the Cookbook CbSelect (RecipeEditor.razor L206-225, a CbSelect<int> of owned cookbooks) → click Save (button text "Save", RecipeEditor L344-347) → assert nav to destination cookbook page + recipe gone from the origin cookbook.

UAT Test 7 (responsive, POLISH-04 + CLEANUP-01/02): load /recipes/{id} at viewport width 719px → assert `.topbar-right-slot` hidden + `.recipe-actions-inline-fallback` visible + the Edit button text present in the fallback row + (post-CLEANUP-02) the hero/body grids stacked to a single column. CSS hooks live in cookbot-design.css L716-722 (the 720px media block) and the classes added by Plan 11-02 (recipe-hero, recipe-body-grid).

UAT Test 4 (validation-fail, QOL-04): RawRecipeEditorDialog only opens when the AI returns malformed output; AiChat.razor L312 gates the canvas on `_lastStructuredRecipe.Ok == true`. Cannot be reached on the happy path → fault-injection seam OR manual/deferred record (DO NOT fake).

Phase 10 UAT expected behaviors — 10-HUMAN-UAT.md Tests 5 & 7 (and Test 4 deferred note in §Gaps).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harness scaffold — package.json, .gitignore, session + app-readiness libs, README</name>
  <files>.gitignore, tests/uat-harness/package.json, tests/uat-harness/README.md, tests/uat-harness/lib/app.mjs, tests/uat-harness/lib/session.mjs</files>
  <read_first>
    - .planning/phases/11-v1.3-uat-cleanup/11-PATTERNS.md (UATAUTO-01 section — app launch, port, /healthz, session recipe; "NONE" analog → use Playwright docs)
    - run.sh and src/CookBot.Web/Properties/launchSettings.json (launch + :7000)
    - src/CookBot.Web/Components/Layout/MainLayout.razor L55-100 (Home Chef default + sessionStorage user restore)
    - src/CookBot.Web/Components/Layout/TopBar.razor L52-58, L160-185 (CbDropdown user picker + hardReloadTo)
    - .gitignore (append target — confirm no existing node_modules rule)
  </read_first>
  <action>
    Create `tests/uat-harness/package.json` with `"type": "module"`, a `playwright` devDependency, and a `"test"` script that runs `node run.mjs`. Append `tests/uat-harness/node_modules/` to the repo `.gitignore`. Do NOT add the harness to `FreelovesCookBot.sln` and do NOT touch `tests/CookBot.Tests/`. Create `lib/app.mjs` exporting a readiness helper that polls `GET http://localhost:7000/healthz` until 200 (timeout ~60s) — and, per Claude's discretion in CONTEXT, EITHER spawns `./run.sh` and waits, OR assumes the app is already running on :7000 (document whichever you choose in the README). Create `lib/session.mjs` exporting `establishSession(page)` that opens `http://localhost:7000/`, lets the default "Home Chef" user auto-establish (and/or sets `sessionStorage["cookbot_current_user"]` to a known id then reloads via the app's `cookbot.hardReloadTo` or a plain reload), and returns once a logged-in surface is visible. Configure Playwright's chromium to reuse `/snap/bin/chromium` via `executablePath` (fallback documented: `npx playwright install chromium`). Write `README.md` documenting: prerequisites (node, chromium), how to run (`npm install` then `npm test`), what it asserts (Tests 5, 7), and the Test 4 disposition (filled in Task 2/3). Use stable visible-text / data hooks, not brittle CSS, for session + nav.
  </action>
  <verify>
    <automated>test -f tests/uat-harness/package.json && grep -q "playwright" tests/uat-harness/package.json && grep -q "uat-harness" .gitignore && grep -qE "healthz|7000" tests/uat-harness/lib/app.mjs && grep -qE "cookbot_current_user|Home Chef|localhost:7000" tests/uat-harness/lib/session.mjs && node --check tests/uat-harness/lib/app.mjs && node --check tests/uat-harness/lib/session.mjs && echo SCAFFOLD_OK</automated>
  </verify>
  <acceptance_criteria>
    - `tests/uat-harness/package.json` exists with `"type":"module"`, a `playwright` devDependency, and a `test` script invoking `run.mjs`.
    - `.gitignore` contains a `tests/uat-harness/node_modules` (or `uat-harness/node_modules`) entry.
    - `tests/uat-harness/` is NOT referenced in `FreelovesCookBot.sln` (`grep -c uat-harness FreelovesCookBot.sln` = 0) and `tests/CookBot.Tests/CookBot.Tests.csproj` has no Playwright/Selenium PackageReference.
    - `lib/app.mjs` polls `/healthz` on :7000; `lib/session.mjs` references the `cookbot_current_user` sessionStorage key or the Home Chef default.
    - `node --check` passes on both lib files (valid ES module syntax).
  </acceptance_criteria>
  <done>The isolated harness scaffold exists (package.json, gitignored node_modules, README, app-readiness + session libs), is not in the .NET solution, and adds no package to CookBot.Tests.</done>
</task>

<task type="auto">
  <name>Task 2: Test 5 (reparenting) + Test 7 (responsive) + Test 4 disposition + runner</name>
  <files>tests/uat-harness/run.mjs, tests/uat-harness/tests/test5-reparenting.mjs, tests/uat-harness/tests/test7-responsive.mjs, tests/uat-harness/tests/test4-validation-fail.mjs, tests/uat-harness/README.md</files>
  <read_first>
    - .planning/phases/11-v1.3-uat-cleanup/11-PATTERNS.md (UATAUTO-01 — the two UAT flows + Test 4 honesty rule)
    - .planning/phases/10-qol-polish-consumer-surfaces/10-HUMAN-UAT.md (Tests 5 & 7 expected; Test 4 §Gaps deferred note)
    - src/CookBot.Web/Components/Pages/RecipeEditor.razor L200-230 (Cookbook CbSelect), L340-348 (Save button)
    - src/CookBot.Web/wwwroot/css/cookbot-design.css L716-722 (the 720px hooks Test 7 asserts on)
    - src/CookBot.Web/Components/Pages/AiChat.razor L308-320 (canvas Ok==true gate — why Test 4 can't reach happy-path)
    - tests/uat-harness/lib/session.mjs + lib/app.mjs (from Task 1)
  </read_first>
  <action>
    Create `tests/test5-reparenting.mjs`: using an established session, navigate to a recipe in a known origin cookbook, click Edit (visible text "Edit"), change the Cookbook CbSelect to a different owned cookbook, click Save (text "Save"), then ASSERT the browser navigated to the destination cookbook page AND the recipe no longer appears in the origin cookbook's recipe list. Create `tests/test7-responsive.mjs`: set viewport to 719px, navigate to `/recipes/{id}`, ASSERT `.topbar-right-slot` is hidden (not visible), `.recipe-actions-inline-fallback` is visible, the "Edit" button text is present inside the fallback row (CLEANUP-01), and the hero grid computed `grid-template-columns` resolves to a single track / the `.recipe-hero` is stacked (CLEANUP-02). Create `tests/test4-validation-fail.mjs` implementing the CHOSEN honesty path: EITHER drive a fault-injection seam (e.g. a harness-only request route/header/env that forces a schema-mismatch AI response so `RawRecipeEditorDialog` opens, then assert the dialog) OR a no-op that records Test 4 as "manual/deferred — cannot trigger on happy path" and returns a `skipped` (not `passed`) result. Whichever you pick, document it in README. Create `run.mjs`: await app readiness (lib/app.mjs), establish session (lib/session.mjs), run Test 5, Test 7, and the Test 4 disposition, collect per-test results, print a summary line per test ("UAT Test 5: PASS|FAIL|SKIP", etc.), and `process.exit(1)` if any test FAILED (a SKIP for Test 4 does not fail the run, but is printed distinctly). Each test must FAIL loudly (assertion error → FAIL result) if the expected condition is not met — never swallow an assertion into a pass.
  </action>
  <verify>
    <automated>for f in tests/uat-harness/run.mjs tests/uat-harness/tests/test5-reparenting.mjs tests/uat-harness/tests/test7-responsive.mjs tests/uat-harness/tests/test4-validation-fail.mjs; do node --check "$f" || exit 1; done && grep -qE "recipe-actions-inline-fallback|topbar-right-slot" tests/uat-harness/tests/test7-responsive.mjs && grep -qiE "skip|deferred|fault" tests/uat-harness/tests/test4-validation-fail.mjs && grep -q "process.exit(1)" tests/uat-harness/run.mjs && echo TESTS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `node --check` passes on run.mjs and all three test modules (valid ES modules).
    - `test7-responsive.mjs` asserts on the exact classes `.topbar-right-slot` and `.recipe-actions-inline-fallback`, asserts the "Edit" text is present in the fallback (CLEANUP-01), and asserts single-column stacking (CLEANUP-02).
    - `test5-reparenting.mjs` performs Edit → change Cookbook select → Save and asserts destination-nav + origin-absence.
    - `test4-validation-fail.mjs` is EITHER a real fault-injection assertion OR returns a `skip`/`deferred` result (grep finds "skip"/"deferred"/"fault"); it NEVER returns a hard-coded PASS without exercising the dialog.
    - `run.mjs` calls `process.exit(1)` on any FAIL and prints a per-test summary line ("UAT Test 5: PASS|FAIL", "UAT Test 7: PASS|FAIL", "UAT Test 4: SKIP|PASS").
    - README documents the Test 4 disposition (fault-injected vs manual/deferred).
  </acceptance_criteria>
  <done>The harness drives Tests 5 and 7 with real assertions, handles Test 4 honestly (fault-injected or recorded skip — never faked), and the runner exits non-zero on any genuine failure with a per-test summary.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Run the UAT harness — Tests 5 & 7 pass, Test 4 honest, exit code 0</name>
  <what-built>The Playwright/Node UAT harness under tests/uat-harness/, driving Phase 10 UAT Test 5 (reparenting) and Test 7 (responsive collapse) against the running app, with Test 4 handled honestly. NOTE: a meaningful Test 7 PASS depends on Plan 11-02 (CLEANUP-01/02) having landed — run this AFTER 11-02 is merged in execute ordering.</what-built>
  <action>Checkpoint task — no autonomous code. Pause for the human to follow the steps in &lt;how-to-verify&gt; and confirm. This is verification of work completed by the preceding auto task(s) in this plan; do not write code here.</action>
  <how-to-verify>
    1. Ensure Plan 11-02 (CLEANUP-01/02) has been executed (Test 7 asserts those fixes).
    2. Start the app if the harness expects it running: `./run.sh` (or let the harness spawn it — see harness README).
    3. `cd tests/uat-harness && npm install` (first run; reuses /snap/bin/chromium, or `npx playwright install chromium` if it complains).
    4. Run `npm test` (or `node run.mjs`).
    5. Confirm the output prints "UAT Test 5: PASS", "UAT Test 7: PASS", and a distinct line for Test 4 (either "UAT Test 4: PASS" via fault injection, or "UAT Test 4: SKIP — manual/deferred").
    6. Confirm the process exit code is 0 when 5 and 7 pass (`echo $?`). Optionally temporarily break a selector to confirm a FAIL yields a non-zero exit (proves it isn't a fake pass).
  </how-to-verify>
  <resume-signal>Type "approved" or paste the failing harness output.</resume-signal>
</task>

</tasks>

<verification>
- `node --check` passes on every harness `.mjs`.
- Harness is NOT in FreelovesCookBot.sln; CookBot.Tests has no browser-test package.
- `tests/uat-harness/node_modules` is gitignored.
- Test 5 and Test 7 assert real behaviors; Test 4 is fault-injected or recorded as skip (never faked).
- `run.mjs` exits non-zero on any FAIL.
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness npm install → registry | `playwright` (+ its deps) pulled from npm; untrusted supply chain crosses here |
| harness → running app on :7000 | harness drives a real session; trusted-LAN, no auth — same posture as a local user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | npm install of `playwright` in tests/uat-harness | mitigate | `playwright` is a first-party Microsoft package (verify on npmjs.com/package/playwright); pin a version in package.json; node_modules is gitignored and never shipped in the .NET app |
| T-11-02 | Information disclosure | harness reuses trusted-LAN session (no auth) | accept | Trusted-LAN posture is the existing project invariant; harness runs locally against localhost only; no secrets handled |
| T-11-SC | Tampering | npm install supply chain | mitigate | Only `playwright` (first-party) is added; no transitive [ASSUMED]/[SUS] runtime package enters the shipped app — harness is isolated from FreelovesCookBot.sln and CookBot.Tests |
</threat_model>

<success_criteria>
A reusable, isolated Playwright/Node harness launches/uses the app on :7000, establishes a trusted-LAN session, and drives chromium through Phase 10 UAT Test 5 and Test 7 with genuine pass/fail assertions; Test 4 is fault-injected or explicitly recorded as manual/deferred (never faked); the runner emits a per-test summary and exits non-zero on any failure. The harness is not part of the .NET solution build and adds no package to CookBot.Tests.
</success_criteria>

<output>
Create `.planning/phases/11-v1.3-uat-cleanup/11-05-SUMMARY.md` when done.
</output>
