---
phase: 11-v1.3-uat-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/CookBot.Web/Components/Pages/RecipeView.razor
  - src/CookBot.Web/wwwroot/css/cookbot-design.css
autonomous: false
requirements: [CLEANUP-01, CLEANUP-02]
# NOTE: cookbot-design.css is ALSO touched by 11-03 (CLEANUP-03). To prevent a
# same-wave write collision, 11-03 is assigned Wave 2 and depends_on this plan.
# RecipeView.razor is ALSO touched by 11-04 (CLEANUP-04 wiring); 11-04 is Wave 2
# and depends_on this plan so the responsive class hooks exist before wiring.
must_haves:
  truths:
    - "At <=720px on /recipes/{id}, the inline-fallback action row shows Edit alongside Share / Schedule / Cook this — no leading button clipped off the left."
    - "At <=720px on /recipes/{id}, the hero grid stacks to one column, the ingredient/method two-column body reflows to full width, and the step-number grid stops per-word wrapping."
    - "At >=721px the layout is visually unchanged from before this plan (the existing TopBar/inline-fallback toggle still works)."
    - "No second responsive breakpoint is introduced — all rules live inside the existing 720px media block."
  artifacts:
    - path: "src/CookBot.Web/wwwroot/css/cookbot-design.css"
      provides: "flex-wrap on .recipe-actions-inline-fallback (CLEANUP-01) + single-column overrides for hero/body/step grids inside the existing @media(max-width:720px) block (CLEANUP-02)"
      contains: "flex-wrap"
    - path: "src/CookBot.Web/Components/Pages/RecipeView.razor"
      provides: "class hooks (recipe-article / recipe-hero / recipe-body-grid / recipe-step-grid) replacing inline grid styles so the media query can target them"
  key_links:
    - from: "cookbot-design.css @media(max-width:720px)"
      to: ".recipe-actions-inline-fallback"
      via: "flex-wrap rule (Edit no longer clipped)"
      pattern: "recipe-actions-inline-fallback"
    - from: "RecipeView.razor hero/body/step divs"
      to: "cookbot-design.css single-column overrides"
      via: "shared class names"
      pattern: "recipe-hero|recipe-body-grid|recipe-step-grid"
---

<objective>
Close CLEANUP-01 and CLEANUP-02 together — they are one CSS unit of work. CLEANUP-01's missing Edit button (root cause: the no-wrap `justify-content:flex-end` fallback row clips the leading child on overflow) is fixed by the SAME flex/media-query work that CLEANUP-02 needs to make RecipeView responsive at <=720px.

Per the PATTERNS root-cause analysis: Suspects B/C/D are RULED OUT — the `_topBarActions` RenderFragment is correct and renders all four buttons in the TopBar slot. Do NOT rewrite the RenderFragment. The fix is entirely in CSS + converting RecipeView's inline grid styles to targetable classes.

Purpose: RecipeView becomes usable at narrow viewports and the Edit button reappears in the fallback row — the two open halves of Phase 10 UAT Test 7.
Output: flex-wrap fix + single-column overrides in the EXISTING 720px media block, and class hooks on RecipeView's previously-inline grids.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-v1.3-uat-cleanup/11-CONTEXT.md
@.planning/phases/11-v1.3-uat-cleanup/11-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Exact lines to edit — extracted from PATTERNS. Do NOT rewrite the RenderFragment. -->

Inline fallback row (CLEANUP-01 clip site) — RecipeView.razor ~L43:
  <div class="recipe-actions-inline-fallback" style="...display:flex;justify-content:flex-end;gap:10px;...">@_topBarActions</div>

Inline grids to convert to classes (CLEANUP-02) — RecipeView.razor:
  ~L47  <article style="max-width:1080px;margin:0 auto;padding:24px 32px 80px;">      → add class recipe-article
  ~L50  <div style="display:grid;grid-template-columns:1fr 1fr;gap:40px;...">          → add class recipe-hero
  ~L113 <div style="display:grid;grid-template-columns:300px 1fr;gap:56px;">           → add class recipe-body-grid
  ~L174 <div style="display:grid;grid-template-columns:40px 1fr;gap:16px;...">         → add class recipe-step-grid

The 64px display cap that overflows narrow viewports — cookbot-design.css L266-273 (.cb-recipe-cap font-size:64px).

The EXISTING 720px media block to EXTEND (do NOT add a second breakpoint) — cookbot-design.css L716-722:
  @media (max-width: 720px) { .topbar-right-slot { display: none !important; } }
  @media (min-width: 721px) { .recipe-actions-inline-fallback { display: none !important; } }

RULED-OUT (do NOT touch) — _topBarActions RenderFragment, RecipeView.razor ~L244-249: builds Edit/Share/Schedule/Cook unconditionally; renders all four correctly in the TopBar slot.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CLEANUP-01 — stop the inline fallback row clipping the leading Edit button</name>
  <files>src/CookBot.Web/wwwroot/css/cookbot-design.css, src/CookBot.Web/Components/Pages/RecipeView.razor</files>
  <read_first>
    - .planning/phases/11-v1.3-uat-cleanup/11-PATTERNS.md (CLEANUP-01 section — Suspect A confirmed, B/C/D ruled out)
    - src/CookBot.Web/Components/Pages/RecipeView.razor L40-55 (the inline fallback row) and L244-249 (the RenderFragment — read only, do not edit)
    - src/CookBot.Web/wwwroot/css/cookbot-design.css L710-722 (the existing 720px media block)
  </read_first>
  <action>
    Move the inline `style=` on the `.recipe-actions-inline-fallback` div (RecipeView.razor ~L43) into a rule in `cookbot-design.css`, keeping the class name. In that rule add `flex-wrap:wrap` and change `justify-content` from `flex-end` to `flex-start` (or `flex-end` WITH `flex-wrap` — the wrap is the load-bearing fix; on overflow the buttons wrap to a second line instead of the leading Edit being clipped off the left). Do NOT modify the `_topBarActions` RenderFragment. Do NOT add owner/auth guards. The Edit button being first in the fragment is correct; the bug is purely the no-wrap flex-end overflow clipping the first child.
  </action>
  <verify>
    <automated>grep -E "recipe-actions-inline-fallback" src/CookBot.Web/wwwroot/css/cookbot-design.css | grep -q "fallback" && grep -vE '^\s*/?\*|^\s*//' src/CookBot.Web/wwwroot/css/cookbot-design.css | grep -A4 "recipe-actions-inline-fallback {" | grep -q "flex-wrap" && echo WRAP_OK</automated>
  </verify>
  <acceptance_criteria>
    - `cookbot-design.css` has a `.recipe-actions-inline-fallback` rule (not just the `min-width:721px` hide rule) containing `flex-wrap:wrap`.
    - The rule changes `justify-content` away from a bare `flex-end` no-wrap (either `flex-start`, or `flex-end` co-located with `flex-wrap:wrap`).
    - `git diff src/CookBot.Web/Components/Pages/RecipeView.razor` shows NO change to the `_topBarActions` RenderFragment lines (the four `<CbButton ...>` declarations are byte-identical).
    - `dotnet build src/CookBot.Web/CookBot.Web.csproj` succeeds.
  </acceptance_criteria>
  <done>The fallback action row wraps instead of clipping; Edit is the first of four buttons and is no longer pushed off the left edge at <=720px. The RenderFragment is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: CLEANUP-02 — responsive single-column collapse at <=720px</name>
  <files>src/CookBot.Web/Components/Pages/RecipeView.razor, src/CookBot.Web/wwwroot/css/cookbot-design.css</files>
  <read_first>
    - .planning/phases/11-v1.3-uat-cleanup/11-PATTERNS.md (CLEANUP-02 section — the four inline grids + the .cb-recipe-cap cap)
    - src/CookBot.Web/Components/Pages/RecipeView.razor L45-180 (article wrapper, hero grid, body two-column grid, step-number grid)
    - src/CookBot.Web/wwwroot/css/cookbot-design.css L264-274 (.cb-recipe-cap) and L710-722 (the 720px media block to extend)
  </read_first>
  <action>
    Add class hooks to RecipeView's four inline-styled grid containers: `recipe-article` (L47 article), `recipe-hero` (L50 `1fr 1fr` hero grid), `recipe-body-grid` (L113 `300px 1fr` body), `recipe-step-grid` (L174 `40px 1fr` step grid). Move the base (wide-viewport) grid declarations from inline `style=` into matching base classes in `cookbot-design.css` so the desktop layout is byte-equivalent, then INSIDE the existing `@media (max-width: 720px)` block add overrides: `.recipe-hero { grid-template-columns:1fr; }`, `.recipe-body-grid { grid-template-columns:1fr; gap:32px; }`, `.recipe-step-grid { grid-template-columns:28px 1fr; gap:12px; }` (keep the number column, just narrow it so it stops per-word wrapping), `.recipe-article { padding:16px 16px 64px; }`, and a smaller `.cb-recipe-cap { font-size:40px; }`. Do NOT add a new `@media` breakpoint — reuse the existing 720px one (POLISH-04 precedent). Keep `!important` only where an inline style would otherwise win; prefer removing the inline style so it isn't needed.
  </action>
  <verify>
    <automated>grep -qE "recipe-hero|recipe-body-grid|recipe-step-grid|recipe-article" src/CookBot.Web/Components/Pages/RecipeView.razor && test "$(grep -cE '@media \(max-width: ?720px\)' src/CookBot.Web/wwwroot/css/cookbot-design.css)" -ge 1 && test "$(grep -cE '@media \(max-width: ?7[0-1][0-9]px\)|@media \(max-width: ?6[0-9][0-9]px\)' src/CookBot.Web/wwwroot/css/cookbot-design.css)" -eq 0 && echo CSS_OK</automated>
  </verify>
  <acceptance_criteria>
    - RecipeView.razor's hero / body / step / article containers carry the classes `recipe-hero`, `recipe-body-grid`, `recipe-step-grid`, `recipe-article` respectively (grep finds all four).
    - Inside `@media (max-width: 720px)` in cookbot-design.css, `.recipe-hero` and `.recipe-body-grid` are set to `grid-template-columns:1fr` (single column).
    - NO new media breakpoint other than 720px/721px exists: `grep -E "@media \(max-width" cookbot-design.css` shows only the 720px family (no 600/640/768/719 etc.).
    - The `.cb-recipe-cap` font-size is reduced inside the 720px block (matches a `.cb-recipe-cap` rule with `font-size` < 64px).
    - `dotnet build src/CookBot.Web/CookBot.Web.csproj` succeeds.
  </acceptance_criteria>
  <done>At <=720px the hero stacks to one column, the body two-column grid becomes full-width single column, the step grid no longer per-word-wraps, the display cap shrinks, and the article padding tightens — all via the single existing 720px breakpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify CLEANUP-01/02 — Edit present + responsive collapse at 720px</name>
  <what-built>CLEANUP-01 flex-wrap fix (Edit button no longer clipped) + CLEANUP-02 single-column responsive collapse at <=720px on RecipeView. (Plan 11-05's harness UAT Test 7 will also assert these automatically, but this is the human visual confirmation.)</what-built>
  <action>Checkpoint task — no autonomous code. Pause for the human to follow the steps in &lt;how-to-verify&gt; and confirm. This is verification of work completed by the preceding auto task(s) in this plan; do not write code here.</action>
  <how-to-verify>
    1. Start the app: `./run.sh` (binds http://localhost:7000).
    2. Open any recipe: http://localhost:7000/recipes/1 (or any valid id).
    3. Resize the browser to 719px wide (or DevTools device toolbar at 719px).
    4. Confirm the inline action row ABOVE the hero shows FOUR buttons — Edit, Share, Schedule, Cook this — with Edit present and not clipped on the left (it may wrap to a second line; that is acceptable).
    5. Confirm the hero is a SINGLE column (title above the photo placeholder, not side-by-side), the ingredients and method are full-width single column, and the recipe title does not overflow the right edge.
    6. Resize back to >=900px and confirm the desktop layout is unchanged (two-column hero, TopBar actions visible, inline row hidden).
  </how-to-verify>
  <resume-signal>Type "approved" or describe what is still clipped/squished.</resume-signal>
</task>

</tasks>

<verification>
- `dotnet build` succeeds.
- `.recipe-actions-inline-fallback` rule contains `flex-wrap:wrap` (CLEANUP-01).
- Hero/body/step grids collapse to single column inside the existing 720px media block (CLEANUP-02).
- Only the 720px/721px breakpoint exists — no second breakpoint added.
- `_topBarActions` RenderFragment is byte-unchanged.
</verification>

<success_criteria>
At <=720px on /recipes/{id} the inline action row shows Edit alongside Share/Schedule/Cook this, and the whole RecipeView layout collapses to a usable single column. At >=721px the desktop layout is unchanged. All achieved through the single existing 720px breakpoint with the RenderFragment untouched.
</success_criteria>

<output>
Create `.planning/phases/11-v1.3-uat-cleanup/11-02-SUMMARY.md` when done.
</output>
