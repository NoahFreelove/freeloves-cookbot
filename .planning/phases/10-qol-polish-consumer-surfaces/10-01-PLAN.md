---
phase: 10-qol-polish-consumer-surfaces
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/CookBot.Application/Services/IRecipeMadeService.cs
  - src/CookBot.Web/Services/RecipeMadeService.cs
  - src/CookBot.Web/Program.cs
  - src/CookBot.Web/Components/Pages/Home.razor.cs
autonomous: true
requirements: [QOL-01]
tags: [layering, refactor, application-layer]

must_haves:
  truths:
    - "Application-layer code can depend on IRecipeMadeService without referencing the Web project"
    - "Existing IRecipeMadeService consumers (Home.razor.cs, RecipeView, RecipeMade) continue to work via the moved interface"
    - "DI registration in Program.cs still resolves IRecipeMadeService to the same concrete RecipeMadeService implementation"
  artifacts:
    - path: "src/CookBot.Application/Services/IRecipeMadeService.cs"
      provides: "Application-layer interface with GetLastCookAsync + GetRecentForUserAsync + LogMadeAsync"
      contains: "namespace CookBot.Application.Services"
    - path: "src/CookBot.Web/Services/RecipeMadeService.cs"
      provides: "Web-layer implementation; class declaration retained, interface block removed"
      contains: "using CookBot.Application.Services"
  key_links:
    - from: "src/CookBot.Web/Services/RecipeMadeService.cs"
      to: "src/CookBot.Application/Services/IRecipeMadeService.cs"
      via: "implements interface"
      pattern: "class RecipeMadeService\\s*:\\s*IRecipeMadeService"
    - from: "src/CookBot.Web/Program.cs"
      to: "IRecipeMadeService"
      via: "AddScoped registration unchanged"
      pattern: "AddScoped<IRecipeMadeService, RecipeMadeService>"
---

<objective>
Move the `IRecipeMadeService` interface from `src/CookBot.Web/Services/` to `src/CookBot.Application/Services/` so the Phase 10 `PantryMatchService` (Application-layer) can depend on the recency-debounce abstraction without inverting the Clean/Onion dependency direction.

Purpose: Without this move, Plan 10-03 (`PantryMatchService`) would have to either (a) live in the Web layer, breaking the canonical_refs "Source files this phase creates" path, or (b) duplicate the recency-lookup logic. Path A from PATTERNS.md Layering Note 1 cleanly resolves the inversion: interface moves, implementation stays.

Output: Interface relocated; all existing consumers continue to compile via a single added `using CookBot.Application.Services;` statement; DI registration unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-qol-polish-consumer-surfaces/10-CONTEXT.md
@.planning/phases/10-qol-polish-consumer-surfaces/10-PATTERNS.md
@CLAUDE.md
@src/CookBot.Web/Services/RecipeMadeService.cs

<interfaces>
<!-- The interface to move (currently at src/CookBot.Web/Services/RecipeMadeService.cs:12-18). -->

```csharp
public interface IRecipeMadeService
{
    Task<RecipeMade> LogMadeAsync(int recipeId, int userId, string? notes = null, CancellationToken ct = default);
    Task<RecipeMade?> GetLastCookAsync(int recipeId, int userId, CancellationToken ct = default);
    Task<IReadOnlyList<RecipeMade>> GetRecentForUserAsync(int userId, int take = 4, CancellationToken ct = default);
}
```

Required `using` directives in the new file:
- `using CookBot.Domain.Entities;` (for `RecipeMade`)
- Implicit usings cover `System.Threading.Tasks`, `System.Collections.Generic`, `System.Threading` via project SDK defaults.

Required namespace on the new file: `namespace CookBot.Application.Services;`

Current callsite `using` statements that already see the interface via the Web layer (they will switch transitively when RecipeMadeService.cs imports the Application namespace):
- `src/CookBot.Web/Components/Pages/Home.razor.cs` — line 29 `[Inject] private IRecipeMadeService RecipeMadeService { get; set; } = null!;`
- `src/CookBot.Web/Program.cs:60` — `builder.Services.AddScoped<IRecipeMadeService, RecipeMadeService>();`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create IRecipeMadeService.cs in Application layer</name>
  <files>src/CookBot.Application/Services/IRecipeMadeService.cs</files>
  <read_first>
    - src/CookBot.Web/Services/RecipeMadeService.cs (the file containing the current interface declaration at lines 12-18; the `interface` block is moved verbatim)
    - .planning/phases/10-qol-polish-consumer-surfaces/10-PATTERNS.md (Layering Note 1 — Path A recommendation)
    - .planning/phases/10-qol-polish-consumer-surfaces/10-CONTEXT.md (canonical_refs §"Reusable Assets" — IRecipeMadeService recommendation Path A)
    - src/CookBot.Domain/Entities/RecipeMade.cs (the entity type the interface references; confirm `namespace CookBot.Domain.Entities`)
    - src/CookBot.Application/Services/PromptBuilderService.cs (analog for Application-layer service file shape — file-scoped namespace, nullable enabled implicitly via csproj)
  </read_first>
  <action>
    Create the new file `src/CookBot.Application/Services/IRecipeMadeService.cs` with the file-scoped namespace `namespace CookBot.Application.Services;`. The body MUST contain ONLY the three-method `public interface IRecipeMadeService` declaration (LogMadeAsync, GetLastCookAsync, GetRecentForUserAsync) — copy the method signatures verbatim from `RecipeMadeService.cs:12-18`. Add `using CookBot.Domain.Entities;` at the top (covers `RecipeMade`). Do NOT include implementation; do NOT add any other type. Do NOT add `using` statements beyond `CookBot.Domain.Entities` (implicit usings cover `System.*`). The interface MUST be `public` so DI registration in the Web project resolves it.
  </action>
  <verify>
    <automated>test -f src/CookBot.Application/Services/IRecipeMadeService.cs &amp;&amp; grep -c "public interface IRecipeMadeService" src/CookBot.Application/Services/IRecipeMadeService.cs | grep -q "^1$" &amp;&amp; grep -q "namespace CookBot.Application.Services" src/CookBot.Application/Services/IRecipeMadeService.cs &amp;&amp; grep -q "GetLastCookAsync" src/CookBot.Application/Services/IRecipeMadeService.cs &amp;&amp; grep -q "GetRecentForUserAsync" src/CookBot.Application/Services/IRecipeMadeService.cs &amp;&amp; grep -q "LogMadeAsync" src/CookBot.Application/Services/IRecipeMadeService.cs</automated>
  </verify>
  <acceptance_criteria>
    - File `src/CookBot.Application/Services/IRecipeMadeService.cs` exists
    - Source assertion: file contains `namespace CookBot.Application.Services;`
    - Source assertion: file contains exactly one `public interface IRecipeMadeService` declaration
    - Source assertion: interface declares `Task<RecipeMade> LogMadeAsync`, `Task<RecipeMade?> GetLastCookAsync`, `Task<IReadOnlyList<RecipeMade>> GetRecentForUserAsync`
    - Source assertion: `using CookBot.Domain.Entities;` is present
    - Source assertion: file contains no class declaration (no `class RecipeMadeService`)
  </acceptance_criteria>
  <done>New Application-layer interface file exists with the same three-method shape as the original Web-layer declaration; no implementation is moved.</done>
</task>

<task type="auto">
  <name>Task 2: Remove duplicate interface from RecipeMadeService.cs and add Application using</name>
  <files>src/CookBot.Web/Services/RecipeMadeService.cs</files>
  <read_first>
    - src/CookBot.Web/Services/RecipeMadeService.cs (full file — preserve every line except the `public interface IRecipeMadeService { ... }` block at lines 12-18)
    - src/CookBot.Application/Services/IRecipeMadeService.cs (the file just created in Task 1 — confirms the interface lives at the destination)
  </read_first>
  <action>
    Edit `src/CookBot.Web/Services/RecipeMadeService.cs`:
    (a) Delete the entire `public interface IRecipeMadeService { ... }` block (currently lines 12-18) — remove the four lines covering the interface declaration plus its closing brace.
    (b) Add `using CookBot.Application.Services;` to the `using` block at the top of the file (alphabetical order with the existing `using CookBot.Domain.*` lines; insert before `using CookBot.Infrastructure.Data;` if alphabetical, otherwise immediately after the last `using CookBot.*` line). Implicit usings handle `System.*`.
    (c) Leave the `public class RecipeMadeService : IRecipeMadeService` declaration and its full body untouched — implementation stays in this file. The class will now resolve `IRecipeMadeService` from the Application namespace via the new using.
    Do NOT modify the constructor, any method body, or any other type in the file.
  </action>
  <verify>
    <automated>! grep -q "public interface IRecipeMadeService" src/CookBot.Web/Services/RecipeMadeService.cs &amp;&amp; grep -q "using CookBot.Application.Services" src/CookBot.Web/Services/RecipeMadeService.cs &amp;&amp; grep -q "class RecipeMadeService : IRecipeMadeService" src/CookBot.Web/Services/RecipeMadeService.cs</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep "public interface IRecipeMadeService" src/CookBot.Web/Services/RecipeMadeService.cs` returns zero matches
    - Source assertion: `grep "using CookBot.Application.Services" src/CookBot.Web/Services/RecipeMadeService.cs` returns at least one match
    - Source assertion: `class RecipeMadeService : IRecipeMadeService` declaration intact
    - Source assertion: existing method bodies (LogMadeAsync, GetLastCookAsync, GetRecentForUserAsync) are preserved (presence of `_db.RecipesMade` query patterns)
  </acceptance_criteria>
  <done>The interface declaration is gone from the Web-layer file; the class still implements the same interface via the new Application namespace import.</done>
</task>

<task type="auto">
  <name>Task 3: Verify build + all IRecipeMadeService consumers resolve</name>
  <files>
    src/CookBot.Web/Program.cs,
    src/CookBot.Web/Components/Pages/Home.razor.cs,
    src/CookBot.Web/Components/Pages/RecipeMade.razor,
    src/CookBot.Web/Components/Pages/RecipeView.razor
  </files>
  <read_first>
    - src/CookBot.Web/Program.cs (line 60 — confirm `AddScoped<IRecipeMadeService, RecipeMadeService>()` still compiles; no namespace change should be needed because both Application and Web types are in scope)
    - .planning/phases/10-qol-polish-consumer-surfaces/10-PATTERNS.md (Layering Note 1 — confirms no callsite changes expected since Program.cs already has `using CookBot.Web.Services;` AND will see Application types through the moved interface)
  </read_first>
  <action>
    Run `dotnet build` from the repository root. For ANY compilation error that names `IRecipeMadeService` as unresolved, locate the failing file and add `using CookBot.Application.Services;` at the top of the file (alphabetical position with other `using CookBot.*` directives). Expected impact set per PATTERNS.md grep results:
    - `src/CookBot.Web/Program.cs` (already imports `CookBot.Web.Services` for `RecipeMadeService` concrete; may need `using CookBot.Application.Services;` for the interface)
    - `src/CookBot.Web/Components/Pages/Home.razor.cs` (has `[Inject] private IRecipeMadeService` at line 29 — may need an `@using CookBot.Application.Services` directive at the top OR a `using` statement at file top depending on partial-class layout)
    - Any other razor or razor.cs file that references the bare `IRecipeMadeService` symbol
    Do NOT introduce any other code change. Do NOT add fallback `using CookBot.Web.Services;` for the interface — the interface no longer lives there.
  </action>
  <verify>
    <automated>dotnet build /home/noah/Desktop/projects/freeloves-cookbot/CookBot.sln --nologo --verbosity quiet 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - CLI output: `dotnet build` exits 0 with no errors and no new warnings
    - Source assertion: `Program.cs:60` line `builder.Services.AddScoped<IRecipeMadeService, RecipeMadeService>();` is unchanged
    - Behavior assertion: `grep -rn "IRecipeMadeService" src/ tests/ --include="*.cs" --include="*.razor"` shows every reference is reachable via either `using CookBot.Application.Services;` or `@using CookBot.Application.Services` or full qualification
  </acceptance_criteria>
  <done>Solution builds without errors or new warnings; all existing IRecipeMadeService references resolve through the moved interface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | Pure type-system refactor; no runtime data flow crosses a boundary that did not exist before |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-01 | Tampering | Layering refactor | accept | No functional change; same DI registration resolves same concrete; existing authz inside `RecipeMadeService.LogMadeAsync` (lines 75-77 owned-OR-shared cookbook check) is untouched |
</threat_model>

<verification>
- `dotnet build` exits 0
- No `IRecipeMadeService` callsite was rewritten beyond adding a `using` directive
- `git diff` against `src/CookBot.Web/Services/RecipeMadeService.cs` shows only the interface block deletion and the new `using` line — no method-body edits
</verification>

<success_criteria>
- `IRecipeMadeService` interface lives at `src/CookBot.Application/Services/IRecipeMadeService.cs`
- `RecipeMadeService` (Web-layer) implementation is unchanged in behavior
- Plan 10-03 (PantryMatchService) can `using CookBot.Application.Services;` and inject `IRecipeMadeService` without referencing the Web project
- `dotnet build` exits 0 with no warnings
</success_criteria>

<output>
Create `.planning/phases/10-qol-polish-consumer-surfaces/10-01-SUMMARY.md` when done, listing the moved file, the cleaned file, and the consumer files that received a new using statement (if any).
</output>
