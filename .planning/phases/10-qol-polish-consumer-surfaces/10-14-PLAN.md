---
phase: 10-qol-polish-consumer-surfaces
plan: 14
type: execute
wave: 4
depends_on: ["10-07"]
files_modified:
  - src/CookBot.Web/Components/Pages/EditProfile.razor
autonomous: true
requirements: [PROD-17]
tags: [edit-profile, ai-usage, read-surface, telemetry, prod-17-phase10-read-surface]

must_haves:
  truths:
    - "EditProfile.razor shows an 'AI usage' CbCard with rolling 30-day input tokens, output tokens, and estimated cost USD sourced from `AiUsageLog`"
    - "Query filters by `KeyOwnerId == currentUserId AND IsRetryAttempt == false AND Timestamp >= UtcNow.AddDays(-30)` (PROD-17 + PITFALL H9 guard against retry double-counting)"
    - "Empty-state copy 'No AI activity in the last 30 days.' renders when totals are all zero"
    - "Pricing footnote 'Pricing as of {CookBotSettings.AiPricingVerifiedDate}' is displayed (PITFALL H10)"
    - "Cross-user disclosure footnote 'Includes spending by users sharing your key' per Phase 9 README PROD-18 / PITFALL M9"
    - "The 'AI usage' card is inserted at fixed ordinal position #5 in EditProfile (immediately after #4 AI assistant instructions, immediately before #6 AI features) — W-05 card-order contract enforced"
  artifacts:
    - path: "src/CookBot.Web/Components/Pages/EditProfile.razor"
      provides: "New 'AI usage' CbCard at ordinal position #5 with three figures + two footnotes"
  key_links:
    - from: "src/CookBot.Web/Components/Pages/EditProfile.razor"
      to: "AiUsageLog table"
      via: "DbContext.AiUsageLogs.Where(KeyOwnerId == userId)"
      pattern: "AiUsageLogs"
---

<objective>
Surface the Phase 9 `AiUsageLog` rows on EditProfile per CONTEXT Claude's Discretion ("AI usage widget"). The query is the simplest read possible: rolling 30 days, filtered by `KeyOwnerId == currentUserId`, excluding `IsRetryAttempt == true` rows so the 2-retry repair loop does not double-count (PITFALL H9 — Phase 9 mitigated structurally, this widget enforces it on the read side).

Purpose: Phase 9 wrote the rows; Phase 10 reads them. Bundled here (not Plan 10-07) because it is read-only telemetry, not part of the prompt-editor workflow.

**Implementation notes — cross-phase requirement tag (W-04):**

This plan is tagged `requirements: [PROD-17]` alongside the existing PROD-17 reference in Phase 9's 09-05 plan. PROD-17 is a **Phase 9 requirement** whose write-side (the AiUsageLog table + the row-write call site) was certified by Phase 9 verification. The **Phase 10 read surface** — the EditProfile card that lets a user actually see the rolling 30-day totals — was deferred to Phase 10 in CONTEXT.md's Claude's Discretion section. Tagging both phases keeps the requirements traceability table clean: Phase 9 owns the write-side, Phase 10 owns the read-side, both reference the same REQ-ID.

The previous frontmatter tag of `QOL-05` was removed (W-04 correction). QOL-05 is the **accent picker** requirement, owned proper by Plan 10-12 (the accent-color CbRadio surface). Including QOL-05 here was a bookkeeping kludge from the earlier draft when the planner was reluctant to introduce a cross-phase PROD-17 tag. The cross-phase tag is the cleaner solution: PROD-17 appears in both 09-05's and 10-14's `requirements:` frontmatter, and the ROADMAP traceability table can list both plan files under PROD-17.

The `tags:` frontmatter field carries `prod-17-phase10-read-surface` as a free-form annotation so verification tooling can distinguish the read-side from the write-side without ambiguity.

Output: One CbCard added to EditProfile.razor at ordinal position #5 (per W-05 card-order contract).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-qol-polish-consumer-surfaces/10-CONTEXT.md
@.planning/phases/10-qol-polish-consumer-surfaces/10-PATTERNS.md
@CLAUDE.md
@src/CookBot.Web/Components/Pages/EditProfile.razor
@src/CookBot.Domain/Entities/AiUsageLog.cs
@src/CookBot.Application/DTOs/CookBotSettings.cs

<interfaces>
<!-- AiUsageLog entity fields (Phase 9 PROD-14): -->
<!--   Id, UserId, KeyOwnerId, ModelName, InputTokens, OutputTokens, EstimatedCostUsd, IsRetryAttempt, Timestamp -->
<!-- Composite index IX_AiUsageLogs_KeyOwnerId_Timestamp makes the query O(log n). -->

<!-- Query (PATTERNS.md §"Razor page modification — `EditProfile.razor` (three new cards)" → "AI usage card"): -->
<!--   var cutoff = DateTime.UtcNow.AddDays(-30); -->
<!--   var rows = DbContext.AiUsageLogs.AsNoTracking() -->
<!--       .Where(r => r.KeyOwnerId == userId && !r.IsRetryAttempt && r.Timestamp >= cutoff); -->
<!--   _aiInputTokens30d = await rows.SumAsync(r => (long)r.InputTokens); -->
<!--   _aiOutputTokens30d = await rows.SumAsync(r => (long)r.OutputTokens); -->
<!--   _aiCost30d = await rows.SumAsync(r => r.EstimatedCostUsd); -->

<!-- CookBotSettings.AiPricingVerifiedDate (Phase 9 line 44, DateOnly? type) — accessed via -->
<!-- IOptions<CookBotSettings> injection. Format as `yyyy-MM-dd` for display. -->

<!-- Cross-user disclosure (Phase 9 PROD-18 README + PITFALL M9): -->
<!--   "Includes spending by users sharing your key" -->

<!-- Empty state: when all three sums are 0 (or null on empty source set), render: -->
<!--   "No AI activity in the last 30 days." -->

<!-- W-05 CARD ORDER CONTRACT (binding across plans 10-12, 10-07, 10-14): -->
<!--    Ordinal #1: Display name           (EXISTING — line 43) -->
<!--    Ordinal #2: Account password       (EXISTING — line 62) -->
<!--    Ordinal #3: Accent color           (NEW — Plan 10-12) -->
<!--    Ordinal #4: AI assistant instructions (NEW — Plan 10-07) -->
<!--    Ordinal #5: AI usage               (NEW — THIS PLAN, 10-14) -->
<!--    Ordinal #6: AI features (toggle)   (EXISTING — line 94 baseline) -->
<!-- Insert the AI usage card IMMEDIATELY AFTER the "AI assistant instructions" closing `</CbCard>` -->
<!-- (added by Plan 10-07) and IMMEDIATELY BEFORE the "AI features" `<CbCard>` opening (baseline line 94). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 'AI usage' CbCard to EditProfile.razor at ordinal position #5</name>
  <files>src/CookBot.Web/Components/Pages/EditProfile.razor</files>
  <read_first>
    - src/CookBot.Web/Components/Pages/EditProfile.razor (full file — find the existing CbCard layout pattern at lines 42-91; find the existing DbContext injection and CurrentUserService injection; find an existing IOptions<CookBotSettings> injection or add one. **W-05 contract:** find the "AI assistant instructions" CbCard added by Plan 10-07 — the new "AI usage" card goes immediately AFTER its closing `</CbCard>` and immediately BEFORE the next existing CbCard (which will be "AI features" at baseline line 94).)
    - src/CookBot.Domain/Entities/AiUsageLog.cs (verify field names: KeyOwnerId, IsRetryAttempt, Timestamp, InputTokens, OutputTokens, EstimatedCostUsd)
    - src/CookBot.Infrastructure/Data/CookBotDbContext.cs (line 34 — confirms `DbSet<AiUsageLog> AiUsageLogs`)
    - src/CookBot.Application/DTOs/CookBotSettings.cs (line 44 — `AiPricingVerifiedDate` of type `DateOnly?`)
    - .planning/phases/10-qol-polish-consumer-surfaces/10-PATTERNS.md (§"Razor page modification — `EditProfile.razor` (three new cards)" → "AI usage card (PROD-17 read surface)" excerpt)
    - .planning/phases/10-qol-polish-consumer-surfaces/10-CONTEXT.md (Claude's discretion "AI usage widget" — full surface spec)
  </read_first>
  <action>
    Edit `src/CookBot.Web/Components/Pages/EditProfile.razor`.

    **W-05 CARD ORDER CONTRACT (binding across plans 10-12, 10-07, 10-14):**
    | Ordinal | Eyebrow text                | Source             |
    |---------|-----------------------------|--------------------|
    | #1      | Display name                | EXISTING (line 43) |
    | #2      | Account password            | EXISTING (line 62) |
    | #3      | Accent color                | NEW — Plan 10-12   |
    | #4      | AI assistant instructions   | NEW — Plan 10-07   |
    | #5      | AI usage                    | NEW — THIS PLAN    |
    | #6      | AI features (toggle)        | EXISTING (line 94) |

    Step 1 — Add `@inject` directives at the top of the file (if not already present):
    - `@inject CookBot.Infrastructure.Data.CookBotDbContext DbContext`
    - `@inject Microsoft.Extensions.Options.IOptions<CookBot.Application.DTOs.CookBotSettings> CookBotSettingsOptions`

    Step 2 — **Insert the new `<CbCard>` IMMEDIATELY AFTER the "AI assistant instructions" closing `</CbCard>` (added by Plan 10-07 at ordinal #4) and IMMEDIATELY BEFORE the "AI features" opening `<CbCard>` (baseline line 94, now ordinal #6).** This satisfies ordinal #5. Card content:

    - `<CbEyebrow>AI usage</CbEyebrow>`
    - Caption paragraph: `<p style="margin:6px 0 14px 0;font-size:13px;color:var(--ink-3);">Rolling 30-day Anthropic API usage for keys you own.</p>`
    - **Conditional rendering** of three figure rows OR empty state:
      - When `_aiInputTokens30d == 0 && _aiOutputTokens30d == 0 && _aiCost30d == 0`: render `<p style="margin:0;font-size:13px;color:var(--ink-3);">No AI activity in the last 30 days.</p>`
      - Otherwise render three figure rows (flex columns or a simple `<dl>`-style block):
        - Input tokens: `@_aiInputTokens30d.ToString("N0")`
        - Output tokens: `@_aiOutputTokens30d.ToString("N0")`
        - Estimated cost: `$@_aiCost30d.ToString("F4")` (4 decimal places — costs are typically fractional USD)
    - Pricing footnote (PITFALL H10): `<p style="margin:8px 0 0 0;font-size:11.5px;color:var(--ink-4);">Pricing as of @(CookBotSettingsOptions.Value.AiPricingVerifiedDate?.ToString("yyyy-MM-dd") ?? "—").</p>` (Renders `Pricing as of —` if AiPricingVerifiedDate is null.)
    - Cross-user footnote (PITFALL M9): `<p style="margin:4px 0 0 0;font-size:11.5px;color:var(--ink-4);">Includes spending by users sharing your key.</p>`

    Step 3 — In `@code`:
    - Add fields: `private long _aiInputTokens30d; private long _aiOutputTokens30d; private decimal _aiCost30d;`
    - Add method:
      ```
      private async Task LoadAiUsageAsync(int userId)
      {
          var cutoff = DateTime.UtcNow.AddDays(-30);
          var rows = DbContext.AiUsageLogs.AsNoTracking()
              .Where(r => r.KeyOwnerId == userId && !r.IsRetryAttempt && r.Timestamp >= cutoff);
          _aiInputTokens30d = await rows.SumAsync(r => (long)r.InputTokens);
          _aiOutputTokens30d = await rows.SumAsync(r => (long)r.OutputTokens);
          _aiCost30d = await rows.SumAsync(r => r.EstimatedCostUsd);
      }
      ```
    - In the existing page-load lifecycle hook (the same one used by Plan 10-07 / Plan 10-12 for `_promptTemplate` and `_accent`), add a call: `if (UserService.CurrentUserId.HasValue) await LoadAiUsageAsync(UserService.CurrentUserId.Value);`
    - Add `using Microsoft.EntityFrameworkCore;` if not already present (for `SumAsync` extension).

    **IMPORTANT — query authz contract:** The filter `r.KeyOwnerId == userId` is the privacy boundary per Phase 9 PROD-17. This MUST stay as a server-side EF predicate (NOT client-side filtering of all rows). The composite index `IX_AiUsageLogs_KeyOwnerId_Timestamp` from Phase 9 PROD-14 supports this query shape.

    Do NOT modify any other card in EditProfile.razor. Do NOT alter the existing AI-enabled toggle, display-name card, or any earlier plan's additions (Accent color from 10-12 or AI assistant instructions from 10-07).

    **W-05 ordinal-position check at end of edit:** After inserting the card, the file should contain `<CbEyebrow>AI usage</CbEyebrow>` AFTER `<CbEyebrow>AI assistant instructions</CbEyebrow>` (if Plan 10-07 has landed) AND BEFORE `<CbEyebrow>AI features</CbEyebrow>`. Verify by reading the resulting file's CbEyebrow sequence top-to-bottom.
  </action>
  <verify>
    <automated>grep -q "AI usage" src/CookBot.Web/Components/Pages/EditProfile.razor &amp;&amp; grep -q "_aiInputTokens30d" src/CookBot.Web/Components/Pages/EditProfile.razor &amp;&amp; grep -q "_aiCost30d" src/CookBot.Web/Components/Pages/EditProfile.razor &amp;&amp; grep -q "KeyOwnerId == userId" src/CookBot.Web/Components/Pages/EditProfile.razor &amp;&amp; grep -q "!r.IsRetryAttempt" src/CookBot.Web/Components/Pages/EditProfile.razor &amp;&amp; grep -q "AddDays(-30)" src/CookBot.Web/Components/Pages/EditProfile.razor &amp;&amp; grep -q "Pricing as of" src/CookBot.Web/Components/Pages/EditProfile.razor &amp;&amp; grep -q "Includes spending by users sharing your key" src/CookBot.Web/Components/Pages/EditProfile.razor &amp;&amp; grep -q "No AI activity in the last 30 days" src/CookBot.Web/Components/Pages/EditProfile.razor &amp;&amp; awk '/<CbEyebrow>AI assistant instructions<\/CbEyebrow>/{a=NR} /<CbEyebrow>AI usage<\/CbEyebrow>/{b=NR} /<CbEyebrow>AI features<\/CbEyebrow>/{c=NR} END{exit !(a&amp;&amp;b&amp;&amp;c&amp;&amp;a&lt;b&amp;&amp;b&lt;c)}' src/CookBot.Web/Components/Pages/EditProfile.razor &amp;&amp; dotnet build /home/noah/Desktop/projects/freeloves-cookbot/CookBot.sln --nologo --verbosity quiet 2&gt;&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: EditProfile.razor contains the eyebrow "AI usage"
    - Source assertion: declares `_aiInputTokens30d`, `_aiOutputTokens30d`, `_aiCost30d` fields
    - Source assertion: query filter contains `KeyOwnerId == userId && !r.IsRetryAttempt && r.Timestamp >= cutoff`
    - Source assertion: pricing footnote contains literal `"Pricing as of"`
    - Source assertion: cross-user footnote contains literal `"Includes spending by users sharing your key"`
    - Source assertion: empty-state copy `"No AI activity in the last 30 days"` present
    - **W-05 source assertion:** Awk-based ordinal check — `<CbEyebrow>AI assistant instructions</CbEyebrow>` appears BEFORE `<CbEyebrow>AI usage</CbEyebrow>` which appears BEFORE `<CbEyebrow>AI features</CbEyebrow>` (line-number monotonicity verifies the insertion ordinal #5)
    - CLI output: `dotnet build` exits 0
  </acceptance_criteria>
  <done>EditProfile renders the rolling-30-day AI usage card at ordinal position #5 with pricing footnote and cross-user disclosure; empty state handled; W-05 contract satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → telemetry query | Filter `KeyOwnerId == currentUserId` is the privacy boundary per PROD-17 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-14-01 | Information Disclosure | Cross-user telemetry leak | mitigate | EF query predicate `KeyOwnerId == userId` runs server-side; users see only AiUsageLog rows where they are the key owner. Cross-user disclosure footnote per PROD-18 explains shared-key recipients' usage is included (legitimate, owner has consented by sharing). |
| T-10-14-02 | Tampering | Retry double-count | mitigate | `!r.IsRetryAttempt` excludes the 2-retry repair-loop rows per PITFALL H9; widget shows only successful original attempts |
| T-10-14-03 | Denial of Service | Slow query at scale | mitigate | Composite index `IX_AiUsageLogs_KeyOwnerId_Timestamp` from Phase 9 PROD-14 makes this O(log n); rolling 30-day window caps the result set even at high-volume keys |
</threat_model>

<verification>
- `dotnet build` exits 0
- Manual smoke (deferred): visit EditProfile; confirm the AI usage card renders correct figures for the test user; with an empty DB the card shows the empty-state copy
- `grep "Includes spending by users sharing your key" src/CookBot.Web/Components/Pages/EditProfile.razor` returns 1 hit (PROD-18 disclosure honored)
- Ordinal check: `<CbEyebrow>AI usage</CbEyebrow>` sits between `<CbEyebrow>AI assistant instructions</CbEyebrow>` and `<CbEyebrow>AI features</CbEyebrow>` (W-05 enforced)
- Frontmatter: `grep "requirements: \[PROD-17\]" .planning/phases/10-qol-polish-consumer-surfaces/10-14-PLAN.md` returns 1 hit (W-04 enforced — PROD-17 is the canonical tag; QOL-05 removed)
</verification>

<success_criteria>
- AI usage card surfaces rolling-30-day totals
- Query authz predicate is server-side (`KeyOwnerId == userId`)
- Retry rows are excluded (`!IsRetryAttempt`)
- Pricing-as-of footnote shows AiPricingVerifiedDate
- Cross-user disclosure footnote present
- Empty state renders cleanly when no data
- Card sits at ordinal position #5 in EditProfile (after AI assistant instructions, before AI features) — W-05 contract enforced
- Frontmatter tagged `requirements: [PROD-17]` — W-04 enforced (PROD-17 is the proper Phase 9 → Phase 10 cross-phase tag; the prior QOL-05 bookkeeping kludge is removed since QOL-05 is owned by Plan 10-12 proper)
</success_criteria>

<output>
Create `.planning/phases/10-qol-polish-consumer-surfaces/10-14-SUMMARY.md` when done.
</output>
</content>
</invoke>