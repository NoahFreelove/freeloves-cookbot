---
phase: 10-qol-polish-consumer-surfaces
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/CookBot.Application/DTOs/PantryMatchOptions.cs
  - src/CookBot.Application/DTOs/PantryMatchResult.cs
  - src/CookBot.Application/Services/IPantryMatchService.cs
  - src/CookBot.Web/appsettings.json
  - src/CookBot.Web/Program.cs
autonomous: true
requirements: [QOL-01, QOL-02, QOL-03]
tags: [pantry-match, contracts, application-layer, configuration]

must_haves:
  truths:
    - "Pantry-match scoring weights are read from `CookBot:PantryMatch` in appsettings.json, with defaults baked into the POCO when the section is missing"
    - "Application-layer code can reference `IPantryMatchService` and the two DTOs without referencing the Web project"
    - "`IOptions<PantryMatchOptions>` resolves at runtime via DI registered in Program.cs (NOT via an AddApplication signature change)"
  artifacts:
    - path: "src/CookBot.Application/DTOs/PantryMatchOptions.cs"
      provides: "IOptions-bound POCO with RecencyPenaltyWeight, RecencyHalfLifeDays, MinCoverageRatio, ResultCount + defaults"
    - path: "src/CookBot.Application/DTOs/PantryMatchResult.cs"
      provides: "sealed record PantryMatchResult(int, string, int, int, double, string?, string?)"
    - path: "src/CookBot.Application/Services/IPantryMatchService.cs"
      provides: "Task<IReadOnlyList<PantryMatchResult>> GetMatchesAsync(int userId, CancellationToken ct = default)"
    - path: "src/CookBot.Web/appsettings.json"
      provides: "CookBot:PantryMatch section with default values"
      contains: "RecencyPenaltyWeight"
  key_links:
    - from: "src/CookBot.Web/Program.cs"
      to: "PantryMatchOptions"
      via: "services.Configure<PantryMatchOptions>(GetSection)"
      pattern: "Configure<PantryMatchOptions>"
    - from: "src/CookBot.Web/appsettings.json"
      to: "PantryMatchOptions"
      via: "CookBot:PantryMatch section"
      pattern: "\"PantryMatch\""
---

<objective>
Lay down the contracts and configuration scaffold for Plan 10-03's `PantryMatchService` implementation: define `PantryMatchOptions` (IOptions-bound POCO), `PantryMatchResult` (sealed record), `IPantryMatchService` (interface only — no implementation), the `CookBot:PantryMatch` block in appsettings, and the DI binding line in Program.cs.

Purpose (D-46): D-46 makes pantry-match weights operator-configurable in appsettings — the ONE tuning surface v1.3 exposes (per CONTEXT specifics). Defining the contracts first lets Plan 10-03 (impl) and Plan 10-04 (Home swap + tests) proceed in parallel against a stable shape. PATTERNS.md correction #6: do NOT extend `AddApplication` to take `IConfiguration`; register `PantryMatchOptions` in Program.cs:61 alongside the existing `CookBotSettings` Configure call.

Output: Three Application-layer files (one DTO POCO, one record DTO, one interface), one appsettings block, one DI line in Program.cs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-qol-polish-consumer-surfaces/10-CONTEXT.md
@.planning/phases/10-qol-polish-consumer-surfaces/10-PATTERNS.md
@CLAUDE.md
@src/CookBot.Application/DTOs/CookBotSettings.cs
@src/CookBot.Application/DTOs/AiPricingEntry.cs

<interfaces>
<!-- Existing analog: CookBotSettings.cs (IOptions-bound POCO with property defaults). -->
<!-- Existing analog: Home.razor.cs:470-477 HomePantryMatch (record shape to mirror). -->
<!-- Existing analog: Program.cs:61 builder.Services.Configure<CookBotSettings>(builder.Configuration.GetSection("CookBot")). -->

PantryMatchOptions defaults (D-46):
- RecencyPenaltyWeight: 0.3 (double)
- RecencyHalfLifeDays: 7.0 (double)
- MinCoverageRatio: 0.6 (double)
- ResultCount: 3 (int)

PantryMatchResult fields (mirrors Home.razor.cs:470-477 HomePantryMatch positional record so Plan 10-04 Home swap is mechanical):
- RecipeId: int
- RecipeName: string
- MatchedCount: int
- TotalCount: int
- Score: double
- PhotoUrl: string?
- FirstMissingIngredientName: string?

IPantryMatchService surface (single method per D-47 + canonical_refs):
- Task<IReadOnlyList<PantryMatchResult>> GetMatchesAsync(int userId, CancellationToken ct = default);
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PantryMatchOptions and PantryMatchResult DTOs</name>
  <files>
    src/CookBot.Application/DTOs/PantryMatchOptions.cs,
    src/CookBot.Application/DTOs/PantryMatchResult.cs
  </files>
  <read_first>
    - .planning/phases/10-qol-polish-consumer-surfaces/10-PATTERNS.md (§"Application DTOs — `PantryMatchOptions` + `PantryMatchResult`" — exact analog excerpts)
    - .planning/phases/10-qol-polish-consumer-surfaces/10-CONTEXT.md (D-44, D-46 — formula + defaults)
    - src/CookBot.Application/DTOs/CookBotSettings.cs (analog for sealed-class-with-property-defaults pattern)
    - src/CookBot.Application/DTOs/AiPricingEntry.cs (analog for POCO with `{ get; set; } = ...` defaults baked in)
    - src/CookBot.Web/Components/Pages/Home.razor.cs (lines 470-477 — HomePantryMatch sealed record shape to mirror in PantryMatchResult)
  </read_first>
  <action>
    Create `src/CookBot.Application/DTOs/PantryMatchOptions.cs`:
    - File-scoped namespace `namespace CookBot.Application.DTOs;`
    - `public sealed class PantryMatchOptions`
    - Properties: `public double RecencyPenaltyWeight { get; set; } = 0.3;`, `public double RecencyHalfLifeDays { get; set; } = 7.0;`, `public double MinCoverageRatio { get; set; } = 0.6;`, `public int ResultCount { get; set; } = 3;`
    - XML doc summary on the class referencing D-46 + Phase 10 and explaining that defaults apply when the `CookBot:PantryMatch` section is missing
    - XML doc summary on `RecencyPenaltyWeight` referencing D-44 (linear-decay coefficient)
    - XML doc summary on `RecencyHalfLifeDays` referencing D-44 (decay half-life in days)
    - No constructors; no methods; only properties

    Create `src/CookBot.Application/DTOs/PantryMatchResult.cs`:
    - File-scoped namespace `namespace CookBot.Application.DTOs;`
    - `public sealed record PantryMatchResult(int RecipeId, string RecipeName, int MatchedCount, int TotalCount, double Score, string? PhotoUrl, string? FirstMissingIngredientName);`
    - XML doc summary referencing Home.razor.cs:470 `HomePantryMatch` (this record is its Application-layer counterpart; Home.razor.cs will project from this into the view-layer record)

    Both files use implicit usings (no `using` statements needed). Match `CookBotSettings.cs` file-header convention (file-scoped namespace, no `#nullable enable` directive — implicit via csproj).
  </action>
  <verify>
    <automated>test -f src/CookBot.Application/DTOs/PantryMatchOptions.cs &amp;&amp; test -f src/CookBot.Application/DTOs/PantryMatchResult.cs &amp;&amp; grep -q "public sealed class PantryMatchOptions" src/CookBot.Application/DTOs/PantryMatchOptions.cs &amp;&amp; grep -q "RecencyPenaltyWeight { get; set; } = 0.3" src/CookBot.Application/DTOs/PantryMatchOptions.cs &amp;&amp; grep -q "RecencyHalfLifeDays { get; set; } = 7" src/CookBot.Application/DTOs/PantryMatchOptions.cs &amp;&amp; grep -q "MinCoverageRatio { get; set; } = 0.6" src/CookBot.Application/DTOs/PantryMatchOptions.cs &amp;&amp; grep -q "ResultCount { get; set; } = 3" src/CookBot.Application/DTOs/PantryMatchOptions.cs &amp;&amp; grep -q "public sealed record PantryMatchResult" src/CookBot.Application/DTOs/PantryMatchResult.cs &amp;&amp; grep -q "FirstMissingIngredientName" src/CookBot.Application/DTOs/PantryMatchResult.cs</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `PantryMatchOptions.cs` declares `public sealed class PantryMatchOptions` with all four properties and exact defaults `0.3 / 7.0 / 0.6 / 3`
    - Source assertion: `PantryMatchResult.cs` declares `public sealed record PantryMatchResult(int RecipeId, string RecipeName, int MatchedCount, int TotalCount, double Score, string? PhotoUrl, string? FirstMissingIngredientName);` with exactly that positional parameter list and order
    - Source assertion: both files use `namespace CookBot.Application.DTOs;` (file-scoped)
    - CLI output: `dotnet build src/CookBot.Application/CookBot.Application.csproj` exits 0 with no warnings (verified after Task 2 below)
  </acceptance_criteria>
  <done>Both DTO files exist with the exact field shapes Plan 10-03 / Plan 10-04 will consume; default values match D-46.</done>
</task>

<task type="auto">
  <name>Task 2: Create IPantryMatchService interface</name>
  <files>src/CookBot.Application/Services/IPantryMatchService.cs</files>
  <read_first>
    - .planning/phases/10-qol-polish-consumer-surfaces/10-PATTERNS.md (§"Application services — `IPantryMatchService` + `PantryMatchService`" — interface+impl pair pattern from RecipeMadeService analog)
    - .planning/phases/10-qol-polish-consumer-surfaces/10-CONTEXT.md (canonical_refs §"Source files this phase creates" — IPantryMatchService.cs entry)
    - src/CookBot.Application/Services/IRecipeMadeService.cs (created in Plan 10-01 — analog for Application-layer interface file structure)
    - src/CookBot.Application/DTOs/PantryMatchResult.cs (just created in Task 1 — referenced by the interface)
  </read_first>
  <action>
    Create `src/CookBot.Application/Services/IPantryMatchService.cs`:
    - File-scoped namespace `namespace CookBot.Application.Services;`
    - `using CookBot.Application.DTOs;` (covers `PantryMatchResult`)
    - `public interface IPantryMatchService` with a single method signature: `Task<IReadOnlyList<PantryMatchResult>> GetMatchesAsync(int userId, CancellationToken ct = default);`
    - XML doc summary on the interface explaining the contract: returns recipes ranked by D-44's linear-decay score, filtered per D-45 dietary rules, stable-sorted per PITFALL H8 by (score desc, recipeId asc, recipe-name asc). References Phase 10 / QOL-01..03.
    - XML doc summary on `GetMatchesAsync` documenting: empty list when no recipes survive filtering; the user's `DietaryPreferences` are read inside the service (caller does not pass them); `CancellationToken` is standard.
    - No implementation; no other type in the file.
  </action>
  <verify>
    <automated>test -f src/CookBot.Application/Services/IPantryMatchService.cs &amp;&amp; grep -q "public interface IPantryMatchService" src/CookBot.Application/Services/IPantryMatchService.cs &amp;&amp; grep -q "Task<IReadOnlyList<PantryMatchResult>> GetMatchesAsync" src/CookBot.Application/Services/IPantryMatchService.cs &amp;&amp; grep -q "namespace CookBot.Application.Services" src/CookBot.Application/Services/IPantryMatchService.cs &amp;&amp; dotnet build src/CookBot.Application/CookBot.Application.csproj --nologo --verbosity quiet 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `src/CookBot.Application/Services/IPantryMatchService.cs` exists
    - Source assertion: `public interface IPantryMatchService` declared exactly once
    - Source assertion: method signature is exactly `Task<IReadOnlyList<PantryMatchResult>> GetMatchesAsync(int userId, CancellationToken ct = default);`
    - CLI output: `dotnet build src/CookBot.Application/CookBot.Application.csproj` exits 0
  </acceptance_criteria>
  <done>Interface file exists; Application project builds clean with the new contract referenced.</done>
</task>

<task type="auto">
  <name>Task 3: Add CookBot:PantryMatch to appsettings.json and Configure binding in Program.cs</name>
  <files>
    src/CookBot.Web/appsettings.json,
    src/CookBot.Web/Program.cs
  </files>
  <read_first>
    - src/CookBot.Web/appsettings.json (verify the `CookBot` object location and the sibling-key style; the new block goes inside `CookBot`, after `AiPricing` or at end of object)
    - src/CookBot.Web/Program.cs (line 61 — `builder.Services.Configure<CookBotSettings>(builder.Configuration.GetSection("CookBot"));` — the new line goes immediately after)
    - .planning/phases/10-qol-polish-consumer-surfaces/10-PATTERNS.md (§"DI registration — `AddApplication` + `Program.cs`" — recommendation: register in Program.cs to keep AddApplication signature stable, per PATTERNS.md correction #6)
    - .planning/phases/10-qol-polish-consumer-surfaces/10-CONTEXT.md (D-46 — appsettings shape)
  </read_first>
  <action>
    Edit `src/CookBot.Web/appsettings.json`:
    - Add a new sibling key `"PantryMatch"` inside the existing top-level `"CookBot"` object (use the same nesting depth as `"AiPricing"`)
    - Value MUST be: `{ "RecencyPenaltyWeight": 0.3, "RecencyHalfLifeDays": 7, "MinCoverageRatio": 0.6, "ResultCount": 3 }`
    - Preserve all existing keys / formatting / trailing commas in JSON (JSON does not allow trailing commas — insert with correct comma placement). Place after `AiPricingVerifiedDate` if present, otherwise immediately before the closing `}` of `CookBot`.

    Edit `src/CookBot.Web/Program.cs`:
    - Locate line 61 (`builder.Services.Configure<CookBotSettings>(builder.Configuration.GetSection("CookBot"));`)
    - Insert immediately AFTER it: `builder.Services.Configure<PantryMatchOptions>(builder.Configuration.GetSection("CookBot:PantryMatch"));`
    - If `using CookBot.Application.DTOs;` is not already present at the top of Program.cs, add it (alphabetical with other `using CookBot.*` directives). PATTERNS.md correction #6: do NOT modify `AddApplication`'s signature; this binding lives in Program.cs alongside the existing `CookBotSettings` binding.

    Do NOT modify any other appsettings keys. Do NOT modify any other Program.cs line.
  </action>
  <verify>
    <automated>grep -q "\"PantryMatch\"" src/CookBot.Web/appsettings.json &amp;&amp; grep -q "\"RecencyPenaltyWeight\": 0.3" src/CookBot.Web/appsettings.json &amp;&amp; grep -q "\"ResultCount\": 3" src/CookBot.Web/appsettings.json &amp;&amp; grep -q "Configure<PantryMatchOptions>" src/CookBot.Web/Program.cs &amp;&amp; grep -q "CookBot:PantryMatch" src/CookBot.Web/Program.cs &amp;&amp; dotnet build /home/noah/Desktop/projects/freeloves-cookbot/CookBot.sln --nologo --verbosity quiet 2>&amp;1 | tail -5 &amp;&amp; python3 -c "import json; json.load(open('src/CookBot.Web/appsettings.json'))"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `appsettings.json` contains `"PantryMatch":` inside the top-level `"CookBot"` object
    - Source assertion: `appsettings.json` contains the four child keys with values `0.3, 7, 0.6, 3` respectively
    - Source assertion: `Program.cs` contains the line `builder.Services.Configure<PantryMatchOptions>(builder.Configuration.GetSection("CookBot:PantryMatch"));`
    - Source assertion: `AddApplication` signature in `src/CookBot.Application/DependencyInjection.cs` is unchanged (still `public static IServiceCollection AddApplication(this IServiceCollection services)`)
    - CLI output: `python3 -c "import json; json.load(open('src/CookBot.Web/appsettings.json'))"` exits 0 (JSON parses)
    - CLI output: `dotnet build` exits 0 with no warnings
  </acceptance_criteria>
  <done>Config block exists in appsettings.json; DI registration in Program.cs binds the options type; solution builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → config | `appsettings.json` values are operator-controlled; env-var overrides via `CookBot__PantryMatch__*` per Phase 9 PROD-19 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02-01 | Tampering | PantryMatchOptions config | accept | Trusted-LAN operator configures appsettings; defaults baked into POCO so missing section is safe; misconfigured values (negative weights) are scoring-correctness issues, not security |
| T-10-02-02 | Information Disclosure | appsettings.json | accept | No secrets in PantryMatch block; values are pure numerical knobs |
</threat_model>

<verification>
- `appsettings.json` is valid JSON (python json.load parses)
- `dotnet build` exits 0
- New types are reachable from the Web project via the Application project reference chain
</verification>

<success_criteria>
- `PantryMatchOptions` exists with all four properties and correct defaults
- `PantryMatchResult` record exists with all seven positional fields
- `IPantryMatchService` interface declares the single `GetMatchesAsync` method
- `CookBot:PantryMatch` section present in appsettings.json with default values
- `Program.cs` registers `IOptions<PantryMatchOptions>` via Configure
- `AddApplication` signature is unchanged (PATTERNS.md correction #6 honored)
</success_criteria>

<output>
Create `.planning/phases/10-qol-polish-consumer-surfaces/10-02-SUMMARY.md` when done.
</output>
