---
phase: 10-qol-polish-consumer-surfaces
plan: 04
type: execute
wave: 4
depends_on: ["10-03"]
files_modified:
  - src/CookBot.Web/Components/Pages/Home.razor.cs
  - tests/CookBot.Tests/Services/PantryMatchIndexSnapshotTests.cs
autonomous: true
requirements: [QOL-01, QOL-03]
tags: [home-page, pantry-match, integration, index-verification]

must_haves:
  truths:
    - "Home `BuildPantryMatchesAsync` no longer contains scoring logic — it is a thin projection over `IPantryMatchService.GetMatchesAsync(userId, ct)` results into `HomePantryMatch` view records"
    - "The deterministic 60%-coverage stub is removed; Home pantry-match hero ranks by real D-44 scores"
    - "The Home.razor.cs:232 caller is updated from `BuildPantryMatchesAsync(userId, allItems)` to `BuildPantryMatchesAsync(userId)` — the `allItems` local is retained for the surrounding pantry-counts logic (lines 209-221) but no longer threaded into pantry-match"
    - "A snapshot test asserts the composite indexes from Phase 8 (`IX_RecipeIngredients_RecipeId_IngredientId` and `IX_PantryItems_PantryId_IngredientId`) are still present in the EF model — guards against accidental Phase 10 removal (per CONTEXT D-supplement `AddPantryMatchIndexes verification`)"
  artifacts:
    - path: "src/CookBot.Web/Components/Pages/Home.razor.cs"
      provides: "PantryMatchService injection + projection in BuildPantryMatchesAsync + updated line-232 caller"
      contains: "IPantryMatchService"
    - path: "tests/CookBot.Tests/Services/PantryMatchIndexSnapshotTests.cs"
      provides: "EF model snapshot assertion for composite pantry-match indexes"
  key_links:
    - from: "src/CookBot.Web/Components/Pages/Home.razor.cs"
      to: "IPantryMatchService.GetMatchesAsync"
      via: "DI-injected service call"
      pattern: "PantryMatchService\\.GetMatchesAsync"
---

<objective>
Swap Home's deterministic 60%-coverage stub for the real `IPantryMatchService` implementation, projecting `PantryMatchResult` → `HomePantryMatch` (view-layer record). Ship one EF model snapshot test that asserts the Phase 8 composite indexes still exist — defends QOL-03's O(n log n) guarantee against accidental removal in future phases.

Purpose: Delivers QOL-01 + QOL-03 user-facing behavior. Home pantry-match hero stops being a stub.

Output: Home.razor.cs swap (signature change + call-site update at line 232) + one index snapshot test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-qol-polish-consumer-surfaces/10-CONTEXT.md
@.planning/phases/10-qol-polish-consumer-surfaces/10-PATTERNS.md
@CLAUDE.md
@src/CookBot.Web/Components/Pages/Home.razor.cs
@src/CookBot.Application/Services/IPantryMatchService.cs
@src/CookBot.Application/DTOs/PantryMatchResult.cs

<interfaces>
<!-- VERIFIED FACTS from Home.razor.cs (lines read before plan finalization): -->
<!--   - Line 30 declares the injected field as `[Inject] private IJSRuntime JS { get; set; } = null!;` -->
<!--     (the canonical name for any JS interop in this file is `JS`). -->
<!--   - Line 208: `var allItems = await PantryService.GetAllUserAccessibleItemsAsync(userId);` -->
<!--   - Lines 209-221: `allItems` is consumed by `_accessiblePantryItemCount`, `_pantryCount`, -->
<!--     `_pantryLowCount`, `_pantryExpiringCount`. KEEP the `allItems` local — multiple downstream -->
<!--     pantry-count calculations depend on it. -->
<!--   - Line 232 (the caller to update): -->
<!--       _pantryMatches = await BuildPantryMatchesAsync(userId, allItems); -->
<!--     becomes: -->
<!--       _pantryMatches = await BuildPantryMatchesAsync(userId); -->
<!--     (Drop the `allItems` argument only; the variable itself stays alive for lines 218-221.) -->
<!--   - Lines 297-339: the BuildPantryMatchesAsync stub body to delete. Current signature -->
<!--       `private async Task<List<HomePantryMatch>> BuildPantryMatchesAsync(int userId, IList<PantryItem> pantryItems)` -->
<!--     becomes -->
<!--       `private async Task<List<HomePantryMatch>> BuildPantryMatchesAsync(int userId, CancellationToken ct = default)`. -->
<!--   - HomePantryMatch shape at Home.razor.cs:470-477 (existing — read it; projection target). -->

<!-- Current code at Home.razor.cs:232 (caller of the stub) -->
<!--   _pantryMatches = await BuildPantryMatchesAsync(userId, allItems); -->

<!-- Current stub method at Home.razor.cs:297-339 — DELETE the entire scoring/sort body; -->
<!-- replace with a thin caller: -->
<!--   private async Task<List<HomePantryMatch>> BuildPantryMatchesAsync(int userId, CancellationToken ct = default) -->
<!--   { -->
<!--       var results = await PantryMatchService.GetMatchesAsync(userId, ct); -->
<!--       return results.Select(r => new HomePantryMatch(...projection...)).ToList(); -->
<!--   } -->
<!-- (The pantryItems parameter is no longer needed — PantryMatchService loads them internally.) -->

<!-- DI injection (add near Home.razor.cs:29 alongside [Inject] IRecipeMadeService): -->
<!--   [Inject] private IPantryMatchService PantryMatchService { get; set; } = null!; -->

<!-- Required namespace import in Home.razor.cs (already present at line 1-9; `using CookBot.Application.Services;` -->
<!-- and `using CookBot.Application.DTOs;` are imported, so no additional usings are needed). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap Home.razor.cs BuildPantryMatchesAsync to call IPantryMatchService</name>
  <files>src/CookBot.Web/Components/Pages/Home.razor.cs</files>
  <read_first>
    - src/CookBot.Web/Components/Pages/Home.razor.cs (full file; load-bearing reads at lines 29 (Inject block), 208 (`allItems` declaration), 209-221 (`allItems` consumers — KEEP these), 232 (caller of BuildPantryMatchesAsync), 297-339 (the stub body to delete), 470-477 (HomePantryMatch projection target))
    - src/CookBot.Application/Services/IPantryMatchService.cs (interface signature)
    - src/CookBot.Application/DTOs/PantryMatchResult.cs (the seven-field record)
    - .planning/phases/10-qol-polish-consumer-surfaces/10-PATTERNS.md (§"Razor page modification — `Home.razor.cs`" — exact projection shape; "Reusable Assets" — keep HomePantryMatch as view-layer DTO)
  </read_first>
  <action>
    Edit `src/CookBot.Web/Components/Pages/Home.razor.cs` in five numbered steps:

    **Step 1 — Add `[Inject]` for `IPantryMatchService`** near line 29 (alongside the existing `[Inject] private IRecipeMadeService RecipeMadeService { get; set; } = null!;`):
       `[Inject] private IPantryMatchService PantryMatchService { get; set; } = null!;`

    **Step 2 — Add `using` directives if missing** at the top of the file:
       - `using CookBot.Application.Services;` (covers `IPantryMatchService`) — already imported at line 2; verify and only add if absent
       - `using CookBot.Application.DTOs;` (covers `PantryMatchResult`) — already imported at line 1; verify and only add if absent

    **Step 3 — Replace the stub method body** at lines 297-339:
       - Keep the method name `BuildPantryMatchesAsync` for minimal call-site disruption, but change its signature to: `private async Task<List<HomePantryMatch>> BuildPantryMatchesAsync(int userId, CancellationToken ct = default)`
       - Body becomes:
         1. `var results = await PantryMatchService.GetMatchesAsync(userId, ct);`
         2. Project each `PantryMatchResult` into a `HomePantryMatch` instance: name-positional mapping from PantryMatchResult's seven fields to whatever HomePantryMatch's record-positional list is. **Read `HomePantryMatch` at line 470-477 of the same file to confirm exact field order**; mapping must mirror that order. Any field HomePantryMatch has that PantryMatchResult does not (e.g. the badge metadata) must be computed inline in the projection (refer to the deleted stub's logic for the meta-line / badge formatting if those derive from match counts).
         3. Return the projected list.
       - **Delete** every line of the old stub between the method opening brace and closing brace — including the LINQ scoring, the `MinCoverageRatio` check, and the deterministic sort. ALL scoring logic now lives in `PantryMatchService`.

    **Step 4 — Update the caller at line 232 (FIRST-CLASS STEP — not optional).** The current line reads:
       ```
       _pantryMatches = await BuildPantryMatchesAsync(userId, allItems);
       ```
       Change to:
       ```
       _pantryMatches = await BuildPantryMatchesAsync(userId);
       ```
       (Drop the second argument only.) **DO NOT remove the `allItems` local declaration at line 208.** The local is consumed by lines 209-221 for `_accessiblePantryItemCount`, `_pantryCount`, `_pantryLowCount`, and `_pantryExpiringCount` — those usages MUST be preserved. Verified facts before plan finalization: lines 209, 210, 218, 219 all reference `allItems`; removing the declaration would break four pantry-glance counters.

    **Step 5 — Preserve everything else** — do NOT touch `OnAfterRenderAsync`, do NOT touch the active-timer logic (Plan 10-13 handles that), do NOT touch any other method body, do NOT alter `allItems`'s declaration or its other consumers.
  </action>
  <verify>
    <automated>grep -q "IPantryMatchService PantryMatchService" src/CookBot.Web/Components/Pages/Home.razor.cs &amp;&amp; grep -q "PantryMatchService.GetMatchesAsync" src/CookBot.Web/Components/Pages/Home.razor.cs &amp;&amp; ! grep -q "MinCoverageRatio" src/CookBot.Web/Components/Pages/Home.razor.cs &amp;&amp; grep -q "BuildPantryMatchesAsync(userId);" src/CookBot.Web/Components/Pages/Home.razor.cs &amp;&amp; ! grep -q "BuildPantryMatchesAsync(userId, allItems)" src/CookBot.Web/Components/Pages/Home.razor.cs &amp;&amp; grep -q "var allItems = await PantryService.GetAllUserAccessibleItemsAsync" src/CookBot.Web/Components/Pages/Home.razor.cs &amp;&amp; grep -c "BuildPantryMatchesAsync" src/CookBot.Web/Components/Pages/Home.razor.cs | awk '$1 &gt;= 2 {exit 0} {exit 1}' &amp;&amp; dotnet build /home/noah/Desktop/projects/freeloves-cookbot/CookBot.sln --nologo --verbosity quiet 2&gt;&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `[Inject] private IPantryMatchService PantryMatchService` present
    - Source assertion: `BuildPantryMatchesAsync` body calls `PantryMatchService.GetMatchesAsync`
    - Source assertion: `grep "MinCoverageRatio" src/CookBot.Web/Components/Pages/Home.razor.cs` returns ZERO matches (stub scoring constants removed — constant now lives only in PantryMatchOptions defaults)
    - Source assertion: the caller line at the former line 232 reads `BuildPantryMatchesAsync(userId);` (single-arg) — NOT `BuildPantryMatchesAsync(userId, allItems)`
    - Source assertion: `var allItems = await PantryService.GetAllUserAccessibleItemsAsync` is STILL present (the declaration was not deleted; it remains alive for the pantry-glance counts on lines 209-221)
    - Source assertion: `BuildPantryMatchesAsync` appears at least twice in the file (declaration + caller)
    - Behavior assertion: `dotnet build` exits 0
  </acceptance_criteria>
  <done>Home.razor.cs swap is complete; the deterministic stub is gone; PantryMatchService is the source of truth; the line-232 caller is updated to the single-argument form; `allItems` remains in scope for the pantry-glance counters.</done>
</task>

<task type="auto">
  <name>Task 2: Write PantryMatchIndexSnapshotTests</name>
  <files>tests/CookBot.Tests/Services/PantryMatchIndexSnapshotTests.cs</files>
  <read_first>
    - tests/CookBot.Tests/Services/OwnershipTests.cs (analog — in-memory SQLite + EnsureCreated() pattern; we use EF model API not the migration files)
    - src/CookBot.Infrastructure/Migrations/20260516034227_AddPantryMatchIndexes.cs (confirms `IX_RecipeIngredients_RecipeId_IngredientId` was added; this test guards against its removal)
    - src/CookBot.Infrastructure/Data/CookBotDbContext.cs (contains the model configuration; the test reads `Model.FindEntityType(typeof(RecipeIngredient)).GetIndexes()` to assert presence)
    - .planning/phases/10-qol-polish-consumer-surfaces/10-CONTEXT.md (Claude's Discretion: `AddPantryMatchIndexes` migration verification — exactly this test)
  </read_first>
  <action>
    Create `tests/CookBot.Tests/Services/PantryMatchIndexSnapshotTests.cs`:
    - File-scoped namespace `namespace CookBot.Tests.Services;`
    - Usings: `Microsoft.EntityFrameworkCore`, `CookBot.Infrastructure.Data`, `CookBot.Domain.Entities`, `Xunit`
    - Public class `PantryMatchIndexSnapshotTests : IDisposable`
    - Constructor builds an in-memory SQLite `CookBotDbContext` exactly like `OwnershipTests.cs:11-23`:
      ```
      var options = new DbContextOptionsBuilder<CookBotDbContext>().UseSqlite("DataSource=:memory:").Options;
      _db = new CookBotDbContext(options);
      _db.Database.OpenConnection();
      _db.Database.EnsureCreated();
      ```
    - `Dispose() => _db.Dispose();`

    Three tests (all `[Fact]`):

    1. **`RecipeIngredient_HasCompositeIndexOn_RecipeId_IngredientId`** — `var indexes = _db.Model.FindEntityType(typeof(RecipeIngredient))!.GetIndexes();` then assert that at least one index has `Properties.Select(p => p.Name).SequenceEqual(new[] { "RecipeId", "IngredientId" })` (order matters for the composite index).

    2. **`PantryItem_HasCompositeIndexOn_PantryId_IngredientId`** — same shape, asserts `PantryItem` has an index over `["PantryId", "IngredientId"]`.

    3. **`AddPantryMatchIndexes_MigrationFile_Exists`** — `Assert.True(File.Exists("../../../../src/CookBot.Infrastructure/Migrations/20260516034227_AddPantryMatchIndexes.cs"));` OR scan via `Directory.GetFiles` with a wildcard pattern matching `*_AddPantryMatchIndexes.cs`. Picks whichever is simpler relative to test working directory; the goal is to fail-loud if the migration file is deleted.

    All three tests use `Assert.True` / `Assert.Contains` with descriptive failure messages. Build target: `dotnet test --filter FullyQualifiedName~PantryMatchIndexSnapshotTests` exits 0.
  </action>
  <verify>
    <automated>test -f tests/CookBot.Tests/Services/PantryMatchIndexSnapshotTests.cs &amp;&amp; grep -q "RecipeIngredient_HasCompositeIndexOn_RecipeId_IngredientId" tests/CookBot.Tests/Services/PantryMatchIndexSnapshotTests.cs &amp;&amp; grep -q "PantryItem_HasCompositeIndexOn_PantryId_IngredientId" tests/CookBot.Tests/Services/PantryMatchIndexSnapshotTests.cs &amp;&amp; grep -q "AddPantryMatchIndexes_MigrationFile_Exists" tests/CookBot.Tests/Services/PantryMatchIndexSnapshotTests.cs &amp;&amp; dotnet test tests/CookBot.Tests/CookBot.Tests.csproj --filter "FullyQualifiedName~PantryMatchIndexSnapshotTests" --nologo --verbosity quiet 2&gt;&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/CookBot.Tests/Services/PantryMatchIndexSnapshotTests.cs` exists
    - Source assertion: contains all three test method names listed in `<action>`
    - Source assertion: uses `_db.Model.FindEntityType(typeof(RecipeIngredient))` pattern (real EF model introspection, not migration file parsing for tests 1+2)
    - Test command: `dotnet test --filter FullyQualifiedName~PantryMatchIndexSnapshotTests` exits 0 with all 3 tests passing
  </acceptance_criteria>
  <done>Snapshot tests assert both composite indexes exist; future phases that remove them will fail-loud.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → Home page | userId from CurrentUserService is authenticated; Plan 10-03 PantryMatchService enforces owned-or-shared cookbook authz |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04-01 | Information Disclosure | Home pantry-match | mitigate | All recipe filtering happens inside `PantryMatchService.GetMatchesAsync` per Plan 10-03 T-10-03-01; Home is now a thin projection — no authz code path is duplicated or bypassed. |
| T-10-04-02 | Denial of Service | Composite index removal | mitigate | `PantryMatchIndexSnapshotTests` fails-loud if either index disappears, preventing accidental QOL-03 regression in later milestones |
</threat_model>

<verification>
- `dotnet build` exits 0
- `dotnet test --filter PantryMatchServiceTests` still exits 0 (Plan 10-03 work preserved)
- `dotnet test --filter PantryMatchIndexSnapshotTests` exits 0
- `grep "MinCoverageRatio" src/CookBot.Web/Components/Pages/Home.razor.cs` returns 0 hits (scoring logic gone)
- `grep "BuildPantryMatchesAsync(userId);" src/CookBot.Web/Components/Pages/Home.razor.cs` returns at least 1 hit (line-232 caller updated)
- `grep "var allItems = await PantryService.GetAllUserAccessibleItemsAsync" src/CookBot.Web/Components/Pages/Home.razor.cs` returns 1 hit (allItems declaration retained)
- Manual smoke: `./run.sh` and visit Home — pantry-match hero renders ranked recipes (deferred to verify-work later, but the build is the gate now)
</verification>

<success_criteria>
- Home.razor.cs deterministic stub is gone
- `IPantryMatchService` is injected and called from Home
- Line-232 caller updated from `BuildPantryMatchesAsync(userId, allItems)` to `BuildPantryMatchesAsync(userId)`
- `allItems` local retained for the four pantry-glance counters on lines 209-221
- Composite-index snapshot tests pass
- All Phase 10-03 tests still pass
- Solution builds with no warnings
</success_criteria>

<output>
Create `.planning/phases/10-qol-polish-consumer-surfaces/10-04-SUMMARY.md` when done.
</output>
</content>
</invoke>