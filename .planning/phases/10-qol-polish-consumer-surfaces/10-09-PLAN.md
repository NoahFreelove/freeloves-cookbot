---
phase: 10-qol-polish-consumer-surfaces
plan: 09
type: execute
wave: 4
depends_on: ["10-08"]
files_modified:
  - src/CookBot.Web/Components/Layout/MainLayout.razor
  - src/CookBot.Web/Components/Layout/TopBar.razor
  - src/CookBot.Web/Components/Pages/RecipeView.razor
  - src/CookBot.Web/Components/Pages/RecipeEditor.razor
  - src/CookBot.Web/wwwroot/css/cookbot-design.css
autonomous: true
requirements: [POLISH-04]
tags: [main-layout, top-bar, recipe-view, recipe-editor, responsive-css]

must_haves:
  truths:
    - "`MainLayout.razor` injects `ICbTopBarService`, subscribes to `OnChanged` in OnInitialized, and passes `TopBarService.RightSlot` to the `<TopBar RightSlot=...>` parameter"
    - "MainLayout implements `IDisposable` and unsubscribes from `OnChanged` in `Dispose`"
    - "`RecipeView.razor` and `RecipeEditor.razor` both inject `ICbTopBarService` and call `TopBarService.SetRightSlot(builder => ...)` in their `OnInitialized`/`OnInitializedAsync`"
    - "TopBar.razor wraps its `@RightSlot` rendering in a container with `class=\"topbar-right-slot\"` so CSS can hide it on narrow viewports"
    - "Each migrating page retains its existing inline-above-hero action row, wrapped with `class=\"recipe-actions-inline-fallback\"` (RecipeView) or footer-action-row equivalent (RecipeEditor)"
    - "`cookbot-design.css` contains a `@media (max-width: 720px) { .topbar-right-slot { display: none !important; } }` rule AND a `@media (min-width: 721px) { .recipe-actions-inline-fallback { display: none !important; } }` rule (D-59)"
  artifacts:
    - path: "src/CookBot.Web/Components/Layout/MainLayout.razor"
      provides: "ICbTopBarService injection + OnChanged subscription + Dispose"
    - path: "src/CookBot.Web/Components/Layout/TopBar.razor"
      provides: "`class=topbar-right-slot` wrapper around the existing @RightSlot render"
    - path: "src/CookBot.Web/Components/Pages/RecipeView.razor"
      provides: "OnInitialized SetRightSlot call + inline fallback retained with class"
    - path: "src/CookBot.Web/Components/Pages/RecipeEditor.razor"
      provides: "OnInitialized SetRightSlot call + inline fallback retained with class"
    - path: "src/CookBot.Web/wwwroot/css/cookbot-design.css"
      provides: "two @media rules for responsive slot/inline-fallback toggling"
  key_links:
    - from: "src/CookBot.Web/Components/Layout/MainLayout.razor"
      to: "ICbTopBarService.RightSlot"
      via: "TopBar.RightSlot parameter passthrough"
      pattern: "RightSlot=\"@TopBarService.RightSlot\""
    - from: "src/CookBot.Web/Components/Pages/RecipeView.razor"
      to: "ICbTopBarService.SetRightSlot"
      via: "OnInitialized invocation"
      pattern: "TopBarService.SetRightSlot"
---

<objective>
Wire `ICbTopBarService` from Plan 10-08 into the TopBar render pipeline (via MainLayout) and migrate two pages — `RecipeView.razor` (read surface, RV-05 actions per the literal POLISH-04 requirement) and `RecipeEditor.razor` (write surface, Save/Cancel/Delete actions per D-58) — to feed their action rows through the slot. Adds the responsive `@media (max-width: 720px)` CSS rule per D-59 so the slot hides on narrow viewports and each page's inline fallback takes over.

Purpose (D-58, D-59): D-58 picks two pages to exercise the mechanism (one read, one write); D-59 picks hide-on-narrow over overflow-menu (simplest mobile path; each page handles its own narrow layout via the kept-inline fallback).

Output: MainLayout subscription wiring, TopBar wrapper class, two page migrations, two media-query CSS rules.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-qol-polish-consumer-surfaces/10-CONTEXT.md
@.planning/phases/10-qol-polish-consumer-surfaces/10-PATTERNS.md
@CLAUDE.md
@src/CookBot.Web/Services/ICbTopBarService.cs
@src/CookBot.Web/Components/Layout/MainLayout.razor
@src/CookBot.Web/Components/Layout/TopBar.razor
@src/CookBot.Web/Components/Pages/RecipeView.razor
@src/CookBot.Web/Components/Pages/RecipeEditor.razor

<interfaces>
<!-- MainLayout subscription pattern (analog: CbToastHost.razor:30-33, 77-82): -->
<!--   @inject ICbTopBarService TopBarService -->
<!--   @implements IDisposable -->
<!--   ... -->
<!--   <TopBar IsDarkMode="_isDarkMode" ... RightSlot="@TopBarService.RightSlot" /> -->
<!--   @code { -->
<!--       protected override void OnInitialized() => TopBarService.OnChanged += HandleSlotChanged; -->
<!--       private void HandleSlotChanged() => _ = InvokeAsync(StateHasChanged); -->
<!--       public void Dispose() => TopBarService.OnChanged -= HandleSlotChanged; -->
<!--   } -->

<!-- TopBar.razor existing structure at line 46: @RightSlot is rendered directly inside a flex container. -->
<!-- The change: wrap @RightSlot with `<div class="topbar-right-slot">@RightSlot</div>` so the CSS media -->
<!-- query has a stable hook. (Or add the class to the existing div surrounding @RightSlot if line 45's -->
<!-- `<div style="display:flex;align-items:center;gap:10px;">` is the right-slot container — verify by -->
<!-- reading TopBar.razor lines 40-60.) -->

<!-- Page consumer pattern (PATTERNS.md §"Web service" — page consumer with builder API, OR alternately -->
<!-- expose a RenderFragment fragment field in the page's @code and pass it directly to SetRightSlot): -->

<!-- Recommended: define the action row as a `RenderFragment` field in the page's @code block, render it -->
<!-- BOTH into the slot (via SetRightSlot in OnInitialized) AND inline (in a `<div class="recipe-actions-inline-fallback">` -->
<!-- wrapper) using `@_topBarActions`. Single source of truth — see PATTERNS.md "No Analog Found" §"RenderFragment built -->
<!-- imperatively in C#". -->

<!-- Example shape: -->
<!--   private RenderFragment? _topBarActions; -->
<!--   protected override void OnInitialized() -->
<!--   { -->
<!--       _topBarActions = @<text> -->
<!--           <CbButton OnClick="OpenShareDialog">Share</CbButton> -->
<!--           ... -->
<!--       </text>; -->
<!--       TopBarService.SetRightSlot(_topBarActions); -->
<!--   } -->
<!-- (The @<text> syntax produces a RenderFragment from Razor markup — verify Razor supports this in .NET 10; -->
<!--  if not, use the builder API or define the fragment in markup with @{ <RenderFragment> ... }.) -->

<!-- Inline fallback markup retains its existing content but is wrapped in: -->
<!--   <div class="recipe-actions-inline-fallback">@_topBarActions</div> -->
<!-- so the same RenderFragment renders both ways (slot + inline) on different viewports per CSS. -->

<!-- D-59 CSS rules (append to cookbot-design.css): -->
<!--   @media (max-width: 720px) { .topbar-right-slot { display: none !important; } } -->
<!--   @media (min-width: 721px) { .recipe-actions-inline-fallback { display: none !important; } } -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire ICbTopBarService into MainLayout + add TopBar wrapper class</name>
  <files>
    src/CookBot.Web/Components/Layout/MainLayout.razor,
    src/CookBot.Web/Components/Layout/TopBar.razor
  </files>
  <read_first>
    - src/CookBot.Web/Components/Layout/MainLayout.razor (full file — find where `<TopBar IsDarkMode="_isDarkMode" ... />` is rendered; note current parameter list)
    - src/CookBot.Web/Components/Layout/TopBar.razor (lines 40-60 — find the `@RightSlot` render site at line 46; identify the closest enclosing `<div>` for the wrapper-class application)
    - src/CookBot.Web/Components/Layout/CbToastHost.razor (analog — lines 30-33 OnInitialized subscription + 77-82 Dispose unsubscription)
    - src/CookBot.Web/Services/ICbTopBarService.cs (the interface; namespace `CookBot.Web.Services`)
    - .planning/phases/10-qol-polish-consumer-surfaces/10-PATTERNS.md (§"Web service — `ICbTopBarService` + `CbTopBarService`" → "MainLayout subscription pattern" excerpt)
  </read_first>
  <action>
    Step 1 — Edit `src/CookBot.Web/Components/Layout/MainLayout.razor`:
    - At the top of the file (after existing `@inject` directives), add: `@inject ICbTopBarService TopBarService`
    - Add `@implements IDisposable` near the top of the file (after any existing `@inherits` directives; before the markup body) — IF MainLayout does not already implement IDisposable. If it already does, the existing Dispose method gains a new line in step 4.
    - Locate the `<TopBar IsDarkMode="_isDarkMode" ... />` render line (~line 30 of MainLayout.razor). Add the parameter `RightSlot="@TopBarService.RightSlot"` to the TopBar tag attribute list. Preserve all existing parameters.
    - Add `using CookBot.Web.Services;` via an `@using CookBot.Web.Services` directive at the top of the file (or in `_Imports.razor` if the project uses that convention).
    - In the `@code` block:
      - Add `protected override void OnInitialized() { base.OnInitialized(); TopBarService.OnChanged += HandleSlotChanged; }` — if a `OnInitialized` already exists, add the subscription line inside it (do NOT replace existing logic; append).
      - Add `private void HandleSlotChanged() => _ = InvokeAsync(StateHasChanged);`
      - Add `public void Dispose() { TopBarService.OnChanged -= HandleSlotChanged; }` — if a Dispose already exists, add the unsubscribe line inside; otherwise create the method.

    Step 2 — Edit `src/CookBot.Web/Components/Layout/TopBar.razor`:
    - Locate the `@RightSlot` render at line 46. Wrap the render with a container `<div class="topbar-right-slot">@RightSlot</div>` IF the existing markup does not already provide a class for the slot wrapper. (If line 45's existing `<div style="display:flex;align-items:center;gap:10px;">` is the slot container, ADD a class attribute: `<div class="topbar-right-slot" style="display:flex;align-items:center;gap:10px;">` — preserve existing inline style.)
    - Do NOT modify the `[Parameter] public RenderFragment? RightSlot { get; set; }` declaration at line 80 — TopBar's API is unchanged per CONTEXT canonical_refs.
    - Do NOT modify any other markup or @code in TopBar.razor.
  </action>
  <verify>
    <automated>grep -q "@inject ICbTopBarService TopBarService" src/CookBot.Web/Components/Layout/MainLayout.razor &amp;&amp; grep -q "RightSlot=\"@TopBarService.RightSlot\"" src/CookBot.Web/Components/Layout/MainLayout.razor &amp;&amp; grep -q "TopBarService.OnChanged += HandleSlotChanged" src/CookBot.Web/Components/Layout/MainLayout.razor &amp;&amp; grep -q "TopBarService.OnChanged -= HandleSlotChanged" src/CookBot.Web/Components/Layout/MainLayout.razor &amp;&amp; grep -q "@implements IDisposable" src/CookBot.Web/Components/Layout/MainLayout.razor &amp;&amp; grep -q "topbar-right-slot" src/CookBot.Web/Components/Layout/TopBar.razor &amp;&amp; dotnet build /home/noah/Desktop/projects/freeloves-cookbot/CookBot.sln --nologo --verbosity quiet 2&gt;&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: MainLayout.razor injects `ICbTopBarService TopBarService`
    - Source assertion: MainLayout.razor passes `RightSlot="@TopBarService.RightSlot"` to `<TopBar>`
    - Source assertion: MainLayout.razor subscribes AND unsubscribes from `TopBarService.OnChanged`
    - Source assertion: MainLayout.razor implements IDisposable (via `@implements IDisposable`)
    - Source assertion: TopBar.razor contains a `topbar-right-slot` class attribute on the slot container
    - CLI output: `dotnet build` exits 0
  </acceptance_criteria>
  <done>MainLayout pipes the slot through to TopBar; TopBar has the stable CSS class for the responsive media query.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate RecipeView and RecipeEditor to use SetRightSlot with inline fallback</name>
  <files>
    src/CookBot.Web/Components/Pages/RecipeView.razor,
    src/CookBot.Web/Components/Pages/RecipeEditor.razor
  </files>
  <read_first>
    - src/CookBot.Web/Components/Pages/RecipeView.razor (lines 38-44 — the existing inline-above-hero RV-05 action row that must be migrated; identify the buttons/actions present)
    - src/CookBot.Web/Components/Pages/RecipeEditor.razor (lines 38-61 — the existing inline action row; identify Save/Cancel/Delete buttons)
    - src/CookBot.Web/Services/ICbTopBarService.cs (interface signature)
    - .planning/phases/10-qol-polish-consumer-surfaces/10-PATTERNS.md (§"Razor page modification — `RecipeView.razor` + `RecipeEditor.razor` (POLISH-04)" — example shape with the dual-render RenderFragment field)
    - .planning/phases/10-qol-polish-consumer-surfaces/10-CONTEXT.md (D-58, D-59)
  </read_first>
  <action>
    For BOTH files (`RecipeView.razor` and `RecipeEditor.razor`), apply the same pattern:

    1. Add `@inject ICbTopBarService TopBarService` at the top of the file (with the existing inject directives)
    2. Add `@implements IDisposable` near the top (preserving any existing `@implements`)
    3. Add `@using CookBot.Web.Services` if not already inherited from `_Imports.razor`

    4. **In `@code`**, declare `private RenderFragment? _topBarActions;`

    5. **Wrap the existing inline action row markup** (RecipeView lines 38-44 / RecipeEditor lines 38-61):
       - Wrap with `<div class="recipe-actions-inline-fallback">...existing markup...</div>` — DO NOT change the contents of the markup itself; just add the wrapper div with the new class
       - Convert the inline markup into a `RenderFragment` field defined in `@code`. Two viable approaches:

         **Approach A — RenderTreeBuilder API (concrete, no Razor syntax tricks):**
         In OnInitialized, set `_topBarActions = builder => { builder.OpenComponent<CbButton>(0); builder.AddAttribute(1, "Variant", CbButton.CbButtonVariant.Ghost); builder.AddAttribute(2, "OnClick", EventCallback.Factory.Create(this, ExistingHandler)); builder.AddAttribute(3, "ChildContent", (RenderFragment)(b => b.AddContent(4, "Share"))); builder.CloseComponent(); ... };` for each button currently in the inline row.

         **Approach B — Define markup as a `<RenderFragment Context>` template variable:**
         If the page already uses `<RenderFragment Name="X">...</RenderFragment>` style elsewhere, use that. Otherwise use Approach A.

       - **Render** `_topBarActions` BOTH inline (inside the `.recipe-actions-inline-fallback` div) AND through `TopBarService.SetRightSlot(_topBarActions)` so the SAME fragment is used on both viewports — single source of truth per PATTERNS.md "No Analog Found" recommendation.

    6. **In `OnInitialized` / `OnInitializedAsync`**: after `_topBarActions` is assigned, call `TopBarService.SetRightSlot(_topBarActions);`. **Note (per D-57):** the service auto-clears on navigation, so pages do not need to call `Clear()` themselves. The Dispose method may be empty (or omitted entirely) on the page side — but `@implements IDisposable` is required if the page wants to call Clear() defensively on dispose, which is acceptable but not required.

    7. **Verify the page still renders correctly at narrow viewports** — the inline fallback retains all functionality below 720px because the slot is hidden by CSS (Task 3 below adds the media rules).

    **For RecipeEditor specifically**, do not alter the cookbook reparenting CbSelect (Plan 10-10 introduces that). Plan 10-10 will inject its own card; the action-row migration in this plan is orthogonal.

    **For RecipeView specifically**, the existing inline row holds Edit/Share/Schedule/Cook this — keep all four in the migrated fragment.

    Do NOT touch any other markup or @code in either file.
  </action>
  <verify>
    <automated>grep -q "@inject ICbTopBarService TopBarService" src/CookBot.Web/Components/Pages/RecipeView.razor &amp;&amp; grep -q "@inject ICbTopBarService TopBarService" src/CookBot.Web/Components/Pages/RecipeEditor.razor &amp;&amp; grep -q "TopBarService.SetRightSlot" src/CookBot.Web/Components/Pages/RecipeView.razor &amp;&amp; grep -q "TopBarService.SetRightSlot" src/CookBot.Web/Components/Pages/RecipeEditor.razor &amp;&amp; grep -q "recipe-actions-inline-fallback" src/CookBot.Web/Components/Pages/RecipeView.razor &amp;&amp; grep -q "recipe-actions-inline-fallback" src/CookBot.Web/Components/Pages/RecipeEditor.razor &amp;&amp; grep -q "_topBarActions" src/CookBot.Web/Components/Pages/RecipeView.razor &amp;&amp; grep -q "_topBarActions" src/CookBot.Web/Components/Pages/RecipeEditor.razor &amp;&amp; dotnet build /home/noah/Desktop/projects/freeloves-cookbot/CookBot.sln --nologo --verbosity quiet 2&gt;&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: BOTH pages inject `ICbTopBarService`
    - Source assertion: BOTH pages call `TopBarService.SetRightSlot(_topBarActions)` in lifecycle
    - Source assertion: BOTH pages wrap their inline action row with `class="recipe-actions-inline-fallback"`
    - Source assertion: BOTH pages declare `_topBarActions` as a `RenderFragment` field
    - CLI output: `dotnet build` exits 0
  </acceptance_criteria>
  <done>Both pages route their action rows through the TopBar slot AND retain the inline fallback for narrow viewports.</done>
</task>

<task type="auto">
  <name>Task 3: Add responsive media-query CSS rules for slot / inline-fallback toggle</name>
  <files>src/CookBot.Web/wwwroot/css/cookbot-design.css</files>
  <read_first>
    - src/CookBot.Web/wwwroot/css/cookbot-design.css (full file or at minimum the last 60 lines — find the end of the file for the appendage; verify the existing `[data-density]` / `[data-accent]` selectors are not @media-scoped already)
    - .planning/phases/10-qol-polish-consumer-surfaces/10-PATTERNS.md (§"CSS — media query for TopBar.RightSlot hide-below-720px (D-59)" — exact rules to append)
    - .planning/phases/10-qol-polish-consumer-surfaces/10-CONTEXT.md (D-59 — hide-on-narrow contract)
  </read_first>
  <action>
    Append the following two `@media` rules to the END of `src/CookBot.Web/wwwroot/css/cookbot-design.css`:

    ```
    /* Phase 10 / Plan 10-09 / POLISH-04 / D-59 — responsive TopBar.RightSlot.
       Below 720px the slot is hidden; each migrating page renders its own
       inline-action-row fallback (class .recipe-actions-inline-fallback). */
    @media (max-width: 720px) {
        .topbar-right-slot { display: none !important; }
    }
    @media (min-width: 721px) {
        .recipe-actions-inline-fallback { display: none !important; }
    }
    ```

    Append as a new block AFTER all existing rules. Do NOT modify any existing rule.
  </action>
  <verify>
    <automated>grep -q "@media (max-width: 720px)" src/CookBot.Web/wwwroot/css/cookbot-design.css &amp;&amp; grep -q ".topbar-right-slot" src/CookBot.Web/wwwroot/css/cookbot-design.css &amp;&amp; grep -q "@media (min-width: 721px)" src/CookBot.Web/wwwroot/css/cookbot-design.css &amp;&amp; grep -q ".recipe-actions-inline-fallback" src/CookBot.Web/wwwroot/css/cookbot-design.css</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: CSS file contains a `@media (max-width: 720px)` rule that hides `.topbar-right-slot`
    - Source assertion: CSS file contains a `@media (min-width: 721px)` rule that hides `.recipe-actions-inline-fallback`
    - Source assertion: no pre-existing CSS rule was modified (only appended)
  </acceptance_criteria>
  <done>Responsive CSS rules in place; layout switches between slot and inline at 720px.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| page → TopBar slot | RenderFragment is built by trusted page code; no user input flows into the markup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-09-01 | Denial of Service | MainLayout OnChanged leak | mitigate | `@implements IDisposable` + unsubscribe in Dispose; tested via Plan 10-08 T-10-08 test 4 already; this plan does the same unsubscribe pattern in MainLayout |
| T-10-09-02 | Tampering | RenderFragment from external source | accept | RenderFragment is constructed by the migrating page's own code; no external input is in the markup chain |
</threat_model>

<verification>
- `dotnet build` exits 0
- Manual smoke (deferred): visit RecipeView → action row shows in TopBar at >720px and below the hero on ≤720px; same for RecipeEditor
- `grep "topbar-right-slot"` finds the class in both TopBar.razor and cookbot-design.css
</verification>

<success_criteria>
- MainLayout wires the slot through; TopBar wraps its render with the responsive class
- Both target pages migrate their action rows
- Each page keeps an inline fallback for narrow viewports
- CSS toggles slot ↔ inline-fallback at the 720px boundary
- All existing functionality preserved
</success_criteria>

<output>
Create `.planning/phases/10-qol-polish-consumer-surfaces/10-09-SUMMARY.md` when done.
</output>
