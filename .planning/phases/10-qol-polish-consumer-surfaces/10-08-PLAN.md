---
phase: 10-qol-polish-consumer-surfaces
plan: 08
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - src/CookBot.Web/Services/ICbTopBarService.cs
  - src/CookBot.Web/Services/CbTopBarService.cs
  - src/CookBot.Web/Program.cs
  - tests/CookBot.Tests/Services/CbTopBarServiceTests.cs
autonomous: true
requirements: [POLISH-04]
tags: [top-bar, slot-service, scoped, navigation-manager, idisposable]

must_haves:
  truths:
    - "`ICbTopBarService` exposes `RightSlot` (RenderFragment? getter), `OnChanged` event, `SetRightSlot(RenderFragment?)`, `Clear()`"
    - "`CbTopBarService` subscribes to `NavigationManager.LocationChanged` in its constructor and auto-clears `RightSlot` on every location change (D-57)"
    - "`CbTopBarService` implements `IDisposable` and unsubscribes from `NavigationManager.LocationChanged` in `Dispose()`"
    - "`Clear()` is idempotent — calling Clear when RightSlot is already null does NOT fire `OnChanged`"
    - "DI registration in Program.cs registers `ICbTopBarService` as Scoped (one instance per SignalR circuit)"
    - "Test `TestNavigationManager.NotifyLocationChanged` re-emits the protected base method using parameter name `isInternalNavigation` — the .NET 10 canonical signature (W-06)"
  artifacts:
    - path: "src/CookBot.Web/Services/ICbTopBarService.cs"
      provides: "Public interface with RightSlot/OnChanged/SetRightSlot/Clear members"
    - path: "src/CookBot.Web/Services/CbTopBarService.cs"
      provides: "internal sealed implementation with NavigationManager subscription + IDisposable"
    - path: "src/CookBot.Web/Program.cs"
      provides: "AddScoped<ICbTopBarService, CbTopBarService>() registration"
    - path: "tests/CookBot.Tests/Services/CbTopBarServiceTests.cs"
      provides: "Four Fact tests covering SetRightSlot/LocationChanged-auto-clear/Clear-idempotent/Dispose-unsubscribes"
  key_links:
    - from: "src/CookBot.Web/Services/CbTopBarService.cs"
      to: "NavigationManager.LocationChanged"
      via: "constructor subscription, Dispose unsubscription"
      pattern: "NavigationManager"
---

<objective>
Ship the scoped service that backs Plan 10-09's TopBar.RightSlot wiring. The service is first-of-kind in two ways (per PATTERNS.md Layering Note 2): it is the first Web-layer scoped service to subscribe to `NavigationManager.LocationChanged` in its constructor, and it is the first to need explicit `IDisposable` for the unsubscription contract.

Purpose (D-56, D-57): D-56 picked `ICbTopBarService` over CascadingValue/CascadingParameter — honors ROADMAP success criteria 4 literal text. D-57 auto-clears on every navigation so pages do not have to remember to reset on unload.

Output: Interface file, implementation file with `IDisposable`, Program.cs registration, four-Fact test class with a hand-rolled TestNavigationManager.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-qol-polish-consumer-surfaces/10-CONTEXT.md
@.planning/phases/10-qol-polish-consumer-surfaces/10-PATTERNS.md
@CLAUDE.md
@src/CookBot.Web/Services/CbToastService.cs

<interfaces>
<!-- PATTERNS.md §"Web service — ICbTopBarService + CbTopBarService" — exact analog excerpt: -->

<!-- public interface ICbTopBarService -->
<!-- { -->
<!--     RenderFragment? RightSlot { get; } -->
<!--     event Action? OnChanged; -->
<!--     void SetRightSlot(RenderFragment? content); -->
<!--     void Clear(); -->
<!-- } -->

<!-- internal sealed class CbTopBarService : ICbTopBarService, IDisposable -->
<!-- { -->
<!--     private readonly NavigationManager _nav; -->
<!--     public RenderFragment? RightSlot { get; private set; } -->
<!--     public event Action? OnChanged; -->
<!--     public CbTopBarService(NavigationManager nav) -->
<!--     { -->
<!--         _nav = nav; -->
<!--         _nav.LocationChanged += HandleLocationChanged; -->
<!--     } -->
<!--     public void SetRightSlot(RenderFragment? content) -->
<!--     { -->
<!--         RightSlot = content; -->
<!--         OnChanged?.Invoke(); -->
<!--     } -->
<!--     public void Clear() -->
<!--     { -->
<!--         if (RightSlot is null) return;  // idempotent — no fire if already empty -->
<!--         RightSlot = null; -->
<!--         OnChanged?.Invoke(); -->
<!--     } -->
<!--     private void HandleLocationChanged(object? sender, LocationChangedEventArgs e) => Clear(); -->
<!--     public void Dispose() => _nav.LocationChanged -= HandleLocationChanged; -->
<!-- } -->

<!-- DI registration in Program.cs (after the existing AddScoped<ICbDialogService, CbDialogService> at line 35): -->
<!--   builder.Services.AddScoped<ICbTopBarService, CbTopBarService>(); -->

<!-- W-06 RESOLUTION — TestNavigationManager parameter name PINNED to `isInternalNavigation`: -->
<!-- The .NET 10 canonical NavigationManager.NotifyLocationChanged signature is: -->
<!--   protected void NotifyLocationChanged(string uri, bool isInternalNavigation) -->
<!-- The test helper exposes this verbatim — do NOT use `isInternalNav` (shortened) or any other name. -->
<!-- DO NOT "verify and adjust" — the parameter name IS `isInternalNavigation` for .NET 10. -->
<!-- -->
<!-- private sealed class TestNavigationManager : NavigationManager -->
<!-- { -->
<!--     public TestNavigationManager() { Initialize("http://localhost/", "http://localhost/"); } -->
<!--     public new void NotifyLocationChanged(string uri, bool isInternalNavigation) -->
<!--         => base.NotifyLocationChanged(uri, isInternalNavigation); -->
<!-- } -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write CbTopBarServiceTests (RED) — four behavior assertions</name>
  <files>tests/CookBot.Tests/Services/CbTopBarServiceTests.cs</files>
  <read_first>
    - tests/CookBot.Tests/Services/FractionFormatterTests.cs (analog — pure xUnit Fact pattern with no DbContext)
    - .planning/phases/10-qol-polish-consumer-surfaces/10-PATTERNS.md (§"Tests — `CbTopBarServiceTests.cs`" — full excerpt with TestNavigationManager)
    - .planning/phases/10-qol-polish-consumer-surfaces/10-CONTEXT.md (D-56, D-57)
  </read_first>
  <behavior>
    Test class with a private `TestNavigationManager : NavigationManager` and four `[Fact]` methods.
  </behavior>
  <action>
    Create `tests/CookBot.Tests/Services/CbTopBarServiceTests.cs`:
    - File-scoped namespace `namespace CookBot.Tests.Services;`
    - Usings: `Microsoft.AspNetCore.Components`, `Microsoft.AspNetCore.Components.Routing`, `CookBot.Web.Services`, `Xunit`
    - `public class CbTopBarServiceTests`

    Private nested class — **W-06 RESOLUTION: the second-parameter name is `isInternalNavigation`** (the .NET 10 canonical name). Do NOT use `isInternalNav` (shortened) or any other variant. Do NOT include "verify and adjust" language — the name is pinned.
    ```
    private sealed class TestNavigationManager : NavigationManager
    {
        public TestNavigationManager() { Initialize("http://localhost/", "http://localhost/"); }
        public new void NotifyLocationChanged(string uri, bool isInternalNavigation)
            => base.NotifyLocationChanged(uri, isInternalNavigation);
    }
    ```

    Four `[Fact]` tests:

    1. **`SetRightSlot_RaisesOnChanged`** — Construct `nav` + `svc`. Track invocation count `fired`. Subscribe `svc.OnChanged += () => fired++;`. Call `svc.SetRightSlot(builder => builder.AddContent(0, "x"));`. Assert `fired == 1` AND `svc.RightSlot != null`.

    2. **`LocationChanged_AutoClearsRightSlot`** — Construct, set a fragment, raise `nav.NotifyLocationChanged("http://localhost/recipes/1", isInternalNavigation: true);` (named-argument call locks the parameter name at the test site too). Assert `svc.RightSlot == null`.

    3. **`Clear_Idempotent_DoesNotFireWhenAlreadyEmpty`** — Construct, do NOT call SetRightSlot. Track fired count. Call `svc.Clear()`. Assert `fired == 0` (no event raised because state was already null).

    4. **`Dispose_UnsubscribesFromNavigationManager`** — Construct svc, call `svc.Dispose()`. Then subscribe `OnChanged` and call `nav.NotifyLocationChanged("http://localhost/x", isInternalNavigation: true);`. Assert `fired == 0` (handler is gone — LocationChanged no longer triggers Clear which would have triggered OnChanged via the prior SetRightSlot; this test confirms the unsubscribe contract).

    All four tests fail at this point (the service class does not yet exist) — RED gate.
  </action>
  <verify>
    <automated>test -f tests/CookBot.Tests/Services/CbTopBarServiceTests.cs &amp;&amp; grep -q "SetRightSlot_RaisesOnChanged" tests/CookBot.Tests/Services/CbTopBarServiceTests.cs &amp;&amp; grep -q "LocationChanged_AutoClearsRightSlot" tests/CookBot.Tests/Services/CbTopBarServiceTests.cs &amp;&amp; grep -q "Clear_Idempotent_DoesNotFireWhenAlreadyEmpty" tests/CookBot.Tests/Services/CbTopBarServiceTests.cs &amp;&amp; grep -q "Dispose_UnsubscribesFromNavigationManager" tests/CookBot.Tests/Services/CbTopBarServiceTests.cs &amp;&amp; grep -q "TestNavigationManager" tests/CookBot.Tests/Services/CbTopBarServiceTests.cs &amp;&amp; grep -q "bool isInternalNavigation" tests/CookBot.Tests/Services/CbTopBarServiceTests.cs &amp;&amp; ! grep -q "bool isInternalNav[^i]" tests/CookBot.Tests/Services/CbTopBarServiceTests.cs</automated>
  </verify>
  <acceptance_criteria>
    - File exists with all four test methods
    - Source assertion: declares a private `TestNavigationManager : NavigationManager` nested class
    - **W-06 source assertion:** the override parameter list reads `(string uri, bool isInternalNavigation)` (full canonical name) — file does NOT contain `bool isInternalNav` followed by anything other than `igation`
    - Source assertion: each `[Fact]` exercises the documented behavior (verify by reading the test bodies — assertions match the description)
    - Behavior assertion: tests fail to compile right now because `CbTopBarService` type does not exist (RED gate)
  </acceptance_criteria>
  <done>Test file exists with the four behaviors specified; intentionally fails to build until Task 2; the NavigationManager parameter name is pinned to `isInternalNavigation`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement ICbTopBarService + CbTopBarService (GREEN) + register in Program.cs</name>
  <files>
    src/CookBot.Web/Services/ICbTopBarService.cs,
    src/CookBot.Web/Services/CbTopBarService.cs,
    src/CookBot.Web/Program.cs
  </files>
  <read_first>
    - .planning/phases/10-qol-polish-consumer-surfaces/10-PATTERNS.md (§"Web service — `ICbTopBarService` + `CbTopBarService`" — full interface + impl excerpt; §"Layering Note 2" — IDisposable contract explained)
    - .planning/phases/10-qol-polish-consumer-surfaces/10-CONTEXT.md (D-56, D-57)
    - src/CookBot.Web/Services/CbToastService.cs (analog — interface + internal sealed impl with `event Action<T>?` pattern)
    - src/CookBot.Web/Program.cs (line 35 area — existing `AddScoped<ICbDialogService, CbDialogService>()` placement)
  </read_first>
  <behavior>
    Implementation satisfies all four tests from Task 1 by raising OnChanged on SetRightSlot, auto-clearing on LocationChanged, suppressing OnChanged on idempotent Clear, and unsubscribing in Dispose.
  </behavior>
  <action>
    Step 1 — Create `src/CookBot.Web/Services/ICbTopBarService.cs`:
    - File-scoped namespace `namespace CookBot.Web.Services;`
    - `using Microsoft.AspNetCore.Components;` (for RenderFragment)
    - `public interface ICbTopBarService` with the four members listed in `<interfaces>` block above (RightSlot getter, OnChanged event, SetRightSlot method, Clear method)
    - XML doc summary on the interface noting Phase 10 / Plan 10-08 / POLISH-04 / D-56

    Step 2 — Create `src/CookBot.Web/Services/CbTopBarService.cs`:
    - File-scoped namespace `namespace CookBot.Web.Services;`
    - Usings: `Microsoft.AspNetCore.Components`, `Microsoft.AspNetCore.Components.Routing` (for `LocationChangedEventArgs`)
    - `internal sealed class CbTopBarService : ICbTopBarService, IDisposable`
    - Private readonly field `_nav` of type `NavigationManager`
    - Public auto-property `RenderFragment? RightSlot { get; private set; }`
    - Public event `public event Action? OnChanged;`
    - Constructor: `public CbTopBarService(NavigationManager nav)` — assigns `_nav = nav;` and subscribes `_nav.LocationChanged += HandleLocationChanged;`
    - Method `public void SetRightSlot(RenderFragment? content)`: `RightSlot = content; OnChanged?.Invoke();`
    - Method `public void Clear()`: **early-return idempotent guard** — `if (RightSlot is null) return;` then `RightSlot = null; OnChanged?.Invoke();`
    - Private `void HandleLocationChanged(object? sender, LocationChangedEventArgs e) => Clear();`
    - `public void Dispose() => _nav.LocationChanged -= HandleLocationChanged;`
    - XML doc summary referencing Phase 10 / Plan 10-08 / D-56 / D-57 / PATTERNS.md Layering Note 2

    Step 3 — Edit `src/CookBot.Web/Program.cs`:
    - Locate `builder.Services.AddScoped<ICbDialogService, CbDialogService>();` (line 35)
    - Insert immediately after: `builder.Services.AddScoped<ICbTopBarService, CbTopBarService>();`
    - Add `using CookBot.Web.Services;` if not already present (verify; it likely is, given CbDialogService is in the same namespace)

    Implementation MUST turn all four tests from Task 1 green.
  </action>
  <verify>
    <automated>test -f src/CookBot.Web/Services/ICbTopBarService.cs &amp;&amp; test -f src/CookBot.Web/Services/CbTopBarService.cs &amp;&amp; grep -q "public interface ICbTopBarService" src/CookBot.Web/Services/ICbTopBarService.cs &amp;&amp; grep -q "internal sealed class CbTopBarService : ICbTopBarService, IDisposable" src/CookBot.Web/Services/CbTopBarService.cs &amp;&amp; grep -q "_nav.LocationChanged += HandleLocationChanged" src/CookBot.Web/Services/CbTopBarService.cs &amp;&amp; grep -q "_nav.LocationChanged -= HandleLocationChanged" src/CookBot.Web/Services/CbTopBarService.cs &amp;&amp; grep -q "if (RightSlot is null) return" src/CookBot.Web/Services/CbTopBarService.cs &amp;&amp; grep -q "AddScoped<ICbTopBarService, CbTopBarService>" src/CookBot.Web/Program.cs &amp;&amp; dotnet test tests/CookBot.Tests/CookBot.Tests.csproj --filter "FullyQualifiedName~CbTopBarServiceTests" --nologo --verbosity quiet 2&gt;&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File `src/CookBot.Web/Services/ICbTopBarService.cs` declares `public interface ICbTopBarService` with all four members
    - File `src/CookBot.Web/Services/CbTopBarService.cs` declares `internal sealed class CbTopBarService : ICbTopBarService, IDisposable`
    - Source assertion: ctor contains `_nav.LocationChanged += HandleLocationChanged`
    - Source assertion: `Dispose()` contains `_nav.LocationChanged -= HandleLocationChanged`
    - Source assertion: `Clear()` body contains the idempotent guard `if (RightSlot is null) return;`
    - Source assertion: Program.cs registers via `AddScoped<ICbTopBarService, CbTopBarService>()`
    - Test command: `dotnet test --filter FullyQualifiedName~CbTopBarServiceTests` exits 0 with all 4 tests passing
  </acceptance_criteria>
  <done>Service interface + implementation exist with the four-test contract satisfied; DI registration is in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scoped service → SignalR circuit | One instance per circuit; NavigationManager events scoped to the circuit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-08-01 | Denial of Service | Memory leak via missed Dispose | mitigate | IDisposable contract + unsubscribe in Dispose; Blazor scoped services dispose with circuit; test `Dispose_UnsubscribesFromNavigationManager` gates the unsubscribe |
| T-10-08-02 | Information Disclosure | Cross-circuit state leakage | accept | Scoped lifetime guarantees one instance per circuit; no static state is mutated; RenderFragment is a delegate over the page's BuildRenderTree context only |
</threat_model>

<verification>
- `dotnet test --filter CbTopBarServiceTests` exits 0
- `dotnet build` exits 0
- Behavior: an instance constructed against a real NavigationManager subscribes successfully (verified through test 1 SetRightSlot path); Dispose tears it down (verified by test 4)
- `grep "bool isInternalNavigation" tests/CookBot.Tests/Services/CbTopBarServiceTests.cs` returns at least 1 hit (W-06 enforced)
</verification>

<success_criteria>
- All four CbTopBarServiceTests pass
- Service is registered Scoped in Program.cs
- IDisposable contract is enforced and tested
- Clear() is verified idempotent
- TestNavigationManager parameter name is `isInternalNavigation` (.NET 10 canonical) — W-06 enforced
</success_criteria>

<output>
Create `.planning/phases/10-qol-polish-consumer-surfaces/10-08-SUMMARY.md` when done.
</output>
</content>
</invoke>