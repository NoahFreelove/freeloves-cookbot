---
phase: 10-qol-polish-consumer-surfaces
plan: 10
type: execute
wave: 5
depends_on: ["10-09"]
files_modified:
  - src/CookBot.Application/Services/RecipeService.cs
  - src/CookBot.Web/Components/Pages/RecipeEditor.razor
autonomous: true
requirements: [POLISH-01]
tags: [recipe-service, recipe-editor, cookbook-reparenting, authz]

must_haves:
  truths:
    - "`RecipeService.UpdateAsync` accepts an optional `int? newCookbookId = null` argument"
    - "When `newCookbookId` is non-null and differs from the recipe's current `CookbookId`, the service validates the destination cookbook exists AND its `UserId == userId` BEFORE assigning `recipe.CookbookId = newCookbookId.Value`"
    - "Validation failure throws `UnauthorizedAccessException` for an unowned destination cookbook OR `InvalidOperationException` for a non-existent destination cookbook — same fail-fast contract as the existing CreateAsync at RecipeService.cs:38-39"
    - "RecipeEditor exposes a `<CbSelect>` listing the current user's cookbooks; Save passes the selected cookbookId to `RecipeService.UpdateAsync`"
    - "On a cookbook change, the page navigates to the updated recipe's view; on no-change, current behavior preserved"
  artifacts:
    - path: "src/CookBot.Application/Services/RecipeService.cs"
      provides: "UpdateAsync signature extended with `int? newCookbookId = null`; reparent block uses inline `cookbook.UserId == userId` check (PATTERNS.md correction #5)"
    - path: "src/CookBot.Web/Components/Pages/RecipeEditor.razor"
      provides: "Cookbook reparenting CbSelect bound to `_selectedCookbookId`; Save wires through"
  key_links:
    - from: "src/CookBot.Application/Services/RecipeService.cs"
      to: "destination cookbook ownership"
      via: "inline check (NOT db.UserCanAccessCookbookAsync — does not exist)"
      pattern: "destination\\.UserId != userId"
    - from: "src/CookBot.Web/Components/Pages/RecipeEditor.razor"
      to: "RecipeService.UpdateAsync"
      via: "Save handler with newCookbookId argument"
      pattern: "UpdateAsync.*_selectedCookbookId"
---

<objective>
Close POLISH-01 — allow users to move a recipe to a different cookbook they own, from the Recipe Editor. The change is two-part: backend extension of `RecipeService.UpdateAsync` to accept a `newCookbookId` with inline destination-ownership validation (per PATTERNS.md correction #5, NOT the non-existent `db.UserCanAccessCookbookAsync`), and a CbSelect on RecipeEditor that surfaces the user's cookbook list and wires through Save.

Purpose: Closes v1.2 D-26. Removes the "no way to reparent a recipe" friction point from the v1.2 carry-forward list.

Output: One method-signature change in RecipeService + one CbSelect card + one Save-handler argument.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-qol-polish-consumer-surfaces/10-CONTEXT.md
@.planning/phases/10-qol-polish-consumer-surfaces/10-PATTERNS.md
@CLAUDE.md
@src/CookBot.Application/Services/RecipeService.cs
@src/CookBot.Web/Components/Pages/RecipeEditor.razor
@src/CookBot.Web/Components/Pages/SaveRecipeDialog.razor

<interfaces>
<!-- CONTEXT.md notes the `db.UserCanAccessCookbookAsync` ownership check; PATTERNS.md correction #5 -->
<!-- documents that this method DOES NOT EXIST. Use the inline pattern from RecipeService.cs:38-39: -->
<!--   var cookbook = await _cookbookRepo.GetByIdAsync(cookbookId) -->
<!--       ?? throw new InvalidOperationException("Cookbook not found."); -->
<!--   if (cookbook.UserId != userId) -->
<!--       throw new UnauthorizedAccessException("You do not own this cookbook."); -->

<!-- Current UpdateAsync signature at RecipeService.cs:139: -->
<!--   public async Task<Recipe> UpdateAsync(int recipeId, int userId, ParsedRecipe parsed) -->

<!-- New signature (POLISH-01): -->
<!--   public async Task<Recipe> UpdateAsync(int recipeId, int userId, ParsedRecipe parsed, int? newCookbookId = null) -->

<!-- Reparent logic inserted AFTER the existing ownership check at line 148, BEFORE `recipe.Name = parsed.Name`: -->
<!--   if (newCookbookId.HasValue && newCookbookId.Value != recipe.CookbookId) -->
<!--   { -->
<!--       var destination = await _cookbookRepo.GetByIdAsync(newCookbookId.Value) -->
<!--           ?? throw new InvalidOperationException("Destination cookbook not found."); -->
<!--       if (destination.UserId != userId) -->
<!--           throw new UnauthorizedAccessException("You do not own the destination cookbook."); -->
<!--       recipe.CookbookId = newCookbookId.Value; -->
<!--   } -->

<!-- CbSelect analog at SaveRecipeDialog.razor:11-16 — CbSelect<int> bound to int with foreach CbOption -->
<!-- emitting `<CbOption TValue="int" Value="@cb.Id" Label="@cb.Name" />` per cookbook. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend RecipeService.UpdateAsync with newCookbookId parameter + reparent block</name>
  <files>src/CookBot.Application/Services/RecipeService.cs</files>
  <read_first>
    - src/CookBot.Application/Services/RecipeService.cs (lines 139-160 — the full UpdateAsync method body; identify the ownership check at line 144-148 and the recipe-mutation block that begins at line 149+)
    - src/CookBot.Application/Services/RecipeService.cs (lines 33-50 — analog for the inline ownership-check pattern used at CreateAsync; copy verbatim)
    - .planning/phases/10-qol-polish-consumer-surfaces/10-PATTERNS.md (§"Application service modification — `RecipeService.UpdateAsync` cookbook reparenting (POLISH-01)" — full excerpt with the reparent block)
    - .planning/phases/10-qol-polish-consumer-surfaces/10-CONTEXT.md (POLISH-01 + Claude's discretion supplement — UI is in Task 2; this task is backend-only)
  </read_first>
  <action>
    Edit `src/CookBot.Application/Services/RecipeService.cs`:

    Step 1 — Change the method signature at line 139 from `public async Task<Recipe> UpdateAsync(int recipeId, int userId, ParsedRecipe parsed)` to `public async Task<Recipe> UpdateAsync(int recipeId, int userId, ParsedRecipe parsed, int? newCookbookId = null)`. The new parameter is OPTIONAL with a `null` default so every existing call site continues to compile unchanged.

    Step 2 — Insert the reparent block AFTER the existing ownership check (currently around line 148, the `if (cookbook.UserId != userId)` line and its throw) and BEFORE the first `recipe.Name = parsed.Name` mutation line. The block contains a guard `if (newCookbookId.HasValue && newCookbookId.Value != recipe.CookbookId)`, inside which: load `destination` via `_cookbookRepo.GetByIdAsync(newCookbookId.Value)` with a null-coalescing throw of `InvalidOperationException("Destination cookbook not found.")`, then check `if (destination.UserId != userId) throw new UnauthorizedAccessException("You do not own the destination cookbook.");`, then assign `recipe.CookbookId = newCookbookId.Value;`.

    **PATTERNS.md correction #5 enforcement:** use the inline ownership check above. DO NOT call any `db.UserCanAccessCookbookAsync` extension — such a method does not exist in this codebase.

    Step 3 — Add XML doc on the method (or update existing doc) noting: `Phase 10 / Plan 10-10 / POLISH-01 — newCookbookId allows the caller to reparent the recipe; destination ownership validated via inline check (PATTERNS.md correction #5).`

    Do NOT modify any other method body in RecipeService.cs. Do NOT modify CreateAsync's existing ownership check.
  </action>
  <verify>
    <automated>grep -q "int? newCookbookId = null" src/CookBot.Application/Services/RecipeService.cs &amp;&amp; grep -q "Destination cookbook not found" src/CookBot.Application/Services/RecipeService.cs &amp;&amp; grep -q "You do not own the destination cookbook" src/CookBot.Application/Services/RecipeService.cs &amp;&amp; grep -q "destination.UserId != userId" src/CookBot.Application/Services/RecipeService.cs &amp;&amp; ! grep -q "UserCanAccessCookbookAsync" src/CookBot.Application/Services/RecipeService.cs &amp;&amp; dotnet build /home/noah/Desktop/projects/freeloves-cookbot/CookBot.sln --nologo --verbosity quiet 2&gt;&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `UpdateAsync` signature carries `int? newCookbookId = null` as the optional final parameter
    - Source assertion: reparent block contains `destination.UserId != userId` (inline ownership check per PATTERNS.md #5)
    - Source assertion: `grep UserCanAccessCookbookAsync src/CookBot.Application/Services/RecipeService.cs` returns ZERO matches (PATTERNS.md #5 honored)
    - Source assertion: `InvalidOperationException("Destination cookbook not found.")` present
    - Source assertion: `UnauthorizedAccessException("You do not own the destination cookbook.")` present
    - CLI output: `dotnet build` exits 0 — confirms every existing UpdateAsync caller still compiles
  </acceptance_criteria>
  <done>UpdateAsync accepts newCookbookId with inline destination-ownership validation; existing callers continue to work.</done>
</task>

<task type="auto">
  <name>Task 2: Add cookbook reparenting CbSelect to RecipeEditor and wire to Save handler</name>
  <files>src/CookBot.Web/Components/Pages/RecipeEditor.razor</files>
  <read_first>
    - src/CookBot.Web/Components/Pages/RecipeEditor.razor (full file — find the existing Save handler that calls `RecipeService.UpdateAsync`; identify where the meta rail / right column begins so the new card lands in a logical place; locate the existing recipe-load path that gets the current recipe's CookbookId)
    - src/CookBot.Web/Components/Pages/SaveRecipeDialog.razor (lines 11-16 — analog for `<CbSelect TValue="int">` plus `@foreach <CbOption TValue="int" Value="@cb.Id" Label="@cb.Name" />`)
    - src/CookBot.Application/Services/RecipeService.cs (modified UpdateAsync signature from Task 1)
    - .planning/phases/10-qol-polish-consumer-surfaces/10-PATTERNS.md (§"Razor page modification — `RecipeView.razor` + `RecipeEditor.razor` (POLISH-04)" → "RecipeEditor.razor cookbook reparenting CbSelect (POLISH-01)" excerpt)
    - src/CookBot.Web/Components/Pages/AiChat.razor (lines 687-691 — analog for loading the user's cookbooks via DbContext.Cookbooks.Where + ToListAsync)
  </read_first>
  <action>
    Edit `src/CookBot.Web/Components/Pages/RecipeEditor.razor`:

    Step 1 — Add a new `<CbCard>` in the right-column meta rail (analog: SaveRecipeDialog.razor:11-16). Place it ABOVE any existing meta-rail cards so it serves as the "this recipe lives in" indicator. The card contains a `<CbEyebrow>Cookbook</CbEyebrow>` and a `<CbSelect TValue="int" Value="_selectedCookbookId" ValueChanged="@((int v) => _selectedCookbookId = v)">` containing a `@foreach (var cb in _userCookbooks)` loop that emits `<CbOption TValue="int" Value="@cb.Id" Label="@cb.Name" />` per cookbook.

    Step 2 — In `@code`:
    - Add `private int _selectedCookbookId;`
    - Add `private List<Cookbook> _userCookbooks = new();`
    - In the existing recipe-load lifecycle hook (the method that already loads the `Recipe` into local state), AFTER the recipe is loaded: set `_selectedCookbookId = recipe.CookbookId;` and load `_userCookbooks = await DbContext.Cookbooks.AsNoTracking().Where(c => c.UserId == UserService.CurrentUserId.Value).OrderBy(c => c.Name).ToListAsync();`

    Step 3 — Modify the existing Save handler to pass `_selectedCookbookId` to `RecipeService.UpdateAsync`:
    - Find the existing call `await RecipeService.UpdateAsync(recipeId, userId, parsed);`
    - Change to `var updatedRecipe = await RecipeService.UpdateAsync(recipeId, userId, parsed, _selectedCookbookId);`
    - Capture the original `recipe.CookbookId` into `originalCookbookId` BEFORE save; if `_selectedCookbookId != originalCookbookId` AND the save succeeded, call `Navigation.NavigateTo($"/recipes/{recipeId}");` to land on the destination context. If the existing Save handler already navigates after save, do not double-navigate.

    Step 4 — Add `using CookBot.Domain.Entities;` if not already present (for `Cookbook` type).

    Do NOT modify any other Save-related logic. Do NOT touch the photo composite, the ingredient editor, or the step composer.
  </action>
  <verify>
    <automated>grep -q "_selectedCookbookId" src/CookBot.Web/Components/Pages/RecipeEditor.razor &amp;&amp; grep -q "_userCookbooks" src/CookBot.Web/Components/Pages/RecipeEditor.razor &amp;&amp; grep -q "CbSelect TValue=\"int\"" src/CookBot.Web/Components/Pages/RecipeEditor.razor &amp;&amp; grep -q "UpdateAsync.*_selectedCookbookId" src/CookBot.Web/Components/Pages/RecipeEditor.razor &amp;&amp; dotnet build /home/noah/Desktop/projects/freeloves-cookbot/CookBot.sln --nologo --verbosity quiet 2&gt;&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `_selectedCookbookId` field declared in RecipeEditor.razor
    - Source assertion: `_userCookbooks` field declared and populated from `DbContext.Cookbooks` filtered by current user
    - Source assertion: `<CbSelect TValue="int"` present in markup
    - Source assertion: `RecipeService.UpdateAsync(... _selectedCookbookId)` call present
    - CLI output: `dotnet build` exits 0
  </acceptance_criteria>
  <done>RecipeEditor surfaces the cookbook list and routes Save through the new reparenting argument.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → service | RecipeEditor surfaces an int CookbookId from a user-controlled select; service re-validates ownership before applying |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-10-01 | Elevation of Privilege | Cross-user cookbook reparenting | mitigate | Inline destination-ownership check (`destination.UserId != userId`) inside RecipeService.UpdateAsync runs BEFORE the assignment. Attempting to send another user's cookbookId throws UnauthorizedAccessException. PATTERNS.md correction #5 enforces the inline check (no reliance on a non-existent extension method). |
| T-10-10-02 | Information Disclosure | Cookbook list exposure | mitigate | `_userCookbooks` query filters by `c.UserId == currentUserId`; the user only ever sees their own cookbooks in the select |
| T-10-10-03 | Tampering | Optional parameter backward compatibility | accept | `int? newCookbookId = null` default preserves every existing UpdateAsync call site; behavior is identical when the parameter is omitted |
</threat_model>

<verification>
- `dotnet build` exits 0
- Manual smoke (deferred): edit a recipe; pick a different cookbook from the select; save; confirm the recipe lands in the destination and the URL still works
- `grep UserCanAccessCookbookAsync src/` returns 0 hits (PATTERNS.md #5 enforced project-wide)
</verification>

<success_criteria>
- UpdateAsync signature extended with optional `int? newCookbookId`
- Inline destination-ownership check matches the CreateAsync pattern at line 38-39
- RecipeEditor lists the current user's cookbooks in a CbSelect bound to `_selectedCookbookId`
- Save handler routes the selection through UpdateAsync
- All existing UpdateAsync call sites compile unchanged
</success_criteria>

<output>
Create `.planning/phases/10-qol-polish-consumer-surfaces/10-10-SUMMARY.md` when done.
</output>
