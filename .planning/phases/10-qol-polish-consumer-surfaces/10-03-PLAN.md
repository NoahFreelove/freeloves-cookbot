---
phase: 10-qol-polish-consumer-surfaces
plan: 03
type: execute
wave: 3
depends_on: ["10-01", "10-02"]
files_modified:
  - src/CookBot.Application/Services/PantryMatchService.cs
  - src/CookBot.Application/DependencyInjection.cs
  - tests/CookBot.Tests/Services/PantryMatchServiceTests.cs
autonomous: true
requirements: [QOL-01, QOL-02, QOL-03]
tags: [pantry-match, application-layer, scoring, dietary-filter, tests]

must_haves:
  truths:
    - "`PantryMatchService.GetMatchesAsync(userId, ct)` returns recipes ranked by `(matched / total) - RecencyPenaltyWeight * exp(-daysSinceCooked / RecencyHalfLifeDays)` per D-44"
    - "Stable sort key is (Score desc, RecipeId asc, RecipeName asc) — reload-volatility eliminated (PITFALL H8)"
    - "Dietary filter (D-45) AND-combines positive `RecipeTag` match against `UserProfile.DietaryPreferences` AND negative `IngredientCategory` exclude via the curated diet→category map (D-47); both run BEFORE scoring"
    - "Recipes the user cannot access (not owned and not shared with them) are excluded from results"
    - "Recipes whose coverage ratio falls below `MinCoverageRatio` are excluded after scoring"
  artifacts:
    - path: "src/CookBot.Application/Services/PantryMatchService.cs"
      provides: "Implementation of IPantryMatchService using IRepository<Recipe>, IRepository<PantryItem>, IRecipeMadeService, IOptions<PantryMatchOptions>"
      contains: "public class PantryMatchService : IPantryMatchService"
    - path: "tests/CookBot.Tests/Services/PantryMatchServiceTests.cs"
      provides: "Scoring matrix tests + diet-filter integration tests using in-memory SQLite"
      contains: "public class PantryMatchServiceTests"
  key_links:
    - from: "src/CookBot.Application/Services/PantryMatchService.cs"
      to: "IRecipeMadeService.GetLastCookAsync"
      via: "recency-debounce read"
      pattern: "_recipeMade\\.GetLastCookAsync"
    - from: "src/CookBot.Application/Services/PantryMatchService.cs"
      to: "PantryMatchOptions"
      via: "IOptions<T> injection"
      pattern: "IOptions<PantryMatchOptions>"
    - from: "src/CookBot.Application/DependencyInjection.cs"
      to: "PantryMatchService"
      via: "AddScoped registration"
      pattern: "AddScoped<IPantryMatchService"
---

<objective>
Implement `PantryMatchService` (the QOL-01..03 algorithm) and register it in the Application-layer DI extension. Ships with unit tests covering the recency-decay curve (days 0/1/3/7/30), the stable sort tie-breaker, and the AND-combined dietary filter behavior.

Purpose: Closes the v1.2 Home pantry-match deterministic stub (Home.razor.cs:297-339) with a real scoring algorithm per the user's chosen formula (D-44 smooth-decay variant), the user's strictest dietary filter (D-45 AND-combined), and the user's chosen configurability stance (D-46 appsettings knobs). Composite indexes from Phase 8 (`IX_RecipeIngredients_RecipeId_IngredientId` + the existing `IX_PantryItems_PantryId_IngredientId`) make this O(n log n) per PITFALL H7.

Output: One service file, one DI line, one test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-qol-polish-consumer-surfaces/10-CONTEXT.md
@.planning/phases/10-qol-polish-consumer-surfaces/10-PATTERNS.md
@CLAUDE.md
@src/CookBot.Application/Services/IPantryMatchService.cs
@src/CookBot.Application/DTOs/PantryMatchOptions.cs
@src/CookBot.Application/DTOs/PantryMatchResult.cs
@src/CookBot.Application/Services/IRecipeMadeService.cs
@src/CookBot.Application/Services/RecipeService.cs
@src/CookBot.Web/Components/Pages/Home.razor.cs
@src/CookBot.Domain/Enums/IngredientCategory.cs
@src/CookBot.Domain/Entities/Recipe.cs
@src/CookBot.Domain/Entities/PantryItem.cs
@src/CookBot.Domain/Entities/RecipeTag.cs
@src/CookBot.Domain/Interfaces/IRepository.cs

<interfaces>
<!-- Scoring formula (D-44 exact text — implement verbatim): -->
<!-- score = (matched / total) - RecencyPenaltyWeight * exp(-daysSinceCooked / RecencyHalfLifeDays) -->
<!-- where: -->
<!--   matched: count of distinct RecipeIngredient.IngredientId values in the user's PantryItem set -->
<!--   total:   recipe.RecipeIngredients.Count -->
<!--   daysSinceCooked: (DateTime.UtcNow - lastCookedUtc).TotalDays; if lastCookedUtc is null, penalty term is 0 -->
<!--   coverage and penalty use the IOptions<PantryMatchOptions>.Value -->

<!-- Diet → excluded IngredientCategory map (D-47 corrected via PATTERNS.md #4): -->
<!-- The IngredientCategory enum (src/CookBot.Domain/Enums/IngredientCategory.cs) has these values ONLY: -->
<!--   Produce, Dairy, Meat, Seafood, Bakery, Pantry, Frozen, Spices, Condiments, Beverages, Grains, Canned, Snacks, Other -->
<!-- (NO Poultry, Fish, or Eggs as categories — CONTEXT D-47 examples must use only the real enum values.) -->

<!-- Corrected diet → excluded-category map (CONTEXT correction per PATTERNS.md correction #4): -->
<!--   ["vegan"]       => [ IngredientCategory.Meat, IngredientCategory.Seafood, IngredientCategory.Dairy ] -->
<!--   ["vegetarian"]  => [ IngredientCategory.Meat, IngredientCategory.Seafood ] -->
<!--   ["dairy-free"]  => [ IngredientCategory.Dairy ] -->
<!--   ["gluten-free"] => [ IngredientCategory.Grains, IngredientCategory.Bakery ] -->
<!-- Map uses StringComparer.OrdinalIgnoreCase. Unknown diet labels skip the negative filter (positive RecipeTag match still applies). -->

<!-- Authz pattern (PATTERNS.md §"Authz pattern — owned OR shared cookbook"; canonical from RecipeMadeService.cs:75-77): -->
<!--   .Where(r => r.Cookbook.UserId == userId || r.Cookbook.Shares.Any(s => s.SharedWithUserId == userId)) -->

<!-- Cross-layer query problem (PATTERNS.md §"Application services" + "No Analog Found"): -->
<!-- Application layer cannot reference CookBotDbContext (Infrastructure). PantryMatchService MUST use IRepository<T> -->
<!-- abstractions for ALL data access. IRepository<Recipe>.FindAsync(predicate) returns Recipes; navigation properties -->
<!-- (RecipeIngredients, Cookbook, Tags) are loaded via the existing Include configuration set in CookBotDbContext / repos. -->
<!-- If predicate-based loading does not produce sufficiently populated navigation properties, fall back to -->
<!-- two repository calls (recipes by predicate, then pantry items by predicate) and join in memory; the dataset -->
<!-- size at trusted-LAN scale (<1000 recipes typical) makes this acceptable. -->

<!-- UserProfile.DietaryPreferences shape — Read once via IRepository<UserProfile> at start of GetMatchesAsync; -->
<!-- it is a `string?` JSON-serialized array or comma-delimited list (verify shape by reading UserProfile entity -->
<!-- before implementing; CookBot already deserializes it elsewhere — reuse that pattern). -->

<!-- Stable sort (D-44, PITFALL H8): -->
<!-- .OrderByDescending(t => t.Score) -->
<!-- .ThenBy(t => t.RecipeId) -->
<!-- .ThenBy(t => t.RecipeName, StringComparer.OrdinalIgnoreCase) -->

<!-- Cardinal index access: composite indexes from Phase 8 (`IX_RecipeIngredients_RecipeId_IngredientId`) — verified -->
<!-- present at src/CookBot.Infrastructure/Migrations/20260516034227_AddPantryMatchIndexes.cs:18. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write PantryMatchServiceTests (RED) — scoring matrix + diet filter</name>
  <files>tests/CookBot.Tests/Services/PantryMatchServiceTests.cs</files>
  <read_first>
    - .planning/phases/10-qol-polish-consumer-surfaces/10-PATTERNS.md (§"Tests — `PantryMatchServiceTests.cs`" — full test scaffold with InlineData scoring matrix)
    - tests/CookBot.Tests/Services/RecipePhotoUrlValidatorTests.cs (analog A — `[Theory] [InlineData]` matrix pattern)
    - tests/CookBot.Tests/Services/OwnershipTests.cs (analog B — in-memory SQLite `CookBotDbContext` + `EnsureCreated()` + `IDisposable`)
    - tests/CookBot.Tests/TestHost.cs (existing test helpers — Profile factory, parser bootstrap)
    - src/CookBot.Domain/Entities/RecipeTag.cs (RecipeTag.Name shape for dietary positive-match seeding)
    - src/CookBot.Domain/Entities/UserProfile.cs (DietaryPreferences serialization shape)
  </read_first>
  <behavior>
    Test class `PantryMatchServiceTests : IDisposable` with an in-memory SQLite DbContext and a real `PantryMatchService` instance (constructed against `EfRepository<T>` analogs or a hand-rolled IRepository test double — match the OwnershipTests bootstrap pattern exactly).

    Required test cases:

    1. **`[Theory] RecencyPenalty_ExponentialDecay(daysSinceCooked, expectedPenalty)`** with `[InlineData(0, 0.30)]`, `[InlineData(1, 0.260)]`, `[InlineData(3, 0.197)]`, `[InlineData(7, 0.110)]`, `[InlineData(30, 0.0040)]` — Asserts that `0.3 * Math.Exp(-daysSinceCooked / 7.0)` matches the expected value within tolerance `0.01`. This is a pure-math sanity test (no DbContext) that locks the formula behavior.

    2. **`[Fact] GetMatchesAsync_StableSort_TieBreaksByRecipeIdAscending`** — Seed two recipes that produce identical scores (same matched/total, both never cooked). Assert returned order has the lower `RecipeId` first.

    3. **`[Fact] GetMatchesAsync_AppliesMinCoverageRatio_ExcludesLowCoverage`** — Seed a recipe with 1/10 ingredient match (coverage 0.1, below default MinCoverageRatio=0.6). Assert it is NOT in the result list.

    4. **`[Fact] GetMatchesAsync_NeverCooked_NoPenalty`** — Seed a recipe with 5/5 match and `IRecipeMadeService.GetLastCookAsync` returning null. Assert returned Score equals 1.0 (no penalty term).

    5. **`[Fact] GetMatchesAsync_RecentlyCooked_AppliesPenalty`** — Seed a recipe with 5/5 match cooked 1 day ago. Assert Score is approximately `1.0 - 0.3 * exp(-1/7)` ≈ `0.7404`, tolerance 0.01.

    6. **`[Fact] GetMatchesAsync_DietFilter_VeganExcludesMeatCategory`** — Seed user with `DietaryPreferences` containing "vegan"; seed two recipes — one with ingredients all in `IngredientCategory.Produce`, one with at least one `IngredientCategory.Meat` ingredient. Assert the meat-containing recipe is NOT in the result.

    7. **`[Fact] GetMatchesAsync_DietFilter_VegetarianRequiresMatchingRecipeTag`** — Seed user with `DietaryPreferences` containing "vegetarian"; seed two recipes — one with `RecipeTag(Name="vegetarian")`, one without. Assert only the tagged recipe survives (positive RecipeTag match required by D-45 AND-combined).

    8. **`[Fact] GetMatchesAsync_UnknownDietLabel_SkipsNegativeFilter_KeepsPositiveTagMatch`** — Seed user with `DietaryPreferences = "keto"` (not in DietExcludeMap); seed two recipes — one with `RecipeTag(Name="keto")`, one without. Assert only the keto-tagged recipe survives.

    9. **`[Fact] GetMatchesAsync_OnlyReturnsAccessibleRecipes`** — Seed three recipes: one in a cookbook owned by user, one in a cookbook shared to user via CookbookShare, one in an unrelated user's cookbook. Assert only the first two appear in the result set.

    10. **`[Fact] GetMatchesAsync_RespectsResultCount`** — Seed 10 high-scoring recipes, leave ResultCount at default 3. Assert exactly 3 results.

    All tests use `xUnit` (`[Fact]`, `[Theory]`, `[InlineData]`, `Assert.Equal`, `Assert.InRange`, `Assert.DoesNotContain`, `Assert.Equal(expectedCount, results.Count)`). No Verify snapshots.
  </behavior>
  <action>
    Create `tests/CookBot.Tests/Services/PantryMatchServiceTests.cs`. Use the in-memory SQLite bootstrap pattern from `OwnershipTests.cs:11-23`:
    ```
    var options = new DbContextOptionsBuilder<CookBotDbContext>().UseSqlite("DataSource=:memory:").Options;
    _db = new CookBotDbContext(options);
    _db.Database.OpenConnection();
    _db.Database.EnsureCreated();
    ```
    Implement all 10 tests listed in the `<behavior>` block above with the named test methods.

    `IRecipeMadeService` can be a hand-rolled test double (small inline class implementing the three methods) OR seed `RecipesMade` table directly and instantiate the real `RecipeMadeService` against the test DbContext (real-implementation approach matches OwnershipTests style — preferred).

    Service construction: `new PantryMatchService(recipeRepo, pantryRepo, recipeMadeService, Microsoft.Extensions.Options.Options.Create(new PantryMatchOptions()))` — uses real `EfRepository<T>` instances if such a concrete exists, otherwise hand-rolled `TestRepository<T>` implementing `IRepository<T>` (check what `OwnershipTests.cs` uses; mirror).

    Tests MUST fail to compile until Task 2 lands (PantryMatchService class does not yet exist) — that is the RED gate. Implementation in Task 2 turns them GREEN.

    Use `[Theory]` with `[InlineData]` for the scoring matrix (test 1). All other tests are `[Fact]`. Tolerance for floating-point compares uses `Assert.InRange(Math.Abs(actual - expected), 0, 0.01)`.
  </action>
  <verify>
    <automated>test -f tests/CookBot.Tests/Services/PantryMatchServiceTests.cs &amp;&amp; grep -c "\[Fact\]" tests/CookBot.Tests/Services/PantryMatchServiceTests.cs | awk '$1 &gt;= 9 {exit 0} {exit 1}' &amp;&amp; grep -q "RecencyPenalty_ExponentialDecay" tests/CookBot.Tests/Services/PantryMatchServiceTests.cs &amp;&amp; grep -q "DietFilter_VeganExcludesMeatCategory" tests/CookBot.Tests/Services/PantryMatchServiceTests.cs &amp;&amp; grep -q "StableSort_TieBreaksByRecipeIdAscending" tests/CookBot.Tests/Services/PantryMatchServiceTests.cs &amp;&amp; ! dotnet build tests/CookBot.Tests/CookBot.Tests.csproj --nologo --verbosity quiet 2&gt;/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/CookBot.Tests/Services/PantryMatchServiceTests.cs` exists
    - Source assertion: at least 9 `[Fact]` annotations + at least 1 `[Theory]` annotation
    - Source assertion: test method names match the 10 listed in `<behavior>` block (exact identifiers)
    - Source assertion: file imports `using CookBot.Application.Services;` and `using CookBot.Application.DTOs;`
    - CLI output: `dotnet build tests/CookBot.Tests/CookBot.Tests.csproj` FAILS at this point (RED gate — `PantryMatchService` type does not exist yet)
  </acceptance_criteria>
  <done>Test file exists with the full matrix; solution intentionally fails to build until Task 2 supplies the implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement PantryMatchService (GREEN) + register in AddApplication</name>
  <files>
    src/CookBot.Application/Services/PantryMatchService.cs,
    src/CookBot.Application/DependencyInjection.cs
  </files>
  <read_first>
    - .planning/phases/10-qol-polish-consumer-surfaces/10-PATTERNS.md (§"Application services" — full constructor-injection + EF query + scoring formula + stable sort excerpts; §"Layering Notes" #1)
    - .planning/phases/10-qol-polish-consumer-surfaces/10-CONTEXT.md (D-44 formula text; D-45 dietary filter rules; D-47 diet→category map)
    - src/CookBot.Web/Components/Pages/Home.razor.cs (lines 297-339 — the deterministic stub being replaced; line 303-308 EF read pattern to mirror; line 470 HomePantryMatch projection shape that informs PantryMatchResult)
    - src/CookBot.Application/Services/RecipeService.cs (lines 7-31 — constructor injection of multiple IRepository<T>; line 38-39 inline cookbook ownership pattern)
    - src/CookBot.Application/Services/IPantryMatchService.cs (the interface created in Plan 10-02)
    - src/CookBot.Application/Services/IRecipeMadeService.cs (created in Plan 10-01)
    - src/CookBot.Application/DTOs/PantryMatchOptions.cs (created in Plan 10-02)
    - src/CookBot.Application/DTOs/PantryMatchResult.cs (created in Plan 10-02)
    - src/CookBot.Application/DependencyInjection.cs (line 15-30 — AddScoped pattern to follow for IPantryMatchService registration)
    - src/CookBot.Domain/Entities/UserProfile.cs (DietaryPreferences serialization — copy whatever helper Home.razor.cs:303-308 uses)
    - src/CookBot.Domain/Enums/IngredientCategory.cs (the real 14-value enum — confirms map uses only Meat/Seafood/Dairy/Grains/Bakery)
  </read_first>
  <behavior>
    Implementation must satisfy ALL 10 tests from Task 1 by passing the scoring matrix, applying the dietary filter AND-combined, returning empty list when nothing survives, and stable-sorting per D-44.
  </behavior>
  <action>
    Create `src/CookBot.Application/Services/PantryMatchService.cs`:
    - File-scoped namespace `namespace CookBot.Application.Services;`
    - Usings: `CookBot.Application.DTOs`, `CookBot.Domain.Entities`, `CookBot.Domain.Enums`, `CookBot.Domain.Interfaces`, `Microsoft.Extensions.Options`
    - `public class PantryMatchService : IPantryMatchService`
    - Constructor injects: `IRepository<Recipe> recipeRepo`, `IRepository<PantryItem> pantryRepo`, `IRepository<UserProfile> userProfileRepo`, `IRecipeMadeService recipeMade`, `IOptions<PantryMatchOptions> opts`
    - Private readonly fields for all four dependencies and `_opts = opts.Value;`
    - Private static readonly `DietExcludeMap` of type `Dictionary<string, IngredientCategory[]>` with `StringComparer.OrdinalIgnoreCase`. **CONTEXT correction** (PATTERNS.md #4): map MUST contain ONLY the four keys with the corrected enum values per the `<interfaces>` block above: `["vegan"] = [Meat, Seafood, Dairy]`, `["vegetarian"] = [Meat, Seafood]`, `["dairy-free"] = [Dairy]`, `["gluten-free"] = [Grains, Bakery]`. **DO NOT** use `Poultry`, `Fish`, or `Eggs` — these are not in the `IngredientCategory` enum.

    Public method `GetMatchesAsync(int userId, CancellationToken ct = default)`:
    1. Load the user's `PantryItem` set: `var pantryItems = await _pantryRepo.FindAsync(p => p.Pantry.UserId == userId);` (use whatever ownership predicate the project uses for Pantry — verify via `PantryService` analog).
    2. Build `var pantryIngredientIds = pantryItems.Select(p => p.IngredientId).ToHashSet();`.
    3. Load the user's profile for `DietaryPreferences`: `var profile = (await _userProfileRepo.FindAsync(p => p.UserId == userId)).FirstOrDefault();` — parse `DietaryPreferences` using the same approach Home.razor.cs uses today (read Home.razor.cs lines 280-296 to copy the exact pattern; if it's a JSON string, deserialize via `JsonSerializer.Deserialize<List<string>>`; if it's CSV, split on commas).
    4. Load candidate recipes for this user (owned + shared via cookbook) using `IRepository<Recipe>.FindAsync` with the predicate from PATTERNS.md §"Authz pattern" — `r => r.Cookbook.UserId == userId || r.Cookbook.Shares.Any(s => s.SharedWithUserId == userId)`. Navigation includes must cover `RecipeIngredients.Ingredient` and `Tags`.
    5. **Dietary filter (D-45 AND-combined)** — for each preference string in the user's diet set:
       - Positive RecipeTag match: keep only recipes where `r.Tags.Any(t => string.Equals(t.Name, pref, StringComparison.OrdinalIgnoreCase))`
       - Negative category exclude: look up `DietExcludeMap[pref]`; if present, drop recipes where ANY `RecipeIngredient.Ingredient.Category` is in the excluded set; if not present, skip negative filter (positive still applied)
    6. For each surviving recipe, compute matched count: `var matched = r.RecipeIngredients.Count(ri => pantryIngredientIds.Contains(ri.IngredientId));`.
    7. Compute coverage: `var coverage = (double)matched / r.RecipeIngredients.Count;`. Drop recipes with `coverage < _opts.MinCoverageRatio`.
    8. Compute recency penalty: `var lastCook = await _recipeMade.GetLastCookAsync(r.Id, userId, ct);` — null means no penalty, score = coverage; non-null means `score = coverage - _opts.RecencyPenaltyWeight * Math.Exp(-(DateTime.UtcNow - lastCook.MadeAtUtc).TotalDays / _opts.RecencyHalfLifeDays);` (the property name from `RecipeMade` may be `MadeAt` or `MadeAtUtc` — read RecipeMade.cs to confirm; use the actual property name).
    9. Sort stable per D-44: `OrderByDescending(t => t.Score).ThenBy(t => t.RecipeId).ThenBy(t => t.RecipeName, StringComparer.OrdinalIgnoreCase)`.
    10. Take `_opts.ResultCount`. Project to `PantryMatchResult(r.Id, r.Name, matched, total, score, r.PhotoUrl, firstMissingIngredient?.Name)` where `firstMissingIngredient` is the first `RecipeIngredient.Ingredient` whose `IngredientId` is not in `pantryIngredientIds` (ordered by RecipeIngredient natural order, may be `Id` ascending).
    11. Return as `IReadOnlyList<PantryMatchResult>` via `.ToList()`.

    Empty handling: if `pantryIngredientIds` is empty OR the user has no recipes accessible, return `Array.Empty<PantryMatchResult>()`.

    Edit `src/CookBot.Application/DependencyInjection.cs`:
    - Add `services.AddScoped<IPantryMatchService, PantryMatchService>();` near line 19 (alongside the other AddScoped lines for application services)
    - Do NOT modify the `AddApplication` signature (PATTERNS.md correction #6)

    The implementation MUST turn all 10 tests from Task 1 green.
  </action>
  <verify>
    <automated>test -f src/CookBot.Application/Services/PantryMatchService.cs &amp;&amp; grep -q "public class PantryMatchService : IPantryMatchService" src/CookBot.Application/Services/PantryMatchService.cs &amp;&amp; grep -q "DietExcludeMap" src/CookBot.Application/Services/PantryMatchService.cs &amp;&amp; grep -q "IngredientCategory.Meat" src/CookBot.Application/Services/PantryMatchService.cs &amp;&amp; ! grep -q "IngredientCategory.Poultry\|IngredientCategory.Fish\|IngredientCategory.Eggs" src/CookBot.Application/Services/PantryMatchService.cs &amp;&amp; grep -q "OrderByDescending" src/CookBot.Application/Services/PantryMatchService.cs &amp;&amp; grep -q "AddScoped<IPantryMatchService, PantryMatchService>" src/CookBot.Application/DependencyInjection.cs &amp;&amp; dotnet test tests/CookBot.Tests/CookBot.Tests.csproj --filter "FullyQualifiedName~PantryMatchServiceTests" --nologo --verbosity quiet 2&gt;&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File `src/CookBot.Application/Services/PantryMatchService.cs` exists
    - Source assertion: `public class PantryMatchService : IPantryMatchService` declared
    - Source assertion: `DietExcludeMap` static dictionary present
    - Source assertion: `grep "IngredientCategory.\(Poultry\|Fish\|Eggs\)" src/CookBot.Application/Services/PantryMatchService.cs` returns ZERO matches (PATTERNS.md correction #4 honored)
    - Source assertion: `grep "IngredientCategory.\(Meat\|Seafood\|Dairy\|Grains\|Bakery\)" src/CookBot.Application/Services/PantryMatchService.cs` returns at least 5 matches
    - Source assertion: `OrderByDescending` + `ThenBy(t => t.RecipeId)` + `ThenBy(t => ..., StringComparer.OrdinalIgnoreCase)` chain present
    - Source assertion: `Math.Exp(` present (formula uses exponential decay)
    - Source assertion: `DependencyInjection.cs` contains `services.AddScoped<IPantryMatchService, PantryMatchService>();`
    - Source assertion: `DependencyInjection.cs` `AddApplication` signature unchanged: `public static IServiceCollection AddApplication(this IServiceCollection services)` (no `IConfiguration` parameter)
    - Test command: `dotnet test --filter FullyQualifiedName~PantryMatchServiceTests` exits 0 with all 10+ tests passing
  </acceptance_criteria>
  <done>PantryMatchService is implemented and registered; all 10 tests from Task 1 turn GREEN; AddApplication signature is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → service (Home page) | userId arrives from CurrentUserService (Web layer) — already authenticated trust context |
| service → DB | EF Core query layer; PantryMatchService filters by ownership BEFORE returning candidates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03-01 | Information Disclosure | Recipe lookup | mitigate | Authz predicate `r.Cookbook.UserId == userId || r.Cookbook.Shares.Any(s => s.SharedWithUserId == userId)` runs server-side BEFORE scoring (PATTERNS.md analog from RecipeMadeService.cs:75-77). Test 9 (`GetMatchesAsync_OnlyReturnsAccessibleRecipes`) gates this. |
| T-10-03-02 | Tampering | PantryMatchOptions values | accept | Trusted-LAN operator owns appsettings.json; misconfigured values affect scoring quality, not security. Negative values (e.g. RecencyPenaltyWeight=-1) cannot leak data — they just produce poor rankings. |
| T-10-03-03 | Denial of Service | Large recipe sets | accept | Composite index `IX_RecipeIngredients_RecipeId_IngredientId` from Phase 8 keeps the join O(n log n) per PITFALL H7. In-memory diet filtering caps at user's cookbook set (typical <1000 recipes at trusted-LAN scale). |
</threat_model>

<verification>
- `dotnet test --filter PantryMatchServiceTests` exits 0
- `dotnet build` exits 0 with no warnings
- `grep -c "IngredientCategory" src/CookBot.Application/Services/PantryMatchService.cs` is ≥ 5 (Meat, Seafood, Dairy, Grains, Bakery)
- `grep "IngredientCategory.\(Poultry\|Fish\|Eggs\)"` returns 0 hits
- `grep "AddApplication(this IServiceCollection services," src/CookBot.Application/DependencyInjection.cs` shows the original signature (no IConfiguration overload)
</verification>

<success_criteria>
- All 10 PantryMatchService tests pass
- D-44 scoring formula is implemented verbatim (linear-decay penalty, NOT cliff cutoff)
- D-45 AND-combined dietary filter applies positive RecipeTag match AND negative IngredientCategory exclude
- D-47 diet→category map uses only real enum values (Meat/Seafood/Dairy/Grains/Bakery)
- PATTERNS.md correction #6 honored: AddApplication signature unchanged
- Authz predicate matches RecipeMadeService analog (owned OR shared)
</success_criteria>

<output>
Create `.planning/phases/10-qol-polish-consumer-surfaces/10-03-SUMMARY.md` when done.
</output>
