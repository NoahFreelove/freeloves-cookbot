---
phase: 10-qol-polish-consumer-surfaces
plan: 06
type: execute
wave: 2
depends_on: []
files_modified:
  - src/CookBot.Application/Services/PromptBuilderService.cs
  - tests/CookBot.Tests/Services/PromptBuilderServiceNullFallbackTests.cs
  - tests/CookBot.Tests/Prompts/PromptSnapshotTests.cs
autonomous: true
requirements: [QOL-06]
tags: [prompt-builder, ai, null-fallback, snapshot]

must_haves:
  truths:
    - "`PromptBuilderService.BuildSystemPrompt(profile, pantry)` calls `ResolveTemplate(DefaultTemplate, …)` when `profile.AiSystemPromptTemplate` is null OR whitespace-only"
    - "`PromptBuilderService.BuildSystemPrompt(profile, pantry)` calls `ResolveTemplate(profile.AiSystemPromptTemplate, …)` when the template is non-null and non-whitespace"
    - "Phase 8's existing `PromptSnapshotTests` continues to pass against the unchanged DefaultTemplate (the test calls `ResolveTemplate` directly, not `BuildSystemPrompt`, so the wiring change does not invalidate the snapshot)"
    - "A new `PromptBuilderServiceNullFallbackTests` class asserts the three branches: null → default, whitespace → default, non-empty → custom"
  artifacts:
    - path: "src/CookBot.Application/Services/PromptBuilderService.cs"
      provides: "Line ~39 BuildSystemPrompt rewired with null-fallback ternary"
    - path: "tests/CookBot.Tests/Services/PromptBuilderServiceNullFallbackTests.cs"
      provides: "Three Fact tests covering the null/whitespace/custom branches"
  key_links:
    - from: "src/CookBot.Application/Services/PromptBuilderService.cs"
      to: "UserProfile.AiSystemPromptTemplate"
      via: "null-fallback ternary"
      pattern: "IsNullOrWhiteSpace\\(profile\\.AiSystemPromptTemplate\\)"
---

<objective>
Rewire `PromptBuilderService.BuildSystemPrompt` to actually consume `UserProfile.AiSystemPromptTemplate` per D-52. The field has existed on UserProfile since v1.0 but was dead code — REQUIREMENTS QOL-06's "already loaded by PromptBuilderService.BuildSystemPrompt" claim is incorrect per CONTEXT D-52. This plan corrects it.

Purpose: Plan 10-07 builds the EditProfile UI that lets users edit the template. This plan ships the backend wiring that makes the user's saved template actually influence AI requests. Done as its own plan (separate from the UI) so the change can be regression-tested against the existing PromptSnapshotTests in isolation before the EditProfile card lands.

Output: One-line change in PromptBuilderService + new three-Fact test file. PromptSnapshotTests stays untouched (it does not call BuildSystemPrompt).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-qol-polish-consumer-surfaces/10-CONTEXT.md
@.planning/phases/10-qol-polish-consumer-surfaces/10-PATTERNS.md
@CLAUDE.md
@src/CookBot.Application/Services/PromptBuilderService.cs
@tests/CookBot.Tests/Prompts/PromptSnapshotTests.cs
@tests/CookBot.Tests/TestHost.cs

<interfaces>
<!-- Current code at PromptBuilderService.cs:37-40 (BEFORE): -->
<!--   public string BuildSystemPrompt(UserProfile profile, IEnumerable<PantryItem>? pantryItems = null) -->
<!--   { -->
<!--       return ResolveTemplate(DefaultTemplate, profile, pantryItems); -->
<!--   } -->

<!-- After (D-52 — corrects REQUIREMENTS QOL-06 "already loaded" claim): -->
<!--   public string BuildSystemPrompt(UserProfile profile, IEnumerable<PantryItem>? pantryItems = null) -->
<!--   { -->
<!--       var template = string.IsNullOrWhiteSpace(profile.AiSystemPromptTemplate) -->
<!--           ? DefaultTemplate -->
<!--           : profile.AiSystemPromptTemplate; -->
<!--       return ResolveTemplate(template, profile, pantryItems); -->
<!--   } -->

<!-- PromptSnapshotTests.cs:11-18 calls svc.ResolveTemplate(DefaultTemplate, profile, pantry) DIRECTLY, -->
<!-- NOT BuildSystemPrompt. The wiring change therefore CANNOT invalidate the existing snapshot. -->
<!-- This is documented in PATTERNS.md §"Application service modification — PromptBuilderService.BuildSystemPrompt null-fallback (D-52)". -->

<!-- TestHost.GetPromptBuilderService() and TestHost.MakeProfile() are the test helpers used. -->
<!-- TestHost.MakeProfile() sets AiSystemPromptTemplate = "aisystemprompttemplate" (per existing test fixture). -->
<!-- The new tests override that to null / whitespace / custom-string as needed. -->

<!-- DefaultTemplate marker text — confirm by reading PromptBuilderService.cs lines 5-30 area; -->
<!-- the existing PromptSnapshotTests assert specific substrings of the default. Use a stable -->
<!-- substring (e.g. "CookBot, an expert AI cooking assistant" if that exists, or a longer phrase -->
<!-- guaranteed to be present in DefaultTemplate but unlikely to appear in a custom template). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write PromptBuilderServiceNullFallbackTests (RED)</name>
  <files>tests/CookBot.Tests/Services/PromptBuilderServiceNullFallbackTests.cs</files>
  <read_first>
    - tests/CookBot.Tests/Prompts/PromptSnapshotTests.cs (lines 1-30 — pattern for using TestHost.GetPromptBuilderService + TestHost.MakeProfile)
    - tests/CookBot.Tests/TestHost.cs (lines 21-63 — confirm GetPromptBuilderService + MakeProfile signatures; identify what AiSystemPromptTemplate default MakeProfile uses)
    - src/CookBot.Application/Services/PromptBuilderService.cs (lines 1-50 — find a DEFAULTTEMPLATE marker substring that is stable across all phases; ALSO confirm the current line 37-40 BuildSystemPrompt body shape)
    - .planning/phases/10-qol-polish-consumer-surfaces/10-PATTERNS.md (§"Tests — `PromptBuilderServiceNullFallbackTests.cs`" — the three test methods to implement)
  </read_first>
  <behavior>
    Test class with three `[Fact]` methods asserting BuildSystemPrompt's three branches.
  </behavior>
  <action>
    Create `tests/CookBot.Tests/Services/PromptBuilderServiceNullFallbackTests.cs`:
    - File-scoped namespace `namespace CookBot.Tests.Services;`
    - Usings: `CookBot.Domain.Entities`, `CookBot.Tests` (or wherever TestHost lives — read TestHost.cs to confirm), `Xunit`
    - `public class PromptBuilderServiceNullFallbackTests`

    Three `[Fact]` tests:

    1. **`BuildSystemPrompt_NullTemplate_UsesDefault`** — `var profile = TestHost.MakeProfile(); profile.AiSystemPromptTemplate = null;` — call `svc.BuildSystemPrompt(profile, Array.Empty<PantryItem>())` — assert the returned string contains a stable DefaultTemplate substring (read PromptBuilderService.cs to identify a phrase like the one suggested in PATTERNS.md `"CookBot, an expert AI cooking assistant"`; if the codebase uses a different stable marker, use whatever phrase appears verbatim in `DefaultTemplate` near line 20).

    2. **`BuildSystemPrompt_WhitespaceTemplate_UsesDefault`** — `profile.AiSystemPromptTemplate = "   \n\t  ";` — same assertion as test 1 (whitespace-only must fall back to default).

    3. **`BuildSystemPrompt_CustomTemplate_RespectsOverride`** — `profile.AiSystemPromptTemplate = "You are Bob. Answer cooking questions.";` — assert returned string `Contains("Bob")` AND `DoesNotContain` the DefaultTemplate marker from tests 1+2.

    Use `TestHost.GetPromptBuilderService()` to construct the service and `TestHost.MakeProfile()` to seed the profile (then mutate `AiSystemPromptTemplate` in each test).

    Tests MUST fail at this point (the production wiring at PromptBuilderService.cs:37-40 always passes `DefaultTemplate` — so test 3 will fail because `Bob` is not in the rendered prompt). This is the RED gate.
  </action>
  <verify>
    <automated>test -f tests/CookBot.Tests/Services/PromptBuilderServiceNullFallbackTests.cs &amp;&amp; grep -q "BuildSystemPrompt_NullTemplate_UsesDefault" tests/CookBot.Tests/Services/PromptBuilderServiceNullFallbackTests.cs &amp;&amp; grep -q "BuildSystemPrompt_WhitespaceTemplate_UsesDefault" tests/CookBot.Tests/Services/PromptBuilderServiceNullFallbackTests.cs &amp;&amp; grep -q "BuildSystemPrompt_CustomTemplate_RespectsOverride" tests/CookBot.Tests/Services/PromptBuilderServiceNullFallbackTests.cs &amp;&amp; ! dotnet test tests/CookBot.Tests/CookBot.Tests.csproj --filter "FullyQualifiedName~PromptBuilderServiceNullFallbackTests" --nologo --verbosity quiet 2&gt;&amp;1 | grep -q "Passed!.*Failed: 0"</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/CookBot.Tests/Services/PromptBuilderServiceNullFallbackTests.cs` exists
    - Source assertion: contains all three test method names listed
    - Behavior assertion: at least one of the three tests FAILS at this point (RED gate — production wiring not yet updated)
    - Source assertion: tests use `TestHost.GetPromptBuilderService()` and `TestHost.MakeProfile()`
  </acceptance_criteria>
  <done>Test file exists with three Fact methods covering null/whitespace/custom branches; tests fail against the unchanged production wiring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewire BuildSystemPrompt with null-fallback ternary (GREEN)</name>
  <files>src/CookBot.Application/Services/PromptBuilderService.cs</files>
  <read_first>
    - src/CookBot.Application/Services/PromptBuilderService.cs (lines 30-50 — find the exact line numbers of the current BuildSystemPrompt body; locate `ResolveTemplate(DefaultTemplate, profile, pantryItems)`)
    - .planning/phases/10-qol-polish-consumer-surfaces/10-PATTERNS.md (§"Application service modification — `PromptBuilderService.BuildSystemPrompt` null-fallback (D-52)" — the exact one-statement change)
    - .planning/phases/10-qol-polish-consumer-surfaces/10-CONTEXT.md (D-52)
    - src/CookBot.Domain/Entities/UserProfile.cs (line 24 — confirms `public string? AiSystemPromptTemplate { get; set; }` exists)
  </read_first>
  <action>
    Edit `src/CookBot.Application/Services/PromptBuilderService.cs`:

    Replace the existing `BuildSystemPrompt` body. The new body is:
    ```
    var template = string.IsNullOrWhiteSpace(profile.AiSystemPromptTemplate)
        ? DefaultTemplate
        : profile.AiSystemPromptTemplate;
    return ResolveTemplate(template, profile, pantryItems);
    ```

    Preserve the method signature `public string BuildSystemPrompt(UserProfile profile, IEnumerable<PantryItem>? pantryItems = null)` and the surrounding XML doc comment (if any). Add or update an XML doc summary noting: `Phase 10 / Plan 10-06 / D-52 — null-fallback override on profile.AiSystemPromptTemplate. Whitespace-only treated as null. Corrects REQUIREMENTS QOL-06 "already loaded" misclaim.`

    Do NOT modify any other method (`ResolveTemplate`, the `DefaultTemplate` constant, the constructor, any private helper). The change is exactly two extra statements (the local `template` var + the modified `ResolveTemplate` call).
  </action>
  <verify>
    <automated>grep -q "string.IsNullOrWhiteSpace(profile.AiSystemPromptTemplate)" src/CookBot.Application/Services/PromptBuilderService.cs &amp;&amp; grep -q "ResolveTemplate(template, profile, pantryItems)" src/CookBot.Application/Services/PromptBuilderService.cs &amp;&amp; grep -c "ResolveTemplate" src/CookBot.Application/Services/PromptBuilderService.cs | awk '$1 &gt;= 2 {exit 0} {exit 1}' &amp;&amp; dotnet test tests/CookBot.Tests/CookBot.Tests.csproj --filter "FullyQualifiedName~PromptBuilderServiceNullFallbackTests" --nologo --verbosity quiet 2&gt;&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep "string.IsNullOrWhiteSpace(profile.AiSystemPromptTemplate)" src/CookBot.Application/Services/PromptBuilderService.cs` returns at least one match
    - Source assertion: `BuildSystemPrompt` body uses the local `template` variable that resolves via the IsNullOrWhiteSpace ternary
    - Test command: `dotnet test --filter FullyQualifiedName~PromptBuilderServiceNullFallbackTests` exits 0 with all 3 tests passing
    - Test command: `dotnet test --filter FullyQualifiedName~PromptSnapshotTests` continues to exit 0 (Phase 8 snapshot still passes — confirms the change does not invalidate the byte-stable baseline because PromptSnapshotTests calls ResolveTemplate directly, not BuildSystemPrompt)
  </acceptance_criteria>
  <done>BuildSystemPrompt honors UserProfile.AiSystemPromptTemplate with null-fallback; new tests pass; Phase 8 PromptSnapshotTests still pass.</done>
</task>

<task type="auto">
  <name>Task 3: Verify Phase 8 PromptSnapshotTests still passes</name>
  <files>tests/CookBot.Tests/Prompts/PromptSnapshotTests.cs</files>
  <read_first>
    - tests/CookBot.Tests/Prompts/PromptSnapshotTests.cs (full file — verify the test calls `svc.ResolveTemplate(DefaultTemplate, profile, pantry)` and NOT `svc.BuildSystemPrompt(profile, pantry)`)
    - .planning/phases/10-qol-polish-consumer-surfaces/10-PATTERNS.md (§"Application service modification" — confirms PromptSnapshotTests is unaffected; do NOT re-verify any .verified file)
  </read_first>
  <action>
    Do NOT modify `PromptSnapshotTests.cs`. Do NOT modify any `.verified.txt` snapshot file.

    Run the existing Phase 8 PromptSnapshotTests to confirm Plan 10-06's change does not break the snapshot baseline:
    `dotnet test tests/CookBot.Tests/CookBot.Tests.csproj --filter "FullyQualifiedName~PromptSnapshotTests"`

    Expected behavior: PromptSnapshotTests is unaffected because it calls `ResolveTemplate(DefaultTemplate, profile, pantry)` directly — bypassing the new IsNullOrWhiteSpace logic in BuildSystemPrompt. The Verify `.verified` file content is identical.

    If the test fails (unexpected — would indicate that something in Plan 10-06 Task 2 went beyond the documented change), STOP and investigate: the only acceptable resolution is reverting changes to PromptBuilderService.cs that go beyond the BuildSystemPrompt body. Do not regenerate `.verified` files in this plan.
  </action>
  <verify>
    <automated>dotnet test tests/CookBot.Tests/CookBot.Tests.csproj --filter "FullyQualifiedName~PromptSnapshotTests" --nologo --verbosity quiet 2&gt;&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Test command: `dotnet test --filter FullyQualifiedName~PromptSnapshotTests` exits 0
    - Source assertion: `tests/CookBot.Tests/Prompts/PromptSnapshotTests.cs` is UNCHANGED (no `git diff` against this file)
    - Source assertion: no `.verified.txt` file under `tests/CookBot.Tests/Snapshots/` has been modified by this plan
  </acceptance_criteria>
  <done>Phase 8 snapshot baseline confirmed intact; the null-fallback rewiring is internally consistent with Phase 8's structural invariants.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → system prompt | `UserProfile.AiSystemPromptTemplate` is user-owned data injected verbatim into the system prompt sent to Anthropic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06-01 | Tampering | AiSystemPromptTemplate injection | accept (with documentation) | The custom template is owner-controlled and goes into the system prompt — NOT into the page DOM (no XSS surface). Plan 10-07 ships the QOL-07 warning UI that explicitly tells the user the template is not wrapped by PromptInjectionGuard. Trust posture: owner sets their own template; only the key-owner's spend is on the line. |
| T-10-06-02 | Information Disclosure | No new surface | accept | The template text is read from `UserProfile.AiSystemPromptTemplate` (already persisted; no new column); SecretRedactor still wraps generated output for telemetry per Phase 9 PROD-10 |
</threat_model>

<verification>
- `dotnet test --filter PromptBuilderServiceNullFallbackTests` passes (3 tests)
- `dotnet test --filter PromptSnapshotTests` still passes (Phase 8 baseline intact)
- `dotnet build` exits 0
- `git diff src/CookBot.Application/Services/PromptBuilderService.cs` shows ONLY the BuildSystemPrompt body change
- `git diff tests/CookBot.Tests/Prompts/PromptSnapshotTests.cs` is empty
- `git diff tests/CookBot.Tests/Snapshots/` shows no modified `.verified.txt` files
</verification>

<success_criteria>
- BuildSystemPrompt's three branches (null / whitespace / custom) all behave per D-52
- New test class exercises all three branches
- Phase 8 PromptSnapshotTests continues to pass without snapshot regeneration
</success_criteria>

<output>
Create `.planning/phases/10-qol-polish-consumer-surfaces/10-06-SUMMARY.md` when done.
</output>
