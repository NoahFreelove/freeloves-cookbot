---
phase: 10-qol-polish-consumer-surfaces
plan: 07
type: execute
wave: 3
depends_on: ["10-06", "10-12"]
files_modified:
  - src/CookBot.Web/Components/Pages/EditProfile.razor
  - src/CookBot.Web/Components/Dialogs/CbConfirmDialog.razor
  - src/CookBot.Web/wwwroot/js/prompt-editor-insert.js
  - src/CookBot.Web/Components/App.razor
  - src/CookBot.Application/Services/PromptBuilderService.cs
autonomous: true
requirements: [QOL-06, QOL-07]
tags: [edit-profile, prompt-editor, chip-row, injection-warning, js-interop, cb-confirm-dialog]

must_haves:
  truths:
    - "EditProfile.razor displays an 'AI assistant instructions' CbCard with a chip row + textarea + warning + Reset/Save actions"
    - "Clicking a chip inserts its token at the textarea caret position via `window.CookbotPromptEditor.insertAtCursor(elementId, token)`"
    - "Reset-to-default opens a CbDialog confirm (via the new CbConfirmDialog component invoked through CbDialogService); on confirm, the textarea swaps to `PromptBuilderService.DefaultTemplate` text (visible for editing, NOT a placeholder)"
    - "Save persists `UserProfile.AiSystemPromptTemplate` and shows a success toast"
    - "Inline injection-warning CbCard with `var(--accent-soft)` background is always visible while the editor is on screen (D-55)"
    - "The 'AI assistant instructions' card is inserted at fixed ordinal position #4 in EditProfile (after #1 Display name, #2 Account password, #3 Accent color — see W-05 card-order contract)"
    - "`PromptBuilderService.DefaultTemplate` is `public static readonly string` (compile-time-constant impossible because `string.Join(...)` initializer is a runtime expression); already publicly accessible at line 18 — only an XML doc comment is added in this plan"
  artifacts:
    - path: "src/CookBot.Web/wwwroot/js/prompt-editor-insert.js"
      provides: "window.CookbotPromptEditor.insertAtCursor(textareaId, token) using setRangeText"
    - path: "src/CookBot.Web/Components/Pages/EditProfile.razor"
      provides: "New 'AI assistant instructions' CbCard at ordinal position #4 with chip row + textarea + warning + reset/save actions"
    - path: "src/CookBot.Web/Components/App.razor"
      provides: "<script src=\"js/prompt-editor-insert.js\"></script> tag registered"
    - path: "src/CookBot.Application/Services/PromptBuilderService.cs"
      provides: "XML doc summary added to the existing `public static readonly string DefaultTemplate` (no visibility change required — it is already public)"
    - path: "src/CookBot.Web/Components/Dialogs/CbConfirmDialog.razor"
      provides: "Minimal reusable Yes/No confirm dialog invoked via CbDialogService.ShowAsync<CbConfirmDialog>(...) — closes W-01 by committing to CbDialog (not window.confirm)"
  key_links:
    - from: "src/CookBot.Web/Components/Pages/EditProfile.razor"
      to: "window.CookbotPromptEditor.insertAtCursor"
      via: "IJSRuntime.InvokeVoidAsync"
      pattern: "CookbotPromptEditor.insertAtCursor"
    - from: "src/CookBot.Web/Components/Pages/EditProfile.razor"
      to: "UserProfile.AiSystemPromptTemplate"
      via: "EF update on save"
      pattern: "AiSystemPromptTemplate ="
    - from: "src/CookBot.Web/Components/Pages/EditProfile.razor"
      to: "CbConfirmDialog"
      via: "CbDialogService.ShowAsync<CbConfirmDialog>"
      pattern: "ShowAsync<CbConfirmDialog>"
---

<objective>
Add the QOL-06 + QOL-07 surface to EditProfile: a card that lets users edit their AI system-prompt template with a clickable variable-chip row for inserting `{{tokens}}` at the cursor, a CbDialog-protected reset-to-default button (using a new minimal `CbConfirmDialog` component — committed choice per W-01, NOT browser `window.confirm`), and an always-visible inline warning explaining that custom templates are NOT wrapped by `PromptInjectionGuard`.

Purpose: Closes DEFERRED-PROF-AIPROMPT. The backend wiring landed in Plan 10-06; this plan ships the UI that lets the user actually set the value. Together they deliver D-52..D-55.

Output: One new JS helper, one App.razor script-tag registration, one EditProfile card with five sub-elements (at ordinal position #4 per the W-05 card-order contract), one XML doc note on `PromptBuilderService.DefaultTemplate` (no visibility change — it is already `public static readonly string` per source verification), one new minimal `CbConfirmDialog` component.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-qol-polish-consumer-surfaces/10-CONTEXT.md
@.planning/phases/10-qol-polish-consumer-surfaces/10-PATTERNS.md
@CLAUDE.md
@src/CookBot.Web/Components/Pages/EditProfile.razor
@src/CookBot.Web/wwwroot/js/recipe-chip-composer.js
@src/CookBot.Application/Services/PromptBuilderService.cs

<interfaces>
<!-- VERIFIED FACTS from source files (read before plan finalization): -->

<!-- PromptBuilderService.cs:18 already declares: -->
<!--     public static readonly string DefaultTemplate = string.Join("\n", new[] { ... }); -->
<!-- This is ALREADY public. It CANNOT be `const` because `string.Join(...)` is a runtime expression -->
<!-- (compile-time-constant requires literal or constant expression). -->
<!-- B-03 fix: this plan does NOT change visibility. Step 3 below is a doc-comment-only edit. -->

<!-- EditProfile.razor card ordinal positions (verified from grep -n "CbCard\|CbEyebrow" in source): -->
<!--   Existing (before this plan + Plan 10-12 + Plan 10-14): -->
<!--     line 43:  CbCard #1 — "Display name"        (Display name) -->
<!--     line 62:  CbCard #2 — "Account password"   (Account password) -->
<!--     line 94:  CbCard    — "AI features"        (existing AI toggle) -->
<!--     line 126: CbCard    — "API key & model"    (existing API-key block) -->
<!--     line 228: CbCard    — "Cooking preferences" -->
<!--     line 275: CbCard    — "Theme & density" -->
<!--     line 285: CbCard    — "Kitchen tools" -->
<!--     line 307: CbCard    — "Dietary preferences" -->
<!--   Plan 10-12 inserts Accent color AFTER "Account password" (line 91) and BEFORE "AI features" (line 94). -->
<!--   This plan (10-07) inserts "AI assistant instructions" AFTER "Accent color" and BEFORE "AI features". -->
<!--   Plan 10-14 inserts "AI usage" AFTER "AI assistant instructions" and BEFORE "AI features". -->

<!-- W-05 FIXED CARD INSERTION ORDER (binding contract for plans 10-12, 10-07, 10-14): -->
<!--    Ordinal #1: Display name           (EXISTING — top of file) -->
<!--    Ordinal #2: Account password       (EXISTING) -->
<!--    Ordinal #3: Accent color           (NEW — Plan 10-12) -->
<!--    Ordinal #4: AI assistant instructions (NEW — THIS PLAN, 10-07) -->
<!--    Ordinal #5: AI usage               (NEW — Plan 10-14) -->
<!--    Ordinal #6: AI features (toggle)   (EXISTING — line 94 in current file) -->
<!--    Ordinal #7+: API key & model, Cooking preferences, Theme & density, Kitchen tools, Dietary preferences -->
<!--                 (EXISTING — preserved order) -->

<!-- JS helper analog (recipe-chip-composer.js:1-29 — same window.<Namespace> = { ... } shape). -->

<!-- Six tokens for the chip row (D-53): -->
<!--   {{experience_level}}, {{unit_system}}, {{equipment}}, {{dietary_preferences}}, {{pantry}}, {{recipe_format}} -->

<!-- DefaultTemplate access — already `public static readonly string` at PromptBuilderService.cs:18. -->
<!-- No visibility change is needed. EditProfile reads `PromptBuilderService.DefaultTemplate` directly. -->
<!-- (B-03 corrected — original plan body incorrectly described the field as private const.) -->

<!-- EditProfile.razor structure (analog: lines 42-91 — Display name + Account password cards): -->
<!--   <CbCard> -->
<!--     <CbEyebrow>AI assistant instructions</CbEyebrow> -->
<!--     <p style="margin:6px 0 14px 0;font-size:13px;color:var(--ink-3);">caption</p> -->
<!--     <div>chip row</div> -->
<!--     <textarea id="@_promptTextareaId" ... /> -->
<!--     <CbCard>warning (inline, accent-soft background)</CbCard> -->
<!--     <div>buttons (Reset + Save)</div> -->
<!--   </CbCard> -->

<!-- App.razor script-tag insertion point: search for `<script src="js/recipe-chip-composer.js">` -->
<!-- and add the new tag immediately after. -->

<!-- W-01 RESOLUTION — Reset confirmation dialog: -->
<!-- D-54 names CbDialog as the confirm surface. Commit to CbConfirmDialog (new minimal component). -->
<!-- Pattern: Plan 10-05's cascading-dialog-service pattern (RawRecipeEditorDialog → SaveRecipeDialog hop) -->
<!-- is the canonical analog. We add a tiny reusable CbConfirmDialog.razor that takes Title + Body + -->
<!-- ConfirmLabel + CancelLabel parameters and resolves a bool via CbDialogService.ShowAsync<CbConfirmDialog, bool>. -->
<!-- DO NOT fall back to window.confirm() — that was the W-01 ambiguity the checker rejected. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create prompt-editor-insert.js + register script tag in App.razor + add XML doc to DefaultTemplate</name>
  <files>
    src/CookBot.Web/wwwroot/js/prompt-editor-insert.js,
    src/CookBot.Web/Components/App.razor,
    src/CookBot.Application/Services/PromptBuilderService.cs
  </files>
  <read_first>
    - src/CookBot.Web/wwwroot/js/recipe-chip-composer.js (analog — verify exact module-shape pattern; copy header style)
    - src/CookBot.Web/Components/App.razor (locate existing script-tag block for `js/recipe-chip-composer.js`)
    - src/CookBot.Application/Services/PromptBuilderService.cs (line 18 — confirm declaration reads `public static readonly string DefaultTemplate = string.Join("\n", new[]`. This is ALREADY public; B-03 corrected the plan body — only an XML doc comment is added in this plan)
    - .planning/phases/10-qol-polish-consumer-surfaces/10-PATTERNS.md (§"JS — `prompt-editor-insert.js`" — full file body excerpt)
  </read_first>
  <action>
    Step 1 — Create `src/CookBot.Web/wwwroot/js/prompt-editor-insert.js`:
    - Header comment: `// Phase 10 / Plan 10-07 / QOL-06 / D-53 — variable-chip insertion at the textarea caret.`
    - Second comment: `// Module shape mirrors recipe-chip-composer.js (window.<Name> = { ... }; no ES modules).`
    - Single module assignment: `window.CookbotPromptEditor = { insertAtCursor(textareaId, token) { ... } };`
    - Inside `insertAtCursor`:
      - `const el = document.getElementById(textareaId);`
      - guard: `if (!el || typeof el.selectionStart !== 'number') return false;`
      - `const start = el.selectionStart; const end = el.selectionEnd;`
      - `el.setRangeText(token, start, end, 'end');` (preserves undo stack)
      - `el.dispatchEvent(new Event('input', { bubbles: true }));` (so Blazor `@oninput` fires)
      - `el.focus();`
      - `return true;`
    - No `'use strict';`, no ES module exports.

    Step 2 — Edit `src/CookBot.Web/Components/App.razor`:
    - Locate `<script src="js/recipe-chip-composer.js"></script>` (or the nearest existing wwwroot `<script src="js/...">` tag if the chip-composer one is not present)
    - Insert immediately after: `<script src="js/prompt-editor-insert.js"></script>`
    - Do NOT modify any other script tag, JS reference, or import.

    Step 3 — Edit `src/CookBot.Application/Services/PromptBuilderService.cs` (XML doc only — B-03 correction):
    - **CRITICAL — DO NOT change the declaration.** The field at line 18 already reads `public static readonly string DefaultTemplate = string.Join("\n", new[]` and is ALREADY publicly accessible. Visibility cannot be raised further. The field cannot be `const` because the `string.Join(...)` initializer is not a compile-time-constant expression.
    - Add an XML doc summary comment immediately ABOVE the existing line 18 declaration noting: `Phase 10 / Plan 10-07: doc-comment added so EditProfile's reset-to-default affordance (D-54) references this template through a clearly-typed public surface. Visibility is unchanged (already public static readonly).`
    - Do NOT modify any other field, method, or constant. Do NOT add or remove modifiers on the declaration itself.

    None of these three steps changes runtime behavior or any public API shape; step 3 is doc-only.
  </action>
  <verify>
    <automated>test -f src/CookBot.Web/wwwroot/js/prompt-editor-insert.js &amp;&amp; grep -q "window.CookbotPromptEditor" src/CookBot.Web/wwwroot/js/prompt-editor-insert.js &amp;&amp; grep -q "insertAtCursor" src/CookBot.Web/wwwroot/js/prompt-editor-insert.js &amp;&amp; grep -q "setRangeText" src/CookBot.Web/wwwroot/js/prompt-editor-insert.js &amp;&amp; grep -q "prompt-editor-insert.js" src/CookBot.Web/Components/App.razor &amp;&amp; grep -q "public static readonly string DefaultTemplate" src/CookBot.Application/Services/PromptBuilderService.cs &amp;&amp; ! grep -q "public const string DefaultTemplate" src/CookBot.Application/Services/PromptBuilderService.cs &amp;&amp; dotnet build /home/noah/Desktop/projects/freeloves-cookbot/CookBot.sln --nologo --verbosity quiet 2&gt;&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `src/CookBot.Web/wwwroot/js/prompt-editor-insert.js` exists
    - Source assertion: contains `window.CookbotPromptEditor`, `insertAtCursor`, `setRangeText`
    - Source assertion: `src/CookBot.Web/Components/App.razor` references `js/prompt-editor-insert.js` in a `<script src=` tag
    - Source assertion: `src/CookBot.Application/Services/PromptBuilderService.cs` declares `public static readonly string DefaultTemplate` (UNCHANGED from baseline — verified at line 18; the plan only adds an XML doc comment)
    - Source assertion: file does NOT declare `public const string DefaultTemplate` (B-03 — `const` is impossible because the initializer is a runtime expression)
    - CLI output: `dotnet build` exits 0
  </acceptance_criteria>
  <done>JS helper exists, script tag registered, DefaultTemplate has a Phase-10 XML doc note (visibility unchanged — already public).</done>
</task>

<task type="auto">
  <name>Task 2: Add minimal CbConfirmDialog component (closes W-01 — commits to CbDialog over window.confirm)</name>
  <files>src/CookBot.Web/Components/Dialogs/CbConfirmDialog.razor</files>
  <read_first>
    - src/CookBot.Web/Components/Dialogs/SaveRecipeDialog.razor (canonical CbDialog cascading-service pattern — copy header style + `[CascadingParameter] CbDialogReference?` + `Close` invocation pattern)
    - src/CookBot.Web/Components/Dialogs/RawRecipeEditorDialog.razor (Plan 10-05 analog showing dialog-to-dialog hop — verify the `CbDialogService.ShowAsync<X, T>` resolve-value pattern)
    - src/CookBot.Web/Services/CbDialogService.cs (the `ShowAsync<TDialog, TResult>` generic signature)
    - src/CookBot.Web/Components/Atoms/CbButton.razor (Variant enum values for the Yes/No buttons)
  </read_first>
  <action>
    Create `src/CookBot.Web/Components/Dialogs/CbConfirmDialog.razor`. This is the reusable "Yes/No" confirm surface needed by EditProfile's reset action and any future destructive confirm flows (replaces every potential `JSRuntime.InvokeAsync<bool>("confirm", ...)` fallback).

    Component contract:
    - `@inherits CbDialogBase<bool>` (or whatever the project's dialog base class is — verify in CbDialogService.cs / SaveRecipeDialog.razor; mirror exactly)
    - `[Parameter] public string Title { get; set; } = "Confirm";`
    - `[Parameter] public string Body { get; set; } = "Are you sure?";`
    - `[Parameter] public string ConfirmLabel { get; set; } = "Confirm";`
    - `[Parameter] public string CancelLabel { get; set; } = "Cancel";`
    - `[Parameter] public CbButton.CbButtonVariant ConfirmVariant { get; set; } = CbButton.CbButtonVariant.Primary;` (callers can pass `Danger` for destructive actions)

    Markup (mirror SaveRecipeDialog.razor's outer dialog scaffold — CbCard or whatever the canonical wrapper is):
    ```
    <h2 style="margin:0;font-size:16px;font-weight:600;color:var(--ink);">@Title</h2>
    <p style="margin:8px 0 16px 0;font-size:13.5px;color:var(--ink-2);line-height:1.5;">@Body</p>
    <div style="display:flex;gap:8px;justify-content:flex-end;">
        <CbButton Variant="CbButton.CbButtonVariant.Ghost" OnClick="@(() => Close(false))">@CancelLabel</CbButton>
        <CbButton Variant="@ConfirmVariant" OnClick="@(() => Close(true))">@ConfirmLabel</CbButton>
    </div>
    ```

    `@code` is empty beyond the four `[Parameter]` properties — the `Close(bool)` call comes from the base class established by SaveRecipeDialog. If the project does not use a generic `CbDialogBase<TResult>` (verify), then use the cascading-parameter pattern from SaveRecipeDialog.razor directly — match its signature 1:1.

    No other files are modified by this task.
  </action>
  <verify>
    <automated>test -f src/CookBot.Web/Components/Dialogs/CbConfirmDialog.razor &amp;&amp; grep -q "Title" src/CookBot.Web/Components/Dialogs/CbConfirmDialog.razor &amp;&amp; grep -q "Body" src/CookBot.Web/Components/Dialogs/CbConfirmDialog.razor &amp;&amp; grep -q "ConfirmLabel" src/CookBot.Web/Components/Dialogs/CbConfirmDialog.razor &amp;&amp; grep -q "CancelLabel" src/CookBot.Web/Components/Dialogs/CbConfirmDialog.razor &amp;&amp; grep -q "Close(true)" src/CookBot.Web/Components/Dialogs/CbConfirmDialog.razor &amp;&amp; grep -q "Close(false)" src/CookBot.Web/Components/Dialogs/CbConfirmDialog.razor &amp;&amp; dotnet build /home/noah/Desktop/projects/freeloves-cookbot/CookBot.sln --nologo --verbosity quiet 2&gt;&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `src/CookBot.Web/Components/Dialogs/CbConfirmDialog.razor` exists
    - Source assertion: declares the four `[Parameter]` properties (Title, Body, ConfirmLabel, CancelLabel)
    - Source assertion: calls `Close(true)` and `Close(false)` for the Confirm/Cancel buttons (mirrors SaveRecipeDialog cascading-dialog-service pattern)
    - CLI output: `dotnet build` exits 0
  </acceptance_criteria>
  <done>Reusable CbConfirmDialog exists; EditProfile (Task 3) will invoke it via CbDialogService.ShowAsync<CbConfirmDialog, bool> — no window.confirm fallback path remains.</done>
</task>

<task type="auto">
  <name>Task 3: Add 'AI assistant instructions' card to EditProfile.razor at ordinal position #4</name>
  <files>src/CookBot.Web/Components/Pages/EditProfile.razor</files>
  <read_first>
    - src/CookBot.Web/Components/Pages/EditProfile.razor (full file — load-bearing reads at lines 42-91 for the existing CbCard structure and the existing save handler pattern; identify the `@code` block, the existing UserProfile load path, and the toast service injection. **Confirm the W-05 card-order contract** — at execution time, depending on whether Plan 10-12 has landed, the previous card may be "Account password" (line 91) or "Accent color" (Plan 10-12). This plan inserts AFTER "Accent color" if present, otherwise AFTER "Account password". The "AI features" CbCard at line 94 of the baseline file is the next card to follow.)
    - src/CookBot.Web/Components/Atoms/CbCard.razor (Padding parameter, Style parameter — needed for the inline warning card)
    - src/CookBot.Web/Components/Atoms/CbChip.razor (chip styling and parameter shape)
    - src/CookBot.Web/Components/Atoms/CbButton.razor (Variant enum values, OnClick parameter)
    - src/CookBot.Web/Components/Atoms/Icon.razor (confirm `Icon.Names.Save` exists; if not, use a stub or the Pencil icon)
    - src/CookBot.Web/Components/Dialogs/CbConfirmDialog.razor (created in Task 2 — the confirm surface this card invokes)
    - src/CookBot.Web/Services/CbDialogService.cs (confirm `CbDialogService.ShowAsync<TDialog, TResult>` signature; this card uses the bool-resolving overload)
    - .planning/phases/10-qol-polish-consumer-surfaces/10-PATTERNS.md (§"Razor page modification — `EditProfile.razor` (three new cards)" — full excerpt for the AI assistant instructions card)
    - .planning/phases/10-qol-polish-consumer-surfaces/10-CONTEXT.md (D-52..D-55 — exact UI affordances and copy)
    - src/CookBot.Application/Services/PromptBuilderService.cs (line 18: `public static readonly string DefaultTemplate` is publicly accessible)
  </read_first>
  <action>
    Edit `src/CookBot.Web/Components/Pages/EditProfile.razor`.

    **W-05 CARD ORDER CONTRACT (binding across plans 10-12, 10-07, 10-14):**
    | Ordinal | Eyebrow text                | Source             |
    |---------|-----------------------------|--------------------|
    | #1      | Display name                | EXISTING (line 43) |
    | #2      | Account password            | EXISTING (line 62) |
    | #3      | Accent color                | NEW — Plan 10-12   |
    | #4      | AI assistant instructions   | NEW — THIS PLAN    |
    | #5      | AI usage                    | NEW — Plan 10-14   |
    | #6      | AI features (toggle)        | EXISTING (line 94) |

    Insert the new `<CbCard>` immediately BEFORE the existing "AI features" CbCard (baseline line 94) and immediately AFTER the "Accent color" CbCard (added by Plan 10-12). If Plan 10-12 has not yet executed when this plan runs, insert immediately after the "Account password" closing `</CbCard>` (baseline line 91) — the later Plan 10-12 will splice its card in BEFORE this one to satisfy the contract.

    Card content (the new CbCard at ordinal #4):

    (a) `<CbEyebrow>AI assistant instructions</CbEyebrow>`

    (b) Caption paragraph: `<p style="margin:6px 0 14px 0;font-size:13px;color:var(--ink-3);">Customize how CookBot's AI introduces itself. Variables expand at request time.</p>`

    (c) Clickable variable-chip row — a `<div>` with `style="display:flex;gap:6px;flex-wrap:wrap;margin-bottom:8px;"`. Inside, render six chip buttons via `@foreach (var token in PromptTokens) { <button type="button" class="cb-chip" style="cursor:pointer;border:0;" @onclick="@(() => InsertToken(token))">@token</button> }`. `PromptTokens` is a static readonly array in `@code`: `{ "{{experience_level}}", "{{unit_system}}", "{{equipment}}", "{{dietary_preferences}}", "{{pantry}}", "{{recipe_format}}" }`.

    (d) The textarea — use a raw `<textarea>` (NOT CbTextarea, because we need a stable element id for the JS interop helper): `<textarea id="@_promptTextareaId" value="@_promptTemplate" @oninput="@((ChangeEventArgs e) => _promptTemplate = e.Value as string ?? "")" rows="12" style="width:100%;font-family:var(--font-mono);font-size:13px;resize:vertical;min-height:240px;" aria-label="Custom AI system prompt template" />`. `_promptTextareaId` is a `Guid.NewGuid():N`-suffixed string set in OnInitialized.

    (e) Inline warning CbCard (D-55 — always visible while editing): `<CbCard Padding="14" Style="background:var(--accent-soft);margin-top:12px;">` containing:
       - `<strong style="font-size:13px;">About custom prompts</strong>`
       - `<p style="margin:6px 0 0 0;font-size:12.5px;color:var(--ink-2);line-height:1.5;">Your custom template is injected verbatim into the system prompt. CookBot's PromptInjectionGuard wraps user-supplied <em>content</em>, but not the system template itself. Avoid instructions that tell the model to disregard or override the rest of the prompt.</p>`
       - Closing `</CbCard>`

    (f) Action row aligned right (`<div style="display:flex;gap:8px;margin-top:12px;justify-content:flex-end;">`):
       - `<CbButton Variant="CbButton.CbButtonVariant.Ghost" OnClick="ConfirmResetPromptAsync">Reset to default</CbButton>`
       - `<CbButton Variant="CbButton.CbButtonVariant.Primary" StartIcon="@Icon.Names.Save" OnClick="SavePromptTemplateAsync">Save</CbButton>`

    `@code` additions:

    - `private static readonly string[] PromptTokens = { "{{experience_level}}", "{{unit_system}}", "{{equipment}}", "{{dietary_preferences}}", "{{pantry}}", "{{recipe_format}}" };`
    - `private string _promptTemplate = "";`
    - `private string _promptTextareaId = "";` (initialized in OnInitializedAsync)
    - `[Inject] private IJSRuntime JS { get; set; } = default!;` (already injected at file-top line 17 — verify; do not double-inject)

    Hook OnInitializedAsync (or OnAfterRenderAsync for the user-load if that is the existing pattern in EditProfile) to:
    - Set `_promptTextareaId = $"prompt-editor-textarea-{Guid.NewGuid():N}";`
    - Load `_promptTemplate` from the current user's profile: `_promptTemplate = profile.AiSystemPromptTemplate ?? "";` — locate the existing UserProfile load in EditProfile's @code block (it already loads `_displayName`, `_aiEnabled`, etc.) and add the AiSystemPromptTemplate load alongside

    Method `private async Task InsertToken(string token)`:
    - `try { await JS.InvokeVoidAsync("CookbotPromptEditor.insertAtCursor", _promptTextareaId, token); }`
    - `catch (JSException) { } catch (Microsoft.JSInterop.JSDisconnectedException) { }`
    - Per PATTERNS.md §"JS interop error handling"

    Method `private async Task ConfirmResetPromptAsync()` — **W-01 RESOLUTION**:
    - **Commit to CbDialog confirm** via the new `CbConfirmDialog` component from Task 2. Do NOT use `JSRuntime.InvokeAsync<bool>("confirm", ...)` — that fallback was rejected by the checker.
    - Invocation (use the exact CbDialogService API verified by reading SaveRecipeDialog.razor / RawRecipeEditorDialog.razor):
      ```
      var parameters = new Microsoft.AspNetCore.Components.DialogParameters // (or whatever the project's parameter container is — match SaveRecipeDialog usage)
      {
          { nameof(CbConfirmDialog.Title), "Reset prompt template?" },
          { nameof(CbConfirmDialog.Body), "Your custom template will be lost. The default will be loaded into the editor — you can still cancel by closing the page without saving." },
          { nameof(CbConfirmDialog.ConfirmLabel), "Reset" },
          { nameof(CbConfirmDialog.ConfirmVariant), CbButton.CbButtonVariant.Danger },
      };
      var confirmed = await CbDialogService.ShowAsync<CbConfirmDialog, bool>(parameters);
      ```
      (The exact `DialogParameters` shape may differ — read SaveRecipeDialog.razor to match the canonical CbDialogService.ShowAsync call shape used by Plan 10-05. If the service exposes a typed-builder pattern, use that; do NOT invent a new convention.)
    - On `confirmed == true`: `_promptTemplate = PromptBuilderService.DefaultTemplate;` (reads the existing public static readonly string from PromptBuilderService.cs:18)
    - `StateHasChanged();` to refresh the textarea

    Method `private async Task SavePromptTemplateAsync()`:
    - Load the current UserProfile via existing pattern (same way other EditProfile save handlers do it; check existing `SaveDisplayName` or equivalent for the canonical save shape — UserProfile loaded from `DbContext.UserProfiles.FirstOrDefaultAsync(p => p.UserId == currentUserId)`)
    - Set `profile.AiSystemPromptTemplate = string.IsNullOrWhiteSpace(_promptTemplate) ? null : _promptTemplate;` (null-empty normalization mirrors D-52's IsNullOrWhiteSpace logic at the read site)
    - `await DbContext.SaveChangesAsync();`
    - `Toast.Show("AI assistant instructions saved.", CbToastSeverity.Success);`
    - On exception: `Toast.Show($"Save failed: {ex.Message}", CbToastSeverity.Error);`

    Add `using CookBot.Application.Services;` at the top of the file IF it's not already there (needed for `PromptBuilderService.DefaultTemplate` reference).

    Do NOT touch any other existing card or @code section in EditProfile.razor.

    **W-05 ordinal-position check at end of edit:** After inserting the card, the file should contain `<CbEyebrow>AI assistant instructions</CbEyebrow>` AFTER `<CbEyebrow>Account password</CbEyebrow>` AND AFTER `<CbEyebrow>Accent color</CbEyebrow>` (if Plan 10-12 has already landed) AND BEFORE `<CbEyebrow>AI features</CbEyebrow>` AND BEFORE `<CbEyebrow>AI usage</CbEyebrow>` (if Plan 10-14 has already landed). Verify by reading the resulting file's CbEyebrow sequence top-to-bottom.
  </action>
  <verify>
    <automated>grep -q "AI assistant instructions" src/CookBot.Web/Components/Pages/EditProfile.razor &amp;&amp; grep -q "PromptTokens" src/CookBot.Web/Components/Pages/EditProfile.razor &amp;&amp; grep -q "{{experience_level}}" src/CookBot.Web/Components/Pages/EditProfile.razor &amp;&amp; grep -q "{{recipe_format}}" src/CookBot.Web/Components/Pages/EditProfile.razor &amp;&amp; grep -q "About custom prompts" src/CookBot.Web/Components/Pages/EditProfile.razor &amp;&amp; grep -q "PromptInjectionGuard wraps user-supplied" src/CookBot.Web/Components/Pages/EditProfile.razor &amp;&amp; grep -q "PromptBuilderService.DefaultTemplate" src/CookBot.Web/Components/Pages/EditProfile.razor &amp;&amp; grep -q "CookbotPromptEditor.insertAtCursor" src/CookBot.Web/Components/Pages/EditProfile.razor &amp;&amp; grep -q "AiSystemPromptTemplate = " src/CookBot.Web/Components/Pages/EditProfile.razor &amp;&amp; grep -q "ShowAsync<CbConfirmDialog" src/CookBot.Web/Components/Pages/EditProfile.razor &amp;&amp; ! grep -q "InvokeAsync<bool>(\"confirm\"" src/CookBot.Web/Components/Pages/EditProfile.razor &amp;&amp; awk '/<CbEyebrow>Account password<\/CbEyebrow>/{a=NR} /<CbEyebrow>AI assistant instructions<\/CbEyebrow>/{b=NR} /<CbEyebrow>AI features<\/CbEyebrow>/{c=NR} END{exit !(a&amp;&amp;b&amp;&amp;c&amp;&amp;a&lt;b&amp;&amp;b&lt;c)}' src/CookBot.Web/Components/Pages/EditProfile.razor &amp;&amp; dotnet build /home/noah/Desktop/projects/freeloves-cookbot/CookBot.sln --nologo --verbosity quiet 2&gt;&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: EditProfile.razor contains the eyebrow "AI assistant instructions"
    - Source assertion: contains all six prompt tokens (`{{experience_level}}` through `{{recipe_format}}`)
    - Source assertion: contains the warning copy phrase "PromptInjectionGuard wraps user-supplied"
    - Source assertion: references `PromptBuilderService.DefaultTemplate` (reset path) and `CookbotPromptEditor.insertAtCursor` (chip insertion path)
    - Source assertion: persistence path assigns to `AiSystemPromptTemplate`
    - **W-01 source assertion:** uses `ShowAsync<CbConfirmDialog` (or the project's equivalent typed dialog API) AND file contains NO occurrence of `InvokeAsync<bool>("confirm"` (window.confirm fallback eliminated)
    - **W-05 source assertion:** Awk-based ordinal check — `<CbEyebrow>Account password</CbEyebrow>` appears BEFORE `<CbEyebrow>AI assistant instructions</CbEyebrow>` which appears BEFORE `<CbEyebrow>AI features</CbEyebrow>` (line-number monotonicity verifies the insertion ordinal)
    - CLI output: `dotnet build` exits 0
  </acceptance_criteria>
  <done>EditProfile shows the new card at the contracted ordinal position #4; chip clicks insert tokens at the cursor; reset opens a CbConfirmDialog (no browser confirm) before swapping to DefaultTemplate; save persists the template and toasts success.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → UserProfile.AiSystemPromptTemplate | Owner-set template persists to DB; injected verbatim into Anthropic system prompt at AI-call time (per Plan 10-06) |
| EditProfile textarea → DOM | Raw `<textarea>` value — browser treats content as text, no innerHTML interpolation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07-01 | Tampering | Custom prompt template | accept (with disclosure) | D-55 warning copy is always visible; owner-controlled template only affects the owner's AI requests (and recipients of their shared key per Phase 9 PROD-18 disclosure) |
| T-10-07-02 | Cross-Site Scripting | Chip insertion via setRangeText | accept | `setRangeText` operates on textarea value (text), not HTML. Token strings are static client-side constants, not user-supplied. |
| T-10-07-03 | Information Disclosure | DefaultTemplate doc comment | accept | DefaultTemplate is already `public static readonly`; adding a doc comment changes no surface. The field was always accessible. |
</threat_model>

<verification>
- `dotnet build` exits 0
- Manual smoke (deferred to verify-work): visit EditProfile; click each chip and confirm token appears at cursor; click Reset and confirm CbConfirmDialog appears (NOT a browser dialog); confirm Save persists across reload
- `grep -c "CookbotPromptEditor" src/CookBot.Web/wwwroot/js/prompt-editor-insert.js` returns at least 1
- `grep "<script src=\"js/prompt-editor-insert.js\">" src/CookBot.Web/Components/App.razor` returns 1 hit
- `grep "public static readonly string DefaultTemplate" src/CookBot.Application/Services/PromptBuilderService.cs` returns 1 hit (declaration unchanged from baseline at line 18)
- `grep "ShowAsync<CbConfirmDialog" src/CookBot.Web/Components/Pages/EditProfile.razor` returns at least 1 hit (W-01 enforced)
- `grep 'InvokeAsync<bool>("confirm"' src/CookBot.Web/Components/Pages/EditProfile.razor` returns 0 hits (no window.confirm fallback)
</verification>

<success_criteria>
- All six chips render and click-insert tokens at the textarea caret
- Reset-to-default surfaces a **CbConfirmDialog** confirm step (NOT window.confirm) and on confirm swaps to `PromptBuilderService.DefaultTemplate`
- Save persists to `UserProfile.AiSystemPromptTemplate` (with whitespace-only normalized to null)
- Inline warning card with accent-soft background is always visible
- `PromptBuilderService.DefaultTemplate` declaration is unchanged from baseline (already `public static readonly`; only an XML doc comment is added)
- The "AI assistant instructions" CbCard is at ordinal position #4 (after Display name, Account password, Accent color; before AI features toggle) — W-05 contract enforced
</success_criteria>

<output>
Create `.planning/phases/10-qol-polish-consumer-surfaces/10-07-SUMMARY.md` when done.
</output>
</content>
</invoke>