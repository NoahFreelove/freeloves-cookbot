---
phase: 15-nutrition-offline-cnf-canadian-nutrient-file
plan: 04
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/CookBot.Application/Services/IngredientDensityProvider.cs
  - src/CookBot.Application/DependencyInjection.cs
  - tests/CookBot.Tests/Nutrition/IngredientDensityProviderTests.cs
autonomous: true
requirements: [NUTR-03]
must_haves:
  truths:
    - "IngredientDensityProvider returns ingredient-specific g/mL for at least 20 common cooking ingredients — never water density (1.0 g/mL) as a fallback"
    - "All-purpose flour density (~0.507 g/mL, ~120 g/US cup) is present so the density-fallback path can resolve 1 cup flour to ~435-455 kcal"
    - "Unknown ingredients return null (no density), so the caller marks the conversion unmatched/low-confidence rather than guessing water"
  artifacts:
    - path: "src/CookBot.Application/Services/IngredientDensityProvider.cs"
      provides: "Curated ≥23-ingredient g/mL density table (KA + FAO/INFOODS sourced) with null-on-unknown"
      contains: "GetDensityGPerMl"
  key_links:
    - from: "src/CookBot.Application/DependencyInjection.cs"
      to: "src/CookBot.Application/Services/IngredientDensityProvider.cs"
      via: "services.AddSingleton<IngredientDensityProvider>()"
      pattern: "IngredientDensityProvider"
---

<objective>
Build the curated per-ingredient fallback density table (NUTR-03) — the guard against the water-density bug. `IngredientDensityProvider` returns g/mL for ≥23 common cooking ingredients (King Arthur + FAO/INFOODS sourced) and `null` for unknowns, so the matcher can convert volume→mass with an ingredient-specific density only when CNF has no conversion factor, never falling back to 1.0 g/mL. Test-first: ≥20 ingredients asserted, plus the explicit "no water density" guard.

Purpose: NUTR-03 fallback half + SC3 guard. The flour anchor (1 cup ≈ 120-125 g, NOT 237 g) is impossible to get right with water density; this table makes the density path correct in isolation and unit-testable.
Output: `IngredientDensityProvider`, its DI registration, and a ≥20-ingredient test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/15-nutrition-offline-cnf-canadian-nutrient-file/15-CONTEXT.md
@.planning/phases/15-nutrition-offline-cnf-canadian-nutrient-file/15-RESEARCH.md
@.planning/phases/15-nutrition-offline-cnf-canadian-nutrient-file/15-PATTERNS.md

<interfaces>
<!-- Analog: hardcoded-dictionary + nullable-return service pattern -->
From src/CookBot.Application/Services/UnitConversionService.cs — `private static readonly Dictionary<...>` inline init; `new(StringComparer.OrdinalIgnoreCase)`; nullable `double?` returns. Density provider mirrors this shape with string keys.

From src/CookBot.Application/DependencyInjection.cs — `services.AddSingleton<IUnitConverter, UnitConversionService>();` line is the registration analog; add the density provider as a Singleton (stateless).
</interfaces>
</context>

<feature>
  <name>IngredientDensityProvider — curated g/mL fallback table</name>
  <files>src/CookBot.Application/Services/IngredientDensityProvider.cs, tests/CookBot.Tests/Nutrition/IngredientDensityProviderTests.cs</files>
  <behavior>
    From RESEARCH §"Research Target 3" table (g/mL, key = normalized ingredient name):
    - "all-purpose flour" → 0.507 (≈120 g/US cup, KA HIGH) — the SC3 anchor density
    - "bread flour" → 0.507; "whole wheat flour" → 0.478; "cake flour" → 0.507; "almond flour" → 0.406
    - "granulated white sugar"/"granulated sugar" → 0.837; "brown sugar" → 0.900; "confectioners sugar" → 0.478
    - "butter"/"unsalted butter" → 0.955
    - "vegetable oil" → 0.837; "olive oil" → 0.845
    - "whole milk" → 0.959; "heavy cream" → 0.959; "sour cream" → 0.959; "yogurt"/"plain yogurt" → 0.959
    - "honey" → 1.420; "maple syrup" → 1.319
    - "cocoa powder" → 0.355; "cornstarch" → 0.473; "rolled oats" → 0.478
    - "baking powder" → 0.900; "salt" → 1.380; "chocolate chips" → 0.719
    - plus: "cream cheese" → 0.959; "ricotta cheese" → 0.960; "peanut butter" → 1.09; "shredded coconut" → 0.360
    Guard cases:
    - GetDensityGPerMl("all-purpose flour") returns a value in [0.45, 0.55] AND is NOT 1.0 (never water density)
    - GetDensityGPerMl("unobtainium") returns null
    - lookup is case-insensitive ("All-Purpose Flour" resolves the same as "all-purpose flour")
    - the table has ≥23 entries (count assertion)
  </behavior>
  <action>
    Create `public class IngredientDensityProvider` in `CookBot.Application.Services` (mirror UnitConversionService's `private static readonly Dictionary<string,double>` with `StringComparer.OrdinalIgnoreCase`). Populate from the RESEARCH §"Research Target 3" g/mL table above, with an inline `// KA`/`// FAO` source comment per entry. Expose `public double? GetDensityGPerMl(string normalizedName)` returning the value or `null`. The keys are expected to be already-normalized names (the caller normalizes via IngredientNormalizer before lookup) — but lookup is OrdinalIgnoreCase as a belt-and-suspenders.
    Register in `src/CookBot.Application/DependencyInjection.cs` as `services.AddSingleton<IngredientDensityProvider>();` with a `// Phase 15 / NUTR-03 — curated density fallback (stateless)` comment, alongside the existing IUnitConverter registration.
    Then write the test suite covering every <behavior> case: assert each of the ≥20 named ingredient densities (within ±0.01), the no-water-density guard for flour, the null-on-unknown, the case-insensitive lookup, and the ≥23-entry count.
  </action>
  <implementation>
    RED: write IngredientDensityProviderTests with all behavior cases → run → fails (provider absent).
    GREEN: create IngredientDensityProvider with the full table + DI registration → run → passes.
    REFACTOR: group entries by source (flours/sugars/fats/dairy/syrups/other) with comments; keep the count assertion green.
  </implementation>
</feature>

<tasks>

<task type="tdd">
  <name>Task 1: TDD the IngredientDensityProvider (RED → GREEN → REFACTOR)</name>
  <files>src/CookBot.Application/Services/IngredientDensityProvider.cs, src/CookBot.Application/DependencyInjection.cs, tests/CookBot.Tests/Nutrition/IngredientDensityProviderTests.cs</files>
  <read_first>
    - src/CookBot.Application/Services/UnitConversionService.cs (the static-dictionary + nullable-return pattern)
    - src/CookBot.Application/DependencyInjection.cs (the AddSingleton registration block)
    - .planning/phases/15-nutrition-offline-cnf-canadian-nutrient-file/15-RESEARCH.md §"Research Target 3" (the full g/mL table + the "Additional densities needed for ≥20-ingredient unit test coverage" note + the "Anti-Patterns → Water density fallback" warning)
    - .planning/phases/15-nutrition-offline-cnf-canadian-nutrient-file/15-PATTERNS.md §"IngredientDensityProvider.cs"
  </read_first>
  <action>
    Implement per the <feature> block. Density values verbatim from RESEARCH §"Research Target 3". The single most important property: NO entry is 1.0 g/mL except where a real ingredient genuinely measures ~1.0 (none in this set) — and unknowns return null so the caller never substitutes water density.
  </action>
  <verify>
    <automated>dotnet test tests/CookBot.Tests/CookBot.Tests.csproj --filter "FullyQualifiedName~IngredientDensityProvider" 2>&1 | grep -E "Passed!|Failed!|Passed:|Failed:|error" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - ≥20 named-ingredient density assertions pass (each within ±0.01 of the RESEARCH value).
    - `GetDensityGPerMl("all-purpose flour")` ∈ [0.45,0.55] and is NOT 1.0 (water-density guard, SC3 / P5).
    - `GetDensityGPerMl("unobtainium")` returns null; lookup is case-insensitive; the table has ≥23 entries.
    - `IngredientDensityProvider` is registered as a Singleton in Application DI.
  </acceptance_criteria>
  <done>Density fallback is correct in isolation, water-density bug is impossible, and the SC3 flour density is present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ingredient name → density lookup | Untrusted recipe name keys a bounded dictionary; unknown → null (no guess). No execution, no injection surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-09 | Tampering | Water-density fallback producing wrong calories | mitigate | The provider returns null on unknown (caller marks unmatched/low-confidence) and never returns 1.0 as a default; the test asserts flour density ≠ 1.0. This is the SC3/P5 guard encoded as a test. |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | No packages installed; static dictionary + BCL only. |
</threat_model>

<verification>
- `dotnet test --filter IngredientDensityProvider` passes all ≥20 ingredient assertions + the no-water-density + null-on-unknown + count guards.
- `IngredientDensityProvider` registered in Application DI (singleton).
</verification>

<success_criteria>
- NUTR-03 (fallback half): an ingredient-specific density table exists, is DI-registered, returns null for unknowns, and never substitutes water density — the SC3 flour density is present and tested.
</success_criteria>

<output>
Create `.planning/phases/15-nutrition-offline-cnf-canadian-nutrient-file/15-04-SUMMARY.md` when done.
</output>
