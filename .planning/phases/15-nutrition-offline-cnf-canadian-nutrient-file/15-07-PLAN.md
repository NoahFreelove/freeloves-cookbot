---
phase: 15-nutrition-offline-cnf-canadian-nutrient-file
plan: 07
type: execute
wave: 4
depends_on: ["15-05", "15-06"]
files_modified:
  - src/CookBot.Web/Components/Pages/RecipeView.razor
autonomous: false
requirements: [NUTR-04, NUTR-05, NUTR-06]
user_setup: []
must_haves:
  truths:
    - "RecipeView shows the 5-state nutrition panel below the recipe body: not-yet-calculated (CTA), calculated (per-serving + total + coverage), stale (recalculate), calculating, error (try again)"
    - "The 'Calculate nutrition' CTA is the only trigger — clicking it computes nutrition; it never runs on page load or save"
    - "Unmatched ingredients render '--' (never '0'); low-confidence conversions render '≈' with the matched CNF description + FoodId visible; the coverage summary reads 'Matched n of total ingredients'"
    - "Every panel state carries the non-dismissable disclaimer 'Estimated nutrition — not suitable for medical dietary planning. Data: Health Canada, Canadian Nutrient File (2015).'; the heading reads 'Estimated nutrition', never 'Calories'"
    - "When a current (non-stale) cache exists, RecipeView passes a per-serving NutritionInfoDto into JsonLdRecipeProjector so nutrition.calories appears in the JSON-LD"
  artifacts:
    - path: "src/CookBot.Web/Components/Pages/RecipeView.razor"
      provides: "5-state nutrition panel + CTA + coverage list + disclaimer + JSON-LD nutrition wiring"
      contains: "Estimated nutrition"
  key_links:
    - from: "src/CookBot.Web/Components/Pages/RecipeView.razor"
      to: "INutritionService"
      via: "GetCacheAsync on load + ComputeAsync on CTA click"
      pattern: "NutritionSvc|INutritionService"
    - from: "src/CookBot.Web/Components/Pages/RecipeView.razor"
      to: "JsonLdRecipeProjector.Project"
      via: "pass NutritionInfoDto when cache is current"
      pattern: "JsonLdRecipeProjector.Project"
---

<objective>
Render the nutrition panel on RecipeView (NUTR-04/05) per the UI-SPEC 5-state machine, wire the "Calculate nutrition" CTA to `INutritionService.ComputeAsync`, and pass a per-serving `NutritionInfoDto` into `JsonLdRecipeProjector.Project` when a current cache exists (NUTR-06 consumer side). The panel shows per-serving + total macros, an honest coverage list ("--" for unmatched, "≈" + CNF description+FoodId for low-confidence), and the verbatim non-dismissable Health Canada disclaimer in every state. A human-verify checkpoint confirms the visual contract.

Purpose: The consumer surface — the only place a user sees nutrition. Encodes SC2 (coverage honesty + CNF match visibility), SC4 (disclaimer + "Estimated nutrition" heading), SC1 (CTA-only trigger), and SC5 (JSON-LD wiring from the cache).
Output: the RecipeView panel + CTA + coverage + disclaimer + JSON-LD wiring, then a checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/15-nutrition-offline-cnf-canadian-nutrient-file/15-CONTEXT.md
@.planning/phases/15-nutrition-offline-cnf-canadian-nutrient-file/15-UI-SPEC.md
@.planning/phases/15-nutrition-offline-cnf-canadian-nutrient-file/15-PATTERNS.md

<interfaces>
<!-- Service + DTO + projector this plan consumes (Plans 05/06/02) -->
From src/CookBot.Application/Services/INutritionService.cs: Task<RecipeNutritionCache?> GetCacheAsync(int recipeId, int userId); Task<RecipeNutritionCache> ComputeAsync(int recipeId, int userId).
From src/CookBot.Domain/Entities/RecipeNutritionCache.cs: IsStale, Total*/PerServing* doubles, MatchedIngredients, TotalIngredients, PerIngredientMatchJson (JSON: [{name, normalizedName, cnfFoodId, cnfFoodDescription, confidence, energyKcal, proteinG, fatG, carbG}]), Servings.
From src/CookBot.Application/DTOs/NutritionInfoDto.cs: record (CaloriesPerServing, ProteinGPerServing, FatGPerServing, CarbGPerServing).
From src/CookBot.Application/Recipes/JsonLdRecipeProjector.cs: Project(doc, absoluteImageUrl, NutritionInfoDto? nutrition = null).
From src/CookBot.Web/Components/Pages/RecipeView.razor (existing, line refs):
  - lines 12-22: @inject block (DbContext, RecipePhotoSvc, CurrentUserService UserService, etc.) — add @inject INutritionService.
  - line 48-57: the @if(_jsonLd) HeadContent + MarkupString script block (unchanged structurally).
  - line 421: `private string? _jsonLd;` field block — add nutrition state fields here.
  - line 543-569: absoluteImageUrl computation + `_jsonLd = JsonLdRecipeProjector.Project(doc, absoluteImageUrl);` — this is where the nutrition DTO is passed.
  - the current user id comes from UserService (the same source RecipePhotoSvc calls use).
UI-SPEC: src/CookBot.Web ... cookbot-design.css tokens; CbButton variants Accent/Ghost; .num tabular numerals; role="note" disclaimer; role="status" aria-live banners; .cb-chip/.cb-badge confidence badges; cb-pulse busy.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render the 5-state nutrition panel + CTA + coverage list + disclaimer, and wire nutrition into the JSON-LD</name>
  <files>src/CookBot.Web/Components/Pages/RecipeView.razor</files>
  <read_first>
    - src/CookBot.Web/Components/Pages/RecipeView.razor (full — the @inject block 12-22, the prerender load 481-575 where _jsonLd is built, the hero stat row 113-133, the recipe-body-grid + recipe-article structure, the step-number accent at ~306, the ingredient sidebar rows ~231)
    - .planning/phases/15-nutrition-offline-cnf-canadian-nutrient-file/15-UI-SPEC.md (the ENTIRE 5-state contract: copy, color tokens, spacing, typography, the macro 4-up grid, the per-serving/total segmented toggle, the coverage list + confidence badges, the disclaimer block, the panel placement at margin-top:48px below recipe-body-grid)
    - src/CookBot.Web/Components/Pages/RecipeEditorParts/RecipePhotoGalleryManager.razor (lines 173/190 role=status banners; lines 225-231 role=note disclaimer DOM shape to mirror)
    - .planning/phases/15-nutrition-offline-cnf-canadian-nutrient-file/15-PATTERNS.md §"RecipeView.razor (MODIFY — nutrition panel + CTA)" (the 7 sub-patterns: inject, state fields, JSON-LD call site, panel placement, disclaimer, stat tile, CbButton variants)
  </read_first>
  <action>
    Add `@inject INutritionService NutritionSvc` to the inject block. Add private state fields near line 421: `RecipeNutritionCache? _nutritionCache`, `bool _nutritionCalculating`, `bool _nutritionError`, `bool _showAllMatches`, `bool _nutritionPerServing = true`, and a parsed per-ingredient match list (deserialize `_nutritionCache.PerIngredientMatchJson`).
    LOAD: in the existing load path (where _recipe/_doc and _jsonLd are set, ~481-575), after ownership/recipe load, call `_nutritionCache = await NutritionSvc.GetCacheAsync(recipeId, currentUserId)` (best-effort; null on failure — never block the page). Do NOT compute on load.
    JSON-LD wiring (~569): build `NutritionInfoDto? nutritionDto = _nutritionCache is { IsStale: false } ? new NutritionInfoDto(_nutritionCache.PerServingEnergyKcal, _nutritionCache.PerServingProteinG, _nutritionCache.PerServingFatG, _nutritionCache.PerServingCarbG) : null;` then `_jsonLd = JsonLdRecipeProjector.Project(doc, absoluteImageUrl, nutritionDto);` (nutrition appears in JSON-LD only when a current cache exists — SC5).
    CTA handler: `CalculateNutrition()` sets `_nutritionCalculating=true; _nutritionError=false; StateHasChanged();`, then `try { _nutritionCache = await NutritionSvc.ComputeAsync(recipeId, currentUserId); } catch { _nutritionError=true; } finally { _nutritionCalculating=false; StateHasChanged(); }`. The CTA is the ONLY compute trigger (SC1/P7).
    PANEL markup: render a `<section aria-label="Estimated nutrition" style="margin-top:48px;">` inside `<article class="recipe-article">` below the recipe-body-grid, implementing the UI-SPEC 5-state machine exactly:
      State 1 (null cache): CbEyebrow "Estimated nutrition" + "Nutrition not yet calculated." + Accent "Calculate nutrition" CbButton.
      State 4 (_nutritionCalculating): same as State 1 with the button disabled showing `<span class="cb-pulse">Calculating…</span>`.
      State 5 (_nutritionError): heading + error role=status banner "Nutrition calculation failed — try again." + Accent "Try again" CbButton (re-invokes CalculateNutrition).
      State 2 (cache, IsStale=false): header row (eyebrow + per-serving/total segmented toggle, role=radiogroup, default Per serving) → 4-up macro grid (Energy kcal · Protein g · Carbs g · Fat g; .num tabular, accent numerals; per-serving or total per toggle; kcal 0 dp, macros 1 dp; 2-col at ≤720px) → cb-rule → coverage summary "Matched {MatchedIngredients} of {TotalIngredients} ingredients" → coverage list (collapsed: show unmatched + low-confidence rows; "Show all matches" ghost toggle reveals matched rows; each row: confidence badge [HIGH green / MEDIUM amber "≈" / UNMATCHED neutral], ingredient name, kcal value or "--", and on a second line the CNF description + "[{FoodId}]" for matched rows). Unmatched value renders "--" in var(--ink-4), NEVER "0". Low-confidence prepends "≈".
      State 3 (cache, IsStale=true): State 2 layout + a role=status warn banner "Recipe has changed — values may be outdated." with a Ghost "Recalculate nutrition" button (re-invokes CalculateNutrition); stale macro values shown dimmed (opacity:0.7).
    Disclaimer (ALL states, never conditional, never dismissable): the role="note" aria-label="Nutrition data notice" block with the VERBATIM copy "Estimated nutrition — not suitable for medical dietary planning. Data: Health Canada, Canadian Nutrient File (2015)." at font-size:12.5px;color:var(--ink-3);margin-top:16px. Do not paraphrase, split, or drop the trailing period.
    Use ONLY Cb atoms + cookbot-design.css tokens (no MudBlazor). The toggle and "Show all matches" are client-side StateHasChanged only (no server round-trip).
  </action>
  <verify>
    <automated>dotnet build src/CookBot.Web/CookBot.Web.csproj -c Debug 2>&1 | grep -E "Build succeeded|error" | head -8; grep -c "Estimated nutrition — not suitable for medical dietary planning. Data: Health Canada, Canadian Nutrient File (2015)." src/CookBot.Web/Components/Pages/RecipeView.razor</automated>
  </verify>
  <acceptance_criteria>
    - The Web project builds; RecipeView injects `INutritionService` and renders the `<section aria-label="Estimated nutrition">` panel below the recipe body.
    - The verbatim disclaimer string appears in RecipeView.razor (grep count ≥1) and is rendered in every panel state, non-dismissable (SC4/NUTR-05).
    - The heading uses "Estimated nutrition" (CbEyebrow), never "Calories" (SC4).
    - The panel implements all five states; the only compute trigger is the CTA handler (no compute on load/save — SC1/P7).
    - Coverage rows render "--" for unmatched (never "0"), "≈" for low-confidence, and show the CNF description + "[{FoodId}]" for matched rows (SC2/P4); the coverage summary reads "Matched n of total ingredients".
    - JSON-LD receives a per-serving `NutritionInfoDto` only when `_nutritionCache is { IsStale: false }` (SC5/NUTR-06 consumer side).
    - No MudBlazor; only Cb atoms + design tokens.
  </acceptance_criteria>
  <done>The nutrition panel renders the full 5-state UI-SPEC contract with honest coverage, the mandatory disclaimer, CTA-only compute, and JSON-LD wiring.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify the nutrition panel visual + functional contract</name>
  <files>src/CookBot.Web/Components/Pages/RecipeView.razor</files>
  <what-built>
    The 5-state nutrition panel on RecipeView: "Calculate nutrition" CTA (the only trigger), per-serving/total toggle, 4-up macro grid, honest coverage list ("--" unmatched / "≈" low-confidence with CNF description + FoodId visible), stale "Recalculate" affordance, error "Try again", and the non-dismissable Health Canada disclaimer in every state. Nutrition is also wired into the page's JSON-LD when a current cache exists.
  </what-built>
  <action>
    PAUSE for human verification. Do not auto-advance (this checkpoint is never auto-approvable). The implementer has finished Task 1; a human must run the dev server and confirm the visual + functional contract using the steps in <how-to-verify> before the phase is considered complete. Restart the dev server first (a stale :7000 process will lack the new migration/seed).
  </action>
  <how-to-verify>
    1. Restart the dev server (per the workflow memory — a stale :7000 server will not have the new migration/seed). Run `./run.sh`; confirm startup applies the AddNutritionTables migration and loads the CNF seed (logs).
    2. Open a recipe at http://localhost:7000/recipes/{id} that includes "all-purpose flour" (or add one). Confirm the panel shows State 1 ("Nutrition not yet calculated.") with a "Calculate nutrition" button and the disclaimer is visible.
    3. Click "Calculate nutrition". Confirm it transitions to State 2: heading "Estimated nutrition" (NOT "Calories"), a per-serving Energy value, and that flour contributes a sane calorie figure (≈455 kcal per US cup, not ~860). Toggle "Total"/"Per serving" — values update with no page reload.
    4. Confirm the coverage line reads "Matched n of total ingredients". Add or include an unmatched ingredient (e.g. "pinch of saffron") and recalculate — confirm it shows "--" (NOT "0") and is listed; confirm any low-confidence row shows "≈" and the matched CNF food description + [FoodId].
    5. Edit and save the recipe, return to RecipeView — confirm State 3 (stale banner "Recipe has changed — values may be outdated." + "Recalculate nutrition"); confirm the save itself was instant (never blocked on nutrition).
    6. Confirm the disclaimer "Estimated nutrition — not suitable for medical dietary planning. Data: Health Canada, Canadian Nutrient File (2015)." appears under the panel in every state and cannot be dismissed.
    7. View page source / dev-tools and confirm the `<script type="application/ld+json">` block contains a `nutrition` `NutritionInformation` object with `calories` once nutrition is calculated (and that it is absent before calculating).
    8. Check ≤720px width: the macro grid collapses to 2 columns; the panel stays full-width and readable.
  </how-to-verify>
  <verify>
    <human-check>The human confirms all five states render per the UI-SPEC; flour ≈455 kcal/cup; unmatched shows "--" not "0"; low-confidence shows "≈" + CNF description + FoodId; the verbatim disclaimer is present + non-dismissable in every state; save is instant; JSON-LD contains nutrition.calories only after calculation.</human-check>
  </verify>
  <acceptance_criteria>
    - All five states render per the UI-SPEC; heading is "Estimated nutrition"; disclaimer present + non-dismissable in every state (SC4).
    - Flour resolves to a sane calorie figure (~455/cup, not water-density) (SC3).
    - Unmatched → "--" (not 0); low-confidence → "≈" with CNF description + FoodId visible (SC2).
    - Save is instant and never blocked by nutrition; stale state appears after an edit (SC1/P7).
    - JSON-LD contains nutrition.calories only after calculation (SC5).
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe issues (e.g. wrong calorie figure, missing disclaimer, "0" shown for unmatched, panel not collapsing at ≤720px).</resume-signal>
  <done>A human has confirmed the nutrition panel's 5-state visual + functional contract (flour ≈455, "--"/"≈" coverage with CNF visibility, disclaimer everywhere, instant save + stale state, JSON-LD nutrition present-after-calc) or filed concrete issues.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| viewer → ComputeAsync(recipeId, userId) | The CTA invokes ComputeAsync with the current user id; ownership is enforced in NutritionService (Plan 05). |
| CNF description + computed numbers → Blazor render | Matched CNF FoodDescription and computed numbers are rendered into the panel and into the JSON-LD script. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-16 | Tampering (XSS) | Rendering CNF description + numbers in Razor | mitigate | The panel renders CNF text via normal Razor interpolation (auto HTML-encoded) — NOT MarkupString. The JSON-LD path uses the projector's HTML-safe encoder (Plan 06). No raw-HTML/MarkupString is introduced for nutrition content. |
| T-15-17 | Elevation of Privilege | CTA computing nutrition for a non-owned recipe | mitigate | ComputeAsync/GetCacheAsync enforce AssertOwnershipAsync (Plan 05); RecipeView passes the authenticated current user id (same source as the existing RecipePhotoSvc calls). |
| T-15-18 | Denial of Service | Compute on every render | mitigate | Nutrition is read from cache on load (cheap) and computed ONLY on explicit CTA click — never on render or save (SC1/P7). |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | No packages installed; Razor + existing Cb atoms only. |
</threat_model>

<verification>
- `dotnet build src/CookBot.Web/CookBot.Web.csproj` succeeds.
- `grep` confirms the verbatim disclaimer string is present in RecipeView.razor.
- Human checkpoint confirms the 5-state visual + functional contract (flour ≈455, "--" for unmatched, "≈" + CNF visibility, disclaimer everywhere, instant save + stale state, JSON-LD nutrition present-after-calc).
</verification>

<success_criteria>
- NUTR-04: per-serving + total panel with honest coverage ("--" unmatched, "≈" low-confidence, CNF description + FoodId visible) — never silently zeroed (SC2).
- NUTR-05: non-dismissable Health Canada disclaimer on every nutrition surface; heading "Estimated nutrition" (SC4).
- NUTR-06 (consumer): nutrition.calories appears in JSON-LD when a current cache exists, absent otherwise (SC5).
- SC1/P7: CTA-only compute; save never blocks on nutrition.
</success_criteria>

<output>
Create `.planning/phases/15-nutrition-offline-cnf-canadian-nutrient-file/15-07-SUMMARY.md` when done.
</output>
