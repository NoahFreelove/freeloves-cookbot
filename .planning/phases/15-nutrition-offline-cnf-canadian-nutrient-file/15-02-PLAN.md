---
phase: 15-nutrition-offline-cnf-canadian-nutrient-file
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/CookBot.Domain/Entities/CnfFood.cs
  - src/CookBot.Domain/Entities/CnfConversionFactor.cs
  - src/CookBot.Domain/Entities/RecipeNutritionCache.cs
  - src/CookBot.Application/DTOs/NutritionInfoDto.cs
  - src/CookBot.Application/Services/IngredientNormalizer.cs
  - tests/CookBot.Tests/Nutrition/IngredientNormalizerTests.cs
autonomous: true
requirements: [NUTR-01, NUTR-02, NUTR-06]
must_haves:
  truths:
    - "CnfFood stores energy + 3 macros per-100g with FoodId = CNF FoodCode (not internal FoodID)"
    - "RecipeNutritionCache holds total + per-serving values, a canonical-doc content hash, an IsStale flag, coverage counts, and per-ingredient match JSON — and is keyed by RecipeId (never written into CanonicalDocumentJson)"
    - "NutritionInfoDto is a pure per-serving value object the JSON-LD projector can consume with no DI"
    - "IngredientNormalizer strips the D-15-05 deny-list (prep/quality modifiers) as whole words while KEEPING nutrition-changing modifiers (unsalted, whole, lowfat, ...)"
  artifacts:
    - path: "src/CookBot.Domain/Entities/CnfFood.cs"
      provides: "CNF food POCO entity (verbatim per-100g macros + normalized-description column)"
      contains: "EnergyKcalPer100g"
    - path: "src/CookBot.Domain/Entities/CnfConversionFactor.cs"
      provides: "CNF household-measure→gram factor POCO (FK to CnfFood)"
      contains: "ConversionFactorValue"
    - path: "src/CookBot.Domain/Entities/RecipeNutritionCache.cs"
      provides: "Per-recipe computed-nutrition cache POCO with content hash + staleness"
      contains: "CanonicalDocHash"
    - path: "src/CookBot.Application/DTOs/NutritionInfoDto.cs"
      provides: "Per-serving nutrition value object for JsonLdRecipeProjector"
      contains: "CaloriesPerServing"
    - path: "src/CookBot.Application/Services/IngredientNormalizer.cs"
      provides: "Shared name normalizer + deny-list (used by seeder AND runtime matcher)"
      contains: "Normalize"
  key_links:
    - from: "src/CookBot.Domain/Entities/CnfConversionFactor.cs"
      to: "src/CookBot.Domain/Entities/CnfFood.cs"
      via: "FoodId FK + Food nav prop"
      pattern: "CnfFood"
---

<objective>
Define the pure POCO data contracts and the shared name normalizer that the rest of Phase 15 builds against (interface-first ordering): the two CNF seed entities (`CnfFood`, `CnfConversionFactor`), the per-recipe `RecipeNutritionCache`, the `NutritionInfoDto` value object the JSON-LD projector consumes, and the `IngredientNormalizer` used BOTH at seed load (Plan 03, to pre-compute `NormalizedDescription`) AND at runtime match (Plan 05). One owner of normalization eliminates seed/runtime drift.

Purpose: Eliminate the scavenger-hunt anti-pattern — downstream plans receive exact field names/types rather than exploring. Encodes the hard invariant in code: nutrition lives in `RecipeNutritionCache` keyed by `RecipeId`, never in `CanonicalDocumentJson`. Guarantees the seeder and the matcher normalize identically.
Output: 3 Domain entities + 1 Application DTO + 1 normalizer + normalizer tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/15-nutrition-offline-cnf-canadian-nutrient-file/15-CONTEXT.md
@.planning/phases/15-nutrition-offline-cnf-canadian-nutrient-file/15-PATTERNS.md
@.planning/phases/15-nutrition-offline-cnf-canadian-nutrient-file/15-RESEARCH.md

<interfaces>
<!-- Analog entity (FK-child + null! nav + = [] collection conventions). Executor copies these conventions. -->
From src/CookBot.Domain/Entities/RecipePhoto.cs — pure POCO, no using directives, XML doc citing phase/req, `null!` nav on FK side, `= []` collections on principal side.

From src/CookBot.Domain/Entities/Recipe.cs — `CanonicalDocumentJson` is the TEXT JSON column precedent; RecipeNutritionCache mirrors the TEXT-column shape for PerIngredientMatchJson.

From src/CookBot.Application/Services/IngredientResolver.cs — the exact Normalize pipeline (ToLowerInvariant → replace [-_] with space → collapse \s+ → trim) that IngredientNormalizer extends with the deny-list.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create the three CNF/nutrition Domain entities</name>
  <files>src/CookBot.Domain/Entities/CnfFood.cs, src/CookBot.Domain/Entities/CnfConversionFactor.cs, src/CookBot.Domain/Entities/RecipeNutritionCache.cs</files>
  <read_first>
    - src/CookBot.Domain/Entities/RecipePhoto.cs (the FK-child + nav-prop + collection-init conventions to copy verbatim)
    - src/CookBot.Domain/Entities/Recipe.cs (the TEXT/JSON column precedent + the FK-to-Recipe principal side)
    - .planning/phases/15-nutrition-offline-cnf-canadian-nutrient-file/15-PATTERNS.md §"CnfFood.cs", §"CnfConversionFactor.cs", §"RecipeNutritionCache.cs" (exact field lists per D-15-02 / D-15-11)
    - .planning/phases/15-nutrition-offline-cnf-canadian-nutrient-file/15-RESEARCH.md §"Research Target 5 → Table designs" (verbatim field declarations)
  </read_first>
  <action>
    In `CookBot.Domain.Entities` (no framework refs — pure POCOs, mirror RecipePhoto.cs which has no using directives):
    `CnfFood` (D-15-02): `int FoodId` (PK = CNF FoodCode, user-visible code from the REST API, NOT internal FoodID), `string FoodDescription = string.Empty`, `string? NormalizedDescription` (pre-computed at seed load for runtime match), `string? FoodGroup`, and four verbatim per-100g doubles: `EnergyKcalPer100g`, `ProteinGPer100g`, `FatGPer100g`, `CarbGPer100g`; principal-side collection `ICollection<CnfConversionFactor> ConversionFactors = []`. XML doc must state values are stored verbatim (OGL-Canada forbids modification) and that FoodId is the FoodCode.
    `CnfConversionFactor` (D-15-02): surrogate `int Id` PK, `int FoodId` FK, `string MeasureDescription = string.Empty` (e.g. "250ml", "15ml"), `double ConversionFactorValue` (grams_in_measure / 100), nav `CnfFood Food = null!`.
    `RecipeNutritionCache` (D-15-11): `int RecipeId` (PK + FK to Recipe, 1:1), `string CanonicalDocHash = string.Empty` (SHA-256 hex of CanonicalDocumentJson), `bool IsStale`, totals `TotalEnergyKcal/TotalProteinG/TotalFatG/TotalCarbG` (double), `int? Servings` (snapshot of doc.Servings at compute time), per-serving `PerServingEnergyKcal/PerServingProteinG/PerServingFatG/PerServingCarbG` (double), `int MatchedIngredients`, `int TotalIngredients`, `string PerIngredientMatchJson = string.Empty` (TEXT — JSON array of {name, cnfFoodId, cnfDesc, confidence, kcal, proteinG, fatG, carbG}), `DateTime ComputedAt`, nav `Recipe Recipe = null!`. XML doc must state this is the ONLY home for computed nutrition and is NEVER serialized into CanonicalDocumentJson (hard invariant).
    Do NOT add EF attributes, EF configs, or any `using Microsoft.EntityFrameworkCore` — configuration is Plan 03.
  </action>
  <verify>
    <automated>dotnet build src/CookBot.Domain/CookBot.Domain.csproj -c Debug 2>&1 | grep -E "Build succeeded|error" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - The three entity files compile in `CookBot.Domain` with no framework references (no `using Microsoft.*`).
    - `CnfFood` exposes the four `*Per100g` doubles + `NormalizedDescription` + `ConversionFactors` collection; XML doc states FoodId = FoodCode and values verbatim.
    - `RecipeNutritionCache` exposes `CanonicalDocHash`, `IsStale`, total + per-serving fields, `MatchedIngredients`/`TotalIngredients`, `PerIngredientMatchJson`, keyed by `RecipeId`; XML doc states it is never written into CanonicalDocumentJson.
    - `CnfConversionFactor` has `FoodId` FK + `Food` nav + `ConversionFactorValue`.
  </acceptance_criteria>
  <done>Domain entities exist and compile; downstream EF/service/projector plans have fixed field contracts.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create the NutritionInfoDto per-serving value object</name>
  <files>src/CookBot.Application/DTOs/NutritionInfoDto.cs</files>
  <read_first>
    - .planning/phases/15-nutrition-offline-cnf-canadian-nutrient-file/15-PATTERNS.md §"NutritionInfoDto.cs" (sealed record shape, D-15-13)
    - .planning/phases/15-nutrition-offline-cnf-canadian-nutrient-file/15-RESEARCH.md §"Research Target 6 → Value object type name"
    - src/CookBot.Application/Recipes/JsonLdRecipeProjector.cs (the consumer; confirm the namespace it can reference — projector is pure static, so the DTO must carry no EF/DI)
  </read_first>
  <action>
    In `CookBot.Application.DTOs` create `public sealed record NutritionInfoDto(double CaloriesPerServing, double ProteinGPerServing, double FatGPerServing, double CarbGPerServing)` (D-15-13). Pure value type — no EF, no DI, no data-service access. XML doc: per-serving nutrition passed to `JsonLdRecipeProjector.Project` (NUTR-06); Schema.org `nutrition` is per-serving.
  </action>
  <verify>
    <automated>dotnet build src/CookBot.Application/CookBot.Application.csproj -c Debug 2>&1 | grep -E "Build succeeded|error" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `NutritionInfoDto` is a `sealed record` in `CookBot.Application.DTOs` with exactly the four per-serving double members; the Application project compiles.
    - The DTO has no EF/DI references (it is consumable by the pure static projector).
  </acceptance_criteria>
  <done>Per-serving value object exists; Plan 06 (projector) and Plan 05/07 (service/UI) can construct it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create the shared IngredientNormalizer (deny-list) + tests</name>
  <files>src/CookBot.Application/Services/IngredientNormalizer.cs, tests/CookBot.Tests/Nutrition/IngredientNormalizerTests.cs</files>
  <read_first>
    - src/CookBot.Application/Services/IngredientResolver.cs (the full Normalize pipeline to mirror — lines 10-16)
    - .planning/phases/15-nutrition-offline-cnf-canadian-nutrient-file/15-PATTERNS.md §"IngredientNormalizer.cs" (deny-list array + \b word-boundary loop)
    - .planning/phases/15-nutrition-offline-cnf-canadian-nutrient-file/15-CONTEXT.md §"D-15-05" (the exact deny-list + the KEEP list)
    - .planning/phases/15-nutrition-offline-cnf-canadian-nutrient-file/15-RESEARCH.md §"Research Target 4 → Deny-list application" (strip full-word not substring; keep nutrition-changing modifiers)
  </read_first>
  <behavior>
    - "finely chopped unsalted butter" → normalized contains "unsalted" and "butter", does NOT contain "finely" or "chopped"
    - "room-temperature whole milk" → contains "whole" and "milk", does NOT contain "room" or "temperature"
    - "good-quality olive oil, divided" → contains "olive" and "oil", does NOT contain "good", "quality", or "divided"
    - "low-fat plain yogurt" → KEEPS "low" and "fat" (lowfat/low-fat is nutrition-changing); does NOT strip them
    - "Grains, wheat flour, white, all purpose, enriched" (a CNF description) → contains "flour" and "purpose"; punctuation/commas collapsed; deny-listed quality words removed
    - Deny-list match is whole-word only: "buttermilk" is NOT altered by stripping "butter" (no substring strip)
  </behavior>
  <action>
    Create `public static class IngredientNormalizer` in `CookBot.Application.Services` with `public static string Normalize(string name)`. Mirror `IngredientResolver.Normalize` (lowercase, `Regex.Replace [-_]`→space, collapse `\s+`, trim), then strip the D-15-05 deny-list tokens as WHOLE WORDS via `Regex.Replace(lower, $@"\b{Regex.Escape(token)}\b", " ")` and re-collapse whitespace. Deny-list (strip — non-nutritive): chopped, minced, diced, sliced, shredded, grated, ground, sifted, packed, finely, roughly, freshly, room-temperature, room temperature, cold, warm, good-quality, good, fine, coarse, large, small, medium, ripe, organic, to taste, optional, divided, for garnish, plus more. KEEP (do NOT add to deny-list — these change nutrition): unsalted, salted, skinless, lowfat, low-fat, whole, light, heavy. Also strip commas/punctuation so CNF genus-first descriptions tokenize cleanly. This single function is consumed by the seeder (Plan 03 NormalizedDescription pre-compute) and the runtime matcher (Plan 05) — there is exactly one normalization owner.
    Write the test class covering every case in <behavior>, plus the whole-word-not-substring guard (buttermilk).
  </action>
  <verify>
    <automated>dotnet test tests/CookBot.Tests/CookBot.Tests.csproj --filter "FullyQualifiedName~IngredientNormalizer" 2>&1 | grep -E "Passed!|Failed!|Passed:|Failed:|error" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - All <behavior> cases pass, including: prep modifiers stripped, nutrition-changing modifiers (low-fat, whole, unsalted) KEPT, and whole-word-only stripping ("buttermilk" unaffected by "butter" in the deny-list — though "butter" is not in the deny-list, the test asserts no substring damage for any deny token).
    - `IngredientNormalizer.Normalize` is a single static method usable by both the seeder and the matcher (no DI, no DB).
  </acceptance_criteria>
  <done>One shared, tested normalizer; seed-time and runtime matching cannot drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recipe ingredient name → normalizer | Untrusted recipe text enters the normalizer; it must be bounded and non-injectable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04 | Tampering | RecipeNutritionCache as a parallel nutrition store | mitigate | The entity's XML doc and `RecipeId` keying encode the display-only invariant in code: nutrition lives here, never in CanonicalDocumentJson. Write-ownership enforced in Plan 05. |
| T-15-08 | Denial of Service | ReDoS in normalization regex | mitigate | All regexes are linear-time (`[-_]`, `\s+`, fixed `\b{escaped-literal}\b`) over a bounded ingredient name; no nested quantifiers or catastrophic backtracking. Deny-list tokens are `Regex.Escape`-d. |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | No packages installed; BCL regex + record/POCO only. |
</threat_model>

<verification>
- `dotnet build` on Domain + Application succeed.
- `dotnet test --filter IngredientNormalizer` passes all behavior cases.
- No `using Microsoft.EntityFrameworkCore` in the Domain entities.
</verification>

<success_criteria>
- Data contracts for NUTR-01/02/06 exist as compiling types and a single tested normalizer is shared by seed-time and runtime, unblocking Plans 03–07.
</success_criteria>

<output>
Create `.planning/phases/15-nutrition-offline-cnf-canadian-nutrient-file/15-02-SUMMARY.md` when done.
</output>
