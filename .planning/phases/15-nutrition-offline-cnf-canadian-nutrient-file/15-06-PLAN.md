---
phase: 15-nutrition-offline-cnf-canadian-nutrient-file
plan: 06
type: execute
wave: 2
depends_on: ["15-02"]
files_modified:
  - src/CookBot.Application/Recipes/JsonLdRecipeProjector.cs
  - tests/CookBot.Tests/Nutrition/JsonLdNutritionProjectorTests.cs
autonomous: true
requirements: [NUTR-06]
must_haves:
  truths:
    - "When a NutritionInfoDto is passed, the JSON-LD includes a NutritionInformation object with per-serving calories + protein/carbs/fat in Schema.org format"
    - "When nutrition is null, the JSON-LD output is byte-identical to the Phase 13 baseline — no 'nutrition' key, existing field order unchanged (SC5)"
    - "The projector stays a pure static function — no DI, no data-service access, never emits aggregateRating/review"
  artifacts:
    - path: "src/CookBot.Application/Recipes/JsonLdRecipeProjector.cs"
      provides: "Project(doc, absoluteImageUrl, nutrition?) — optional NutritionInformation emission"
      contains: "NutritionInformation"
  key_links:
    - from: "src/CookBot.Application/Recipes/JsonLdRecipeProjector.cs"
      to: "src/CookBot.Application/DTOs/NutritionInfoDto.cs"
      via: "optional third param consumed only when non-null"
      pattern: "NutritionInfoDto"
---

<objective>
Wire nutrition into the Schema.org JSON-LD output (NUTR-06) by adding an optional third parameter to `JsonLdRecipeProjector.Project`. When a `NutritionInfoDto` is supplied, emit a `NutritionInformation` object (`calories: "N calories"`, `proteinContent`/`carbohydrateContent`/`fatContent: "N g"`, per-serving); when null, omit it cleanly and leave the Phase 13 output byte-identical. The projector remains a pure static function (no DI).

Purpose: NUTR-06 + SC5 — nutrition appears in structured data only when computed, and the absent-nutrition path must not regress the Phase 13 golden snapshot.
Output: the projector change + present/absent unit & golden tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/15-nutrition-offline-cnf-canadian-nutrient-file/15-CONTEXT.md
@.planning/phases/15-nutrition-offline-cnf-canadian-nutrient-file/15-RESEARCH.md
@.planning/phases/15-nutrition-offline-cnf-canadian-nutrient-file/15-PATTERNS.md

<interfaces>
<!-- The DTO from Plan 02 + the existing projector contract -->
From src/CookBot.Application/DTOs/NutritionInfoDto.cs: sealed record NutritionInfoDto(double CaloriesPerServing, double ProteinGPerServing, double FatGPerServing, double CarbGPerServing).
From src/CookBot.Application/Recipes/JsonLdRecipeProjector.cs (existing, line refs):
  - line 20-22: `LdOptions` with DefaultIgnoreCondition = WhenWritingNull + the HTML-safe STJ encoder (keep using LdOptions — do NOT introduce a raw encoder).
  - line 69: `public static string Project(RecipeDocument doc, string? absoluteImageUrl)` — the signature to extend.
  - lines 131-142: the `if (x is not null) model["key"] = x;` conditional-entry pattern; `author` is added at line 142.
  - line 143: `// NEVER emit: aggregateRating, review, datePublished` — keep unchanged.
  - line 145: `return JsonSerializer.Serialize(model, LdOptions);`
Existing golden: tests/CookBot.Tests/Snapshots/JsonLdRecipeProjectorTests.FullDocument_ProducesExpectedJsonLd.verified.txt — MUST remain unchanged (nutrition-absent).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the optional nutrition param to JsonLdRecipeProjector.Project</name>
  <files>src/CookBot.Application/Recipes/JsonLdRecipeProjector.cs</files>
  <read_first>
    - src/CookBot.Application/Recipes/JsonLdRecipeProjector.cs (full file — the LdOptions encoder at 20-22, the signature at 69, the conditional model entries 131-142, the doc-comment invariant at lines ~8-15 forbidding DI/data-service access)
    - .planning/phases/15-nutrition-offline-cnf-canadian-nutrient-file/15-RESEARCH.md §"Research Target 6 → Schema.org NutritionInformation" (calories "N calories" not "N kcal"; proteinContent/carbohydrateContent/fatContent "N g"; per-serving; recipeYield must already be present — it is via model["recipeYield"]=doc.Servings) and "→ Minimal projector change"
    - .planning/phases/15-nutrition-offline-cnf-canadian-nutrient-file/15-PATTERNS.md §"JsonLdRecipeProjector.cs (MODIFY — add optional nutrition? param)" (the exact dict shape + format strings)
  </read_first>
  <action>
    Change the signature to `public static string Project(RecipeDocument doc, string? absoluteImageUrl, NutritionInfoDto? nutrition = null)` (the default keeps all existing 2-arg call sites compiling). After the `author` conditional entry (line ~142) and before the `return JsonSerializer.Serialize(model, LdOptions)`, add: `if (nutrition is not null) model["nutrition"] = new Dictionary<string,string> { ["@type"]="NutritionInformation", ["calories"]=$"{nutrition.CaloriesPerServing:0} calories", ["proteinContent"]=$"{nutrition.ProteinGPerServing:0.#} g", ["carbohydrateContent"]=$"{nutrition.CarbGPerServing:0.#} g", ["fatContent"]=$"{nutrition.FatGPerServing:0.#} g" };`. Keep serializing through `LdOptions` (HTML-safe encoder — do NOT change it). Do NOT add DI, a constructor, or any data-service access; do NOT emit aggregateRating/review. Add an XML-doc line on the new param noting it is per-serving and emitted only when non-null (SC5). Update the projector's own doc-comment param list if it enumerates params.
  </action>
  <verify>
    <automated>dotnet build src/CookBot.Application/CookBot.Application.csproj -c Debug 2>&1 | grep -E "Build succeeded|error" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `Project` has the new optional `NutritionInfoDto? nutrition = null` third param; existing 2-arg call sites still compile (default arg).
    - When non-null, `model["nutrition"]` is a NutritionInformation dict with `calories` formatted as `"{N} calories"` (0 dp) and the three macros as `"{N} g"` (1 dp).
    - The projector remains pure static (no DI/data-service); it still serializes via `LdOptions` (HTML-safe) and never emits aggregateRating/review.
  </acceptance_criteria>
  <done>Projector emits per-serving NutritionInformation only when nutrition is supplied; pure-function invariant preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Present/absent nutrition projector tests + golden regression</name>
  <files>tests/CookBot.Tests/Nutrition/JsonLdNutritionProjectorTests.cs</files>
  <read_first>
    - tests/CookBot.Tests/Recipes/JsonLdRecipeProjectorTests.cs (the MakeFullDocument helper + JsonDocument.Parse assertion style + the Verifier.Verify golden pattern)
    - tests/CookBot.Tests/Snapshots/JsonLdRecipeProjectorTests.FullDocument_ProducesExpectedJsonLd.verified.txt (the baseline that must NOT change for the nutrition-absent path)
    - .planning/phases/15-nutrition-offline-cnf-canadian-nutrient-file/15-PATTERNS.md §"JsonLdNutritionProjectorTests.cs" (the four test shapes)
  </read_first>
  <behavior>
    - Nutrition_OmittedWhenNull: Project(doc, null, null) → parsed JSON has NO "nutrition" property (SC5)
    - WithNutrition_IncludesNutritionInformation: Project(doc, "https://host/img.jpg", new NutritionInfoDto(455,12.9,1.2,95.4)) → parsed "nutrition" has @type "NutritionInformation", calories "455 calories", proteinContent "12.9 g", carbohydrateContent "95.4 g", fatContent "1.2 g"
    - NutritionCalories_RoundsToWholeNumber: CaloriesPerServing 455.6 → "456 calories" (0 dp); ProteinGPerServing 12.94 → "12.9 g" (1 dp)
    - Baseline_NutritionAbsentGoldenUnchanged: the existing FullDocument golden snapshot still verifies byte-for-byte with the nutrition-absent call (regression guard — do NOT modify the .verified.txt)
  </behavior>
  <action>
    Create `tests/CookBot.Tests/Nutrition/JsonLdNutritionProjectorTests.cs`. Reuse a full RecipeDocument (copy/adapt `MakeFullDocument` from the existing JsonLdRecipeProjectorTests) for the present-case golden, and a minimal doc for the omitted-case. Use `JsonDocument.Parse` + `TryGetProperty`/`GetProperty` assertions for the explicit checks. For the present-case golden, add a new `.verified.txt` snapshot (do NOT touch the existing one). Add the regression assertion that the nutrition-absent path produces the same output the Phase 13 test already verifies (call the 2-arg / null-nutrition form and confirm no "nutrition" key — the existing FullDocument golden in JsonLdRecipeProjectorTests continues to pass unchanged, which this plan must not break).
  </action>
  <verify>
    <automated>dotnet test tests/CookBot.Tests/CookBot.Tests.csproj --filter "FullyQualifiedName~JsonLd" 2>&1 | grep -E "Passed!|Failed!|Passed:|Failed:|error" | head -6</automated>
  </verify>
  <acceptance_criteria>
    - Nutrition_OmittedWhenNull passes: no "nutrition" key when DTO is null (SC5).
    - WithNutrition test passes: NutritionInformation block has the exact Schema.org keys + per-serving formatted values ("455 calories", "12.9 g", ...).
    - Rounding test passes (calories 0 dp, macros 1 dp).
    - The existing `JsonLdRecipeProjectorTests.FullDocument_ProducesExpectedJsonLd` golden still passes byte-for-byte (nutrition-absent regression) — its `.verified.txt` is unmodified.
  </acceptance_criteria>
  <done>Nutrition present/absent JSON-LD behavior is proven; the Phase 13 baseline is regression-locked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nutrition numbers + CNF text → JSON-LD HTML | Computed numbers and (indirectly) CNF-derived strings are serialized into a `<script type="application/ld+json">` block rendered into the page. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-14 | Tampering (XSS / JSON-LD injection) | JsonLdRecipeProjector serialize | mitigate | Output is serialized via the existing `LdOptions` HTML-safe System.Text.Json encoder (`< > &` → `\u00xx`); the new nutrition values are numeric-formatted strings ("N calories", "N g") with no untrusted free text. RecipeView already wraps the output as a MarkupString relying on this encoder — unchanged here. |
| T-15-15 | Tampering | Projector self-fetching / mutating canonical | mitigate | The projector stays a pure static function receiving the DTO by argument — no DI, no data-service, no CanonicalDocumentJson access (P15). |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | No packages installed; System.Text.Json only. |
</threat_model>

<verification>
- `dotnet test --filter JsonLd` passes (present, absent, rounding, baseline-unchanged).
- The existing `JsonLdRecipeProjectorTests.FullDocument_ProducesExpectedJsonLd.verified.txt` is byte-for-byte unchanged.
</verification>

<success_criteria>
- NUTR-06 / SC5: per-serving `nutrition.calories` + macros appear in JSON-LD only when nutrition is supplied; absent path is byte-identical to the Phase 13 baseline.
</success_criteria>

<output>
Create `.planning/phases/15-nutrition-offline-cnf-canadian-nutrient-file/15-06-SUMMARY.md` when done.
</output>
