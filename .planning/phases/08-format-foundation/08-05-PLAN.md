---
phase: 08-format-foundation
plan: 05
type: execute
wave: 3
depends_on:
  - 08-03
files_modified:
  - src/CookBot.Application/Services/RecipeFormatParser.cs
  - src/CookBot.Application/Services/JsonRecipeSerializer.cs
  - src/CookBot.Application/Recipes/Converters/StepTemperatureJsonConverter.cs
  - src/CookBot.Domain/Interfaces/IRecipeFormatParser.cs
  - tests/CookBot.Tests/Services/RecipeFormatParserTests.cs
  - tests/CookBot.Tests/Recipes/RecipeDocumentRoundTripTests.cs
  - tests/CookBot.Tests/Fixtures/Recipes/v3-canonical/simple-with-photo.json
  - tests/CookBot.Tests/Fixtures/Recipes/v3-canonical/sectioned-with-temperature.json
  - tests/CookBot.Tests/Fixtures/Recipes/v3-canonical/full-v3-all-units.json
autonomous: true
requirements:
  - SCHEMA-08
  - SCHEMA-09
  - SCHEMA-12
must_haves:
  truths:
    - "RecipeFormatParser reads and writes the three v3 fields in YAML wire format (photoUrl:, description:, temperature: { value, unit } per ContentStep)"
    - "JsonRecipeSerializer round-trips RecipeDocument with all three v3 fields through the canonical JSON column"
    - "Wire format always uses { value: N, unit: \"X\" } for temperature — even gas half-stops are { value: 4.5, unit: \"gas\" }"
    - "Indented JSON export (SerializeIndented) renders gas half-stops as the human-readable string \"4½\" via StepTemperatureJsonConverter"
    - "ParsedRecipe / ParsedStep flat DTOs (defined in src/CookBot.Domain/Interfaces/IRecipeFormatParser.cs) carry the new fields end-to-end so RecipeService can write them to Recipe.PhotoUrl/Description and ContentStep.Temperature in the canonical doc"
    - "All existing RecipeFormatParserTests still pass (SCHEMA-12 / H11 — no test deletion); new v3 fixture tests cover the three units F/C/gas plus null"
  artifacts:
    - path: src/CookBot.Application/Services/RecipeFormatParser.cs
      provides: "YAML/JSON read+write of PhotoUrl, Description, Temperature"
    - path: src/CookBot.Application/Services/JsonRecipeSerializer.cs
      provides: "Indented serializer wires StepTemperatureJsonConverter"
    - path: src/CookBot.Application/Recipes/Converters/StepTemperatureJsonConverter.cs
      provides: "Gas half-stop string rendering for human-readable export"
      contains: "JsonConverter<StepTemperature>"
    - path: src/CookBot.Domain/Interfaces/IRecipeFormatParser.cs
      provides: "ParsedRecipe / ParsedStep records extended with v3 fields (PhotoUrl, Description, Temperature)"
      contains: "ParsedRecipe"
  key_links:
    - from: src/CookBot.Application/Services/JsonRecipeSerializer.cs
      to: src/CookBot.Application/Recipes/Converters/StepTemperatureJsonConverter.cs
      via: "_indented.Converters.Add(new StepTemperatureJsonConverter())"
      pattern: "StepTemperatureJsonConverter"
---

<objective>
Extend the parser/serializer stack to read and write the three v3 fields per SCHEMA-08, SCHEMA-09, SCHEMA-12. Wire format is always `{ value, unit }` for temperature (even gas half-stops); the human-readable indented JSON export renders gas as `"4½"` via a custom JsonConverter (D-27 + planner discretion). Round-trip fixture coverage for null, valid F, valid C, valid Gas (whole and half), and the all-three-fields case.

Purpose: Close SCHEMA-08 (parser), SCHEMA-09 (serializer round-trip), and SCHEMA-12 (parser test coverage with no deletion). After this plan ships, an AI-generated v3 recipe end-to-end round-trips through the canonical pipeline (parse YAML/JSON -> RecipeDocument -> JsonRecipeSerializer.Serialize -> store -> Deserialize -> back to RecipeDocument).
Output: Two existing application files modified, one new converter file, one Domain interface file extended (ParsedRecipe / ParsedStep records gain v3 fields), two existing test files extended, three new v3 fixtures.
</objective>

<execution_context>
@.claude/../get-shit-done/workflows/execute-plan.md
@.claude/../get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-format-foundation/08-CONTEXT.md
@.planning/phases/08-format-foundation/08-PATTERNS.md
@src/CookBot.Application/Services/RecipeFormatParser.cs
@src/CookBot.Application/Services/JsonRecipeSerializer.cs
@src/CookBot.Domain/Recipes/StepTemperature.cs
@src/CookBot.Domain/Recipes/RecipeDocument.cs
@src/CookBot.Domain/Recipes/StepNode.cs
@src/CookBot.Domain/Interfaces/IRecipeFormatParser.cs
@tests/CookBot.Tests/Services/RecipeFormatParserTests.cs
@tests/CookBot.Tests/Recipes/RecipeDocumentRoundTripTests.cs
@CLAUDE.md

<interfaces>
<!-- Existing serialization shape — extend, do not restructure. -->

From src/CookBot.Application/Services/JsonRecipeSerializer.cs:
- public sealed class JsonRecipeSerializer
  - Constructor configures _compact (DefaultIgnoreCondition = WhenWritingNull, CamelCase) and _indented (WriteIndented = true, inherits from _compact)
  - Serialize(RecipeDocument) — uses _compact (DB column)
  - SerializeIndented(RecipeDocument) — uses _indented (export, README example)
  - Deserialize(string) — back to RecipeDocument

From src/CookBot.Application/Services/RecipeFormatParser.cs:
- Internal RecipeFrontmatter / StepFrontmatter / IngredientFrontmatter / TimerFrontmatter classes mirror the YAML wire shape
- ProjectToParsedRecipe(...) at line 252 — the inverse projection from RecipeDocument to ParsedRecipe (the flat DTO RecipeService consumes)

From src/CookBot.Domain/Interfaces/IRecipeFormatParser.cs (the canonical home of ParsedRecipe / ParsedStep — the flat DTOs RecipeService consumes; both record types live in this single file alongside the IRecipeFormatParser interface):
- public class ParsedRecipe { Name; Servings; PrepTimeMinutes; CookTimeMinutes; List<string> Tags; List<ParsedIngredient> Ingredients; List<ParsedStep> Steps; }
- public class ParsedStep { Text; bool IsSection; List<ParsedTimer>? Timers; }
- This plan extends ParsedRecipe with `PhotoUrl: string?` and `Description: string?`, and ParsedStep with `Temperature: StepTemperature?`.

From src/CookBot.Domain/Recipes/StepTemperature.cs (Plan 02):
- public sealed record StepTemperature { decimal Value; TemperatureUnit Unit; }
- public enum TemperatureUnit { F, C, Gas }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create StepTemperatureJsonConverter for gas half-stop rendering (D-27 + Discretion)</name>
  <files>src/CookBot.Application/Recipes/Converters/StepTemperatureJsonConverter.cs, src/CookBot.Application/Services/JsonRecipeSerializer.cs</files>
  <read_first>
    - src/CookBot.Application/Services/JsonRecipeSerializer.cs (the constructor shape — _compact and _indented JsonSerializerOptions; insertion point per PATTERNS.md "JsonRecipeSerializer.cs (modify — wire converter)" block ~line 436)
    - src/CookBot.Domain/Recipes/StepTemperature.cs (the type being converted)
    - .planning/phases/08-format-foundation/08-PATTERNS.md "StepTemperatureJsonConverter.cs (create)" block ~line 314 (full converter spec — Read at default depth converter; Write switches on Unit==Gas + half-step to render "4½"/"7½"; otherwise emit standard {value, unit})
    - .planning/phases/08-format-foundation/08-CONTEXT.md §D-27 (compact wire format ALWAYS { value, unit }; "4½" rendering is human-readable export only)
    - CLAUDE.md (Things to avoid — no Newtonsoft)
  </read_first>
  <behavior>
    - Read: pass-through to default StepTemperature deserialization. Per PATTERNS.md "Implement Read as pass-through (delegate to default deserialization shape)" — accepts both `{ "value": 4.5, "unit": "gas" }` (the standard wire shape) and the human-readable `"4½"` string (defensive — a user editing the indented JSON might keep the string form). For the string form, parse the leading number plus the optional fraction glyph (½ = 0.5) and infer unit=Gas (only Gas uses the glyph form per D-27).
    - Write: when Unit == Gas AND Value % 1m != 0m, write the JSON STRING `"4½"` (with the actual ½ Unicode character U+00BD). Otherwise emit the standard `{ "value": N, "unit": "X" }` object.
    - Wired ONLY into _indented (SerializeIndented) — _compact (DB column) always emits the standard object form per D-27.
  </behavior>
  <action>
    Step A — create directory + file `src/CookBot.Application/Recipes/Converters/StepTemperatureJsonConverter.cs`. File-scoped namespace `CookBot.Application.Recipes.Converters`. `using System.Text.Json;` and `using System.Text.Json.Serialization;` and `using CookBot.Domain.Recipes;`. Class:

    Subclass `JsonConverter<StepTemperature>`. Implement Read(reader, typeToConvert, options): if `reader.TokenType == JsonTokenType.String`, parse the string form (read the value, check for trailing ½, set Unit=Gas, Value=numericPart + (hasHalf ? 0.5m : 0m)). Otherwise (TokenType == JsonTokenType.StartObject), call `JsonSerializer.Deserialize<StepTemperature>(ref reader, /* options without this converter */)` — to avoid recursion, you must construct a new `JsonSerializerOptions` that excludes this converter, OR delegate to a default-shape struct via the JsonSerializerOptions overload that ignores converters. Simpler: read manually — `using var doc = JsonDocument.ParseValue(ref reader); var v = doc.RootElement.GetProperty("value").GetDecimal(); var u = Enum.Parse<TemperatureUnit>(doc.RootElement.GetProperty("unit").GetString()!, ignoreCase: true); return new StepTemperature { Value = v, Unit = u };`.

    Implement Write(writer, value, options): if `value.Unit == TemperatureUnit.Gas && value.Value % 1m != 0m`, build the human-readable string with `((int)value.Value).ToString() + "½"` (e.g. `4½`, `7½`) and `writer.WriteStringValue(humanReadable)`. Otherwise emit standard object: `writer.WriteStartObject(); writer.WriteNumber("value", value.Value); writer.WriteString("unit", value.Unit.ToString().ToLowerInvariant()); writer.WriteEndObject();` — note the lowercase unit per D-27 wire format.

    Step B — modify `src/CookBot.Application/Services/JsonRecipeSerializer.cs` per PATTERNS.md "JsonRecipeSerializer.cs (modify — wire converter)" block: in the constructor, after `_indented = new JsonSerializerOptions(_compact) { WriteIndented = true };`, add `_indented.Converters.Add(new StepTemperatureJsonConverter());`. Do NOT add it to _compact — the SQLite-stored canonical JSON and wire format always use the standard object form per D-27.

    Add `using CookBot.Application.Recipes.Converters;` to JsonRecipeSerializer.cs.

    Per CLAUDE.md / Pattern S5: System.Text.Json only. Per Pattern S6: file-scoped namespace, single public type per file.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/freeloves-cookbot &amp;&amp; dotnet build src/CookBot.Application/CookBot.Application.csproj 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f src/CookBot.Application/Recipes/Converters/StepTemperatureJsonConverter.cs`
    - Subclasses JsonConverter&lt;StepTemperature&gt;: `grep -c "JsonConverter&lt;StepTemperature&gt;" src/CookBot.Application/Recipes/Converters/StepTemperatureJsonConverter.cs` returns 1
    - Both Read and Write defined: `grep -c "public override.*Read\|public override.*Write" src/CookBot.Application/Recipes/Converters/StepTemperatureJsonConverter.cs` returns 2
    - Half-stop glyph present in source: `grep -c "½" src/CookBot.Application/Recipes/Converters/StepTemperatureJsonConverter.cs` returns at least 1
    - Wired into _indented only: `grep -c "_indented.Converters.Add(new StepTemperatureJsonConverter" src/CookBot.Application/Services/JsonRecipeSerializer.cs` returns 1
    - NOT wired into _compact: `grep -c "_compact.Converters.Add(new StepTemperatureJsonConverter" src/CookBot.Application/Services/JsonRecipeSerializer.cs` returns 0
    - Compiles: `dotnet build src/CookBot.Application/CookBot.Application.csproj` exits 0
    - No Newtonsoft: `grep -c "Newtonsoft" src/CookBot.Application/Recipes/Converters/StepTemperatureJsonConverter.cs` returns 0
  </acceptance_criteria>
  <done>SerializeIndented now renders gas half-stops as "4½"; Serialize (compact, DB column) still emits the standard object form per D-27.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend RecipeFormatParser for YAML/JSON read+write of v3 fields (SCHEMA-08)</name>
  <files>src/CookBot.Application/Services/RecipeFormatParser.cs, src/CookBot.Domain/Interfaces/IRecipeFormatParser.cs</files>
  <read_first>
    - src/CookBot.Application/Services/RecipeFormatParser.cs (the file being extended; specifically the inner RecipeFrontmatter / StepFrontmatter classes around line 416 and the ProjectToParsedRecipe at line 252)
    - src/CookBot.Domain/Interfaces/IRecipeFormatParser.cs (the canonical location of the ParsedRecipe / ParsedStep records — both classes live in this single file alongside the IRecipeFormatParser interface; this plan extends both record types in place)
    - .planning/phases/08-format-foundation/08-PATTERNS.md "RecipeFormatParser.cs (modify — YAML/JSON for new fields)" block ~line 396
    - .planning/phases/08-format-foundation/08-CONTEXT.md §D-27 (YAML wire format: `temperature: { value: 4.5, unit: "gas" }` — never half-stop string)
  </read_first>
  <behavior>
    - YAML wire format gains:
      - Top-level `photoUrl: <string>` and `description: <string>` (omitted when null per YamlDotNet OmitNull config at line 57 of the parser).
      - Per-step `temperature: { value: <decimal>, unit: <"F"|"C"|"gas"> }` (omitted when null).
    - JSON wire format (when callers parse JSON via TryParse): same fields under camelCase keys.
    - The flat DTO ParsedStep gains `Temperature: StepTemperature?` so the RecipeService end-to-end pipeline can populate ContentStep.Temperature from the ParsedRecipe.
    - The flat DTO ParsedRecipe gains `PhotoUrl: string?` and `Description: string?`.
    - Existing v2 YAML/JSON without the new fields still parses successfully (the new fields just stay null).
    - ProjectToParsedRecipe (the inverse projection used by RecipeService writes) populates the new fields from the canonical RecipeDocument.
  </behavior>
  <action>
    Step A — extend the inner `RecipeFrontmatter` class per PATTERNS.md: add `public string? PhotoUrl { get; set; }` and `public string? Description { get; set; }`. YamlDotNet's `ConfigureDefaultValuesHandling(DefaultValuesHandling.OmitNull)` (line ~57) handles backward-compat automatically — null values are simply not written. No new constructor needed.

    Step B — add a new private `TemperatureFrontmatter` class adjacent to `TimerFrontmatter`: `private class TemperatureFrontmatter { public decimal Value { get; set; } public string? Unit { get; set; } }`. Add `public TemperatureFrontmatter? Temperature { get; set; }` on `StepFrontmatter` (only the content-step branch logically uses it; section steps will leave it null).

    Step C — update the YAML serialization path in the existing `Serialize`-style method that builds `RecipeFrontmatter` (PATTERNS.md shows the analog around line 401-414):
      - Set `frontmatter.PhotoUrl = recipe.PhotoUrl;` and `frontmatter.Description = recipe.Description;`.
      - When projecting steps, populate `Temperature = s.Temperature is null ? null : new TemperatureFrontmatter { Value = s.Temperature.Value, Unit = s.Temperature.Unit.ToString().ToLowerInvariant() }`.

    Step D — update the YAML parsing path: deserialize `RecipeFrontmatter`, then map to ParsedRecipe — set `parsedRecipe.PhotoUrl = frontmatter.PhotoUrl; parsedRecipe.Description = frontmatter.Description;`. For each step's TemperatureFrontmatter, build `new StepTemperature { Value = tf.Value, Unit = Enum.Parse<TemperatureUnit>(tf.Unit ?? "F", ignoreCase: true) }` and assign to ParsedStep.Temperature. Catch parse failures of the unit string and log via the existing parser warning mechanism (no throw — RecipeFormatParser never throws per FORMAT-07 invariant from Phase 1 D-08).

    Step E — extend `ProjectToParsedRecipe` (line 252): when projecting RecipeDocument back to ParsedRecipe, set `PhotoUrl = doc.PhotoUrl, Description = doc.Description` and per-step `Temperature = step.Temperature` (the value-type record copies cleanly).

    Step F — extend the `ParsedRecipe` and `ParsedStep` records defined in `src/CookBot.Domain/Interfaces/IRecipeFormatParser.cs` to include `PhotoUrl: string?`, `Description: string?` on `ParsedRecipe`, and `Temperature: StepTemperature?` on `ParsedStep`. Both records live in this single file alongside the `IRecipeFormatParser` interface — extend in place. Add `using CookBot.Domain.Recipes;` to that file if not already present (StepTemperature is in CookBot.Domain.Recipes; ParsedRecipe/ParsedStep live in CookBot.Domain.Interfaces — they are sibling namespaces inside the same Domain assembly so no extra project reference is needed).

    Per CLAUDE.md: NO new YAML library; YamlDotNet 16.3.0 already covers wire serialization. NO Newtonsoft. Per Pattern S5: System.Text.Json only.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/freeloves-cookbot &amp;&amp; dotnet build src/CookBot.Application/CookBot.Application.csproj 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - RecipeFrontmatter carries new fields: `grep -c "string\? PhotoUrl\|string\? Description" src/CookBot.Application/Services/RecipeFormatParser.cs` returns at least 2
    - TemperatureFrontmatter class exists: `grep -c "class TemperatureFrontmatter" src/CookBot.Application/Services/RecipeFormatParser.cs` returns 1
    - StepFrontmatter has Temperature: `grep -c "TemperatureFrontmatter\? Temperature" src/CookBot.Application/Services/RecipeFormatParser.cs` returns at least 1
    - ParsedRecipe carries new fields (extended in IRecipeFormatParser.cs — the canonical home of both records): `grep -c "string\? PhotoUrl\|string\? Description" src/CookBot.Domain/Interfaces/IRecipeFormatParser.cs` returns at least 2
    - ParsedStep carries Temperature (same file): `grep -c "StepTemperature\? Temperature" src/CookBot.Domain/Interfaces/IRecipeFormatParser.cs` returns at least 1
    - Compiles: `dotnet build src/CookBot.Application/CookBot.Application.csproj` exits 0
  </acceptance_criteria>
  <done>YAML and JSON wire formats round-trip the three new fields end-to-end through the parser. ParsedRecipe / ParsedStep (in src/CookBot.Domain/Interfaces/IRecipeFormatParser.cs) carry the v3 data so RecipeService can persist it (consumed in Plan 10's RecipeService update).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: V3 fixtures + round-trip test extensions (SCHEMA-09, SCHEMA-12)</name>
  <files>tests/CookBot.Tests/Recipes/RecipeDocumentRoundTripTests.cs, tests/CookBot.Tests/Services/RecipeFormatParserTests.cs, tests/CookBot.Tests/Fixtures/Recipes/v3-canonical/simple-with-photo.json, tests/CookBot.Tests/Fixtures/Recipes/v3-canonical/sectioned-with-temperature.json, tests/CookBot.Tests/Fixtures/Recipes/v3-canonical/full-v3-all-units.json</files>
  <read_first>
    - tests/CookBot.Tests/Recipes/RecipeDocumentRoundTripTests.cs (the existing round-trip test; PATTERNS.md "Pattern S1" already specifies the MemberData filesystem pattern)
    - tests/CookBot.Tests/Services/RecipeFormatParserTests.cs (audited in Plan 01 — extend; do not delete)
    - tests/CookBot.Tests/Fixtures/Recipes/v2-canonical/*.json (read at least one to confirm fixture shape — match the same outer JSON structure)
    - .planning/phases/08-format-foundation/08-PATTERNS.md "Pattern S1" + "v3-canonical" entry in Created files table
    - .planning/phases/08-format-foundation/08-CONTEXT.md §D-27 (gas wire format: { value: 4.5, unit: "gas" }; the "4½" string only appears in SerializeIndented output)
  </read_first>
  <behavior>
    - Three v3-canonical fixture files cover: simple recipe with photoUrl set; sectioned recipe with one ContentStep carrying temperature in F; full recipe with all three fields populated and three steps each demonstrating a different temperature unit.
    - Each fixture has version=3 in the JSON body.
    - Round-trip test (RecipeDocumentRoundTripTests) gains a new MemberData source for v3-canonical/* and a `[Theory]` asserting structural equality after Deserialize -> Serialize -> Deserialize.
    - Parser tests (RecipeFormatParserTests) gain at least 4 new InlineData rows covering: null value (recipe without temperature still parses), valid F (350), valid C (180), valid Gas half-step (4.5).
    - SerializeIndented round-trip: the Gas half-step fixture, when serialized via SerializeIndented, contains the literal `"½"` glyph in the output (Test the converter).
    - All existing tests still pass — no deletion (SCHEMA-12 / H11).
  </behavior>
  <action>
    Step A — create three v3 fixture files in `tests/CookBot.Tests/Fixtures/Recipes/v3-canonical/`:

    1. `simple-with-photo.json` — minimal v3 recipe with `version: 3, name: "Simple", servings: 2, photoUrl: "https://example.com/photo.jpg", ingredients: [], steps: []`. No description, no per-step temperature.

    2. `sectioned-with-temperature.json` — v3 recipe with one SectionStep ("Prep") and one ContentStep with `temperature: { value: 350, unit: "F" }`. No photoUrl.

    3. `full-v3-all-units.json` — v3 recipe with ALL THREE top-level v3 fields populated AND three ContentSteps each carrying a different temperature unit:
       - Step 1: `temperature: { value: 350, unit: "F" }`
       - Step 2: `temperature: { value: 180, unit: "C" }`
       - Step 3: `temperature: { value: 4.5, unit: "gas" }`  (the gas half-step is the load-bearing case for the converter)

    Each fixture is valid JSON (verify with `jq`). Auto-deploy via existing csproj glob.

    Step B — extend `tests/CookBot.Tests/Recipes/RecipeDocumentRoundTripTests.cs` per Pattern S1: add a new MemberData source `V3CanonicalFixtures()` reading from `Fixtures/Recipes/v3-canonical/*.json` and a new `[Theory]` `[MemberData(nameof(V3CanonicalFixtures))]` `V3Canonical_RoundTripIsIdempotent(string fixtureName, string jsonText)`. Test body: `JsonRecipeSerializer s = new(); var doc = s.Deserialize(jsonText); var roundTripped = s.Deserialize(s.Serialize(doc));` — assert structural equality on each new field (photoUrl, description, per-step temperature).

    Step C — extend `tests/CookBot.Tests/Services/RecipeFormatParserTests.cs` per SCHEMA-12 with at least four new InlineData rows or a new MemberData source covering the three units + null case. Each test parses a YAML or JSON string with the temperature field populated, then asserts ParsedStep.Temperature has the expected Value and Unit. Add one separate `[Fact]` for SerializeIndented gas-half-stop rendering: build a RecipeDocument with one ContentStep carrying `new StepTemperature { Value = 4.5m, Unit = TemperatureUnit.Gas }`, call `serializer.SerializeIndented(doc)`, assert the output contains `"4½"` (verify with `result.Contains("4½")`).

    NO existing test in either file is removed (SCHEMA-12 / H11). Audit footprint comment from Plan 01 stays.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/freeloves-cookbot &amp;&amp; dotnet test tests/CookBot.Tests/CookBot.Tests.csproj --filter "FullyQualifiedName~RecipeDocumentRoundTripTests|FullyQualifiedName~RecipeFormatParserTests" 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - All three fixture files exist: `ls tests/CookBot.Tests/Fixtures/Recipes/v3-canonical/*.json | wc -l` returns 3
    - All fixtures valid JSON: `for f in tests/CookBot.Tests/Fixtures/Recipes/v3-canonical/*.json; do jq . "$f" > /dev/null || echo "INVALID: $f"; done | grep -c INVALID` returns 0
    - All fixtures declare version 3: `for f in tests/CookBot.Tests/Fixtures/Recipes/v3-canonical/*.json; do jq -r '.version' "$f"; done | sort -u` returns only `3`
    - Gas half-step is present in full fixture: `jq -r '.steps[].temperature?.value' tests/CookBot.Tests/Fixtures/Recipes/v3-canonical/full-v3-all-units.json | grep -c "4.5"` returns at least 1
    - V3 round-trip test exists: `grep -c "V3Canonical_RoundTrip\|V3CanonicalFixtures" tests/CookBot.Tests/Recipes/RecipeDocumentRoundTripTests.cs` returns at least 1
    - RecipeFormatParser tests gained v3 temperature coverage (absolute threshold; baseline pre-Plan-05 = 0 occurrences in this file): `grep -Ec "(T|t)emperature" tests/CookBot.Tests/Services/RecipeFormatParserTests.cs` returns at least 4
    - Indented gas-half-stop test exists: `grep -c "SerializeIndented.*[gG]as\|SerializeIndented.*½\|.*½.*SerializeIndented" tests/CookBot.Tests/Services/RecipeFormatParserTests.cs` returns at least 1
    - All tests pass: `dotnet test tests/CookBot.Tests/CookBot.Tests.csproj --filter "FullyQualifiedName~RecipeDocumentRoundTripTests|FullyQualifiedName~RecipeFormatParserTests"` exits 0 with `Failed: 0`
    - No existing test deleted (SCHEMA-12 / H11): the test count in both files is &gt;= the pre-change count
    - Full suite passes: `dotnet test tests/CookBot.Tests/CookBot.Tests.csproj` exits 0
  </acceptance_criteria>
  <done>Phase 8 Success Criterion #3 met: parser tests cover all three new fields with round-trip fixtures for null, valid value, and the three temperature units; no existing test was deleted.</done>
</task>

</tasks>

<verification>
- `dotnet build src/CookBot.Web/CookBot.Web.csproj` exits 0
- `dotnet test tests/CookBot.Tests/CookBot.Tests.csproj --filter "FullyQualifiedName~RecipeFormatParserTests"` exits 0
- `dotnet test tests/CookBot.Tests/CookBot.Tests.csproj --filter "FullyQualifiedName~RecipeDocumentRoundTripTests"` exits 0
- `dotnet test tests/CookBot.Tests/CookBot.Tests.csproj` exits 0 (full suite)
- The three v3 fixtures exist and parse as valid JSON with version=3
</verification>

<success_criteria>
- SCHEMA-08: parser reads/writes v3 fields end-to-end through YAML and JSON.
- SCHEMA-09: serializer round-trips RecipeDocument with all v3 fields cleanly.
- SCHEMA-12: parser tests cover null, valid value, and all three temperature units; no existing test deleted (H11 contract).
- StepTemperatureJsonConverter renders gas half-stops as "4½" only in SerializeIndented; the canonical wire format stays { value, unit } in DB and AI prompt schema.
</success_criteria>

<output>
Create `.planning/phases/08-format-foundation/08-05-SUMMARY.md` when done.
</output>
