---
phase: 08-format-foundation
plan: 11
type: execute
wave: 6
depends_on:
  - 08-08
  - 08-10
files_modified:
  - src/CookBot.Domain/Entities/Recipe.cs
  - src/CookBot.Infrastructure/Data/Configurations/RecipeConfiguration.cs
  - src/CookBot.Application/Services/RecipeService.cs
  - src/CookBot.Infrastructure/Migrations/{timestamp}_DropTagsJsonColumn.cs
autonomous: true
requirements:
  - CLEAN-02
must_haves:
  truths:
    - "Recipe.TagsJson property is removed from the entity"
    - "RecipeConfiguration's HasDefaultValue('[]') line for TagsJson is removed"
    - "RecipeService no longer dual-writes TagsJson — only the relational RecipeTag.Add(...) path remains"
    - "EF migration DropTagsJsonColumn is generated and removes the column from the Recipes table"
    - "All tag-read callsites continue to work because Plan 08 already switched them to relational"
    - "After this plan, grep -E 'TagsJson' src/ returns zero hits in production code (Migrations file may still reference it in Down())"
  artifacts:
    - path: src/CookBot.Infrastructure/Migrations/{timestamp}_DropTagsJsonColumn.cs
      provides: "EF migration that drops the legacy column"
      contains: "DropColumn"
  key_links:
    - from: src/CookBot.Infrastructure/Migrations/{timestamp}_DropTagsJsonColumn.cs
      to: src/CookBot.Domain/Entities/Recipe.cs
      via: "EF model snapshot reflects the property removal"
      pattern: "DropColumn"
---

<objective>
Drop the `Recipe.TagsJson` column per CLEAN-02 finalization (D-26 sequencing — the second migration that lands AFTER Plan 08's callsite switchover and tests pass). Remove the C# property, the configuration default-value clause, and the RecipeService dual-write. Generate the EF migration via `dotnet ef migrations add DropTagsJsonColumn`.

Purpose: Close CLEAN-02 fully. After this plan, the relational `RecipeTag` table is the ONLY source of truth for recipe tags; the legacy JSON column is gone.
Output: Recipe entity edit, configuration edit, RecipeService dual-write removal, one new EF migration. Database backup `cookbot.db.pre-DropTagsJsonColumn.bak` is auto-fired by the Plan 07 backup-label fix.
</objective>

<execution_context>
@.claude/../get-shit-done/workflows/execute-plan.md
@.claude/../get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-format-foundation/08-CONTEXT.md
@.planning/phases/08-format-foundation/08-PATTERNS.md
@src/CookBot.Domain/Entities/Recipe.cs
@src/CookBot.Infrastructure/Data/Configurations/RecipeConfiguration.cs
@src/CookBot.Application/Services/RecipeService.cs
@src/CookBot.Infrastructure/Migrations/20260425223916_RecipeCanonicalDocument.cs
@CLAUDE.md

<interfaces>
From src/CookBot.Domain/Entities/Recipe.cs (current — REMOVE TagsJson):
- public string TagsJson { get; set; } = "[]";  // REMOVE

From src/CookBot.Infrastructure/Data/Configurations/RecipeConfiguration.cs (REMOVE the default-value line):
- builder.Property(r => r.TagsJson).HasDefaultValue("[]");  // REMOVE

From src/CookBot.Application/Services/RecipeService.cs (REMOVE the dual-write to TagsJson):
- Currently in CreateAsync/UpdateAsync (post-Plan-08): TagsJson = JsonSerializer.Serialize(parsed.Tags) AND foreach (var name in parsed.Tags...) recipe.Tags.Add(new RecipeTag { Name = name });
- Plan 11 removes the TagsJson dual-write; only the relational add remains.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove Recipe.TagsJson property + RecipeConfiguration default + RecipeService dual-write (CLEAN-02 finalization)</name>
  <files>src/CookBot.Domain/Entities/Recipe.cs, src/CookBot.Infrastructure/Data/Configurations/RecipeConfiguration.cs, src/CookBot.Application/Services/RecipeService.cs</files>
  <read_first>
    - src/CookBot.Domain/Entities/Recipe.cs (locate and remove the TagsJson property line)
    - src/CookBot.Infrastructure/Data/Configurations/RecipeConfiguration.cs (locate and remove the HasDefaultValue line)
    - src/CookBot.Application/Services/RecipeService.cs (locate the JsonSerializer.Serialize(parsed.Tags) lines; verify Plan 08's relational add is still present BEFORE removing the JSON write)
    - .planning/phases/08-format-foundation/08-PATTERNS.md "DropTagsJsonColumn.cs (create)" block ~line 682 (drop migration shape)
    - .planning/phases/08-format-foundation/08-CONTEXT.md §D-26 (sequencing rationale — column drops in this plan AFTER Plan 08's switchover)
    - tests/CookBot.Tests/* (search for any test that references TagsJson — must be removed or updated to use Tags relational)
  </read_first>
  <behavior>
    - Recipe entity loses the TagsJson property entirely.
    - RecipeConfiguration loses the HasDefaultValue("[]") clause.
    - RecipeService.CreateAsync and UpdateAsync no longer reference TagsJson — only the relational `recipe.Tags.Add(new RecipeTag { Name = name })` path remains.
    - Any test that constructs a Recipe and sets TagsJson is updated to use Tags collection instead — OR deleted if it was a TagsJson-specific test.
    - After this plan, `grep -rE '\.TagsJson|TagsJson\s*=' src/ tests/` returns 0 matches in production code (migration files containing DropColumn("TagsJson") in Up() and AddColumn("TagsJson") in Down() are expected and acceptable references; their pattern is `name: "TagsJson"` not `\.TagsJson`).
  </behavior>
  <action>
    Step A — `src/CookBot.Domain/Entities/Recipe.cs`: delete the line `public string TagsJson { get; set; } = "[]";`. Leave the Tags navigation collection (added in Plan 08) untouched.

    Step B — `src/CookBot.Infrastructure/Data/Configurations/RecipeConfiguration.cs`: delete the line `builder.Property(r => r.TagsJson).HasDefaultValue("[]");`. The HasMaxLength constraints from Plan 07 stay; the FK + Tags navigation configuration from Plan 08 stays.

    Step C — `src/CookBot.Application/Services/RecipeService.cs`: locate the lines in CreateAsync and UpdateAsync that read `TagsJson = JsonSerializer.Serialize(parsed.Tags)` (or assign to recipe.TagsJson) and DELETE them. Leave the relational `foreach (var name in parsed.Tags.Select(t => t.Trim())...) recipe.Tags.Add(new RecipeTag { Name = name });` block from Plan 08 in place — that becomes the sole tag persistence path.

    If `using System.Text.Json;` is no longer needed in RecipeService.cs after the deletion (check whether any other code in the file uses it — likely YES because the canonical doc construction in Plan 10 uses JsonRecipeSerializer which wraps STJ; but the direct STJ usage may be gone), REMOVE the unused using.

    Step D — search tests for TagsJson references: `grep -rE '\.TagsJson|TagsJson\s*=' tests/`. For each hit:
    - If the test seeds a Recipe via TagsJson assignment as a setup convenience, switch to seeding via the Tags navigation collection (`new Recipe { ..., Tags = new List<RecipeTag> { new() { Name = "x" } } }`).
    - If the test specifically validates TagsJson semantics (only one likely candidate: `RecipeTagBackfillTests` from Plan 08 may seed TagsJson directly to test the SQL backfill — that test is testing HISTORICAL data state and is fine; the seed path is a one-time setup, not a behavior assertion). Verify the test still passes — if it requires the TagsJson column to exist, mark it `[Trait("Category", "PreDropMigration")]` or convert to setting Tags directly via the relational table. Read RecipeTagBackfillTests carefully — its purpose is to verify the backfill SQL ran correctly during Plan 08; that backfill happened in production already, so the test's value is regression protection on the SQL itself. Decide: either (a) keep it but seed via raw SQL `INSERT INTO Recipes(..., TagsJson) VALUES (..., '[...]'); INSERT INTO RecipeTags(...) SELECT ...` to simulate a pre-drop DB state, or (b) DELETE the test as obsolete. Prefer (a) — the test still has regression value because the SQL is in the migration history. Document the choice in the SUMMARY.

    Per CLAUDE.md: NO Newtonsoft. Per Pattern S6: file-scoped namespace stays.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/freeloves-cookbot &amp;&amp; dotnet build src/CookBot.Web/CookBot.Web.csproj 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - TagsJson property gone from Recipe entity: `grep -c 'TagsJson' src/CookBot.Domain/Entities/Recipe.cs` returns 0
    - HasDefaultValue line gone: `grep -c 'TagsJson' src/CookBot.Infrastructure/Data/Configurations/RecipeConfiguration.cs` returns 0
    - RecipeService dual-write removed: `grep -c 'TagsJson' src/CookBot.Application/Services/RecipeService.cs` returns 0
    - Relational add still present in RecipeService (from Plan 08): `grep -c 'recipe.Tags.Add(new RecipeTag' src/CookBot.Application/Services/RecipeService.cs` returns at least 2
    - No production-code TagsJson refs remain: `grep -rE '\.TagsJson|TagsJson\s*=' src/ | grep -v 'Migrations/'` returns 0 lines (migration history files may contain `name: "TagsJson"` in DropColumn/AddColumn — those are expected and not flagged by this grep pattern)
    - Tests updated or preserved with raw-SQL seeding: `dotnet test tests/CookBot.Tests/CookBot.Tests.csproj --filter "FullyQualifiedName~RecipeTagBackfillTests"` exits 0 (either the test was reframed via raw SQL or was deleted; either way the suite passes)
    - Build clean: `dotnet build src/CookBot.Web/CookBot.Web.csproj` exits 0
    - Full suite green: `dotnet test tests/CookBot.Tests/CookBot.Tests.csproj` exits 0
  </acceptance_criteria>
  <done>The C# code no longer has any TagsJson references. The EF model now expects the column to be absent — Task 2 generates the migration that physically drops it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generate DropTagsJsonColumn EF migration (CLEAN-02 finalization, D-31 migration #3)</name>
  <files>src/CookBot.Infrastructure/Migrations/{timestamp}_DropTagsJsonColumn.cs</files>
  <read_first>
    - src/CookBot.Infrastructure/Migrations/20260425223916_RecipeCanonicalDocument.cs (Down() method shows the DropColumn shape — analog at PATTERNS.md "DropTagsJsonColumn.cs (create)" block ~line 682)
    - .planning/phases/08-format-foundation/08-CONTEXT.md §D-31 migration #3 + §D-26 (rollback granularity rationale: this migration ships AFTER Plan 08's callsite switchover and tests pass, so reverting it alone restores the column without losing relational data)
    - src/CookBot.Infrastructure/Data/DatabaseSeeder.cs (Plan 07's backup-label fix produces cookbot.db.pre-DropTagsJsonColumn.bak automatically)
    - .planning/phases/08-format-foundation/08-PATTERNS.md "DropTagsJsonColumn.cs (create)" block (full Up/Down spec; HasDefaultValue is dropped automatically when the column goes — no extra code per Discretion note)
  </read_first>
  <behavior>
    - `dotnet ef migrations add DropTagsJsonColumn` generates the migration with Up() = DropColumn("TagsJson", "Recipes") and Down() = AddColumn (re-adds with the original default value for hypothetical rollback completeness; forward-only project policy means Down is for completeness only).
    - The CookBotDbContextModelSnapshot.cs is auto-updated to remove TagsJson from Recipe.
    - On app boot, the migration applies; cookbot.db.pre-DropTagsJsonColumn.bak appears (Plan 07's backup-label fix); the Recipes table no longer has TagsJson; existing data (which lives in RecipeTags now per Plan 08's backfill + Plan 08's dual-write) survives intact.
    - `sqlite3 cookbot.db ".schema Recipes" | grep -c TagsJson` returns 0 after migration.
    - All tests still pass after migration applies.
  </behavior>
  <action>
    Step A — generate the migration:
    ```
    dotnet ef migrations add DropTagsJsonColumn \
      --project src/CookBot.Infrastructure \
      --startup-project src/CookBot.Web
    ```

    Inspect the generated `Up()` body. It MUST contain:
    ```
    migrationBuilder.DropColumn(
        name: "TagsJson",
        table: "Recipes");
    ```

    The `Down()` should symmetrically AddColumn the TagsJson back as `string` non-null with default `"[]"` for completeness — match the EF generator output. Forward-only project policy means Down is unused; do NOT hand-edit it unless the generator drifts.

    Per the PATTERNS.md "Discretion note": the existing HasDefaultValue("[]") constraint is dropped automatically when the column goes — no extra code needed.

    Step B — verify the snapshot updated: `grep -c "TagsJson" src/CookBot.Infrastructure/Migrations/CookBotDbContextModelSnapshot.cs` returns 0 in the Recipe entity block (it MAY appear in older migration files in their fluent column declarations — those are historical and untouchable).

    Step C — boot smoke:
    1. Run `./run.sh` for ~10 seconds.
    2. Verify `cookbot.db.pre-DropTagsJsonColumn.bak` exists relative to the working DB.
    3. Verify `sqlite3 src/CookBot.Web/cookbot.db ".schema Recipes" | grep -c TagsJson` returns 0.
    4. Verify `sqlite3 src/CookBot.Web/cookbot.db "SELECT COUNT(*) FROM RecipeTags"` returns &gt; 0 (data survives because Plan 08 backfilled it).
    5. Stop the app.

    Per CLAUDE.md: NO Newtonsoft. NO new NuGet packages. Forward-only EF migration. EF Core 10.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/freeloves-cookbot &amp;&amp; dotnet ef migrations list --project src/CookBot.Infrastructure --startup-project src/CookBot.Web 2>&amp;1 | grep -c "DropTagsJsonColumn"</automated>
  </verify>
  <acceptance_criteria>
    - Migration file exists: `ls src/CookBot.Infrastructure/Migrations/*_DropTagsJsonColumn.cs | wc -l` returns 1
    - Class name correct: `grep -c 'class DropTagsJsonColumn : Migration' src/CookBot.Infrastructure/Migrations/*_DropTagsJsonColumn.cs` returns 1
    - DropColumn in Up(): `grep -c 'DropColumn' src/CookBot.Infrastructure/Migrations/*_DropTagsJsonColumn.cs` returns at least 1
    - Targets TagsJson on Recipes: `grep -c 'name: "TagsJson"\|table: "Recipes"' src/CookBot.Infrastructure/Migrations/*_DropTagsJsonColumn.cs` returns at least 2
    - Snapshot no longer has TagsJson on Recipe: inspect `src/CookBot.Infrastructure/Migrations/CookBotDbContextModelSnapshot.cs` — the Recipe entity block (after `b =&gt; b.ToTable("Recipes");` style) has no TagsJson Property declaration; confirm via reading the relevant lines
    - Migration listed: `dotnet ef migrations list --project src/CookBot.Infrastructure --startup-project src/CookBot.Web` includes `DropTagsJsonColumn`
    - Build clean: `dotnet build src/CookBot.Web/CookBot.Web.csproj` exits 0
    - Smoke: app boots; backup file with name pre-DropTagsJsonColumn.bak appears; sqlite schema confirms TagsJson is gone; RecipeTags row count is preserved
    - Full test suite passes: `dotnet test tests/CookBot.Tests/CookBot.Tests.csproj` exits 0
  </acceptance_criteria>
  <done>CLEAN-02 fully closed. The relational RecipeTag table is the sole source of truth for recipe tags. Phase 8 success criterion #5 (the TagsJson half) met.</done>
</task>

</tasks>

<verification>
- `dotnet build src/CookBot.Web/CookBot.Web.csproj` exits 0
- `dotnet ef migrations list --project src/CookBot.Infrastructure --startup-project src/CookBot.Web` includes `DropTagsJsonColumn`
- `dotnet test tests/CookBot.Tests/CookBot.Tests.csproj` exits 0
- App boot smoke: `./run.sh` brings the app up; backup file present; sqlite schema confirms TagsJson is dropped; relational tag rows intact
- `grep -rE '\.TagsJson|TagsJson\s*=' src/ | grep -v 'Migrations/' | wc -l` returns 0
</verification>

<success_criteria>
- CLEAN-02 fully closed.
- D-26 sequencing honored: AddRecipeTagTable migration + callsite switchover (Plan 08) shipped first; DropTagsJsonColumn (Plan 11) ships AFTER tests pass — providing rollback granularity per the user's gap-answer.
- D-31 migration #3 of 4 ships with the exact name CONTEXT promises.
- After this plan, the relational table is the only tag store; the JSON column is permanently gone.
</success_criteria>

<output>
Create `.planning/phases/08-format-foundation/08-11-SUMMARY.md` when done.
</output>
