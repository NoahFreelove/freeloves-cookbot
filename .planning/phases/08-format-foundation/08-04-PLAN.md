---
phase: 08-format-foundation
plan: 04
type: execute
wave: 3
depends_on:
  - 08-03
files_modified:
  - src/CookBot.Application/Recipes/Migration_V2_To_V3.cs
  - src/CookBot.Application/Recipes/RecipeUpcasterChain.cs
  - src/CookBot.Application/DependencyInjection.cs
  - tests/CookBot.Tests/Recipes/Migration_V2_To_V3_Tests.cs
  - tests/CookBot.Tests/Fixtures/Recipes/upcaster/v2-to-v3-no-fields.json
  - tests/CookBot.Tests/Fixtures/Recipes/upcaster/v2-to-v3-photo-only.json
  - tests/CookBot.Tests/Fixtures/Recipes/upcaster/v2-to-v3-description-only.json
  - tests/CookBot.Tests/Fixtures/Recipes/upcaster/v2-to-v3-temperature-only.json
  - tests/CookBot.Tests/Fixtures/Recipes/upcaster/v2-to-v3-all-present.json
autonomous: true
requirements:
  - SCHEMA-04
  - SCHEMA-05
must_haves:
  truths:
    - "A v2 RecipeDocument JSON node passed through RecipeUpcasterChain.UpcastToCurrent() comes out with version=3 and no exception, regardless of which combination of new fields are missing/present in the input"
    - "Migration_V2_To_V3 contains three INDEPENDENT null-coalescing guards (per D-29 + PITFALLS C7) — failure of one cannot break the others"
    - "Migration_V2_To_V3 NEVER zero-fills temperature; missing temperature stays absent (PITFALLS M2)"
    - "RecipeUpcasterChain.CurrentVersion = 3 (D-30)"
    - "Chain gap-detection at construction confirms 1->2->3 covers every step (existing assertion in RecipeUpcasterChain ctor)"
  artifacts:
    - path: src/CookBot.Application/Recipes/Migration_V2_To_V3.cs
      provides: "v2 -> v3 upcaster step"
      contains: "Migration_V2_To_V3 : IRecipeUpcaster"
    - path: src/CookBot.Application/Recipes/RecipeUpcasterChain.cs
      provides: "CurrentVersion = 3 constant"
      contains: "CurrentVersion = 3"
    - path: tests/CookBot.Tests/Recipes/Migration_V2_To_V3_Tests.cs
      provides: "Per-field fixture matrix for the upcaster"
  key_links:
    - from: src/CookBot.Application/DependencyInjection.cs
      to: src/CookBot.Application/Recipes/Migration_V2_To_V3.cs
      via: "AddSingleton<IRecipeUpcaster, Migration_V2_To_V3>"
      pattern: "Migration_V2_To_V3"
---

<objective>
Wire the V2 -> V3 upcaster step into the chain per D-29 and D-30. The class is a near-noop because System.Text.Json maps absent JSON keys to nullable C# properties as `null` (so PhotoUrl/Description/Temperature default to null without explicit null-injection); but per D-29 the body still contains three INDEPENDENT per-field guards as documented contract — defending PITFALLS C7 (no bundle-throw) and M2 (no zero-fill). Bumps `RecipeUpcasterChain.CurrentVersion` to 3.

Purpose: Land the upcaster so a v2 cookbook imported into a post-Phase-8 install upcasts to v3 with no data loss and no throw — the Phase 8 Success Criterion #1.
Output: One new application file, two existing application files modified, one new test file, five new fixture JSON files. DI wiring updated.
</objective>

<execution_context>
@.claude/../get-shit-done/workflows/execute-plan.md
@.claude/../get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-format-foundation/08-CONTEXT.md
@.planning/phases/08-format-foundation/08-PATTERNS.md
@.planning/research/PITFALLS.md
@src/CookBot.Application/Recipes/IRecipeUpcaster.cs
@src/CookBot.Application/Recipes/Migration_V1_To_V2.cs
@src/CookBot.Application/Recipes/RecipeUpcasterChain.cs
@src/CookBot.Application/DependencyInjection.cs
@tests/CookBot.Tests/Recipes/RecipeUpcasterTests.cs
@tests/CookBot.Tests/Recipes/RecipeDocumentRoundTripTests.cs
@CLAUDE.md

<interfaces>
<!-- Direct precedent: Migration_V1_To_V2.cs is the closest analog. -->

From src/CookBot.Application/Recipes/IRecipeUpcaster.cs:
- public interface IRecipeUpcaster
  - int FromVersion { get; }
  - int ToVersion { get; }
  - JsonNode Upcast(JsonNode input);

From src/CookBot.Application/Recipes/RecipeUpcasterChain.cs:
- public sealed class RecipeUpcasterChain
  - public const int CurrentVersion = 2;  // BUMP TO 3 in this plan
  - public RecipeUpcasterChain(IEnumerable&lt;IRecipeUpcaster&gt; upcasters)
    - constructor sorts by FromVersion and validates contiguous chain (1-&gt;2-&gt;3 must have no gaps)
  - public JsonNode UpcastToCurrent(JsonNode input)

From src/CookBot.Application/Recipes/Migration_V1_To_V2.cs (full analog):
- public sealed class Migration_V1_To_V2 : IRecipeUpcaster
  - public int FromVersion =&gt; 1;
  - public int ToVersion =&gt; 2;
  - public JsonNode Upcast(JsonNode input) — operates on JsonNode.AsObject(), ends with obj["version"] = 2; return obj;
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Migration_V2_To_V3 + bump CurrentVersion + DI registration (SCHEMA-04, SCHEMA-05, D-29, D-30)</name>
  <files>src/CookBot.Application/Recipes/Migration_V2_To_V3.cs, src/CookBot.Application/Recipes/RecipeUpcasterChain.cs, src/CookBot.Application/DependencyInjection.cs</files>
  <read_first>
    - src/CookBot.Application/Recipes/Migration_V1_To_V2.cs (the precedent — copy structure verbatim per Pattern S6)
    - src/CookBot.Application/Recipes/RecipeUpcasterChain.cs (single-line change per PATTERNS.md)
    - src/CookBot.Application/DependencyInjection.cs (one new AddSingleton line per Pattern S3)
    - .planning/phases/08-format-foundation/08-PATTERNS.md "Migration_V2_To_V3.cs (create)" block (~line 209) and "RecipeUpcasterChain.cs (modify)" block (~line 264) and "DependencyInjection.cs (modify)" block (~line 296)
    - .planning/phases/08-format-foundation/08-CONTEXT.md §D-29 (single class; null-coalescing per field; three independent guards)
    - .planning/phases/08-format-foundation/08-CONTEXT.md §D-30 (Version constant 2 -&gt; 3; CurrentVersion 2 -&gt; 3; reads accept any int &gt;= 1 per Phase 1 D-04)
    - .planning/research/PITFALLS.md C7 (per-field, never bundle-throw) and M2 (never zero-fill temperature)
  </read_first>
  <behavior>
    - Migration_V2_To_V3 implements IRecipeUpcaster with FromVersion=2, ToVersion=3.
    - Upcast(JsonNode) sets obj["version"] = 3 and returns obj.
    - The body contains THREE independent per-field guards (per D-29 contract): one checks photoUrl, one description, one steps[*].temperature. Each guard is a no-op `if (obj["X"] is null) { /* leave absent */ }` style — PITFALLS notes STJ deserialization treats absent keys as null on nullable properties, so explicit JSON null-injection is unnecessary. The guards exist as documented contract per D-29 so future schema additions follow the same per-field pattern.
    - For temperature: walk steps[] (if present); for each step that is a JsonObject lacking a "kind" of "section" (or with kind=="content"), the temperature field is left absent — never set to a default object. PITFALLS M2 prevention.
    - RecipeUpcasterChain.CurrentVersion bumps from 2 to 3 (single line change).
    - DI registration: one line in AddApplication() under the existing Migration_V1_To_V2 registration.
    - Constructor gap-detection still passes (1->2 + 2->3 is contiguous).
  </behavior>
  <action>
    Step A — create `src/CookBot.Application/Recipes/Migration_V2_To_V3.cs` per PATTERNS.md "Migration_V2_To_V3.cs (create)" block. File-scoped namespace `CookBot.Application.Recipes`. `using System.Text.Json.Nodes;` only. Class shape:

    ```
    public sealed class Migration_V2_To_V3 : IRecipeUpcaster
    {
        public int FromVersion => 2;
        public int ToVersion => 3;

        public JsonNode Upcast(JsonNode input)
        {
            var obj = input.AsObject();

            // Guard 1: photoUrl absent => stays absent (deserializes to null). PITFALLS C7 — independent from other guards.
            if (obj["photoUrl"] is null) { /* no-op: STJ maps absent -> null on PhotoUrl: string? */ }

            // Guard 2: description absent => stays absent.
            if (obj["description"] is null) { /* no-op */ }

            // Guard 3: per-step temperature absent => stays absent (NEVER zero-fill — PITFALLS M2).
            if (obj["steps"] is JsonArray steps)
            {
                foreach (var step in steps.OfType<JsonObject>())
                {
                    if (step["kind"]?.GetValue<string>() == "content" && step["temperature"] is null)
                    {
                        // no-op: ContentStep.Temperature is StepTemperature?; STJ maps absent -> null.
                    }
                }
            }

            obj["version"] = 3;
            return obj;
        }
    }
    ```

    XML doc comment summary block matching Migration_V1_To_V2.cs:5-17 style — use `<list type="bullet">` listing the three additions (PhotoUrl, Description, per-step Temperature) and a remarks paragraph noting "all guards are no-ops by design — STJ's absent-key-to-null mapping handles the actual fill; the explicit guards document the per-field contract per Phase 8 D-29 and defend PITFALLS C7 against future modifications that might bundle the additions". Per Pattern S6: file-scoped namespace, sealed class, single public type per file.

    Step B — modify `src/CookBot.Application/Recipes/RecipeUpcasterChain.cs`: change the single line `public const int CurrentVersion = 2;` to `public const int CurrentVersion = 3;`. No other edits. The constructor's gap-detection loop (lines 26-29 in the analog) already validates the chain at construction — when both upcasters are registered, it confirms 1->2->3 is contiguous.

    Step C — modify `src/CookBot.Application/DependencyInjection.cs` per Pattern S3: add one line directly under the existing `services.AddSingleton<IRecipeUpcaster, Migration_V1_To_V2>();`:
    ```
    services.AddSingleton<IRecipeUpcaster, Migration_V2_To_V3>();
    ```
    MS DI multi-registration collects both into the constructor's IEnumerable.

    Per CLAUDE.md / Pattern S5: NO `Newtonsoft.Json`. NO `NJsonSchema`. NO new NuGet package (verify by NOT touching CookBot.Application.csproj).
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/freeloves-cookbot &amp;&amp; dotnet build src/CookBot.Application/CookBot.Application.csproj 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Migration file exists: `test -f src/CookBot.Application/Recipes/Migration_V2_To_V3.cs`
    - Implements IRecipeUpcaster: `grep -c "Migration_V2_To_V3 : IRecipeUpcaster" src/CookBot.Application/Recipes/Migration_V2_To_V3.cs` returns 1
    - FromVersion=2, ToVersion=3: `grep -c "FromVersion =&gt; 2\|ToVersion =&gt; 3" src/CookBot.Application/Recipes/Migration_V2_To_V3.cs` returns 2
    - Three independent guards present: `grep -c 'photoUrl\|description\|temperature' src/CookBot.Application/Recipes/Migration_V2_To_V3.cs` returns at least 3
    - Stamps version=3: `grep -c 'obj\["version"\] = 3' src/CookBot.Application/Recipes/Migration_V2_To_V3.cs` returns 1
    - CurrentVersion bumped: `grep -c "CurrentVersion = 3" src/CookBot.Application/Recipes/RecipeUpcasterChain.cs` returns 1
    - Old constant gone: `grep -c "CurrentVersion = 2" src/CookBot.Application/Recipes/RecipeUpcasterChain.cs` returns 0
    - DI registration: `grep -c "Migration_V2_To_V3" src/CookBot.Application/DependencyInjection.cs` returns 1
    - Both upcaster registrations present: `grep -c "AddSingleton<IRecipeUpcaster" src/CookBot.Application/DependencyInjection.cs` returns 2
    - No Newtonsoft/NJsonSchema added: `grep -c "Newtonsoft\|NJsonSchema" src/CookBot.Application/CookBot.Application.csproj` returns 0
    - Builds: `dotnet build src/CookBot.Application/CookBot.Application.csproj` exits 0
  </acceptance_criteria>
  <done>The chain now covers 1->2->3 with three sorted upcasters. Plan 03's schema-assertion tests still pass. Application layer compiles cleanly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fixture matrix + Migration_V2_To_V3_Tests with per-field assertions (D-29, PITFALLS C7/M2)</name>
  <files>tests/CookBot.Tests/Recipes/Migration_V2_To_V3_Tests.cs, tests/CookBot.Tests/Fixtures/Recipes/upcaster/v2-to-v3-no-fields.json, tests/CookBot.Tests/Fixtures/Recipes/upcaster/v2-to-v3-photo-only.json, tests/CookBot.Tests/Fixtures/Recipes/upcaster/v2-to-v3-description-only.json, tests/CookBot.Tests/Fixtures/Recipes/upcaster/v2-to-v3-temperature-only.json, tests/CookBot.Tests/Fixtures/Recipes/upcaster/v2-to-v3-all-present.json</files>
  <read_first>
    - tests/CookBot.Tests/Recipes/RecipeUpcasterTests.cs (chain-wiring + version-assertion analog — see PATTERNS.md "Migration_V2_To_V3_Tests.cs (create)" excerpt around line 962)
    - tests/CookBot.Tests/Recipes/RecipeDocumentRoundTripTests.cs (MemberData filesystem-fixture pattern; Pattern S1 in PATTERNS.md)
    - .planning/phases/08-format-foundation/08-PATTERNS.md "Migration_V2_To_V3_Tests.cs (create)" block + Pattern S1
    - .planning/phases/08-format-foundation/08-CONTEXT.md §D-29 (per-field test isolation via fixture matrix)
    - tests/CookBot.Tests/CookBot.Tests.csproj (the existing `<None Update="Fixtures\**\*.*" CopyToOutputDirectory="PreserveNewest" />` entry already auto-deploys new fixture files; no csproj edit needed)
    - .planning/research/PITFALLS.md C7 + M2
  </read_first>
  <behavior>
    - The five fixture files cover the missing/present combinations per D-29:
      - `v2-to-v3-no-fields.json`: a minimal v2 recipe with version=2, name, ingredients=[], steps=[] (no photoUrl, no description, no step temperature). Upcast must produce version=3, photoUrl absent (or explicitly null), description absent, every step's temperature absent. NO throw.
      - `v2-to-v3-photo-only.json`: v2 recipe with photoUrl set (yes, in v2 — testing the case where someone hand-edited a v2 doc to add photoUrl ahead of time; upcaster must preserve it).
      - `v2-to-v3-description-only.json`: similar with description present.
      - `v2-to-v3-temperature-only.json`: a v2 recipe with one ContentStep carrying `temperature: { value: 350, unit: "F" }`.
      - `v2-to-v3-all-present.json`: all three fields set.
    - Each test case: load the fixture as JsonNode; run through `RecipeUpcasterChain.UpcastToCurrent`; assert version is 3; assert per-field expectation; assert NO exception.
    - One additional `[Fact]` per PATTERNS.md: chain gap validation still works (constructing a chain missing 2->3 throws InvalidOperationException — this is the existing constructor assertion; verifies the gap-detection regression hasn't broken).
    - One additional `[Fact]`: a v2 recipe with NO temperature data upcasts and `ContentStep.Temperature == null` after final deserialization (PITFALLS C7/M2 explicit guard test).
    - One additional `[Fact]`: a recipe already at version 3 round-trips identity (no upcaster runs because FromVersion never matches).
  </behavior>
  <action>
    Step A — create the five fixture JSON files in `tests/CookBot.Tests/Fixtures/Recipes/upcaster/`. Each is a minimal v2 RecipeDocument JSON shape consistent with existing `v2-canonical/` fixtures (Read one at `tests/CookBot.Tests/Fixtures/Recipes/v2-canonical/*.json` to confirm the exact field shape — `version`, `name`, `servings`, `ingredients`, `steps`). Each fixture is the BEFORE-upcast input (version=2 in the file). Keep them small (one or two ingredients, one or two steps).

    Per Pattern S1, fixture files auto-deploy via the existing `<None Update="Fixtures\**\*.*" CopyToOutputDirectory="PreserveNewest" />` glob in CookBot.Tests.csproj — no csproj edit needed; verify the glob covers `Fixtures/Recipes/upcaster/`.

    Step B — create `tests/CookBot.Tests/Recipes/Migration_V2_To_V3_Tests.cs` per PATTERNS.md "Migration_V2_To_V3_Tests.cs (create)" block. File-scoped namespace `CookBot.Tests.Recipes`. Public class. Combine both Pattern S1 (filesystem MemberData) and the upcaster-fact pattern from RecipeUpcasterTests.

    Helper: `private static RecipeUpcasterChain MakeChain() => new(new IRecipeUpcaster[] { new Migration_V1_To_V2(), new Migration_V2_To_V3() });`.

    `[Theory]` `[MemberData(nameof(V2ToV3Fixtures))]` driven by `Directory.GetFiles(Path.Combine(AppContext.BaseDirectory, "Fixtures", "Recipes", "upcaster"), "v2-to-v3-*.json")` — for each fixture, parse to JsonNode, call chain.UpcastToCurrent, assert `result["version"]!.GetValue<int>() == 3` and assert NO exception was thrown (the test method just running to completion without throwing satisfies this).

    Plus three `[Fact]` methods per the behavior block:
    - `Upcast_NoTemperature_ContentStepTemperatureIsNull` — load v2-to-v3-no-fields.json, upcast, deserialize the result via JsonRecipeSerializer, walk steps, assert all ContentStep.Temperature are null.
    - `Upcast_VersionAlreadyThree_IsIdentity` — start from a doc with version=3, upcast through chain, assert no field changed.
    - `ChainConstructor_ThrowsOnGap` — construct `new RecipeUpcasterChain(new IRecipeUpcaster[] { new Migration_V1_To_V2() /* missing 2->3 */, /* simulate gap */ })`; assert InvalidOperationException is thrown when a deliberate gap is introduced (this guards the existing chain-validation logic — see RecipeUpcasterChain.cs constructor). Note: the existing chain only throws if upcasters are sorted and a gap exists between adjacent entries; if only one upcaster is registered, there's no gap to detect — adapt the test per the actual constructor behavior (read the code in read_first first).

    Per CLAUDE.md: NO Newtonsoft. Per Pattern S5: System.Text.Json everywhere.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/freeloves-cookbot &amp;&amp; dotnet test tests/CookBot.Tests/CookBot.Tests.csproj --filter "FullyQualifiedName~Migration_V2_To_V3_Tests" 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - All five fixture files exist: `ls tests/CookBot.Tests/Fixtures/Recipes/upcaster/v2-to-v3-*.json | wc -l` returns 5
    - Each fixture parses as valid JSON: `for f in tests/CookBot.Tests/Fixtures/Recipes/upcaster/v2-to-v3-*.json; do jq . "$f" > /dev/null || echo "INVALID: $f"; done | grep -c INVALID` returns 0
    - Test file exists: `test -f tests/CookBot.Tests/Recipes/Migration_V2_To_V3_Tests.cs`
    - All Migration_V2_To_V3_Tests pass: `dotnet test tests/CookBot.Tests/CookBot.Tests.csproj --filter "FullyQualifiedName~Migration_V2_To_V3_Tests"` exits 0 with `Failed: 0`
    - Theory + Facts present: `grep -c "\[Theory\]\|\[Fact\]" tests/CookBot.Tests/Recipes/Migration_V2_To_V3_Tests.cs` returns at least 4 (1 Theory + 3 Facts)
    - The C7/M2 guard test exists: `grep -c "ContentStepTemperatureIsNull\|Temperature.*null" tests/CookBot.Tests/Recipes/Migration_V2_To_V3_Tests.cs` returns at least 1
    - All previously-passing tests still pass: `dotnet test tests/CookBot.Tests/CookBot.Tests.csproj` exits 0
  </acceptance_criteria>
  <done>The upcaster has fixture coverage for every per-field combination plus the C7/M2 null-fill guards. Phase 8 Success Criterion #1 is structurally proven.</done>
</task>

</tasks>

<verification>
- `dotnet build src/CookBot.Web/CookBot.Web.csproj` exits 0
- `dotnet test tests/CookBot.Tests/CookBot.Tests.csproj --filter "FullyQualifiedName~Migration_V2_To_V3_Tests"` exits 0
- `dotnet test tests/CookBot.Tests/CookBot.Tests.csproj --filter "FullyQualifiedName~RecipeUpcasterTests"` exits 0 (existing tests still pass after CurrentVersion bump)
- `dotnet test tests/CookBot.Tests/CookBot.Tests.csproj` exits 0 (full suite)
- App boot smoke (Wave 4 will re-run with full guard): `./run.sh` starts and binds http://localhost:7000 with no exception (manual sanity in execution; not blocking)
</verification>

<success_criteria>
- Phase 8 Success Criterion #1 met: a v2 recipe imported through the chain upcasts to v3 with all three new fields null and no throw.
- D-29's three-independent-guards contract is implemented as documented (visible in source, even if no-op due to STJ behavior).
- D-30's CurrentVersion=3 is in place.
- DI registration auto-collects Migration_V2_To_V3 into the chain via Pattern S3.
</success_criteria>

<output>
Create `.planning/phases/08-format-foundation/08-04-SUMMARY.md` when done.
</output>
