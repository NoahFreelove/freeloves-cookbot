---
phase: 08-format-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/CookBot.Tests/Recipes/SchemaAssertionTests.cs
  - tests/CookBot.Tests/Services/RecipeFormatParserTests.cs
autonomous: true
requirements:
  - SCHEMA-11
  - SCHEMA-12
user_setup: []
must_haves:
  truths:
    - "SchemaAssertionTests.cs exists and compiles in CookBot.Tests"
    - "Running the schema-assertion test BEFORE Plan 02-03 produces a RED failure (the new fields do not exist on RecipeDocument yet)"
    - "RecipeFormatParserTests has been audited for string-comparison assertions per PITFALLS H11; any string-equality YAML/JSON comparison is converted to structural equality OR null fields are omitted from serializer output before any v3 work merges"
    - "No existing parser test is deleted (SCHEMA-12 / H11 contract)"
  artifacts:
    - path: tests/CookBot.Tests/Recipes/SchemaAssertionTests.cs
      provides: "JSON schema contract gate for v3 fields"
      contains: "GetSchema_Includes_PhotoUrl_Description_StepTemperature"
    - path: tests/CookBot.Tests/Services/RecipeFormatParserTests.cs
      provides: "Audited round-trip parser tests with structural assertions"
  key_links:
    - from: tests/CookBot.Tests/Recipes/SchemaAssertionTests.cs
      to: src/CookBot.Application/Recipes/RecipeJsonSchemaProvider.cs
      via: "new RecipeJsonSchemaProvider().GetSchema()"
      pattern: "RecipeJsonSchemaProvider\\(\\)\\.GetSchema"
---

<objective>
Land the SCHEMA-11 schema-assertion test FIRST (per PITFALLS C8 / D-35 ordering) and audit the existing RecipeFormatParserTests for string-comparison brittleness (per PITFALLS H11) BEFORE any production code touches RecipeDocument or the upcaster chain. This plan creates the gate that all later plans must keep green.

Purpose: Establish the schema contract as an executable assertion so subsequent plans (02 StepTemperature, 03 RecipeDocument v3, 04 upcaster) cannot silently regress the JSON schema sent to Anthropic. The test starts RED and turns GREEN at the end of Plan 03.
Output: Two test files — one new (SchemaAssertionTests.cs), one audited (RecipeFormatParserTests.cs).
</objective>

<execution_context>
@.claude/../get-shit-done/workflows/execute-plan.md
@.claude/../get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-format-foundation/08-CONTEXT.md
@.planning/phases/08-format-foundation/08-PATTERNS.md
@.planning/research/PITFALLS.md
@src/CookBot.Application/Recipes/RecipeJsonSchemaProvider.cs
@tests/CookBot.Tests/Services/RecipeFormatParserTests.cs
@tests/CookBot.Tests/Recipes/RecipeUpcasterTests.cs
@tests/CookBot.Tests/TestHost.cs
@CLAUDE.md

<interfaces>
<!-- Existing types the test will assert against. Do NOT modify in this plan. -->

From src/CookBot.Application/Recipes/RecipeJsonSchemaProvider.cs:
- public sealed class RecipeJsonSchemaProvider — parameterless constructor; .GetSchema() returns JsonNode
- The schema's top-level "properties" object MUST eventually contain photoUrl + description (added in Plan 03).
- Steps are polymorphic; ContentStep schema lives under an anyOf branch where one branch's "properties" object MUST eventually contain temperature (added in Plan 03).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create SchemaAssertionTests.cs (RED — gates Plans 02-03)</name>
  <files>tests/CookBot.Tests/Recipes/SchemaAssertionTests.cs</files>
  <read_first>
    - tests/CookBot.Tests/Recipes/RecipeUpcasterTests.cs (xUnit Fact + System.Text.Json.Nodes navigation analog)
    - src/CookBot.Application/Recipes/RecipeJsonSchemaProvider.cs (the GetSchema() target — SetAdditionalPropertiesFalse walker around line 52 visits every nested object so the assertions can rely on additionalProperties:false at every depth)
    - .planning/phases/08-format-foundation/08-PATTERNS.md (SchemaAssertionTests block ~line 980)
    - .planning/phases/08-format-foundation/08-CONTEXT.md (D-35 ordering rule; SCHEMA-11 spec)
    - .planning/research/PITFALLS.md (C8 + M3 — alternate field-name regression and nullable-shape regression are the failure modes this test guards against)
  </read_first>
  <behavior>
    - Test 1 (GetSchema_Includes_PhotoUrl_Description): RecipeJsonSchemaProvider().GetSchema().AsObject()["properties"] contains "photoUrl" and "description" keys (currently FAILS — fields not added until Plan 03; that is the intended RED state).
    - Test 2 (GetSchema_StepTemperature_NullableShape): the ContentStep anyOf branch's properties contains "temperature" with a nullable shape per PITFALLS M3 (the type clause includes "null" or the schema wraps it in an anyOf with null) (currently FAILS until Plan 02 + Plan 03).
    - Test 3 (GetSchema_AdditionalPropertiesFalse_OnStepTemperatureSubschema): the temperature subschema (object) carries additionalProperties: false — regression guard for the SetAdditionalPropertiesFalse walker (currently FAILS until Plan 02 + Plan 03).
  </behavior>
  <action>
    Create `tests/CookBot.Tests/Recipes/SchemaAssertionTests.cs` per PATTERNS.md "SchemaAssertionTests.cs (create)" block. File-scoped namespace `CookBot.Tests.Recipes`; one public class `SchemaAssertionTests`; three `[Fact]` methods named per the behaviors above. Each Fact instantiates `new RecipeJsonSchemaProvider()` (no DI — parameterless ctor per PATTERNS S3 / DI registration analog), calls `.GetSchema()`, navigates `JsonNode` via `.AsObject()` / index-by-string per the RecipeUpcasterTests pattern. For Test 2's anyOf navigation: walk `properties["steps"]["items"]["anyOf"]` (or whatever shape `JsonSchemaExporter` produces — assertion lookups must locate the ContentStep branch by checking which anyOf entry has a `properties` object containing `text`, then asserting `temperature` is one of THAT branch's properties). Fail messages must include the actual schema fragment (`schema.ToJsonString()`) for diagnosability when later plans introduce schema regressions.

    Per D-35 / SCHEMA-11 ordering: this test MUST exist and run RED before Plan 02 or 03 ships. After Plan 03 lands, all three tests MUST go green. No code in this plan modifies RecipeJsonSchemaProvider or RecipeDocument. Use `using System.Text.Json.Nodes;` only — no Newtonsoft (CLAUDE.md anti-pattern).

    Implementation note: REGISTER the public class as `internal` is wrong — xUnit needs `public class SchemaAssertionTests` for test discovery (TestHost.cs and RecipeUpcasterTests.cs both use `public`).
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/freeloves-cookbot &amp;&amp; dotnet build tests/CookBot.Tests/CookBot.Tests.csproj 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f tests/CookBot.Tests/Recipes/SchemaAssertionTests.cs &amp;&amp; echo OK`
    - File compiles: `dotnet build tests/CookBot.Tests/CookBot.Tests.csproj` exits 0 with no warnings escalated
    - Source contains the three Fact names: `grep -c "GetSchema_Includes_PhotoUrl_Description\|GetSchema_StepTemperature_NullableShape\|GetSchema_AdditionalPropertiesFalse_OnStepTemperatureSubschema" tests/CookBot.Tests/Recipes/SchemaAssertionTests.cs` returns 3
    - File-scoped namespace: `grep -q "^namespace CookBot.Tests.Recipes;$" tests/CookBot.Tests/Recipes/SchemaAssertionTests.cs`
    - Tests run RED before Plans 02-03 ship: `dotnet test tests/CookBot.Tests/CookBot.Tests.csproj --filter "FullyQualifiedName~SchemaAssertionTests" 2>&amp;1 | grep -E "Failed:.*[1-9]"` returns at least one match (3 failing tests is the intended state at end of Wave 1)
    - No `using Newtonsoft` reference: `grep -c "Newtonsoft" tests/CookBot.Tests/Recipes/SchemaAssertionTests.cs` returns 0
    - public class declaration: `grep -c "^public class SchemaAssertionTests" tests/CookBot.Tests/Recipes/SchemaAssertionTests.cs` returns 1
  </acceptance_criteria>
  <done>SchemaAssertionTests.cs is committed. The file compiles cleanly. Running its three tests fails with assertion errors that name the missing keys (photoUrl, description, temperature) — confirming the RED-before-GREEN ordering D-35 requires.</done>
</task>

<task type="auto">
  <name>Task 2: Audit RecipeFormatParserTests for string-comparison brittleness (PITFALLS H11)</name>
  <files>tests/CookBot.Tests/Services/RecipeFormatParserTests.cs</files>
  <read_first>
    - tests/CookBot.Tests/Services/RecipeFormatParserTests.cs (the file being audited — read in full)
    - .planning/research/PITFALLS.md §H11 (the exact failure mode: round-trip string-comparison assertions break when v3 nullable fields are added)
    - .planning/phases/08-format-foundation/08-CONTEXT.md (SCHEMA-12 spec — every existing test must pass after audit; no test deletion allowed)
    - src/CookBot.Application/Services/JsonRecipeSerializer.cs (confirms WhenWritingNull is already on _compact — null fields should NOT serialize and so should NOT change existing fixtures, but verify and document)
  </read_first>
  <action>
    Audit `tests/CookBot.Tests/Services/RecipeFormatParserTests.cs` per H11 protocol:

    Step 1: Read the file in full and identify any assertion that compares a SERIALIZED YAML or JSON string to an expected string literal (patterns to flag: `Assert.Equal(yaml, expected)`, `Assert.Equal(json, expected)`, `Assert.Equal(serialized, fixtureText)` where the right-hand side is a multi-line literal).

    Step 2: For each flagged assertion:
      (a) If the existing assertion is structural (`Assert.Equal(parsed.Name, "X")`, `Assert.Equal(2, parsed.PrepTimeMinutes)`, etc.), leave it alone.
      (b) If the existing assertion compares serialized output as a string blob, convert it to structural assertions over the round-tripped object (`Assert.Equal(originalDoc.Name, roundTripped.Name)`) — H11's prevention pattern. Do NOT delete the assertion; rewrite it.
      (c) If structural conversion is impossible (e.g., the test specifically guards YAML formatting), confirm `JsonRecipeSerializer._compact` uses `DefaultIgnoreCondition = WhenWritingNull` (line 26) — null v3 fields will NOT appear in serialized output, so existing fixtures stay byte-stable. Add an inline comment `// SCHEMA-12 audit (Plan 08-01): WhenWritingNull keeps v2 fixtures byte-stable when v3 nullable fields land.`

    Step 3: Run `dotnet test --filter "FullyQualifiedName~RecipeFormatParserTests"` BEFORE the audit and AFTER the audit. Both runs MUST be green. The audit is a refactor — behavior is preserved.

    Step 4: If you find ZERO string-comparison assertions (the file is already structural), add a single header comment at the top of the test class: `// SCHEMA-12 audit (Plan 08-01): structural assertions verified; v3 nullable additions in Plans 02-04 will not regress this file.` This documents that the audit was performed.

    Per SCHEMA-12 / H11 / D-35: NO existing test in this file is deleted. NO test is disabled. The audit is observational + defensive. New v3 fixture tests land in Plan 05, not here.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/freeloves-cookbot &amp;&amp; dotnet test tests/CookBot.Tests/CookBot.Tests.csproj --filter "FullyQualifiedName~RecipeFormatParserTests" --no-restore 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - All RecipeFormatParserTests still pass: `dotnet test tests/CookBot.Tests/CookBot.Tests.csproj --filter "FullyQualifiedName~RecipeFormatParserTests"` exits 0 with `Failed: 0`
    - Audit footprint exists: at least one comment line referencing SCHEMA-12: `grep -c "SCHEMA-12 audit" tests/CookBot.Tests/Services/RecipeFormatParserTests.cs` returns at least 1
    - Test count is unchanged or higher (no deletions): compare `grep -c "\[Fact\]\|\[Theory\]" tests/CookBot.Tests/Services/RecipeFormatParserTests.cs` against pre-audit count (record pre-audit count in commit message)
    - No string-comparison assertion of serialized blob remains: `grep -E 'Assert\.Equal\([^,]+,\s*@?"---' tests/CookBot.Tests/Services/RecipeFormatParserTests.cs | grep -v "audit" || echo "PASS"` shows PASS (no multi-line YAML literal compared by Assert.Equal)
  </acceptance_criteria>
  <done>RecipeFormatParserTests.cs has been audited and (if needed) refactored to structural assertions. All existing tests still pass. The file carries an inline SCHEMA-12 audit comment documenting the H11 prevention work for downstream plans.</done>
</task>

</tasks>

<verification>
- `dotnet build src/CookBot.Web/CookBot.Web.csproj` exits 0
- `dotnet build tests/CookBot.Tests/CookBot.Tests.csproj` exits 0
- `dotnet test tests/CookBot.Tests/CookBot.Tests.csproj --filter "FullyQualifiedName~SchemaAssertionTests" 2>&amp;1 | grep -E "Failed:.*[1-9]"` shows failing tests (intentional RED at end of Wave 1)
- `dotnet test tests/CookBot.Tests/CookBot.Tests.csproj --filter "FullyQualifiedName~RecipeFormatParserTests"` exits 0 (audit preserved behavior)
- `test -f tests/CookBot.Tests/Recipes/SchemaAssertionTests.cs`
</verification>

<success_criteria>
- SchemaAssertionTests.cs exists, compiles, and contains all three required Facts
- Running SchemaAssertionTests produces failures naming `photoUrl`, `description`, and step-level `temperature` — RED gate per D-35
- RecipeFormatParserTests has been audited; all existing tests pass; no test is deleted
- Wave 2-5 plans now have an executable contract to verify against (the schema-assertion test must turn GREEN by end of Plan 03; the parser tests must stay GREEN through Plan 05)
</success_criteria>

<output>
Create `.planning/phases/08-format-foundation/08-01-SUMMARY.md` when done.
</output>
