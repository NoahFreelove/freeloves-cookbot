---
phase: 08-format-foundation
plan: 13
type: execute
wave: 7
depends_on:
  - 08-05
  - 08-11
files_modified:
  - README.md
autonomous: true
requirements:
  - CLEAN-04
must_haves:
  truths:
    - "README.md gains a new inline section titled 'Recipe Format' (D-37 — inline, not a separate docs/recipe-format.md)"
    - "Section contains five subsections per D-37: (a) one-paragraph description of RecipeDocument as canonical, (b) YAML wire example with all v3 fields populated, (c) JSON export example for the same recipe (uses SerializeIndented gas half-stop rendering '4½'), (d) V1->V2->V3 upcaster lineage as a markdown bullet list, (e) note that the format is internally-managed and the upcaster chain is forward-only"
    - "The section sits below any 'Install' sections (which Phase 9 will write — for now sits below the existing repo intro)"
    - "Worked example uses the canonical field names photoUrl, description, temperature — never alias tokens (consistent with the SCHEMA-10 denylist contract)"
  artifacts:
    - path: README.md
      provides: "Recipe Format section documenting v3 schema for self-hosters and contributors"
      contains: "## Recipe Format"
  key_links:
    - from: README.md
      to: src/CookBot.Application/Recipes/RecipeSchemaDocumentationProvider.cs
      via: "README example aligns with the AI-prompt schema example for visual parity (PATTERNS.md note)"
      pattern: "photoUrl"
---

<objective>
Add the inline "Recipe Format" section to README.md per CLEAN-04 + D-37. Documents the canonical RecipeDocument v3 structure, the YAML and JSON wire formats, the V1->V2->V3 upcaster lineage, and the internally-managed-format note. Lifts the schema example from RecipeSchemaDocumentationProvider for visual parity per PATTERNS.md.

Purpose: Close CLEAN-04 (FUTURE-V1.1-05 carry-forward). Phase 8 success criterion #5 fully met (the README half).
Output: One README.md edit. No code changes. No tests required (markdown content).
</objective>

<execution_context>
@.claude/../get-shit-done/workflows/execute-plan.md
@.claude/../get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-format-foundation/08-CONTEXT.md
@.planning/phases/08-format-foundation/08-PATTERNS.md
@README.md
@src/CookBot.Application/Recipes/RecipeSchemaDocumentationProvider.cs
@src/CookBot.Application/Recipes/Migration_V1_To_V2.cs
@src/CookBot.Application/Recipes/Migration_V2_To_V3.cs
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add inline 'Recipe Format' section to README.md (CLEAN-04, D-37)</name>
  <files>README.md</files>
  <read_first>
    - README.md (read in full to find the appropriate insertion point — below repo intro / above any future Install section that Phase 9 will write)
    - src/CookBot.Application/Recipes/RecipeSchemaDocumentationProvider.cs (post-Plan-06 state — the schema example prose to lift for visual parity per PATTERNS.md "README.md (modify — add Recipe Format section)" block ~line 1054)
    - src/CookBot.Application/Recipes/Migration_V1_To_V2.cs (V1->V2 lineage description: time-field rename, localId->id, step kind discriminator)
    - src/CookBot.Application/Recipes/Migration_V2_To_V3.cs (V2->V3 lineage description: photoUrl, description, per-step temperature)
    - .planning/phases/08-format-foundation/08-CONTEXT.md §D-37 (full section spec — 5 subsections; inline; below Install)
    - .planning/phases/08-format-foundation/08-PATTERNS.md "README.md (modify — add Recipe Format section)" block ~line 1054
  </read_first>
  <action>
    Per PATTERNS.md "README.md (modify — add Recipe Format section)" block + CONTEXT.md D-37:

    Step A — locate the insertion point in README.md. Per D-37: "Section sits below the existing 'Install' sections (which Phase 9 will write)." For Phase 8: there is no Install section yet, so the new section sits below the repo intro / project description. Read README.md to find the existing top-level structure; insert the new `## Recipe Format` heading immediately after the introduction prose (above any "Active milestone" or planning-related sections, since this is a user/contributor-facing doc).

    Step B — author the section with five subsections per D-37:

    **(a) One-paragraph description.** Two-sentence prose: "FreelovesCookBot stores every recipe in a single canonical format: the `RecipeDocument` C# record (in `CookBot.Domain.Recipes`). YAML, JSON export, and the database column all serialize to the same shape via `JsonRecipeSerializer`; the AI prompt's structured-output schema is generated automatically from the same C# type."

    **(b) YAML wire example with all v3 fields.** A fenced YAML code block (frontmatter form `---\n...\n---\n`) with a worked example. Suggested example:

    ```yaml
    ---
    version: 3
    name: Brown Butter Cookies
    servings: 24
    prepTimeMinutes: 20
    cookTimeMinutes: 12
    photoUrl: https://example.com/cookies.jpg
    description: Crisp-edged, chewy-centered butter cookies.
    tags: [baking, dessert]
    ingredients:
      - id: 1
        name: butter
        amount: 226
        unit: g
      - id: 2
        name: flour
        amount: 250
        unit: g
    steps:
      - kind: section
        heading: Brown the butter
      - kind: content
        text: Melt the [butter](#1) over medium heat until amber and nutty.
      - kind: content
        text: Bake until edges are set.
        temperature:
          value: 375
          unit: F
        timers:
          - duration: 12
            unit: min
            label: bake
    ---
    ```

    **(c) JSON export example.** Same recipe in indented JSON form. Note that gas half-stops render as the human-readable string "4½" in indented export per StepTemperatureJsonConverter (Plan 05) — though the example above uses Fahrenheit. Add a SECOND code block showing a single-step example with a gas half-stop to demonstrate:

    ```json
    {
      "kind": "content",
      "text": "Roast vegetables.",
      "temperature": "4½"
    }
    ```

    Add prose: "In the canonical wire format (database column, AI prompt schema, .cookbook.json export from `Serialize`), gas half-stops are stored as `{ \"value\": 4.5, \"unit\": \"gas\" }`. Only the human-readable indented JSON (`SerializeIndented`) renders them as `\"4½\"` for visual ergonomics."

    **(d) V1->V2->V3 upcaster lineage as a bullet list:**

    - **V1 -> V2** (Migration_V1_To_V2): renames `prepTime` -> `prepTimeMinutes` and `cookTime` -> `cookTimeMinutes`; renames per-ingredient `localId` -> `id`; replaces the `IsSection: true` boolean discriminator with the `kind: "section"` / `kind: "content"` polymorphic discriminator.
    - **V2 -> V3** (Migration_V2_To_V3): introduces `photoUrl` (string?, max 2048), `description` (string?, max 4096), and per-step `temperature` (`{ value, unit }` where unit is `"F"`, `"C"`, or `"gas"`). All three fields default to null on existing v2 documents — per-field null-coalescing per PITFALLS C7.

    **(e) Internally-managed-format note:** "The recipe format is managed internally; users do not need to author YAML or JSON directly. The recipe editor produces canonical-format documents through the chip composer. The upcaster chain is forward-only — `.cookbook.json` files exported from older app versions upcast on import; downgrade is unsupported."

    Step C — verify no SCHEMA-10 alias tokens leaked into the section (consistency with Plan 06's denylist):
    - The README is NOT scanned by PromptDenylistTests (the test reads PromptBuilderService.cs and RecipeSchemaDocumentationProvider.cs only, NOT README.md). However, planner-author hygiene matters — use canonical names (photoUrl, description, temperature) throughout.
    - The literal word "image" may appear in non-field contexts (e.g., "tile photos render via `<img>` fallback") but NOT as a field name. Quick sanity grep: `grep -E '"image"|"imageUrl"|"picture"|"summary"|"desc"|"oven"' README.md` should return 0.

    Step D — preserve the rest of README.md untouched. Only INSERT the new section; do not refactor existing content.

    Per CLAUDE.md: this is a docs-only change; no code, no tests, no NuGet additions.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/freeloves-cookbot &amp;&amp; grep -c "^## Recipe Format" README.md</automated>
  </verify>
  <acceptance_criteria>
    - Section heading present: `grep -c '^## Recipe Format' README.md` returns at least 1
    - All three v3 field names appear in the example: `grep -c 'photoUrl\|description\|temperature' README.md` returns at least 3
    - Worked YAML example present: `grep -c '^---$' README.md` returns at least 2 (open + close of YAML frontmatter)
    - Version 3 declared in example: `grep -c 'version: 3\|"version": 3' README.md` returns at least 1
    - Upcaster lineage mentioned: `grep -c 'V1.*V2.*V3\|Migration_V1_To_V2\|Migration_V2_To_V3' README.md` returns at least 1
    - Both upcasters described: `grep -c 'prepTime\|prepTimeMinutes' README.md` returns at least 1 (V1->V2 description) AND `grep -c 'photoUrl\|temperature' README.md` returns at least 1 (V2->V3 description)
    - Forward-only note: `grep -ic 'forward-only\|upcaster chain' README.md` returns at least 1
    - Gas half-stop callout: `grep -c '½\|"4½"\|gas' README.md` returns at least 1
    - No SCHEMA-10 alias-token leakage in field-name positions: `grep -Ec '"imageUrl"|"picture"|"summary"|"oven"' README.md` returns 0
    - No code regressions (this is a docs change): `dotnet build src/CookBot.Web/CookBot.Web.csproj` exits 0
    - All tests still pass: `dotnet test tests/CookBot.Tests/CookBot.Tests.csproj` exits 0
  </acceptance_criteria>
  <done>CLEAN-04 closed. The README documents the v3 canonical format with a worked example for self-hosters and contributors. Phase 8 success criterion #5 fully met.</done>
</task>

</tasks>

<verification>
- `grep -c '^## Recipe Format' README.md` returns at least 1
- `dotnet build src/CookBot.Web/CookBot.Web.csproj` exits 0 (no regressions)
- `dotnet test tests/CookBot.Tests/CookBot.Tests.csproj` exits 0
- The new section is visually correct when rendered (manual sanity in execution)
</verification>

<success_criteria>
- CLEAN-04 closed.
- D-37 honored: inline section in README.md (not docs/), with all five subsections per spec.
- Worked example uses canonical field names exclusively.
- Phase 8 success criterion #5 fully met (the prompt-snapshot test from Plan 09 + the README section from this plan close out the criterion together).
</success_criteria>

<output>
Create `.planning/phases/08-format-foundation/08-13-SUMMARY.md` when done.
</output>
