---
phase: 08-format-foundation
plan: 10
type: execute
wave: 5
depends_on:
  - 08-08
files_modified:
  - src/CookBot.Infrastructure/Data/DatabaseSeeder.cs
  - src/CookBot.Application/Services/RecipeService.cs
  - src/CookBot.Application/DependencyInjection.cs
  - src/CookBot.Infrastructure/DependencyInjection.cs
  - src/CookBot.Web/Program.cs
  - src/CookBot.Application/Recipes/IRecipeProjector.cs
  - src/CookBot.Infrastructure/Data/Migrations/Helpers/LegacyRecipeProjector.cs
autonomous: true
requirements:
  - CLEAN-01
must_haves:
  truths:
    - "DatabaseSeeder.SeedAsync contains a permanent (per D-33) null-canonical guard that throws InvalidOperationException with row count + remediation hint if any Recipe.CanonicalDocumentJson is null"
    - "The guard runs BEFORE any code that depends on CanonicalDocumentJson and BEFORE projector is removed (D-32 step a sequencing — guard first, then deletion)"
    - "RecipeService no longer takes IRecipeProjector in its constructor; canonical doc construction happens directly from ParsedRecipe via new RecipeDocument { ... } per PATTERNS.md"
    - "DI registrations for IRecipeProjector / LegacyRecipeProjector are removed from BOTH AddApplication() and AddInfrastructure() (depending on which side registered them)"
    - "Both source files (IRecipeProjector.cs and LegacyRecipeProjector.cs) are deleted via git rm"
    - "grep -rE 'LegacyRecipeProjector|IRecipeProjector' src/ (filtered to exclude Migrations/) returns ZERO matches after this plan"
    - "The dual-write to RecipeTag rows from Plan 08 keeps tag persistence working without the projector"
  artifacts:
    - path: src/CookBot.Infrastructure/Data/DatabaseSeeder.cs
      provides: "Permanent null-canonical structural guard"
      contains: "CanonicalDocumentJson"
  key_links:
    - from: src/CookBot.Application/Services/RecipeService.cs
      to: src/CookBot.Domain/Recipes/RecipeDocument.cs
      via: "new RecipeDocument { Version = RecipeUpcasterChain.CurrentVersion, ... } directly from ParsedRecipe"
      pattern: "new RecipeDocument"
---

<objective>
Execute D-32's exact 5-step LegacyRecipeProjector deletion sequence per CLEAN-01: (a) add the permanent null-canonical guard in DatabaseSeeder, (b) replace _projector.Project(recipe) in RecipeService with direct RecipeDocument construction, (c) remove IRecipeProjector ctor param, (d) remove DI registration, (e) delete both source files. Step ordering is load-bearing — the guard must be in place BEFORE the projector is removed so any pre-existing null row fails loud at boot rather than silently downstream.

Purpose: Close CLEAN-01 (FUTURE-V1.1-03 carry-forward; the Phase 1 DELETE-AFTER-V1.1 marker). Phase 8 success criterion #4 met: grep returns zero hits; startup guard fails loud on null.
Output: DatabaseSeeder + RecipeService modified; two DI files updated; Program.cs SeedAsync call signature update; two source files deleted.
</objective>

<execution_context>
@.claude/../get-shit-done/workflows/execute-plan.md
@.claude/../get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-format-foundation/08-CONTEXT.md
@.planning/phases/08-format-foundation/08-PATTERNS.md
@src/CookBot.Infrastructure/Data/DatabaseSeeder.cs
@src/CookBot.Application/Services/RecipeService.cs
@src/CookBot.Application/Recipes/IRecipeProjector.cs
@src/CookBot.Infrastructure/Data/Migrations/Helpers/LegacyRecipeProjector.cs
@src/CookBot.Application/DependencyInjection.cs
@src/CookBot.Infrastructure/DependencyInjection.cs
@src/CookBot.Web/Program.cs
@src/CookBot.Application/Recipes/RecipeUpcasterChain.cs
@src/CookBot.Domain/Recipes/RecipeDocument.cs
@src/CookBot.Domain/Interfaces/IRecipeFormatParser.cs
@CLAUDE.md

<interfaces>
From src/CookBot.Application/Services/RecipeService.cs (current — MUST drop the projector dep):
- public RecipeService(
    IRecipeFormatParser parser,
    IRepository<Recipe> recipeRepo,
    IRepository<Ingredient> ingredientRepo,
    IRepository<Cookbook> cookbookRepo,
    IRecipeProjector projector,            // REMOVE this
    JsonRecipeSerializer canonicalSerializer)
- CreateAsync calls var canonicalDoc = _projector.Project(recipe); recipe.CanonicalDocumentJson = _canonicalSerializer.Serialize(canonicalDoc);

From src/CookBot.Infrastructure/Data/DatabaseSeeder.cs:
- public static async Task SeedAsync(
    CookBotDbContext context,
    IDatabaseBackupService backupService,
    LegacyRecipeProjector projector,        // REMOVE this
    JsonRecipeSerializer serializer,
    string contentRootPath)
- Currently calls BackfillCanonicalDocumentAsync(context, projector, serializer) after MigrateAsync — REMOVE this call (per CONTEXT.md: backfill is a no-op now since all rows have CanonicalDocumentJson from Phase 1's milestone backfill); REMOVE the BackfillCanonicalDocumentAsync helper method

From src/CookBot.Web/Program.cs:
- DatabaseSeeder.SeedAsync(...) call site — must drop the projector argument when DatabaseSeeder's signature changes
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add permanent null-canonical guard in DatabaseSeeder.SeedAsync (D-32 step a; D-33 permanent)</name>
  <files>src/CookBot.Infrastructure/Data/DatabaseSeeder.cs</files>
  <read_first>
    - src/CookBot.Infrastructure/Data/DatabaseSeeder.cs (read in full — note the current SeedAsync signature, the MigrateAsync call, and the BackfillCanonicalDocumentAsync usage)
    - .planning/phases/08-format-foundation/08-PATTERNS.md "DatabaseSeeder.cs (modify — guard + drop projector arg)" block ~line 741 (full guard code; throw style; tone matching Pattern S4)
    - .planning/phases/08-format-foundation/08-CONTEXT.md §D-32 step a + §D-33 (the guard is PERMANENT — not a delete-after-V1.x marker)
    - src/CookBot.Application/Recipes/RecipeUpcasterChain.cs (existing InvalidOperationException tone analog at lines ~26-29 for the gap-detection throw)
  </read_first>
  <behavior>
    - DatabaseSeeder.SeedAsync gains, AFTER MigrateAsync and BEFORE any seed logic / backfill calls, a count query: `var nullCanonicalCount = await context.Recipes.CountAsync(r =&gt; r.CanonicalDocumentJson == null);`.
    - If nullCanonicalCount &gt; 0, throws InvalidOperationException with: row count + concrete remediation hint pointing to the most recent .pre-*.bak file.
    - The guard is permanent (D-33) — no DELETE-AFTER comment.
    - This task does NOT remove the projector argument or BackfillCanonicalDocumentAsync yet — Task 2 handles that. Order matters per D-32 sequencing — guard FIRST, then deletion.
    - On a fresh dev DB (no recipes): nullCanonicalCount == 0, guard passes silently.
    - On a v1.1+ DB with all recipes already backfilled (the expected production state): nullCanonicalCount == 0, guard passes.
    - On a corrupt DB or test DB with a manually-inserted null row: guard throws on boot with the row count and remediation hint.
  </behavior>
  <action>
    Per PATTERNS.md "DatabaseSeeder.cs (modify — guard + drop projector arg)" block: in SeedAsync, AFTER `await context.Database.MigrateAsync();` and BEFORE the existing `await BackfillCanonicalDocumentAsync(context, projector, serializer);` call, insert:

    ```
    // D-32 step a / CLEAN-01 / D-33: permanent structural invariant.
    // Any code path that creates a Recipe without writing CanonicalDocumentJson is a bug
    // and the guard is the load-bearing detection mechanism going forward.
    var nullCanonicalCount = await context.Recipes.CountAsync(r =&gt; r.CanonicalDocumentJson == null);
    if (nullCanonicalCount &gt; 0)
    {
        throw new InvalidOperationException(
            $"{nullCanonicalCount} recipe(s) have null CanonicalDocumentJson after migrate. " +
            "This indicates an incomplete v1.1 backfill — restore from cookbot.db.pre-* backup and re-run.");
    }
    ```

    Apply tone consistent with existing throws in the codebase (Pattern S4 — RecipeUpcasterChain ~line 26-29 throws InvalidOperationException with concise message + remediation context). Per project CONVENTIONS.md (no logging framework in production code), do NOT add an ILogger call.

    Add `using Microsoft.EntityFrameworkCore;` at the top if not already present (CountAsync extension method).

    Do NOT remove the projector argument or BackfillCanonicalDocumentAsync helper in this task. Task 2 owns those deletions per the strict D-32 ordering.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/freeloves-cookbot &amp;&amp; dotnet build src/CookBot.Web/CookBot.Web.csproj 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Guard query present: `grep -c 'CountAsync.*CanonicalDocumentJson == null\|nullCanonicalCount' src/CookBot.Infrastructure/Data/DatabaseSeeder.cs` returns at least 1
    - Throw with row count: `grep -c 'InvalidOperationException.*CanonicalDocumentJson\|null CanonicalDocumentJson after migrate' src/CookBot.Infrastructure/Data/DatabaseSeeder.cs` returns at least 1
    - Remediation hint references backup file: `grep -c 'cookbot.db.pre-\|.bak' src/CookBot.Infrastructure/Data/DatabaseSeeder.cs` returns at least 1
    - Guard runs AFTER MigrateAsync (line-order check by reading): inspect that the line containing nullCanonicalCount appears after the line containing MigrateAsync in the source
    - Permanent — no DELETE-AFTER marker per D-33: `grep -c 'DELETE-AFTER\|TODO.*remove\|TEMPORARY' src/CookBot.Infrastructure/Data/DatabaseSeeder.cs` returns 0 in the new lines (existing comments may remain elsewhere — only assert the guard block has none)
    - Compiles: `dotnet build src/CookBot.Web/CookBot.Web.csproj` exits 0
    - Smoke (dev DB): `./run.sh` starts; no exception (because dev DB recipes all have CanonicalDocumentJson populated from Phase 1 backfill)
  </acceptance_criteria>
  <done>The guard is in place and permanent. Plan 10 Task 2 can now safely remove the projector — any future regression that creates null-canonical rows fails at boot.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-32 steps b-e — replace _projector.Project, drop ctor param, drop DI registration, delete files</name>
  <files>src/CookBot.Application/Services/RecipeService.cs, src/CookBot.Application/DependencyInjection.cs, src/CookBot.Infrastructure/DependencyInjection.cs, src/CookBot.Web/Program.cs, src/CookBot.Infrastructure/Data/DatabaseSeeder.cs, src/CookBot.Application/Recipes/IRecipeProjector.cs, src/CookBot.Infrastructure/Data/Migrations/Helpers/LegacyRecipeProjector.cs</files>
  <read_first>
    - src/CookBot.Application/Services/RecipeService.cs (the constructor + CreateAsync + UpdateAsync — read in full to find every _projector.Project call)
    - src/CookBot.Domain/Interfaces/IRecipeFormatParser.cs (post-Plan-05 state — confirm PhotoUrl, Description, Temperature are present on the ParsedRecipe / ParsedStep records that live in this single file alongside the IRecipeFormatParser interface, so the direct RecipeDocument construction in RecipeService can populate them)
    - src/CookBot.Application/DependencyInjection.cs (drop the IRecipeProjector AddSingleton if present)
    - src/CookBot.Infrastructure/DependencyInjection.cs (drop the LegacyRecipeProjector AddSingleton if present)
    - src/CookBot.Web/Program.cs (the DatabaseSeeder.SeedAsync(...) call site — drop projector argument)
    - src/CookBot.Infrastructure/Data/DatabaseSeeder.cs (drop the LegacyRecipeProjector projector parameter from SeedAsync; delete BackfillCanonicalDocumentAsync helper method)
    - .planning/phases/08-format-foundation/08-PATTERNS.md "RecipeService.cs (modify — drop projector + relational tag writes)" block ~line 474 (FULL replacement code for the canonical-doc construction; uses RecipeUpcasterChain.CurrentVersion which is now 3 from Plan 04)
    - .planning/phases/08-format-foundation/08-CONTEXT.md §D-32 steps b-e (sequencing) + §D-33 (guard is permanent — Task 1 already handled it)
  </read_first>
  <behavior>
    - RecipeService no longer takes IRecipeProjector in its constructor.
    - The `_projector.Project(recipe)` call in CreateAsync (and UpdateAsync if present) is REPLACED with direct construction `var canonicalDoc = new RecipeDocument { Version = RecipeUpcasterChain.CurrentVersion, ... }` populating ALL fields from the ParsedRecipe (which now carries PhotoUrl, Description, and per-step Temperature post-Plan-05).
    - DI registration for IRecipeProjector and LegacyRecipeProjector is removed from BOTH dependency injection extension files (whichever registers the implementation — likely Application registers the interface and Infrastructure registers the concrete projector helper).
    - DatabaseSeeder.SeedAsync signature drops the LegacyRecipeProjector parameter; the BackfillCanonicalDocumentAsync helper method is deleted; Program.cs's call site updated.
    - Both source files are git-removed: src/CookBot.Application/Recipes/IRecipeProjector.cs and src/CookBot.Infrastructure/Data/Migrations/Helpers/LegacyRecipeProjector.cs.
    - `grep -rE 'LegacyRecipeProjector|IRecipeProjector' src/` returns 0 matches.
    - The dual-write to RecipeTag from Plan 08 keeps tag persistence working — the projector's previous tag-projection logic is replaced by reading from `recipe.Tags` directly in the new RecipeDocument construction.
  </behavior>
  <action>
    Step B (D-32 step b) — `src/CookBot.Application/Services/RecipeService.cs` per PATTERNS.md "RecipeService.cs (modify — drop projector + relational tag writes)" block:

    1. Remove `IRecipeProjector projector` parameter from the constructor; drop the `_projector` field.

    2. In CreateAsync (and UpdateAsync if it has the same pattern), find the `var canonicalDoc = _projector.Project(recipe);` line and REPLACE with the direct construction per PATTERNS.md:

    ```
    var canonicalDoc = new RecipeDocument
    {
        Version = RecipeUpcasterChain.CurrentVersion,  // = 3 post-Plan-04
        Name = parsed.Name,
        Servings = parsed.Servings,
        PrepTimeMinutes = parsed.PrepTimeMinutes,
        CookTimeMinutes = parsed.CookTimeMinutes,
        PhotoUrl = parsed.PhotoUrl,                     // Plan 05 added
        Description = parsed.Description,               // Plan 05 added
        Tags = recipe.Tags.Select(t => t.Name).ToList(), // Read from RecipeTag relational rows (Plan 08)
        Ingredients = parsed.Ingredients.Select(i => new IngredientEntry { Id = i.LocalId, Name = i.Name, Amount = i.Amount, Unit = i.Unit, Note = i.Note }).ToList(),
        Steps = parsed.Steps.Select<ParsedStep, StepNode>(s => s.IsSection
            ? new SectionStep { Heading = s.Text }
            : new ContentStep
            {
                Text = s.Text,
                Timers = s.Timers?.Select(t => new TimerEntry { Duration = t.Duration, Unit = t.Unit, Label = t.Label }).ToList(),
                Temperature = s.Temperature  // Plan 05 added
            }).ToList(),
    };
    recipe.CanonicalDocumentJson = _canonicalSerializer.Serialize(canonicalDoc);
    ```

    Verify each property name matches the actual ParsedRecipe / ParsedStep / IngredientEntry / TimerEntry / ContentStep / SectionStep shapes by reading those files. Adjust if PATTERNS.md naming drifts from actual.

    3. Add `using CookBot.Domain.Recipes;` and `using CookBot.Application.Recipes;` if not already present.

    Step C (D-32 step c) — already covered in step B (constructor parameter removed).

    Step D (D-32 step d) — remove the IRecipeProjector / LegacyRecipeProjector DI registrations:
    - In `src/CookBot.Application/DependencyInjection.cs`: delete the `services.AddSingleton<IRecipeProjector, ...>` line if present (or wherever the interface is wired).
    - In `src/CookBot.Infrastructure/DependencyInjection.cs`: delete the `services.AddSingleton<LegacyRecipeProjector>()` line if present (LegacyRecipeProjector is in Infrastructure per the Migrations/Helpers path; the Application-side registration may bind interface-to-impl).
    - The exact registration shape may differ (Singleton vs Scoped; interface-bound vs concrete) — read both DI files first and remove every reference to either type.

    Update DatabaseSeeder.SeedAsync signature to drop the `LegacyRecipeProjector projector` parameter. Delete the `BackfillCanonicalDocumentAsync` helper method (it is now dead code — per CONTEXT.md "the backfill is a no-op now per CONTEXT.md").

    Update `src/CookBot.Web/Program.cs`: the DatabaseSeeder.SeedAsync(...) call site MUST drop the projector argument to match the new signature.

    Step E (D-32 step e) — delete the two source files:
    ```
    git rm src/CookBot.Application/Recipes/IRecipeProjector.cs
    git rm src/CookBot.Infrastructure/Data/Migrations/Helpers/LegacyRecipeProjector.cs
    ```

    If a Phase 1 DELETE-AFTER-V1.1 assertion test exists (PATTERNS.md and CONTEXT.md mention it speculatively in deferred section), delete that test too — it is no longer relevant. Search with: `grep -rE 'LegacyRecipeProjector.*Exists\|FileExists.*LegacyRecipeProjector' tests/`.

    Final check: `grep -rE 'LegacyRecipeProjector|IRecipeProjector' src/ tests/` returns 0 matches.

    Per CLAUDE.md: NO Newtonsoft. NO Microsoft.Extensions.AI. NO new packages added.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/freeloves-cookbot &amp;&amp; dotnet build src/CookBot.Web/CookBot.Web.csproj 2>&amp;1 | tail -10 &amp;&amp; grep -rE 'LegacyRecipeProjector|IRecipeProjector' src/ | grep -v 'Migrations/' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - IRecipeProjector.cs deleted: `test ! -f src/CookBot.Application/Recipes/IRecipeProjector.cs`
    - LegacyRecipeProjector.cs deleted: `test ! -f src/CookBot.Infrastructure/Data/Migrations/Helpers/LegacyRecipeProjector.cs`
    - Zero references in src/ (defensive: filters out Migrations/ in case future EF snapshot churn references the type name): `grep -rE 'LegacyRecipeProjector|IRecipeProjector' src/ | grep -v 'Migrations/' | wc -l` returns 0
    - Zero references in tests/ (assertion test removed if present): `grep -rE 'LegacyRecipeProjector|IRecipeProjector' tests/ | wc -l` returns 0
    - RecipeService no longer takes IRecipeProjector: `grep -c 'IRecipeProjector' src/CookBot.Application/Services/RecipeService.cs` returns 0
    - Direct RecipeDocument construction in RecipeService: `grep -c 'new RecipeDocument' src/CookBot.Application/Services/RecipeService.cs` returns at least 1
    - Uses CurrentVersion: `grep -c 'RecipeUpcasterChain.CurrentVersion' src/CookBot.Application/Services/RecipeService.cs` returns at least 1
    - Populates v3 fields: `grep -c 'PhotoUrl = parsed.PhotoUrl\|Description = parsed.Description\|Temperature = s.Temperature' src/CookBot.Application/Services/RecipeService.cs` returns at least 3
    - Reads tags relationally: `grep -c 'recipe.Tags.Select' src/CookBot.Application/Services/RecipeService.cs` returns at least 1
    - DatabaseSeeder signature updated: `grep -c 'LegacyRecipeProjector projector' src/CookBot.Infrastructure/Data/DatabaseSeeder.cs` returns 0
    - BackfillCanonicalDocumentAsync method removed: `grep -c 'BackfillCanonicalDocumentAsync' src/CookBot.Infrastructure/Data/DatabaseSeeder.cs` returns 0
    - Program.cs call site updated: `grep -c 'LegacyRecipeProjector\|IRecipeProjector' src/CookBot.Web/Program.cs` returns 0
    - DI registrations gone: `grep -c 'AddSingleton<IRecipeProjector\|AddSingleton<LegacyRecipeProjector' src/CookBot.Application/DependencyInjection.cs src/CookBot.Infrastructure/DependencyInjection.cs` returns 0
    - Build clean: `dotnet build src/CookBot.Web/CookBot.Web.csproj` exits 0 (NO errors about missing types)
    - Tests still green: `dotnet test tests/CookBot.Tests/CookBot.Tests.csproj` exits 0
    - Smoke: `./run.sh` brings up the app cleanly; create or edit a recipe via the UI; verify CanonicalDocumentJson is populated for the new/updated recipe via `sqlite3 src/CookBot.Web/cookbot.db "SELECT Name, length(CanonicalDocumentJson) FROM Recipes ORDER BY Id DESC LIMIT 3"` (lengths should be &gt; 0)
  </acceptance_criteria>
  <done>Phase 8 success criterion #4 met. CLEAN-01 closed. The Phase 1 DELETE-AFTER-V1.1 marker is honored. The permanent guard from Task 1 catches future bugs that create null-canonical rows. Wave 4 plan 11 can now safely drop the TagsJson column.</done>
</task>

</tasks>

<verification>
- `dotnet build src/CookBot.Web/CookBot.Web.csproj` exits 0
- `grep -rE 'LegacyRecipeProjector|IRecipeProjector' src/ tests/ | grep -v 'Migrations/' | wc -l` returns 0
- `dotnet test tests/CookBot.Tests/CookBot.Tests.csproj` exits 0
- App boot smoke: `./run.sh` succeeds; null-canonical guard does NOT fire on a clean dev DB
- Manual sanity: editing/creating a recipe produces a non-null CanonicalDocumentJson value
</verification>

<success_criteria>
- CLEAN-01 closed.
- Phase 8 Success Criterion #4: grep returns zero hits; startup guard fails loud on null-canonical rows; the guard is permanent (D-33).
- D-32's exact 5-step sequence executed in the prescribed order — guard FIRST, deletion LAST.
- All tag persistence flows through the relational RecipeTag rows (Plan 08's dual-write means tags survived the projector's removal).
</success_criteria>

<output>
Create `.planning/phases/08-format-foundation/08-10-SUMMARY.md` when done.
</output>
