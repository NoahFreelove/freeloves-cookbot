---
phase: 08-format-foundation
plan: 06
type: execute
wave: 3
depends_on:
  - 08-03
files_modified:
  - tests/CookBot.Tests/Prompts/PromptDenylistTests.cs
  - src/CookBot.Application/Recipes/RecipeSchemaDocumentationProvider.cs
autonomous: true
requirements:
  - SCHEMA-10
must_haves:
  truths:
    - "PromptDenylistTests regex covers the seven SCHEMA-10 alias tokens (image, imageUrl, picture, summary, desc, temp, oven) in addition to the existing opt-out tokens"
    - "The denylist test continues to scan PromptBuilderService.cs and RecipeSchemaDocumentationProvider.cs and PASSES (no false positives against legitimate prose like the literal word \"temperature\" — word-boundary regex \\btemp\\b is precise)"
    - "RecipeSchemaDocumentationProvider's prompt example was updated for v3 (version: 3, photoUrl, description, temperature) AND uses ONLY canonical names — no alias tokens leak into the prose, so the denylist scan stays green"
    - "A negative-path test injects an alias token (e.g., AiSystemPromptTemplate = \"use the imageUrl field\") into a profile and verifies the existing scan would catch it — proving the denylist is self-checking"
  artifacts:
    - path: tests/CookBot.Tests/Prompts/PromptDenylistTests.cs
      provides: "Extended denylist regex covering v3 aliases"
      contains: "imageUrl"
    - path: src/CookBot.Application/Recipes/RecipeSchemaDocumentationProvider.cs
      provides: "v3 schema example with photoUrl / description / temperature"
      contains: "photoUrl"
  key_links:
    - from: tests/CookBot.Tests/Prompts/PromptDenylistTests.cs
      to: src/CookBot.Application/Recipes/RecipeSchemaDocumentationProvider.cs
      via: "Source-file regex scan at test time"
      pattern: "PromptSourceFiles_ContainNoOptOutPhrases"
---

<objective>
Extend the SCHEMA-10 denylist per D-36 to catch AI-emitted alternate field names that bypass the canonical schema. Update RecipeSchemaDocumentationProvider's schema-example prose for v3 (bump version to 3; add photoUrl, description, temperature) using ONLY canonical names. Add one negative-path fixture test that injects an alias token into a synthetic source string and verifies the regex would fire — making the denylist self-checking per CONTEXT.md planner discretion.

Purpose: Close SCHEMA-10. The test scans the actual prompt source files at test time (Phase 1's D-22 mechanism), so any future regression that types "imageUrl" into PromptBuilderService.cs or the schema docs would fail the build.
Output: Two existing files modified. No new files.
</objective>

<execution_context>
@.claude/../get-shit-done/workflows/execute-plan.md
@.claude/../get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-format-foundation/08-CONTEXT.md
@.planning/phases/08-format-foundation/08-PATTERNS.md
@tests/CookBot.Tests/Prompts/PromptDenylistTests.cs
@src/CookBot.Application/Recipes/RecipeSchemaDocumentationProvider.cs
@src/CookBot.Application/Services/PromptBuilderService.cs
@CLAUDE.md

<interfaces>
<!-- Existing test shape (D-22 from Phase 1). -->

From tests/CookBot.Tests/Prompts/PromptDenylistTests.cs:
- public class PromptDenylistTests
  - private static readonly Regex Denylist = new(@"\b(fallback|informal|plain numbered|If you can'?t follow)\b", RegexOptions.IgnoreCase);
  - [Theory] [InlineData("src/CookBot.Application/Services/PromptBuilderService.cs")]
            [InlineData("src/CookBot.Application/Recipes/RecipeSchemaDocumentationProvider.cs")]
  - public void PromptSourceFiles_ContainNoOptOutPhrases(string relativePath) — reads file at TestHost.FindRepoRoot()/relativePath, asserts no Denylist match

From src/CookBot.Application/Recipes/RecipeSchemaDocumentationProvider.cs:
- private const string FormatPrompt = """ ... """; (multi-line raw string with the schema JSON example using "version": 2)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update RecipeSchemaDocumentationProvider for v3 schema example</name>
  <files>src/CookBot.Application/Recipes/RecipeSchemaDocumentationProvider.cs</files>
  <read_first>
    - src/CookBot.Application/Recipes/RecipeSchemaDocumentationProvider.cs (the const string FormatPrompt — read in full to confirm exact JSON example shape)
    - .planning/phases/08-format-foundation/08-PATTERNS.md "RecipeSchemaDocumentationProvider.cs (modify — schema prose example)" block ~line 444 (specifies: bump version 2 -> 3; add photoUrl + description at top-object level; extend ONE content step with temperature)
    - .planning/phases/08-format-foundation/08-CONTEXT.md §D-36 (denylist stays — the prose example must not introduce alias tokens that would fail the scan)
    - tests/CookBot.Tests/Prompts/PromptDenylistTests.cs (the regex this prose must avoid matching)
  </read_first>
  <behavior>
    - The schema example in FormatPrompt declares version: 3.
    - The example object includes "photoUrl" and "description" fields at the top level (canonical names — never image/imageUrl/picture/summary/desc).
    - One ContentStep example carries temperature: { "value": 375, "unit": "F" } (canonical wire form per D-27 — never "temp" or "oven").
    - The closing strict-mode directive ("If you cannot emit a recipe in the structured format, ask the user a clarifying question instead.") is preserved exactly — D-22 prohibits opt-out language and the existing prose is the canonical replacement directive.
    - Denylist test continues to pass after this edit (the prose contains the WORD "temperature" which contains the substring "temp", but the denylist uses \btemp\b with word boundaries so "temperature" does not match).
  </behavior>
  <action>
    Per PATTERNS.md "RecipeSchemaDocumentationProvider.cs (modify — schema prose example)" block:

    1. Locate the multi-line const string `FormatPrompt`. Bump the literal `"version": 2` to `"version": 3`.

    2. After the top-level "name" field in the example object, add two new lines:
       - `"photoUrl": "https://example.com/photo.jpg",`
       - `"description": "A short summary of the dish.",`
       Keep adjacent to other top-level fields per the existing JSON example layout.

    3. In one of the existing content-step examples, add `"temperature": { "value": 375, "unit": "F" }` as a sibling of `"text"` and `"timers"`. Choose the step that has timers as the demonstration site (consolidates the "step has cooking parameters" examples).

    4. Do NOT introduce ANY alias token. The full denylist for this plan covers: `image`, `imageUrl`, `picture`, `summary`, `desc`, `temp`, `oven`. The prose must use ONLY `photoUrl`, `description`, `temperature`. Verify post-edit by searching the file for any of those alias tokens with word-boundary precision: `grep -E "\\b(image|imageUrl|picture|summary|desc|temp|oven)\\b" src/CookBot.Application/Recipes/RecipeSchemaDocumentationProvider.cs` returns 0 matches (the literal "temperature" should not match `\btemp\b` because there is no word boundary after "temp" in "temperature").

    5. Preserve the closing strict-mode directive exactly as it stands. Per D-22 / D-36: do NOT reintroduce opt-out language ("plain numbered steps", "informal", "fallback", "If you can't follow").

    Per Pattern S5/S6: file-scoped namespace stays; no new usings; System.Text.Json only (no Newtonsoft).
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/freeloves-cookbot &amp;&amp; dotnet build src/CookBot.Application/CookBot.Application.csproj 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Version bumped to 3 in prose: `grep -c '"version": 3' src/CookBot.Application/Recipes/RecipeSchemaDocumentationProvider.cs` returns at least 1
    - Old version literal gone: `grep -c '"version": 2' src/CookBot.Application/Recipes/RecipeSchemaDocumentationProvider.cs` returns 0
    - photoUrl example present: `grep -c 'photoUrl' src/CookBot.Application/Recipes/RecipeSchemaDocumentationProvider.cs` returns at least 1
    - description example present: `grep -c '"description"' src/CookBot.Application/Recipes/RecipeSchemaDocumentationProvider.cs` returns at least 1
    - temperature example present: `grep -c '"temperature"' src/CookBot.Application/Recipes/RecipeSchemaDocumentationProvider.cs` returns at least 1
    - No alias token leakage (word-boundary test): `grep -Ec '\b(imageUrl|picture|summary|oven)\b' src/CookBot.Application/Recipes/RecipeSchemaDocumentationProvider.cs` returns 0
    - Strict-mode directive preserved: `grep -c "ask the user a clarifying question" src/CookBot.Application/Recipes/RecipeSchemaDocumentationProvider.cs` returns at least 1
    - No opt-out tokens: `grep -Ei '\b(fallback|informal|plain numbered)\b' src/CookBot.Application/Recipes/RecipeSchemaDocumentationProvider.cs` returns 0
    - Compiles: `dotnet build src/CookBot.Application/CookBot.Application.csproj` exits 0
  </acceptance_criteria>
  <done>The AI prompt schema example demonstrates v3 with the canonical field names. No alias token leaked into the prose.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend PromptDenylistTests regex + add self-check negative-path test (SCHEMA-10, D-36 + Discretion)</name>
  <files>tests/CookBot.Tests/Prompts/PromptDenylistTests.cs</files>
  <read_first>
    - tests/CookBot.Tests/Prompts/PromptDenylistTests.cs (existing test — extend regex, do not replace structure)
    - .planning/phases/08-format-foundation/08-PATTERNS.md "PromptDenylistTests.cs (modify — extend regex)" block ~line 894 (full replacement regex spec; word-boundary precision caveat)
    - .planning/phases/08-format-foundation/08-CONTEXT.md §D-36 (denylist stays + extends with the seven alias tokens) and §"Claude's Discretion" final bullet (one negative-path test recommended — keeps denylist self-checking)
    - src/CookBot.Application/Recipes/RecipeSchemaDocumentationProvider.cs (post-Task-1 state — verify it's clean)
    - src/CookBot.Application/Services/PromptBuilderService.cs (the second file the existing scan covers — verify nothing alias-shaped leaked here either)
  </read_first>
  <behavior>
    - Denylist regex extends to cover SCHEMA-10 alias tokens: `image`, `imageUrl`, `picture`, `summary`, `desc`, `temp`, `oven` (in addition to the existing tokens).
    - Word boundaries (`\b`) are used precisely so legitimate prose ("temperature", "image-related comment") does not false-positive on bare substrings. The literal C# word "temperature" inside FormatPrompt MUST NOT match `\btemp\b` (no word boundary after "temp" mid-word).
    - The PromptSourceFiles_ContainNoOptOutPhrases Theory continues to pass against PromptBuilderService.cs and RecipeSchemaDocumentationProvider.cs.
    - A new negative-path Fact `Denylist_FiresOn_AliasToken_InSyntheticInput` constructs a synthetic source string containing `"use the imageUrl field"` and asserts the Denylist regex matches — proving the regex would catch a regression if someone typed an alias into the prompt source.
  </behavior>
  <action>
    Step A — replace the `Denylist` regex literal per PATTERNS.md "PromptDenylistTests.cs (modify — extend regex)" block:

    Old: `new(@"\b(fallback|informal|plain numbered|If you can'?t follow)\b", RegexOptions.IgnoreCase);`

    New: `new(@"\b(fallback|informal|plain numbered|If you can'?t follow|image|imageUrl|picture|summary|desc|temp|oven)\b", RegexOptions.IgnoreCase);`

    Per PATTERNS.md caveat: `\btemp\b` excludes "temperature" because "temperature" has no word boundary right after "temp" — the next character "e" is a word character. Verify by writing a quick mental test: "temperature".Contains("temp") is true, but Regex.IsMatch("temperature", `\btemp\b`) is false. Confirm via Task 2 acceptance criteria.

    Note on `image|imageUrl`: `imageUrl` contains `image` as a prefix — the regex will match either against `image` first because of regex left-to-right alternation, but both halt the same way (assertion fires). Order doesn't matter for the failing-test outcome.

    Step B — add a new `[Fact]` named `Denylist_FiresOn_AliasToken_InSyntheticInput`:

    Body: build a small synthetic string `"// dev note: use the imageUrl field instead of photoUrl"` (or similar — must contain at least one alias token in a context that exercises word boundaries). Assert `Assert.True(PromptDenylistTests.Denylist.Matches(synthetic).Count > 0, "Denylist regex must catch alias tokens in source");`. To access the static field cleanly, either (a) make the regex internal-not-private, or (b) duplicate the regex literal in the test for readability. Prefer (a) per Pattern S6 (file-scoped namespace allows internal-but-visible-to-tests via the same assembly).

    Add a second negative assertion: confirm "temperature" alone does NOT match (`Assert.False(PromptDenylistTests.Denylist.Matches("the oven temperature is 350").Count > 0)` — wait, "oven" IS in the denylist; pick a synthetic that excludes alias substrings. Try `"the temperature is 350"` — both "temperature" and "is" should not match any alias). This protects against the planner accidentally weakening the regex (e.g., dropping `\b`).

    Step C — re-run both Theory rows to confirm the production source files PASS the extended scan. PromptBuilderService.cs and RecipeSchemaDocumentationProvider.cs (post-Task-1) must contain ZERO matches of the new regex. If a match surfaces, fix the offending source line — don't relax the regex.

    Per CLAUDE.md: NO Newtonsoft. xUnit 2.9.2.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/freeloves-cookbot &amp;&amp; dotnet test tests/CookBot.Tests/CookBot.Tests.csproj --filter "FullyQualifiedName~PromptDenylistTests" 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Regex contains the seven alias tokens: `grep -E "image\|imageUrl\|picture\|summary\|desc\|temp\|oven" tests/CookBot.Tests/Prompts/PromptDenylistTests.cs | wc -l` returns at least 1 line containing all seven
    - Word-boundary anchors preserved: `grep -c '\\\\b' tests/CookBot.Tests/Prompts/PromptDenylistTests.cs` returns at least 2 (opening and closing \b)
    - Negative-path Fact exists: `grep -c "Denylist_FiresOn_AliasToken_InSyntheticInput\|FiresOn_AliasToken" tests/CookBot.Tests/Prompts/PromptDenylistTests.cs` returns at least 1
    - All denylist tests pass: `dotnet test tests/CookBot.Tests/CookBot.Tests.csproj --filter "FullyQualifiedName~PromptDenylistTests"` exits 0 with `Failed: 0` and at least 3 passing (2 existing Theory rows + 1 new Fact)
    - Both production source files pass the scan: `dotnet test --filter "PromptSourceFiles_ContainNoOptOutPhrases"` exits 0 (Theory passes both InlineData rows)
    - Verify "temperature" does not match: paste this into a quick C# repl mentally — `Regex.IsMatch("temperature", @"\btemp\b", RegexOptions.IgnoreCase)` returns false. (Sanity: `dotnet test` running PromptSourceFiles_ContainNoOptOutPhrases will catch this regression on the source file containing "temperature".)
    - Full suite still green: `dotnet test tests/CookBot.Tests/CookBot.Tests.csproj` exits 0
  </acceptance_criteria>
  <done>SCHEMA-10 closed. The denylist self-checks via the negative-path Fact and continues to scan production source files for canonical-name violations.</done>
</task>

</tasks>

<verification>
- `dotnet build src/CookBot.Application/CookBot.Application.csproj` exits 0
- `dotnet test tests/CookBot.Tests/CookBot.Tests.csproj --filter "FullyQualifiedName~PromptDenylistTests"` exits 0
- `dotnet test tests/CookBot.Tests/CookBot.Tests.csproj` exits 0 (no regressions)
- The denylist regex literal contains all seven SCHEMA-10 alias tokens with word boundaries
</verification>

<success_criteria>
- SCHEMA-10 closed: denylist regex catches `image`, `imageUrl`, `picture`, `summary`, `desc`, `temp`, `oven` as alias tokens in any future prompt source change.
- D-36 honored: the existing PromptDenylistTests structure is preserved; only the regex literal grew.
- Self-checking negative-path Fact proves the regex actually fires.
- RecipeSchemaDocumentationProvider's v3 schema example uses canonical names exclusively.
</success_criteria>

<output>
Create `.planning/phases/08-format-foundation/08-06-SUMMARY.md` when done.
</output>
