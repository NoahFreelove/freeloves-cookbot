---
phase: 08-format-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 08-02
files_modified:
  - src/CookBot.Domain/Recipes/RecipeDocument.cs
  - src/CookBot.Domain/Recipes/StepNode.cs
  - src/CookBot.Application/Recipes/RecipeValidator.cs
  - tests/CookBot.Tests/Recipes/RecipeValidatorTests.cs
autonomous: true
requirements:
  - SCHEMA-01
  - SCHEMA-02
  - SCHEMA-06
  - SCHEMA-07
must_haves:
  truths:
    - "RecipeDocument has nullable PhotoUrl (string?, max 2048) and nullable Description (string?, max 4096) properties"
    - "ContentStep has nullable Temperature (StepTemperature?) property"
    - "RecipeJsonSchemaProvider regenerates without manual edits — SCHEMA-07 satisfied by construction"
    - "After this plan ships, all three SchemaAssertionTests from Plan 01 turn GREEN"
    - "RecipeValidator surfaces INVALID_TEMPERATURE errors per D-27 rules: F/C must be whole-degree; Gas must be 0.5-step in [1.0, 9.5]"
  artifacts:
    - path: src/CookBot.Domain/Recipes/RecipeDocument.cs
      provides: "v3 nullable fields PhotoUrl + Description"
      contains: "PhotoUrl"
    - path: src/CookBot.Domain/Recipes/StepNode.cs
      provides: "ContentStep.Temperature nullable property"
      contains: "StepTemperature"
    - path: src/CookBot.Application/Recipes/RecipeValidator.cs
      provides: "Per-unit temperature validation rules"
      contains: "INVALID_TEMPERATURE"
  key_links:
    - from: src/CookBot.Domain/Recipes/StepNode.cs
      to: src/CookBot.Domain/Recipes/StepTemperature.cs
      via: "ContentStep.Temperature: StepTemperature? property"
      pattern: "StepTemperature\\?"
    - from: src/CookBot.Application/Recipes/RecipeJsonSchemaProvider.cs
      to: src/CookBot.Domain/Recipes/RecipeDocument.cs
      via: "JsonSchemaExporter reflects new properties at first GetSchema() call"
      pattern: "JsonSchemaExporter"
---

<objective>
Add the three v3 nullable fields to the canonical record (`RecipeDocument.PhotoUrl`, `RecipeDocument.Description`, `ContentStep.Temperature`) per D-28, leaving the upcaster bump and Version constant change for Plan 04. Add per-unit temperature validation rules to `RecipeValidator` per D-27. After this plan ships, the SchemaAssertionTests from Plan 01 turn GREEN — proving SCHEMA-07 (no manual schema editing) holds by construction because `RecipeJsonSchemaProvider` regenerates automatically against the type.

Purpose: Land the type-system change for v3 fields. This is the keystone Wave 1 plan — the code change is small but its correctness gates Plans 04 (upcaster), 05 (parser/serializer), and 06 (denylist). The nullable-by-construction shape directly defends PITFALLS C7 / M2 (no zero-fill).
Output: Two domain files modified, one application file modified, one test file modified. No new files.
</objective>

<execution_context>
@.claude/../get-shit-done/workflows/execute-plan.md
@.claude/../get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-format-foundation/08-CONTEXT.md
@.planning/phases/08-format-foundation/08-PATTERNS.md
@.planning/research/PITFALLS.md
@src/CookBot.Domain/Recipes/RecipeDocument.cs
@src/CookBot.Domain/Recipes/StepNode.cs
@src/CookBot.Domain/Recipes/StepTemperature.cs
@src/CookBot.Application/Recipes/RecipeValidator.cs
@src/CookBot.Application/Recipes/RecipeJsonSchemaProvider.cs
@tests/CookBot.Tests/Recipes/RecipeValidatorTests.cs
@tests/CookBot.Tests/Recipes/SchemaAssertionTests.cs
@CLAUDE.md

<interfaces>
<!-- Existing public surfaces being extended in this plan. -->

From src/CookBot.Domain/Recipes/RecipeDocument.cs (v2 shape — extend in place; do not restructure):
- public sealed record RecipeDocument
  - [JsonPropertyName("version")] public required int Version { get; init; }
  - [JsonPropertyName("name")] public required string Name { get; init; }
  - ... existing PrepTimeMinutes, CookTimeMinutes, Tags, Ingredients, Steps ...
  - [JsonExtensionData] public Dictionary&lt;string, JsonElement&gt; Extras { get; init; } = new();

From src/CookBot.Domain/Recipes/StepNode.cs:
- public sealed record ContentStep : StepNode
  - [JsonPropertyName("text")] public required string Text { get; init; }
  - [JsonPropertyName("timers")] public IReadOnlyList&lt;TimerEntry&gt;? Timers { get; init; }
  - [JsonExtensionData] public Dictionary&lt;string, JsonElement&gt; Extras { get; init; } = new();

From src/CookBot.Domain/Recipes/StepTemperature.cs (created in Plan 02):
- public sealed record StepTemperature
  - public required decimal Value { get; init; }
  - public required TemperatureUnit Unit { get; init; }
- public enum TemperatureUnit { F, C, Gas }

From src/CookBot.Application/Recipes/RecipeValidator.cs:
- public ValidationResult Validate(RecipeDocument doc) — never throws, returns result with Errors list
- Error code patterns already in use: REQUIRED, OUT_OF_RANGE, DUPLICATE_ID, DANGLING_REF
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add PhotoUrl + Description to RecipeDocument (SCHEMA-01, SCHEMA-02, SCHEMA-06)</name>
  <files>src/CookBot.Domain/Recipes/RecipeDocument.cs</files>
  <read_first>
    - src/CookBot.Domain/Recipes/RecipeDocument.cs (the file being extended — read in full to confirm property ordering)
    - .planning/phases/08-format-foundation/08-PATTERNS.md "RecipeDocument.cs (modify)" block (~line 103 — explicit insertion point: after CookTimeMinutes, before Tags)
    - .planning/phases/08-format-foundation/08-CONTEXT.md §D-28 (max 2048 / 4096 chars; planner discretion to add MaxLength attributes for AI schema propagation)
    - .planning/research/PITFALLS.md C7 (null-coalescing per field; never bundle-throw)
  </read_first>
  <behavior>
    - RecipeDocument gains two nullable string properties: PhotoUrl (max 2048) and Description (max 4096).
    - Both default to null. Existing v2 fixtures that lack these keys deserialize to null without error.
    - JsonRecipeSerializer's _compact serializer uses WhenWritingNull (verified in Plan 01 audit) — null values do NOT appear in serialized output, so v2 round-trip stays byte-stable.
    - The Version constant stays at 2 in this plan; Plan 04 bumps RecipeUpcasterChain.CurrentVersion to 3 and writes 3 into upcasted documents.
  </behavior>
  <action>
    Per PATTERNS.md "RecipeDocument.cs (modify)" block: insert two new properties after `CookTimeMinutes`, before `Tags` (preserve the existing nullable-scalar group ordering):

    ```
    [JsonPropertyName("photoUrl")]
    [System.ComponentModel.DataAnnotations.MaxLength(2048)]
    public string? PhotoUrl { get; init; }

    [JsonPropertyName("description")]
    [System.ComponentModel.DataAnnotations.MaxLength(4096)]
    public string? Description { get; init; }
    ```

    The `[MaxLength(...)]` attributes are the planner discretion call from D-28 — System.Text.Json.Schema.JsonSchemaExporter honors `[MaxLength]` and surfaces `"maxLength": N` to Anthropic's structured-output schema, improving AI compliance with no functional cost. CookBot.Domain already uses System.ComponentModel.DataAnnotations attributes (verify with `grep -r "DataAnnotations" src/CookBot.Domain/`); if not, add `using System.ComponentModel.DataAnnotations;` at the top.

    Per CLAUDE.md / Pattern S5: no `[JsonProperty]`, no Newtonsoft. Per Pattern S6: file-scoped namespace already in place — preserve.

    DO NOT touch the `Version` constant or the `Version` property default in this task. DO NOT add the property to `Extras`. DO NOT restructure existing properties. DO NOT modify the `[JsonExtensionData]` Extras dictionary at the bottom of the record.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/freeloves-cookbot &amp;&amp; dotnet build src/CookBot.Domain/CookBot.Domain.csproj 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Both properties present: `grep -c "string? PhotoUrl\|string? Description" src/CookBot.Domain/Recipes/RecipeDocument.cs` returns 2
    - JsonPropertyName attributes present: `grep -c 'JsonPropertyName("photoUrl")\|JsonPropertyName("description")' src/CookBot.Domain/Recipes/RecipeDocument.cs` returns 2
    - MaxLength attributes present: `grep -c "MaxLength(2048)\|MaxLength(4096)" src/CookBot.Domain/Recipes/RecipeDocument.cs` returns 2
    - Compiles: `dotnet build src/CookBot.Domain/CookBot.Domain.csproj` exits 0
    - Version property unchanged (still required int): `grep -c "public required int Version" src/CookBot.Domain/Recipes/RecipeDocument.cs` returns 1
  </acceptance_criteria>
  <done>RecipeDocument carries the two new v3 nullable fields. Domain compiles. Plan 01's `GetSchema_Includes_PhotoUrl_Description` Fact will turn GREEN once Tasks 2-3 also land and CookBot.Application rebuilds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Temperature to ContentStep (SCHEMA-03)</name>
  <files>src/CookBot.Domain/Recipes/StepNode.cs</files>
  <read_first>
    - src/CookBot.Domain/Recipes/StepNode.cs (the file being extended)
    - src/CookBot.Domain/Recipes/StepTemperature.cs (Plan 02 output — confirms type is available)
    - .planning/phases/08-format-foundation/08-PATTERNS.md "StepNode.cs (modify)" block (~line 132 — insertion point: between Timers and Extras)
    - .planning/research/PITFALLS.md C7 + M2 (NEVER zero-fill; nullable-only)
  </read_first>
  <behavior>
    - ContentStep gains a `Temperature: StepTemperature?` property (nullable).
    - SectionStep is NOT modified — sections never carry temperature (per PATTERNS.md note).
    - Existing v2 fixtures without `temperature:` deserialize with `Temperature == null` per STJ default behavior.
  </behavior>
  <action>
    Per PATTERNS.md "StepNode.cs (modify)" block: insert one property in `ContentStep` between `Timers` and `Extras`:

    ```
    [JsonPropertyName("temperature")]
    public StepTemperature? Temperature { get; init; }
    ```

    Per PITFALLS C7/M2: the type is `StepTemperature?` (nullable). Default value is `null` (no `= new(...)` initializer). Do NOT add the property to SectionStep.

    No other changes to StepNode.cs. The polymorphic `[JsonPolymorphic]` discriminator stays.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/freeloves-cookbot &amp;&amp; dotnet build src/CookBot.Domain/CookBot.Domain.csproj 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Property present in ContentStep (not SectionStep): `grep -c "StepTemperature? Temperature" src/CookBot.Domain/Recipes/StepNode.cs` returns 1
    - JsonPropertyName present: `grep -c 'JsonPropertyName("temperature")' src/CookBot.Domain/Recipes/StepNode.cs` returns 1
    - Property is nullable (defends C7/M2): `grep -c "StepTemperature[^?]" src/CookBot.Domain/Recipes/StepNode.cs | head -1` returns 0 (no non-nullable usage)
    - Compiles: `dotnet build src/CookBot.Domain/CookBot.Domain.csproj` exits 0
    - SectionStep unchanged: `grep -c "Temperature" src/CookBot.Domain/Recipes/StepNode.cs` returns exactly 2 (one in property name, one in JsonPropertyName) — neither inside the SectionStep block
  </acceptance_criteria>
  <done>ContentStep carries the new nullable Temperature property. Domain compiles. Plan 01's `GetSchema_StepTemperature_NullableShape` Fact will turn GREEN once Task 3 lands and dependent projects rebuild.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend RecipeValidator with per-unit Temperature rules (D-27)</name>
  <files>src/CookBot.Application/Recipes/RecipeValidator.cs, tests/CookBot.Tests/Recipes/RecipeValidatorTests.cs</files>
  <read_first>
    - src/CookBot.Application/Recipes/RecipeValidator.cs (the validator being extended; insertion point is the existing ContentStep branch in the steps loop, after the markdown-link assertion)
    - tests/CookBot.Tests/Recipes/RecipeValidatorTests.cs (existing test patterns for the same Validate(doc) entry point — extend in the same file; do not create a new test file)
    - .planning/phases/08-format-foundation/08-PATTERNS.md "RecipeValidator.cs (modify)" block (~line 338 — exact code snippet for the per-unit guard)
    - .planning/phases/08-format-foundation/08-CONTEXT.md §D-27 (rules: F/C whole-degree; Gas 0.5-step in [1.0, 9.5])
    - src/CookBot.Application/Recipes/ValidationResult.cs (the ValidationError record shape; existing error codes)
  </read_first>
  <behavior>
    - Adding `ContentStep.Temperature` with valid F/C whole-degree (e.g. 350 F, 180 C) produces no errors.
    - Adding `ContentStep.Temperature` with fractional F or fractional C produces one `ValidationError` with code `INVALID_TEMPERATURE` and path `/steps/{i}/temperature/value`.
    - Adding `ContentStep.Temperature` with Gas outside [1.0, 9.5] OR not a 0.5-step multiple produces one INVALID_TEMPERATURE error with the same path shape.
    - `Temperature == null` is valid (no error).
    - Existing validator tests continue to pass — no behavior change for documents without temperature.
  </behavior>
  <action>
    Per PATTERNS.md "RecipeValidator.cs (modify)" block: inside `RecipeValidator.Validate(doc)`, in the existing `case ContentStep content:` branch of the steps loop, AFTER the existing markdown-link `IngredientLinkPatterns` loop, insert the per-unit guard exactly as PATTERNS.md spells it out:

    ```
    if (content.Temperature is { } temp)
    {
        switch (temp.Unit)
        {
            case TemperatureUnit.F:
            case TemperatureUnit.C:
                if (temp.Value != Math.Truncate(temp.Value))
                    errors.Add(new ValidationError($"/steps/{i}/temperature/value",
                        "INVALID_TEMPERATURE", $"{temp.Unit} temperature must be whole-degree."));
                break;
            case TemperatureUnit.Gas:
                if (temp.Value % 0.5m != 0m || temp.Value < 1.0m || temp.Value > 9.5m)
                    errors.Add(new ValidationError($"/steps/{i}/temperature/value",
                        "INVALID_TEMPERATURE", "Gas mark must be a 0.5-step value in [1.0, 9.5]."));
                break;
        }
    }
    ```

    Add `using CookBot.Domain.Recipes;` at the top if not already present (TemperatureUnit lives there).

    Per D-27: these are ERRORS, not warnings — `IsValid` flips, triggering the AI repair loop. Use error code `INVALID_TEMPERATURE` consistent with existing codes (REQUIRED, OUT_OF_RANGE, DUPLICATE_ID, DANGLING_REF — verify the existing code style before committing).

    Then EXTEND `tests/CookBot.Tests/Recipes/RecipeValidatorTests.cs` with a new `[Theory]`-driven group named `Temperature_*` per the matrix in PATTERNS.md "StepTemperatureTests.cs (create)" block (because that block recommends the validator-rule matrix lives where the validator-rule code does — Plan 02 deliberately deferred validator-rule tests here):

    - F, 350 → valid
    - F, 350.5 → INVALID_TEMPERATURE
    - C, 180 → valid
    - C, 180.5 → INVALID_TEMPERATURE
    - Gas, 4 → valid
    - Gas, 4.5 → valid
    - Gas, 0.5 → INVALID_TEMPERATURE (below range)
    - Gas, 9.5 → valid
    - Gas, 10 → INVALID_TEMPERATURE (above range)
    - Gas, 4.25 → INVALID_TEMPERATURE (not 0.5-step)

    Helper: build a single-step `RecipeDocument` per test row; assert `result.Errors.Any(e => e.Code == "INVALID_TEMPERATURE")` for negative cases and `result.IsValid` for positive cases. NO existing RecipeValidatorTests test is removed.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/freeloves-cookbot &amp;&amp; dotnet test tests/CookBot.Tests/CookBot.Tests.csproj --filter "FullyQualifiedName~RecipeValidatorTests|FullyQualifiedName~SchemaAssertionTests" 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Validator carries the new error code: `grep -c "INVALID_TEMPERATURE" src/CookBot.Application/Recipes/RecipeValidator.cs` returns at least 2 (one F/C, one Gas)
    - Validator references TemperatureUnit: `grep -c "TemperatureUnit\." src/CookBot.Application/Recipes/RecipeValidator.cs` returns at least 3
    - Path format follows existing convention: `grep -c "/steps/.*/temperature/value" src/CookBot.Application/Recipes/RecipeValidator.cs` returns at least 1
    - All RecipeValidatorTests pass: `dotnet test tests/CookBot.Tests/CookBot.Tests.csproj --filter "FullyQualifiedName~RecipeValidatorTests"` exits 0 with `Failed: 0`
    - All SchemaAssertionTests now turn GREEN (this is THE GREEN-GATE moment from Plan 01): `dotnet test tests/CookBot.Tests/CookBot.Tests.csproj --filter "FullyQualifiedName~SchemaAssertionTests"` exits 0 with `Failed: 0` and at least 3 passing
    - At least 10 new InlineData rows in RecipeValidatorTests covering temperature: `grep -c "InlineData.*Temperature\|Temperature.*InlineData" tests/CookBot.Tests/Recipes/RecipeValidatorTests.cs` is &gt;= 10 OR the test count delta vs pre-change is &gt;= 10 (record pre-change count in commit message)
    - No existing RecipeValidatorTests test is deleted: `grep -c "\[Fact\]\|\[Theory\]" tests/CookBot.Tests/Recipes/RecipeValidatorTests.cs` is &gt;= the pre-change count
  </acceptance_criteria>
  <done>RecipeValidator emits INVALID_TEMPERATURE errors per D-27 rules and the test matrix passes. The Plan 01 schema-assertion tests turn GREEN — proving SCHEMA-07 (no manual schema editing) holds because RecipeJsonSchemaProvider regenerated automatically.</done>
</task>

</tasks>

<verification>
- `dotnet build src/CookBot.Domain/CookBot.Domain.csproj` exits 0
- `dotnet build src/CookBot.Application/CookBot.Application.csproj` exits 0
- `dotnet build src/CookBot.Web/CookBot.Web.csproj` exits 0
- `dotnet test tests/CookBot.Tests/CookBot.Tests.csproj --filter "FullyQualifiedName~SchemaAssertionTests"` exits 0 (all 3 GREEN — the Plan 01 RED-to-GREEN transition is complete)
- `dotnet test tests/CookBot.Tests/CookBot.Tests.csproj --filter "FullyQualifiedName~RecipeValidatorTests"` exits 0
- `dotnet test tests/CookBot.Tests/CookBot.Tests.csproj` exits 0 (no existing test regresses)
</verification>

<success_criteria>
- The five Phase 8 Success Criterion #2 condition is met: RecipeJsonSchemaProvider emits a JSON schema that includes photoUrl, description, and step-level temperature — the SchemaAssertionTests pass.
- RecipeDocument and ContentStep carry the v3 fields with correct nullable shape (defends PITFALLS C7/M2).
- RecipeValidator surfaces INVALID_TEMPERATURE per D-27 rules; the existing validator behavior for v2 documents is unchanged.
- SCHEMA-07 holds by construction — the schema provider was not manually edited.
</success_criteria>

<output>
Create `.planning/phases/08-format-foundation/08-03-SUMMARY.md` when done.
</output>
