---
phase: 08-format-foundation
plan: 12
type: execute
wave: 4
depends_on:
  - 08-07
files_modified:
  - src/CookBot.Infrastructure/Migrations/{timestamp}_AddPantryMatchIndexes.cs
autonomous: true
requirements: []
must_haves:
  truths:
    - "Two non-unique composite indexes are created: IX_RecipeIngredients_RecipeId_IngredientId on RecipeIngredients(RecipeId, IngredientId) and IX_PantryItems_UserId_IngredientId on PantryItems(UserId, IngredientId)"
    - "EF migration AddPantryMatchIndexes is generated; backup file cookbot.db.pre-AddPantryMatchIndexes.bak fires automatically via Plan 07's backup-label fix"
    - "No functional change in Phase 8 — the indexes are Phase 10 fuel for QOL-03 (smart pantry-match performance)"
    - "Down() symmetrically drops both indexes via DropIndex"
  artifacts:
    - path: src/CookBot.Infrastructure/Migrations/{timestamp}_AddPantryMatchIndexes.cs
      provides: "Composite indexes for Phase 10 pantry-match readiness"
      contains: "CreateIndex"
  key_links:
    - from: src/CookBot.Infrastructure/Migrations/{timestamp}_AddPantryMatchIndexes.cs
      to: src/CookBot.Infrastructure/Data/CookBotDbContext.cs
      via: "Index definitions match the existing entity schemas RecipeIngredient and PantryItem"
      pattern: "CreateIndex"
---

<objective>
Land D-31's fourth migration: composite indexes on `RecipeIngredient(RecipeId, IngredientId)` and `PantryItem(UserId, IngredientId)`. These prepare for Phase 10 QOL-03 (pantry-match O(n log n) performance per PITFALLS H7) without any functional change in Phase 8 — bundling them here means Phase 10 can be a pure code-and-test phase with zero EF migrations.

Purpose: Pure pre-fuel for Phase 10. No Phase 8 requirement points at this directly; it lives in the phase because the user's CONTEXT.md explicitly bundles it (D-31 migration #4) and PITFALLS H7 anchors the rationale.
Output: One new EF migration. No source-code changes outside the migration file.
</objective>

<execution_context>
@.claude/../get-shit-done/workflows/execute-plan.md
@.claude/../get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-format-foundation/08-CONTEXT.md
@.planning/phases/08-format-foundation/08-PATTERNS.md
@.planning/research/PITFALLS.md
@src/CookBot.Infrastructure/Migrations/20260428004334_ScheduledRecipesAndRecipeMades.cs
@src/CookBot.Infrastructure/Data/CookBotDbContext.cs
@CLAUDE.md

<interfaces>
From src/CookBot.Infrastructure/Migrations/20260428004334_ScheduledRecipesAndRecipeMades.cs (analog at PATTERNS.md ~line 699):
- migrationBuilder.CreateIndex(name: "IX_RecipeMades_RecipeId_CompletedAt", table: "RecipeMades", columns: new[] { "RecipeId", "CompletedAt" });
- migrationBuilder.CreateIndex(name: "IX_RecipeMades_UserId_CompletedAt", table: "RecipeMades", columns: new[] { "UserId", "CompletedAt" });

Existing schema (verify table + column names by reading CookBotDbContext + entity files):
- RecipeIngredients table — likely columns: Id, RecipeId, IngredientId (verify via `sqlite3 src/CookBot.Web/cookbot.db ".schema RecipeIngredients"`)
- PantryItems table — likely columns: Id, UserId, IngredientId, ... (verify same way)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generate AddPantryMatchIndexes EF migration with two composite indexes (D-31 migration #4, Phase 10 readiness)</name>
  <files>src/CookBot.Infrastructure/Migrations/{timestamp}_AddPantryMatchIndexes.cs</files>
  <read_first>
    - src/CookBot.Infrastructure/Migrations/20260428004334_ScheduledRecipesAndRecipeMades.cs (analog — composite-index pattern at lines 71-89; PATTERNS.md "AddPantryMatchIndexes.cs (create)" block ~line 699)
    - src/CookBot.Infrastructure/Data/CookBotDbContext.cs (verify the DbSet&lt;RecipeIngredient&gt; and DbSet&lt;PantryItem&gt; exist and confirm table names — EF default is the DbSet property name pluralized)
    - src/CookBot.Domain/Entities/RecipeIngredient.cs and src/CookBot.Domain/Entities/PantryItem.cs (verify exact column names — RecipeId, IngredientId, UserId)
    - .planning/phases/08-format-foundation/08-PATTERNS.md "AddPantryMatchIndexes.cs (create)" block (D-31 migration #4 spec; non-unique composite indexes; Phase 10 QOL-03 readiness)
    - .planning/research/PITFALLS.md H7 (smart pantry-match O(n log n) requires the composite index on RecipeIngredient(IngredientId, RecipeId) per the pitfall — but PATTERNS.md says (RecipeId, IngredientId); cross-check before generating; the column-order choice depends on the dominant query shape — read PITFALLS H7 for the rationale)
    - src/CookBot.Infrastructure/Data/DatabaseSeeder.cs (Plan 07's backup-label fix produces cookbot.db.pre-AddPantryMatchIndexes.bak automatically)
  </read_first>
  <behavior>
    - The migration generated by `dotnet ef migrations add AddPantryMatchIndexes` may be EMPTY if the entity configurations do not declare the indexes. Per Phase 8's strategy (no functional change to entities; only the migration), the migration body should be HAND-WRITTEN with two CreateIndex calls — OR the planner adds two HasIndex declarations to the entity configurations and lets EF generate them.
    - Prefer the hand-written approach because: (a) no entity configuration changes belong in Phase 8 outside the explicit RecipeTag work; (b) the migration is forward-only; (c) PATTERNS.md "AddPantryMatchIndexes.cs (create)" block specifies the hand-written shape directly.
    - Two non-unique composite indexes:
      1. `IX_RecipeIngredients_RecipeId_IngredientId` on `RecipeIngredients(RecipeId, IngredientId)`
      2. `IX_PantryItems_UserId_IngredientId` on `PantryItems(UserId, IngredientId)`
    - Down() drops both via DropIndex.
    - On boot: cookbot.db.pre-AddPantryMatchIndexes.bak fires; the indexes are created; existing queries become faster but no behavior changes.
  </behavior>
  <action>
    Step A — verify the actual table + column names by reading `src/CookBot.Infrastructure/Data/CookBotDbContext.cs` and the entity files (`RecipeIngredient.cs`, `PantryItem.cs`). Confirm:
    - Table name `RecipeIngredients` exists (DbSet&lt;RecipeIngredient&gt; pluralized).
    - Column names on RecipeIngredient are `RecipeId` and `IngredientId` (not `RecipeID` or alternates).
    - Table name `PantryItems` exists with columns `UserId` and `IngredientId`.
    - If any name differs, ADJUST the migration's column references accordingly. Do NOT proceed with assumed names.

    Step B — generate the migration scaffold:
    ```
    dotnet ef migrations add AddPantryMatchIndexes \
      --project src/CookBot.Infrastructure \
      --startup-project src/CookBot.Web
    ```

    The generated `Up()` body will likely be EMPTY (because no entity-configuration HasIndex was added). The model snapshot will also stay clean — which is what we want. NO entity-configuration changes are needed; we hand-write the index creation.

    Step C — HAND-WRITE the Up() body per PATTERNS.md "AddPantryMatchIndexes.cs (create)" block:

    ```
    migrationBuilder.CreateIndex(
        name: "IX_RecipeIngredients_RecipeId_IngredientId",
        table: "RecipeIngredients",
        columns: new[] { "RecipeId", "IngredientId" });

    migrationBuilder.CreateIndex(
        name: "IX_PantryItems_UserId_IngredientId",
        table: "PantryItems",
        columns: new[] { "UserId", "IngredientId" });
    ```

    Note on column ordering: PITFALLS H7 mentions `(IngredientId, RecipeId)` for the join from PantryItem -&gt; RecipeIngredient direction, but PATTERNS.md mandates `(RecipeId, IngredientId)`. Both orderings can be useful depending on query shape; PATTERNS.md is the planning artifact and takes precedence. If Phase 10 discovers the (IngredientId, RecipeId) ordering is needed, that phase can add a second index (Phase 10 was promised "zero EF migrations" but adding a non-functional perf index in Phase 10 if Phase 8 picked wrong is a forgivable schedule slip; flag this for the developer's awareness).

    Symmetrically populate Down() with two DropIndex calls.

    Keep `#nullable disable` per existing migration generator convention.

    Step D — boot smoke: `./run.sh` for ~10 seconds. Verify:
    - `cookbot.db.pre-AddPantryMatchIndexes.bak` exists.
    - `sqlite3 src/CookBot.Web/cookbot.db ".indexes RecipeIngredients" | grep -c IX_RecipeIngredients_RecipeId_IngredientId` returns 1.
    - `sqlite3 src/CookBot.Web/cookbot.db ".indexes PantryItems" | grep -c IX_PantryItems_UserId_IngredientId` returns 1.
    - App home page loads without exception.

    Per CLAUDE.md: NO Newtonsoft. NO new NuGet. Forward-only EF migration. EF Core 10 only.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/freeloves-cookbot &amp;&amp; dotnet ef migrations list --project src/CookBot.Infrastructure --startup-project src/CookBot.Web 2>&amp;1 | grep -c "AddPantryMatchIndexes"</automated>
  </verify>
  <acceptance_criteria>
    - Migration file exists: `ls src/CookBot.Infrastructure/Migrations/*_AddPantryMatchIndexes.cs | wc -l` returns 1
    - Class name correct: `grep -c 'class AddPantryMatchIndexes : Migration' src/CookBot.Infrastructure/Migrations/*_AddPantryMatchIndexes.cs` returns 1
    - Two CreateIndex calls in Up(): `grep -c 'CreateIndex' src/CookBot.Infrastructure/Migrations/*_AddPantryMatchIndexes.cs` returns at least 2
    - Specific RecipeIngredients index: `grep -c 'IX_RecipeIngredients_RecipeId_IngredientId' src/CookBot.Infrastructure/Migrations/*_AddPantryMatchIndexes.cs` returns at least 1
    - Specific PantryItems index: `grep -c 'IX_PantryItems_UserId_IngredientId' src/CookBot.Infrastructure/Migrations/*_AddPantryMatchIndexes.cs` returns at least 1
    - Two DropIndex in Down(): `grep -c 'DropIndex' src/CookBot.Infrastructure/Migrations/*_AddPantryMatchIndexes.cs` returns at least 2
    - Migration listed: `dotnet ef migrations list --project src/CookBot.Infrastructure --startup-project src/CookBot.Web` includes `AddPantryMatchIndexes`
    - Build clean: `dotnet build src/CookBot.Web/CookBot.Web.csproj` exits 0
    - Full test suite passes: `dotnet test tests/CookBot.Tests/CookBot.Tests.csproj` exits 0 (no functional change should regress anything)
    - Smoke (boot): `./run.sh` brings app up; cookbot.db.pre-AddPantryMatchIndexes.bak appears; sqlite ".indexes" confirms both new indexes
    - No model snapshot drift on RecipeIngredient/PantryItem (the indexes are migration-only, not configuration-declared): inspect the snapshot — RecipeIngredient and PantryItem blocks should be unchanged from pre-migration state
  </acceptance_criteria>
  <done>D-31 migration #4 of 4 lands. Phase 10 has the indexes ready and can ship as a pure code-and-test phase per CONTEXT.md.</done>
</task>

</tasks>

<verification>
- `dotnet build src/CookBot.Web/CookBot.Web.csproj` exits 0
- `dotnet ef migrations list --project src/CookBot.Infrastructure --startup-project src/CookBot.Web` includes `AddPantryMatchIndexes`
- `dotnet test tests/CookBot.Tests/CookBot.Tests.csproj` exits 0
- App boot smoke: `./run.sh` brings app up; both new indexes present in sqlite
</verification>

<success_criteria>
- D-31 migration #4 (the final EF migration) ships in Phase 8.
- No functional change in Phase 8; pure pre-fuel for Phase 10.
- Phase 10 can deliver QOL-03 (pantry-match performance) without authoring any EF migration.
</success_criteria>

<output>
Create `.planning/phases/08-format-foundation/08-12-SUMMARY.md` when done.
</output>
