---
phase: 08-format-foundation
plan: 09
type: execute
wave: 4
depends_on:
  - 08-06
files_modified:
  - tests/CookBot.Tests/CookBot.Tests.csproj
  - tests/CookBot.Tests/ModuleInitializer.cs
  - tests/CookBot.Tests/Prompts/PromptSnapshotTests.cs
  - tests/CookBot.Tests/Snapshots/PromptSnapshotTests.BuildSystemPrompt.verified.txt
  - tests/CookBot.Tests/Fixtures/Prompts/expected-system-prompt.txt (deleted)
autonomous: true
requirements:
  - CLEAN-03
must_haves:
  truths:
    - "Verify.Xunit 31.12.5 is added to CookBot.Tests as the ONLY new package — no Verify.XunitV3, no other NuGet adds (D-35)"
    - "ModuleInitializer.cs configures Verifier.DerivePathInfo to route .verified.txt files to tests/CookBot.Tests/Snapshots/"
    - "PromptSnapshotTests.cs is REPLACED with a single Fact returning Verifier.Verify(promptOutput) — class decorated [UsesVerify]"
    - "The initial PromptSnapshotTests.BuildSystemPrompt.verified.txt is committed and equals the byte-stable PromptBuilderService.BuildSystemPrompt output for the deterministic fixture profile"
    - "The legacy hand-rolled fixture file tests/CookBot.Tests/Fixtures/Prompts/expected-system-prompt.txt is DELETED in the same commit"
  artifacts:
    - path: tests/CookBot.Tests/ModuleInitializer.cs
      provides: "Verify path config (centralized snapshots directory)"
      contains: "DerivePathInfo"
    - path: tests/CookBot.Tests/Snapshots/PromptSnapshotTests.BuildSystemPrompt.verified.txt
      provides: "Approved prompt snapshot — diffs surface in PRs"
    - path: tests/CookBot.Tests/Prompts/PromptSnapshotTests.cs
      provides: "Verify-based snapshot test"
      contains: "Verifier.Verify"
  key_links:
    - from: tests/CookBot.Tests/Prompts/PromptSnapshotTests.cs
      to: tests/CookBot.Tests/Snapshots/PromptSnapshotTests.BuildSystemPrompt.verified.txt
      via: "Verifier.Verify(systemPrompt) compares against the .verified.txt snapshot"
      pattern: "Verifier\\.Verify"
---

<objective>
Replace Phase 1's hand-rolled `PromptSnapshotTests` with `Verify.Xunit 31.12.5` per CLEAN-03 + D-35. Verify owns the .received/.verified workflow; intentional prompt changes are accepted by promoting `.received.txt` to `.verified.txt` (visible diff in PR). Add the package, the ModuleInitializer for path configuration, the new test class, and the initial verified snapshot. Delete the legacy fixture file.

Purpose: Close CLEAN-03 (FUTURE-V1.1-04 carry-forward). Phase 8 success criterion #5 partway: the prompt-snapshot test asserts PromptBuilderService.BuildSystemPrompt is byte-stable.
Output: Test csproj edit (one new package reference + glob for snapshots), one new ModuleInitializer.cs, one rewritten PromptSnapshotTests.cs, one new committed verified snapshot file, one deleted legacy fixture.
</objective>

<execution_context>
@.claude/../get-shit-done/workflows/execute-plan.md
@.claude/../get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-format-foundation/08-CONTEXT.md
@.planning/phases/08-format-foundation/08-PATTERNS.md
@.planning/research/STACK.md
@tests/CookBot.Tests/CookBot.Tests.csproj
@tests/CookBot.Tests/Prompts/PromptSnapshotTests.cs
@tests/CookBot.Tests/Prompts/PromptDenylistTests.cs
@tests/CookBot.Tests/TestHost.cs
@CLAUDE.md

<interfaces>
From tests/CookBot.Tests/Prompts/PromptSnapshotTests.cs (current shape):
- public class PromptSnapshotTests
  - [Fact] public void DefaultTemplate_AssembledPrompt_MatchesSnapshot()
    - Reads tests/CookBot.Tests/Fixtures/Prompts/expected-system-prompt.txt
    - Assert.Equal(expectedFromFile, actualFromService)

From tests/CookBot.Tests/TestHost.cs:
- TestHost.MakeProfile() — deterministic fixture profile (W4 rules)
- TestHost.GetPromptBuilderService() — wires up the service for snapshot testing
- TestHost.FindRepoRoot() — repo-root path discovery

Verify.Xunit 31.12.5 (per STACK.md):
- Compatible with xunit 2.9.2 (verified in research)
- [UsesVerify] class attribute opts in to the Verify infrastructure
- Verifier.Verify(...) returns Task and writes .received.txt on first run; .verified.txt is the committed approved baseline; mismatch fails the test with an inline diff
- Verifier.DerivePathInfo customizes the snapshot directory layout
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Verify.Xunit 31.12.5 to CookBot.Tests.csproj + Snapshots directory glob (D-35)</name>
  <files>tests/CookBot.Tests/CookBot.Tests.csproj</files>
  <read_first>
    - tests/CookBot.Tests/CookBot.Tests.csproj (lines 10-17 ItemGroup with PackageReferences; line 31 area for None Update Fixtures glob)
    - .planning/phases/08-format-foundation/08-PATTERNS.md "CookBot.Tests.csproj (modify — add Verify.Xunit)" block ~line 784
    - .planning/research/STACK.md (lines 16, 41 — Verify.Xunit 31.12.5 compatibility with xunit 2.9.2; Verify.XunitV3 explicitly OUT of scope)
    - .planning/phases/08-format-foundation/08-CONTEXT.md §D-35 (the only new test package; Snapshots/ path)
    - CLAUDE.md (Things to avoid — list does not include Verify.Xunit; STACK.md confirms it is GPL-3.0 compatible)
  </read_first>
  <behavior>
    - CookBot.Tests.csproj gains exactly ONE new PackageReference: `Verify.Xunit` at version `31.12.5`. NO `Verify.XunitV3` (would force xUnit v3 migration). NO other adds.
    - The csproj also gains a None Update glob for `Snapshots\**\*.verified.txt` matching the existing Fixtures glob.
    - `dotnet restore` succeeds; transitive `Verify.DiffPlex` arrives.
    - `dotnet build tests/CookBot.Tests/CookBot.Tests.csproj` succeeds with no escalated warnings.
  </behavior>
  <action>
    Per PATTERNS.md "CookBot.Tests.csproj (modify — add Verify.Xunit)" block: append exactly ONE PackageReference for Verify.Xunit version 31.12.5 inside the existing PackageReference ItemGroup. Place it after the existing xunit line. Add a second None Update entry near line 31 next to the existing Fixtures glob with path Snapshots/**/*.verified.txt and CopyToOutputDirectory PreserveNewest. Run dotnet restore against the test csproj to pull the package. Confirm Verify.DiffPlex arrives transitively via dotnet list package include-transitive grep Verify lists both. Per CLAUDE.md: NO Newtonsoft.Json, NJsonSchema, Microsoft.Extensions.AI, official Anthropic NuGet, or MudBlazor may sneak in via this change. Verify the csproj diff touches ONLY two lines (one PackageReference, one None Update).
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/freeloves-cookbot &amp;&amp; dotnet restore tests/CookBot.Tests/CookBot.Tests.csproj 2>&amp;1 | tail -5 &amp;&amp; dotnet build tests/CookBot.Tests/CookBot.Tests.csproj 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one Verify.Xunit reference: `grep -c 'Verify.Xunit.*31.12.5' tests/CookBot.Tests/CookBot.Tests.csproj` returns 1
    - No Verify.XunitV3: `grep -c 'Verify.XunitV3' tests/CookBot.Tests/CookBot.Tests.csproj` returns 0
    - Snapshots glob present: `grep -c 'Snapshots' tests/CookBot.Tests/CookBot.Tests.csproj` returns at least 1
    - No forbidden adds (Newtonsoft, NJsonSchema, MEAI, Anthropic, MudBlazor): `grep -Ec 'Newtonsoft|NJsonSchema|Microsoft.Extensions.AI|PackageReference Include="Anthropic"|MudBlazor' tests/CookBot.Tests/CookBot.Tests.csproj` returns 0
    - Restore + build succeed: `dotnet build tests/CookBot.Tests/CookBot.Tests.csproj` exits 0
    - Transitive DiffPlex arrives: `dotnet list tests/CookBot.Tests/CookBot.Tests.csproj package --include-transitive 2>&amp;1 | grep -ic 'Verify.DiffPlex'` returns at least 1
  </acceptance_criteria>
  <done>Verify.Xunit 31.12.5 is the only new test-project NuGet add; the Snapshots glob ensures committed verified snapshots ship with the test binaries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create ModuleInitializer.cs for Verify path config (D-35 / Snapshots directory)</name>
  <files>tests/CookBot.Tests/ModuleInitializer.cs</files>
  <read_first>
    - tests/CookBot.Tests/TestHost.cs (FindRepoRoot path-discovery analog; PATTERNS.md "ModuleInitializer.cs (create)" block ~line 806 includes the canonical Verify shape)
    - .planning/phases/08-format-foundation/08-PATTERNS.md "ModuleInitializer.cs (create)" block (full Init signature; uses System.Runtime.CompilerServices.ModuleInitializer attribute; Verifier.DerivePathInfo path config)
    - .planning/phases/08-format-foundation/08-CONTEXT.md §D-35 (snapshots directory tests/CookBot.Tests/Snapshots/)
    - CLAUDE.md (file-scoped namespace; nullable enable everywhere; one type per file)
  </read_first>
  <behavior>
    - tests/CookBot.Tests/ModuleInitializer.cs is created with a static class containing one ModuleInitializer-attributed method.
    - On test-assembly load, Verifier.DerivePathInfo is configured to write/read snapshots from {projectDirectory}/Snapshots/ rather than the per-test-class default sibling directory.
    - Test-class snapshot files appear under tests/CookBot.Tests/Snapshots/{TypeName}.{MethodName}.verified.txt — e.g., PromptSnapshotTests.BuildSystemPrompt.verified.txt.
  </behavior>
  <action>
    Create `tests/CookBot.Tests/ModuleInitializer.cs` per PATTERNS.md "ModuleInitializer.cs (create)" block. Standard Verify ModuleInitializer shape: file-scoped namespace `CookBot.Tests`; using `System.Runtime.CompilerServices` and `VerifyTests`; public static class `ModuleInitializer` with one method `[ModuleInitializer] public static void Init()`; body calls `Verifier.DerivePathInfo` with a delegate that returns `new PathInfo(directory: Path.Combine(projectDirectory, "Snapshots"), typeName: type.Name, methodName: method.Name)`. The projectDirectory parameter Verify provides points to the CookBot.Tests project root by default.

    Per Pattern S6: file-scoped namespace, single public type per file. Per CLAUDE.md: nullable enabled (project default).
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/freeloves-cookbot &amp;&amp; dotnet build tests/CookBot.Tests/CookBot.Tests.csproj 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f tests/CookBot.Tests/ModuleInitializer.cs`
    - ModuleInitializer attribute present: `grep -c '\[ModuleInitializer\]' tests/CookBot.Tests/ModuleInitializer.cs` returns 1
    - DerivePathInfo configured: `grep -c 'Verifier.DerivePathInfo' tests/CookBot.Tests/ModuleInitializer.cs` returns 1
    - Snapshots directory referenced: `grep -c 'Snapshots' tests/CookBot.Tests/ModuleInitializer.cs` returns at least 1
    - File-scoped namespace: `grep -q '^namespace CookBot.Tests;$' tests/CookBot.Tests/ModuleInitializer.cs`
    - Compiles: `dotnet build tests/CookBot.Tests/CookBot.Tests.csproj` exits 0
  </acceptance_criteria>
  <done>Verify routes all snapshots through tests/CookBot.Tests/Snapshots/ — one centralized directory rather than per-test-class siblings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: REPLACE PromptSnapshotTests.cs with Verify-based test + commit initial verified snapshot + delete legacy fixture (D-35)</name>
  <files>tests/CookBot.Tests/Prompts/PromptSnapshotTests.cs, tests/CookBot.Tests/Snapshots/PromptSnapshotTests.BuildSystemPrompt.verified.txt, tests/CookBot.Tests/Fixtures/Prompts/expected-system-prompt.txt</files>
  <read_first>
    - tests/CookBot.Tests/Prompts/PromptSnapshotTests.cs (the file being REPLACED — read for the existing fixture-load pattern + TestHost wiring)
    - tests/CookBot.Tests/TestHost.cs (MakeProfile + GetPromptBuilderService — the deterministic fixture entry points)
    - tests/CookBot.Tests/Fixtures/Prompts/expected-system-prompt.txt (the legacy fixture being DELETED — confirm contents match what BuildSystemPrompt should produce, so the new .verified.txt is byte-equivalent)
    - .planning/phases/08-format-foundation/08-PATTERNS.md "PromptSnapshotTests.cs (modify — REPLACE with Verify)" block ~line 850 (full new class shape)
    - .planning/phases/08-format-foundation/08-CONTEXT.md §D-35 (single Fact; old fixture deleted in same commit)
    - src/CookBot.Application/Services/PromptBuilderService.cs (the BuildSystemPrompt or ResolveTemplate entry point being snapshotted)
  </read_first>
  <behavior>
    - PromptSnapshotTests.cs is REWRITTEN: class decorated with [UsesVerify], one [Fact] named BuildSystemPrompt that returns `Task` and calls `Verifier.Verify(promptOutput)`. The deterministic fixture profile from TestHost.MakeProfile() is the input.
    - First run produces tests/CookBot.Tests/Snapshots/PromptSnapshotTests.BuildSystemPrompt.received.txt; the executor renames it to .verified.txt (or runs `dotnet verify-cli` if installed) and commits the .verified.txt.
    - The .verified.txt content equals the existing expected-system-prompt.txt byte-for-byte (because PromptBuilderService output has not changed in this plan; the snapshot is just a different storage mechanism).
    - The legacy tests/CookBot.Tests/Fixtures/Prompts/expected-system-prompt.txt is DELETED in the same commit.
    - Per CONTEXT.md "Claude's Discretion" final bullet: a SECOND [Fact] is added that injects a SCHEMA-10 alias token (e.g., a fixture profile with `AiSystemPromptTemplate = "use the imageUrl field instead"`) and verifies the rendered prompt contains that token — combined with PromptDenylistTests.cs the resulting workflow is: if a developer sets a profile field that injects an alias into the rendered prompt, the snapshot test catches it (via diff to .verified.txt) AND the denylist scan catches it (via source-file scan if the literal lands in PromptBuilderService.cs source). Self-checking redundancy.
  </behavior>
  <action>
    Step A — REWRITE `tests/CookBot.Tests/Prompts/PromptSnapshotTests.cs` per PATTERNS.md "PromptSnapshotTests.cs (modify — REPLACE with Verify)" block. New shape: file-scoped namespace `CookBot.Tests.Prompts`; using `VerifyXunit;`; class `PromptSnapshotTests` decorated `[UsesVerify]`; single primary `[Fact]` method named `BuildSystemPrompt` that returns `Task`. The body builds the deterministic fixture (`var profile = TestHost.MakeProfile(); var pantry = Array.Empty<PantryItem>(); var svc = TestHost.GetPromptBuilderService(); var actual = svc.ResolveTemplate(PromptBuilderService.DefaultTemplate, profile, pantry);`) and returns `Verifier.Verify(actual);`. Preserve the TestHost wiring exactly so the input is byte-stable across runs. Add a second [Fact] named `BuildSystemPrompt_WithAliasInTemplate_DiffsAreVisible` that builds a profile whose `AiSystemPromptTemplate` field embeds `"use the imageUrl field"` and asserts (via Verify or a simple Assert.Contains on the rendered output) that the alias token appears in the rendered prompt — this is the "self-checking" Discretion test. The Verify snapshot for this second method may be omitted (skip Verifier.Verify; use a direct Assert.Contains) so the second test does not require its own .verified.txt artifact.

    Step B — RUN the test once locally to generate `PromptSnapshotTests.BuildSystemPrompt.received.txt`. This is the executor's manual step in the execute-plan workflow. The executor confirms the .received.txt content matches the existing legacy fixture byte-for-byte (this is the proof that the snapshot mechanism switch is non-behavioral). Then the executor renames `.received.txt` to `.verified.txt` (`mv tests/CookBot.Tests/Snapshots/PromptSnapshotTests.BuildSystemPrompt.received.txt tests/CookBot.Tests/Snapshots/PromptSnapshotTests.BuildSystemPrompt.verified.txt`) and commits.

    Step C — DELETE the legacy fixture file: `git rm tests/CookBot.Tests/Fixtures/Prompts/expected-system-prompt.txt` (per D-35 "the old expected-system-prompt.txt fixture is deleted in the same commit").

    Step D — re-run the full test suite. PromptSnapshotTests.BuildSystemPrompt now passes (Verifier.Verify finds .verified.txt and the rendered output matches). PromptDenylistTests still passes (the second Fact's alias-token injection is a runtime-behavior test, not a source-scan test, so it does not trigger the denylist regex against PromptBuilderService.cs source — the alias appears only in the test's synthetic profile string, never in production source).

    Per CLAUDE.md: NO Newtonsoft. xUnit 2.9.2 only.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/freeloves-cookbot &amp;&amp; dotnet test tests/CookBot.Tests/CookBot.Tests.csproj --filter "FullyQualifiedName~PromptSnapshotTests|FullyQualifiedName~PromptDenylistTests" 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - PromptSnapshotTests rewritten with [UsesVerify]: `grep -c 'UsesVerify' tests/CookBot.Tests/Prompts/PromptSnapshotTests.cs` returns 1
    - Verifier.Verify call present: `grep -c 'Verifier.Verify' tests/CookBot.Tests/Prompts/PromptSnapshotTests.cs` returns at least 1
    - Primary BuildSystemPrompt Fact returns Task: `grep -E 'public Task BuildSystemPrompt' tests/CookBot.Tests/Prompts/PromptSnapshotTests.cs` matches
    - Second alias-injection Fact present: `grep -c 'WithAliasInTemplate\|imageUrl' tests/CookBot.Tests/Prompts/PromptSnapshotTests.cs` returns at least 1
    - .verified.txt committed: `test -f tests/CookBot.Tests/Snapshots/PromptSnapshotTests.BuildSystemPrompt.verified.txt`
    - Legacy fixture deleted: `test ! -f tests/CookBot.Tests/Fixtures/Prompts/expected-system-prompt.txt`
    - All snapshot tests pass: `dotnet test tests/CookBot.Tests/CookBot.Tests.csproj --filter "FullyQualifiedName~PromptSnapshotTests"` exits 0 with `Failed: 0`
    - Denylist scans still pass (the new test code does not introduce alias literals into production source — only into test synthetic strings): `dotnet test tests/CookBot.Tests/CookBot.Tests.csproj --filter "FullyQualifiedName~PromptDenylistTests"` exits 0
    - Full suite green: `dotnet test tests/CookBot.Tests/CookBot.Tests.csproj` exits 0
    - .verified.txt is checked in (not in .gitignore): `git check-ignore tests/CookBot.Tests/Snapshots/PromptSnapshotTests.BuildSystemPrompt.verified.txt; test $? -ne 0` (exit 1 from check-ignore means the file is NOT ignored)
    - No .received.txt left lying around (would indicate an unapproved diff): `find tests/CookBot.Tests/Snapshots -name '*.received.txt' | wc -l` returns 0
  </acceptance_criteria>
  <done>CLEAN-03 closed. Verify owns the snapshot workflow; the legacy fixture is gone; future intentional prompt changes manifest as a .received.txt that the developer promotes to .verified.txt with a visible diff in the PR.</done>
</task>

</tasks>

<verification>
- `dotnet build src/CookBot.Web/CookBot.Web.csproj` exits 0
- `dotnet test tests/CookBot.Tests/CookBot.Tests.csproj --filter "FullyQualifiedName~PromptSnapshotTests"` exits 0
- `dotnet test tests/CookBot.Tests/CookBot.Tests.csproj --filter "FullyQualifiedName~PromptDenylistTests"` exits 0
- `dotnet test tests/CookBot.Tests/CookBot.Tests.csproj` exits 0 (full suite)
- File presence: `test -f tests/CookBot.Tests/Snapshots/PromptSnapshotTests.BuildSystemPrompt.verified.txt` AND `test ! -f tests/CookBot.Tests/Fixtures/Prompts/expected-system-prompt.txt`
- `grep -c 'Verify.Xunit' tests/CookBot.Tests/CookBot.Tests.csproj` returns 1
</verification>

<success_criteria>
- CLEAN-03 closed: Verify.Xunit 31.12.5 is the prompt-snapshot mechanism; the hand-rolled Phase 1 implementation is gone.
- D-35 honored: ModuleInitializer routes snapshots to a centralized Snapshots/ directory.
- The new test is byte-equivalent to the old behavior — the prompt did not change in this plan; only the snapshot storage did.
- Self-checking redundancy: alias-injection test demonstrates Verify will catch future prompt changes that introduce SCHEMA-10 alias tokens.
</success_criteria>

<output>
Create `.planning/phases/08-format-foundation/08-09-SUMMARY.md` when done.
</output>
