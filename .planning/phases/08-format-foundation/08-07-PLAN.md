---
phase: 08-format-foundation
plan: 07
type: execute
wave: 3
depends_on:
  - 08-03
files_modified:
  - src/CookBot.Domain/Entities/Recipe.cs
  - src/CookBot.Infrastructure/Data/Configurations/RecipeConfiguration.cs
  - src/CookBot.Infrastructure/Data/DatabaseSeeder.cs
  - src/CookBot.Infrastructure/Migrations/{timestamp}_AddRecipePhotoUrlAndDescription.cs
autonomous: true
requirements:
  - SCHEMA-01
  - SCHEMA-02
  - SCHEMA-05
  - SCHEMA-06
must_haves:
  truths:
    - "Recipe entity gains nullable string columns PhotoUrl (max 2048) and Description (max 4096) via a forward-only EF migration"
    - "EF migration name is exactly AddRecipePhotoUrlAndDescription per D-31; produces backup file cookbook.db.pre-AddRecipePhotoUrlAndDescription.bak via IDatabaseBackupService"
    - "DatabaseSeeder backup label is derived from the pending migration list (the hardcoded \"RecipeCanonicalDocument\" string is removed) — this makes Plans 08, 11, 12 produce correctly-named .pre-{MigrationName}.bak files automatically per D-31"
    - "App boot via ./run.sh applies the migration cleanly; existing recipes get NULL in both new columns; CookbookList / Home / Recipe pages still render"
    - "EF model snapshot is updated by the EF tool; no hand-edited snapshot drift"
  artifacts:
    - path: src/CookBot.Domain/Entities/Recipe.cs
      provides: "PhotoUrl + Description entity columns"
      contains: "PhotoUrl"
    - path: src/CookBot.Infrastructure/Data/Configurations/RecipeConfiguration.cs
      provides: "MaxLength constraints (2048 / 4096)"
      contains: "HasMaxLength(2048)"
    - path: src/CookBot.Infrastructure/Data/DatabaseSeeder.cs
      provides: "Pending-migration-derived backup label (drops hardcoded \"RecipeCanonicalDocument\" literal)"
      contains: "GetPendingMigrationsAsync"
  key_links:
    - from: src/CookBot.Infrastructure/Data/Configurations/RecipeConfiguration.cs
      to: src/CookBot.Domain/Entities/Recipe.cs
      via: "EntityTypeBuilder<Recipe>.Property(r => r.PhotoUrl).HasMaxLength(2048)"
      pattern: "HasMaxLength\\(2048\\)"
    - from: src/CookBot.Infrastructure/Data/DatabaseSeeder.cs
      to: src/CookBot.Infrastructure/Migrations/{timestamp}_AddRecipePhotoUrlAndDescription.cs
      via: "BackupBeforeMigrationAsync(label) where label is derived from pending[0]"
      pattern: "pending\\[0\\]|GetPendingMigrationsAsync"
---

<objective>
Land the first of D-31's four EF migrations: add `Recipe.PhotoUrl` (nullable string, max 2048) and `Recipe.Description` (nullable string, max 4096) as forward-only schema additions. Per the existing IDatabaseBackupService pattern, this migration produces an automatic pre-migration backup file. No data motion — both columns default to NULL on existing rows.

Purpose: Land Phase 8 SCHEMA-05 / SCHEMA-06 — the entity-column half of the v3 schema additions. Plan 03 established the domain record nullable shape; this plan persists that shape to disk. Also fixes the DatabaseSeeder backup-label hardcode so Plans 08/11/12 produce correctly-named backup files.
Output: One domain entity modified, one EF configuration modified, DatabaseSeeder backup-label code rewritten, one new EF migration. App boots cleanly with the migration applied.
</objective>

<execution_context>
@.claude/../get-shit-done/workflows/execute-plan.md
@.claude/../get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-format-foundation/08-CONTEXT.md
@.planning/phases/08-format-foundation/08-PATTERNS.md
@src/CookBot.Domain/Entities/Recipe.cs
@src/CookBot.Infrastructure/Data/Configurations/RecipeConfiguration.cs
@src/CookBot.Infrastructure/Data/DatabaseSeeder.cs
@src/CookBot.Infrastructure/Migrations/20260425223916_RecipeCanonicalDocument.cs
@CLAUDE.md

<interfaces>
<!-- Existing entity + configuration shapes — extend in place. -->

From src/CookBot.Domain/Entities/Recipe.cs:
- public class Recipe
  - public int Id { get; set; }
  - public int CookbookId { get; set; }
  - public string Name { get; set; } = string.Empty;
  - ... existing properties ...
  - public string? CanonicalDocumentJson { get; set; }
  - Navigation: Cookbook, Steps, RecipeIngredients

From src/CookBot.Infrastructure/Data/Configurations/RecipeConfiguration.cs:
- public class RecipeConfiguration : IEntityTypeConfiguration<Recipe>
  - public void Configure(EntityTypeBuilder<Recipe> builder)
    - builder.HasKey(r => r.Id);
    - builder.Property(r => r.Name).HasMaxLength(300).IsRequired();
    - builder.Property(r => r.TagsJson).HasDefaultValue("[]");
    - builder.Property(r => r.CanonicalDocumentJson).HasColumnType("TEXT");

From src/CookBot.Infrastructure/Migrations/20260425223916_RecipeCanonicalDocument.cs (analog):
- public partial class RecipeCanonicalDocument : Migration
  - protected override void Up(MigrationBuilder migrationBuilder) — single AddColumn<string>("CanonicalDocumentJson", "Recipes", type: "TEXT", nullable: true)
  - protected override void Down(MigrationBuilder migrationBuilder) — DropColumn

From src/CookBot.Infrastructure/Data/DatabaseSeeder.cs:
- DatabaseSeeder.SeedAsync — currently calls IDatabaseBackupService.BackupBeforeMigrationAsync("RecipeCanonicalDocument", ...) with a HARDCODED label string (Pattern S2 callout). This plan replaces the hardcoded literal with a label derived from the pending-migration list so each subsequent migration in Phase 8 produces its own correctly-named .bak file.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Recipe.PhotoUrl + Recipe.Description entity columns + RecipeConfiguration max-length constraints (SCHEMA-05, SCHEMA-06, D-28)</name>
  <files>src/CookBot.Domain/Entities/Recipe.cs, src/CookBot.Infrastructure/Data/Configurations/RecipeConfiguration.cs</files>
  <read_first>
    - src/CookBot.Domain/Entities/Recipe.cs (the entity being extended)
    - src/CookBot.Infrastructure/Data/Configurations/RecipeConfiguration.cs (the configuration being extended)
    - .planning/phases/08-format-foundation/08-PATTERNS.md "Recipe.cs (modify)" block ~line 156 and "RecipeConfiguration.cs (modify)" block ~line 576
    - .planning/phases/08-format-foundation/08-CONTEXT.md §D-28 (max lengths 2048/4096; planner's call to add via EntityTypeConfiguration not data annotation)
  </read_first>
  <behavior>
    - Recipe class gains two nullable get/set properties: `string? PhotoUrl` and `string? Description`.
    - Both default to null. Existing recipes load without modification.
    - RecipeConfiguration's Configure method adds two HasMaxLength clauses: 2048 for PhotoUrl, 4096 for Description.
    - The existing TagsJson configuration line stays (this plan does NOT remove TagsJson — Plan 11 handles that via a separate migration).
    - Adding navigation `public ICollection<RecipeTag> Tags { get; set; } = new List<RecipeTag>();` is DEFERRED to Plan 08 (which creates the RecipeTag entity). Do NOT add it here.
  </behavior>
  <action>
    Step A — `src/CookBot.Domain/Entities/Recipe.cs` per PATTERNS.md "Recipe.cs (modify)" block: insert two properties next to `CanonicalDocumentJson` (other `string?` fields cluster there). Concretely:

    ```
    public string? PhotoUrl { get; set; }
    public string? Description { get; set; }
    ```

    Place them adjacent to the existing `public string? CanonicalDocumentJson { get; set; }` line. Do NOT touch `TagsJson` in this plan (Plans 08 and 11 own its lifecycle). Do NOT add the Tags navigation collection — Plan 08 creates the RecipeTag entity and adds the navigation.

    Step B — `src/CookBot.Infrastructure/Data/Configurations/RecipeConfiguration.cs`: add two `builder.Property` lines per D-28:

    ```
    builder.Property(r => r.PhotoUrl).HasMaxLength(2048);
    builder.Property(r => r.Description).HasMaxLength(4096);
    ```

    Place them adjacent to the existing `builder.Property(r => r.CanonicalDocumentJson).HasColumnType("TEXT");` block. Per D-28: max-length lives in the EF configuration ONLY (the [MaxLength] attribute on RecipeDocument from Plan 03 is for the AI schema; the entity-column constraint is enforced via fluent API per project convention).

    DO NOT delete `builder.Property(r => r.TagsJson).HasDefaultValue("[]");` — that is Plan 11's responsibility (D-26 sequencing — TagsJson stays through this migration).

    Per CLAUDE.md: no Newtonsoft. Per Pattern S6: file-scoped namespace stays.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/freeloves-cookbot &amp;&amp; dotnet build src/CookBot.Infrastructure/CookBot.Infrastructure.csproj 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Recipe.PhotoUrl property: `grep -c "string? PhotoUrl" src/CookBot.Domain/Entities/Recipe.cs` returns 1
    - Recipe.Description property: `grep -c "string? Description" src/CookBot.Domain/Entities/Recipe.cs` returns 1
    - RecipeConfiguration HasMaxLength clauses: `grep -c "HasMaxLength(2048)\|HasMaxLength(4096)" src/CookBot.Infrastructure/Data/Configurations/RecipeConfiguration.cs` returns 2
    - TagsJson configuration NOT removed yet: `grep -c "TagsJson" src/CookBot.Infrastructure/Data/Configurations/RecipeConfiguration.cs` returns at least 1
    - Tags navigation NOT added yet (deferred to Plan 08): `grep -c "ICollection<RecipeTag>" src/CookBot.Domain/Entities/Recipe.cs` returns 0
    - Compiles: `dotnet build src/CookBot.Infrastructure/CookBot.Infrastructure.csproj` exits 0
  </acceptance_criteria>
  <done>Entity model carries the two new columns with the correct lengths and nullability. EF migration generation in Task 2 will detect the schema delta automatically.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generate AddRecipePhotoUrlAndDescription EF migration + fix DatabaseSeeder backup label (SCHEMA-05, D-31)</name>
  <files>src/CookBot.Infrastructure/Migrations/{timestamp}_AddRecipePhotoUrlAndDescription.cs, src/CookBot.Infrastructure/Data/DatabaseSeeder.cs</files>
  <read_first>
    - src/CookBot.Infrastructure/Migrations/20260425223916_RecipeCanonicalDocument.cs (the analog migration — confirm Up/Down shape; copy AddColumn<string> pattern)
    - src/CookBot.Infrastructure/Data/DatabaseSeeder.cs (lines 29-36 — confirms backup-on-pending pattern; the current code calls `await backupService.BackupBeforeMigrationAsync("RecipeCanonicalDocument", CancellationToken.None);` with a HARDCODED string — this plan rewrites it)
    - .planning/phases/08-format-foundation/08-PATTERNS.md "AddRecipePhotoUrlAndDescription.cs (create)" block ~line 605 and "Pattern S2" ~line 1088 (the latter notes a planning question: the backup label is currently a hardcoded string; planner addresses it here)
    - .planning/phases/08-format-foundation/08-CONTEXT.md §D-31 (migration #1 of four; combined nullable adds; backup file name)
    - CLAUDE.md (Forward-only EF migrations under src/CookBot.Infrastructure/Migrations/; never reintroduce Newtonsoft or NJsonSchema)
  </read_first>
  <behavior>
    - Running `dotnet ef migrations add AddRecipePhotoUrlAndDescription --project src/CookBot.Infrastructure --startup-project src/CookBot.Web` produces a new migration file with a timestamp prefix.
    - The Up() method contains exactly two `AddColumn<string>` calls: one for `PhotoUrl` (maxLength: 2048, nullable: true), one for `Description` (maxLength: 4096, nullable: true), both on table "Recipes".
    - The Down() method drops both columns (forward-only project policy means Down() is generated for completeness but unused — match existing migration convention).
    - The model snapshot file (`CookBotDbContextModelSnapshot.cs`) is auto-updated to include both new properties on Recipe.
    - `dotnet ef migrations list --project src/CookBot.Infrastructure --startup-project src/CookBot.Web` shows the new migration as Pending.
    - DatabaseSeeder.SeedAsync no longer contains the hardcoded literal `"RecipeCanonicalDocument"` — the backup label is derived from `(await context.Database.GetPendingMigrationsAsync()).First()` (split on the first underscore to recover just the migration class name). This makes Plans 08/11/12 produce backups named `.pre-AddRecipeTagTable.bak`, `.pre-DropTagsJsonColumn.bak`, etc. automatically.
    - On app boot via `./run.sh`, IDatabaseBackupService creates a backup file named `cookbot.db.pre-AddRecipePhotoUrlAndDescription.bak` per D-31 BEFORE the migration applies.
    - `MigrateAsync()` applies the migration; both columns appear in the SQLite Recipes table; existing rows have NULL in both.
  </behavior>
  <action>
    Step A — generate the migration via the EF CLI:
    ```
    dotnet ef migrations add AddRecipePhotoUrlAndDescription \
      --project src/CookBot.Infrastructure \
      --startup-project src/CookBot.Web
    ```

    Verify the generated file appears under `src/CookBot.Infrastructure/Migrations/{timestamp}_AddRecipePhotoUrlAndDescription.cs`. The class name is `public partial class AddRecipePhotoUrlAndDescription : Migration`.

    Step B — inspect the generated `Up()` body. It MUST contain exactly two `AddColumn<string>` calls matching the analog at `20260425223916_RecipeCanonicalDocument.cs`:

    ```
    migrationBuilder.AddColumn<string>(
        name: "PhotoUrl",
        table: "Recipes",
        type: "TEXT",
        maxLength: 2048,
        nullable: true);

    migrationBuilder.AddColumn<string>(
        name: "Description",
        table: "Recipes",
        type: "TEXT",
        maxLength: 4096,
        nullable: true);
    ```

    The `Down()` method should symmetrically drop both. Do NOT hand-edit the migration body unless the EF generator missed the maxLength constraint (which can happen if the entity configuration is wrong — fix the configuration in Task 1 instead). Keep `#nullable disable` at the top per existing migration generator convention.

    Step C — fix the DatabaseSeeder backup-label issue per PATTERNS.md "Pattern S2" callout: the current code at `DatabaseSeeder.cs:29-36` calls `await backupService.BackupBeforeMigrationAsync("RecipeCanonicalDocument", CancellationToken.None);` with a HARDCODED string. To produce the four distinct backup files D-31 promises, change the label to be derived from the pending migration list. Concretely:

    ```
    var pending = (await context.Database.GetPendingMigrationsAsync()).ToList();
    if (pending.Count > 0)
    {
        // Use the name of the FIRST pending migration as the backup label.
        // For multi-migration sequences (Plans 07/08/11/12), each app boot that picks up
        // a single new migration produces a uniquely-named .bak file.
        var label = pending[0].Split('_', 2).Length == 2 ? pending[0].Split('_', 2)[1] : pending[0];
        await backupService.BackupBeforeMigrationAsync(label, CancellationToken.None);
    }
    ```

    The migration name format is `{timestamp}_{Name}` — split on the first underscore to recover just `AddRecipePhotoUrlAndDescription`. This produces backup file `cookbot.db.pre-AddRecipePhotoUrlAndDescription.bak` per D-31.

    Caveat: if multiple pending migrations apply in one boot (which can happen during development), only the first migration's name labels the backup. The actual data backed up is the database state BEFORE all pending migrations apply — same backup, same set of data. Consider iterating over `pending` and calling backup per-migration only if PATTERNS.md S2 explicitly requires it; the simpler approach is fine.

    Step D — boot the app with `./run.sh` (or `dotnet run --project src/CookBot.Web`) on a development DB. Verify:
    - `cookbot.db.pre-AddRecipePhotoUrlAndDescription.bak` exists in the same directory as `cookbot.db`.
    - The Recipes table now has `PhotoUrl` and `Description` columns (`sqlite3 src/CookBot.Web/cookbot.db ".schema Recipes" | grep -E "PhotoUrl|Description"` returns both).
    - Existing rows have NULL in both columns.
    - The Recipe Editor page loads without throwing (since the Razor pages don't yet read the new fields, this is just a smoke test that nothing broke).

    Per CLAUDE.md: NO new NuGet packages. NO Newtonsoft. EF Core 10 only. NO MudBlazor reintroduction.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/freeloves-cookbot &amp;&amp; dotnet ef migrations list --project src/CookBot.Infrastructure --startup-project src/CookBot.Web 2>&amp;1 | grep -c "AddRecipePhotoUrlAndDescription"</automated>
  </verify>
  <acceptance_criteria>
    - Migration file exists: `ls src/CookBot.Infrastructure/Migrations/*_AddRecipePhotoUrlAndDescription.cs | wc -l` returns 1
    - Migration class declared: `grep -c "class AddRecipePhotoUrlAndDescription : Migration" src/CookBot.Infrastructure/Migrations/*_AddRecipePhotoUrlAndDescription.cs` returns 1
    - Two AddColumn calls in Up(): `grep -c 'AddColumn<string>' src/CookBot.Infrastructure/Migrations/*_AddRecipePhotoUrlAndDescription.cs` returns at least 2
    - PhotoUrl maxLength 2048: `grep -c "maxLength: 2048" src/CookBot.Infrastructure/Migrations/*_AddRecipePhotoUrlAndDescription.cs` returns at least 1
    - Description maxLength 4096: `grep -c "maxLength: 4096" src/CookBot.Infrastructure/Migrations/*_AddRecipePhotoUrlAndDescription.cs` returns at least 1
    - Both columns nullable: `grep -c "nullable: true" src/CookBot.Infrastructure/Migrations/*_AddRecipePhotoUrlAndDescription.cs` returns at least 2
    - Snapshot updated: `grep -c "PhotoUrl\|Description" src/CookBot.Infrastructure/Migrations/CookBotDbContextModelSnapshot.cs` returns at least 2
    - Hardcoded backup-label literal is gone: `grep -c '"RecipeCanonicalDocument"' src/CookBot.Infrastructure/Data/DatabaseSeeder.cs` returns 0
    - Pending-migration-derived label is in place (any one of these patterns satisfies the criterion — pick whichever your action's chosen approach matches): `grep -cE 'pending\[0\]|pendingMigration\.Name|migration\.GetId|GetPendingMigrationsAsync' src/CookBot.Infrastructure/Data/DatabaseSeeder.cs` returns at least 1
    - Migration listed as known: `dotnet ef migrations list --project src/CookBot.Infrastructure --startup-project src/CookBot.Web` includes `AddRecipePhotoUrlAndDescription`
    - Build clean: `dotnet build src/CookBot.Web/CookBot.Web.csproj` exits 0
    - Smoke test: launch app via `./run.sh` for 10 seconds; check that the backup file `cookbot.db.pre-AddRecipePhotoUrlAndDescription.bak` exists relative to the working DB; check that `sqlite3 src/CookBot.Web/cookbot.db ".schema Recipes"` shows both new columns. (Run `pkill -f CookBot.Web` after smoke check.)
  </acceptance_criteria>
  <done>Migration #1 of D-31's four lands cleanly. The app boots; the backup file appears with the correct name; the Recipes table has the two new columns. The DatabaseSeeder backup-label fix means Plans 08/11/12 will produce their own correctly-named .bak files automatically.</done>
</task>

</tasks>

<verification>
- `dotnet build src/CookBot.Web/CookBot.Web.csproj` exits 0
- `dotnet ef migrations list --project src/CookBot.Infrastructure --startup-project src/CookBot.Web` includes `AddRecipePhotoUrlAndDescription`
- `dotnet test tests/CookBot.Tests/CookBot.Tests.csproj` exits 0 (no regressions from Wave 1/2 plans)
- App boot smoke (manual sanity in execution): `./run.sh` starts; SQLite schema shows new columns; .bak file present with correct name
</verification>

<success_criteria>
- SCHEMA-05 + SCHEMA-06 closed at the persistence layer.
- D-31 migration #1 of 4 ships with the exact name the CONTEXT promises.
- The DatabaseSeeder backup-label fix means each subsequent migration (Plans 08, 11, 12) will produce its own named backup file automatically.
- No data motion; existing rows get NULL in both new columns.
</success_criteria>

<output>
Create `.planning/phases/08-format-foundation/08-07-SUMMARY.md` when done.
</output>
