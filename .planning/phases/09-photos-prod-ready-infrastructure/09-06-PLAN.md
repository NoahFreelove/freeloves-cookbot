---
phase: 09-photos-prod-ready-infrastructure
plan: 06
type: execute
wave: 4
depends_on: ["09-04"]
files_modified:
  - src/CookBot.Web/CookBot.Web.csproj
  - src/CookBot.Web/Program.cs
  - Dockerfile
  - docker-compose.yml
  - .dockerignore
autonomous: false
requirements: [PROD-01, PROD-02, PROD-03, PROD-04, PROD-05]
must_haves:
  truths:
    - "Microsoft.Extensions.Diagnostics.HealthChecks.EntityFrameworkCore 10.0.* added to CookBot.Web (PROD-05, D-43)"
    - "Program.cs registers builder.Services.AddHealthChecks().AddDbContextCheck<CookBotDbContext>('database') (D-43)"
    - "Program.cs adds app.MapHealthChecks('/healthz') BEFORE app.Run() (PROD-05, D-43)"
    - "Multi-stage Dockerfile uses mcr.microsoft.com/dotnet/sdk:10.0 build stage and mcr.microsoft.com/dotnet/aspnet:10.0 runtime stage (PROD-01)"
    - "Dockerfile sets ASPNETCORE_URLS=http://+:7000, TZ=UTC, EXPOSE 7000, ENTRYPOINT [\"dotnet\", \"CookBot.Web.dll\"] (PROD-01, PROD-03, PITFALL M4, PITFALL M7)"
    - "docker-compose.yml exposes ${COOKBOT_PORT:-7000}, sets two named volumes cookbot_db at /data and cookbot_uploads at /app/wwwroot/uploads (PROD-02, PROD-04, PITFALL M5)"
    - "docker-compose env vars: ASPNETCORE_URLS, ConnectionStrings__DefaultConnection=Data Source=/data/cookbot.db, COOKBOT_PORT (PROD-02)"
    - "docker-compose uses restart: on-failure with retries: 3 (NOT unless-stopped — D-43 override of PROD-02; PITFALL M6)"
    - "docker-compose healthcheck stanza calls curl -f http://localhost:7000/healthz with interval 30s, timeout 5s, start_period 30s, retries 3 (D-43)"
    - "Key ring colocates in cookbot.db via PROD-07 — no third named volume needed (D-43 + Plan 09-04 invariant)"
    - ".dockerignore excludes bin/, obj/, .git/, .planning/, *.db, *.db-wal, *.db-shm, *.db.pre-*.bak, .gitignore noise"
  artifacts:
    - path: "Dockerfile"
      provides: "Multi-stage build to /app/CookBot.Web.dll with .NET 10 ASP.NET runtime base"
      contains: "ENTRYPOINT"
    - path: "docker-compose.yml"
      provides: "Single-service deployment with named volumes, env vars, healthcheck, on-failure restart"
      contains: "healthcheck:"
    - path: ".dockerignore"
      provides: "Build context filter — keeps cookbot.db / planning notes / bin obj out of the layer cache"
    - path: "src/CookBot.Web/CookBot.Web.csproj"
      provides: "Microsoft.Extensions.Diagnostics.HealthChecks.EntityFrameworkCore PackageReference"
      contains: "Microsoft.Extensions.Diagnostics.HealthChecks.EntityFrameworkCore"
    - path: "src/CookBot.Web/Program.cs"
      provides: "AddHealthChecks + AddDbContextCheck + MapHealthChecks wiring"
      contains: "MapHealthChecks(\"/healthz\")"
  key_links:
    - from: "docker-compose.yml"
      to: "Dockerfile"
      via: "build: . context"
      pattern: "build:"
    - from: "docker-compose.yml healthcheck"
      to: "/healthz endpoint"
      via: "curl -f http://localhost:7000/healthz"
      pattern: "healthz"
    - from: "docker-compose.yml ConnectionStrings__DefaultConnection"
      to: "/data volume"
      via: "Data Source=/data/cookbot.db"
      pattern: "/data/cookbot.db"
---

<objective>
Ship the Docker delivery story end-to-end: container image, compose file, healthcheck, restart policy. The key-ring volume problem is already solved by Plan 09-04's PersistKeysToDbContext (key ring colocated in cookbot.db, so only TWO volumes — db and uploads — not three). D-43 picks `on-failure` over `unless-stopped` so startup failures surface visibly. The /healthz endpoint protects against silent restart loops on migration errors (PITFALL M6).

Purpose: After this plan a fresh `docker compose up` on a clean LAN machine starts the app, applies migrations, encrypts any plaintext keys (Plan 09-04 + 09-05 boot logic), and answers /healthz with 200. A bad migration produces a failed healthcheck → container exits visibly after 3 retries (NOT a silent restart spiral).

Output: Dockerfile + docker-compose.yml + .dockerignore + the /healthz wire-up in Program.cs + one new NuGet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-photos-prod-ready-infrastructure/09-CONTEXT.md
@.planning/phases/09-photos-prod-ready-infrastructure/09-RESEARCH.md
@.planning/phases/09-photos-prod-ready-infrastructure/09-PATTERNS.md
@.planning/research/PITFALLS.md
@src/CookBot.Web/Program.cs
@src/CookBot.Web/CookBot.Web.csproj

<interfaces>
<!-- Anchors for the executor. -->

NEW NuGet: Microsoft.Extensions.Diagnostics.HealthChecks.EntityFrameworkCore version 10.0.* (latest 10.0.7 per 09-RESEARCH lines 99–106). First-party Microsoft, MS Learn cited, legitimacy approved (09-RESEARCH lines 108–115). Add to src/CookBot.Web/CookBot.Web.csproj.

Program.cs (Plan 09-04 already modified this file — DataProtection registration is in place):
- Already has AddInfrastructure (line 18-ish) which registers CookBotDbContext.
- Add `using Microsoft.Extensions.Diagnostics.HealthChecks;` near the top (or fully-qualify).
- After AddDataProtection block (from Plan 09-04), insert:
  builder.Services.AddHealthChecks().AddDbContextCheck<CookBotDbContext>(name: "database");
- After app.Build() and existing middleware setup (UseAntiforgery, MapStaticAssets, MapRazorComponents, the explicit UseStaticFiles for /uploads from Plan 09-01), and BEFORE app.Run() — insert:
  app.MapHealthChecks("/healthz");

Dockerfile structure per 09-RESEARCH §"Stack" + 09-PATTERNS.md lines 421–439:
- Build stage: FROM mcr.microsoft.com/dotnet/sdk:10.0 AS build, WORKDIR /src, COPY [project files], RUN dotnet restore + publish to /app.
- Runtime stage: FROM mcr.microsoft.com/dotnet/aspnet:10.0 AS runtime, WORKDIR /app, COPY --from=build /app, ENV ASPNETCORE_URLS=http://+:7000, ENV TZ=UTC, EXPOSE 7000, ENTRYPOINT ["dotnet", "CookBot.Web.dll"].
- The runtime base image (mcr.microsoft.com/dotnet/aspnet:10.0) is Debian-based and includes curl by default (per 09-RESEARCH Environment Availability table line 548). Healthcheck uses curl, no extra packages needed.

docker-compose.yml per 09-PATTERNS.md lines 443–478:
- services.cookbot.build = "."
- ports = "${COOKBOT_PORT:-7000}:7000"
- environment block: ASPNETCORE_URLS=http://+:7000, ConnectionStrings__DefaultConnection=Data Source=/data/cookbot.db, COOKBOT_PORT=7000 (the latter is read by the compose file itself, but it's harmless to expose it inside the container too).
- volumes: ["cookbot_db:/data", "cookbot_uploads:/app/wwwroot/uploads"]
- restart: on-failure
- (no max_retries — Docker compose accepts the deploy.restart_policy block, but `restart: on-failure` in standalone mode just retries until the container exits permanently; the healthcheck stanza's retries: 3 is the operational guard.)
- healthcheck: per 09-PATTERNS.md lines 449–460 verbatim.
- bottom of file: volumes: { cookbot_db: {}, cookbot_uploads: {} }

The application reads its connection string from ConnectionStrings:DefaultConnection in appsettings.json (current value: `Data Source=cookbot.db` — relative path). Setting the env var `ConnectionStrings__DefaultConnection=Data Source=/data/cookbot.db` in compose overrides this at runtime. The double-underscore is .NET configuration's section delimiter — verify the binding picks up correctly.

.dockerignore must exclude things that bloat the build context or leak data:
- bin/, obj/, .git/, .vs/, .vscode/, .idea/, .planning/
- All *.db, *.db-wal, *.db-shm, *.db.pre-*.bak (operator's local DB MUST NOT leak into a built image)
- node_modules/ if present
- Dockerfile.* and docker-compose.override*.yml siblings
- The wwwroot/uploads/* runtime contents (the .gitkeep CAN be included — Dockerfile build will create the directory either way via Plan 09-01's behavior)

The application's current run path (`./run.sh`) wraps `dotnet run --project src/CookBot.Web`. The Dockerfile must build the published artifact, not source-mount.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 0: PACKAGE LEGITIMACY GATE — confirm Microsoft.Extensions.Diagnostics.HealthChecks.EntityFrameworkCore 10.0.*</name>
  <what-built>
    Confirm the second NuGet package added in Phase 9.
  </what-built>
  <how-to-verify>
    1. Visit https://www.nuget.org/packages/Microsoft.Extensions.Diagnostics.HealthChecks.EntityFrameworkCore/
    2. Confirm publisher = Microsoft (verified-publisher tick)
    3. Confirm latest stable 10.0.x exists (~10.0.7 per 09-RESEARCH lines 104–106; floating 10.0.* is acceptable per 09-RESEARCH line 98)
    4. Confirm source repo link points at github.com/dotnet/aspnetcore
    5. Confirm download count is in the tens of millions/year
    6. 09-RESEARCH Package Legitimacy Audit (lines 108–115) lists this package as Approved.
  </how-to-verify>
  <action>Stop. Verify the package legitimacy per the steps in <how-to-verify> above using a browser. Do NOT proceed to Task 1 until the user confirms approval via the resume signal.</action>
  <verify>
    <automated>echo "Manual checkpoint — proceed only after user types approved"</automated>
  </verify>
  <done>User types "approved" — package legitimacy confirmed; Task 1 may proceed.</done>
  <resume-signal>Type "approved" or "blocked: {reason}"</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Add HealthChecks NuGet + Program.cs wiring for /healthz</name>
  <files>src/CookBot.Web/CookBot.Web.csproj, src/CookBot.Web/Program.cs</files>
  <read_first>
    - /home/noah/Desktop/projects/freeloves-cookbot/src/CookBot.Web/CookBot.Web.csproj (entire file)
    - /home/noah/Desktop/projects/freeloves-cookbot/src/CookBot.Web/Program.cs (entire file — note that Plan 09-04 added DataProtection registration; this plan inserts the HealthChecks block AFTER that)
    - /home/noah/Desktop/projects/freeloves-cookbot/.planning/phases/09-photos-prod-ready-infrastructure/09-RESEARCH.md lines 235–273 (healthcheck wire-up)
  </read_first>
  <action>
    1. Modify src/CookBot.Web/CookBot.Web.csproj — add PackageReference:
       <PackageReference Include="Microsoft.Extensions.Diagnostics.HealthChecks.EntityFrameworkCore" Version="10.0.*" />

       The floating-version pattern is acceptable per 09-RESEARCH line 98 — it tracks the DataProtection package's 10.0.* pinning, both staying current with .NET 10 service updates.

       Run `dotnet restore` to pull the package.

    2. Modify src/CookBot.Web/Program.cs:
       - Add `using Microsoft.Extensions.Diagnostics.HealthChecks;` if needed (the AddDbContextCheck extension is in Microsoft.Extensions.DependencyInjection which is already implicitly using'd).
       - After the AddDataProtection block from Plan 09-04 (and before app.Build()), insert:
         `builder.Services.AddHealthChecks()`
         `    .AddDbContextCheck<CookBotDbContext>(name: "database");`
         // PROD-05 / D-43: /healthz hits CookBotDbContext.Database.CanConnectAsync(). If the seeder fails the app doesn't start; this check guards against runtime DB disconnect.
       - After all existing middleware (app.UseAntiforgery, app.MapStaticAssets, the Plan 09-01 UseStaticFiles for /uploads, app.MapRazorComponents) and BEFORE the seeder scope or app.Run(), insert:
         `app.MapHealthChecks("/healthz");`
         The exact placement: after MapRazorComponents (line 39 area) and before the `using (var scope = app.Services.CreateScope())` seeder block (line 42 area) — OR before app.Run() at the very end. Either is correct; pick the natural mid-file placement.

    3. Run `dotnet build` (whole solution). dotnet test (whole suite). Both green.

    4. Manual smoke (in summary): start the app via `./run.sh`. In another terminal: `curl -fsS http://localhost:7000/healthz` returns 200 with a small JSON body. Stop the app. (Docker smoke is Task 3.)
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/freeloves-cookbot &amp;&amp; dotnet restore --nologo -v quiet &amp;&amp; dotnet build --nologo -v quiet &amp;&amp; dotnet test tests/CookBot.Tests/CookBot.Tests.csproj --no-build --nologo -v quiet &amp;&amp; grep -q 'Microsoft.Extensions.Diagnostics.HealthChecks.EntityFrameworkCore' src/CookBot.Web/CookBot.Web.csproj &amp;&amp; grep -q 'AddHealthChecks' src/CookBot.Web/Program.cs &amp;&amp; grep -q 'AddDbContextCheck<CookBotDbContext>' src/CookBot.Web/Program.cs &amp;&amp; grep -q 'MapHealthChecks("/healthz")' src/CookBot.Web/Program.cs</automated>
  </verify>
  <acceptance_criteria>
    - CookBot.Web.csproj contains the new HealthChecks.EntityFrameworkCore PackageReference
    - Program.cs registers AddHealthChecks() with AddDbContextCheck<CookBotDbContext> named "database"
    - Program.cs maps "/healthz" via MapHealthChecks BEFORE app.Run()
    - dotnet build + dotnet test full suite green
    - Manual smoke (in summary): curl /healthz returns 200 on the running app
  </acceptance_criteria>
  <done>PROD-05 closed (healthz endpoint exists; the seeder boot order from Plans 09-04 + 09-05 makes the (a) prerequisite implicit — if seeder fails, app never starts listening, so /healthz unreachable, so the operator sees the failure via docker ps + docker logs).</done>
</task>

<task type="auto">
  <name>Task 2: Dockerfile + .dockerignore</name>
  <files>Dockerfile, .dockerignore</files>
  <read_first>
    - /home/noah/Desktop/projects/freeloves-cookbot/src/CookBot.Web/CookBot.Web.csproj (entire — confirm target framework net10.0)
    - /home/noah/Desktop/projects/freeloves-cookbot/run.sh (entire — confirm current run mechanism)
    - /home/noah/Desktop/projects/freeloves-cookbot/.gitignore (Phase 9 Plan 09-01 added uploads entries; mirror the same patterns in .dockerignore)
    - /home/noah/Desktop/projects/freeloves-cookbot/.planning/phases/09-photos-prod-ready-infrastructure/09-RESEARCH.md lines 49 + Stack section
    - /home/noah/Desktop/projects/freeloves-cookbot/.planning/phases/09-photos-prod-ready-infrastructure/09-PATTERNS.md lines 421–439 (Dockerfile reference excerpt)
  </read_first>
  <action>
    1. Create Dockerfile at repo root with this verbatim shape (adjust the COPY layers if the solution structure differs from the standard `*.sln + src/ + tests/`):

       # syntax=docker/dockerfile:1.7
       # Phase 9 / PROD-01 / PROD-03 / D-43 — multi-stage build for FreelovesCookBot.
       # Build stage uses .NET 10 SDK; runtime stage uses ASP.NET 10 base (includes curl for healthcheck).

       FROM mcr.microsoft.com/dotnet/sdk:10.0 AS build
       WORKDIR /src
       # Copy project files first to maximize layer cache hits on dotnet restore.
       COPY *.sln ./
       COPY src/CookBot.Domain/*.csproj src/CookBot.Domain/
       COPY src/CookBot.Application/*.csproj src/CookBot.Application/
       COPY src/CookBot.Infrastructure/*.csproj src/CookBot.Infrastructure/
       COPY src/CookBot.Web/*.csproj src/CookBot.Web/
       COPY tests/CookBot.Tests/*.csproj tests/CookBot.Tests/
       RUN dotnet restore src/CookBot.Web/CookBot.Web.csproj
       # Copy remaining source and publish.
       COPY src/ src/
       RUN dotnet publish src/CookBot.Web/CookBot.Web.csproj -c Release -o /app /p:UseAppHost=false

       FROM mcr.microsoft.com/dotnet/aspnet:10.0 AS runtime
       WORKDIR /app
       COPY --from=build /app ./
       # PROD-03 + PITFALL M4: bind on 0.0.0.0; default localhost is unreachable from outside the container.
       ENV ASPNETCORE_URLS=http://+:7000
       # PITFALL M7: stable UTC inside the container — guarantees AiUsageLog Timestamp + DataProtection key rotation use a known clock.
       ENV TZ=UTC
       # Default port; compose can override via COOKBOT_PORT mapping.
       EXPOSE 7000
       ENTRYPOINT ["dotnet", "CookBot.Web.dll"]

       Note: `/p:UseAppHost=false` keeps the published artifact as `CookBot.Web.dll` (executed via `dotnet`), not a self-contained binary. This shrinks the image. The runtime image already has the .NET 10 runtime, so the dll is sufficient.

       The solution file: confirm by `ls *.sln` what the actual name is — if it's not `FreelovesCookBot.sln`, use whatever the actual `.sln` filename is. The `COPY *.sln ./` glob handles either case.

    2. Create .dockerignore at repo root with this content:

       # .NET / C# build artifacts
       **/bin/
       **/obj/
       **/publish/
       *.dll
       *.exe
       *.pdb

       # IDE
       .vs/
       .vscode/
       .idea/

       # OS
       .DS_Store
       Thumbs.db

       # VCS
       .git/
       .gitignore

       # GSD planning / docs — not needed inside the container
       .planning/
       *.md
       README.md
       LICENSE

       # Local runtime data — never leak operator's DB into a built image
       **/*.db
       **/*.db-wal
       **/*.db-shm
       **/*.db.pre-*.bak

       # User-uploaded photos at build time (runtime volume mounts here)
       src/CookBot.Web/wwwroot/uploads/*
       !src/CookBot.Web/wwwroot/uploads/.gitkeep

       # appsettings overrides (operator-specific; mounted/env at runtime, not baked)
       **/appsettings.Development.json
       **/appsettings.Production.json

       # Docker meta
       Dockerfile.*
       docker-compose.override*.yml

       Exception note: README.md IS excluded from the image even though Plan 09-07 will rewrite it. The container does not need the README; operators read it via the GitHub repo or their local checkout. If a future phase needs README inside the image (e.g. for a /about page), drop the README.md line from .dockerignore.

    3. Verify Dockerfile syntax: `docker build -t cookbot:plan-09-06 .` from repo root.

       Expected outcomes:
       - First build is slow (full restore + publish) — that's fine.
       - Build succeeds with exit code 0.
       - Image size is reasonable (≤ ~250 MB; ASP.NET 10 base is ~200 MB).
       - `docker images cookbot:plan-09-06` confirms the tag exists.

       If Docker is not available on the executor's machine (per 09-RESEARCH Environment Availability table), document this in the SUMMARY and defer the smoke test to a checkpoint with the user.
  </action>
  <verify>
    <automated>test -f Dockerfile &amp;&amp; test -f .dockerignore &amp;&amp; grep -q 'mcr.microsoft.com/dotnet/sdk:10.0' Dockerfile &amp;&amp; grep -q 'mcr.microsoft.com/dotnet/aspnet:10.0' Dockerfile &amp;&amp; grep -q 'ASPNETCORE_URLS=http://+:7000' Dockerfile &amp;&amp; grep -q 'TZ=UTC' Dockerfile &amp;&amp; grep -q 'EXPOSE 7000' Dockerfile &amp;&amp; grep -q 'ENTRYPOINT \["dotnet", "CookBot.Web.dll"\]' Dockerfile &amp;&amp; grep -q '**/*.db' .dockerignore &amp;&amp; grep -q 'src/CookBot.Web/wwwroot/uploads/\*' .dockerignore &amp;&amp; grep -q '!src/CookBot.Web/wwwroot/uploads/.gitkeep' .dockerignore</automated>
  </verify>
  <acceptance_criteria>
    - Dockerfile uses mcr.microsoft.com/dotnet/sdk:10.0 build stage and mcr.microsoft.com/dotnet/aspnet:10.0 runtime stage
    - Dockerfile sets ASPNETCORE_URLS=http://+:7000, TZ=UTC, EXPOSE 7000
    - Dockerfile ENTRYPOINT is exactly `["dotnet", "CookBot.Web.dll"]`
    - .dockerignore excludes bin/, obj/, .git/, .planning/, *.db, *.db-wal, *.db-shm, *.db.pre-*.bak, appsettings.Production.json, appsettings.Development.json, wwwroot/uploads/* (but allows .gitkeep)
    - If Docker is available: `docker build .` succeeds; image is tagged successfully
    - If Docker is unavailable on executor's machine: documented in summary; smoke-test deferred to checkpoint
  </acceptance_criteria>
  <done>PROD-01 closed; PROD-03 closed; PITFALL M4/M7 mitigations baked. The image is buildable; runtime test happens in Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: docker-compose.yml with named volumes, healthcheck stanza, restart policy + manual smoke</name>
  <files>docker-compose.yml</files>
  <read_first>
    - /home/noah/Desktop/projects/freeloves-cookbot/Dockerfile (just-created — confirm port 7000)
    - /home/noah/Desktop/projects/freeloves-cookbot/.planning/phases/09-photos-prod-ready-infrastructure/09-PATTERNS.md lines 443–478 (compose reference + volume names)
    - /home/noah/Desktop/projects/freeloves-cookbot/.planning/phases/09-photos-prod-ready-infrastructure/09-RESEARCH.md lines 258–273 (healthcheck stanza)
    - /home/noah/Desktop/projects/freeloves-cookbot/.planning/phases/09-photos-prod-ready-infrastructure/09-CONTEXT.md D-43 (restart: on-failure decision)
    - /home/noah/Desktop/projects/freeloves-cookbot/.planning/research/PITFALLS.md M5, M6 (WAL directory mount, restart policy)
  </read_first>
  <action>
    1. Create docker-compose.yml at repo root with this content:

       # Phase 9 / PROD-02 / PROD-04 / D-43 — single-service compose for FreelovesCookBot.
       # Two named volumes: cookbot_db mounts /data for cookbot.db + WAL + DataProtection keys (Plan 09-04 colocates the key ring in cookbot.db).
       # cookbot_uploads mounts /app/wwwroot/uploads.
       # restart: on-failure + healthcheck (D-43) makes startup failures visible instead of looping silently (PITFALL M6).

       services:
         cookbot:
           build: .
           image: freelovescookbot:latest
           container_name: cookbot
           ports:
             - "${COOKBOT_PORT:-7000}:7000"
           environment:
             ASPNETCORE_URLS: http://+:7000
             ConnectionStrings__DefaultConnection: "Data Source=/data/cookbot.db"
             COOKBOT_PORT: "7000"
             # PROD-19 hint: set CookBot__AnthropicApiKey here as a per-host fallback, or leave empty and rely on per-user keys.
             # CookBot__AnthropicApiKey: ""
           volumes:
             - cookbot_db:/data
             - cookbot_uploads:/app/wwwroot/uploads
           restart: on-failure
           healthcheck:
             test: ["CMD", "curl", "-f", "http://localhost:7000/healthz"]
             interval: 30s
             timeout: 5s
             start_period: 30s
             retries: 3

       volumes:
         cookbot_db: {}
         cookbot_uploads: {}

       Notes for executor:
       - The Data Source path inside the container is `/data/cookbot.db` because cookbot_db mounts at /data. The Plan 09-04 PersistKeysToDbContext keys live in this same SQLite file — only ONE volume backs both data and keys (D-43 + Plan 09-04 invariant).
       - The wwwroot/uploads path inside the container is `/app/wwwroot/uploads` because the Dockerfile WORKDIR is /app and the published content is COPYed there. The compose mount lands volume cookbot_uploads at exactly that location, overlaying any directory the publish step left there (including a possible .gitkeep — that's fine; it gets shadowed by the volume).
       - On first `docker compose up`, the seeder runs migrations (including AddDataProtectionKeysTable + AddAiUsageLog from Plans 09-04 + 09-05), then the null-canonical guard, then the 365-day cleanup, then the sentinel-prefix re-encryption pass, then existing seed logic. Backup files (cookbot.db.pre-*.bak) land in /data alongside cookbot.db.

    2. Manual smoke (Docker required — if unavailable, document and defer):
       a. `docker compose config` — exit 0 (validates compose syntax without launching).
       b. `docker compose up --build -d` — builds, starts, returns to prompt.
       c. Wait ~35 seconds (matches the 30s start_period in the healthcheck).
       d. `docker ps --filter name=cookbot --format "table {{.Names}}\t{{.Status}}"` — confirms status includes `(healthy)`.
       e. From the host: `curl -fsS http://localhost:7000/` returns the app's HTML. `curl -fsS http://localhost:7000/healthz` returns 200 + healthy JSON.
       f. `docker compose stop` then `docker compose start` — confirm the data + uploads + key ring all survive. Set an AI key via EditProfile (or leave the smoke at the manual step) and confirm decryption still works.
       g. `docker compose down` (NO -v flag — keep the volumes for future runs).

    3. If Docker is unavailable, the .yml file syntax check uses `docker compose config` if available; otherwise the executor flags it as a manual checkpoint for the user to run.
  </action>
  <verify>
    <automated>test -f docker-compose.yml &amp;&amp; grep -q 'services:' docker-compose.yml &amp;&amp; grep -q 'cookbot_db:/data' docker-compose.yml &amp;&amp; grep -q 'cookbot_uploads:/app/wwwroot/uploads' docker-compose.yml &amp;&amp; grep -q 'restart: on-failure' docker-compose.yml &amp;&amp; ! grep -q 'restart: unless-stopped' docker-compose.yml &amp;&amp; grep -q 'healthcheck:' docker-compose.yml &amp;&amp; grep -q 'http://localhost:7000/healthz' docker-compose.yml &amp;&amp; grep -q 'start_period: 30s' docker-compose.yml &amp;&amp; grep -q 'retries: 3' docker-compose.yml &amp;&amp; grep -q 'COOKBOT_PORT' docker-compose.yml &amp;&amp; grep -q '/data/cookbot.db' docker-compose.yml &amp;&amp; (command -v docker >/dev/null 2>&amp;1 &amp;&amp; docker compose config >/dev/null 2>&amp;1 || echo "Docker unavailable — compose config skipped per Environment Availability")</automated>
  </verify>
  <acceptance_criteria>
    - docker-compose.yml uses restart: on-failure (NOT unless-stopped) per D-43
    - Two named volumes (cookbot_db, cookbot_uploads); NO third volume for keys (Plan 09-04 invariant)
    - Volumes mount at /data and /app/wwwroot/uploads inside the container
    - healthcheck stanza uses curl -f http://localhost:7000/healthz with interval 30s, timeout 5s, start_period 30s, retries 3
    - ConnectionStrings__DefaultConnection points at /data/cookbot.db (using the double-underscore .NET config delimiter)
    - COOKBOT_PORT environment variable defaults to 7000 via "${COOKBOT_PORT:-7000}" interpolation
    - `docker compose config` exits 0 (or — if Docker is unavailable on the executor's machine — the SUMMARY explicitly documents that this is the case and flags the smoke for the user to run)
    - Manual smoke (in SUMMARY if Docker is available): docker compose up + healthz check + stop/start cycle confirming data + key-ring persist
  </acceptance_criteria>
  <done>PROD-02 closed; PROD-04 closed (WAL files colocate in /data per PITFALL M5); D-43 restart-on-failure + healthcheck overrides PROD-02's unless-stopped (PITFALL M6 first-option-preferred). The deployment story is end-to-end testable: a fresh `docker compose up` on a clean machine starts the app, /healthz returns 200 within 35s, and a stop+start cycle preserves all data + encrypted keys.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host → container network | Port 7000 exposed; default 0.0.0.0:7000 binding (trusted LAN posture) |
| Container → host volume | Two volumes (cookbot_db, cookbot_uploads); operator owns backup |
| Build context → image | .dockerignore prevents operator's local DB / planning docs from leaking into the image |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06-01 | Information Disclosure | Operator's local cookbot.db copied into image layers | mitigate | .dockerignore excludes all *.db and *.db.pre-*.bak patterns; volume mount overlays /data at runtime so even if it leaked through to the layer, runtime uses the named volume. |
| T-09-06-02 | Denial of Service | Silent restart loop hides migration failure | mitigate | restart: on-failure (not unless-stopped) + healthcheck retries: 3 — container exits visibly after 3 failed seeders; operator sees the failure via `docker ps`. |
| T-09-06-03 | Information Disclosure | Container binds 127.0.0.1 and is unreachable from LAN | mitigate | ASPNETCORE_URLS=http://+:7000 in Dockerfile ENV (PITFALL M4); `+` is .NET's wildcard binding equivalent to 0.0.0.0. |
| T-09-06-04 | Tampering | SQLite WAL files end up on a non-mounted directory | mitigate | Connection string points at /data/cookbot.db; SQLite writes cookbot.db-wal + cookbot.db-shm in the SAME directory (per PITFALL M5); named volume cookbot_db preserves all three. |
| T-09-06-05 | Information Disclosure | TLS/HTTPS unavailable inside container | accept | Trusted-LAN posture per CookBotSettings.AuthMode.Disabled. README PROD-20 (Plan 09-07) tells operators to put a reverse proxy (Caddy/nginx) in front for TLS termination. |
| T-09-06-06 | Spoofing | container_name "cookbot" hardcoded — collision with operator's existing container | accept | Operator can remove or rename the container_name; cosmetic. Documented in README. |
| T-09-06-SC | Tampering (supply chain) | base image tags mcr.microsoft.com/dotnet/sdk:10.0, aspnet:10.0 | accept | First-party Microsoft Container Registry; tags pinned to .NET 10 line. Floating tag is acceptable since Microsoft signs all official .NET images. |
| T-09-06-SC2 | Tampering (supply chain) | Microsoft.Extensions.Diagnostics.HealthChecks.EntityFrameworkCore NuGet | mitigate | Task 0 BLOCKING-HUMAN gate. |
</threat_model>

<verification>
- /healthz returns 200 on dev (./run.sh) and inside docker compose.
- `docker compose config` exits 0 (or executor flags Docker absence in SUMMARY).
- If Docker is available: full smoke per Task 3 step 2 — up, wait 35s, healthcheck reports healthy, stop, start, data + key ring survive.
- Full test suite remains green (HealthChecks code paths covered by manual smoke; no new automated test required for Phase 9 scope per 09-RESEARCH validation map).
</verification>

<success_criteria>
1. NuGet added; Program.cs registers AddHealthChecks + AddDbContextCheck + MapHealthChecks("/healthz") in correct order
2. Dockerfile multi-stage with sdk:10.0 → aspnet:10.0; ASPNETCORE_URLS / TZ / EXPOSE / ENTRYPOINT all per spec
3. docker-compose.yml uses restart: on-failure (NOT unless-stopped); two named volumes; healthcheck stanza per D-43
4. .dockerignore prevents *.db, *.planning/, bin/, obj/, etc. from leaking into image
5. Manual smoke documented in SUMMARY: /healthz returns 200; if Docker available, full up/stop/start cycle preserves data + key ring + uploads
6. dotnet build + dotnet test full suite green; no regression in Phase 8 tests
</success_criteria>

<output>
Create .planning/phases/09-photos-prod-ready-infrastructure/09-06-SUMMARY.md when done. Include: csproj diff for the new NuGet; the full Dockerfile + docker-compose.yml + .dockerignore content (operator-visible artifacts); whether Docker was available on executor's machine and smoke results if so; the exact path to /healthz response example (`curl http://localhost:7000/healthz` JSON).
</output>
