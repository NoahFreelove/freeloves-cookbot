---
phase: 09-photos-prod-ready-infrastructure
plan: 05
type: execute
wave: 4
depends_on: ["09-04"]
files_modified:
  - src/CookBot.Domain/Entities/AiUsageLog.cs
  - src/CookBot.Infrastructure/Data/Configurations/AiUsageLogConfiguration.cs
  - src/CookBot.Infrastructure/Data/CookBotDbContext.cs
  - src/CookBot.Application/DTOs/CookBotSettings.cs
  - src/CookBot.Application/DTOs/AiPricingEntry.cs
  - src/CookBot.Web/appsettings.json
  - src/CookBot.Infrastructure/AI/AnthropicAiService.cs
  - src/CookBot.Application/AI/StructuredResult.cs
  - src/CookBot.Application/AI/AiRecipeGenerator.cs
  - src/CookBot.Application/Services/PromptBuilderService.cs
  - src/CookBot.Infrastructure/Data/DatabaseSeeder.cs
  - src/CookBot.Application/Services/RecipePhotoUrlValidator.cs
  - src/CookBot.Infrastructure/Migrations/<timestamp>_AddAiUsageLog.cs
  - tests/CookBot.Tests/AI/AnthropicAiServiceTokenTests.cs
  - tests/CookBot.Tests/AI/TokenTelemetryTests.cs
  - tests/CookBot.Tests/Configuration/AiPricingTests.cs
  - tests/CookBot.Tests/Snapshots/PromptSnapshotTests.BuildSystemPrompt.verified.txt
autonomous: true
requirements: [PROD-12, PROD-13, PROD-14, PROD-15, PROD-16, PROD-17, PHOTO-07]
must_haves:
  truths:
    - "AnthropicAiService SSE parse loop captures input_tokens from message_start.message.usage (PROD-12)"
    - "AnthropicAiService SSE parse loop captures cumulative output_tokens from message_delta.usage — LAST value wins, NOT a sum (PROD-12, 09-RESEARCH correction)"
    - "StructuredResult<T> record gains int InputTokens, int OutputTokens with default 0 (PROD-13)"
    - "AiUsageLog entity (Id, UserId, KeyOwnerId, ModelName, InputTokens, OutputTokens, EstimatedCostUsd, IsRetryAttempt, Timestamp) lands with EF composite index IX_AiUsageLogs_KeyOwnerId_Timestamp (PROD-14, PITFALL M8)"
    - "AiRecipeGenerator.GenerateAsync writes ONE AiUsageLog row PER ATTEMPT at the END of generation (consolidated, not inside the retry loop body), with IsRetryAttempt=true for retries (PROD-15, PITFALL H9)"
    - "Per-model pricing lives in appsettings.json under CookBot:AiPricing keyed by CuratedModels IDs; PricingVerifiedDate = 2026-05-16 (PROD-16, PITFALL H10)"
    - "EstimatedCostUsd computed via decimal arithmetic: (InputTokens * Input$/1M + OutputTokens * Output$/1M) / 1_000_000m (09-RESEARCH currency math; column type decimal(18,6))"
    - "Telemetry write only happens when CookBotSettings.AiFeaturesEnabled AND UserProfile.AiEnabled — the v1.2 AI-off contract is preserved"
    - "PromptBuilderService.BuildSystemPrompt has D-42 prose clauses: description = '1–2 sentence summary; no history, no cooking advice', steps = 'begin with the first cooking action — no introductory paragraph as step 1'; Phase 8 Verify snapshot regenerates in the same commit"
    - "AnthropicAiService AI return path runs RecipePhotoUrlValidator on emitted PhotoUrl; invalid AI-emitted URLs are nulled (PHOTO-07, PITFALL H5)"
    - "365-day AiUsageLog cleanup runs at boot in DatabaseSeeder via ExecuteDeleteAsync, INSERTED at the marker comment Plan 09-04 placed; runs BEFORE the sentinel-prefix re-encryption pass (D-41)"
  artifacts:
    - path: "src/CookBot.Domain/Entities/AiUsageLog.cs"
      provides: "POCO log row for AI calls"
      contains: "AiUsageLog"
    - path: "src/CookBot.Infrastructure/Data/Configurations/AiUsageLogConfiguration.cs"
      provides: "Composite index IX_AiUsageLogs_KeyOwnerId_Timestamp; decimal(18,6) for EstimatedCostUsd; two FKs to User (UserId Cascade, KeyOwnerId Restrict)"
      contains: "IX_AiUsageLog"
    - path: "src/CookBot.Application/DTOs/AiPricingEntry.cs"
      provides: "POCO {decimal InputTokensPerMillionUsd, decimal OutputTokensPerMillionUsd}"
    - path: "src/CookBot.Application/DTOs/CookBotSettings.cs"
      provides: "Dictionary<string, AiPricingEntry>? AiPricing + DateOnly? AiPricingVerifiedDate"
    - path: "src/CookBot.Web/appsettings.json"
      provides: "CookBot:AiPricing entries for claude-haiku-4-5-20251001, claude-sonnet-4-6, claude-opus-4-7; AiPricingVerifiedDate 2026-05-16"
    - path: "src/CookBot.Application/AI/StructuredResult.cs"
      provides: "Backwards-compatible record gains InputTokens, OutputTokens with default 0"
    - path: "src/CookBot.Infrastructure/AI/AnthropicAiService.cs"
      provides: "SSE message_start.usage.input_tokens + message_delta.usage.output_tokens (cumulative); PhotoUrl validator wired on return path"
    - path: "src/CookBot.Application/AI/AiRecipeGenerator.cs"
      provides: "Telemetry write at END of GenerateAsync — list of (attempt, result) pairs accumulated through the loop, then one DbContext write per pair with IsRetryAttempt for retries"
    - path: "src/CookBot.Application/Services/PromptBuilderService.cs"
      provides: "D-42 prose clauses appended to system prompt"
    - path: "src/CookBot.Infrastructure/Migrations/*_AddAiUsageLog.cs"
      provides: "CreateTable AiUsageLogs + composite index"
    - path: "tests/CookBot.Tests/Snapshots/PromptSnapshotTests.BuildSystemPrompt.verified.txt"
      provides: "Regenerated Verify snapshot reflecting D-42 prose"
  key_links:
    - from: "AnthropicAiService.SendStructuredAsync"
      to: "RecipePhotoUrlValidator"
      via: "Apply validator to deserialized doc.PhotoUrl; null out invalid URLs before returning StructuredResult"
      pattern: "RecipePhotoUrlValidator.*TryValidate"
    - from: "AiRecipeGenerator.GenerateAsync"
      to: "AiUsageLog write"
      via: "Accumulate per-attempt (result, isRetry) tuples; write at function exit"
      pattern: "AiUsageLog"
    - from: "DatabaseSeeder.SeedAsync"
      to: "365-day cleanup"
      via: "ExecuteDeleteAsync against AiUsageLogs where Timestamp < cutoff"
      pattern: "AiUsageLogs.*ExecuteDeleteAsync"
---

<objective>
Ship the AI token-cost telemetry write path end-to-end: capture per-call usage in AnthropicAiService SSE parsing, plumb through StructuredResult, write one AiUsageLog row per attempt (with IsRetryAttempt for repair retries) at the END of AiRecipeGenerator.GenerateAsync, and price every row from appsettings.json. Add the D-42 prompt prose and regenerate the Verify snapshot atomically. Wire the AI-return-path photo URL validation (PHOTO-07 second half). Insert the D-41 365-day cleanup pass into DatabaseSeeder at the marker Plan 09-04 placed.

Purpose: This is the only place in the codebase that knows what each AI call cost. The write site is consolidated at the function END (not inside the retry loop body) so PITFALL H9 (double-count when the loop reuses a counter variable) cannot occur. message_delta.usage.output_tokens is cumulative per the Anthropic streaming spec — capturing the LAST seen value instead of summing prevents the n*(n+1)/2 over-count. Pricing lives in config so self-hosters update it when Anthropic raises prices. PROD-17's read widget is Phase 10's concern; this plan ships the WRITE path that PROD-17 will read from.

Output: One new domain entity + configuration + migration, two AI service modifications, one application-service modification, one prompt change with snapshot regen, one DatabaseSeeder cleanup insertion, three test files, and an appsettings.json delta.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-photos-prod-ready-infrastructure/09-CONTEXT.md
@.planning/phases/09-photos-prod-ready-infrastructure/09-RESEARCH.md
@.planning/phases/09-photos-prod-ready-infrastructure/09-PATTERNS.md
@.planning/research/PITFALLS.md
@.planning/phases/09-photos-prod-ready-infrastructure/09-04-SUMMARY.md
@src/CookBot.Domain/Entities/RecipeMade.cs
@src/CookBot.Infrastructure/Data/Configurations/RecipeMadeConfiguration.cs
@src/CookBot.Infrastructure/AI/AnthropicAiService.cs
@src/CookBot.Application/AI/StructuredResult.cs
@src/CookBot.Application/AI/AiRecipeGenerator.cs
@src/CookBot.Application/AI/IStructuredAiService.cs
@src/CookBot.Application/AI/IAiRecipeGenerator.cs
@src/CookBot.Application/Services/PromptBuilderService.cs
@src/CookBot.Application/DTOs/CookBotSettings.cs
@src/CookBot.Web/appsettings.json
@src/CookBot.Infrastructure/Data/DatabaseSeeder.cs
@tests/CookBot.Tests/AI/AiRecipeGeneratorTests.cs

<interfaces>
<!-- Anchors for the executor. -->

RecipeMade analog (src/CookBot.Domain/Entities/RecipeMade.cs lines 1–19): POCO with Id, RecipeId, UserId, CompletedAt, Notes, plus two navs. AiUsageLog mirrors this shape with the field map from 09-PATTERNS.md lines 63–77.

RecipeMadeConfiguration analog (src/CookBot.Infrastructure/Data/Configurations/RecipeMadeConfiguration.cs lines 1–26): composite index on (UserId, CompletedAt) + (RecipeId, CompletedAt). Per Phase 9 PROD-14: AiUsageLog gets composite index on (KeyOwnerId, Timestamp) — DESC hint is a comment (SQLite ignores direction on single-column index parts).

CookBotDbContext (src/CookBot.Infrastructure/Data/CookBotDbContext.cs): Phase 9 Plan 09-04 already added DbSet<DataProtectionKey>. Add `public DbSet<AiUsageLog> AiUsageLogs => Set<AiUsageLog>();` next to existing DbSets. OnModelCreating.ApplyConfigurationsFromAssembly (line 30) picks up the new configuration automatically.

AnthropicAiService SSE parse loop (src/CookBot.Infrastructure/AI/AnthropicAiService.cs lines 284–317): existing handlers for content_block_delta (line 296) and message_delta (line 306 — captures stop_reason). Phase 9 adds:
- New `else if (type == "message_start")` branch capturing usage.input_tokens
- Inside existing message_delta branch, capture usage.output_tokens (cumulative — overwrite, do not sum)

StructuredResult<T> record (src/CookBot.Application/AI/StructuredResult.cs lines 15–21): currently 5 positional params (Ok, Value, RawResponse, Validation, SanitizedError). Phase 9 adds InputTokens and OutputTokens with `= 0` defaults per record's positional-params shape — Records allow defaults in the primary constructor. Backwards-compatible because all existing constructors pass 5 args; the new params default to 0.

AnthropicAiService.SendStructuredAsync return sites (per 09-PATTERNS.md line 972): lines 322, 335, 344, 352, 361, 370 — each new StructuredResult<T>(...) call must add InputTokens, OutputTokens at the end. Only the SUCCESS path (line 322 / "Ok=true" return) has meaningful values; error returns pass 0 (default).

AiRecipeGenerator.GenerateAsync (src/CookBot.Application/AI/AiRecipeGenerator.cs lines 40–119): initial call (line 56), repair loop (line 80–106). Per PITFALL H9 the telemetry write must NOT live inside the loop body. Pattern: accumulate a `List<(StructuredResult<RecipeDocument> result, bool isRetry)> attempts` through the function, then at every `return` site write all rows atomically.

PromptBuilderService.BuildSystemPrompt (src/CookBot.Application/Services/PromptBuilderService.cs): Phase 8 CLEAN-03 made this output byte-stable via Verify.Xunit snapshot. D-42 adds two prose clauses. The snapshot file must be regenerated in the same commit; the executor runs the snapshot test, accepts the .received → .verified rename (or uses the Verify CLI), and commits both the source change AND the .verified file.

The 365-day cleanup insertion point in DatabaseSeeder.SeedAsync is the marker comment from Plan 09-04: `// 365-day AiUsageLog cleanup will be inserted here by Plan 09-05`. Plan 09-05 replaces this comment with the actual ExecuteDeleteAsync code (per 09-RESEARCH Item 4 lines 280–289). The cleanup runs BEFORE the sentinel-prefix re-encryption pass.

appsettings.json (src/CookBot.Web/appsettings.json lines 12–17): current shape:
{
  "Logging": { ... },
  "AllowedHosts": "*",
  "ConnectionStrings": { ... },
  "CookBot": {
    "AuthMode": "Disabled",
    "AppName": "CookBot",
    "AiFeaturesEnabled": true,
    "AnthropicApiKey": ""
  }
}
Phase 9 ADDS under CookBot the AiPricing block and AiPricingVerifiedDate (per 09-RESEARCH Item 1 lines 132–151).

CookBotSettings POCO (src/CookBot.Application/DTOs/CookBotSettings.cs): add two properties — `Dictionary<string, AiPricingEntry>? AiPricing` (nullable; binding from config produces null if absent) and `DateOnly? AiPricingVerifiedDate`. The Phase 9 read site uses TryGetValue with fallback to default $1/$5 to handle missing entries gracefully.

AiPricingEntry POCO (new file src/CookBot.Application/DTOs/AiPricingEntry.cs): two decimal properties, matching the JSON keys exactly: InputTokensPerMillionUsd and OutputTokensPerMillionUsd.

AnthropicAiService.CuratedModels list (src/CookBot.Infrastructure/AI/AnthropicAiService.cs line 18): three model IDs — claude-haiku-4-5-20251001, claude-sonnet-4-6 (DefaultModelId line 25), claude-opus-4-7. Pricing keys MUST exactly match these IDs.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AiUsageLog entity + configuration + DbSet + EF migration + AiPricing config wiring (no behavior change yet)</name>
  <files>src/CookBot.Domain/Entities/AiUsageLog.cs, src/CookBot.Infrastructure/Data/Configurations/AiUsageLogConfiguration.cs, src/CookBot.Infrastructure/Data/CookBotDbContext.cs, src/CookBot.Application/DTOs/AiPricingEntry.cs, src/CookBot.Application/DTOs/CookBotSettings.cs, src/CookBot.Web/appsettings.json, tests/CookBot.Tests/Configuration/AiPricingTests.cs</files>
  <read_first>
    - /home/noah/Desktop/projects/freeloves-cookbot/src/CookBot.Domain/Entities/RecipeMade.cs (entire file — POCO shape analog)
    - /home/noah/Desktop/projects/freeloves-cookbot/src/CookBot.Infrastructure/Data/Configurations/RecipeMadeConfiguration.cs (entire file — composite index + FK analog)
    - /home/noah/Desktop/projects/freeloves-cookbot/src/CookBot.Infrastructure/Data/CookBotDbContext.cs (entire file — DbSet placement)
    - /home/noah/Desktop/projects/freeloves-cookbot/src/CookBot.Application/DTOs/CookBotSettings.cs (entire file)
    - /home/noah/Desktop/projects/freeloves-cookbot/src/CookBot.Web/appsettings.json (entire file)
    - /home/noah/Desktop/projects/freeloves-cookbot/.planning/phases/09-photos-prod-ready-infrastructure/09-RESEARCH.md lines 119–155 (pricing matrix + appsettings shape)
    - /home/noah/Desktop/projects/freeloves-cookbot/.planning/phases/09-photos-prod-ready-infrastructure/09-PATTERNS.md lines 34–127, 367–417, 869–909 (entity, configuration, appsettings patterns)
    - /home/noah/Desktop/projects/freeloves-cookbot/src/CookBot.Infrastructure/Migrations/20260516034336_AddRecipeTagTable.cs (analog for CreateTable + index + FK migration)
  </read_first>
  <behavior>
    AiUsageLog entity fields (POCO, namespace CookBot.Domain.Entities, no framework refs):
    - int Id
    - int UserId — who triggered the AI call
    - int KeyOwnerId — whose key paid for the call (PITFALL C2 owner-share semantics)
    - string ModelName — one of the CuratedModels IDs; max length 80
    - int InputTokens
    - int OutputTokens
    - decimal EstimatedCostUsd
    - bool IsRetryAttempt (default false)
    - DateTime Timestamp (default DateTime.UtcNow)
    - User User { get; set; } = null!;
    - User KeyOwner { get; set; } = null!;

    AiUsageLogConfiguration:
    - HasKey(l => l.Id)
    - Property(l => l.ModelName).HasMaxLength(80).IsRequired()
    - Property(l => l.EstimatedCostUsd).HasColumnType("decimal(18, 6)") — sub-cent precision for $0.00035-class Haiku calls
    - HasIndex(l => new { l.KeyOwnerId, l.Timestamp }).HasDatabaseName("IX_AiUsageLogs_KeyOwnerId_Timestamp") — DESC hint in code comment only
    - HasOne(l => l.User).WithMany().HasForeignKey(l => l.UserId).OnDelete(DeleteBehavior.Cascade)
    - HasOne(l => l.KeyOwner).WithMany().HasForeignKey(l => l.KeyOwnerId).OnDelete(DeleteBehavior.Restrict) — telemetry survives even if the key owner is deleted

    AiPricingEntry POCO (CookBot.Application.DTOs namespace):
    - decimal InputTokensPerMillionUsd
    - decimal OutputTokensPerMillionUsd

    CookBotSettings additions:
    - Dictionary<string, AiPricingEntry>? AiPricing { get; set; }
    - DateOnly? AiPricingVerifiedDate { get; set; }

    appsettings.json additions (per 09-RESEARCH Item 1 values):
    - "AiPricing": { "claude-haiku-4-5-20251001": {Input 1.00, Output 5.00}, "claude-sonnet-4-6": {Input 3.00, Output 15.00}, "claude-opus-4-7": {Input 5.00, Output 25.00} }
    - "AiPricingVerifiedDate": "2026-05-16"

    AiPricingTests:
    - Configuration_LoadsAllThreeModels — bind appsettings.json fragment through IConfiguration and assert all three models appear with correct values.
    - PricingVerifiedDate_Is2026_05_16 — assert the date binds correctly.
    - CostCalculation_HaikuExample_BelowOneCent — for InputTokens=100, OutputTokens=50 against Haiku, compute (100*1m + 50*5m) / 1_000_000m = $0.00035 → assert exactly 0.00035m (no rounding to zero).
  </behavior>
  <action>
    RED phase: Write tests/CookBot.Tests/Configuration/AiPricingTests.cs first. Use Microsoft.Extensions.Configuration to load an in-memory IConfiguration with the literal JSON from 09-RESEARCH Item 1. Bind to CookBotSettings via `configuration.GetSection("CookBot").Get<CookBotSettings>()`. Run filter `AiPricingTests` — MUST fail (red — AiPricing type does not exist).

    GREEN phase:
    1. Create src/CookBot.Application/DTOs/AiPricingEntry.cs — `public sealed class AiPricingEntry { public decimal InputTokensPerMillionUsd { get; set; } public decimal OutputTokensPerMillionUsd { get; set; } }`. Namespace CookBot.Application.DTOs.
    2. Modify src/CookBot.Application/DTOs/CookBotSettings.cs — add the two new properties with default `null`.
    3. Modify src/CookBot.Web/appsettings.json — add the AiPricing block + AiPricingVerifiedDate per 09-RESEARCH Item 1. Verify JSON validity via `python3 -m json.tool < src/CookBot.Web/appsettings.json` exit 0.
    4. Create src/CookBot.Domain/Entities/AiUsageLog.cs — POCO per behavior. Doc comment cites Phase 9 / Plan 09-05 / powers Phase 10 PROD-17 widget. Namespace CookBot.Domain.Entities.
    5. Create src/CookBot.Infrastructure/Data/Configurations/AiUsageLogConfiguration.cs — EntityTypeConfiguration mirroring RecipeMadeConfiguration. The HasDatabaseName for the composite index ensures EF emits exactly `IX_AiUsageLogs_KeyOwnerId_Timestamp` (without it EF generates `IX_AiUsageLogs_KeyOwnerId_Timestamp` which is the same name by convention, but explicit is safer for the grep gate).
    6. Modify src/CookBot.Infrastructure/Data/CookBotDbContext.cs — add `public DbSet<AiUsageLog> AiUsageLogs => Set<AiUsageLog>();` next to existing DbSets.
    7. Run `dotnet ef migrations add AddAiUsageLog --project src/CookBot.Infrastructure --startup-project src/CookBot.Web` — generated migration creates AiUsageLogs table with composite index + FKs.
    8. Run `dotnet build` and `dotnet test --filter "FullyQualifiedName~AiPricingTests"` — must pass.
    9. Run full test suite — must remain green.

    No telemetry-write code yet. No SSE changes yet. This task only lands the data/config foundation.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/freeloves-cookbot &amp;&amp; dotnet build --nologo -v quiet &amp;&amp; dotnet test tests/CookBot.Tests/CookBot.Tests.csproj --no-build --nologo -v quiet &amp;&amp; ls src/CookBot.Infrastructure/Migrations/*_AddAiUsageLog.cs 1>/dev/null &amp;&amp; grep -q 'DbSet<AiUsageLog>' src/CookBot.Infrastructure/Data/CookBotDbContext.cs &amp;&amp; grep -q 'IX_AiUsageLogs_KeyOwnerId_Timestamp' src/CookBot.Infrastructure/Data/Configurations/AiUsageLogConfiguration.cs &amp;&amp; grep -q 'AiPricing' src/CookBot.Web/appsettings.json &amp;&amp; grep -q '2026-05-16' src/CookBot.Web/appsettings.json &amp;&amp; python3 -m json.tool < src/CookBot.Web/appsettings.json > /dev/null</automated>
  </verify>
  <acceptance_criteria>
    - AiUsageLog entity exists with all 11 properties + two navigation properties
    - AiUsageLogConfiguration declares composite index on (KeyOwnerId, Timestamp) with HasDatabaseName "IX_AiUsageLogs_KeyOwnerId_Timestamp"; decimal(18,6) on EstimatedCostUsd
    - CookBotDbContext exposes DbSet<AiUsageLog>
    - AiPricingEntry POCO exists in CookBot.Application.DTOs
    - CookBotSettings has Dictionary<string, AiPricingEntry>? AiPricing and DateOnly? AiPricingVerifiedDate
    - appsettings.json contains all three CuratedModels keys with the exact pricing values from 09-RESEARCH Item 1; AiPricingVerifiedDate = "2026-05-16"
    - EF migration *_AddAiUsageLog.cs exists and applies cleanly
    - AiPricingTests pass (3 facts)
    - dotnet build exit 0; full test suite green
  </acceptance_criteria>
  <done>Data + config foundation in place. PROD-14 entity + index shipped. PROD-16 pricing config shipped. Telemetry-write site, SSE capture, and prompt change are Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SSE usage capture + StructuredResult fields + AiRecipeGenerator write site + PromptBuilderService D-42 prose + DatabaseSeeder 365-day cleanup + RecipePhotoUrlValidator on AI return path</name>
  <files>src/CookBot.Infrastructure/AI/AnthropicAiService.cs, src/CookBot.Application/AI/StructuredResult.cs, src/CookBot.Application/AI/AiRecipeGenerator.cs, src/CookBot.Application/Services/PromptBuilderService.cs, src/CookBot.Infrastructure/Data/DatabaseSeeder.cs, tests/CookBot.Tests/AI/AnthropicAiServiceTokenTests.cs, tests/CookBot.Tests/AI/TokenTelemetryTests.cs, tests/CookBot.Tests/Snapshots/PromptSnapshotTests.BuildSystemPrompt.verified.txt</files>
  <read_first>
    - /home/noah/Desktop/projects/freeloves-cookbot/src/CookBot.Infrastructure/AI/AnthropicAiService.cs (entire file — focus on SendStructuredAsync lines 192–380 including SSE parse loop)
    - /home/noah/Desktop/projects/freeloves-cookbot/src/CookBot.Application/AI/StructuredResult.cs (entire file)
    - /home/noah/Desktop/projects/freeloves-cookbot/src/CookBot.Application/AI/AiRecipeGenerator.cs (entire file — 2-retry loop structure)
    - /home/noah/Desktop/projects/freeloves-cookbot/src/CookBot.Application/AI/IStructuredAiService.cs (entire file — interface contract)
    - /home/noah/Desktop/projects/freeloves-cookbot/src/CookBot.Application/AI/IAiRecipeGenerator.cs (entire file)
    - /home/noah/Desktop/projects/freeloves-cookbot/src/CookBot.Application/Services/PromptBuilderService.cs (entire file — BuildSystemPrompt method)
    - /home/noah/Desktop/projects/freeloves-cookbot/src/CookBot.Infrastructure/Data/DatabaseSeeder.cs (entire file — locate the marker comment from Plan 09-04)
    - /home/noah/Desktop/projects/freeloves-cookbot/tests/CookBot.Tests/AI/AiRecipeGeneratorTests.cs (entire file — RecordingFakeStructuredAi pattern)
    - /home/noah/Desktop/projects/freeloves-cookbot/tests/CookBot.Tests/AI/PromptSnapshotTests.cs (entire file — locate from Phase 8 CLEAN-03)
    - /home/noah/Desktop/projects/freeloves-cookbot/.planning/phases/09-photos-prod-ready-infrastructure/09-RESEARCH.md lines 275–298, 499–504, 911–972 (cleanup query, output_tokens cumulative pitfall, SSE pattern)
    - /home/noah/Desktop/projects/freeloves-cookbot/.planning/phases/09-photos-prod-ready-infrastructure/09-PATTERNS.md lines 911–972 (Pattern F SSE sibling case)
  </read_first>
  <behavior>
    AnthropicAiService SSE additions:
    - Track `int inputTokens = 0; int outputTokens = 0;` as method-local variables in SendStructuredAsync.
    - New case `else if (type == "message_start")` reads evt.RootElement.GetProperty("message").GetProperty("usage").GetProperty("input_tokens").GetInt32() and assigns to inputTokens. Defensive TryGetProperty chain — missing fields silently default to 0.
    - Inside existing `else if (type == "message_delta")` block (around line 306), in ADDITION to capturing stop_reason, ALSO read evt.RootElement.GetProperty("delta").GetProperty("usage").GetProperty("output_tokens").GetInt32() and OVERWRITE outputTokens (cumulative semantics per 09-RESEARCH pitfall #4 lines 499–504 — last value wins, NEVER `+=`).
    - All return sites in SendStructuredAsync (lines 322, 335, 344, 352, 361, 370 per 09-PATTERNS.md line 972) propagate inputTokens, outputTokens into the new StructuredResult<T>(...) ctor.

    StructuredResult<T> backwards-compatible extension:
    - Add `int InputTokens = 0` and `int OutputTokens = 0` to the primary-constructor parameter list. The existing 5 positional args remain in the same order; the new params append with defaults so existing call sites still compile unchanged.

    AiRecipeGenerator.GenerateAsync write path (PITFALL H9 prevention):
    - Inject CookBotDbContext (db) + IOptions<CookBotSettings> (settings) + ILogger (existing) + ICurrentUserService-like accessor for UserId/KeyOwnerId. Inspect AiChat.razor's call site to determine how UserId+KeyOwnerId are passed today — likely via the credentials resolver. If they aren't already plumbed, ADD optional `int? userId = null, int? keyOwnerId = null` parameters to GenerateAsync (and update IAiRecipeGenerator + the AiChat call site) so the telemetry write has the foreign-key values. If both are null the write is SKIPPED (graceful — the v1.2 AI-off contract trumps telemetry).
    - Accumulate attempts: `var attempts = new List<(StructuredResult<RecipeDocument> Result, bool IsRetry)>();` Add the initial call result with IsRetry=false. Each repair-loop iteration appends with IsRetry=true.
    - At every return statement in GenerateAsync (currently 4 return sites): BEFORE returning, call a private async helper `WriteTelemetryAsync(attempts, userId, keyOwnerId, modelId)`.
    - The helper computes pricing per attempt using settings.AiPricing TryGetValue with fallback (decimal Input = 0m, Output = 0m if model is not in config). For each (result, isRetry) it inserts an AiUsageLog row: ModelName = modelId ?? DefaultModelId; UserId / KeyOwnerId from params; InputTokens, OutputTokens from result; IsRetryAttempt = isRetry; Timestamp = UtcNow; EstimatedCostUsd = (result.InputTokens * Input + result.OutputTokens * Output) / 1_000_000m. Single SaveChangesAsync at the end of the helper.
    - The AI-off contract check: callers (AiChat.razor) already gate on CookBotSettings.AiFeaturesEnabled + UserProfile.AiEnabled BEFORE invoking GenerateAsync. The generator does not double-check. But to defend in depth, WriteTelemetryAsync skips when userId is null OR settings.AiPricing is null/empty.

    PromptBuilderService D-42 prose:
    - Append to the v3 schema section of BuildSystemPrompt two clauses:
      a. `description` definition: "1–2 sentence summary of what the dish is — no history, no cooking advice."
      b. `steps[]` rule: "Steps begin with the first cooking action — do not write an introductory paragraph as step 1."
    - The exact placement, wording, and formatting affect the Verify snapshot. The Verify test will FAIL with a diff; the executor accepts the diff by renaming `.received.txt` → `.verified.txt` (or using `dotnet test` with the Verify accept-changes flag if available). The .verified file IS committed.

    DatabaseSeeder 365-day cleanup:
    - At the marker comment `// 365-day AiUsageLog cleanup will be inserted here by Plan 09-05` (placed by Plan 09-04), REPLACE the comment with:
      ```
      // D-41: hardcoded 365-day rolling cleanup of AiUsageLog rows.
      // Runs BEFORE the sentinel-prefix re-encryption pass per 09-CONTEXT Established Patterns.
      var aiUsageLogCutoff = DateTime.UtcNow.AddDays(-365);
      var deletedCount = await context.AiUsageLogs
          .Where(r => r.Timestamp < aiUsageLogCutoff)
          .ExecuteDeleteAsync();
      if (deletedCount > 0)
          logger.LogInformation("Pruned {Count} AiUsageLog row(s) older than 365 days.", deletedCount);
      ```

    AnthropicAiService AI return-path PhotoUrl validation (PHOTO-07 second half + PITFALL H5):
    - Inject RecipePhotoUrlValidator via ctor (singleton from Plan 09-01).
    - In SendStructuredAsync, after successful deserialization of the structured-output T (the line that produces `value` of type T), if T is RecipeDocument and the doc has PhotoUrl set: call `_photoValidator.TryValidate(doc.PhotoUrl, out var normalized, out var errorCode)`. If TryValidate returns false, replace doc.PhotoUrl with null (write through a `with` expression since RecipeDocument is a record). If TryValidate succeeds, optionally replace doc.PhotoUrl with normalized.
    - Because RecipeDocument is generic-T, this needs a non-generic guard: `if (deserialized is RecipeDocument recipeDoc && !string.IsNullOrEmpty(recipeDoc.PhotoUrl)) { ... }`. After validation, if value changed, replace `value = (T)(object)recipeDoc with { PhotoUrl = newPhotoUrl }`.

    AnthropicAiServiceTokenTests (new — stub SSE fixture):
    - Test 1: Parser_CapturesInputTokensFromMessageStart — construct a fake SSE stream containing a message_start event with usage.input_tokens=123. Stub the HTTP client (use the same pattern as existing AnthropicAiService unit tests if any, or construct a TestServer-like fake — at minimum, refactor enough of SendStructuredAsync's stream-handling so the SSE parse loop can be tested without full HTTP). If full mocking is impractical for v1.3 scope, mark these as integration tests at the TokenTelemetryTests level instead and rely on the AiRecipeGenerator test surface to verify input/output capture indirectly.
    - Test 2: Parser_CapturesCumulativeOutputTokens_OverwritesNotSums — simulate two message_delta events with usage.output_tokens=100 then usage.output_tokens=250; assert final outputTokens equals 250 (not 350).

    Practical scope note: If wiring full SSE stream mocking is too heavy, this task may collapse AnthropicAiServiceTokenTests into a single behavior-level test inside TokenTelemetryTests that uses RecordingFakeStructuredAi populated with InputTokens=123, OutputTokens=250 results — the cumulative-not-sum property is then verified by the AnthropicAiService unit test ONLY by code-review of the SendStructuredAsync method (grep for `outputTokens =` vs `outputTokens +=`). Document the chosen approach in the SUMMARY.

    TokenTelemetryTests:
    - Success_OnFirstAttempt_WritesOneRow_IsRetryAttemptFalse: configure RecordingFakeStructuredAi to return a valid StructuredResult on first call with InputTokens=100, OutputTokens=200; call GenerateAsync with userId=1, keyOwnerId=2, modelId="claude-sonnet-4-6"; assert one AiUsageLog row in DB with IsRetryAttempt=false and EstimatedCostUsd ≈ (100*3 + 200*15)/1_000_000m = 0.0033m.
    - Repair_ConvergesOnAttempt2_WritesTwoRows_SecondHasIsRetryAttemptTrue: first call returns validation failure; second call returns valid. Assert 2 AiUsageLog rows; second one has IsRetryAttempt=true.
    - BudgetExhaustion_3Calls_WritesThreeRows_RetriesTaggedCorrectly: all 3 calls return validation failures; assert 3 rows; first IsRetryAttempt=false; rows 2 and 3 IsRetryAttempt=true.
    - Aggregation_ExcludesRetryAttempts: after the 3-row scenario, query `db.AiUsageLogs.Where(l => !l.IsRetryAttempt).SumAsync(l => l.EstimatedCostUsd)` returns only the first row's cost.
  </behavior>
  <action>
    Approach this task in clear phases. Where Verify snapshot regeneration is required, do that AFTER the prompt edit so the diff is visible in a single commit.

    1. RED phase tests first:
       a. Write tests/CookBot.Tests/AI/AnthropicAiServiceTokenTests.cs OR fold its scope into TokenTelemetryTests per the practical-scope note in behavior. Pick the cheaper option that still proves the cumulative semantic.
       b. Write tests/CookBot.Tests/AI/TokenTelemetryTests.cs extending AiRecipeGeneratorTests' RecordingFakeStructuredAi pattern. Add a temp-SQLite per-test fixture (per 09-PATTERNS.md Pattern G).
       c. Run filters — MUST fail.

    2. GREEN phase changes:
       a. StructuredResult<T> — extend record positional params with `int InputTokens = 0, int OutputTokens = 0`.
       b. AnthropicAiService.SendStructuredAsync —
          - Inject RecipePhotoUrlValidator via ctor.
          - Add `int inputTokens = 0; int outputTokens = 0;` at the top of the method (before the SSE loop).
          - Add message_start case capturing input_tokens.
          - Modify message_delta case to also overwrite outputTokens (cumulative). NEVER use `+=`.
          - After deserialization succeeds, if value is RecipeDocument with non-empty PhotoUrl, run TryValidate; null-out if rejected.
          - Add inputTokens, outputTokens to all 6 StructuredResult<T> return sites (line 322, 335, 344, 352, 361, 370 per 09-PATTERNS).
       c. AiRecipeGenerator.GenerateAsync — inject CookBotDbContext + IOptions<CookBotSettings>; add userId + keyOwnerId optional params; accumulate (result, isRetry) tuples; WriteTelemetryAsync helper at all 4 return sites.
       d. Update IAiRecipeGenerator interface to match new GenerateAsync signature; update AiChat.razor's call site to pass userId + keyOwnerId from the existing credentials/profile state.
       e. PromptBuilderService.BuildSystemPrompt — append the two D-42 prose clauses at the appropriate section. Re-read the existing file to determine the natural insertion point (likely inside the v3 schema documentation block).
       f. Run PromptSnapshotTests — it fails with a diff. Accept the diff: rename `tests/CookBot.Tests/Snapshots/PromptSnapshotTests.BuildSystemPrompt.received.txt` → `.verified.txt` (or run with Verify accept-flag). Commit BOTH the source change and the .verified file in the same commit.
       g. DatabaseSeeder — replace the Plan 09-04 marker comment with the ExecuteDeleteAsync block per behavior.
       h. Run all telemetry/test filters and the full suite — MUST pass.

    3. Manual smoke (for SUMMARY): run ./run.sh, generate a recipe via AiChat, then query `sqlite3 cookbot.db "SELECT UserId, KeyOwnerId, ModelName, InputTokens, OutputTokens, EstimatedCostUsd, IsRetryAttempt FROM AiUsageLogs ORDER BY Timestamp DESC LIMIT 5"`. Confirm at least one row appeared after the AI generation.

    4. Verify the prompt snapshot diff is intentional — read both the .verified before and after, and confirm only the D-42 prose was added.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/freeloves-cookbot &amp;&amp; dotnet build --nologo -v quiet &amp;&amp; dotnet test tests/CookBot.Tests/CookBot.Tests.csproj --no-build --nologo -v quiet &amp;&amp; grep -q 'InputTokens' src/CookBot.Application/AI/StructuredResult.cs &amp;&amp; grep -q 'OutputTokens' src/CookBot.Application/AI/StructuredResult.cs &amp;&amp; grep -q 'message_start' src/CookBot.Infrastructure/AI/AnthropicAiService.cs &amp;&amp; grep -q 'outputTokens = ' src/CookBot.Infrastructure/AI/AnthropicAiService.cs &amp;&amp; ! grep -q 'outputTokens += ' src/CookBot.Infrastructure/AI/AnthropicAiService.cs &amp;&amp; grep -q 'IsRetryAttempt' src/CookBot.Application/AI/AiRecipeGenerator.cs &amp;&amp; grep -q 'AiUsageLogs' src/CookBot.Application/AI/AiRecipeGenerator.cs &amp;&amp; grep -q 'AiUsageLogs.*ExecuteDeleteAsync' src/CookBot.Infrastructure/Data/DatabaseSeeder.cs &amp;&amp; ! grep -q '365-day AiUsageLog cleanup will be inserted here' src/CookBot.Infrastructure/Data/DatabaseSeeder.cs &amp;&amp; grep -q 'RecipePhotoUrlValidator' src/CookBot.Infrastructure/AI/AnthropicAiService.cs</automated>
  </verify>
  <acceptance_criteria>
    - StructuredResult<T> has InputTokens, OutputTokens with `= 0` defaults; backwards-compatible (existing call sites compile unchanged)
    - AnthropicAiService SSE handler has new message_start branch reading usage.input_tokens AND modified message_delta branch that OVERWRITES outputTokens (never +=)
    - `grep -E 'outputTokens \+=' src/CookBot.Infrastructure/AI/AnthropicAiService.cs` returns zero matches (no accidental summation)
    - AnthropicAiService injects RecipePhotoUrlValidator; AI-return-path nulls invalid emitted PhotoUrl
    - AiRecipeGenerator accumulates per-attempt tuples and writes AiUsageLog rows at GenerateAsync exit (NOT inside the loop body) — verifiable by code-review: the AiUsageLog write site appears exactly ONCE in the file, structured as a single WriteTelemetryAsync helper
    - IAiRecipeGenerator interface updated with userId/keyOwnerId optional params; AiChat.razor passes them
    - PromptBuilderService.BuildSystemPrompt contains the two D-42 prose clauses; PromptSnapshotTests passes against the regenerated .verified.txt
    - DatabaseSeeder.SeedAsync contains the AiUsageLogs ExecuteDeleteAsync block at the place where the Plan 09-04 marker comment used to be (marker comment is gone; cleanup code is in its place)
    - Cleanup runs BEFORE the sentinel-prefix re-encryption pass (verify by reading file order)
    - TokenTelemetryTests pass all 4 scenarios; AnthropicAiServiceTokenTests pass (or the cumulative semantic is otherwise proven per the practical-scope option)
    - Full test suite green; existing Phase 8 PromptSnapshotTests + AiRecipeGeneratorTests + AiApiKeyResolutionService tests all green
  </acceptance_criteria>
  <done>PROD-12 (SSE usage capture); PROD-13 (StructuredResult fields); PROD-15 (per-attempt write with IsRetryAttempt); PROD-17 write-path (rows exist; Phase 10 renders the widget); D-42 prompt prose; D-41 365-day cleanup; PHOTO-07 second half (AI-return-path validator) — all closed in one consolidated plan. Boot order in DatabaseSeeder is now: backup → migrate → seed → null-canonical guard → 365-day cleanup → sentinel-prefix re-encryption → existing seed logic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anthropic SSE stream → AnthropicAiService | Usage counts and content arrive over HTTPS; untrusted shape — defensive TryGetProperty chains |
| Anthropic structured-output value → AiRecipeGenerator → DB | AI-emitted PhotoUrl crosses the trust boundary and must pass the validator (PHOTO-07) |
| AiUsageLog.EstimatedCostUsd → eventual Phase 10 widget | Currency math via decimal; floor not zero for sub-cent precision |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05-01 | Tampering | AI-emitted javascript: PhotoUrl bypasses editor validator | mitigate | RecipePhotoUrlValidator runs in AnthropicAiService AI return path; invalid emitted URLs null out before the doc reaches AiChat / DB. |
| T-09-05-02 | Information Disclosure (cost over-count) | Sum vs. overwrite on cumulative output_tokens | mitigate | message_delta.usage.output_tokens captured via OVERWRITE; grep gate `outputTokens +=` must return zero. |
| T-09-05-03 | Tampering | Retry rows double-count in widget aggregation | mitigate | IsRetryAttempt=true marker on retries; Phase 10 widget aggregates with WHERE IsRetryAttempt=false. PROD-15 invariant. |
| T-09-05-04 | Information Disclosure | Recipient sees that owner spent $50 on AI this month | accept | Per PITFALL M9 and PROD-17 design: trusted-LAN posture; cross-user telemetry visibility is the documented trust model. README PROD-20 calls it out. |
| T-09-05-05 | Denial of Service | Stale pricing produces wildly wrong $ estimates | mitigate | AiPricingVerifiedDate column footnote (Phase 10 widget); operator-facing notice. Pricing config is operator-mutable in appsettings.json. |
| T-09-05-06 | Tampering | Boot-order regression — re-encryption runs before cleanup | mitigate | Cleanup INSERTED at the marker comment's position; physical file order: cleanup THEN re-encryption. Code-review gate. |
| T-09-05-SC | Tampering (supply chain) | NuGet installs in this plan | accept | No new NuGets in Plan 09-05; all dependencies were added in earlier plans (Plan 09-04 added DataProtection; this plan reuses everything). |
</threat_model>

<verification>
- Full test suite green including the new tests and the regenerated PromptSnapshotTests.
- Manual smoke (in summary): generate a recipe via AiChat; query `sqlite3 cookbot.db "SELECT * FROM AiUsageLogs ORDER BY Timestamp DESC LIMIT 5"` confirms ≥ 1 row with IsRetryAttempt=0 and reasonable EstimatedCostUsd; trigger a validation failure to force a retry (e.g. via Anthropic prompt eating that produces a bad doc — or by faking the path with a forced error in dev) and confirm 2 rows land with the second tagged IsRetryAttempt=1.
- Boot smoke: app starts cleanly; no exception during the new cleanup pass; no double-encryption (idempotency from Plan 09-04 still works).
- Phase 8 PromptSnapshotTests passes against the regenerated .verified file; the file diff shows only the D-42 prose addition.
</verification>

<success_criteria>
1. All artifacts present: AiUsageLog entity + configuration + migration + DbSet + AiPricing config + StructuredResult extension + SSE usage capture + AiRecipeGenerator write site + D-42 prose + .verified snapshot + 365-day cleanup + RecipePhotoUrlValidator on AI return path
2. Cumulative output_tokens semantic verified (overwrite, not sum) by code-review + automated grep gate
3. Per-attempt rows written exactly once per (attempt, retry-flag) combination; aggregation excluding retries returns the success-path cost only
4. Boot order in DatabaseSeeder: backup → migrate → null-canonical → 365-day cleanup → re-encryption → existing seed
5. dotnet build exit 0; full test suite green; no Phase 8 regressions
6. Manual smoke: AiUsageLog row appears in DB after an AI generation; ciphertext column survives boot (Plan 09-04 still intact)
</success_criteria>

<output>
Create .planning/phases/09-photos-prod-ready-infrastructure/09-05-SUMMARY.md when done. Include: migration timestamp; AiPricing JSON delta; the chosen approach for AnthropicAiServiceTokenTests (full SSE mock vs collapse-into-TokenTelemetryTests); PromptSnapshotTests diff summary (one screen of context around the D-42 additions); manual smoke results; AiUsageLog row example from the live DB.
</output>
