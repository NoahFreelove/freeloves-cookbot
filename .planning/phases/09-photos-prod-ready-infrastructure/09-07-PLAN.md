---
phase: 09-photos-prod-ready-infrastructure
plan: 07
type: execute
wave: 5
depends_on: ["09-05", "09-06"]
files_modified:
  - README.md
autonomous: true
requirements: [PHOTO-14, PROD-18, PROD-19, PROD-20, PROD-21]
must_haves:
  truths:
    - "README has a new 'Install' section below the existing 'Recipe Format' section covering BOTH docker compose AND ./run.sh paths (PROD-18)"
    - "README has a new 'Configuration' section documenting env-var override patterns for CookBotSettings, ConnectionStrings, ASPNETCORE_URLS, AiPricing (PROD-19)"
    - "README has a new 'Backup & restore' section explaining: stop container; copy BOTH cookbot_db AND cookbot_uploads volumes; mention WAL files; explain Data Protection key ring is inside cookbot.db so losing the DB = losing decrypt capability (PROD-20, PHOTO-14, PITFALL C6)"
    - "README has a new 'Upgrade' section: docker compose pull && docker compose up -d; migrations auto-apply; IDatabaseBackupService writes cookbot.db.pre-{name}.bak (PROD-21)"
    - "README explicitly notes PDF export is text-only (D-40)"
    - "README explicitly notes the reverse-proxy pattern for TLS (planner's discretion — short Caddyfile snippet included)"
    - "README explicitly notes the cross-user telemetry visibility model (PITFALL M9)"
    - "README explicitly notes that the host-wide CookBot:AnthropicApiKey is NOT encrypted-at-rest (only per-user AiApiKey is) — 09-RESEARCH Runtime State Inventory secrets row"
    - "All new sections sit BELOW the existing 'Recipe Format' section (Phase 8 D-37 inline-README precedent)"
  artifacts:
    - path: "README.md"
      provides: "Operator-facing docs covering install, config, backup, upgrade for self-hosters"
      contains: "## Install"
  key_links:
    - from: "README.md Install section"
      to: "Dockerfile + docker-compose.yml from Plan 09-06"
      via: "docker compose up reference"
      pattern: "docker compose up"
    - from: "README.md Configuration section"
      to: "appsettings.json from Plan 09-05"
      via: "Documents the AiPricing block + AiPricingVerifiedDate"
      pattern: "AiPricing"
    - from: "README.md Backup section"
      to: "Plan 09-06 named volumes"
      via: "Names cookbot_db and cookbot_uploads explicitly"
      pattern: "cookbot_db.*cookbot_uploads"
---

<objective>
Operator-facing documentation for self-hosters. Four new sections (Install, Configuration, Backup & restore, Upgrade) below the Phase 8 Recipe Format section. The README is the load-bearing artifact for PHOTO-14 (separate uploads volume backup), PROD-20 (cross-user telemetry disclosure), and the reverse-proxy TLS pattern. The Install section covers both `docker compose up` and `./run.sh` paths so users can choose between containerized self-host and dev-mode local runs.

Purpose: Without these sections, a self-hoster pulls the repo and has to reverse-engineer the deployment story from compose file + DataProtection docs + the Phase 9 PITFALLS. The docs make Phase 9's shippable-to-others promise real.

Output: Four new H2 sections appended to README.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-photos-prod-ready-infrastructure/09-CONTEXT.md
@.planning/phases/09-photos-prod-ready-infrastructure/09-RESEARCH.md
@.planning/phases/09-photos-prod-ready-infrastructure/09-05-SUMMARY.md
@.planning/phases/09-photos-prod-ready-infrastructure/09-06-SUMMARY.md
@.planning/research/PITFALLS.md
@README.md
@Dockerfile
@docker-compose.yml
@src/CookBot.Web/appsettings.json

<interfaces>
<!-- README current structure (verified): -->

The README currently has these H2 sections (per `grep "^## " README.md` from the planner):
1. ## Features (line 18)
2. ## Recipe Format (line 44)
  ### YAML wire format (v3) (line 48)
  ### JSON export format (line 90)
  ### V1 → V2 → V3 upcaster lineage (line 104)
  ### Internally managed format (line 111)

File total: 113 lines.

Phase 9 Plan 09-07 APPENDS four new H2 sections AFTER the existing "Recipe Format" subsections (i.e. starting at line 114 onward), in this order:
- ## Install (PROD-18)
- ## Configuration (PROD-19)
- ## Backup & restore (PROD-20 + PHOTO-14)
- ## Upgrade (PROD-21)

Plus an optional short "## Reverse proxy" sub-section (planner's discretion per 09-CONTEXT — included as a small subsection inside Install OR as its own H2).

Phase 9 artifacts the README references:
- Dockerfile + docker-compose.yml at repo root (Plan 09-06)
- ./run.sh wrapper for local dev (existing)
- src/CookBot.Web/appsettings.json with CookBot:AiPricing block (Plan 09-05)
- /healthz endpoint at http://localhost:7000/healthz (Plan 09-06)
- cookbot.db.pre-{MigrationName}.bak files via IDatabaseBackupService (Phase 1 D-15 / Plan 09-04 + 09-05 each fire one)
- Two named volumes: cookbot_db (mounts /data with cookbot.db + WAL + DataProtection keys) and cookbot_uploads (mounts /app/wwwroot/uploads)
- PDF export is text-only (D-40 from 09-CONTEXT)
- Cross-user telemetry visibility (PITFALL M9): in trusted-LAN mode, key-owner sees who burned their credits
- Host-wide CookBot:AnthropicApiKey is plaintext in appsettings.json (per 09-RESEARCH Runtime State Inventory row "Secrets/env vars") — only PER-USER AiApiKey is encrypted-at-rest
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append Install + Configuration + Backup & restore + Upgrade sections (with reverse-proxy + PDF + telemetry-disclosure callouts)</name>
  <files>README.md</files>
  <read_first>
    - /home/noah/Desktop/projects/freeloves-cookbot/README.md (entire file — confirm current end-of-file structure; Phase 8 D-37 placed the Recipe Format section as inline content)
    - /home/noah/Desktop/projects/freeloves-cookbot/Dockerfile (from Plan 09-06 — confirm port + entrypoint)
    - /home/noah/Desktop/projects/freeloves-cookbot/docker-compose.yml (from Plan 09-06 — confirm volume names + healthcheck stanza + restart policy)
    - /home/noah/Desktop/projects/freeloves-cookbot/src/CookBot.Web/appsettings.json (from Plan 09-05 — confirm AiPricing block shape for the Configuration section's example)
    - /home/noah/Desktop/projects/freeloves-cookbot/.planning/phases/09-photos-prod-ready-infrastructure/09-CONTEXT.md D-40, D-43; canonical_refs line 115 (README explicit notes list)
    - /home/noah/Desktop/projects/freeloves-cookbot/.planning/research/PITFALLS.md M9 (cross-user telemetry), C6 (uploads backup)
  </read_first>
  <action>
    Append the following content after line 113 of README.md (do NOT modify any existing line). Use exactly four new H2 sections in this order.

    ---

    ## Install

    Two supported paths: containerized self-host (recommended for sharing the app with others) or local dev mode (recommended for tinkering and contributing).

    ### Option A — Docker (recommended for self-hosters)

    Requirements: Docker engine 20.10+, Docker Compose v2+.

    ```bash
    git clone https://github.com/<your-fork>/freeloves-cookbot.git
    cd freeloves-cookbot
    docker compose up -d --build
    ```

    First boot takes ~30 seconds while the .NET SDK image builds and the seeder runs migrations + creates the Data Protection key ring. The healthcheck reports `(healthy)` once the app responds on `/healthz`:

    ```bash
    docker ps                             # status column shows "Up X seconds (healthy)" when ready
    curl -fsS http://localhost:7000/healthz
    ```

    Open `http://localhost:7000/` in a browser on the same LAN. First-run UX: no AI key is required to start. Recipe management works offline; AI assistant features gracefully degrade until a user enters a personal Anthropic API key in **Profile → AI**.

    Port override: set `COOKBOT_PORT` before `docker compose up` to bind a different host port (compose maps `${COOKBOT_PORT:-7000}:7000`).

    ### Option B — Local dev mode (./run.sh)

    Requirements: .NET 10 SDK.

    ```bash
    git clone https://github.com/<your-fork>/freeloves-cookbot.git
    cd freeloves-cookbot
    ./run.sh
    ```

    This wraps `dotnet run --project src/CookBot.Web` and binds `http://localhost:7000`. The SQLite database lives at `./cookbot.db` next to the source tree. Uploaded photos land in `src/CookBot.Web/wwwroot/uploads/`. Both paths are gitignored.

    ### Reverse proxy for TLS (recommended for non-localhost deployments)

    Phase 9 does not terminate TLS inside the container — trusted-LAN posture only. Operators who want HTTPS or want to expose the app beyond `127.0.0.1` should put a reverse proxy in front. Caddy is the simplest choice:

    ```caddyfile
    cookbot.example.lan {
        reverse_proxy localhost:7000
    }
    ```

    nginx and Traefik work the same way. Cert provisioning, HSTS, and Let's Encrypt automation are handled by the proxy, not by FreelovesCookBot.

    ### PDF export

    Cookbook PDF export is **text-only**. Recipe photos are not embedded in PDF output. This is intentional for v1.3 — re-fetching arbitrary photo URLs from inside the synchronous PDF builder is a documented foot-gun (PITFALLS H6) that Phase 9 chose to avoid. Photo-in-PDF may revisit in a future release.

    ## Configuration

    All settings can be overridden via environment variables using .NET's standard double-underscore section delimiter. Compose users set them in `docker-compose.yml`; local-dev users use `appsettings.Development.json` (gitignored).

    | Env var | Source key | Default | Purpose |
    |---|---|---|---|
    | `ConnectionStrings__DefaultConnection` | `ConnectionStrings:DefaultConnection` | `Data Source=cookbot.db` | SQLite file path. Compose sets this to `Data Source=/data/cookbot.db` so the file lands on the named volume. |
    | `ASPNETCORE_URLS` | (Kestrel) | `http://localhost:5000` | Bind address. Dockerfile sets `http://+:7000`. |
    | `CookBot__AuthMode` | `CookBot:AuthMode` | `Disabled` | Reserved for future use; do not rely on this for security. |
    | `CookBot__AiFeaturesEnabled` | `CookBot:AiFeaturesEnabled` | `true` | Host-wide kill switch for AI assistant; per-user toggle (`UserProfile.AiEnabled`) is the second gate. |
    | `CookBot__AnthropicApiKey` | `CookBot:AnthropicApiKey` | `""` | Host-wide fallback key. **Plaintext in config** — intended for single-operator scenarios where appsettings.json is read-only via env vars. Per-user keys (set via Profile UI) are encrypted-at-rest with ASP.NET Core Data Protection. |
    | `CookBot__DatabaseBackupRetention` | `CookBot:DatabaseBackupRetention` | `3` | Number of `.pre-*.bak` files kept alongside `cookbot.db`. Clamped to [1, 10]. |

    ### AI pricing

    Per-million-token pricing for cost-estimation telemetry lives in `appsettings.json` under `CookBot:AiPricing`. The current pricing was verified against `https://platform.claude.com/docs/en/about-claude/pricing` on `2026-05-16`. When Anthropic raises prices, update this block:

    ```json
    {
      "CookBot": {
        "AiPricing": {
          "claude-haiku-4-5-20251001": { "InputTokensPerMillionUsd": 1.00, "OutputTokensPerMillionUsd": 5.00 },
          "claude-sonnet-4-6":          { "InputTokensPerMillionUsd": 3.00, "OutputTokensPerMillionUsd": 15.00 },
          "claude-opus-4-7":            { "InputTokensPerMillionUsd": 5.00, "OutputTokensPerMillionUsd": 25.00 }
        },
        "AiPricingVerifiedDate": "2026-05-16"
      }
    }
    ```

    Each AI call writes one `AiUsageLog` row capturing input/output tokens and the computed `EstimatedCostUsd`. Repair-loop retries (up to 2 per generation) are tagged `IsRetryAttempt = true` so aggregation queries can exclude them.

    ### Cross-user AI usage visibility

    In trusted-LAN mode, when one user shares their AI key with another (Profile → "Share my AI key"), the key-owner can see exactly who has burned their credits via the per-user usage telemetry. This is the documented trust model for self-hosted small-team deployments. If you need stricter isolation, do not enable key-sharing in your deployment.

    ## Backup & restore

    Two volumes hold all persistent state. Backup BOTH together — they are not independently recoverable.

    | Volume | Contents | Recovery without it |
    |---|---|---|
    | `cookbot_db` (`/data` inside container) | `cookbot.db` (recipes, users, profiles, encrypted AI keys, Data Protection key ring), `cookbot.db-wal`, `cookbot.db-shm`, `cookbot.db.pre-*.bak` migration backups | Everything is lost. There is no copy of the Data Protection keys anywhere else — losing this volume means every encrypted AI key in user profiles becomes unrecoverable (the trust model). |
    | `cookbot_uploads` (`/app/wwwroot/uploads/` inside container) | User-uploaded recipe photos | Photos referenced via paste-URL still resolve (the URL is in the canonical doc); uploaded photos show as broken images until re-uploaded. |

    ### Backup procedure (Docker)

    ```bash
    # Stop the container so SQLite isn't mid-write during the copy.
    docker compose stop

    # Snapshot both volumes. The exact path depends on your Docker engine.
    docker run --rm -v cookbot_db:/source -v "$PWD/backups":/dest alpine \
        tar czf /dest/cookbot_db-$(date +%Y%m%d).tar.gz -C /source .
    docker run --rm -v cookbot_uploads:/source -v "$PWD/backups":/dest alpine \
        tar czf /dest/cookbot_uploads-$(date +%Y%m%d).tar.gz -C /source .

    docker compose start
    ```

    The `cookbot.db-wal` and `cookbot.db-shm` files in the `cookbot_db` volume are SQLite write-ahead log files. They MUST be backed up together with `cookbot.db` (a backup of just `cookbot.db` without its WAL may be inconsistent if SQLite was mid-checkpoint at backup time). The volume snapshot above captures all three by design.

    The `cookbot.db.pre-*.bak` files are migration safety nets created automatically by `IDatabaseBackupService` before every EF migration runs (Phase 1 D-15 invariant). They are useful for rolling back a migration but they are NOT a substitute for proper backups.

    ### Restore procedure

    ```bash
    docker compose stop

    # Wipe and restore from a snapshot.
    docker run --rm -v cookbot_db:/dest -v "$PWD/backups":/source alpine \
        sh -c "rm -rf /dest/* && tar xzf /source/cookbot_db-YYYYMMDD.tar.gz -C /dest"
    docker run --rm -v cookbot_uploads:/dest -v "$PWD/backups":/source alpine \
        sh -c "rm -rf /dest/* && tar xzf /source/cookbot_uploads-YYYYMMDD.tar.gz -C /dest"

    docker compose start
    ```

    No UI backup/restore button is shipped in v1.3 — volumes + docs only. A UI backup feature may land in a future release.

    ## Upgrade

    ```bash
    docker compose pull        # if you're tracking a published image; skip if you build locally
    docker compose up -d --build
    ```

    Migrations auto-apply at container start via `DatabaseSeeder.SeedAsync` → `MigrateAsync()`. Before each migration runs, `IDatabaseBackupService` writes a `cookbot.db.pre-{MigrationName}.bak` snapshot alongside `cookbot.db` — useful as a rollback point if a migration produces unexpected results. The retention count is controlled by `CookBot:DatabaseBackupRetention` (default 3, clamped [1, 10]).

    Migrations are **forward-only**. Downgrading to an earlier version is not supported. If you need to revert to an older release, restore from a backup snapshot taken before that release ran for the first time.

    Boot sequence on every container start (FYI for troubleshooting):

    1. **Backup** — `IDatabaseBackupService` snapshots `cookbot.db` if any migration is pending.
    2. **Migrate** — `dotnet ef` applies all pending EF migrations forward.
    3. **Null-canonical guard** — fails loud if any recipe row has `null CanonicalDocumentJson` (Phase 8 invariant).
    4. **365-day cleanup** — `AiUsageLog` rows older than 365 days are pruned via a single SQL DELETE (Phase 9 D-41).
    5. **Sentinel-prefix re-encryption** — any plaintext `UserProfile.AiApiKey` rows (legacy installs upgrading to Phase 9) are encrypted via ASP.NET Core Data Protection. Idempotent: a second boot is a no-op.
    6. **Seed** — admin user / personal pantry / default cookbook setup runs only on a truly empty database.

    If `/healthz` returns 503 after a long restart, check `docker logs cookbot` — the seeder logs each step with structured log lines, and the failure point will be visible.

    ---

    Apply NO other changes — do not modify the existing Features section, the Recipe Format section, or any line above 113.
  </action>
  <verify>
    <automated>grep -q '^## Install$' README.md &amp;&amp; grep -q '^## Configuration$' README.md &amp;&amp; grep -q '^## Backup &amp; restore$' README.md &amp;&amp; grep -q '^## Upgrade$' README.md &amp;&amp; grep -q 'docker compose up -d' README.md &amp;&amp; grep -q '\./run\.sh' README.md &amp;&amp; grep -q 'reverse_proxy localhost:7000' README.md &amp;&amp; grep -q 'cookbot_db' README.md &amp;&amp; grep -q 'cookbot_uploads' README.md &amp;&amp; grep -q 'cookbot.db-wal' README.md &amp;&amp; grep -q 'CookBot:AiPricing\|CookBot__AnthropicApiKey\|AiPricing' README.md &amp;&amp; grep -q '2026-05-16' README.md &amp;&amp; grep -q 'PDF export is.*text-only\|text-only' README.md &amp;&amp; grep -q 'Cross-user\|cross-user' README.md &amp;&amp; grep -q 'forward-only\|forward only' README.md</automated>
  </verify>
  <acceptance_criteria>
    - Four new H2 sections present in exact order: Install, Configuration, Backup &amp; restore, Upgrade
    - Install section covers BOTH docker compose AND ./run.sh paths
    - Install section has a reverse-proxy Caddy snippet
    - Install section explicitly notes PDF export is text-only
    - Configuration section has the env-var override table covering ConnectionStrings__DefaultConnection, ASPNETCORE_URLS, CookBot__AuthMode, CookBot__AiFeaturesEnabled, CookBot__AnthropicApiKey, CookBot__DatabaseBackupRetention
    - Configuration section has the AiPricing JSON example with the exact 2026-05-16 values from Plan 09-05
    - Configuration section explicitly notes CookBot:AnthropicApiKey is plaintext in config; per-user AiApiKey is encrypted-at-rest
    - Configuration section explicitly notes cross-user AI usage visibility (PITFALL M9)
    - Backup section names BOTH cookbot_db AND cookbot_uploads volumes; explains WAL files; explains Data Protection key ring is inside cookbot.db
    - Upgrade section has the docker compose pull command; notes IDatabaseBackupService snapshots; explicitly says migrations are forward-only
    - Upgrade section has the 6-step boot sequence
    - No existing line (1–113) is modified
    - `grep -c "^## " README.md` returns 6 (Features, Recipe Format, Install, Configuration, Backup &amp; restore, Upgrade)
  </acceptance_criteria>
  <done>PROD-18 (Install), PROD-19 (Configuration), PROD-20 (Backup &amp; restore + cross-user telemetry disclosure), PROD-21 (Upgrade), PHOTO-14 (uploads-volume backup callout). The README is now the operator-facing source of truth for self-hosting Phase 9.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator reading README → operational decisions | Mis-documented backup procedure could result in unrecoverable data loss |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07-01 | Information Disclosure | Cross-user AI cost visibility surprises an unsuspecting operator | mitigate | README "Cross-user AI usage visibility" subsection explicitly documents the trust model in Configuration. |
| T-09-07-02 | Denial of Service (irreversible data loss) | Operator backs up only cookbot.db, loses AI keys on restore | mitigate | README Backup table explicitly lists what each volume contains AND the consequence of losing it; explicitly calls out that the Data Protection key ring lives in cookbot.db with no external copy. |
| T-09-07-03 | Tampering | Operator runs `docker compose down -v` and wipes both named volumes | accept | The compose command needs the operator to type `-v`. README backup procedure uses `stop`/`start` (NOT `down`) which preserves volumes. |
| T-09-07-04 | Information Disclosure | Operator exposes container on the public internet without TLS | mitigate | README reverse-proxy section gives a Caddyfile snippet and explicitly warns trusted-LAN posture. |
| T-09-07-SC | Tampering (supply chain) | NuGet installs in this plan | accept | No NuGets in Plan 09-07 — documentation only. |
</threat_model>

<verification>
Visual review by the executor: open README.md, confirm all four new sections render correctly in a Markdown previewer (e.g. `glow README.md` or VS Code preview). Confirm the JSON code block in Configuration parses (copy into `python3 -m json.tool`). Confirm the table renders. Confirm no existing section was modified.

Quality gate (the README is the only artifact and there are no tests for it): the automated grep gate in Verify above covers every must-have content fragment.
</verification>

<success_criteria>
1. README.md contains all four new H2 sections in correct order BELOW the existing Recipe Format section
2. Every must_haves truth is grep-verifiable in README.md
3. No existing README line (1–113) modified
4. Code blocks (bash, json, caddyfile) are syntactically valid (no malformed inline code)
5. Operator running `docker compose up -d` for the first time can follow the README end-to-end without external documentation
</success_criteria>

<output>
Create .planning/phases/09-photos-prod-ready-infrastructure/09-07-SUMMARY.md when done. Include: line count delta (113 → X); the four new section titles in order; any deviations from the prose drafted in the action block; final review notes (e.g. "tested the Caddyfile snippet on local Caddy: works").
</output>
