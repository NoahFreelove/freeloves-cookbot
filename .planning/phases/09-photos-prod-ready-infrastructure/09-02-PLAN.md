---
phase: 09-photos-prod-ready-infrastructure
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/CookBot.Web/Components/Pages/RecipeEditorParts/RecipePhotoComposite.razor
  - src/CookBot.Web/Components/Pages/RecipeEditorParts/StepTemperaturePicker.razor
  - src/CookBot.Web/Components/Pages/RecipeEditor.razor
  - src/CookBot.Web/Components/Pages/RecipeEditorParts/RecipeStepEditor.razor
  - src/CookBot.Application/Services/RecipeService.cs
autonomous: true
requirements: [PHOTO-08, PHOTO-09]
must_haves:
  truths:
    - "RecipePhotoComposite renders at the TOP of RecipeEditor, ABOVE the recipe name field (D-38)"
    - "Composite shows preview thumbnail on LEFT (4:3 ~240×180), URL+Upload+Clear stack on RIGHT — BOTH inputs always visible, no tabs (D-38)"
    - "Description renders as <CbTextarea> 2-3 rows DIRECTLY BELOW the name field, ABOVE the ingredients section (D-39)"
    - "Per-step Temperature picker renders inline in RecipeStepEditor with three-unit selector (F/C/gas) — Phase 8 schema deferred surface"
    - "Whitespace-only paste-URL persists as null (D-38: empty strings are not persisted)"
    - "Preview <img> sets referrerpolicy='no-referrer' AND loading='lazy' (PHOTO-08)"
    - "<img> onerror fallback to <StripedPlaceholder> uses Blazor state flag _photoLoadFailed — ONE-SHOT, not infinite loop (PHOTO-08, PITFALL H4)"
    - "File upload rejects > 10 MB OR non-image with descriptive toast — never silent SignalR drop (PHOTO-02/03)"
    - "RecipeService.UpdateAsync / CreateAsync persists Recipe.PhotoUrl and Recipe.Description; entity columns already exist from Phase 8 SCHEMA-05/06"
  artifacts:
    - path: "src/CookBot.Web/Components/Pages/RecipeEditorParts/RecipePhotoComposite.razor"
      provides: "Composite child component: preview + paste-URL + upload + clear (D-38)"
      contains: "[Parameter] EventCallback<string?> PhotoUrlChanged"
    - path: "src/CookBot.Web/Components/Pages/RecipeEditorParts/StepTemperaturePicker.razor"
      provides: "Per-ContentStep temperature picker (value + F/C/gas unit selector); Phase 8 schema record StepTemperature surfaced in editor"
      contains: "[Parameter] EventCallback<StepTemperature?> TemperatureChanged"
    - path: "src/CookBot.Web/Components/Pages/RecipeEditor.razor"
      provides: "Photo composite at top + Description CbTextarea below name (D-39) + persists PhotoUrl/Description through RecipeService"
    - path: "src/CookBot.Web/Components/Pages/RecipeEditorParts/RecipeStepEditor.razor"
      provides: "Per-step Temperature picker integrated into the step row"
    - path: "src/CookBot.Application/Services/RecipeService.cs"
      provides: "UpdateAsync/CreateAsync persists PhotoUrl + Description into Recipe entity columns AND into Recipe.CanonicalDocumentJson via JsonRecipeSerializer round-trip"
  key_links:
    - from: "RecipePhotoComposite.razor"
      to: "RecipePhotoUrlValidator"
      via: "[Inject] RecipePhotoUrlValidator + TryValidate on paste-URL change"
      pattern: "RecipePhotoUrlValidator.*TryValidate"
    - from: "RecipePhotoComposite.razor"
      to: "LocalRecipePhotoStorage"
      via: "[Inject] LocalRecipePhotoStorage + SaveAsync on <InputFile> OnChange"
      pattern: "LocalRecipePhotoStorage.*SaveAsync"
    - from: "RecipeEditor.razor"
      to: "Recipe.PhotoUrl persistence"
      via: "_photoUrl field flows through PopulateFromRecipe/SaveRecipe into RecipeService.UpdateAsync"
      pattern: "_photoUrl"
    - from: "RecipeEditor.razor"
      to: "Recipe.Description persistence"
      via: "_description (existing) — change input element from borderless <input> to <CbTextarea> 2-3 rows"
      pattern: "CbTextarea"
    - from: "RecipeStepEditor.razor"
      to: "ContentStep.Temperature"
      via: "StepTemperaturePicker child component bound to ContentStep.Temperature"
      pattern: "StepTemperaturePicker"
---

<objective>
Land the editor-side photo + description + temperature surfacing. The composite design is LOCKED per D-38 (both inputs visible at top of editor, thumbnail-left/stack-right layout — no tabs, no accordions). Description placement is LOCKED per D-39 (CbTextarea directly below name, above ingredients). Phase 8 created the Recipe.PhotoUrl, Recipe.Description columns and the canonical doc's ContentStep.Temperature record but deferred the editor UI surfacing to this plan.

Purpose: This is the user-visible payoff of the Phase 8 schema work. After this plan, the editor exposes all three v3 fields. The persistence path is unchanged — RecipeService.UpdateAsync canonicalizes via JsonRecipeSerializer round-trip on every save (v1.1 Phase 1 invariant); the editor's only job is to produce a valid ParsedRecipe with PhotoUrl/Description/Temperature populated.

Output: A working photo composite at the top of RecipeEditor with file upload AND paste-URL, a Description textarea pairing name+lede the way RecipeView already does, and a per-step Temperature picker that supports F/C/gas units.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-photos-prod-ready-infrastructure/09-CONTEXT.md
@.planning/phases/09-photos-prod-ready-infrastructure/09-PATTERNS.md
@.planning/phases/09-photos-prod-ready-infrastructure/09-01-SUMMARY.md
@.planning/research/PITFALLS.md
@src/CookBot.Web/Components/Pages/RecipeEditor.razor
@src/CookBot.Web/Components/Pages/RecipeEditorParts/RecipeStepEditor.razor
@src/CookBot.Web/Components/Atoms/CbTextarea.razor
@src/CookBot.Web/Components/Atoms/CbInput.razor
@src/CookBot.Web/Components/Atoms/StripedPlaceholder.razor
@src/CookBot.Application/Services/RecipeService.cs
@src/CookBot.Domain/Entities/Recipe.cs
@src/CookBot.Domain/Recipes/ContentStep.cs
@src/CookBot.Domain/Recipes/StepTemperature.cs

<interfaces>
<!-- Anchors the executor must use without re-discovering. -->

Existing RecipeEditor.razor anchors:
- Line 88-94: existing borderless input bound to _description with @oninput="OnDescriptionInput" — REPLACE this with a CbTextarea and MOVE it to below the name input (line 87) so reading order is composite → name → description → ingredients.
- Line 96-onwards: existing StripedPlaceholder photo placeholder block — KEEP the placeholder atom but REPLACE the rendering by wrapping it inside the new RecipePhotoComposite bound via @bind-PhotoUrl="_photoUrl". Move the whole composite block ABOVE the name input per D-38.
- Line 335: private string _description = string.Empty; — keep field name. Add private string? _photoUrl;.
- Line 416: _description = string.Empty; (initial state) — add _photoUrl = null; initialization in same block.
- Line 500: OnDescriptionInput handler — kept; the new CbTextarea wires through @bind-Value (no manual @oninput needed).
- Line 394 (PopulateFromRecipe(_existingRecipe)): populate _photoUrl = _existingRecipe.PhotoUrl and _description = _existingRecipe.Description ?? string.Empty inside PopulateFromRecipe.
- Line 708 (SaveRecipe) and line 755 (RecipeService.UpdateAsync(...)): pass the new fields through to RecipeService; RecipeService canonicalizes via JsonRecipeSerializer round-trip.

Recipe entity (already exists from Phase 8):
- public string? PhotoUrl { get; set; } (line 19)
- public string? Description { get; set; } (line 21)

RecipeService.UpdateAsync / CreateAsync signatures live in src/CookBot.Application/Services/RecipeService.cs. ParsedRecipe DTO (in CookBot.Application.Recipes) is the shape returned by the parser and consumed by these methods.

RecipeStepEditor.razor existing shape (lines 1-29 header): @namespace CookBot.Web.Components.Pages.RecipeEditorParts. Inline custom CSS-variable styling, no Mud.

CbTextarea atom (src/CookBot.Web/Components/Atoms/CbTextarea.razor) exists from v1.2 — exposes a Value parameter, a ValueChanged EventCallback, and a Rows parameter. Used as <CbTextarea @bind-Value="_description" Rows="3" Placeholder="A short description…" />.

StripedPlaceholder atom shape: <StripedPlaceholder Width="100%" Height="180" Label="hero photo · 4:3" /> (matches existing RecipeView line 86 usage).

ContentStep schema (Phase 8 — src/CookBot.Domain/Recipes/ContentStep.cs): record contains a Temperature property of type StepTemperature? (Phase 8 SCHEMA-03).

StepTemperature record (src/CookBot.Domain/Recipes/StepTemperature.cs): properties Value (decimal) and Unit (TemperatureUnit). Enum values: F, C, Gas.

EditableContentStep — there is likely an editor-local mutable mirror of ContentStep used in RecipeStepEditor (records are immutable). Inspect the existing step state in RecipeStepEditor before adding the temperature picker; mirror the existing mutable-shape pattern.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RecipePhotoComposite.razor (D-38 locked layout) wiring RecipePhotoUrlValidator + LocalRecipePhotoStorage</name>
  <files>src/CookBot.Web/Components/Pages/RecipeEditorParts/RecipePhotoComposite.razor</files>
  <read_first>
    - /home/noah/Desktop/projects/freeloves-cookbot/src/CookBot.Web/Components/Pages/RecipeEditorParts/RecipeStepEditor.razor (lines 1-30 — namespace + Cb-atom + custom-style header convention)
    - /home/noah/Desktop/projects/freeloves-cookbot/src/CookBot.Web/Components/Atoms/CbInput.razor (entire file — two-way binding pattern via Value/ValueChanged)
    - /home/noah/Desktop/projects/freeloves-cookbot/src/CookBot.Web/Components/Atoms/CbButton.razor (entire file — variant + StartIcon usage)
    - /home/noah/Desktop/projects/freeloves-cookbot/src/CookBot.Web/Components/Atoms/StripedPlaceholder.razor (entire file — Width/Height/Label parameters)
    - /home/noah/Desktop/projects/freeloves-cookbot/.planning/phases/09-photos-prod-ready-infrastructure/09-CONTEXT.md D-38 (locked composite layout)
    - /home/noah/Desktop/projects/freeloves-cookbot/.planning/research/PITFALLS.md sections H1, H4 (size-limit toast, onerror state-flag one-shot)
    - /home/noah/Desktop/projects/freeloves-cookbot/.planning/phases/09-photos-prod-ready-infrastructure/09-01-SUMMARY.md (downstream output: LocalRecipePhotoStorage + RecipePhotoUrlValidator are DI-registered)
  </read_first>
  <action>
    Create src/CookBot.Web/Components/Pages/RecipeEditorParts/RecipePhotoComposite.razor (note: file at Pages/RecipeEditorParts/ to match the established sibling pattern per 09-PATTERNS.md lines 254–285 — DO NOT place it at Components/RecipePhotoComposite.razor; the sibling pattern wins for namespace consistency with RecipeStepEditor).

    Header block per RecipeStepEditor.razor convention:
    - @namespace CookBot.Web.Components.Pages.RecipeEditorParts
    - @using CookBot.Application.Services
    - @using CookBot.Web.Services
    - @using CookBot.Web.Components.Atoms
    - @using Microsoft.AspNetCore.Components.Forms
    - @inject RecipePhotoUrlValidator UrlValidator
    - @inject LocalRecipePhotoStorage PhotoStorage
    - @inject ICbToastService Toast
    - Doc comment block at top citing Phase 9 / Plan 09-02 / PHOTO-08 + PHOTO-09 + D-38 + D-39 sibling.

    Markup (inline style, matching the CbCard + custom-CSS-variable pattern):
    - Outer wrapper div uses class cb-card with grid-template-columns 240px 1fr, gap 18px, padding 18px, margin-bottom 18px.
    - Left column: conditional render — when PhotoUrl is null/empty OR _photoLoadFailed is true, render StripedPlaceholder Width 240 Height 180 Label "hero photo · 4:3"; otherwise render an img element with src bound to PhotoUrl, referrerpolicy attribute set to no-referrer, loading attribute set to lazy, an @onerror handler bound to HandleImageLoadError, inline style width 240px height 180px object-fit cover border-radius 10px.
    - Right column: vertical flex stack of (1) a CbInput with Type="url" and a value/change binding to _urlInput plus a Placeholder for the URL field; (2) a small validation-error div rendered only when _urlError is non-null using color var(--warn) and font-size 12px; (3) an InputFile element with an OnChange handler bound to OnFilePicked and accept attribute "image/jpeg,image/png,image/gif,image/webp"; (4) a CbButton with Ghost variant, OnClick bound to ClearPhoto, Disabled when PhotoUrl is null/empty.

    @code block:
    - [Parameter] public string? PhotoUrl { get; set; }
    - [Parameter] public EventCallback<string?> PhotoUrlChanged { get; set; }
    - private string _urlInput = string.Empty (mirrors PhotoUrl on init + on parent re-set)
    - private string? _urlError
    - private bool _photoLoadFailed
    - protected override void OnParametersSet() — sync _urlInput from PhotoUrl and reset _photoLoadFailed when PhotoUrl changes from null to non-null
    - private async Task OnUrlPaste(string? value) — sets _urlInput. If string.IsNullOrWhiteSpace(value) call PhotoUrlChanged.InvokeAsync(null) and clear _urlError. Otherwise call UrlValidator.TryValidate(value, out var normalized, out var errorCode) — on success set _urlError=null, _photoLoadFailed=false, and InvokeAsync(normalized); on failure map errorCode to a human-friendly string ("Only http and https URLs are allowed" for SCHEME_NOT_ALLOWED and PROTOCOL_RELATIVE_REJECTED; "Not a valid URL" for MALFORMED), set _urlError to it, and DO NOT fire PhotoUrlChanged.
    - private async Task OnFilePicked(InputFileChangeEventArgs e) — if e.File.Size > 10 * 1024 * 1024 show toast "File too large — 10 MB max." and return. Otherwise try PhotoStorage.SaveAsync(e.File, default), set _photoLoadFailed=false, and InvokeAsync(returned URL). On InvalidImageException show toast "Image rejected: {message} — JPEG, PNG, GIF, WebP only." On other Exception show toast "Upload failed: {message}". (InvalidImageException is the type defined inside LocalRecipePhotoStorage.cs from Plan 09-01; reference it via its fully-qualified or appropriate using.)
    - private async Task ClearPhoto() — clear _urlInput and _urlError, reset _photoLoadFailed, InvokeAsync(null).
    - private void HandleImageLoadError() — set _photoLoadFailed=true and call StateHasChanged() ONE-SHOT per PITFALL H4. Because the left-column conditional gates on _photoLoadFailed, the img element will not re-render after the flag flips, so the browser cannot trigger a second onerror.

    Compile check: dotnet build src/CookBot.Web/CookBot.Web.csproj. The file MUST compile against the existing CbInput / CbButton / StripedPlaceholder Cb atoms unchanged.

    Do NOT integrate into RecipeEditor.razor yet — Task 3 does that.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/freeloves-cookbot &amp;&amp; dotnet build src/CookBot.Web/CookBot.Web.csproj --nologo -v quiet &amp;&amp; grep -q 'namespace CookBot.Web.Components.Pages.RecipeEditorParts' src/CookBot.Web/Components/Pages/RecipeEditorParts/RecipePhotoComposite.razor &amp;&amp; grep -q 'EventCallback&lt;string?&gt; PhotoUrlChanged' src/CookBot.Web/Components/Pages/RecipeEditorParts/RecipePhotoComposite.razor &amp;&amp; grep -q 'referrerpolicy="no-referrer"' src/CookBot.Web/Components/Pages/RecipeEditorParts/RecipePhotoComposite.razor &amp;&amp; grep -q 'loading="lazy"' src/CookBot.Web/Components/Pages/RecipeEditorParts/RecipePhotoComposite.razor &amp;&amp; grep -q '_photoLoadFailed' src/CookBot.Web/Components/Pages/RecipeEditorParts/RecipePhotoComposite.razor &amp;&amp; grep -q 'LocalRecipePhotoStorage' src/CookBot.Web/Components/Pages/RecipeEditorParts/RecipePhotoComposite.razor &amp;&amp; grep -q 'RecipePhotoUrlValidator' src/CookBot.Web/Components/Pages/RecipeEditorParts/RecipePhotoComposite.razor</automated>
  </verify>
  <acceptance_criteria>
    - File src/CookBot.Web/Components/Pages/RecipeEditorParts/RecipePhotoComposite.razor exists
    - Namespace is CookBot.Web.Components.Pages.RecipeEditorParts
    - Component exposes both PhotoUrl and PhotoUrlChanged parameters
    - Component injects RecipePhotoUrlValidator AND LocalRecipePhotoStorage
    - The img tag includes both referrerpolicy="no-referrer" AND loading="lazy"
    - _photoLoadFailed Blazor state flag exists and is set inside the @onerror handler
    - Markup grid uses 240px left column for preview (4:3 ratio enforced via inline style height/width pair)
    - No Mud* reference anywhere in the file (grep -i "Mud[A-Z]" returns nothing)
    - Solution compiles: dotnet build src/CookBot.Web/CookBot.Web.csproj exit code 0
  </acceptance_criteria>
  <done>RecipePhotoComposite.razor ready to be plugged into RecipeEditor by Task 3. D-38 layout locked; PHOTO-08 onerror state-flag pattern implemented; uses Plan 09-01 services via DI.</done>
</task>

<task type="auto">
  <name>Task 2: Create StepTemperaturePicker.razor and integrate into RecipeStepEditor (Phase 8 ContentStep.Temperature deferred UI surface)</name>
  <files>src/CookBot.Web/Components/Pages/RecipeEditorParts/StepTemperaturePicker.razor, src/CookBot.Web/Components/Pages/RecipeEditorParts/RecipeStepEditor.razor</files>
  <read_first>
    - /home/noah/Desktop/projects/freeloves-cookbot/src/CookBot.Domain/Recipes/StepTemperature.cs (entire file — record + TemperatureUnit enum F/C/Gas)
    - /home/noah/Desktop/projects/freeloves-cookbot/src/CookBot.Domain/Recipes/ContentStep.cs (entire file — verify Temperature property exists)
    - /home/noah/Desktop/projects/freeloves-cookbot/src/CookBot.Web/Components/Pages/RecipeEditorParts/RecipeStepEditor.razor (entire file — composite step row pattern; identify where to drop the temperature picker and how editable step state is managed)
    - /home/noah/Desktop/projects/freeloves-cookbot/src/CookBot.Web/Components/Atoms/CbInput.razor (number-input usage)
    - /home/noah/Desktop/projects/freeloves-cookbot/src/CookBot.Application/Recipes/RecipeValidator.cs (search for "Temperature" — Phase 8 per-unit validation ranges; the picker does NOT enforce these but should not produce values RecipeValidator rejects in the common case)
  </read_first>
  <action>
    Create src/CookBot.Web/Components/Pages/RecipeEditorParts/StepTemperaturePicker.razor.

    Header:
    - @namespace CookBot.Web.Components.Pages.RecipeEditorParts
    - @using CookBot.Domain.Recipes
    - @using CookBot.Web.Components.Atoms
    - Doc comment citing Phase 9 / Plan 09-02 (Phase 8 SCHEMA-03 schema record StepTemperature surfaced in editor).

    Markup:
    - Outer span uses inline-flex with 6px gap and 13px font.
    - Eyebrow text "Temperature" (small) before the input.
    - Numeric input element: type="number", step="0.5", min="0", max="1000", value bound to _value, @oninput bound to OnValueInput, inline style width 64px border 1px solid var(--line) border-radius 6px padding 4px 6px font-size 13px text-align right.
    - Three pill buttons rendered via a foreach over the array { TemperatureUnit.F, TemperatureUnit.C, TemperatureUnit.Gas }. Each button has type="button", class "cb-pill" plus "active" when selected, @onclick=SelectUnit(u), inline style padding 3px 10px, border-radius 12px, border 1px solid var(--line), background var(--accent) when selected else transparent, color white when selected else var(--ink-2), font-size 12px. Label via FormatUnit returns "°F", "°C", "Gas".

    @code:
    - [Parameter] public StepTemperature? Temperature { get; set; }
    - [Parameter] public EventCallback<StepTemperature?> TemperatureChanged { get; set; }
    - private string _value = string.Empty
    - private TemperatureUnit? _unit
    - protected override void OnParametersSet() — sync _value from Temperature.Value (ToString or invariant-culture string) and _unit from Temperature.Unit; if Temperature is null both reset.
    - private bool IsSelected(TemperatureUnit u) — returns _unit == u
    - private string FormatUnit(TemperatureUnit u) — switch returning "°F" / "°C" / "Gas"
    - private async Task OnValueInput(ChangeEventArgs e) — set _value from e.Value as string, then call EmitAsync
    - private async Task SelectUnit(TemperatureUnit u) — toggle: if _unit equals u then set _unit=null else set _unit=u, then EmitAsync
    - private async Task EmitAsync() — if _value is whitespace/empty OR _unit is null then invoke TemperatureChanged with null; else if decimal.TryParse(_value, NumberStyles.Number, CultureInfo.InvariantCulture, out var v) and v >= 0 then invoke TemperatureChanged with new StepTemperature { Value = v, Unit = _unit.Value }; else do not emit.

    Integration into RecipeStepEditor.razor:
    - Add @using CookBot.Domain.Recipes to the existing @using block if not already present.
    - Find the existing step-row markup (the file owns the step content layout). Identify the editor-local mutable mirror of ContentStep used in this file (records are immutable; the editor uses an editable proxy). Either: (a) if RecipeStepEditor mutates its own state in place, add the picker as a sibling element inline with the existing per-step inline metadata (timers, etc.) and wire <StepTemperaturePicker Temperature="@_editableStep.Temperature" TemperatureChanged="OnTemperatureChanged" /> with a local handler that mutates the editable mirror; (b) if RecipeStepEditor reads Step as immutable and emits change events upward, add an EventCallback parameter for StepTemperatureChanged here and bubble it up to the parent (the parent then re-creates the ContentStep with the new Temperature).
    - Pick the path that matches the existing convention. Document the path chosen at the top of the @code block.
    - Position the picker visually: place it on the same row as the step text or just under it, with subdued styling. Use the same inline-style design language as the existing chip composer.

    Compile check: dotnet build src/CookBot.Web/CookBot.Web.csproj. If pattern (b) is chosen and requires a new EventCallback up to RecipeEditor.razor, wire that EventCallback now and add the corresponding handler in RecipeEditor.razor's @code block (handler stores temperature in the parent's step collection mirror).

    Do NOT add a separate test file for the picker — UI components in this project are visually-verified per 09-RESEARCH §"Validation Architecture" Phase Requirements → Test Map row PHOTO-09..13 ("bUnit defer to v1.4+"). The integration verification is the editor smoke check in Task 3.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/freeloves-cookbot &amp;&amp; dotnet build src/CookBot.Web/CookBot.Web.csproj --nologo -v quiet &amp;&amp; grep -q 'TemperatureChanged' src/CookBot.Web/Components/Pages/RecipeEditorParts/StepTemperaturePicker.razor &amp;&amp; grep -q 'TemperatureUnit.Gas' src/CookBot.Web/Components/Pages/RecipeEditorParts/StepTemperaturePicker.razor &amp;&amp; grep -q 'TemperatureUnit.F' src/CookBot.Web/Components/Pages/RecipeEditorParts/StepTemperaturePicker.razor &amp;&amp; grep -q 'TemperatureUnit.C' src/CookBot.Web/Components/Pages/RecipeEditorParts/StepTemperaturePicker.razor &amp;&amp; grep -q 'StepTemperaturePicker' src/CookBot.Web/Components/Pages/RecipeEditorParts/RecipeStepEditor.razor</automated>
  </verify>
  <acceptance_criteria>
    - File src/CookBot.Web/Components/Pages/RecipeEditorParts/StepTemperaturePicker.razor exists with namespace CookBot.Web.Components.Pages.RecipeEditorParts
    - Component exposes Temperature parameter (StepTemperature?) and TemperatureChanged EventCallback
    - All three units F/C/Gas are referenced in the component
    - RecipeStepEditor.razor references the new picker via grep -q 'StepTemperaturePicker'
    - dotnet build src/CookBot.Web/CookBot.Web.csproj exit code 0
    - dotnet test full suite exit code 0 (no regressions; Phase 8 StepTemperatureTests still green)
    - Component uses InvariantCulture parsing for the numeric value so decimal commas in non-EN locales do not break "350" → 350m
  </acceptance_criteria>
  <done>Per-step Temperature picker shipped in the editor with three-unit selector; Phase 8 schema record StepTemperature is now surfaced in editor input. RecipeStepEditor wires the picker through its established step-state pattern.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate RecipePhotoComposite + Description CbTextarea into RecipeEditor.razor (D-38 + D-39 placements) and persist PhotoUrl/Description through RecipeService</name>
  <files>src/CookBot.Web/Components/Pages/RecipeEditor.razor, src/CookBot.Application/Services/RecipeService.cs</files>
  <read_first>
    - /home/noah/Desktop/projects/freeloves-cookbot/src/CookBot.Web/Components/Pages/RecipeEditor.razor (entire file — current photo-placeholder location, description input, save path)
    - /home/noah/Desktop/projects/freeloves-cookbot/src/CookBot.Application/Services/RecipeService.cs (entire file — UpdateAsync and CreateAsync signatures; identify exact site where Recipe.PhotoUrl and Recipe.Description must be written into the entity)
    - /home/noah/Desktop/projects/freeloves-cookbot/src/CookBot.Application/Recipes/ParsedRecipe.cs (entire file — DTO shape; confirm PhotoUrl + Description are already present from Phase 8 parser work, or add them in the same commit)
    - /home/noah/Desktop/projects/freeloves-cookbot/src/CookBot.Application/Recipes/JsonRecipeSerializer.cs (skim — canonicalization round-trip path; PhotoUrl + Description should already flow through it from Phase 8 SCHEMA-08/09)
    - /home/noah/Desktop/projects/freeloves-cookbot/src/CookBot.Web/Components/Atoms/CbTextarea.razor (entire file — confirm Rows + Placeholder + @bind-Value signature)
    - /home/noah/Desktop/projects/freeloves-cookbot/.planning/phases/09-photos-prod-ready-infrastructure/09-CONTEXT.md D-38, D-39
  </read_first>
  <action>
    Modify src/CookBot.Web/Components/Pages/RecipeEditor.razor:

    1. Markup reordering — current order (lines 80–96) is: name input → description input (borderless) → photo placeholder. NEW order per D-38 + D-39: photo composite → name input → description CbTextarea (2-3 rows) → ingredients section.
       - Move the photo placeholder block ABOVE the current name input (line 87). Replace its body with a single component invocation: <RecipePhotoComposite PhotoUrl="@_photoUrl" PhotoUrlChanged="OnPhotoUrlChanged" /> wrapped in the existing max-width container.
       - Move the description input block to immediately BELOW the name input. Replace the existing borderless input with: <CbTextarea Value="@_description" ValueChanged="OnDescriptionChanged" Rows="3" Placeholder="A short description of this recipe — what it is, not the first step" AriaLabel="Recipe description" /> wrapped in the same div container that currently surrounds it.
       - Keep the existing borderless name input where it is (after the photo composite); it remains the visual title.
       - Visual order check (post-change): cb-card photo composite → borderless title input → CbTextarea description → existing ingredients grid table.

    2. @code block additions:
       - Add private string? _photoUrl = null; alongside private string _description = string.Empty;
       - Add private async Task OnPhotoUrlChanged(string? newUrl) { _photoUrl = newUrl; StateHasChanged(); await Task.CompletedTask; }
       - Replace existing OnDescriptionInput with private async Task OnDescriptionChanged(string? newValue) { _description = newValue ?? string.Empty; await Task.CompletedTask; } if CbTextarea uses ValueChanged; if CbTextarea exposes @bind-Value with a string (not string?) parameter, adjust signature accordingly per CbTextarea.razor's contract.
       - In PopulateFromRecipe(_existingRecipe) (around line 394), add: _photoUrl = _existingRecipe.PhotoUrl; _description = _existingRecipe.Description ?? string.Empty;
       - In the reset block (around line 416), add: _photoUrl = null;
       - In SaveRecipe (around line 708) — when constructing ParsedRecipe or invoking RecipeService, pass _photoUrl and _description through. If ParsedRecipe already carries these fields (Phase 8 SCHEMA-08 should have added them), set them on the parsed object before passing to RecipeService.UpdateAsync/CreateAsync. If RecipeService does not yet accept them, this task wires them in step 3.

    3. Modify src/CookBot.Application/Services/RecipeService.cs:
       - In UpdateAsync — after the existing canonicalization step that produces the new CanonicalDocumentJson from ParsedRecipe, set existingRecipe.PhotoUrl = parsed.PhotoUrl; existingRecipe.Description = parsed.Description; on the loaded Recipe entity, immediately before SaveChangesAsync.
       - In CreateAsync — when constructing the new Recipe entity, set PhotoUrl and Description from parsed.PhotoUrl / parsed.Description.
       - The canonicalization round-trip (JsonRecipeSerializer → CanonicalDocumentJson) is the source of truth for PhotoUrl + Description in the canonical doc; the entity-column writes are duplicate read-paths for SQL queries (e.g. Home tile thumbnails via SQL JOIN). Both must stay in lockstep — the existing v1.1 invariant.
       - If ParsedRecipe does not yet carry PhotoUrl/Description (Phase 8 deferred), add them to ParsedRecipe.cs in the same commit and ensure RecipeFormatParser / JsonRecipeSerializer populate them. Cross-check against Phase 8 work — SCHEMA-08 (parser reads/writes) and SCHEMA-09 (JsonRecipeSerializer round-trip) should already have shipped, so the ParsedRecipe should already include these properties. If they're missing, this is a Phase 8 carry-over; add them and call out the gap in the SUMMARY.

    4. Smoke verification (manual, run by executor):
       - dotnet build entire solution exit 0
       - dotnet test entire suite exit 0 (Phase 8 RecipeFormatParserTests and PromptSnapshotTests must still pass — D-42 prompt change is Plan 09-05)
       - Run ./run.sh in background, open http://localhost:7000/, log in, navigate to a recipe edit page. Observe: composite at top renders striped placeholder; pasting https://example.com/dog.jpg shows preview; pasting javascript:alert(1) shows error; clear works; <CbTextarea> description below name accepts 2-3 rows of text. Saving the recipe persists PhotoUrl + Description (verify by reloading the edit page and seeing values pre-filled).
       - This smoke is a developer-facing check; it does not need to land in an automated test. Phase 9 verify-work will run end-to-end.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/freeloves-cookbot &amp;&amp; dotnet build --nologo -v quiet &amp;&amp; dotnet test tests/CookBot.Tests/CookBot.Tests.csproj --nologo -v quiet --no-build &amp;&amp; grep -q 'RecipePhotoComposite' src/CookBot.Web/Components/Pages/RecipeEditor.razor &amp;&amp; grep -q '_photoUrl' src/CookBot.Web/Components/Pages/RecipeEditor.razor &amp;&amp; grep -q 'CbTextarea' src/CookBot.Web/Components/Pages/RecipeEditor.razor &amp;&amp; grep -q '_existingRecipe.PhotoUrl' src/CookBot.Web/Components/Pages/RecipeEditor.razor &amp;&amp; grep -E 'PhotoUrl\s*=|\.PhotoUrl\s*=' src/CookBot.Application/Services/RecipeService.cs | grep -v '^#' | grep -c PhotoUrl | xargs -I{} test {} -ge 1</automated>
  </verify>
  <acceptance_criteria>
    - RecipeEditor.razor markup order (top to bottom): RecipePhotoComposite, then the borderless title input, then a CbTextarea bound to _description, then the ingredients section
    - RecipeEditor.razor @code block has private string? _photoUrl field
    - PopulateFromRecipe in RecipeEditor.razor reads _existingRecipe.PhotoUrl and _existingRecipe.Description into local fields
    - RecipeService.UpdateAsync writes parsed.PhotoUrl and parsed.Description back to the Recipe entity before SaveChangesAsync
    - RecipeService.CreateAsync sets PhotoUrl and Description on the new Recipe entity
    - dotnet build exit 0; dotnet test entire suite exit 0
    - No Mud* re-introduction (repo grep -i "Mud[A-Z]" still returns zero hits in src/)
    - Manual smoke (in plan summary): paste-url accept/reject behavior and save-then-reload preservation confirmed by executor
  </acceptance_criteria>
  <done>PHOTO-09 closed (editor composite with paste-URL + file-upload + preview + clear); D-38 layout shipped; D-39 description placement shipped; Phase 8 SCHEMA-05/06 entity columns now populated end-to-end through the editor; Phase 8 SCHEMA-03 per-step Temperature exposed in editor via Task 2's picker.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Blazor circuit | paste-URL text + uploaded file bytes cross here |
| RecipePhotoComposite → LocalRecipePhotoStorage | untrusted bytes flow to disk (mitigations live in 09-01) |
| RecipePhotoComposite → RecipePhotoUrlValidator | untrusted URL string is validated before persisting |
| RecipeEditor → RecipeService → Recipe entity | the persistence path; PhotoUrl is written to entity column AND canonical doc JSON |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-01 | Spoofing | paste-URL javascript: scheme reaches <img src> | mitigate | RecipePhotoUrlValidator runs in the composite BEFORE PhotoUrlChanged fires; only http/https URLs round-trip into _photoUrl. |
| T-09-02-02 | Tampering | client-supplied file name | mitigate | LocalRecipePhotoStorage (Plan 09-01) ignores file.Name; composite never persists filename text. |
| T-09-02-03 | Denial of Service | infinite onerror loop from broken PhotoUrl | mitigate | _photoLoadFailed Blazor state flag gates further img render. After first failure the placeholder renders instead, so the browser never re-fetches (PITFALL H4). |
| T-09-02-04 | Denial of Service | 100 MB file upload silently drops SignalR circuit | mitigate | Pre-OpenReadStream check `file.Size > 10 MB` in OnFilePicked; user-facing toast. Server-side size limits (12 MB) from Plan 09-01 are the second line. |
| T-09-02-05 | Information Disclosure | Referer header leaks recipe URL to external photo host | mitigate | <img referrerpolicy="no-referrer"> on every PhotoUrl render in this composite (and in Plan 09-03's view-surface renders). |
| T-09-02-06 | Tampering | Photo URL diverges between canonical doc and Recipe entity column | mitigate | RecipeService.UpdateAsync/CreateAsync writes BOTH the entity column AND canonicalizes via JsonRecipeSerializer in the same SaveChangesAsync transaction. Single source of truth maintained per v1.1 canonical-first invariant. |
| T-09-02-SC | Tampering (supply chain) | NuGet installs in this plan | accept | No new NuGets in Plan 09-02; uses existing Cb atoms + Plan 09-01 services. |
</threat_model>

<verification>
- After Task 3, run ./run.sh in a background terminal. Visit /recipes/{id}/edit. Composite renders at top. Pasting https://placedog.net/300/200?id=1 produces a live preview. Pasting javascript:alert(1) shows the error message and keeps the placeholder. Clearing returns to placeholder. Upload a JPEG → preview swap. Upload a PDF → toast rejection. Description textarea renders below name, 2-3 rows.
- Save the recipe, reload the edit page, both fields restore.
- dotnet test full suite must remain green (no Phase 8 regression). The Phase 8 prompt-snapshot test stays green here because this plan does NOT modify the prompt; D-42 prompt prose lands in Plan 09-05.
</verification>

<success_criteria>
1. dotnet build of entire solution exit 0; dotnet test entire suite exit 0
2. RecipePhotoComposite, StepTemperaturePicker, RecipeEditor wiring all compile and visually verify per the smoke steps
3. Photo persists across save+reload; Description persists; Temperature on a step persists (Phase 8 round-trip tests confirm the schema half; the editor smoke confirms the UI half)
4. Zero Mud* re-introduction
5. No prompt-snapshot regression (Plan 09-05 owns the D-42 prose change; this plan must leave PromptBuilderService output byte-identical)
</success_criteria>

<output>
Create .planning/phases/09-photos-prod-ready-infrastructure/09-02-SUMMARY.md when done. Include: how RecipeStepEditor's temperature-state pattern was wired (option a or b in Task 2), whether ParsedRecipe already carried PhotoUrl/Description from Phase 8 or if Task 3 added them, smoke checklist results, any Cb atom contract surprises.
</output>
