---
phase: 09-photos-prod-ready-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - src/CookBot.Web/wwwroot/uploads/.gitkeep
  - src/CookBot.Web/Program.cs
  - src/CookBot.Application/Services/RecipePhotoUrlValidator.cs
  - src/CookBot.Application/DependencyInjection.cs
  - src/CookBot.Web/Services/LocalRecipePhotoStorage.cs
  - src/CookBot.Web/Services/ImageMagicBytes.cs
  - tests/CookBot.Tests/Services/RecipePhotoUrlValidatorTests.cs
  - tests/CookBot.Tests/Services/LocalRecipePhotoStorageTests.cs
autonomous: true
requirements: [PHOTO-01, PHOTO-02, PHOTO-03, PHOTO-04, PHOTO-05, PHOTO-06, PHOTO-07]
must_haves:
  truths:
    - "wwwroot/uploads/ is in .gitignore — committed BEFORE any upload code (PHOTO-01, PITFALL C5)"
    - "RecipePhotoUrlValidator rejects javascript:, data:, file:, ftp:, vbscript:, protocol-relative // schemes (PHOTO-07, PITFALL H5)"
    - "RecipePhotoUrlValidator accepts http and https, treats null/empty/whitespace as a no-op (PHOTO-07)"
    - "LocalRecipePhotoStorage rejects SVG, HTML-as-jpg, and any non-magic-byte-matching upload (PHOTO-02, PITFALL H3)"
    - "Uploaded files are renamed via Guid.NewGuid():N + magic-byte-derived extension — client filename never trusted (PHOTO-05, PITFALL H2)"
    - "Kestrel MaxRequestBodySize, FormOptions.MultipartBodyLengthLimit, AND AddServerSideBlazor MaximumReceiveMessageSize all set to 12 MB in Program.cs (PHOTO-04, PITFALL H1)"
    - "/uploads is served via explicit UseStaticFiles with PhysicalFileProvider + X-Content-Type-Options: nosniff (PHOTO-06, PITFALL H3)"
  artifacts:
    - path: ".gitignore"
      provides: "wwwroot/uploads/ exclusion plus .gitkeep allowlist"
      contains: "src/CookBot.Web/wwwroot/uploads/"
    - path: "src/CookBot.Web/wwwroot/uploads/.gitkeep"
      provides: "Empty placeholder so directory exists on fresh clone"
    - path: "src/CookBot.Application/Services/RecipePhotoUrlValidator.cs"
      provides: "Singleton URL scheme validator shared with RecipeService and AnthropicAiService AI return path"
      exports: ["RecipePhotoUrlValidator", "TryValidate"]
    - path: "src/CookBot.Web/Services/LocalRecipePhotoStorage.cs"
      provides: "IBrowserFile → magic-byte sniff → GUID filename → wwwroot/uploads/ persistence"
      exports: ["LocalRecipePhotoStorage", "SaveAsync"]
    - path: "src/CookBot.Web/Services/ImageMagicBytes.cs"
      provides: "Pure helper: 12-byte magic header → extension (.jpg/.png/.gif/.webp)"
      exports: ["ImageMagicBytes", "DetectExtension"]
    - path: "src/CookBot.Web/Program.cs"
      provides: "Three size limits raised to 12 MB; UseStaticFiles for /uploads with nosniff; DI registrations for new services"
    - path: "tests/CookBot.Tests/Services/RecipePhotoUrlValidatorTests.cs"
      provides: "Theory matrix for accept/reject lanes including null/empty edge cases"
    - path: "tests/CookBot.Tests/Services/LocalRecipePhotoStorageTests.cs"
      provides: "Magic-byte accept/reject matrix + path-traversal defense test"
  key_links:
    - from: "src/CookBot.Web/Program.cs"
      to: "RecipePhotoUrlValidator"
      via: "services.AddSingleton<RecipePhotoUrlValidator>() (registered in AddApplication)"
      pattern: "AddSingleton<RecipePhotoUrlValidator>"
    - from: "src/CookBot.Web/Program.cs"
      to: "LocalRecipePhotoStorage"
      via: "builder.Services.AddScoped<LocalRecipePhotoStorage>()"
      pattern: "AddScoped<LocalRecipePhotoStorage>"
    - from: "src/CookBot.Web/Program.cs"
      to: "/uploads static file serving"
      via: "app.UseStaticFiles(new StaticFileOptions { ..., RequestPath = \"/uploads\", OnPrepareResponse = X-Content-Type-Options: nosniff })"
      pattern: "RequestPath = \"/uploads\""
---

<objective>
Land the photo-foundation files BEFORE any upload code can be written: gitignore entry first (PITFALL C5), then the three size limits raised to 12 MB (PITFALL H1), then the URL validator and storage service that downstream waves will plug into the editor and AI return path. PHOTO-01 is mechanically the first commit of this phase.

Purpose: Without PHOTO-01 first, the next executor could accidentally `git add` a user's uploaded photo into the repo. Without PHOTO-04, a 10 MB file upload silently drops the SignalR circuit (PITFALL H1). Without the validator, the editor and the AnthropicAiService AI return path each end up doing scheme-allowlist logic differently, opening an XSS vector via paste-URL `javascript:` schemes.

Output: A gitignored uploads directory, a 12 MB-ceiling Blazor server, a singleton URL validator, and a Scoped storage service — plus xUnit Theory matrices covering both H5 (scheme rejection) and H3 (magic-byte rejection).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-photos-prod-ready-infrastructure/09-CONTEXT.md
@.planning/phases/09-photos-prod-ready-infrastructure/09-RESEARCH.md
@.planning/phases/09-photos-prod-ready-infrastructure/09-PATTERNS.md
@.planning/research/PITFALLS.md
@src/CookBot.Web/Program.cs
@src/CookBot.Infrastructure/AI/SecretRedactor.cs
@src/CookBot.Application/DependencyInjection.cs
@src/CookBot.Web/Services/AiApiKeyResolutionService.cs

<interfaces>
<!-- Anchors from the codebase the executor will plug into. -->

Existing Program.cs DI block (Program.cs lines 16–27):
- builder.Services.AddInfrastructure(builder.Configuration);    // line 18
- builder.Services.AddScoped<CurrentUserService>();              // line 19
- builder.Services.AddScoped<AiApiKeyResolutionService>();
- builder.Services.AddScoped<AiApiKeyShareService>();
- builder.Services.AddScoped<CookbookTransferService>();
- builder.Services.AddScoped<CookbookPdfService>();
- builder.Services.AddScoped<IScheduledRecipeService, ScheduledRecipeService>();
- builder.Services.AddScoped<IRecipeMadeService, RecipeMadeService>();
- builder.Services.Configure<CookBotSettings>(builder.Configuration.GetSection("CookBot"));

Existing Program.cs middleware order (lines 30–40):
- app.UseExceptionHandler (Production only)
- app.UseAntiforgery
- app.MapStaticAssets
- app.MapRazorComponents<App>().AddInteractiveServerRenderMode

AddApplication registration pattern from src/CookBot.Application/DependencyInjection.cs — siblings include RecipeValidator. The new RecipePhotoUrlValidator goes here as Singleton.

Validator return shape (RecipeValidator precedent — Application/Recipes/RecipeValidator.cs lines 13–34): never throws, returns a result envelope; null produces a single ValidationError.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PHOTO-01 — Add wwwroot/uploads/ to .gitignore as a STANDALONE commit (must precede every other Phase 9 change)</name>
  <read_first>
    - /home/noah/Desktop/projects/freeloves-cookbot/.gitignore (entire file — 50 lines)
    - /home/noah/Desktop/projects/freeloves-cookbot/.planning/phases/09-photos-prod-ready-infrastructure/09-PATTERNS.md lines 316–325 (exact .gitignore shape)
    - /home/noah/Desktop/projects/freeloves-cookbot/.planning/research/PITFALLS.md §C5 (why uploads must be gitignored FIRST)
  </read_first>
  <action>
    Append a new "## Phase 9 / PHOTO-01" block to .gitignore, sibling to the existing "## ASP.NET" block that already excludes wwwroot/lib/. Use this literal text (no trailing whitespace):

    `## Phase 9 / PHOTO-01 — user-uploaded recipe photos. Runtime contents are excluded`
    `# but the directory must exist on fresh clone, so .gitkeep is force-included.`
    `src/CookBot.Web/wwwroot/uploads/*`
    `!src/CookBot.Web/wwwroot/uploads/.gitkeep`

    Then create the empty placeholder file `src/CookBot.Web/wwwroot/uploads/.gitkeep` (zero bytes — literally `touch`).

    Then commit IMMEDIATELY with message `chore(09-01): PHOTO-01 — gitignore wwwroot/uploads runtime contents [first commit of Phase 9]` and DO NOT proceed to Task 2 until this commit lands. This sequencing is mandated by PITFALL C5 (user uploaded a photo before .gitignore was in place → accidentally tracked) and 09-CONTEXT.md `<canonical_refs>` line 116.

    Touch no other file in this task. Do NOT amend a later commit — PHOTO-01 must be its own commit so the audit trail makes the FIRST-commit-of-phase ordering obvious.
  </action>
  <verify>
    <automated>
      grep -q '^src/CookBot.Web/wwwroot/uploads/\*$' .gitignore && \
      grep -q '^\!src/CookBot.Web/wwwroot/uploads/.gitkeep$' .gitignore && \
      test -f src/CookBot.Web/wwwroot/uploads/.gitkeep && \
      test ! -s src/CookBot.Web/wwwroot/uploads/.gitkeep && \
      git log -1 --pretty=%s | grep -q "PHOTO-01"
    </automated>
  </verify>
  <acceptance_criteria>
    - `.gitignore` contains both `src/CookBot.Web/wwwroot/uploads/*` and `!src/CookBot.Web/wwwroot/uploads/.gitkeep` lines
    - `src/CookBot.Web/wwwroot/uploads/.gitkeep` exists and is exactly 0 bytes
    - The PHOTO-01 commit message contains "PHOTO-01" and is the most recent commit on the branch at the time this task completes
    - `git check-ignore src/CookBot.Web/wwwroot/uploads/test.jpg` returns exit code 0 (file is ignored)
    - `git check-ignore src/CookBot.Web/wwwroot/uploads/.gitkeep` returns exit code 1 (file is NOT ignored — negation works)
  </acceptance_criteria>
  <done>PHOTO-01 closed; uploads/ runtime contents are excluded from git; directory survives fresh clone via .gitkeep; this is the first commit of Phase 9.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PHOTO-07 — Create RecipePhotoUrlValidator (Singleton; never-throw envelope; full H5 rejection matrix) — TEST FIRST</name>
  <files>src/CookBot.Application/Services/RecipePhotoUrlValidator.cs, src/CookBot.Application/DependencyInjection.cs, tests/CookBot.Tests/Services/RecipePhotoUrlValidatorTests.cs</files>
  <read_first>
    - /home/noah/Desktop/projects/freeloves-cookbot/src/CookBot.Application/DependencyInjection.cs (entire file)
    - /home/noah/Desktop/projects/freeloves-cookbot/src/CookBot.Application/Recipes/RecipeValidator.cs (lines 1–60 — never-throw envelope shape; analog for return-envelope philosophy)
    - /home/noah/Desktop/projects/freeloves-cookbot/src/CookBot.Infrastructure/AI/SecretRedactor.cs (entire file — pure-static analog for call shape)
    - /home/noah/Desktop/projects/freeloves-cookbot/tests/CookBot.Tests/Recipes/StepTemperatureTests.cs (entire file — Theory + InlineData matrix pattern)
    - /home/noah/Desktop/projects/freeloves-cookbot/.planning/phases/09-photos-prod-ready-infrastructure/09-PATTERNS.md lines 130–201 (recommended shape for the new validator)
    - /home/noah/Desktop/projects/freeloves-cookbot/.planning/research/PITFALLS.md §H5 (PASTE-URL scheme allowlist matrix)
  </read_first>
  <behavior>
    Test matrix (xUnit Theory + InlineData — see 09-PATTERNS.md lines 647–662 for the exact matrix):
    - "https://example.com/photo.jpg" → accept; normalized = "https://example.com/photo.jpg"
    - "http://example.com/photo.png" → accept
    - "HTTPS://Example.com/PHOTO.JPG" → accept (Uri.Scheme is case-insensitive)
    - "  https://example.com/photo.jpg  " → accept; normalized trims whitespace
    - "javascript:alert(1)" → reject; errorCode = "SCHEME_NOT_ALLOWED"
    - "data:image/png;base64,iVBOR..." → reject; errorCode = "SCHEME_NOT_ALLOWED"
    - "file:///etc/passwd" → reject; errorCode = "SCHEME_NOT_ALLOWED"
    - "ftp://example.com/photo.jpg" → reject; errorCode = "SCHEME_NOT_ALLOWED"
    - "vbscript:msgbox(1)" → reject; errorCode = "SCHEME_NOT_ALLOWED"
    - "//example.com/photo.jpg" → reject; errorCode = "PROTOCOL_RELATIVE_REJECTED"
    - "/relative/path" → reject; errorCode = "MALFORMED" (Uri.TryCreate with Absolute fails)
    - "not a url" → reject; errorCode = "MALFORMED"
    - "" → accept; normalized = null (PhotoUrl is nullable — empty input is the "no photo" signal)
    - null → accept; normalized = null
    - "   " (whitespace only) → accept; normalized = null
    - Validator never throws on any input.
  </behavior>
  <action>
    RED phase: Write `tests/CookBot.Tests/Services/RecipePhotoUrlValidatorTests.cs` first. Class name `RecipePhotoUrlValidatorTests`. Mirror the Theory+InlineData shape from `StepTemperatureTests.cs`. Use a single `[Theory] public void TryValidate_PerH5Matrix(string? input, bool expectAccept, string? expectedErrorCode)` with all 15 rows above as InlineData. Add a separate `[Fact] public void TryValidate_NormalizesWhitespace_Around_Url()` covering the trim case. Run `dotnet test --filter "FullyQualifiedName~RecipePhotoUrlValidatorTests"` — MUST fail (red) because RecipePhotoUrlValidator does not yet exist.

    GREEN phase: Create `src/CookBot.Application/Services/RecipePhotoUrlValidator.cs`. Top-of-file `namespace CookBot.Application.Services;`. `public sealed class RecipePhotoUrlValidator`. One public method:

      `public bool TryValidate(string? input, out string? normalized, out string? errorCode)`

    Logic (in order per 09-PATTERNS.md lines 193–198):
      1. If `string.IsNullOrWhiteSpace(input)` → `normalized = null; errorCode = null; return true;`
      2. `var trimmed = input!.Trim();` then if `trimmed.StartsWith("//", StringComparison.Ordinal)` → `normalized = null; errorCode = "PROTOCOL_RELATIVE_REJECTED"; return false;`
      3. If `!Uri.TryCreate(trimmed, UriKind.Absolute, out var uri)` → `normalized = null; errorCode = "MALFORMED"; return false;`
      4. If `uri.Scheme is not ("http" or "https")` → `normalized = null; errorCode = "SCHEME_NOT_ALLOWED"; return false;`
      5. Otherwise → `normalized = uri.AbsoluteUri; errorCode = null; return true;`

    Add to `src/CookBot.Application/DependencyInjection.cs` `AddApplication` method: `services.AddSingleton<RecipePhotoUrlValidator>();` — sibling to the existing RecipeValidator registration. No new using import needed (same namespace).

    Run `dotnet test --filter "FullyQualifiedName~RecipePhotoUrlValidatorTests" --no-build` then `dotnet build` then `dotnet test` — MUST pass (green) on all 16+ test rows.

    Do NOT plug the validator into AnthropicAiService here (that wiring is Plan 09-05 PROD-07 territory). The validator is created and DI-registered here so downstream plans can `[Inject]` it without writing it themselves.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/freeloves-cookbot && dotnet build src/CookBot.Application/CookBot.Application.csproj --nologo -v quiet && dotnet test tests/CookBot.Tests/CookBot.Tests.csproj --filter "FullyQualifiedName~RecipePhotoUrlValidatorTests" --no-build --nologo -v quiet</automated>
  </verify>
  <acceptance_criteria>
    - File `src/CookBot.Application/Services/RecipePhotoUrlValidator.cs` exists and exports `public sealed class RecipePhotoUrlValidator` with method signature `public bool TryValidate(string? input, out string? normalized, out string? errorCode)`
    - `grep -c "AddSingleton<RecipePhotoUrlValidator>" src/CookBot.Application/DependencyInjection.cs` returns at least 1
    - All test rows in `RecipePhotoUrlValidatorTests` pass (xUnit Theory shows ≥15 test cases passing)
    - Validator never throws — every InlineData row reaches an `Assert`, none triggers an unhandled exception
    - `grep -q 'PROTOCOL_RELATIVE_REJECTED' tests/CookBot.Tests/Services/RecipePhotoUrlValidatorTests.cs` and same for `SCHEME_NOT_ALLOWED` and `MALFORMED` — all three error codes covered
  </acceptance_criteria>
  <done>PHOTO-07 partially closed (validator exists + DI-registered + tested). Wire-up to AnthropicAiService AI return path happens in Plan 09-05 alongside the rest of the AI structured-output work.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: PHOTO-02/03/05 — LocalRecipePhotoStorage + ImageMagicBytes helper + magic-byte + path-traversal test matrix — TEST FIRST</name>
  <files>src/CookBot.Web/Services/LocalRecipePhotoStorage.cs, src/CookBot.Web/Services/ImageMagicBytes.cs, src/CookBot.Web/Program.cs, tests/CookBot.Tests/Services/LocalRecipePhotoStorageTests.cs</files>
  <read_first>
    - /home/noah/Desktop/projects/freeloves-cookbot/src/CookBot.Web/Services/AiApiKeyResolutionService.cs (entire file — DI lifetime + ctor analog)
    - /home/noah/Desktop/projects/freeloves-cookbot/src/CookBot.Web/Services/CookbookPdfService.cs (skim — Scoped Web service with IO output analog)
    - /home/noah/Desktop/projects/freeloves-cookbot/.planning/phases/09-photos-prod-ready-infrastructure/09-RESEARCH.md lines 299–377 (Item 5: magic-byte snippets verbatim + path-traversal defense + test matrix)
    - /home/noah/Desktop/projects/freeloves-cookbot/.planning/phases/09-photos-prod-ready-infrastructure/09-PATTERNS.md lines 204–251 (LocalRecipePhotoStorage shape) and lines 667–700 (storage test matrix)
    - /home/noah/Desktop/projects/freeloves-cookbot/.planning/research/PITFALLS.md §H1, §H2, §H3 (size limits, path traversal, nosniff)
  </read_first>
  <behavior>
    `ImageMagicBytes.DetectExtension(ReadOnlySpan<byte> head)` (static):
    - 12-byte JPEG (FF D8 FF E0 …) → ".jpg"
    - 12-byte JPEG (FF D8 FF E1 …) → ".jpg"
    - 12-byte PNG (89 50 4E 47 0D 0A 1A 0A …) → ".png"
    - 12-byte GIF87a (47 49 46 38 37 61 …) → ".gif"
    - 12-byte GIF89a (47 49 46 38 39 61 …) → ".gif"
    - 12-byte WebP (52 49 46 46 … 57 45 42 50 at offset 8) → ".webp"
    - SVG bytes "<?xml" (3C 3F 78 6D 6C …) → null (rejected)
    - SVG bytes "<svg" (3C 73 76 67 …) → null (rejected)
    - HTML bytes "<!DOC" (3C 21 44 4F 43 …) → null (rejected)
    - Length < 3 → null (too short)
    - Truncated WebP (RIFF only, length < 12) → null

    `LocalRecipePhotoStorage`:
    - Constructor wires `IWebHostEnvironment` (for WebRootPath) and `ILogger<LocalRecipePhotoStorage>`. Computes `_uploadsDir = Path.Combine(env.WebRootPath, "uploads")` and ensures it exists via `Directory.CreateDirectory(_uploadsDir)` (idempotent).
    - `Task<string> SaveAsync(IBrowserFile file, CancellationToken ct)` returns the public URL (e.g. `/uploads/{guid}.jpg`).
    - Opens stream via `file.OpenReadStream(maxAllowedSize: 10 * 1024 * 1024, ct)` (PHOTO-03), reads first 12 bytes via `ReadAtLeastAsync`, calls `ImageMagicBytes.DetectExtension(head)`.
    - If extension is null → throws `InvalidImageException` (custom — inline define inside the file as `public sealed class InvalidImageException : Exception`).
    - Generates filename `$"{Guid.NewGuid():N}{ext}"` (PHOTO-05 — never trust `file.Name`).
    - Defense-in-depth path-traversal check: compare `Path.GetFullPath(savePath)` against `Path.GetFullPath(_uploadsDir)` with `StartsWith(..., StringComparison.Ordinal)` (PITFALL H2). If outside → throws `InvalidOperationException("Path traversal attempt detected.")`.
    - Re-opens via `file.OpenReadStream(maxAllowedSize: 10 * 1024 * 1024, ct)` (the first open has consumed 12 bytes; PITFALLS bottom of 09-RESEARCH section "Reading IBrowserFile twice"), copies to `File.Create(fullPath)`.
    - Returns `$"/uploads/{safeName}"` as the URL the editor binds to PhotoUrl.

    `Program.cs` additions (all in one commit with this task):
    - Raise three size limits to 12 MB (12 * 1024 * 1024 bytes = 12582912):
      - `builder.Services.Configure<Microsoft.AspNetCore.Server.Kestrel.Core.KestrelServerOptions>(o => o.Limits.MaxRequestBodySize = 12 * 1024 * 1024);`
      - `builder.Services.Configure<Microsoft.AspNetCore.Http.Features.FormOptions>(o => o.MultipartBodyLengthLimit = 12 * 1024 * 1024);`
      - The Razor Components builder: change `builder.Services.AddRazorComponents().AddInteractiveServerComponents();` to `builder.Services.AddRazorComponents().AddInteractiveServerComponents(o => o.MaximumReceiveMessageSize = 12 * 1024 * 1024);` (per PITFALL H1 — the SignalR limit is the one that silently drops circuits at 32 KB by default).
    - Register `builder.Services.AddScoped<LocalRecipePhotoStorage>();` sibling to existing `AddScoped<CookbookPdfService>()` (Program.cs line 23).
    - After `app.MapStaticAssets();` (line 38) — insert explicit `UseStaticFiles` for /uploads (PHOTO-06):
      ```
      app.UseStaticFiles(new Microsoft.AspNetCore.Builder.StaticFileOptions
      {
          FileProvider = new Microsoft.Extensions.FileProviders.PhysicalFileProvider(
              System.IO.Path.Combine(app.Environment.WebRootPath, "uploads")),
          RequestPath = "/uploads",
          OnPrepareResponse = ctx =>
          {
              ctx.Context.Response.Headers["X-Content-Type-Options"] = "nosniff";
          }
      });
      ```
      (using statements may be added at the top for brevity instead of fully-qualifying.)
  </behavior>
  <action>
    RED phase: Write `tests/CookBot.Tests/Services/LocalRecipePhotoStorageTests.cs`. The test class focuses on `ImageMagicBytes.DetectExtension` (pure static — does not need IWebHostEnvironment) plus a `[Fact]` path-traversal test that constructs a fake `IWebHostEnvironment` (use `Mock<IWebHostEnvironment>` or a hand-rolled stub returning a temp directory). Use the exact `[Theory] [InlineData(new byte[]{...}, ".jpg")] ...` shape from 09-PATTERNS.md lines 673–700. Run `dotnet test --filter "FullyQualifiedName~LocalRecipePhotoStorageTests"` — MUST fail (red).

    GREEN phase:
    1. Create `src/CookBot.Web/Services/ImageMagicBytes.cs` with the verbatim code from 09-RESEARCH.md lines 304–334. `namespace CookBot.Web.Services;` `public static class ImageMagicBytes`. Methods: `IsJpeg`, `IsPng`, `IsGif`, `IsWebp`, `DetectExtension`. JPEG check requires `head.Length >= 3 && head[0] == 0xFF && head[1] == 0xD8 && head[2] == 0xFF`. PNG check uses an 8-byte signature `ReadOnlySpan<byte> PngSig` and `head[..8].SequenceEqual(PngSig)`. GIF check requires byte[4] is 0x37 or 0x39 (GIF87a/GIF89a). WebP check requires `head.Length >= 12` and bytes 0-3 are "RIFF" and bytes 8-11 are "WEBP".
    2. Create `src/CookBot.Web/Services/LocalRecipePhotoStorage.cs`. `namespace CookBot.Web.Services;` `public sealed class LocalRecipePhotoStorage`. Constructor: `LocalRecipePhotoStorage(IWebHostEnvironment env, ILogger<LocalRecipePhotoStorage> logger)`. Stores `_uploadsDir = Path.Combine(env.WebRootPath, "uploads")`; calls `Directory.CreateDirectory(_uploadsDir)` once in ctor (idempotent). Public method: `public async Task<string> SaveAsync(IBrowserFile file, CancellationToken ct = default)`. Inline `public sealed class InvalidImageException : Exception { public InvalidImageException(string message) : base(message) { } }` at end of file (file-scoped). Use the two-pass stream logic from 09-RESEARCH.md lines 340–362. Use `Guid.NewGuid().ToString("N")` for the filename (matches the documented 32-char hex GUID convention without dashes). The path-traversal check uses `Path.GetFullPath` on both sides and `StringComparison.Ordinal`.
    3. Modify `src/CookBot.Web/Program.cs` per the behavior spec above: three size limits, AddScoped registration, explicit UseStaticFiles for /uploads with nosniff. Place the size-limit Configures BEFORE `builder.Build()` (around line 16–17, before `var app = builder.Build();`). Place the UseStaticFiles call AFTER `app.MapStaticAssets();` (line 38) and BEFORE `app.MapRazorComponents` (line 39).

    Run `dotnet test --filter "FullyQualifiedName~LocalRecipePhotoStorageTests" --no-build` — MUST pass.

    Run `dotnet build` — entire solution must compile.

    Do NOT touch RecipeEditor.razor in this task (that's Plan 09-02). Do NOT add any photo upload `<InputFile>` element anywhere yet — the editor wiring is Plan 09-02.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/freeloves-cookbot && dotnet build --nologo -v quiet && dotnet test tests/CookBot.Tests/CookBot.Tests.csproj --filter "FullyQualifiedName~LocalRecipePhotoStorageTests" --no-build --nologo -v quiet && grep -c "MaximumReceiveMessageSize" src/CookBot.Web/Program.cs | xargs -I{} test {} -ge 1 && grep -c "MaxRequestBodySize" src/CookBot.Web/Program.cs | xargs -I{} test {} -ge 1 && grep -c "MultipartBodyLengthLimit" src/CookBot.Web/Program.cs | xargs -I{} test {} -ge 1 && grep -q 'RequestPath = "/uploads"' src/CookBot.Web/Program.cs && grep -q 'X-Content-Type-Options' src/CookBot.Web/Program.cs</automated>
  </verify>
  <acceptance_criteria>
    - File `src/CookBot.Web/Services/ImageMagicBytes.cs` exists with public static class `ImageMagicBytes` and method `DetectExtension(ReadOnlySpan<byte>)` returning `string?` (one of `.jpg`/`.png`/`.gif`/`.webp` or `null`)
    - File `src/CookBot.Web/Services/LocalRecipePhotoStorage.cs` exists with `public sealed class LocalRecipePhotoStorage` and `public async Task<string> SaveAsync(IBrowserFile, CancellationToken)` method
    - All three size-limit configurations present in Program.cs (verifiable via grep): `MaxRequestBodySize`, `MultipartBodyLengthLimit`, `MaximumReceiveMessageSize`
    - All three size-limit values equal `12 * 1024 * 1024` or `12582912` (verifiable: `grep -E '12 \* 1024 \* 1024|12582912' src/CookBot.Web/Program.cs | wc -l` returns ≥ 3)
    - Program.cs contains `AddScoped<LocalRecipePhotoStorage>` registration
    - Program.cs contains explicit `UseStaticFiles` with `RequestPath = "/uploads"` AND sets `X-Content-Type-Options: nosniff` in `OnPrepareResponse`
    - `LocalRecipePhotoStorageTests` passes all rows in the accept-types matrix AND all rows in the reject matrix (SVG, HTML, too-short, truncated WebP)
    - Path-traversal `[Fact]` test passes (constructed savePath outside _uploadsDir throws InvalidOperationException)
    - `dotnet build` returns 0 warnings related to these new files; full solution still compiles
  </acceptance_criteria>
  <done>PHOTO-02 (magic-byte sniff), PHOTO-03 (10 MB OpenReadStream cap), PHOTO-04 (three size limits), PHOTO-05 (GUID filename), PHOTO-06 (explicit UseStaticFiles + nosniff), PHOTO-07 storage half — all closed. PHOTO-08 onerror handler is editor/view-surface concern (Plans 09-02 / 09-03). The validator and storage service are now ready for Plan 09-02 to plug into RecipeEditor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Blazor circuit (SignalR) | untrusted file bytes + paste-URL strings cross here |
| Blazor circuit → disk (wwwroot/uploads/) | untrusted bytes cross here; filename is server-generated |
| /uploads HTTP static-files response → browser | uploaded bytes leave the trust boundary; sniff-derived Content-Type stays opaque via nosniff |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | Tampering | LocalRecipePhotoStorage.SaveAsync filename | mitigate | Server-generated `Guid.NewGuid().ToString("N")` filename; client `IBrowserFile.Name` never used. `Path.GetFullPath` prefix assertion as defense-in-depth (PITFALL H2). |
| T-09-01-02 | Spoofing | uploaded file MIME / extension | mitigate | Magic-byte sniff (first 12 bytes) — never trusts `IBrowserFile.ContentType`. Extension derived from magic bytes only. Rejects SVG, HTML-as-jpg (PITFALL H3). |
| T-09-01-03 | Information Disclosure | paste-URL `file:///` scheme | mitigate | `RecipePhotoUrlValidator` scheme allowlist (`http`/`https` only). Same validator runs editor-side AND on AI-emitted PhotoUrl in Plan 09-05 (PITFALL H5). |
| T-09-01-04 | Elevation of Privilege | paste-URL `javascript:` scheme reaching `<img src>` and being rendered | mitigate | Scheme-allowlist rejection before persist. Even if it reached `<img>`, browsers don't execute `javascript:` URLs in `<img src>` — but rejection is the canonical defense. |
| T-09-01-05 | Information Disclosure | static-file Content-Type sniffing by browser on /uploads response | mitigate | `X-Content-Type-Options: nosniff` header on every /uploads response via `OnPrepareResponse`. |
| T-09-01-06 | Denial of Service | 100 MB file upload silently drops SignalR circuit (UX failure mode) | mitigate | Three size limits raised to 12 MB at the server: Kestrel `MaxRequestBodySize`, FormOptions `MultipartBodyLengthLimit`, Blazor `MaximumReceiveMessageSize`. Editor will surface a toast on 10 MB excess (Plan 09-02). |
| T-09-01-07 | Denial of Service (storage exhaustion) | malicious user uploads 1000 × 10 MB files | accept | Trusted-LAN posture per CookBotSettings.AuthMode = Disabled. Self-hoster monitors disk. v1.4+ may add quota; out of scope for v1.3. |
| T-09-01-SC | Tampering (supply chain) | NuGet installs in this plan | mitigate | No NEW NuGets in Plan 09-01. Only first-party Microsoft references (already in csproj). Slopcheck not applicable; PROD-06/PROD-05 packages land in later plans with their own gate. |
</threat_model>

<verification>
After Task 3, smoke-check Program.cs by running the app (`./run.sh` in background, then `curl -sf http://localhost:7000/` should return 200, then kill). The /uploads route returns 404 with no file present — expected; an actual upload happens in Plan 09-02.

Wave-merge gate: `dotnet test tests/CookBot.Tests/` must be green. Existing Phase 8 tests must not regress.
</verification>

<success_criteria>
1. `.gitignore` PHOTO-01 entry committed AS A STANDALONE COMMIT preceding all other Phase 9 work
2. `dotnet test --filter "FullyQualifiedName~RecipePhotoUrlValidatorTests"` green (≥15 cases)
3. `dotnet test --filter "FullyQualifiedName~LocalRecipePhotoStorageTests"` green (accept + reject + path-traversal cases)
4. `grep -E 'MaxRequestBodySize|MultipartBodyLengthLimit|MaximumReceiveMessageSize' src/CookBot.Web/Program.cs | wc -l` ≥ 3
5. `grep -q 'X-Content-Type-Options' src/CookBot.Web/Program.cs` exits 0
6. Full solution builds without new warnings; full test suite green
</success_criteria>

<output>
Create `.planning/phases/09-photos-prod-ready-infrastructure/09-01-SUMMARY.md` when done. Include: PHOTO-01 commit SHA, three size-limit verification snippet, validator test count, magic-byte matrix size, any deviation from 09-RESEARCH Item 5 snippets.
</output>
