---
phase: 09-photos-prod-ready-infrastructure
plan: 04
type: execute
wave: 3
depends_on: ["09-01"]
files_modified:
  - src/CookBot.Infrastructure/CookBot.Infrastructure.csproj
  - src/CookBot.Infrastructure/Data/CookBotDbContext.cs
  - src/CookBot.Web/Program.cs
  - src/CookBot.Infrastructure/Data/DatabaseSeeder.cs
  - src/CookBot.Web/Services/AiApiKeyResolutionService.cs
  - src/CookBot.Web/Components/Pages/EditProfile.razor
  - src/CookBot.Infrastructure/AI/SecretRedactor.cs
  - src/CookBot.Infrastructure/Migrations/<timestamp>_AddDataProtectionKeysTable.cs
  - tests/CookBot.Tests/AI/SentinelPrefixMigrationTests.cs
  - tests/CookBot.Tests/AI/KeyShareEncryptionRoundTripTests.cs
  - tests/CookBot.Tests/AI/SecretRedactorDecryptPathTests.cs
autonomous: false
requirements: [PROD-06, PROD-07, PROD-08, PROD-09, PROD-10, PROD-11]
must_haves:
  truths:
    - "Microsoft.AspNetCore.DataProtection.EntityFrameworkCore 10.0.8 added to CookBot.Infrastructure (PROD-06)"
    - "CookBotDbContext implements IDataProtectionKeyContext with DbSet<DataProtectionKey> (PROD-06)"
    - "AddDataProtection().SetApplicationName('FreelovesCookBot').PersistKeysToDbContext<CookBotDbContext>() registered in Program.cs (PROD-07)"
    - "AddDataProtectionKeysTable EF migration adds the DataProtectionKeys table; backup fires automatically via DatabaseSeeder (PROD-06)"
    - "AiApiKeyResolutionService READ PATH wraps the stored value with sentinel-prefix detection: value.Length >= 44 && value.StartsWith('CfDJ8', StringComparison.Ordinal) (PROD-08; NOT an EF ValueConverter per 09-RESEARCH correction)"
    - "Sentinel-prefix re-encryption pass in DatabaseSeeder runs AFTER null-canonical guard, AFTER 365-day cleanup (Plan 09-05), BEFORE AI gate validation; idempotent on second boot (PROD-09, PITFALL C3)"
    - "Single shared protector scope 'AiApiKey.v1' used by encrypt write path AND decrypt read path (PROD-08, PITFALL C2)"
    - "AiApiKeyShare → AiApiKeyResolutionService recipient resolution decrypts owner's row via the same shared scope; recipient never sees plaintext (PROD-11)"
    - "EditProfile.razor save path runs IDataProtector.Protect on new/edited keys before SaveChangesAsync (PROD-08 write path)"
    - "SecretRedactor.Redact covers CryptographicException messages — neither plaintext nor ciphertext leak to logs/UI (PROD-10, PITFALL C4)"
    - "Three integration tests prove: first-boot plaintext→ciphertext idempotent (PROD-09), share-then-resolve round-trip (PROD-11, C2), decrypt-error secret redaction (PROD-10, C4)"
  artifacts:
    - path: "src/CookBot.Infrastructure/CookBot.Infrastructure.csproj"
      provides: "Microsoft.AspNetCore.DataProtection.EntityFrameworkCore 10.0.8 PackageReference"
      contains: "Microsoft.AspNetCore.DataProtection.EntityFrameworkCore"
    - path: "src/CookBot.Infrastructure/Data/CookBotDbContext.cs"
      provides: "Implements IDataProtectionKeyContext; exposes DbSet<DataProtectionKey>"
      contains: "IDataProtectionKeyContext"
    - path: "src/CookBot.Web/Program.cs"
      provides: "AddDataProtection().SetApplicationName('FreelovesCookBot').PersistKeysToDbContext<CookBotDbContext>() registration"
      contains: "AddDataProtection"
    - path: "src/CookBot.Infrastructure/Data/DatabaseSeeder.cs"
      provides: "Idempotent sentinel-prefix re-encryption pass; LooksLikeDataProtectionCiphertext helper (or via shared helper)"
      contains: "AiApiKey.v1"
    - path: "src/CookBot.Web/Services/AiApiKeyResolutionService.cs"
      provides: "Read path uses IDataProtector.Unprotect with sentinel-prefix detection; legacy plaintext rows pass through unchanged (DatabaseSeeder will re-encrypt at next boot)"
      contains: "Unprotect"
    - path: "src/CookBot.Web/Components/Pages/EditProfile.razor"
      provides: "Save path protects new/edited AI API key via IDataProtector.Protect"
      contains: "_protector.Protect"
    - path: "src/CookBot.Infrastructure/AI/SecretRedactor.cs"
      provides: "Redact() covers CryptographicException messages — adds a ciphertext-prefix pattern to strip CfDJ8... blobs"
      contains: "CfDJ8"
    - path: "tests/CookBot.Tests/AI/SentinelPrefixMigrationTests.cs"
      provides: "FirstBoot plaintext → ciphertext + SecondBoot no-op + round-trip Unprotect"
    - path: "tests/CookBot.Tests/AI/KeyShareEncryptionRoundTripTests.cs"
      provides: "Owner sets plaintext key → DatabaseSeeder encrypts → recipient resolves and decrypts to original plaintext via shared scope"
    - path: "tests/CookBot.Tests/AI/SecretRedactorDecryptPathTests.cs"
      provides: "CryptographicException messages do not leak ciphertext or plaintext (PROD-10, C4)"
  key_links:
    - from: "Program.cs"
      to: "DataProtection key ring"
      via: "AddDataProtection().SetApplicationName(...).PersistKeysToDbContext<CookBotDbContext>()"
      pattern: "PersistKeysToDbContext<CookBotDbContext>"
    - from: "DatabaseSeeder.SeedAsync"
      to: "IDataProtectionProvider"
      via: "scope.ServiceProvider.GetRequiredService<IDataProtectionProvider>(); CreateProtector('AiApiKey.v1')"
      pattern: "CreateProtector\\(\"AiApiKey.v1\"\\)"
    - from: "AiApiKeyResolutionService.ResolveAsync"
      to: "IDataProtector.Unprotect"
      via: "DecryptIfNeeded helper using LooksLikeDataProtectionCiphertext"
      pattern: "LooksLikeDataProtectionCiphertext\\|Unprotect"
    - from: "EditProfile.razor SaveProfile"
      to: "IDataProtector.Protect"
      via: "_protector.Protect(_aiApiKeyEdit.Trim()) on non-empty write"
      pattern: "Protect"
---

<objective>
Encrypt UserProfile.AiApiKey at rest using ASP.NET Core Data Protection with the key ring colocated in cookbot.db (PROD-06/07). The migration from existing plaintext rows is idempotent (PROD-09, PITFALL C3) via a sentinel-prefix detection that runs in DatabaseSeeder.SeedAsync. The read path is wrapped in AiApiKeyResolutionService — NOT an EF ValueConverter (the value-converter approach forces every read through Unprotect, which throws on legacy plaintext during the very migration that's supposed to fix it, per 09-RESEARCH correction). The single shared protector scope "AiApiKey.v1" makes the C2-mitigation work for the share semantics: owner encrypts with scope X, recipient decrypts with the same scope X — no per-user purpose.

Purpose: Trusted-LAN posture stays the same, but a stolen cookbot.db.bak suddenly stops handing the attacker a working Anthropic key. Key sharing continues to function — owner's row is decrypted server-side at resolution time. SecretRedactor coverage prevents CryptographicException messages from leaking plaintext OR ciphertext into logs.

Output: One new NuGet, one DbContext interface, one Program.cs DI block, one EF migration, one read-path wrapper, one write-path Protect call, one extended SecretRedactor, and three integration test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-photos-prod-ready-infrastructure/09-CONTEXT.md
@.planning/phases/09-photos-prod-ready-infrastructure/09-RESEARCH.md
@.planning/phases/09-photos-prod-ready-infrastructure/09-PATTERNS.md
@.planning/research/PITFALLS.md
@src/CookBot.Infrastructure/Data/CookBotDbContext.cs
@src/CookBot.Infrastructure/Data/DatabaseSeeder.cs
@src/CookBot.Web/Program.cs
@src/CookBot.Web/Services/AiApiKeyResolutionService.cs
@src/CookBot.Web/Services/AiApiKeyShareService.cs
@src/CookBot.Web/Components/Pages/EditProfile.razor
@src/CookBot.Infrastructure/AI/SecretRedactor.cs
@src/CookBot.Domain/Entities/UserProfile.cs
@tests/CookBot.Tests/AI/SecretRedactorTests.cs
@tests/CookBot.Tests/Recipes/RecipeTagBackfillTests.cs

<interfaces>
<!-- Anchors for the executor. -->

NEW NuGet: Microsoft.AspNetCore.DataProtection.EntityFrameworkCore version 10.0.8 (per 09-RESEARCH §"Standard Stack"; first-party Microsoft, MS Learn cited; legitimacy approved). Add to src/CookBot.Infrastructure/CookBot.Infrastructure.csproj — the [ASSUMED]/[SUS] gate does NOT apply (first-party Microsoft, audit table line 113 of 09-RESEARCH approves).

CookBotDbContext (src/CookBot.Infrastructure/Data/CookBotDbContext.cs):
- Currently declares 15 DbSets (lines 10–26).
- Phase 9 adds: implements `Microsoft.AspNetCore.DataProtection.EntityFrameworkCore.IDataProtectionKeyContext`. Required member: `public DbSet<DataProtectionKey> DataProtectionKeys => Set<DataProtectionKey>();`. Adds `using Microsoft.AspNetCore.DataProtection.EntityFrameworkCore;`.

Program.cs registration block (lines 16–27): after `builder.Services.AddInfrastructure(builder.Configuration);` (line 18), insert:
  `builder.Services.AddDataProtection().SetApplicationName("FreelovesCookBot").PersistKeysToDbContext<CookBotDbContext>();`
The SetApplicationName must be a literal — do NOT read from IConfiguration (per 09-RESEARCH Item 6 pitfall: changing it between deploys silently breaks every encrypted key).

DatabaseSeeder.SeedAsync (src/CookBot.Infrastructure/Data/DatabaseSeeder.cs):
- Current signature (line 19 area): SeedAsync(CookBotDbContext context, IDatabaseBackupService backupService, JsonRecipeSerializer serializer, string contentRootPath)
- Phase 9 adds two parameters: IDataProtectionProvider dataProtectionProvider, ILogger logger
- Boot order per 09-CONTEXT "Established Patterns" line 165: backup → migrate → seed → null-canonical guard (existing) → 365-day cleanup (Plan 09-05) → sentinel-prefix re-encryption pass (THIS PLAN) → existing seed logic. The 365-day cleanup is in a DIFFERENT plan (09-05) — coordinate via files_modified: BOTH plans modify DatabaseSeeder.cs. Plan 09-04 is Wave 3, Plan 09-05 is Wave 5 — sequential, so Plan 09-04 writes the boot order including a placeholder for the 365-day cleanup insertion point ("// 365-day AiUsageLog cleanup will be inserted here by Plan 09-05") OR Plan 09-05 inserts the cleanup before the re-encryption pass and updates Plan 09-04's placement comment. The executor MUST coordinate.

Actually since Plan 09-04 is the FIRST to modify DatabaseSeeder.cs in this phase, this plan adds the sentinel-prefix re-encryption pass and places it AFTER the null-canonical guard and BEFORE the "Step 4: existing seed logic" line 56 block. Plan 09-05 will then insert the 365-day cleanup BEFORE the re-encryption pass.

Program.cs DatabaseSeeder.SeedAsync call (lines 42–52): pass the two new args:
  var dataProtectionProvider = scope.ServiceProvider.GetRequiredService<IDataProtectionProvider>();
  var seederLogger = scope.ServiceProvider.GetRequiredService<ILogger<CookBotDbContext>>();
  // (or any ILogger; the seeder is static and accepts ILogger as a non-generic dependency)

AiApiKeyResolutionService (src/CookBot.Web/Services/AiApiKeyResolutionService.cs):
- Current shape: AiApiKeyResolutionService(CookBotDbContext db). ResolveAsync returns EffectiveAiCredentials? with .ApiKey populated from profile.AiApiKey (line 36) OR chosen.AiApiKey (line 64 share path).
- Phase 9 wraps both reads: inject IDataProtectionProvider via ctor, create protector once with scope "AiApiKey.v1", and replace the bare ApiKey use with a DecryptIfNeeded(stored) call.

UserProfile.AiApiKey (src/CookBot.Domain/Entities/UserProfile.cs): column stays string?; column type stays nvarchar (no size limit change). The encrypted blob (~180–220 chars for a ~100-char plaintext per 09-RESEARCH Item 2) fits comfortably.

EditProfile.razor (src/CookBot.Web/Components/Pages/EditProfile.razor) line 581: `_profile.AiApiKey = string.IsNullOrWhiteSpace(_aiApiKeyEdit) ? null : _aiApiKeyEdit.Trim();` — Phase 9 changes this to call IDataProtector.Protect on the trimmed value before assigning.

SecretRedactor.cs current regex (lines 13–22): ApiKeyPattern matches sk-ant-...; HeaderPattern matches x-api-key/authorization. Phase 9 ADDS a third pattern matching the CfDJ8 ciphertext prefix to redact long blobs from CryptographicException messages.

Tests follow the temp-SQLite-per-test pattern from RecipeTagBackfillTests.cs (lines 39–54 ctor + Dispose).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 0: PACKAGE LEGITIMACY GATE — confirm Microsoft.AspNetCore.DataProtection.EntityFrameworkCore 10.0.8</name>
  <what-built>
    Per the planner's pre-install protocol: confirm the package about to be installed.
  </what-built>
  <how-to-verify>
    1. Visit https://www.nuget.org/packages/Microsoft.AspNetCore.DataProtection.EntityFrameworkCore/10.0.8
    2. Confirm the publisher is "Microsoft" with the verified-publisher tick
    3. Confirm publish date is 2026-05-12 (per 09-RESEARCH lines 105–106)
    4. Confirm the source repo link points at github.com/dotnet/aspnetcore
    5. Confirm download count is in the tens of millions/year
    6. The 09-RESEARCH Package Legitimacy Audit (lines 108–115) lists this package as Approved (first-party Microsoft, MS Learn cited). This checkpoint is the blocking-human confirmation gate that planner cannot auto-approve.
  </how-to-verify>
  <action>Stop. Verify the package legitimacy per the steps in <how-to-verify> above using a browser. Do NOT proceed to Task 1 until the user confirms approval via the resume signal.</action>
  <verify>
    <automated>echo "Manual checkpoint — proceed only after user types approved"</automated>
  </verify>
  <done>User types "approved" — package legitimacy confirmed; Task 1 may proceed.</done>
  <resume-signal>Type "approved" or "blocked: {reason}"</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Add DataProtection NuGet + DbContext implements IDataProtectionKeyContext + EF migration AddDataProtectionKeysTable + Program.cs registration</name>
  <files>src/CookBot.Infrastructure/CookBot.Infrastructure.csproj, src/CookBot.Infrastructure/Data/CookBotDbContext.cs, src/CookBot.Web/Program.cs</files>
  <read_first>
    - /home/noah/Desktop/projects/freeloves-cookbot/src/CookBot.Infrastructure/CookBot.Infrastructure.csproj (entire file)
    - /home/noah/Desktop/projects/freeloves-cookbot/src/CookBot.Infrastructure/Data/CookBotDbContext.cs (entire file)
    - /home/noah/Desktop/projects/freeloves-cookbot/src/CookBot.Web/Program.cs (entire file)
    - /home/noah/Desktop/projects/freeloves-cookbot/.planning/phases/09-photos-prod-ready-infrastructure/09-RESEARCH.md lines 92–106 (Installation + version verification)
    - /home/noah/Desktop/projects/freeloves-cookbot/.planning/phases/09-photos-prod-ready-infrastructure/09-PATTERNS.md lines 329–365 (AddDataProtectionKeysTable migration generation pattern)
    - /home/noah/Desktop/projects/freeloves-cookbot/src/CookBot.Infrastructure/Migrations/20260516034227_AddPantryMatchIndexes.cs (analog — simple, no data motion)
  </read_first>
  <action>
    1. Add PackageReference to src/CookBot.Infrastructure/CookBot.Infrastructure.csproj:
       <PackageReference Include="Microsoft.AspNetCore.DataProtection.EntityFrameworkCore" Version="10.0.8" />

       Run `dotnet restore` to confirm acquisition.

    2. Modify src/CookBot.Infrastructure/Data/CookBotDbContext.cs:
       - Add `using Microsoft.AspNetCore.DataProtection.EntityFrameworkCore;` at the top.
       - Change the class declaration to: `public class CookBotDbContext : DbContext, IDataProtectionKeyContext`
       - Add new property next to the existing DbSets (e.g. line 27 area): `public DbSet<DataProtectionKey> DataProtectionKeys => Set<DataProtectionKey>();`

    3. Modify src/CookBot.Web/Program.cs:
       - Add `using Microsoft.AspNetCore.DataProtection;` at the top.
       - After `builder.Services.AddInfrastructure(builder.Configuration);` (line 18), insert:
         `builder.Services.AddDataProtection()`
         `    .SetApplicationName("FreelovesCookBot")`
         `    .PersistKeysToDbContext<CookBotDbContext>();`
       - The SetApplicationName MUST be a literal string. Add an inline comment: `// PROD-07: ApplicationName is load-bearing — changing it invalidates every encrypted AI key (09-RESEARCH pitfall #6).`

    4. Generate the EF migration:
       Run from repo root:
       `dotnet ef migrations add AddDataProtectionKeysTable --project src/CookBot.Infrastructure --startup-project src/CookBot.Web`

       The generated migration class lives at `src/CookBot.Infrastructure/Migrations/{timestamp}_AddDataProtectionKeysTable.cs`. Inspect it: it should produce a `CreateTable("DataProtectionKeys", ...)` block with columns Id (int autoincrement), FriendlyName (nvarchar nullable), Xml (nvarchar required). Commit the generated file as-is — no hand-editing.

       Update the files_modified frontmatter at the top of THIS plan to record the actual timestamp once it's generated (or leave the placeholder if the orchestrator accepts wildcards).

    5. Run `dotnet build` (entire solution). Run `dotnet test`. Both must exit 0. The Phase 8 test suite must remain green — this migration does not touch any existing table.

    6. Manual smoke (in summary): `dotnet ef migrations script` should include the AddDataProtectionKeysTable as the most recent migration; existing Phase 8 migrations (AddRecipePhotoUrlAndDescription, AddRecipeTagTable, AddPantryMatchIndexes, DropTagsJsonColumn) are unchanged. The DatabaseSeeder backup pattern (line 27 area) will fire `cookbot.db.pre-AddDataProtectionKeysTable.bak` automatically on first run.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/freeloves-cookbot &amp;&amp; dotnet restore --nologo -v quiet &amp;&amp; dotnet build --nologo -v quiet &amp;&amp; dotnet test tests/CookBot.Tests/CookBot.Tests.csproj --no-build --nologo -v quiet &amp;&amp; grep -q 'Microsoft.AspNetCore.DataProtection.EntityFrameworkCore' src/CookBot.Infrastructure/CookBot.Infrastructure.csproj &amp;&amp; grep -q 'IDataProtectionKeyContext' src/CookBot.Infrastructure/Data/CookBotDbContext.cs &amp;&amp; grep -q 'DbSet<DataProtectionKey>' src/CookBot.Infrastructure/Data/CookBotDbContext.cs &amp;&amp; grep -q 'PersistKeysToDbContext<CookBotDbContext>' src/CookBot.Web/Program.cs &amp;&amp; grep -q 'SetApplicationName("FreelovesCookBot")' src/CookBot.Web/Program.cs &amp;&amp; ls src/CookBot.Infrastructure/Migrations/*_AddDataProtectionKeysTable.cs 1>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - csproj contains PackageReference Microsoft.AspNetCore.DataProtection.EntityFrameworkCore Version 10.0.8
    - CookBotDbContext declares ` : DbContext, IDataProtectionKeyContext` and `DbSet<DataProtectionKey> DataProtectionKeys`
    - Program.cs registers AddDataProtection().SetApplicationName("FreelovesCookBot").PersistKeysToDbContext<CookBotDbContext>()
    - The SetApplicationName argument is a string literal, not a configuration read
    - EF migration file `*_AddDataProtectionKeysTable.cs` exists under src/CookBot.Infrastructure/Migrations/
    - dotnet build entire solution exit 0; dotnet test full suite exit 0; existing test suite must not regress
  </acceptance_criteria>
  <done>PROD-06 closed; PROD-07 registration in place. Data Protection key ring is wired to CookBotDbContext and will be created on first run via the new migration (backup fires automatically). Encrypt/decrypt call sites land in Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AiApiKeyResolutionService read path + EditProfile write path + DatabaseSeeder sentinel-prefix re-encryption + SecretRedactor extension + three integration tests</name>
  <files>src/CookBot.Web/Services/AiApiKeyResolutionService.cs, src/CookBot.Web/Components/Pages/EditProfile.razor, src/CookBot.Infrastructure/Data/DatabaseSeeder.cs, src/CookBot.Web/Program.cs, src/CookBot.Infrastructure/AI/SecretRedactor.cs, tests/CookBot.Tests/AI/SentinelPrefixMigrationTests.cs, tests/CookBot.Tests/AI/KeyShareEncryptionRoundTripTests.cs, tests/CookBot.Tests/AI/SecretRedactorDecryptPathTests.cs</files>
  <read_first>
    - /home/noah/Desktop/projects/freeloves-cookbot/src/CookBot.Web/Services/AiApiKeyResolutionService.cs (entire file — owner-then-share semantics)
    - /home/noah/Desktop/projects/freeloves-cookbot/src/CookBot.Web/Services/AiApiKeyShareService.cs (entire file — share creation flow; recipient never sees plaintext)
    - /home/noah/Desktop/projects/freeloves-cookbot/src/CookBot.Web/Components/Pages/EditProfile.razor lines 390–600 (existing _aiApiKeyEdit field + save path at line 581)
    - /home/noah/Desktop/projects/freeloves-cookbot/src/CookBot.Infrastructure/Data/DatabaseSeeder.cs (entire file — current boot sequence at lines 25–55)
    - /home/noah/Desktop/projects/freeloves-cookbot/src/CookBot.Infrastructure/AI/SecretRedactor.cs (entire file — current regex shape)
    - /home/noah/Desktop/projects/freeloves-cookbot/tests/CookBot.Tests/AI/SecretRedactorTests.cs (entire file — test class shape to extend)
    - /home/noah/Desktop/projects/freeloves-cookbot/tests/CookBot.Tests/Recipes/RecipeTagBackfillTests.cs (entire file — temp-SQLite-per-test fixture pattern + Dispose)
    - /home/noah/Desktop/projects/freeloves-cookbot/.planning/phases/09-photos-prod-ready-infrastructure/09-RESEARCH.md lines 157–233 (Item 2: sentinel-prefix helper + migration shape + read path)
    - /home/noah/Desktop/projects/freeloves-cookbot/.planning/phases/09-photos-prod-ready-infrastructure/09-PATTERNS.md lines 482–614 (test patterns for tests #11, #12, #13)
    - /home/noah/Desktop/projects/freeloves-cookbot/.planning/research/PITFALLS.md C2, C3, C4
  </read_first>
  <behavior>
    Sentinel-prefix detection helper (used by DatabaseSeeder migration pass AND by AiApiKeyResolutionService read path):
    - Inputs are stored AiApiKey strings. Output is a bool.
    - Returns true iff value is non-empty AND value.Length >= 44 AND value.StartsWith("CfDJ8", StringComparison.Ordinal).
    - Pure function, no state.

    DatabaseSeeder migration pass:
    - Runs AFTER null-canonical guard (existing) and BEFORE "Step 4: existing seed logic".
    - Reads all UserProfiles where AiApiKey is non-null and non-empty.
    - For each row where LooksLikeDataProtectionCiphertext returns false, calls protector.Protect(plaintext) and ExecuteUpdateAsync's the new ciphertext into the column.
    - Logs reencrypted-row count via LogInformation; logs only the count, not the values.
    - Idempotent on second boot: no plaintext rows remain → zero updates → zero log lines about re-encryption.

    AiApiKeyResolutionService read path:
    - In the constructor, accept IDataProtectionProvider. Create _protector = dpp.CreateProtector("AiApiKey.v1") once and store.
    - DecryptIfNeeded(string stored) helper: if not LooksLikeDataProtectionCiphertext(stored) return stored unchanged (legacy plaintext — seeder will fix at next boot). Else try _protector.Unprotect(stored); on CryptographicException, log via SecretRedactor.Redact($"Failed to decrypt AI API key: {ex.Message}", stored) and rethrow.
    - Apply DecryptIfNeeded to BOTH the owner-self path (line 36 area) AND the share recipient path (line 64 area). The single shared "AiApiKey.v1" scope is the C2 mitigation.

    EditProfile.razor write path:
    - Inject IDataProtectionProvider via @inject. Create the protector lazily on first save (or in OnInitializedAsync) with the same scope.
    - At save time (around line 581): when _aiApiKeyEdit is non-empty, set _profile.AiApiKey = _protector.Protect(_aiApiKeyEdit.Trim()). When empty/whitespace, set null as before.

    SecretRedactor extension:
    - Add a third regex CipherTextPattern matching `CfDJ8[A-Za-z0-9_\-]{40,}` (CfDJ8 prefix followed by ≥40 base64url chars). Compile with Regex.Compiled.
    - In Redact, apply CipherTextPattern.Replace(result, "[REDACTED-CIPHERTEXT]") as a third pass after verbatim + ApiKeyPattern + HeaderPattern.
    - Does NOT change the existing behavior — only adds the new ciphertext-redaction case.

    SentinelPrefixMigrationTests:
    - FirstBoot_PlaintextRow_GetsReencrypted: seed a UserProfile with AiApiKey = "sk-ant-test-plaintext-key-12345"; call DatabaseSeeder.SeedAsync with a real IDataProtectionProvider; reload the row; assert LooksLikeDataProtectionCiphertext(row.AiApiKey) is true; assert protector.Unprotect(row.AiApiKey) returns the original plaintext.
    - SecondBoot_AlreadyEncryptedRow_IsNoOp: continuing from above, capture the ciphertext; run SeedAsync again; reload; assert the ciphertext is unchanged (no double-encryption).
    - DataProtector_Unprotect_RoundTripsToOriginalPlaintext: independent unit-style test confirming protector.Unprotect(protector.Protect("any-string")) == "any-string".

    KeyShareEncryptionRoundTripTests:
    - RecipientResolution_AfterReencryption_DecryptsToOriginalPlaintext: seed owner User + UserProfile with plaintext AiApiKey; seed recipient User + UserProfile (empty key); seed AiApiKeyShare row from owner→recipient; call DatabaseSeeder.SeedAsync (encrypts owner row); call AiApiKeyResolutionService.ResolveAsync(recipientUserId); assert result.ApiKey equals owner's original plaintext; assert result.SharedFromUserId equals ownerId.
    - Use the same temp-SQLite-per-test fixture pattern.
    - The IDataProtectionProvider in this test must be the same instance used by both SeedAsync and AiApiKeyResolutionService — wire via the DI container the test sets up, OR use the ephemeral DataProtectionProvider available via the DataProtectionUtilities (one canonical pattern is: `var services = new ServiceCollection(); services.AddDataProtection().SetApplicationName("test"); var sp = services.BuildServiceProvider(); var dpp = sp.GetRequiredService<IDataProtectionProvider>();`). For tests, .PersistKeysToDbContext is OPTIONAL — the EphemeralDataProtectionProvider via the SetApplicationName-only registration is sufficient (the test's protected blobs need only survive within the test process).

    SecretRedactorDecryptPathTests:
    - Redact_CryptographicException_DoesNotLeakCiphertext: construct ciphertext = "CfDJ8AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA" (50 chars); construct ex = new CryptographicException($"The payload was invalid: {ciphertext}"); call SecretRedactor.Redact($"Failed to decrypt: {ex.Message}"); assert ciphertext is not a substring of the output; assert "[REDACTED-CIPHERTEXT]" IS present.
    - Redact_CryptographicException_DoesNotLeakPlaintextWhenResolvedKeyProvided: plaintext = "sk-ant-MyRealKey123XYZ"; ex.Message contains plaintext; call Redact with resolvedKey: plaintext; assert plaintext is not a substring; assert "[REDACTED]" appears.
    - Both tests mirror SecretRedactorTests shape (xUnit Facts).
  </behavior>
  <action>
    RED phase: Write all THREE test files first.
    1. tests/CookBot.Tests/AI/SecretRedactorDecryptPathTests.cs — extends SecretRedactorTests pattern; two Facts per the behavior. Run `dotnet test --filter "FullyQualifiedName~SecretRedactorDecryptPathTests"` — MUST fail.
    2. tests/CookBot.Tests/AI/SentinelPrefixMigrationTests.cs — temp-SQLite-per-test pattern from RecipeTagBackfillTests (IDisposable + per-test DB path). Constructor sets up: IDataProtectionProvider via `var services = new ServiceCollection(); services.AddDataProtection().SetApplicationName("test-FreelovesCookBot"); var sp = services.BuildServiceProvider(); var dpp = sp.GetRequiredService<IDataProtectionProvider>();`. The DbContext options point at the temp file. Tests as specified in behavior. The seeder is invoked directly: `await DatabaseSeeder.SeedAsync(_db, fakeBackupService, fakeSerializer, dpp, fakeLogger, contentRootPath);` — fakeBackupService can be a stub that no-ops `BackupBeforeMigrationAsync`; fakeSerializer can be the real one (or a minimal stub if construction overhead is high). Run filter `SentinelPrefixMigrationTests` — MUST fail (red — new args don't exist yet).
    3. tests/CookBot.Tests/AI/KeyShareEncryptionRoundTripTests.cs — same pattern, but also constructs a real AiApiKeyResolutionService instance using the temp DbContext and the same DataProtectionProvider. Test asserts share resolution. Run filter — MUST fail.

    GREEN phase:
    1. Modify src/CookBot.Infrastructure/AI/SecretRedactor.cs:
       - Add `using System.Text.RegularExpressions;` if not already imported (it is).
       - Add a third compiled regex `private static readonly Regex CipherTextPattern = new(@"CfDJ8[A-Za-z0-9_\-]{40,}", RegexOptions.Compiled);` after HeaderPattern.
       - In Redact, after the HeaderPattern.Replace line, add: `result = CipherTextPattern.Replace(result, "[REDACTED-CIPHERTEXT]");`
       - Run the SecretRedactorDecryptPathTests filter — MUST pass.

    2. Modify src/CookBot.Infrastructure/Data/DatabaseSeeder.cs:
       - Update signature: add `IDataProtectionProvider dataProtectionProvider` and `ILogger logger` parameters.
       - After the null-canonical guard block (around line 55, before "Step 4: existing seed logic"), insert:
         a. A static private helper at file-scope inside the class (or as a sibling internal static method): `internal static bool LooksLikeDataProtectionCiphertext(string? value) => !string.IsNullOrEmpty(value) && value.Length >= 44 && value.StartsWith("CfDJ8", StringComparison.Ordinal);`
         b. The migration pass:
            `var protector = dataProtectionProvider.CreateProtector("AiApiKey.v1");`
            Read all UserProfiles where AiApiKey is non-null+non-empty as anonymous {UserId, AiApiKey}. Loop. For each row where `!LooksLikeDataProtectionCiphertext(row.AiApiKey)`, call `var encrypted = protector.Protect(row.AiApiKey!);` then `await context.UserProfiles.Where(p => p.UserId == row.UserId).ExecuteUpdateAsync(s => s.SetProperty(p => p.AiApiKey, encrypted));` Increment a counter.
            After the loop, if `reencrypted > 0` log via `logger.LogInformation("Re-encrypted {Count} legacy plaintext AI API key(s) at startup.", reencrypted);`
       - Add `// 365-day AiUsageLog cleanup will be inserted here by Plan 09-05` as a marker comment BEFORE the re-encryption pass.

    3. Modify src/CookBot.Web/Program.cs:
       - Inside the seeder scope (lines 42–52), resolve the new dependencies: `var dpp = scope.ServiceProvider.GetRequiredService<IDataProtectionProvider>();` and `var seederLogger = scope.ServiceProvider.GetRequiredService<ILoggerFactory>().CreateLogger("DatabaseSeeder");`
       - Pass them to SeedAsync.

    4. Modify src/CookBot.Web/Services/AiApiKeyResolutionService.cs:
       - Add ctor parameter `IDataProtectionProvider dataProtectionProvider`. Create `_protector = dataProtectionProvider.CreateProtector("AiApiKey.v1");`. Inject ILogger<AiApiKeyResolutionService> too for the redaction logging.
       - Add private helper: `private string DecryptIfNeeded(string stored) { if (!DatabaseSeeder.LooksLikeDataProtectionCiphertext(stored)) return stored; try { return _protector.Unprotect(stored); } catch (CryptographicException ex) { _logger.LogError(SecretRedactor.Redact($"Failed to decrypt AI API key: {ex.Message}", stored)); throw; } }`
       - Modify the owner-self return: change `profile.AiApiKey.Trim()` to `DecryptIfNeeded(profile.AiApiKey).Trim()` (the IsNullOrWhiteSpace check on the raw column still happens first).
       - Modify the share-recipient return: change `chosen.AiApiKey!.Trim()` to `DecryptIfNeeded(chosen.AiApiKey!).Trim()`.

    5. Modify src/CookBot.Web/Components/Pages/EditProfile.razor:
       - Add `@inject Microsoft.AspNetCore.DataProtection.IDataProtectionProvider DataProtectionProvider`
       - In @code add: `private Microsoft.AspNetCore.DataProtection.IDataProtector? _protector; protected override void OnInitialized() { _protector = DataProtectionProvider.CreateProtector("AiApiKey.v1"); base.OnInitialized(); }` (note: EditProfile likely has an existing OnInitialized or OnInitializedAsync — extend it, do not replace).
       - At line 581: change `_profile.AiApiKey = string.IsNullOrWhiteSpace(_aiApiKeyEdit) ? null : _aiApiKeyEdit.Trim();` to:
         `_profile.AiApiKey = string.IsNullOrWhiteSpace(_aiApiKeyEdit) ? null : _protector!.Protect(_aiApiKeyEdit.Trim());`

    6. Run all three test filters — MUST pass.
    7. Run `dotnet test tests/CookBot.Tests/CookBot.Tests.csproj` — full suite must be green. Existing SecretRedactorTests must still pass (the new ciphertext pattern adds, does not subtract).

    Manual smoke (in summary): Run ./run.sh; log into EditProfile; enter a fake AI key like "sk-ant-test-NOT-A-REAL-KEY"; save; query the database (`sqlite3 cookbot.db "SELECT AiApiKey FROM UserProfiles WHERE Id = 1"`) → confirm the stored value starts with "CfDJ8" and is significantly longer than the input.
  </action>
  <verify>
    <automated>cd /home/noah/Desktop/projects/freeloves-cookbot &amp;&amp; dotnet build --nologo -v quiet &amp;&amp; dotnet test tests/CookBot.Tests/CookBot.Tests.csproj --no-build --nologo -v quiet &amp;&amp; grep -q 'LooksLikeDataProtectionCiphertext' src/CookBot.Infrastructure/Data/DatabaseSeeder.cs &amp;&amp; grep -q 'CreateProtector\("AiApiKey.v1"\)' src/CookBot.Infrastructure/Data/DatabaseSeeder.cs &amp;&amp; grep -q 'CreateProtector\("AiApiKey.v1"\)' src/CookBot.Web/Services/AiApiKeyResolutionService.cs &amp;&amp; grep -q 'CreateProtector\("AiApiKey.v1"\)' src/CookBot.Web/Components/Pages/EditProfile.razor &amp;&amp; grep -q 'CipherTextPattern\|CfDJ8' src/CookBot.Infrastructure/AI/SecretRedactor.cs &amp;&amp; grep -q '365-day AiUsageLog cleanup will be inserted here' src/CookBot.Infrastructure/Data/DatabaseSeeder.cs &amp;&amp; ls tests/CookBot.Tests/AI/SentinelPrefixMigrationTests.cs tests/CookBot.Tests/AI/KeyShareEncryptionRoundTripTests.cs tests/CookBot.Tests/AI/SecretRedactorDecryptPathTests.cs 1>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - SecretRedactor has a third regex matching CfDJ8 ciphertext prefix; the new SecretRedactorDecryptPathTests pass
    - DatabaseSeeder.SeedAsync has signature with IDataProtectionProvider + ILogger; declares LooksLikeDataProtectionCiphertext helper; runs the re-encryption loop AFTER the null-canonical guard
    - The "365-day AiUsageLog cleanup will be inserted here by Plan 09-05" marker comment exists in DatabaseSeeder.cs BEFORE the re-encryption pass — Plan 09-05 reads this marker to know exactly where to insert its work
    - AiApiKeyResolutionService injects IDataProtectionProvider + ILogger; uses the shared "AiApiKey.v1" scope; applies DecryptIfNeeded to BOTH owner-self and share-recipient code paths
    - EditProfile.razor injects IDataProtectionProvider; protects _aiApiKeyEdit before assigning to _profile.AiApiKey
    - All three test classes exist and pass:
      - SentinelPrefixMigrationTests: 3 facts (FirstBoot, SecondBoot, Unprotect round-trip)
      - KeyShareEncryptionRoundTripTests: 1 fact (RecipientResolution_AfterReencryption_DecryptsToOriginalPlaintext)
      - SecretRedactorDecryptPathTests: 2 facts (no-leak ciphertext, no-leak plaintext)
    - Full test suite green; Phase 8 tests unchanged in count and pass-state
    - Application name in Program.cs IS the literal "FreelovesCookBot" string (no IConfiguration read)
    - The owner-self and share-recipient code paths in AiApiKeyResolutionService BOTH route through DecryptIfNeeded — verifiable by counting `DecryptIfNeeded(` occurrences in the file: must be ≥ 2 call sites
  </acceptance_criteria>
  <done>PROD-08 closed (read-path-only encryption per 09-RESEARCH correction); PROD-09 closed (sentinel-prefix idempotent migration in DatabaseSeeder); PROD-10 closed (SecretRedactor covers decrypt path with CfDJ8 pattern); PROD-11 closed (share roundtrip integration test green). End-to-end: a plaintext key stored before this commit will be silently re-encrypted on next app start; key sharing continues to work; CryptographicException messages do not leak secrets.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UserProfile.AiApiKey column (disk) ↔ encrypted blob | Plaintext never lands on disk after Plan 09-04 ships |
| IDataProtector.Protect / Unprotect ↔ DataProtectionKeys table | Key ring lives in cookbot.db; loss of cookbot.db = loss of all encrypted keys (intended trust model) |
| CryptographicException.Message ↔ logs/UI | Must never contain plaintext OR ciphertext after redaction |
| Owner's encrypted row ↔ recipient's resolved key | Recipient never sees plaintext at the wire; resolution happens server-side via shared protector scope |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04-01 | Information Disclosure | Stolen cookbot.db.bak hands attacker working Anthropic keys | mitigate | UserProfile.AiApiKey is encrypted-at-rest via IDataProtector. Without the key ring (also in cookbot.db, but a backup-stealer who lacks DataProtectionKeys.Xml column cannot decrypt). |
| T-09-04-02 | Tampering | Different ApplicationName between deploys silently invalidates all keys | mitigate | SetApplicationName("FreelovesCookBot") is a literal in Program.cs with inline pitfall warning comment. Not configurable. |
| T-09-04-03 | Denial of Service | Migration pass throws on a legacy plaintext row, app fails to start | mitigate | Sentinel-prefix detection short-circuits Unprotect for legacy plaintext. EF value converter rejected for this exact reason (09-RESEARCH Item 2 correction). |
| T-09-04-04 | Denial of Service | Per-user protector scope breaks share resolution | mitigate | Single shared scope "AiApiKey.v1" used by encrypt site AND decrypt site. Owner.Protect(scope=v1) ↔ Recipient.Unprotect(scope=v1) succeeds. C2 mitigation locked. |
| T-09-04-05 | Information Disclosure | CryptographicException leaks plaintext or ciphertext in logs | mitigate | SecretRedactor.Redact extended with CfDJ8 ciphertext pattern + verbatim resolvedKey replacement. All decrypt catch sites pipe through Redact before logging. |
| T-09-04-06 | Information Disclosure | Recipient sees owner's plaintext key in UI | accept | AiApiKeyResolutionService returns EffectiveAiCredentials to the server-side AnthropicAiService; the key never crosses the wire to the recipient's browser. This was true pre-Phase-9 and remains true. The share semantic is "recipient can USE the key without SEEING it." |
| T-09-04-07 | Tampering | Plan 09-05's 365-day cleanup inserted in the wrong place breaks boot order | mitigate | Plan 09-04 leaves an explicit marker comment "365-day AiUsageLog cleanup will be inserted here by Plan 09-05" BEFORE the re-encryption pass. Plan 09-05 inserts its work above the marker. |
| T-09-04-SC | Tampering (supply chain) | Microsoft.AspNetCore.DataProtection.EntityFrameworkCore 10.0.8 NuGet install | mitigate | Task 0 BLOCKING-HUMAN gate confirms publisher = Microsoft, source repo = dotnet/aspnetcore, publish date = 2026-05-12, source = MS Learn citation (09-RESEARCH Package Legitimacy Audit lines 108–115). Legitimacy approved. |
</threat_model>

<verification>
- Three new integration test files green; existing SecretRedactorTests still green.
- `dotnet ef migrations list` shows AddDataProtectionKeysTable as the latest migration.
- Manual smoke (in summary): set a key via EditProfile → DB shows CfDJ8...; restart app → AI still works; create a share → recipient can use AI; intentionally corrupt the ciphertext for a row → app logs a [REDACTED-CIPHERTEXT] message and the user sees a UI error (no leak).
- Existing v1.2 Phase 7 PROF-02 AI sharing tests remain green; the encryption is transparent to that surface.
</verification>

<success_criteria>
1. NuGet added; Program.cs registers AddDataProtection with literal ApplicationName + PersistKeysToDbContext
2. CookBotDbContext implements IDataProtectionKeyContext
3. EF migration AddDataProtectionKeysTable exists and applies cleanly
4. DatabaseSeeder.SeedAsync re-encrypts legacy plaintext idempotently; second boot is a no-op
5. AiApiKeyResolutionService owner-self AND share-recipient paths both use the shared protector scope
6. EditProfile.razor write path protects the key before persist
7. SecretRedactor covers CfDJ8 ciphertext pattern
8. Three integration test files (SentinelPrefixMigrationTests, KeyShareEncryptionRoundTripTests, SecretRedactorDecryptPathTests) all green
9. Marker comment for Plan 09-05's 365-day cleanup insertion point present in DatabaseSeeder
10. Full test suite + dotnet build exit 0; no regressions
</success_criteria>

<output>
Create .planning/phases/09-photos-prod-ready-infrastructure/09-04-SUMMARY.md when done. Include: migration timestamp; .csproj diff; SentinelPrefixMigrationTests scenario summary; manual smoke result (key encrypts in DB after EditProfile save); any deviation from 09-RESEARCH Item 2 pseudo-code.
</output>
