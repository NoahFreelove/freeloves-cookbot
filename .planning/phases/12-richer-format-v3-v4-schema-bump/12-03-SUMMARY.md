---
phase: 12-richer-format-v3-v4-schema-bump
plan: "03"
subsystem: application-recipes,tests
tags: [ai-prompt, schema-assertion, snapshot, format-06, tdd]
dependency_graph:
  requires: [v4-domain-pocos]
  provides: [v4-format-prompt, prompt-snapshot-v4, schema-assertions-v4]
  affects: [RecipeSchemaDocumentationProvider, SchemaAssertionTests, PromptSnapshotTests.BuildSystemPrompt.verified.txt]
tech_stack:
  added: []
  patterns: [snapshot-regen-atomic-with-prose, tdd-schema-contract-assertion]
key_files:
  created: []
  modified:
    - src/CookBot.Application/Recipes/RecipeSchemaDocumentationProvider.cs
    - tests/CookBot.Tests/Snapshots/PromptSnapshotTests.BuildSystemPrompt.verified.txt
    - tests/CookBot.Tests/Recipes/SchemaAssertionTests.cs
decisions:
  - "FormatPrompt updated to version 4 with equipment, substitutions, donenessCue, and provenance per D-12-11"
  - "D-12-09 no-fabrication directive embedded in provenance field guidance (NEVER fabricate URL or author)"
  - "Snapshot regenerated atomically with prose change per P3 gate (manual copy from .received.txt required — VERIFY_AUTO=true does not auto-accept in Verify.Xunit 31.12.5)"
  - "RecipeJsonSchemaProvider confirmed unchanged — auto-reflects v4 POCOs per FLAG 4"
  - "Rule 1 fix: replaced 'internal temp' with 'internal temperature' in donenessCue example to pass PromptDenylistTests \\btemp\\b guard; snapshot regenerated again atomically"
metrics:
  duration_minutes: 4
  completed_date: "2026-06-06"
  tasks_completed: 2
  tasks_total: 2
  files_created: 0
  files_modified: 3
---

# Phase 12 Plan 03: AI Schema + Snapshot v4 Alignment — Summary

**One-liner:** Updated FormatPrompt prose to v4 (equipment, substitutions, donenessCue, provenance with D-12-09 no-fabrication directive), regenerated byte-stable snapshot atomically, and added four schema-presence assertions proving RecipeJsonSchemaProvider auto-reflects the v4 POCOs with additionalProperties:false on the new nested records (SC3 / FORMAT-06 / P3 / P4).

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Update FormatPrompt prose to v4 with default-fill + no-fabrication guidance, regenerate snapshot | 6cce268 | RecipeSchemaDocumentationProvider.cs, PromptSnapshotTests.BuildSystemPrompt.verified.txt |
| 2 | Assert reflected JSON schema includes four new v4 fields (TDD) + Rule 1 fix | 4461bb5 | SchemaAssertionTests.cs, RecipeSchemaDocumentationProvider.cs (fix), verified.txt (reregen) |

## Verification

- `dotnet test --filter "Category!=RequiresApiKey"`: 372 passed, 0 failed
- `dotnet test --filter "FullyQualifiedName~BuildSystemPrompt"`: 6 passed (snapshot matches — P3 satisfied)
- `dotnet test --filter "Category!=RequiresApiKey&FullyQualifiedName~SchemaAssertion"`: 9 passed (4 new v4 assertions + 5 existing)
- SC3 proven: FormatPrompt describes all four v4 fields; snapshot test passing; reflected schema includes equipment, provenance, substitutions, donenessCue
- RecipeJsonSchemaProvider.cs confirmed unchanged (FLAG 4)
- IngredientSubstitution and RecipeProvenance subschemas carry additionalProperties:false (P4)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] PromptDenylistTests \btemp\b word-boundary violation**
- **Found during:** Task 2 full test suite run
- **Issue:** The donenessCue field guidance example contained `"165°F internal temp"` — the standalone word `temp` matches the `\btemp\b` regex in `PromptDenylistTests.PromptSourceFiles_ContainNoOptOutPhrases`, which denylists opt-out / alias tokens.
- **Fix:** Replaced `"internal temp"` with `"internal temperature"` — `\btemp\b` does not match inside `temperature` because the character after `temp` is `e` (a word character, so no word boundary). Regenerated snapshot atomically with this fix per P3 gate.
- **Files modified:** `RecipeSchemaDocumentationProvider.cs`, `PromptSnapshotTests.BuildSystemPrompt.verified.txt`
- **Commit:** 4461bb5

## TDD Gate Compliance

Task 2 has `tdd="true"`. The test assertions were written first and committed before the implementation step. However, since Plan 01 already shipped the v4 Domain POCOs, the schema auto-reflects them — the tests pass on first run (GREEN immediately). This is expected behavior for a "schema contract verification" TDD task where the production code is a prerequisite (Plan 01). The TDD pattern proves the contract holds, not that it was missing.

- RED gate: tests written and run → PASSED immediately (POCOs from Plan 01 already present)
- GREEN gate: no additional implementation needed — RecipeJsonSchemaProvider auto-reflects v4 fields per FLAG 4
- REFACTOR gate: N/A — no refactoring needed

## Known Stubs

None — the prompt prose fully describes all four v4 fields with guidance; snapshot is byte-stable; schema assertions are complete.

## Threat Flags

- T-12-05 mitigated: D-12-09 directive embedded in `provenance` field guidance — "NEVER fabricate a URL, author, or source name. If you do not know the real provenance, omit it entirely (leave `provenance` null)." Directive locked by the snapshot test (byte-stable).
- T-12-06 mitigated: Snapshot regenerated atomically with the prose change (P3); SchemaAssertionTests proves the reflected schema includes all four fields with additionalProperties:false (P4).

## Self-Check: PASSED

Files confirmed present:
- src/CookBot.Application/Recipes/RecipeSchemaDocumentationProvider.cs — FOUND
- tests/CookBot.Tests/Snapshots/PromptSnapshotTests.BuildSystemPrompt.verified.txt — FOUND
- tests/CookBot.Tests/Recipes/SchemaAssertionTests.cs — FOUND

Commits confirmed: 6cce268, 4461bb5 — both present in git log.
