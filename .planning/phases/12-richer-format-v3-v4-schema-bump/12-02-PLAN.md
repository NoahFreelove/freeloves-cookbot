---
phase: 12-richer-format-v3-v4-schema-bump
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - src/CookBot.Domain/Interfaces/IRecipeFormatParser.cs
  - src/CookBot.Application/Services/RecipeFormatParser.cs
  - tests/CookBot.Tests/Recipes/RecipeRoundTripTests.cs
autonomous: true
requirements: [FORMAT-07]

must_haves:
  truths:
    - "ParsedRecipe/ParsedStep/ParsedIngredient carry the four new field groups so the editor save path does not silently drop them (P5/SC2)"
    - "RecipeFormatParser.ProjectToParsedRecipe maps equipment, provenance, per-ingredient substitutions, and per-step doneness cue from RecipeDocument into ParsedRecipe"
    - "RecipeFormatParser.Serialize round-trips all four new field groups through the YAML frontmatter path"
    - "A RecipeDocument with all four field groups populated survives Serialize→Deserialize with field-level equality (SC2 data layer)"
  artifacts:
    - path: "src/CookBot.Domain/Interfaces/IRecipeFormatParser.cs"
      provides: "Editor DTOs extended with Equipment/Provenance/Substitutions/DonenessCue"
      contains: "RecipeProvenance? Provenance"
    - path: "src/CookBot.Application/Services/RecipeFormatParser.cs"
      provides: "Bridge mapping the four new groups in ProjectToParsedRecipe + Serialize + frontmatter classes"
      contains: "SubstitutionFrontmatter"
    - path: "tests/CookBot.Tests/Recipes/RecipeRoundTripTests.cs"
      provides: "Round-trip coverage for the four new field groups (null, present, edge)"
      contains: "RoundTrip"
  key_links:
    - from: "src/CookBot.Application/Services/RecipeFormatParser.cs"
      to: "src/CookBot.Domain/Interfaces/IRecipeFormatParser.cs"
      via: "ProjectToParsedRecipe populates ParsedRecipe.Equipment/Provenance and ParsedIngredient.Substitutions and ParsedStep.DonenessCue"
      pattern: "Equipment = doc.Equipment"
    - from: "src/CookBot.Application/Services/RecipeFormatParser.cs"
      to: "src/CookBot.Domain/Recipes/RecipeProvenance.cs"
      via: "RecipeFrontmatter.Provenance reuses the Domain record directly"
      pattern: "RecipeProvenance\\? Provenance"
---

<objective>
Extend the second shape system (editor DTOs in `IRecipeFormatParser.cs`) and the `RecipeFormatParser` bridge so all four new v4 field groups survive the editor save/reload round-trip and the YAML frontmatter serialization path. This is the FORMAT-07 / SC2 data-layer guarantee.

Purpose: The codebase has two parallel shapes — `RecipeDocument` (canonical) and `ParsedRecipe` (editor DTO). Plan 01 updated the canonical POCO; if `ParsedRecipe` and `RecipeFormatParser.ProjectToParsedRecipe`/`Serialize` are not extended too, the new fields silently drop at save (PITFALL P5). This plan closes that gap so SC2 round-trip holds.
Output: extended `ParsedRecipe`/`ParsedStep`/`ParsedIngredient`, extended `ProjectToParsedRecipe` + `Serialize` + frontmatter classes (incl. new `SubstitutionFrontmatter`), and a round-trip test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-richer-format-v3-v4-schema-bump/12-CONTEXT.md
@.planning/phases/12-richer-format-v3-v4-schema-bump/12-RESEARCH.md
@.planning/phases/12-richer-format-v3-v4-schema-bump/12-PATTERNS.md

<interfaces>
<!-- Plan 01 produced these Domain types — consume them directly, no re-derivation. -->
From src/CookBot.Domain/Recipes/ (created/modified in Plan 01):
  - IngredientSubstitution: required string Note; string? Name; double? Amount; string? Unit; Extras
  - RecipeProvenance: string? SourceUrl; string? AuthorName; string? SourceName; Extras
  - RecipeDocument.Equipment (IReadOnlyList<string>), RecipeDocument.Provenance (RecipeProvenance?)
  - IngredientEntry.Substitutions (IReadOnlyList<IngredientSubstitution>)
  - ContentStep.DonenessCue (string?)

Files to extend (read these directly — they ARE the analogs, self-modification):
  - src/CookBot.Domain/Interfaces/IRecipeFormatParser.cs (ParsedRecipe lines ~5-16; ParsedStep ~18-24; ParsedIngredient ~33-40)
  - src/CookBot.Application/Services/RecipeFormatParser.cs:
      Serialize(ParsedRecipe) frontmatter build starts at line 133 (RecipeFrontmatter init at 135; Ingredients .Select at 144; Steps .Select at 152)
      ProjectToParsedRecipe at line 259
      frontmatter inner classes (RecipeFrontmatter / IngredientFrontmatter / StepFrontmatter / TimerFrontmatter) at lines 302-343
  - using CookBot.Domain.Recipes; is already present at line 1 of IRecipeFormatParser.cs
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the editor DTOs (ParsedRecipe/ParsedStep/ParsedIngredient)</name>
  <files>src/CookBot.Domain/Interfaces/IRecipeFormatParser.cs</files>
  <read_first>
    - src/CookBot.Domain/Interfaces/IRecipeFormatParser.cs (ParsedRecipe, ParsedStep, ParsedIngredient definitions)
    - .planning/phases/12-richer-format-v3-v4-schema-bump/12-PATTERNS.md (IRecipeFormatParser.cs section — FLAG 1 two parallel shape systems; exact insertions)
    - 12-RESEARCH.md open question #2 (reuse RecipeProvenance directly, no ParsedProvenance parallel)
  </read_first>
  <behavior>
    - ParsedRecipe exposes a mutable Equipment list (default empty) and a nullable Provenance reusing the Domain RecipeProvenance record.
    - ParsedStep exposes a nullable DonenessCue string.
    - ParsedIngredient exposes a mutable Substitutions list (default empty) of Domain IngredientSubstitution.
  </behavior>
  <action>
    In `IRecipeFormatParser.cs`, following the existing mutable-class + `List<>`-init convention used by the editor DTOs (FLAG 1):
    - On `ParsedRecipe`: add `public List<string> Equipment { get; set; } = new();` and `public RecipeProvenance? Provenance { get; set; }`. Reuse the Domain `RecipeProvenance` record directly (no `ParsedProvenance` — RESEARCH open question #2; Domain POCO has no framework deps).
    - On `ParsedStep`: add `public string? DonenessCue { get; set; }` (follows the `Temperature` nullable pattern).
    - On `ParsedIngredient`: add `public List<IngredientSubstitution> Substitutions { get; set; } = new();`.
    `using CookBot.Domain.Recipes;` is already present at line 1 — no new import needed.
  </action>
  <verify>
    <automated>dotnet build src/CookBot.Domain/CookBot.Domain.csproj 2>&1 | grep -qE "Build succeeded|0 Error" && echo BUILD_OK</automated>
  </verify>
  <acceptance_criteria>
    - `IRecipeFormatParser.cs` `ParsedRecipe` contains `List<string> Equipment` and `RecipeProvenance? Provenance`.
    - `ParsedStep` contains `string? DonenessCue`.
    - `ParsedIngredient` contains `List<IngredientSubstitution> Substitutions`.
    - `grep -c 'class ParsedProvenance' src/CookBot.Domain/Interfaces/IRecipeFormatParser.cs` returns 0 (Domain record reused, no parallel DTO).
    - `dotnet build src/CookBot.Domain` succeeds with 0 errors.
  </acceptance_criteria>
  <done>All three editor DTOs carry the new field groups using the existing mutable-class convention; the Domain RecipeProvenance/IngredientSubstitution records are reused directly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the four groups through RecipeFormatParser (bridge + Serialize + frontmatter) and prove round-trip</name>
  <files>src/CookBot.Application/Services/RecipeFormatParser.cs, tests/CookBot.Tests/Recipes/RecipeRoundTripTests.cs</files>
  <read_first>
    - src/CookBot.Application/Services/RecipeFormatParser.cs (Serialize at 133-172; ProjectToParsedRecipe at 259-298; frontmatter classes 302-343)
    - .planning/phases/12-richer-format-v3-v4-schema-bump/12-PATTERNS.md (RecipeFormatParser.cs section — exact additions to ProjectToParsedRecipe, frontmatter classes, SubstitutionFrontmatter, Serialize)
    - src/CookBot.Application/Recipes/JsonRecipeSerializer.cs (NO CHANGE — confirm _compact WhenWritingNull semantics for the round-trip assertion)
    - 12-RESEARCH.md "Round-Trip Path (SC2 verification target)" section
  </read_first>
  <behavior>
    - ProjectToParsedRecipe(doc) carries doc.Equipment, doc.Provenance, each ingredient's Substitutions, and each content step's DonenessCue into the resulting ParsedRecipe.
    - Serialize(ParsedRecipe) emits the four new groups into the YAML frontmatter (equipment list, provenance, per-ingredient substitutions, per-step donenessCue).
    - A RecipeDocument with all four groups populated serialized via JsonRecipeSerializer then deserialized yields field-level equality on equipment, provenance (all three fields), substitutions (note/name/amount/unit), and per-step donenessCue.
    - A RecipeDocument with none of the four groups round-trips with equipment empty, provenance null, substitutions empty, donenessCue null — no exception.
    - Edge: a substitution with only Note (no structured fields) and a provenance with only SourceName round-trip intact.
  </behavior>
  <action>
    In `RecipeFormatParser.cs` `ProjectToParsedRecipe` (line 259): add `Equipment = doc.Equipment.ToList(),` and `Provenance = doc.Provenance,` to the `ParsedRecipe` initializer; add `Substitutions = i.Substitutions.ToList(),` inside the `ParsedIngredient` initializer; add `DonenessCue = c.DonenessCue,` inside the `ContentStep => new ParsedStep` branch.

    Extend the frontmatter classes (lines 302-343): on `RecipeFrontmatter` add `public List<string>? Equipment { get; set; }` and `public RecipeProvenance? Provenance { get; set; }`; on `IngredientFrontmatter` add `public List<SubstitutionFrontmatter>? Substitutions { get; set; }`; on `StepFrontmatter` add `public string? DonenessCue { get; set; }`. Add a new private inner class `SubstitutionFrontmatter` modeled on `TimerFrontmatter` with `string? Note`, `string? Name`, `double? Amount`, `string? Unit`.

    Extend the `Serialize(ParsedRecipe recipe)` frontmatter build (starting line 135) to map the new fields onto `RecipeFrontmatter`: `Equipment = recipe.Equipment.Any() ? recipe.Equipment : null` (match the existing Tags null-when-empty idiom at line 143); `Provenance = recipe.Provenance`; in the Ingredients `.Select` (line 144) add `Substitutions = i.Substitutions.Any() ? i.Substitutions.Select(s => new SubstitutionFrontmatter { Note = s.Note, Name = s.Name, Amount = s.Amount, Unit = s.Unit }).ToList() : null`; in the content-step `StepFrontmatter` branch (line 154) add `DonenessCue = s.DonenessCue`. Also confirm the TryParse/deserialize side maps `RecipeFrontmatter.Equipment/Provenance`, `IngredientFrontmatter.Substitutions`, and `StepFrontmatter.DonenessCue` back into the `RecipeDocument`/`ParsedRecipe` it builds (mirror the existing field mappings).

    `JsonRecipeSerializer.cs` needs NO change — confirm only.

    Create `tests/CookBot.Tests/Recipes/RecipeRoundTripTests.cs`: build a `RecipeDocument` with all four groups populated (equipment list; provenance with all three fields; one ingredient with two substitutions — one note-only, one structured; one content step with donenessCue), serialize via `JsonRecipeSerializer`, deserialize, and assert field-level equality on all four groups. Add a null/empty case (no groups → empty/null after round-trip, no throw) and the two edge cases from the behavior block. Do NOT delete or modify any existing v3 test.
  </action>
  <verify>
    <automated>dotnet test tests/CookBot.Tests/CookBot.Tests.csproj --filter "Category!=RequiresApiKey&FullyQualifiedName~RoundTrip" 2>&1 | grep -qE "Passed!|Passed:" && echo TESTS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `RecipeFormatParser.cs` `ProjectToParsedRecipe` contains `Equipment = doc.Equipment`, `Provenance = doc.Provenance`, `Substitutions = i.Substitutions`, and `DonenessCue = c.DonenessCue`.
    - `RecipeFormatParser.cs` contains a `SubstitutionFrontmatter` inner class and `RecipeFrontmatter` carries `Equipment` + `Provenance`; `IngredientFrontmatter` carries `Substitutions`; `StepFrontmatter` carries `DonenessCue`.
    - `JsonRecipeSerializer.cs` is unmodified (`git diff --stat` shows it is not in this plan's changeset).
    - `RecipeRoundTripTests.cs` exists with an all-present round-trip test asserting equality on all four groups, plus a null/empty case and at least two edge cases.
    - `dotnet test --filter "...RoundTrip"` exits 0; no pre-existing v3 test removed (`git diff` shows no deletions in existing test files).
  </acceptance_criteria>
  <done>The four new groups flow ProjectToParsedRecipe → Serialize → Deserialize without loss; the round-trip test suite proves field-level equality for null/present/edge fixtures (FORMAT-07, SC2 data layer).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor DTO ↔ canonical JSON | ParsedRecipe (mutable editor state) is projected to/from the canonical RecipeDocument JSON |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Tampering | Field drop / silent data loss when the two parallel shapes diverge (P5) | mitigate | ProjectToParsedRecipe + Serialize extended for all four groups; round-trip tests assert field-level equality (SC2). |

Minimal surface: this plan is pure data-mapping and serialization (no new network/auth/input boundary). `[MaxLength]` caps from Plan 01 already bound string sizes; no new DoS vector introduced here.
</threat_model>

<verification>
- `dotnet build` of Domain + Application succeeds.
- `dotnet test tests/CookBot.Tests/ --filter "Category!=RequiresApiKey"` passes; no existing v3 test deleted.
- `JsonRecipeSerializer.cs` confirmed unchanged.
</verification>

<success_criteria>
- FORMAT-07: parser + serializer round-trip all four new groups; tests cover null/present/edge.
- SC2 (data layer): a fully-populated RecipeDocument survives Serialize→Deserialize with field-level equality (UI completion of SC2 lands in Plan 04).
</success_criteria>

<output>
Create `.planning/phases/12-richer-format-v3-v4-schema-bump/12-02-SUMMARY.md` when done.
</output>
