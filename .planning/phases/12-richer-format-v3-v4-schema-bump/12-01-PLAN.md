---
phase: 12-richer-format-v3-v4-schema-bump
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/CookBot.Domain/Recipes/IngredientSubstitution.cs
  - src/CookBot.Domain/Recipes/RecipeProvenance.cs
  - src/CookBot.Domain/Recipes/RecipeDocument.cs
  - src/CookBot.Domain/Recipes/IngredientEntry.cs
  - src/CookBot.Domain/Recipes/StepNode.cs
  - src/CookBot.Application/Recipes/Migration_V3_To_V4.cs
  - src/CookBot.Application/Recipes/RecipeUpcasterChain.cs
  - src/CookBot.Application/DependencyInjection.cs
  - src/CookBot.Application/Recipes/RecipeValidator.cs
  - tests/CookBot.Tests/Recipes/Migration_V3_To_V4_Tests.cs
  - tests/CookBot.Tests/Recipes/Migration_V3_To_V4_ChainTests.cs
  - tests/CookBot.Tests/Recipes/RecipeUpcasterTests.cs
  - tests/CookBot.Tests/Fixtures/Recipes/upcaster/v3-to-v4-no-fields.json
  - tests/CookBot.Tests/Fixtures/Recipes/upcaster/v3-to-v4-all-present.json
  - tests/CookBot.Tests/Fixtures/Recipes/upcaster/v3-to-v4-substitutions-only.json
  - tests/CookBot.Tests/Fixtures/Recipes/upcaster/v3-to-v4-equipment-only.json
  - tests/CookBot.Tests/Fixtures/Recipes/upcaster/v3-to-v4-doneness-only.json
  - tests/CookBot.Tests/Fixtures/Recipes/upcaster/v3-to-v4-provenance-only.json
autonomous: true
requirements: [FORMAT-01, FORMAT-02, FORMAT-03, FORMAT-04, FORMAT-05, FORMAT-06]

must_haves:
  truths:
    - "A v3 recipe document upcasts to v4 with all four new field groups null/empty — no throw, no data loss, no bundle-throw across the four guards (SC1)"
    - "RecipeUpcasterChain.CurrentVersion equals 4 and Migration_V3_To_V4 is registered in DI (SC4)"
    - "The gap-detection test covers v3→v4 explicitly (SC4)"
    - "A RecipeDocument can carry equipment, provenance, per-ingredient substitutions, and per-step doneness cues as in-memory shape (FORMAT-01..04)"
    - "RecipeValidator emits non-blocking warnings for invalid provenance SourceUrl scheme and empty substitutions, and never throws (FORMAT-06)"
  artifacts:
    - path: "src/CookBot.Domain/Recipes/IngredientSubstitution.cs"
      provides: "IngredientSubstitution POCO record (required Note, optional Name/Amount/Unit, Extras)"
      contains: "public sealed record IngredientSubstitution"
    - path: "src/CookBot.Domain/Recipes/RecipeProvenance.cs"
      provides: "RecipeProvenance POCO record (optional SourceUrl/AuthorName/SourceName, Extras)"
      contains: "public sealed record RecipeProvenance"
    - path: "src/CookBot.Application/Recipes/Migration_V3_To_V4.cs"
      provides: "v3→v4 upcaster with four independent null-guards"
      contains: "obj[\"version\"] = 4"
    - path: "src/CookBot.Application/Recipes/RecipeUpcasterChain.cs"
      provides: "CurrentVersion bumped to 4"
      contains: "CurrentVersion = 4"
    - path: "src/CookBot.Application/DependencyInjection.cs"
      provides: "Migration_V3_To_V4 DI registration before RecipeUpcasterChain"
      contains: "Migration_V3_To_V4"
  key_links:
    - from: "src/CookBot.Application/DependencyInjection.cs"
      to: "src/CookBot.Application/Recipes/Migration_V3_To_V4.cs"
      via: "AddSingleton<IRecipeUpcaster, Migration_V3_To_V4>() before AddSingleton<RecipeUpcasterChain>()"
      pattern: "AddSingleton<IRecipeUpcaster, Migration_V3_To_V4>"
    - from: "src/CookBot.Domain/Recipes/RecipeDocument.cs"
      to: "src/CookBot.Domain/Recipes/RecipeProvenance.cs"
      via: "RecipeProvenance? Provenance property"
      pattern: "RecipeProvenance\\? Provenance"
    - from: "src/CookBot.Domain/Recipes/IngredientEntry.cs"
      to: "src/CookBot.Domain/Recipes/IngredientSubstitution.cs"
      via: "IReadOnlyList<IngredientSubstitution> Substitutions property"
      pattern: "IReadOnlyList<IngredientSubstitution> Substitutions"
---

<objective>
Add the four v4 field groups to the canonical Domain POCOs and ship the v3→v4 upcaster atomically with its DI registration, CurrentVersion bump, validator rules, and the full upcaster/chain/fixture test matrix. This is the schema root every other Phase 12 plan depends on.

Purpose: Make the canonical `RecipeDocument` stably v4 — equipment list, provenance, per-ingredient substitutions, per-step doneness cue — using the proven v2→v3 bump pattern with zero new packages and no EF migration. Closes SC1 (upcast, no throw) and SC4 (CurrentVersion=4, registered, gap-test).
Output: 2 new Domain records, 1 new upcaster, modified RecipeDocument/IngredientEntry/StepNode/RecipeUpcasterChain/DependencyInjection/RecipeValidator, 2 new test files, 6 new fixtures, extended RecipeUpcasterTests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-richer-format-v3-v4-schema-bump/12-CONTEXT.md
@.planning/phases/12-richer-format-v3-v4-schema-bump/12-RESEARCH.md
@.planning/phases/12-richer-format-v3-v4-schema-bump/12-PATTERNS.md

<interfaces>
<!-- Copy-targets — read these files directly; do not re-derive shape from scratch. -->
Domain POCO conventions (camelCase JsonPropertyName, [JsonExtensionData] Extras, [MaxLength], = [] defaults):
  - src/CookBot.Domain/Recipes/IngredientEntry.cs (full file — analog for IngredientSubstitution)
  - src/CookBot.Domain/Recipes/StepTemperature.cs (full file — analog for RecipeProvenance structure)
  - src/CookBot.Domain/Recipes/RecipeDocument.cs (PhotoUrl/Description nullable + MaxLength at lines 29-35; Tags/Ingredients/Steps empty-list at 37-44)
  - src/CookBot.Domain/Recipes/StepNode.cs (ContentStep.Temperature at line 24-25 — analog for DonenessCue)

Upcaster copy-targets:
  - src/CookBot.Application/Recipes/Migration_V2_To_V3.cs (full file — verbatim template for Migration_V3_To_V4)
  - src/CookBot.Application/Recipes/RecipeUpcasterChain.cs (CurrentVersion const at line 14)
  - src/CookBot.Application/DependencyInjection.cs (upcaster registration block lines 29-31)

Validator copy-target:
  - src/CookBot.Application/Recipes/RecipeValidator.cs (DetectOrphanIngredients pattern ~lines 109-136; dispatch site ~lines 97-101; class has NO constructor — all static helpers)

Test copy-targets:
  - tests/CookBot.Tests/Recipes/Migration_V2_To_V3_Tests.cs (template for Migration_V3_To_V4_Tests)
  - tests/CookBot.Tests/Recipes/Migration_V2_To_V3_ChainTests.cs (template for Migration_V3_To_V4_ChainTests)
  - tests/CookBot.Tests/Recipes/RecipeUpcasterTests.cs (gap-detection test ~lines 100-107, MakeChain ~line 16)
  - tests/CookBot.Tests/Fixtures/Recipes/upcaster/v2-to-v3-no-fields.json and v2-to-v3-all-present.json (fixture format)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the four v4 field groups to the Domain POCOs</name>
  <files>src/CookBot.Domain/Recipes/IngredientSubstitution.cs, src/CookBot.Domain/Recipes/RecipeProvenance.cs, src/CookBot.Domain/Recipes/RecipeDocument.cs, src/CookBot.Domain/Recipes/IngredientEntry.cs, src/CookBot.Domain/Recipes/StepNode.cs</files>
  <read_first>
    - src/CookBot.Domain/Recipes/IngredientEntry.cs (analog for IngredientSubstitution + Substitutions insertion point)
    - src/CookBot.Domain/Recipes/StepTemperature.cs (structural analog for RecipeProvenance sealed record)
    - src/CookBot.Domain/Recipes/RecipeDocument.cs (Equipment/Provenance insertion after Steps; MaxLength precedents)
    - src/CookBot.Domain/Recipes/StepNode.cs (ContentStep.Temperature precedent for DonenessCue)
    - .planning/phases/12-richer-format-v3-v4-schema-bump/12-PATTERNS.md (sections for each of these 5 files — exact field shapes)
    - 12-CONTEXT.md D-12-01, D-12-02, D-12-03, D-12-05, D-12-06, D-12-07
  </read_first>
  <behavior>
    - IngredientSubstitution serializes/deserializes round-trip: required Note, optional Name/Amount/Unit, with [JsonExtensionData] Extras preserving unknown keys.
    - RecipeProvenance serializes/deserializes round-trip with all-optional SourceUrl/AuthorName/SourceName + Extras; absent JSON keys deserialize to null.
    - RecipeDocument with Equipment populated and Provenance populated round-trips; absent equipment deserializes to empty list (not null), absent provenance to null.
    - IngredientEntry with Substitutions populated round-trips; absent substitutions deserializes to empty list (not null).
    - ContentStep with DonenessCue populated round-trips; absent donenessCue deserializes to null; SectionStep has no DonenessCue property.
  </behavior>
  <action>
    Create `IngredientSubstitution.cs` (namespace `CookBot.Domain.Recipes`, `public sealed record`) per D-12-01/02/03 modeled on `IngredientEntry.cs`: `required string Note` (`[JsonPropertyName("note")]`, `[MaxLength(512)]`), `string? Name` (`[JsonPropertyName("name")]`, `[MaxLength(256)]`), `double? Amount` (`[JsonPropertyName("amount")]`), `string? Unit` (`[JsonPropertyName("unit")]`), and `[JsonExtensionData] Dictionary<string, JsonElement> Extras { get; init; } = new();`. Imports: `System.ComponentModel.DataAnnotations`, `System.Text.Json`, `System.Text.Json.Serialization`.

    Create `RecipeProvenance.cs` (namespace `CookBot.Domain.Recipes`, `public sealed record`) per D-12-07 modeled on `StepTemperature.cs`: `string? SourceUrl` (`[JsonPropertyName("sourceUrl")]`, `[MaxLength(2048)]`), `string? AuthorName` (`[JsonPropertyName("authorName")]`, `[MaxLength(256)]`), `string? SourceName` (`[JsonPropertyName("sourceName")]`, `[MaxLength(512)]`), and `[JsonExtensionData] Dictionary<string, JsonElement> Extras { get; init; } = new();`. No `AdaptedDate` (deferred). Same three imports as above.

    In `RecipeDocument.cs`, after the `Steps` property (after line 44), add `[JsonPropertyName("equipment")] public IReadOnlyList<string> Equipment { get; init; } = [];` (D-12-05) and `[JsonPropertyName("provenance")] public RecipeProvenance? Provenance { get; init; }` (D-12-07). Equipment follows the empty-list-not-null convention; Provenance follows the nullable-record convention.

    In `IngredientEntry.cs`, between the `Note` property and the `Extras` property (after line 29), add `[JsonPropertyName("substitutions")] public IReadOnlyList<IngredientSubstitution> Substitutions { get; init; } = [];` (D-12-02).

    In `StepNode.cs`, on `ContentStep` ONLY, after the `Temperature` property (after line 25), add `[JsonPropertyName("donenessCue")] public string? DonenessCue { get; init; }` with `[MaxLength(512)]` (D-12-06). Add `using System.ComponentModel.DataAnnotations;` to StepNode.cs if not already present. Do NOT add DonenessCue to SectionStep.
  </action>
  <verify>
    <automated>dotnet build src/CookBot.Domain/CookBot.Domain.csproj 2>&1 | grep -qE "Build succeeded|0 Error" && echo BUILD_OK</automated>
  </verify>
  <acceptance_criteria>
    - `IngredientSubstitution.cs` exists and contains `public sealed record IngredientSubstitution`, `required string Note`, and `[JsonExtensionData]`.
    - `RecipeProvenance.cs` exists and contains `public sealed record RecipeProvenance` with `string? SourceUrl`, `string? AuthorName`, `string? SourceName`; contains NO `AdaptedDate`.
    - `grep -c 'AdaptedDate' src/CookBot.Domain/Recipes/RecipeProvenance.cs` returns 0.
    - `RecipeDocument.cs` contains `IReadOnlyList<string> Equipment` and `RecipeProvenance? Provenance`.
    - `IngredientEntry.cs` contains `IReadOnlyList<IngredientSubstitution> Substitutions`.
    - `StepNode.cs` `ContentStep` contains `string? DonenessCue`; `grep` confirms `donenessCue` is NOT added under SectionStep.
    - `dotnet build src/CookBot.Domain` succeeds with 0 errors.
  </acceptance_criteria>
  <done>All five Domain files compile; the four new field groups exist on the canonical POCOs with correct JsonPropertyName/MaxLength/Extras conventions and no scope-reduced placeholders.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ship Migration_V3_To_V4 + DI registration + CurrentVersion bump atomically</name>
  <files>src/CookBot.Application/Recipes/Migration_V3_To_V4.cs, src/CookBot.Application/Recipes/RecipeUpcasterChain.cs, src/CookBot.Application/DependencyInjection.cs</files>
  <read_first>
    - src/CookBot.Application/Recipes/Migration_V2_To_V3.cs (verbatim copy-template — four independent no-op guards + version stamp)
    - src/CookBot.Application/Recipes/RecipeUpcasterChain.cs (CurrentVersion const at line 14; gap-validation in constructor)
    - src/CookBot.Application/DependencyInjection.cs (registration block lines 29-31)
    - .planning/phases/12-richer-format-v3-v4-schema-bump/12-PATTERNS.md (Migration_V3_To_V4 / RecipeUpcasterChain / DependencyInjection sections; Pattern 5/6/7 in RESEARCH)
    - 12-CONTEXT.md D-12-12, D-12-13 (P1 + P2 invariants)
  </read_first>
  <behavior>
    - Migration_V3_To_V4 reports FromVersion=3, ToVersion=4.
    - Upcasting any v3 JsonNode stamps version=4 and never throws, even when only one of the four field groups is present and the other three absent (independent guards — P2).
    - Migration_V3_To_V4 does NOT zero-fill absent fields: it never sets equipment to [], provenance to {}, donenessCue, or substitutions — only stamps version.
    - RecipeUpcasterChain constructed with all three real upcasters resolves without throwing; a chain missing 3→4 throws InvalidOperationException with "gap" in the message.
  </behavior>
  <action>
    ATOMIC TASK — P1 guard: `Migration_V3_To_V4.cs` creation, the `CurrentVersion` bump, and the DI registration MUST all be in this single task. Never split.

    Create `Migration_V3_To_V4.cs` in `CookBot.Application.Recipes` by copying `Migration_V2_To_V3.cs` structure verbatim: `public sealed class Migration_V3_To_V4 : IRecipeUpcaster`, `public int FromVersion => 3;`, `public int ToVersion => 4;`, `public JsonNode Upcast(JsonNode input)` with `var obj = input.AsObject();`. Add FOUR independent `if` guard blocks (each a documented no-op, separate blocks — PITFALL C7/P2, never combined): (1) `if (obj["equipment"] is null) { /* no-op: IReadOnlyList<string> defaults to [] */ }`; (2) `if (obj["provenance"] is null) { /* no-op: RecipeProvenance? defaults to null */ }`; (3) walk `obj["steps"]` as `JsonArray`, `OfType<JsonObject>()`, where `kind=="content"` guard absent `donenessCue` (no-op, string? maps absent→null); (4) walk `obj["ingredients"]` as `JsonArray`, `OfType<JsonObject>()`, guard absent `substitutions` (no-op, empty-list default). Final line: `obj["version"] = 4;` then `return obj;`. Do NOT zero-fill any field.

    In `RecipeUpcasterChain.cs` line 14, change `public const int CurrentVersion = 3;` to `public const int CurrentVersion = 4;`.

    In `DependencyInjection.cs`, insert `services.AddSingleton<IRecipeUpcaster, Migration_V3_To_V4>();` between the `Migration_V2_To_V3` registration (line 30) and `services.AddSingleton<RecipeUpcasterChain>();` (line 31). RecipeUpcasterChain MUST remain the last registration so DI fully resolves `IEnumerable<IRecipeUpcaster>` before the chain constructor runs gap-validation.
  </action>
  <verify>
    <automated>dotnet build src/CookBot.Application/CookBot.Application.csproj 2>&1 | grep -qE "Build succeeded|0 Error" && echo BUILD_OK</automated>
  </verify>
  <acceptance_criteria>
    - `Migration_V3_To_V4.cs` contains `: IRecipeUpcaster`, `FromVersion => 3`, `ToVersion => 4`, and `obj["version"] = 4`.
    - `grep -c '^\s*if (obj\[' src/CookBot.Application/Recipes/Migration_V3_To_V4.cs` shows the two top-level field guards are separate `if` blocks (equipment, provenance), plus the two array-walk guards — four independent guards total, none combined into a compound condition.
    - `Migration_V3_To_V4.cs` contains NO `new JsonArray()` / `new JsonObject()` assignment to equipment/provenance/substitutions/donenessCue (no zero-fill).
    - `RecipeUpcasterChain.cs` contains `CurrentVersion = 4` and NOT `CurrentVersion = 3`.
    - `DependencyInjection.cs` contains `AddSingleton<IRecipeUpcaster, Migration_V3_To_V4>()`, and that line appears BEFORE `AddSingleton<RecipeUpcasterChain>()` (verify ordering).
    - `dotnet build src/CookBot.Application` succeeds with 0 errors.
  </acceptance_criteria>
  <done>The upcaster class, CurrentVersion=4, and DI registration land together; the app can resolve RecipeUpcasterChain without a startup gap (P1 avoided), and the four guards are independent (P2 avoided).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Validator warnings + full upcaster/chain/fixture test matrix</name>
  <files>src/CookBot.Application/Recipes/RecipeValidator.cs, tests/CookBot.Tests/Recipes/Migration_V3_To_V4_Tests.cs, tests/CookBot.Tests/Recipes/Migration_V3_To_V4_ChainTests.cs, tests/CookBot.Tests/Recipes/RecipeUpcasterTests.cs, tests/CookBot.Tests/Fixtures/Recipes/upcaster/v3-to-v4-no-fields.json, tests/CookBot.Tests/Fixtures/Recipes/upcaster/v3-to-v4-all-present.json, tests/CookBot.Tests/Fixtures/Recipes/upcaster/v3-to-v4-substitutions-only.json, tests/CookBot.Tests/Fixtures/Recipes/upcaster/v3-to-v4-equipment-only.json, tests/CookBot.Tests/Fixtures/Recipes/upcaster/v3-to-v4-doneness-only.json, tests/CookBot.Tests/Fixtures/Recipes/upcaster/v3-to-v4-provenance-only.json</files>
  <read_first>
    - src/CookBot.Application/Recipes/RecipeValidator.cs (DetectOrphanIngredients pattern + dispatch site; confirm class has no constructor)
    - tests/CookBot.Tests/Recipes/Migration_V2_To_V3_Tests.cs (template)
    - tests/CookBot.Tests/Recipes/Migration_V2_To_V3_ChainTests.cs (template)
    - tests/CookBot.Tests/Recipes/RecipeUpcasterTests.cs (gap test + MakeChain)
    - tests/CookBot.Tests/Fixtures/Recipes/upcaster/v2-to-v3-all-present.json and v2-to-v3-no-fields.json (fixture format)
    - .planning/phases/12-richer-format-v3-v4-schema-bump/12-PATTERNS.md (RecipeValidator section with DetectInvalidProvenanceUrl/DetectEmptySubstitutions; test sections; fixture section)
    - 12-CONTEXT.md D-12-15 (validator never throws); 12-RESEARCH.md open question #1 (inline scheme check)
  </read_first>
  <behavior>
    - RecipeValidator returns a ValidationResult (never throws). A doc with provenance SourceUrl using a disallowed scheme (e.g. javascript:) yields a warning Code "InvalidProvenanceUrl" at path /provenance/sourceUrl. A valid http/https URL yields no such warning. A null/empty SourceUrl yields no warning.
    - A substitution with neither Note nor Name yields a warning Code "EmptySubstitution" at /ingredients/{i}/substitutions/{j}. These are warnings, not errors — they do not populate the errors list.
    - Each v3-to-v4-*.json fixture upcasts to version=4 with no throw.
    - A v3 doc with no new fields upcasts so that, after deserialization, equipment is empty, provenance is null, every content step's donenessCue is null, and every ingredient's substitutions is empty.
    - A v4 doc passed through the chain is an identity pass (version stays 4).
    - A chain missing the 3→4 upcaster throws InvalidOperationException containing "gap".
  </behavior>
  <action>
    In `RecipeValidator.cs`, add two private static warning methods modeled on `DetectOrphanIngredients` (D-12-15). `DetectInvalidProvenanceUrl(RecipeDocument doc, List<ValidationWarning> warnings)`: read `doc.Provenance?.SourceUrl`; return early if null/whitespace; if `!Uri.TryCreate(url.Trim(), UriKind.Absolute, out var uri) || uri.Scheme is not ("http" or "https")` add `new ValidationWarning(Path: "/provenance/sourceUrl", Code: "InvalidProvenanceUrl", Message: "Provenance SourceUrl must be an absolute http or https URL.")`. Use the inline `Uri` scheme check — do NOT add a constructor dependency on RecipePhotoUrlValidator (RESEARCH open question #1; the validator class stays constructor-free). `DetectEmptySubstitutions(RecipeDocument doc, List<ValidationWarning> warnings)`: for each ingredient index i and substitution index j, if both `Note` and `Name` are null/whitespace add `new ValidationWarning(Path: $"/ingredients/{i}/substitutions/{j}", Code: "EmptySubstitution", Message: "Substitution has neither a Note nor a Name.")`. Dispatch both new methods from the `Validate` method alongside the existing `DetectOrphanIngredients(doc, warnings); DetectEmptySections(doc, warnings);` calls, before `return new ValidationResult(...)`.

    Create six fixtures under `tests/CookBot.Tests/Fixtures/Recipes/upcaster/` by copying the v2→v3 fixture format and setting `"version": 3`. `v3-to-v4-no-fields.json`: simple v3 doc, none of the four new groups. `v3-to-v4-all-present.json`: v3 doc with top-level `equipment` array, `provenance` object (sourceName/authorName/sourceUrl), one ingredient with a `substitutions` array, one content step with `donenessCue` and `temperature`. The four `*-only.json` fixtures each carry version 3 and exactly one named field group (derive from all-present by removing the other three groups).

    Create `Migration_V3_To_V4_Tests.cs` by copying `Migration_V2_To_V3_Tests.cs`: rename class to `Migration_V3_To_V4_Tests`; `MakeChain()` uses `new IRecipeUpcaster[] { new Migration_V1_To_V2(), new Migration_V2_To_V3(), new Migration_V3_To_V4() }`; `V3ToV4Fixtures()` MemberData globs `v3-to-v4-*.json`; `Upcast_V3Fixture_ProducesVersion4` theory asserts version==4 and no throw per fixture (SC1/P2); `Upcast_NoNewFields_NewFieldsAreNull` deserializes the no-fields fixture after upcast and asserts equipment empty, provenance null, every content step donenessCue null, every ingredient substitutions empty; `Upcast_VersionAlreadyFour_IsIdentity` checks v4 identity.

    Create `Migration_V3_To_V4_ChainTests.cs` by copying `Migration_V2_To_V3_ChainTests.cs`: `Migration_V3_To_V4_HasCorrectVersionRange` (FromVersion=3, ToVersion=4); `RecipeUpcasterChain_CurrentVersion_IsFour` (asserts `RecipeUpcasterChain.CurrentVersion == 4`); `Migration_V3_To_V4_UpcastsVersionFieldToFour` (input version 3 → result version 4); `Chain_WithAllThreeUpcasters_UpcastsV1ToV4` (v1 input through all three upcasters → version 4).

    In `RecipeUpcasterTests.cs`, add `RecipeUpcasterChain_GapInVersions_V3ToV4_ThrowsAtConstruction`: construct a chain with `Migration_V1_To_V2`, `Migration_V2_To_V3`, and a `FakeUpcaster(4, 5)` (leaving the 3→4 gap), assert `InvalidOperationException` containing "gap" (SC4/D-12-13). Add `UpcastToCurrent_VersionAlreadyFour_IsIdentity` using a chain of all three real upcasters and a `{"version":4,...}` node asserting version stays 4. Leave the existing gap test intact.
  </action>
  <verify>
    <automated>dotnet test tests/CookBot.Tests/CookBot.Tests.csproj --filter "Category!=RequiresApiKey&FullyQualifiedName~Migration_V3_To_V4|FullyQualifiedName~RecipeUpcaster" 2>&1 | grep -qE "Passed!|Passed:" && echo TESTS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `RecipeValidator.cs` contains `DetectInvalidProvenanceUrl` and `DetectEmptySubstitutions`, both adding to the warnings list (not errors), and both are dispatched from `Validate`.
    - `RecipeValidator.cs` has NO constructor parameter for `RecipePhotoUrlValidator` (inline scheme check confirmed).
    - Six `v3-to-v4-*.json` fixtures exist, each with `"version": 3`.
    - `Migration_V3_To_V4_Tests.cs` and `Migration_V3_To_V4_ChainTests.cs` exist with the named test methods.
    - `RecipeUpcasterTests.cs` contains `RecipeUpcasterChain_GapInVersions_V3ToV4_ThrowsAtConstruction`.
    - `dotnet test --filter "...Migration_V3_To_V4|...RecipeUpcaster"` exits 0 with all matched tests passing (SC1 + SC4 proven).
  </acceptance_criteria>
  <done>The validator emits non-blocking warnings for invalid provenance URL and empty substitutions; the upcaster, chain wiring, and gap detection are proven by passing tests across no-fields/all-present/partial fixtures (SC1, SC4).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored recipe JSON → upcaster/validator | Untrusted/legacy `CanonicalDocumentJson` content (incl. imported `.cookbook.json`) crosses into the v3→v4 upcaster and validator |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | `Provenance.SourceUrl` carrying `javascript:`/`data:` scheme persisted via the canonical doc | mitigate | `RecipeValidator.DetectInvalidProvenanceUrl` inline allowlist (http/https) emits a warning; the live-link defang itself is enforced at render time in Plan 04 (D-12-08). |
| T-12-02 | Denial of Service | Oversized provenance/substitution/doneness strings stored to the JSON column | mitigate | `[MaxLength]` caps on every new string field (Note 512, Name 256, SourceUrl 2048, AuthorName 256, SourceName 512, DonenessCue 512). |
| T-12-03 | Tampering | Partial/malformed v3 doc triggering a bundle-throw mid-upcast (partial upcast / data loss) | mitigate | Four independent null-guards in `Migration_V3_To_V4` (P2); fixture-matrix tests prove no throw across partial-field inputs (SC1). |

Surface is small and additive. No new auth, session, or network surface — these are POCO/migration/validator changes only.
</threat_model>

<verification>
- `dotnet build` of Domain + Application succeeds (0 errors).
- `dotnet test tests/CookBot.Tests/ --filter "Category!=RequiresApiKey"` passes (no existing v3 test deleted; new v3→v4 suite green).
- App startup smoke (DI resolves RecipeUpcasterChain): `dotnet build src/CookBot.Web` succeeds — chain gap would surface at runtime, but the gap test proves construction-time wiring.
</verification>

<success_criteria>
- SC1: a v3 doc upcasts to v4 with all four new groups null/empty, no throw, no bundle-throw (proven by fixture-matrix tests).
- SC4: `RecipeUpcasterChain.CurrentVersion == 4`, `Migration_V3_To_V4` registered in DI, v3→v4 gap-detection test present and passing.
- FORMAT-01..04 in-memory shapes exist on the canonical POCOs; FORMAT-05 upcaster ships; FORMAT-06 validator rules added.
</success_criteria>

<output>
Create `.planning/phases/12-richer-format-v3-v4-schema-bump/12-01-SUMMARY.md` when done.
</output>
