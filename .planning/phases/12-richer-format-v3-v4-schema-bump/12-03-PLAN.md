---
phase: 12-richer-format-v3-v4-schema-bump
plan: 03
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - src/CookBot.Application/Recipes/RecipeSchemaDocumentationProvider.cs
  - tests/CookBot.Tests/Snapshots/PromptSnapshotTests.BuildSystemPrompt.verified.txt
  - tests/CookBot.Tests/Recipes/SchemaAssertionTests.cs
autonomous: true
requirements: [FORMAT-06]

must_haves:
  truths:
    - "The AI system prompt describes the v4 shape: version 4, equipment, per-ingredient substitutions, per-step donenessCue, and provenance (SC3)"
    - "The prompt instructs the AI to naturally populate equipment and donenessCue, emit substitutions only when useful, and NEVER fabricate provenance SourceUrl/AuthorName (D-12-09/D-12-11)"
    - "The prompt-snapshot test is regenerated and passing — no AI schema drift (SC3, P3)"
    - "The reflected AI JSON schema includes equipment, provenance, substitutions, and donenessCue (RecipeJsonSchemaProvider auto-reflects the v4 POCOs — no provider code change)"
  artifacts:
    - path: "src/CookBot.Application/Recipes/RecipeSchemaDocumentationProvider.cs"
      provides: "v4 FormatPrompt prose with the four new fields and D-12-09/D-12-11 guidance"
      contains: "\"version\": 4"
    - path: "tests/CookBot.Tests/Snapshots/PromptSnapshotTests.BuildSystemPrompt.verified.txt"
      provides: "Regenerated byte-stable prompt snapshot"
      contains: "version"
    - path: "tests/CookBot.Tests/Recipes/SchemaAssertionTests.cs"
      provides: "Schema assertions for the four new v4 fields"
      contains: "equipment"
  key_links:
    - from: "src/CookBot.Application/Recipes/RecipeSchemaDocumentationProvider.cs"
      to: "tests/CookBot.Tests/Snapshots/PromptSnapshotTests.BuildSystemPrompt.verified.txt"
      via: "snapshot regenerated in the same change as the prose update (P3)"
      pattern: "version.*4"
---

<objective>
Update the AI system prompt prose to describe the v4 shape and its default-fill / no-fabrication policy, regenerate the prompt snapshot in the same change, and assert the reflected JSON schema includes the four new fields. Closes SC3 (AI emits v4 fields, snapshot passing, no schema drift / P3).

Purpose: `RecipeJsonSchemaProvider` auto-reflects the v4 POCOs (no provider code change — Plan 01 already added the fields), but the hand-authored `FormatPrompt` prose and its byte-stable snapshot must be updated atomically or the snapshot test fails and the AI never learns the new shape (FORMAT-06, PITFALL P3). Per D-12-14 the provider needs no code change and the snapshot is regenerated and committed in the same change.
Output: updated `RecipeSchemaDocumentationProvider.FormatPrompt`, regenerated `PromptSnapshotTests.BuildSystemPrompt.verified.txt`, and new schema-presence assertions in `SchemaAssertionTests.cs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-richer-format-v3-v4-schema-bump/12-CONTEXT.md
@.planning/phases/12-richer-format-v3-v4-schema-bump/12-RESEARCH.md
@.planning/phases/12-richer-format-v3-v4-schema-bump/12-PATTERNS.md

<interfaces>
<!-- Plan 01 already added the v4 fields to the POCOs; the schema reflects automatically. -->
Files to modify (self-analogs — read directly):
  - src/CookBot.Application/Recipes/RecipeSchemaDocumentationProvider.cs (FormatPrompt raw string, lines 11-53)
  - tests/CookBot.Tests/Prompts/PromptSnapshotTests.cs (BuildSystemPrompt — uses Verifier.Verify; do NOT modify, just regenerate the .verified.txt)
  - tests/CookBot.Tests/Recipes/SchemaAssertionTests.cs (existing schema-presence assertion style; add the four new-field assertions)
  - src/CookBot.Application/Recipes/RecipeJsonSchemaProvider.cs (NO CHANGE — JsonSchemaExporter reflects v4 POCOs; SetAdditionalPropertiesFalse + ExternalizeAnyOfBranches passes handle the new nested records IngredientSubstitution / RecipeProvenance automatically — FLAG 4)

Snapshot regen command (Verify.Xunit 31.12.5):
  VERIFY_AUTO=true dotnet test tests/CookBot.Tests/ --filter "FullyQualifiedName~BuildSystemPrompt"
  (then inspect the .verified.txt diff and commit it WITH the prose change — P3 gate)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update FormatPrompt prose to v4 with default-fill + no-fabrication guidance, regenerate snapshot</name>
  <files>src/CookBot.Application/Recipes/RecipeSchemaDocumentationProvider.cs, tests/CookBot.Tests/Snapshots/PromptSnapshotTests.BuildSystemPrompt.verified.txt</files>
  <read_first>
    - src/CookBot.Application/Recipes/RecipeSchemaDocumentationProvider.cs (FormatPrompt raw string literal, lines 11-53)
    - tests/CookBot.Tests/Prompts/PromptSnapshotTests.cs (confirm the test name BuildSystemPrompt and that it Verifies the assembled system prompt)
    - .planning/phases/12-richer-format-v3-v4-schema-bump/12-PATTERNS.md (RecipeSchemaDocumentationProvider section — the six required mutations)
    - 12-CONTEXT.md D-12-09 (never fabricate provenance), D-12-11 (populate equipment + donenessCue naturally; substitutions only when useful), D-12-14 (no provider code change; snapshot regenerated in the same change)
  </read_first>
  <action>
    Edit the `FormatPrompt` raw string in `RecipeSchemaDocumentationProvider.cs` (lines 11-53) with these six mutations (D-12-11, D-12-09):
    1. Change `"version": 3` to `"version": 4` in the example JSON.
    2. Add a top-level `"equipment": ["stand mixer", "9-inch cake pan"]` to the example (after the `"tags"` line).
    3. Add `"substitutions": [{"note": "use oat milk for dairy-free"}]` to at least one ingredient object in the example.
    4. Add `"donenessCue": "golden brown on top and a toothpick comes out clean"` to at least one content step object in the example.
    5. Add a top-level `"provenance": null` to the example with the explicit D-12-09 directive in the field guidance: leave `provenance` null unless a source is explicitly provided; never invent a URL or author.
    6. Extend the "Field guidance" block with D-12-11 instructions: naturally populate `equipment` and per-step `donenessCue` (high value, zero hallucination cost); emit `substitutions` only when genuinely useful; empty/null is always valid output for all four new fields.

    After editing, regenerate the snapshot: run `VERIFY_AUTO=true dotnet test tests/CookBot.Tests/ --filter "FullyQualifiedName~BuildSystemPrompt"`. This rewrites `tests/CookBot.Tests/Snapshots/PromptSnapshotTests.BuildSystemPrompt.verified.txt`. Inspect the diff to confirm it contains the v4 shape and the new fields, then keep both the `.cs` change and the regenerated `.verified.txt` together (P3 — never commit one without the other). Do NOT modify `PromptSnapshotTests.cs` itself. If `VERIFY_AUTO=true` does not auto-accept under this Verify version, manually copy the produced `.received.txt` to `.verified.txt` (RESEARCH assumption A1).
  </action>
  <verify>
    <automated>dotnet test tests/CookBot.Tests/CookBot.Tests.csproj --filter "FullyQualifiedName~BuildSystemPrompt" 2>&1 | grep -qE "Passed!|Passed:" && echo SNAPSHOT_OK</automated>
  </verify>
  <acceptance_criteria>
    - `RecipeSchemaDocumentationProvider.cs` FormatPrompt contains `"version": 4` and does NOT contain `"version": 3`.
    - FormatPrompt contains `equipment`, `substitutions`, `donenessCue`, and `provenance` in the example JSON.
    - FormatPrompt contains the no-fabrication directive for provenance (text instructing the AI to leave provenance null / never invent a URL or author — D-12-09).
    - `PromptSnapshotTests.BuildSystemPrompt.verified.txt` contains `"version": 4` and the four new field names; the `.received.txt` no longer differs.
    - `dotnet test --filter "...BuildSystemPrompt"` exits 0 (snapshot matches — P3 satisfied).
    - `PromptSnapshotTests.cs` is unmodified.
  </acceptance_criteria>
  <done>The AI prompt teaches the v4 shape with the locked default-fill and no-fabrication policy, and the regenerated snapshot passes — no AI schema drift (SC3, FORMAT-06, P3).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Assert the reflected JSON schema includes the four new v4 fields</name>
  <files>tests/CookBot.Tests/Recipes/SchemaAssertionTests.cs</files>
  <read_first>
    - tests/CookBot.Tests/Recipes/SchemaAssertionTests.cs (existing schema-presence assertion style — how it loads/inspects the emitted schema)
    - src/CookBot.Application/Recipes/RecipeJsonSchemaProvider.cs (confirm NO change is needed — FLAG 4; the two post-processing passes handle the new nested records)
    - .planning/phases/12-richer-format-v3-v4-schema-bump/12-PATTERNS.md (FLAG 4 — verify schema assertions; PITFALL P4 anyOf/additionalProperties)
  </read_first>
  <behavior>
    - The schema emitted by RecipeJsonSchemaProvider contains a top-level `equipment` property and a top-level `provenance` property.
    - The schema contains a per-ingredient `substitutions` property and a per-content-step `donenessCue` property.
    - The new nested object subschemas (IngredientSubstitution, RecipeProvenance) carry `additionalProperties: false` (Anthropic strict-mode requirement applied by the existing SetAdditionalPropertiesFalse pass — P4).
  </behavior>
  <action>
    Add assertions to `SchemaAssertionTests.cs` following the file's existing pattern for inspecting the emitted schema (do NOT change `RecipeJsonSchemaProvider` — FLAG 4 confirms no provider code change). Assert presence of: top-level `equipment` and `provenance` properties; the `substitutions` property on the ingredient subschema; the `donenessCue` property on the content-step subschema. Add an assertion that the new nested object subschemas (for `IngredientSubstitution` and `RecipeProvenance`) have `additionalProperties` set to `false` (proves the strict-mode pass handled them — P4). Reuse the existing schema-loading helper in the test class; do not introduce a new schema library (JsonSchema.Net is already present; System.Text.Json node inspection is sufficient and matches the existing assertions).
  </action>
  <verify>
    <automated>dotnet test tests/CookBot.Tests/CookBot.Tests.csproj --filter "Category!=RequiresApiKey&FullyQualifiedName~SchemaAssertion" 2>&1 | grep -qE "Passed!|Passed:" && echo SCHEMA_OK</automated>
  </verify>
  <acceptance_criteria>
    - `SchemaAssertionTests.cs` contains assertions referencing `equipment`, `provenance`, `substitutions`, and `donenessCue`.
    - `SchemaAssertionTests.cs` asserts `additionalProperties` is `false` on the new nested record subschemas.
    - `RecipeJsonSchemaProvider.cs` is unmodified (`git diff --stat` shows it is not in this plan's changeset).
    - `dotnet test --filter "...SchemaAssertion"` exits 0.
  </acceptance_criteria>
  <done>The emitted AI schema is proven to include all four new fields with strict-mode additionalProperties, with zero provider code change (FLAG 4, P4).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI-generated recipe JSON → canonical doc | Untrusted model output is parsed into RecipeDocument |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | Spoofing / Information disclosure | AI fabricating an authoritative provenance SourceUrl/AuthorName ("from nytimes.com/...") | mitigate | D-12-09 prompt directive instructs the model to leave provenance null and never invent a URL or author; prompt prose carries the directive and is locked by the snapshot test. |
| T-12-06 | Tampering | Prompt prose and reflected schema drift apart (model learns a shape the parser cannot accept) | mitigate | Snapshot regenerated atomically with the prose change (P3); SchemaAssertionTests proves the reflected schema includes the four fields with strict-mode additionalProperties (P4). |

No new package is added (`JsonSchema.Net` already present from Phase 1). No new network/auth surface.
</threat_model>

<verification>
- `dotnet test tests/CookBot.Tests/ --filter "Category!=RequiresApiKey"` passes (snapshot + schema assertions green).
- `RecipeJsonSchemaProvider.cs` confirmed unchanged.
- Prose change and regenerated snapshot are in the same changeset.
</verification>

<success_criteria>
- SC3 / FORMAT-06: the prompt describes the v4 fields (even when null), the snapshot test passes, and the reflected schema includes the four fields — no AI schema drift (P3, P4).
</success_criteria>

<output>
Create `.planning/phases/12-richer-format-v3-v4-schema-bump/12-03-SUMMARY.md` when done.
</output>
