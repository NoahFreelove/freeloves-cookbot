---
phase: 12-richer-format-v3-v4-schema-bump
plan: 04
type: execute
wave: 3
depends_on: ["12-01", "12-02"]
files_modified:
  - src/CookBot.Web/Components/Pages/RecipeEditorParts/RecipeStepEditor.razor
  - src/CookBot.Web/Components/Pages/RecipeEditor.razor
  - src/CookBot.Web/Components/Pages/RecipeView.razor
autonomous: false
requirements: [FORMAT-01, FORMAT-02, FORMAT-03, FORMAT-04]

must_haves:
  truths:
    - "RecipeEditor authors all four field groups: equipment chip card, provenance form card, per-ingredient substitution sub-rows, and (via RecipeStepEditor) a per-step doneness cue input (SC5 authoring, UI-SPEC surfaces 5-8)"
    - "The editor save path and PopulateFromParsed carry all four groups so author→save→reload round-trips end-to-end (SC2 UI completion)"
    - "RecipeView displays all four field groups: equipment checklist, substitution sub-lines under ingredients, per-step doneness cue, provenance credit (SC5 display, UI-SPEC surfaces 1-4)"
    - "Provenance SourceUrl renders as a live link only when it passes the RecipePhotoUrlValidator http/https allowlist; otherwise plain text (D-12-08)"
    - "Substitution amounts render as static text and never scale with servings (D-12-04)"
  artifacts:
    - path: "src/CookBot.Web/Components/Pages/RecipeEditor.razor"
      provides: "Equipment card, provenance card, substitution sub-rows, save/populate wiring for the four groups"
      contains: "Source &amp; Credit"
    - path: "src/CookBot.Web/Components/Pages/RecipeEditorParts/RecipeStepEditor.razor"
      provides: "Per-step doneness cue input alongside the temperature picker"
      contains: "DonenessCue"
    - path: "src/CookBot.Web/Components/Pages/RecipeView.razor"
      provides: "Equipment checklist, substitution sub-lines, doneness cue, provenance credit display"
      contains: "Equipment"
  key_links:
    - from: "src/CookBot.Web/Components/Pages/RecipeView.razor"
      to: "src/CookBot.Application/Services/RecipePhotoUrlValidator.cs"
      via: "TryValidate on Provenance.SourceUrl in OnParametersSet → _validatedSourceUrl gates the <a> tag"
      pattern: "RecipePhotoUrlValidator"
    - from: "src/CookBot.Web/Components/Pages/RecipeEditor.razor"
      to: "src/CookBot.Domain/Interfaces/IRecipeFormatParser.cs"
      via: "SaveRecipe builds ParsedRecipe.Equipment/Provenance; PopulateFromParsed reads them back"
      pattern: "Equipment = _equipment"
---

<objective>
Surface the four new v4 field groups in the two authoring/display surfaces: RecipeEditor (+ RecipeStepEditor) for authoring and RecipeView for display, exactly per the approved UI-SPEC. Wire the editor save and PopulateFromParsed paths so author→save→reload completes the SC2 round-trip end-to-end. Closes SC5 and completes SC2.

Purpose: Plan 01 added the canonical shape, Plan 02 made it round-trip through the parser; this plan lets a user author and read the four groups. Per D-12-10 the new fields are surfaced in RecipeEditor + RecipeView ONLY — Cooking Mode is explicitly out of scope (do NOT touch CookingMode.razor). Custom Cb atoms only (no MudBlazor); provenance link safety via the existing RecipePhotoUrlValidator (D-12-08); substitution amounts are display-only (D-12-04).
Output: doneness input in RecipeStepEditor; equipment card + provenance card + substitution sub-rows + save/populate wiring in RecipeEditor; equipment checklist + substitution sub-lines + doneness cue + provenance credit in RecipeView.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-richer-format-v3-v4-schema-bump/12-CONTEXT.md
@.planning/phases/12-richer-format-v3-v4-schema-bump/12-RESEARCH.md
@.planning/phases/12-richer-format-v3-v4-schema-bump/12-PATTERNS.md
@.planning/phases/12-richer-format-v3-v4-schema-bump/12-UI-SPEC.md

<interfaces>
<!-- Plan 01 (POCOs) + Plan 02 (editor DTOs) are prerequisites. -->
Consumed types:
  - RecipeDocument.Equipment (IReadOnlyList<string>), RecipeDocument.Provenance (RecipeProvenance?)
  - IngredientEntry.Substitutions / ParsedIngredient.Substitutions (List<IngredientSubstitution>)
  - ContentStep.DonenessCue / ParsedStep.DonenessCue (string?)
  - ParsedRecipe.Equipment (List<string>), ParsedRecipe.Provenance (RecipeProvenance?)
  - RecipeProvenance: string? SourceUrl, AuthorName, SourceName
  - IngredientSubstitution: required string Note, string? Name, double? Amount, string? Unit

Web surfaces (self-analogs — read directly; UI-SPEC §1-8 is the binding contract):
  - src/CookBot.Web/Components/Pages/RecipeEditorParts/RecipeStepEditor.razor (temperature picker block lines 128-134; OnTemperatureChanged ~line 264 — mutate-in-place pattern)
  - src/CookBot.Web/Components/Pages/RecipeEditor.razor:
      Tags card lines 270-300 (analog for Equipment card); Times & servings card ~230-268 (analog for Provenance card)
      ingredient loop ~118-162 (substitution sub-rows insert here)
      PopulateFromParsed at line 488 (FLAG 2 — load _equipment/_prov*/substitutions from parsed)
      SaveRecipe ParsedRecipe initializer at lines 786-805 (FLAG 3 — add Equipment + Provenance; substitutions already on _ingredients)
  - src/CookBot.Web/Components/Pages/RecipeView.razor:
      hero/description block ~54-67 (provenance insert after description)
      ingredient loop ~149-157 (substitution sub-lines); Tags block ~159-167 (equipment checklist after)
      step loop / ContentStep temperature block ~205-214 (doneness cue after temperature)

URL safety:
  - src/CookBot.Application/Services/RecipePhotoUrlValidator.cs (TryValidate — already a DI Singleton; inject into RecipeView)

UI-SPEC surfaces (binding): §1 equipment checklist, §2 substitution sub-lines, §3 doneness cue, §4 provenance credit (RecipeView); §5 substitution authoring, §6 equipment card, §7 doneness field, §8 provenance card (RecipeEditor). Copywriting Contract and Accessibility Contract in UI-SPEC are binding.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RecipeStepEditor doneness cue input + RecipeEditor authoring (equipment, provenance, substitutions) + save/populate wiring</name>
  <files>src/CookBot.Web/Components/Pages/RecipeEditorParts/RecipeStepEditor.razor, src/CookBot.Web/Components/Pages/RecipeEditor.razor</files>
  <read_first>
    - src/CookBot.Web/Components/Pages/RecipeEditorParts/RecipeStepEditor.razor (temperature picker block 128-134; OnTemperatureChanged ~264)
    - src/CookBot.Web/Components/Pages/RecipeEditor.razor (Tags card 270-300; Times card 230-268; ingredient loop 118-162; PopulateFromParsed @488; SaveRecipe @786-805)
    - .planning/phases/12-richer-format-v3-v4-schema-bump/12-UI-SPEC.md §5, §6, §7, §8 + Copywriting Contract + Accessibility Contract
    - .planning/phases/12-richer-format-v3-v4-schema-bump/12-PATTERNS.md (RecipeStepEditor + RecipeEditor sections; FLAG 2 PopulateFromParsed; FLAG 3 SaveRecipe)
    - 12-CONTEXT.md D-12-09 (provenance never AI-prefilled), D-12-11
  </read_first>
  <action>
    In `RecipeStepEditor.razor` (UI-SPEC §7): after the `StepTemperaturePicker` block (after line 134), add a content-step-only (`@if (_kind == StepKind.Step)`) doneness cue control — a `<div style="margin-top:2px;">` wrapping an inline-flex `<span>` with an uppercase "Doneness" label (`font-size:11px;text-transform:uppercase;letter-spacing:0.06em;font-weight:600;color:var(--ink-3)`) and a `<input type="text" class="cb-input" value="@Step.DonenessCue" placeholder="e.g. golden brown, 165°F internal temp" aria-label="Doneness cue">` bound via `@oninput="@(e => OnDonenessCueInput(e))"`. Add the handler `private Task OnDonenessCueInput(ChangeEventArgs e) { Step.DonenessCue = e.Value as string; return Task.CompletedTask; }` mirroring `OnTemperatureChanged` (mutate-in-place).

    In `RecipeEditor.razor`:
    - Equipment card (UI-SPEC §6): copy the Tags card (lines 270-300) into a new `<CbCard Padding="18">` after it, heading "Equipment", empty state "No equipment yet.", placeholder "add equipment…", chips using `cb-chip tag`. Add state `_equipment: List<string>`, `_equipmentInput: string` and handlers `AddEquipment`, `RemoveEquipment`, `OnEquipmentInputInput`, `OnEquipmentInputKeyDown` (Enter/comma commits; case-insensitive dedup) mirroring the tag handlers.
    - Provenance card (UI-SPEC §8): new `<CbCard Padding="18">` after the Equipment card, heading "Source &amp; Credit", three `cb-label` + `cb-input` rows: "Adapted from" (`_provSourceName`, placeholder "e.g. Smitten Kitchen"), "Author" (`_provAuthorName`, placeholder "e.g. Deb Perelman"), "Source URL" (`_provSourceUrl`, `type="url"`, placeholder "https://…"). On-blur, set `_provUrlWarning` (string?) to "URL must start with http:// or https://" when the value is non-empty and fails an inline `Uri.TryCreate`+scheme check; render it in a `role="alert"` `color:var(--warn)` div. Non-blocking (D-12-15 — user can still save).
    - Substitution sub-rows (UI-SPEC §5): inside the ingredient loop (after each ingredient row, ~line 161), render existing `ing.Substitutions` as indented `--cream-2` rows each with a `cb-input` for `Note` (`aria-label="Substitution {n} for {ingredient}"`) and a trash button, plus an "Add substitution" ghost button. Add handlers `AddSubstitution(int ingIndex)`, `RemoveSubstitution(int ingIndex, int subIndex)`, `OnSubstitutionNoteInput(int ingIndex, int subIndex, ChangeEventArgs e)`. New substitutions are created with `new IngredientSubstitution { Note = "" }` (Note is required) and mutated in place.
    - SaveRecipe wiring (FLAG 3, lines 786-805): add to the `ParsedRecipe` initializer `Equipment = _equipment.ToList(),` and `Provenance = (string.IsNullOrWhiteSpace(_provSourceName) && string.IsNullOrWhiteSpace(_provAuthorName) && string.IsNullOrWhiteSpace(_provSourceUrl)) ? null : new RecipeProvenance { SourceName = NullIfEmpty(_provSourceName), AuthorName = NullIfEmpty(_provAuthorName), SourceUrl = NullIfEmpty(_provSourceUrl) },`. Substitutions already ride on `_ingredients` (ParsedIngredient.Substitutions). Add a small `NullIfEmpty` helper if not present.
    - PopulateFromParsed wiring (FLAG 2, line 488): populate `_equipment = parsed.Equipment.ToList();` and `_provSourceName/_provAuthorName/_provSourceUrl` from `parsed.Provenance` (empty string when null); substitutions arrive on `parsed.Ingredients[i].Substitutions` already carried into `_ingredients`.
  </action>
  <verify>
    <automated>dotnet build src/CookBot.Web/CookBot.Web.csproj 2>&1 | grep -qE "Build succeeded|0 Error" && echo BUILD_OK</automated>
  </verify>
  <acceptance_criteria>
    - `RecipeStepEditor.razor` contains a doneness `cb-input` bound to `Step.DonenessCue` with `aria-label="Doneness cue"` and an `OnDonenessCueInput` handler, gated to content steps only.
    - `RecipeEditor.razor` contains an Equipment card (heading "Equipment", empty state "No equipment yet.", `cb-chip tag`) and a "Source &amp; Credit" card with the three labeled inputs and the on-blur `_provUrlWarning` (`role="alert"`).
    - `RecipeEditor.razor` ingredient loop contains substitution sub-rows with a `cb-input` Note field, an "Add substitution" button, and `AddSubstitution`/`RemoveSubstitution`/`OnSubstitutionNoteInput` handlers.
    - `SaveRecipe` ParsedRecipe initializer contains `Equipment = _equipment` and builds `RecipeProvenance` (null when all three fields blank).
    - `PopulateFromParsed` (line ~488) reads `parsed.Equipment` and `parsed.Provenance` into the editor state fields.
    - No MudBlazor component is introduced (`grep -c 'Mud' src/CookBot.Web/Components/Pages/RecipeEditor.razor` does not increase).
    - `dotnet build src/CookBot.Web` succeeds with 0 errors.
  </acceptance_criteria>
  <done>A user can author equipment, provenance, per-step doneness, and per-ingredient substitutions; save and re-open the editor and see them persisted (SC5 authoring + SC2 UI round-trip).</done>
</task>

<task type="auto">
  <name>Task 2: RecipeView display of equipment checklist, substitution sub-lines, doneness cue, and validated provenance credit</name>
  <files>src/CookBot.Web/Components/Pages/RecipeView.razor</files>
  <read_first>
    - src/CookBot.Web/Components/Pages/RecipeView.razor (hero/description ~54-67; ingredient loop ~149-157; Tags block ~159-167; ContentStep temperature block ~205-214)
    - src/CookBot.Application/Services/RecipePhotoUrlValidator.cs (TryValidate signature for the SourceUrl gate)
    - .planning/phases/12-richer-format-v3-v4-schema-bump/12-UI-SPEC.md §1, §2, §3, §4 + Copywriting + Accessibility Contracts
    - .planning/phases/12-richer-format-v3-v4-schema-bump/12-PATTERNS.md (RecipeView section — four insertion points; FormatSubAmount; BuildProvenanceCredit; _validatedSourceUrl)
    - 12-CONTEXT.md D-12-04 (substitution amounts do not scale), D-12-08 (link safety)
  </read_first>
  <action>
    In `RecipeView.razor`, inject `RecipePhotoUrlValidator` (already a DI Singleton). Add four display surfaces per UI-SPEC §1-4:

    §4 Provenance credit — after the description `<p>` block (~line 67), render when `_doc.Provenance is { } prov && (prov.AuthorName != null || prov.SourceName != null)`. Compose the credit via a `BuildProvenanceCredit(prov)` helper: both → "Adapted from {SourceName} by {AuthorName}"; SourceName only → "Adapted from {SourceName}"; AuthorName only → "By {AuthorName}". Render as an `<a href="@_validatedSourceUrl" target="_blank" rel="noopener noreferrer">` (italic, `--ink-3`, underlined) when `_validatedSourceUrl != null`, else a plain italic `<p>`. Compute `_validatedSourceUrl` (string?) in `OnParametersSet` by calling `RecipePhotoUrlValidator.TryValidate` on `prov.SourceUrl` and storing the normalized URL only when it passes the http/https allowlist (D-12-08) — never render a live link for a failing/`javascript:`/`data:` URL.

    §2 Substitution sub-lines — inside the `@foreach (var ing in _doc.Ingredients)` loop, after each ingredient row (~line 157), render when `ing.Substitutions.Count > 0`: an indented block, one sub-line per substitution with an "or" prefix (`--ink-4`, 11px) and the text `@(sub.Name != null ? $"{FormatSubAmount(sub)} {sub.Name}" : sub.Note)` (append " — {Note}" when both Name and Note present). Add `FormatSubAmount(sub)` helper: `"{amount} {unit}"` when both present, `"{amount}"` when only amount, else "". Substitution amounts are STATIC — never multiply by the servings scale factor (D-12-04).

    §1 Equipment checklist — after the Tags block (~line 167), render when `_doc.Equipment.Count > 0`: a `<CbEyebrow>` "Equipment" heading and a `<ul role="list" aria-label="Equipment list">` of `<label class="cb-checkbox">` rows with an ephemeral checkbox. Add `_checkedEquipment: HashSet<string>` and `ToggleEquipment(string item)`; checked items get `text-decoration:line-through;color:var(--ink-4)`. State is ephemeral — never persisted.

    §3 Doneness cue — inside the `else if (step is ContentStep content)` branch, after the temperature `@if` block (~line 214), render when `!string.IsNullOrWhiteSpace(content.DonenessCue)`: an inline-flex italic `--ink-3` div with `<Icon Name="@Icon.Names.Check" Size="13" />` and `@content.DonenessCue`.
  </action>
  <verify>
    <automated>dotnet build src/CookBot.Web/CookBot.Web.csproj 2>&1 | grep -qE "Build succeeded|0 Error" && echo BUILD_OK</automated>
  </verify>
  <acceptance_criteria>
    - `RecipeView.razor` injects `RecipePhotoUrlValidator` and computes `_validatedSourceUrl` in `OnParametersSet`; the provenance `<a>` renders only when `_validatedSourceUrl != null`, otherwise a plain `<p>` (D-12-08).
    - `RecipeView.razor` contains a `BuildProvenanceCredit` helper producing the three copy forms from UI-SPEC §4.
    - `RecipeView.razor` contains substitution sub-lines with an "or" prefix and a `FormatSubAmount` helper; substitution amounts are NOT multiplied by the servings scale factor (`grep` shows no scale multiplier applied to `sub.Amount`).
    - `RecipeView.razor` contains an Equipment `<ul role="list" aria-label="Equipment list">` checklist with `_checkedEquipment` ephemeral state and `ToggleEquipment`.
    - `RecipeView.razor` renders `content.DonenessCue` with `Icon.Names.Check` only when non-empty.
    - No MudBlazor component introduced.
    - `dotnet build src/CookBot.Web` succeeds with 0 errors.
  </acceptance_criteria>
  <done>RecipeView shows all four field groups per UI-SPEC; the provenance link is defanged through the existing allowlist and substitution amounts never scale (SC5 display).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify the eight v4 UI surfaces end-to-end</name>
  <action>Pause for human verification. Claude has already automated all UI work (Tasks 1-2); this checkpoint only confirms the eight v4 surfaces render, the four groups round-trip through save/reload, and the provenance SourceUrl allowlist defang works. Follow the steps in <how-to-verify> and wait for the resume signal.</action>
  <what-built>
    RecipeEditor now authors equipment (chip card), provenance (Source &amp; Credit card), per-ingredient substitution sub-rows, and per-step doneness cues. RecipeView now displays an equipment checklist, substitution sub-lines under ingredients, per-step doneness cues, and a provenance credit (linked only when the SourceUrl passes the http/https allowlist). All four groups round-trip through save/reload.
  </what-built>
  <how-to-verify>
    1. Start the app: `./run.sh` (binds http://localhost:7000).
    2. Create or edit a recipe. In the editor right rail, add two equipment items, fill the Source &amp; Credit card (SourceName, Author, and a valid https Source URL), and add at least one ingredient substitution note. On at least one step, type a doneness cue.
    3. Save, then re-open the same recipe in the editor — confirm equipment, provenance fields, the substitution note, and the doneness cue are all still populated (SC2 round-trip).
    4. Open the recipe in RecipeView (`/recipes/{id}`): confirm the equipment checklist (check an item — it should strike through, ephemeral), the substitution sub-line under its ingredient (amount shown static, does NOT change when you change servings), the doneness cue under the step, and the provenance credit rendered as a clickable link.
    5. Edit the recipe and set the Source URL to `javascript:alert(1)`, save, reopen RecipeView — confirm the credit renders as PLAIN TEXT (no link), proving the allowlist defang (D-12-08).
    6. Confirm visuals match the approved 12-UI-SPEC (custom Cb atoms, no MudBlazor, correct copy: "Equipment", "Source & Credit", "Adapted from", "Add substitution", "Doneness").
  </how-to-verify>
  <resume-signal>Type "approved" or describe any surface that does not match the UI-SPEC / round-trip / link-safety expectations.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored recipe → rendered HTML | Provenance SourceUrl (possibly attacker-authored or imported) is rendered as an anchor href in RecipeView |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-07 | Tampering (XSS) | `Provenance.SourceUrl` rendered as `<a href>` with `javascript:`/`data:` scheme | mitigate | `RecipePhotoUrlValidator.TryValidate` gate in OnParametersSet; render plain text when the allowlist fails (D-12-08). Markdig DisableHtml lockdown remains for step text. Verified explicitly in the human-verify checkpoint step 5. |
| T-12-08 | Information disclosure | External provenance link leaking referrer / opener | mitigate | `target="_blank" rel="noopener noreferrer"` on the provenance anchor. |

UI display + authoring only; no new server endpoint. Equipment checkbox state is ephemeral and never persisted, so it carries no integrity surface.
</threat_model>

<verification>
- `dotnet build src/CookBot.Web` succeeds (0 errors).
- Human-verify checkpoint confirms all eight UI surfaces, the end-to-end round-trip, and the SourceUrl allowlist defang.
- No MudBlazor introduced; custom Cb atoms only.
</verification>

<success_criteria>
- SC5: equipment checklist, substitution chips/sub-lines, per-step doneness cue, and provenance link/author credit are displayed in RecipeView and authored in RecipeEditor.
- SC2 (UI completion): a recipe authored with all four groups round-trips through save/reload without corruption.
- FORMAT-01..04 authoring + display surfaces shipped per the approved UI-SPEC.
</success_criteria>

<output>
Create `.planning/phases/12-richer-format-v3-v4-schema-bump/12-04-SUMMARY.md` when done.
</output>
