---
milestone: v1.2
milestone_name: UI Redesign
audited: 2026-05-01
status: tech_debt
scores:
  requirements: 75/75
  phases: 3/3
  integration: 7/7
  flows: 7/7
nyquist:
  enabled: false
  note: workflow.nyquist_validation = false in config; section skipped
verification_artifact_note: |
  v1.2 phases 5/6/7 ship *-PHASE-SUMMARY.md instead of *-VERIFICATION.md.
  These contain the same content (success-criteria pass/fail tables,
  satisfied-REQ lists, decision logs, evidence). Audit treats them as the
  verification source.
gaps:
  requirements: []  # zero unsatisfied requirements; FAIL gate not triggered
  integration: []   # all cross-phase wiring verified end-to-end
  flows: []         # all 7 E2E flows pass
warnings:
  - id: WARN-MIG-02-COMMENT-RESIDUE
    severity: low
    affects: [MIG-02, MIG-03]
    location: src/CookBot.Web/wwwroot/js/recipe-chip-composer.js:2,6,12
    finding: |
      3 stale `MudAutocomplete` / `MudTextField` references in JSDoc/comment
      text. Phase 7 D-53 ("Mud documentation comments stripped per Hard
      Invariant #1") and the milestone summary's stated criterion
      (`grep -rn "Mud[A-Z]" src/ tests/` returns ZERO hits) are both untrue
      — actual hit count is 3. No runtime impact (executable JS is Mud-free
      and the production fallback path renders <CbTextarea>, not MudTextField).
    fix: 3 comment edits, ~5 minutes
  - id: WARN-REQ-CHECKBOX-DRIFT
    severity: low
    affects: [DS-01, DS-02, DS-03, DS-04, DS-05, DS-06, ATOM-01, ATOM-02, ATOM-03, ATOM-04, ATOM-05, ATOM-06, ATOM-07, ATOM-08]
    location: .planning/REQUIREMENTS.md
    finding: |
      14 satisfied requirements still show `- [ ]` (unchecked) in REQUIREMENTS.md.
      All 14 are listed as satisfied in 05-PHASE-SUMMARY.md frontmatter
      (requirements_satisfied list) and in v1.2-MILESTONE-SUMMARY.md
      requirements coverage table. Per workflow status matrix:
      "passed + listed + [ ]" → satisfied (update checkbox).
    fix: bulk find-and-replace `[ ]` → `[x]` on 14 lines
  - id: WARN-RV-04-STALE-COMMENT
    severity: trivial
    affects: [RV-04]
    location: src/CookBot.Web/Components/Pages/RecipeView.razor:27
    finding: |
      Razor comment claims "Notes from your last cook never renders in v1.2 —
      no RecipeMade." Slice 09 wired RecipeMadeService.GetLastCookAsync into
      _lastCookNote; the callout now lights up correctly when a cook is logged.
      Code is correct; comment is stale.
    fix: 1 comment edit
  - id: WARN-AICHAT-RAW-EDIT-EDGE
    severity: trivial
    affects: [AIC-04]
    location: src/CookBot.Web/Components/Pages/AiChat.razor:725-744
    finding: |
      "Edit anyway" path (validation-failed recipe) routes RawResponse
      (unvalidated LLM output) through IRecipeFormatParser.TryParse. If the
      raw JSON fails parse, falls through to "Could not parse the draft" toast
      with no navigation. Degraded but non-crashing — known fragile edge,
      not new tech debt, just worth flagging for a future hardening pass.
    fix: defer; add to v1.3 backlog if pursued
  - id: WARN-BAK-FILE-UNTRACKED
    severity: trivial
    affects: []  # repo hygiene only
    location: src/CookBot.Web/cookbot.db.pre-RecipeCanonicalDocument.bak
    finding: |
      176KB SQLite backup from v1.1 Phase 1 canonical migration sits untracked.
      .gitignore covers *.db but not *.bak. Could accidentally be committed.
    fix: add `*.bak` to .gitignore (or delete the backup if no longer needed)
tech_debt:
  - phase: 06-marquee-surfaces
    items:
      - "DEFERRED-PROF-AIPROMPT (Plan 07-04 D-38) — AiChat assistant-instructions panel removed per design-handoff; UserProfile.AiSystemPromptTemplate is still loaded but has no editor UI. Profile is the natural home."
      - "FUTURE-13 — Smart pantry-match algorithm for HOME-02 (expiration-aware, %-pantry-used, dietary-filtered). v1.2 ships deterministic stub."
      - "D-25 (Plan 06-04) — RecipeEditor description input wired in markup but not persisted; Recipe entity has no Description column. One-line wiring once schema gains the field."
      - "D-26 (Plan 06-04) — Cookbook switcher on RecipeEditor right rail is visual-only on edits (RecipeService.UpdateAsync does not reparent recipes by CookbookId)."
      - "D-16 (Plan 06-03) — TopBar exposes RightSlot but MainLayout has no per-page passthrough; RV-05 + future right-action surfaces render inline above hero. Tracked as future SHELL-03 polish."
      - "D-15 (Plan 05-05) — Dark-mode toggle icon stays as Sun for both light/dark states (36-icon set has no moon)."
  - phase: 07-remaining-surfaces
    items:
      - "FUTURE-14 — User-facing accent variant picker (terracotta/sage). DS-02 wires the tokens; no surface UI."
      - "D-37 (Plan 07-02) — PantryView per-row 'Add to grocery' cart icon is a disabled affordance; quick-add lives in 07-03 but the row-level button is not yet wired through."
      - "FUTURE-EDITOR-AT-TRIGGER (Plan 06-04 ED-03 carve-out) — inline @-keystroke autocomplete deferred. Explicit '+ ingredient' picker button covers the action with full keyboard nav."
  - phase: cross-cutting
    items:
      - "Live JS tick on Home active-timer band (slice 09) — current implementation reads localStorage snapshot; remaining-seconds doesn't tick down without page reload. Punch-list item explicitly noted in 07-09-FEATURES-SUMMARY."
---

# v1.2 Milestone Audit — UI Redesign

**Milestone:** v1.2 (UI Redesign)
**Audited:** 2026-05-01
**Status:** ⚡ `tech_debt` — all requirements met, all flows pass, accumulated deferred items + minor warnings need review before close

---

## Verdict

All 75 requirements are functionally satisfied. All 7 cross-phase E2E flows pass. The MudBlazor strip is complete in the dependency graph (csproj, _Imports, Program.cs, App.razor, MainLayout) and in executable code (zero `Mud[A-Z]` hits in C#/Razor). Build is clean, 196/196 tests pass.

The audit is downgraded from `passed` to `tech_debt` because:

1. **9 pre-acknowledged deferrals** logged across Phase 6 + 7 decision tables (DEFERRED-PROF-AIPROMPT, FUTURE-13/14, FUTURE-EDITOR-AT-TRIGGER, D-25/D-26/D-37/D-16/D-15, plus the live-tick punch-list from slice 09). None block v1.2's stated goals; they are explicit decisions to ship without those slices.
2. **5 minor warnings**, listed in the gap table above. Three are documentation drift (stale Mud comments in JS, stale RV-04 comment, untracked .bak file), one is a checkbox-sync gap, one is a known fragile edge. Together they fall under "minor tech debt to address before close" rather than blockers.

---

## Scores

| Dimension | Score | Notes |
|-----------|-------|-------|
| Requirements | **75 / 75** | All satisfied per phase verification + summary frontmatter |
| Phases | **3 / 3** | Phases 5, 6, 7 all complete; PHASE-SUMMARY.md present for each |
| Integration | **7 / 7** | All cross-phase wiring contracts verified |
| E2E Flows | **7 / 7** | All flows traced end-to-end through code |
| Nyquist | n/a | `workflow.nyquist_validation = false`; section skipped |

---

## Phase Status

| Phase | Plans | Status | Verification | Completed |
|-------|-------|--------|--------------|-----------|
| 5. Foundation — Design tokens, atoms, shell, dialogs | 5/5 | ✅ Complete | 05-PHASE-SUMMARY.md | 2026-04-27 |
| 6. Marquee surfaces — Home, CookingMode, RecipeView, RecipeEditor | 4/4 | ✅ Complete | 06-PHASE-SUMMARY.md | 2026-04-27 |
| 7. Remaining surfaces, accessibility, MudBlazor strip | 7/7 + 2 post-milestone slices | ✅ Complete | 07-PHASE-SUMMARY.md (+ 07-08-SMOKE-FIXES, 07-09-FEATURES) | 2026-04-27 |

**Naming-convention note.** v1.2 phases ship `*-PHASE-SUMMARY.md` files instead of the workflow's expected `*-VERIFICATION.md`. Functionally equivalent: each PHASE-SUMMARY contains the success-criteria pass/fail table, satisfied-REQ list, decision log, and evidence pointers. The audit treats PHASE-SUMMARY as the verification source. (v1.1 phases 1/2/3 do ship `VERIFICATION.md` — convention drifted at v1.2 start.)

---

## Requirements Coverage (3-Source Cross-Reference)

**Sources cross-referenced:**
- **A.** REQUIREMENTS.md per-requirement traceability table + checkbox state
- **B.** Phase verification (PHASE-SUMMARY.md) success-criteria + satisfied-REQ tables
- **C.** SUMMARY.md frontmatter `requirements_satisfied` / `requirements_complete` lists

| Phase | REQ count | Source A `[x]` | Source A `[ ]` | Source B satisfied | Source C frontmatter | Final Status |
|-------|-----------|----------------|----------------|--------------------|--------------------|--------------|
| 5 | 24 | 10 | 14 | 24 / 24 | 24 / 24 | All satisfied — 14 checkboxes need `[x]` flip |
| 6 | 24 | 24 | 0 | 24 / 24 | 24 / 24 | All satisfied |
| 7 | 27 | 27 | 0 | 27 / 27 | 27 / 27 | All satisfied |
| **Total** | **75** | **61** | **14** | **75 / 75** | **75 / 75** | **75 satisfied; 14 checkbox-drift** |

**Per-matrix interpretation** (`passed + listed + [ ]` → satisfied, update checkbox): the 14 unchecked DS-01..06 + ATOM-01..08 lines need their checkboxes flipped during `/gsd-complete-milestone`. Treated as a low-severity warning, not a gap.

**Orphan detection:** zero. Every REQ-ID in the milestone traceability table (75/75) appears in at least one PHASE-SUMMARY's satisfied list.

**FAIL gate:** not triggered. Zero `unsatisfied` requirements.

---

## Cross-Phase Integration

Spawned `gsd-integration-checker` over phases 5/6/7 + slice 09. Checker walked production code (not just summaries) and verified every cross-phase wiring contract.

**Wired (no issues):**
- Phase 5 atoms (CbButton/Chip/Card/Stat/Eyebrow/Badge/StripedPlaceholder/Toggle/Checkbox/Radio/Input/Textarea/Select/Dropdown/Dialog/Toast) consumed across all 9 surfaces in Phase 6/7
- `ICbDialogService` + `ICbToastService` registered in Program.cs (Scoped + Singleton); `CbDialogHost` + `CbToastHost` mounted in MainLayout; all 14+ dialog call sites use `CbDialogService.ShowAsync<T>`
- AI-off contract (`UserProfile.AiEnabled` AND `AiFeaturesEnabled`) gated identically in Sidebar (Phase 5), Home (Phase 6), Recipe Editor (Phase 6), Pantry (Phase 7), AiChat + PromptBuilder routes (Phase 7)
- Canonical `RecipeDocument` (v1.1 Phase 1) consumed directly by Recipe View / Cooking Mode / Recipe Editor / AI Chat canvas — zero legacy column projection
- POLISH-01 invariant preserved: AiChat canvas binds to `_lastStructuredRecipe.Value` (RecipeDocument from `IAiRecipeGenerator`); no three-tier `ExtractRecipeContent` revival

**Partial (pre-acknowledged carve-out):**
- SHELL-03 — TopBar exposes `RightSlot` but MainLayout has no per-page passthrough; RV-05 actions render inline (D-16 PRAGMATIC fallback)

---

## E2E Flows

| # | Flow | Verdict | Evidence |
|---|------|---------|----------|
| 1 | Recipe authoring (cookbook → editor → save → view → cook) | ✅ Passed | RecipeEditor.razor → RecipeService canonical-write → RecipeView.razor reads `CanonicalDocumentJson` only → CookingMode.razor uses `IngredientLinkPatterns.Pattern.Matches` for [name](#id) link resolution |
| 2 | AI generation (prompt → IAiRecipeGenerator → canvas → save) | ✅ Passed | AiChat.razor:312 binds to `_lastStructuredRecipe.Value`; SaveRecipeDialog routes through `RecipeFormatParser.TryParse` (JSON branch) → `RecipeService.CreateAsync`. POLISH-01 invariant intact. |
| 3 | AI-off contract across 5 surfaces | ✅ Passed | `_aiEnabled` / `_aiOff` derived from same two flags in Sidebar / Home / RecipeEditor / PantryView / AiChat / PromptBuilder. Each surface gates correctly. |
| 4 | Dialog system (CbDialog focus trap + ESC + scrim close) | ✅ Passed | CbDialogService Scoped in Program.cs:17; CbDialogHost mounted in MainLayout.razor:21; DynamicComponent + CascadingValue<CbDialogInstance> wiring verified |
| 5 | Toast system (CbToastHost severity tints) | ✅ Passed | CbToastService Singleton in Program.cs:18; CookbookDownloadHelper migrated from ISnackbar (D-28); zero ISnackbar refs in src/ |
| 6 | Cooking session resilience (timer survives page nav) | ✅ Passed (static) | cooking-session-state.js loaded in App.razor:21; CookingMode StartTimer/StopTimer/Finish lifecycle wires localStorage; Home reads on first render and renders Resume band. Live JS tick deferred per slice 09 punch-list. |
| 7 | MudBlazor dependency graph clean | ✅ Passed | csproj/Program/_Imports/App.razor/MainLayout all clean; `dotnet build` 0/0; `dotnet test` 196/196; DesignSandbox + SampleDialogContent deleted. (3 JS comment hits flagged separately as WARN-MIG-02-COMMENT-RESIDUE.) |

---

## Tech Debt by Phase

### Phase 6 (Marquee surfaces)

- **DEFERRED-PROF-AIPROMPT** (Plan 07-04 D-38) — AiChat assistant-instructions panel removed per design handoff. `UserProfile.AiSystemPromptTemplate` still loaded by `BuildSystemPrompt` → `IAiService.StreamMessageAsync` but has no editor UI. Profile is the natural home.
- **FUTURE-13** — Smart pantry-match algorithm for HOME-02 hero (expiration-aware, %-pantry-used, dietary-filtered). v1.2 ships deterministic stub with extension point in place.
- **D-25 (Plan 06-04)** — RecipeEditor description input wired in markup (38px title + 15px description per design handoff) but Recipe entity has no Description column. One-line wiring once schema gains the field.
- **D-26 (Plan 06-04)** — Cookbook switcher on RecipeEditor right rail is visual-only on edits — `RecipeService.UpdateAsync` does not reparent recipes by CookbookId. Selector renders for parity with the design handoff; reparenting is FUTURE.
- **D-16 (Plan 06-03)** — TopBar exposes a `RightSlot` parameter but MainLayout instantiates it without a per-page passthrough mechanism. Right-action surfaces (RV-05) render inline above hero as a PRAGMATIC fallback. Tracked as future SHELL-03 polish.
- **D-15 (Plan 05-05)** — Dark-mode toggle icon stays as Sun for both light/dark states (36-icon set has sun but no moon). Tooltip provides the directional cue.

### Phase 7 (Remaining surfaces)

- **FUTURE-14** — User-facing accent variant picker (terracotta/sage in addition to orange). DS-02 wires the tokens; no surface UI.
- **D-37 (Plan 07-02)** — PantryView per-row "Add to grocery" cart icon is a disabled affordance (`title="Add to grocery list"`). Grocery quick-add lives in Plan 07-03; per-row wiring not yet hooked through.
- **FUTURE-EDITOR-AT-TRIGGER** (Plan 06-04 ED-03 carve-out) — inline `@`-keystroke autocomplete trigger deferred. Explicit "+ ingredient" picker button covers the action with full keyboard nav (ArrowUp/Down + Enter + Escape).

### Cross-cutting

- **Live JS tick on Home active-timer band** (slice 09) — current implementation reads localStorage snapshot and renders remaining-seconds; the value doesn't tick without a page reload. Explicitly noted as a punch-list item in `07-09-FEATURES-SUMMARY.md`.

**Total:** 10 deferred items across 3 phase buckets. None are critical. All are explicit decisions logged at execution time.

---

## Warnings (close-out fixes)

| ID | Severity | REQ | Fix effort |
|----|----------|-----|------------|
| WARN-MIG-02-COMMENT-RESIDUE | low | MIG-02, MIG-03 | 3 comment edits in `recipe-chip-composer.js` (lines 2, 6, 12) |
| WARN-REQ-CHECKBOX-DRIFT | low | DS-01..06, ATOM-01..08 | bulk `[ ]` → `[x]` on 14 lines in `REQUIREMENTS.md` |
| WARN-RV-04-STALE-COMMENT | trivial | RV-04 | 1 comment edit in `RecipeView.razor:27` |
| WARN-AICHAT-RAW-EDIT-EDGE | trivial | AIC-04 | defer; add to v1.3 backlog if pursued |
| WARN-BAK-FILE-UNTRACKED | trivial | — | add `*.bak` to `.gitignore` (or delete the backup) |

The two "low" warnings make the milestone summary's literal claim (`Mud[A-Z]` returns ZERO hits) untrue and leave 14 satisfied-but-unchecked requirements in REQUIREMENTS.md. They're 5–10 minutes of cleanup; doing them before `/gsd-complete-milestone v1.2` makes the milestone shippable as `passed`.

---

## Recommendation

Either:

**A. Quick close-out (preferred)** — apply the 5 close-out fixes (~15 min total), then `/gsd-complete-milestone v1.2`. The audit upgrades to `passed` with the deferrals carried forward into the v1.3 seed list.

**B. Accept and ship** — the deferred items + warnings are pre-acknowledged; `/gsd-complete-milestone v1.2` proceeds as-is; tech debt rolls to v1.3 backlog. Minor inaccuracy in the milestone-summary verification claim survives.

The integration check found zero broken flows and zero unsatisfied requirements. The milestone is shippable in either path.

---

*Audited 2026-05-01 by `/gsd-audit-milestone v1.2`. Integration check by `gsd-integration-checker` (sonnet). All 7 E2E flows + 75 requirements verified against source code (not just summary docs).*
